# 미륵이 v9 TODO 리스트 (2026-07-04 기준)

기반: `mireuki_v9_구현계획_v2_2026-07-04.md`. v9-dev 브랜치 작업 로그 —
[[project_v9_dev_branch_split]] 규칙에 따라 dev_memory 4개 파일 대신 여기서 관리한다.
완료 시 `[x]`로 표시, 완료일 기록. dev_memory 관례와 동일하게 항목 삭제하지 않고 보존.

> **[2026-07-05 갱신]** origin/dev(MW0602) 290/291차가 아래 P0~P2의 감사 유래 항목
> 대부분을 구현·완료해 v9-dev로 머지했다(`git merge origin/dev`, 커밋 `333ff60`).
> 완료 확인된 항목에 `(dev 290/291차로 이관완료)` 표기를 추가한다 — 원본 항목은
> 삭제하지 않고 보존. 이후 v9의 스코프는 **Track L1(데이 레짐 엔진)·Track L3(소프트게이트
> 점수화)·Track L2(레짐조건부 프로파일, L1 선행조건)** 셋으로 좁혀진다.
> 상세: `mireuki_v9_구현계획_v3_2026-07-05.md`.

---

## P0 — 안전·평가축 (최우선)

- [x] **CB② 원칙 위반 확정** (2026-07-04) — `CB_CONSEC_STOP_LIMIT=9999`(`config/settings.py:581`)는
      **의도적 조치**로 확정: 모의투자 단계 거래데이터 최대 축적 목적. 폐기 아닌 유예 — 주석
      갱신 완료(v9-dev 커밋). **값은 그대로 유지, 실전 전환 시 2~3 복원 필수(아래 신규 항목).**
- [ ] **[신규] 실전 전환 전 CB② 복원 확인** — `CB_CONSEC_STOP_LIMIT`을 2~3으로 되돌리고
      최소 1회 정상 발동 확인. v9 실전 전환 게이트의 필수 조건으로 등록
      (`mireuki_v9_최종설계안_2026-07-03.md` §6 검증기준에 추가 반영 필요). ROADMAP.md Phase 5
      체크리스트에도 동일 조건 등록 확인(dev 291차).
- [x] 거래당 순EV 일일 리포트 신설 (dev 290차로 이관완료, 2026-07-05) — `strategy/ops/daily_exporter.py`
      + 대시보드 "최근20건 순EV" 타일 구현 완료.

## P1 — 알파·집행·청산 (W2~5 병행)

- [x] 트리플배리어 라벨 오프라인 비교 — `triple_barrier_label_report.md`(2026-07-03) — 기존 라벨
      FLAT 62~68% vs 신규 0.04~0.6% 확인.
- [x] 트리플배리어 배리어를 실제 청산 규칙과 정합 + 섀도우 병행학습 (dev 290차로 이관완료,
      2026-07-05) — `model/target_builder.py`, `scripts/run_shadow_triple_barrier_retrain.py`.
      **승격 판정은 아직 미정**(표본 축적 중) — Track L3 승격 판단과 별개로 계속 진행.
- [x] Meta-Labeling 게이트 섀도우 연결 (dev 290차로 이관완료, 2026-07-05) — `learning/meta_label_classifier.py`
      (meta labels 65,183건 학습) + `strategy/entry/meta_gate.py`에 섀도우 필드로 연결.
      **아직 실거래 action/size_multiplier에는 관여하지 않음(로깅 전용)** — 실제 사이징 반영은
      Track L3(아래 §Track L3)에서 일반화해 처리.
- [x] qty_ok/mode_filter_ok 근본원인 규명 — `qty_ok_mode_filter_근본원인_2026-07-04.md` —
      체크리스트 9항목+Degraded/ExecutionGovernor/MetaGate/ToxicityGate 4종 오버라이드가
      원인, entry_mode 토글 아님(정정 완료). **[2026-07-05 재확인] 이 병목의 실제 해법은
      별도 트랙이 아니라 아래 Track L3(소프트게이트 점수화)와 동일하다 — 트랙 통합.**
- [x] MAE/MFE 분석 스크립트 신설 (dev 290차로 이관완료, 2026-07-05) — `scripts/analyze_mae_mfe.py`.
- [x] 신호 소멸 청산(P4.5) (dev 290/291차로 이관완료, 2026-07-05) — `main.py` 구현, 기본 ON
      (모의투자 한정), counterfactual 롤백기준도 291차에서 등록.
- [x] 지정가 우선 집행 (dev 290차로 이관완료, 2026-07-05) — Cybos CpTd6831/6833 TR 구현,
      기본 OFF(실브로커 수동검증 대기).
- [x] 게이트/레이어 ablation 주간 리포트 (dev 290차로 이관완료, 2026-07-05) —
      `scripts/generate_gate_ablation_report.py`. **qty_ok는 `_SAFETY_OPERATIONAL_GATES`로
      분류되어 분석 제외 대상** — 즉 ablation이 qty_ok 자체를 없애는 방향이 아님을 확인,
      Track L3(직렬 AND 구조 재설계)가 여전히 필요한 이유.
- [x] 4종 오버라이드(Degraded/ExecutionGovernor/MetaGate/ToxicityGate) 동작원리·임계값·
      JointGateBlock 규명 — `게이트4종_역할동작원리_2026-07-04.md`. L3 점수화 설계 입력값으로 사용.
- [x] 게이트 차단 30일 집계 — `gate_blocking_report.md`(2026-07-03).
- [x] 레짐 사후 라벨링 + 왜곡회피 — `regime_postmortem_report.md`(2026-07-04 보강, CB일 제외 로직).

## P2 — 모델·피처·챌린저 정밀화

- [x] HistGradientBoosting 전환 (dev 290차 확인, 2026-07-05) — 이미 적용 중이었음(추가작업 불필요).
- [x] 분위 회귀(Quantile Regression) 보조 채널 (dev 290차로 이관완료, 2026-07-05) —
      `learning/quantile_regressor.py` 섀도우 로깅.
- [x] 신규 피처 4종 (dev 290차로 이관완료, 2026-07-05, 전부 pending_validation):
  - [x] 선물-현물 베이시스 & 변화율 — `features/technical/basis.py`(KOSPI200 지수코드 K2G01P 정정)
  - [x] 프로그램매매 순매수 — CpSvr8111 필드매핑 오류 발견·수정
  - [x] 외국인 선물 실시간 누적 순매수 — 기존 매핑 정확함 확인
  - [x] VKOSPI 장중값 — O2901P
  - [x] 만기 구조 더미(위클리/월간 위칭데이·월간만기주·월말리밸런싱 4종) — `features/technical/expiry.py`
  - [ ] KTB 국채선물·달러선물 장중 수익률 — **미이관, v9 남은 스코프**(수집 경로만 확인 필요)
  - [ ] 야간 갭 피처(CME/ES 오버나이트) — **미이관, v9 남은 스코프**
  - [ ] 삼성전자+SK하이닉스 체결 불균형 — **미이관, v9 남은 스코프**
  - [ ] 점프 지표(bipower variation) — **미이관, v9 남은 스코프**
- [ ] cvd_direction 피처셋 정리 — dev 290/291차 diff에서 확인 안 됨, **미이관 가능성 — 다음
      세션에서 `featureset by horizon/horizon_feature_sets.json` 직접 확인 필요**.
- [ ] macro_vix 잔존 제거 검토 — 위와 동일, 미확인.
- [x] 챌린저 부트스트랩 승격 판정 + min_trades 상향 + 챔피언 heartbeat (dev 290차로 이관완료,
      2026-07-05) — `challenger/promotion_manager.py` 5,000회 리샘플 우위확률 + Wilson CI heartbeat.
      **CHAMPION_BASELINE 콜드스타트 제약(자체 shadow 이력 없음) 발견 — 미해소, 별도 설계 필요.**
- [x] TP1 부분청산 A/B (dev 290차로 이관완료, 2026-07-05) — `challenger/variants/champion_tp1_skip_trail.py`.
- [x] 레짐 조건부 배수 (dev 290차로 이관완료, 2026-07-05) — `HURST_REGIME_ATR_MULT`
      (추세×1.20/평균회귀×0.85, config/settings.py).
- [x] 트레일링 갱신 순서 확인 (dev 290차, 2026-07-05) — **감사 지목 대상(`exit_manager.py`)은
      이미 죽은 코드였고, 실제 로직(`main.py:_ts_check_exit_triggers()`)은 이미 정상 순서로
      갱신 중이었음** — 감사가 죽은 코드를 보고 오판한 사례. 코드 변경 없음.
- [ ] 사이징 배수체인 로깅+하한/상한 — **미이관, v9 남은 스코프로 유지**.

## P3 — 운영·정리

- [x] 대시보드 상단 고정 상태 스트립 (dev 290차로 이관완료, 2026-07-05) — `StatusStripPanel` 신규.
- [ ] 대시보드 탭 재편 15개→4그룹 — dev도 보류(사용자 직접 진행 예정), **미이관 상태 유지**.
- [x] 30m 호라이즌 퇴역 심사 (dev 290차, 2026-07-05) — **퇴역하지 않음, 대신 `batch_retrainer.py`의
      실제 버그(호라이즌별 학습피처명 결정 로직) 발견·수정**. 다음 EOD 재학습 후 재측정 예정.
- [x] 다중비교 보정 (dev 290차로 이관완료, 2026-07-05) — Bonferroni, `promotion_manager.py`.
- [ ] 백테스트-실거래-shadow 3자 대사 자동화 — **미이관, v9 남은 스코프로 유지**.
- [ ] 알파 리서치 봇(Phase 6) 장외 스케줄 연결 — 보류 유지(변경 없음).

---

## [2026-07-05 신규] Track L1 — 데이 레짐 엔진 (섀도우)

기존 레짐분류기 3개(`RegimeClassifier`/`MicroRegimeClassifier`/`IntradayTacticalRegime`)를
재구현하지 않고 통합하는 애그리게이터. 상세: `mireuki_v9_구현계획_v3_2026-07-05.md` §3.

- [x] `strategy/regime/day_regime_engine.py` `DayRegimeEngine` 구현 (2026-07-05) — 기존
      3개 출력 + Hurst + 시가이격 + opt_gex_bn/opt_chain_pcr + 첫30분 모멘텀 →
      TREND_UP/TREND_DN/RANGE/CHAOS + confidence + 히스테리시스(최소 3분 dwell, CHAOS는
      즉시 전환). 대표 입력값(TREND/RANGE/VOLATILE/CRASH) 스모크 테스트 통과.
- [x] main.py 섀도우 배선 (2026-07-05) — Layer2(IntradayTacticalRegime) 갱신 직후 매분 호출,
      `entry_gate_json.day_regime_shadow`/`day_regime_shadow_confidence`로 저장. 정책 분기
      없음(읽기 전용). `reset_daily()`도 배선 완료.
- [x] `scripts/generate_day_regime_backtest_report.py` (2026-07-05) — 실거래 섀도우 로그
      (entry_gate_json)를 읽어 레짐별 realized_move 집계 + TREND vs RANGE 비교. raw_candles
      오프라인 재현 대신 실거래 섀도우 로그를 쓰는 이유: 이 엔진은 Hurst/day_ret/opt_gex_bn 등
      raw_candles만으로 재구성 불가능한 입력을 쓰기 때문(문서 내 명시). **표본은 배포 이후부터
      쌓임 — 다음 세션에서 재실행해 L2 착수 여부 판단.**

## [2026-07-05 신규] Track L3 — 소프트게이트 점수화 (qty_ok/mode_filter_ok 병목의 실제 해법)

qty_ok/mode_filter_ok 근본원인(위 참고) = 9항목 체크리스트 + Degraded/ExecutionGovernor/
MetaGate/ToxicityGate 4종 오버라이드의 직렬 AND. 별도 트랙이 아니라 이 트랙 자체가 해법.

- [x] `strategy/entry/soft_gate_scorer.py` `SoftGateScorer` 구현 (2026-07-05) — 직렬 AND
      대신 가중 점수 합산(체크리스트 0.40 + Degraded/ExecutionGovernor/MetaGate/ToxicityGate
      각 0.15). `meta_gate.py`의 등급별 블렌딩 패턴 참고. `_final_grade`/`_qty_display`는
      건드리지 않음(읽기 전용 섀도우). A/C/X 대표 케이스 스모크 테스트 통과.
- [x] main.py 섀도우 배선 (2026-07-05) — 4종 오버라이드 확정 직후·L2 사이징 이후,
      `entry_gate_json.soft_gate_shadow_grade`/`_score`/`_size_mult`로 저장.
- [x] `scripts/generate_soft_gate_shadow_report.py` (2026-07-05) — "직렬AND가 막았지만
      소프트게이트라면 통과했을 신호"의 realized_move 집계 스크립트. 실행 확인(표본 0건,
      배포 전이라 정상 — entry_gate_json에 필드 없음을 정상 감지). **합격선은 아직
      미확정 — 표본 축적 후 별도 세션에서 사전등록**.

## [2026-07-05 신규] Track L2 — 레짐조건부 프로파일 (블록드: L1 검증 완료 후 착수)

- [ ] (블록드) 레짐별 앙상블 프로파일 분기 — TREND면 10·15m 주축+홀딩편향, RANGE면 1·3·5m
      축소사이즈, CHAOS면 전면 관망. Track L1 백테스트 리포트로 레짐 분류 유의성 확인 후 착수.

## 조사/스팟체크 완료 (참고용, 액션 없음)

- [x] day_range_pct 이상치 — 실제 거래소 CB 발동일(06-08·06-23·06-26)과 데이터 결측(06-08
      개장 44분 누락) 확인. `CB발동일_왜곡회피_2026-07-04.md`.
- [x] entry_gate_json 스냅샷이 2026-06-17부터만 존재 — 이전 리포트 "30일" 표현은 실제 ~17일.

---

⚠️ 본 문서는 시스템 설계 프로세스를 돕기 위한 것이며, 수익률을 보장하거나 투자 자문을 제공하지 않습니다.
