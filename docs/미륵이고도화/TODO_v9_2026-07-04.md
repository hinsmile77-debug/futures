# 미륵이 v9 TODO 리스트 (2026-07-04 기준)

기반: `mireuki_v9_구현계획_v2_2026-07-04.md`. v9-dev 브랜치 작업 로그 —
[[project_v9_dev_branch_split]] 규칙에 따라 dev_memory 4개 파일 대신 여기서 관리한다.
완료 시 `[x]`로 표시, 완료일 기록. dev_memory 관례와 동일하게 항목 삭제하지 않고 보존.

---

## P0 — 안전·평가축 (최우선)

- [ ] **CB② 원칙 위반 확정** — `CB_CONSEC_STOP_LIMIT=9999`(`config/settings.py:581`) 복원(2~3) 또는
      CLAUDE.md 원칙 개정. **사용자 결정 대기 — 코드 미변경**.
- [ ] 거래당 순EV 일일 리포트 신설 — 등급별·게이트별·호라이즌별 EV 테이블, EOD 자동생성 +
      대시보드 노출. 대상: `strategy/ops/daily_exporter.py`(확장), 대시보드.

## P1 — 알파·집행·청산 (W2~5 병행)

- [x] 트리플배리어 라벨 오프라인 비교 — `triple_barrier_label_report.md`(2026-07-03) — 기존 라벨
      FLAT 62~68% vs 신규 0.04~0.6% 확인.
- [ ] 트리플배리어 배리어를 실제 청산 규칙(`ATR_STOP_MULT=1.5`)과 정합 + `batch_retrainer.py`
      병행학습 경로 신설(섀도우). 2~4주 병행 후 예측-손익 상관으로 승격 판정.
- [ ] Meta-Labeling 게이트 활성화 — `learning/meta_labeling.py`+`strategy/entry/meta_gate.py`
      기존 인프라("meta labels 65,183건 준비완료") 연결. 체크리스트는 8_time·9_risk·entry_ok
      하드 veto만 존치.
- [x] qty_ok/mode_filter_ok 근본원인 규명 — `qty_ok_mode_filter_근본원인_2026-07-04.md` —
      체크리스트 9항목+Degraded/ExecutionGovernor/MetaGate/ToxicityGate 4종 오버라이드가
      원인, entry_mode 토글 아님(정정 완료).
- [ ] MAE/MFE 분석 스크립트 신설(`scripts/analyze_mae_mfe.py`) — trades.db 전 체결 분포로
      스톱은 패배 MAE 굴절점, TP는 승리 MFE 중앙값 근처로 재산정. 분기 1회 정기화.
- [ ] 신호 소멸 청산(P4.5) — 보유 중 앙상블 반대방향 고신뢰 전환 시 부분/전량 청산.
      대상: `exit_manager.py`.
- [ ] 지정가 우선 집행 — microprice 기준 유리한 쪽 1틱 지정가, N초(10~15s) 미체결 시 취소/시장가
      전환. 대상: `main.py` 주문부, `features/technical/microprice.py` 재활용.
- [ ] 게이트/레이어 ablation 주간 리포트 — 15겹 앙상블 보정 + 10겹 진입게이트 leave-one-out
      가상손익 집계, 무기여 레이어 퇴역. 대상: `strategy/shadow_evaluator.py`.
      **§13 L3 소프트게이트 착수 전 선행 진단으로 권장.**
- [x] 4종 오버라이드(Degraded/ExecutionGovernor/MetaGate/ToxicityGate) 동작원리·임계값·
      JointGateBlock 규명 — `게이트4종_역할동작원리_2026-07-04.md`. L3 점수화 설계 입력값으로 사용.
- [x] 게이트 차단 30일 집계 — `gate_blocking_report.md`(2026-07-03).
- [x] 레짐 사후 라벨링 + 왜곡회피 — `regime_postmortem_report.md`(2026-07-04 보강, CB일 제외 로직).

## P2 — 모델·피처·챌린저 정밀화

- [ ] HistGradientBoosting 전환 — `learning/batch_retrainer.py`. py310_64 학습→py37_32 로드
      pickle 호환 유지 확인.
- [ ] 분위 회귀(Quantile Regression) 보조 채널 — q10/q50/q90 직접 추정.
- [ ] 신규 피처 8종 (IC 사전검증 → SHAP 심사, 한 번에 2개 이하):
  - [ ] 선물-현물 베이시스 & 변화율 (1m~10m)
  - [ ] 프로그램매매 순매수(차익/비차익 분리) (5m~30m)
  - [ ] 외국인 선물 실시간 누적 순매수 (10m~30m)
  - [ ] VKOSPI 장중값 (10m~30m)
  - [ ] KTB 국채선물·달러선물 장중 수익률 (10m~30m)
  - [ ] 야간 갭 피처 (CME/ES 오버나이트) (시초 1시간)
  - [ ] 삼성전자+SK하이닉스 체결 불균형 (1m~5m)
  - [ ] 점프 지표(bipower variation) (1m~15m)
  - [ ] 만기 구조 더미 (전체)
- [ ] cvd_direction 피처셋 정리 — 1m/3m `include`에서 제거 또는 `cvd_delta_norm`으로 교체
      (`featureset by horizon/horizon_feature_sets.json`, 06-25 CORE 강등 결정과 불일치 확인됨).
- [ ] macro_vix 잔존 제거 검토 — 3m/5m/10m/15m/30m 피처셋 (CORE 강등 로직과 일관성).
- [ ] 챌린저 부트스트랩 승격 판정 — PnL 차이 5,000회 리샘플, "챌린저 우위 확률≥95%".
      대상: `promotion_manager.py`.
- [ ] min_trades 30→100(전역), 레짐트랙 50 — 표본오차 ±10%p 이내.
- [ ] 챔피언 heartbeat — 최근 60거래 롤링 승률 CI 하한 미달 시 자동 사이즈 50%+알림.
- [ ] TP1 부분청산 A/B — "TP1 스킵·트레일 단독" variant 챌린저 등록, 20일 병행.
- [ ] 레짐 조건부 배수 — 추세장 스톱 넓게/TP 멀게, 횡보장 반대.
- [ ] 트레일링 갱신 순서 수정 — `exit_manager.check_and_exit` P5 갱신을 P2 앞으로(경미, 무료수정).
- [ ] 사이징 배수체인 로깅+하한/상한 — 최종 수량 결정 직전 배수 체인 전체 로그 1줄,
      곱 0.2 미만 진입포기.

## P3 — 운영·정리

- [ ] 대시보드 상단 고정 상태 스트립 — 포지션/당일PnL/CB·헬스/모델방향·차단사유 1줄.
      "3초 파악 테스트" 통과 목표.
- [ ] 대시보드 탭 재편 15개→4그룹(운용/모델/학습/시스템).
- [ ] 30m 호라이즌 퇴역 심사 — need_add 피처 탑재 후 acc 회복 실패 시.
- [ ] 다중비교 보정 — 동시 챌린저 n종 시 승격 유의수준 α/n(Bonferroni).
- [ ] 백테스트-실거래-shadow 3자 대사 자동화 — 집행비용 실측.
- [ ] 알파 리서치 봇(Phase 6) 장외 스케줄 연결 — 챌린저 통계 기준 강화(P2) 선행 필요, 보류.

## 조사/스팟체크 완료 (참고용, 액션 없음)

- [x] day_range_pct 이상치 — 실제 거래소 CB 발동일(06-08·06-23·06-26)과 데이터 결측(06-08
      개장 44분 누락) 확인. `CB발동일_왜곡회피_2026-07-04.md`.
- [x] entry_gate_json 스냅샷이 2026-06-17부터만 존재 — 이전 리포트 "30일" 표현은 실제 ~17일.

---

⚠️ 본 문서는 시스템 설계 프로세스를 돕기 위한 것이며, 수익률을 보장하거나 투자 자문을 제공하지 않습니다.
