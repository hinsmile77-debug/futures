# 미륵이 일일 점검 — MW0602 / 2026-08-24 (월요일 · 거래일)

> 📄 **하루 한 파일이다.** 이 파일은 장전(pre) 세션이 만들었다.
> 장중은 `0i-C` 이월 처리표부터, 장후는 `0p-C` 이월 처리표부터 **같은 파일 끝에 이어 붙인다.**
> 앞 국면 절의 본문은 고치거나 지우지 않는다 — 정정은 뒤 국면 절에서 하고 앞 절에는 포인터 한 줄만 단다.

---

# 장전(pre)

- 점검 시각: **09:00 KST** (증거 수집기 발화 2026-08-24 09:00:36) · PC `MW0602` · 브랜치 `dev` ✅
- HEAD `d187218` · 미커밋 **5건**(전부 untracked) · 당일 커밋 **2건**(`cfd16bb` 489차 07:05 · `d187218` 490차 07:44)
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260824_pre.md` (62.0KB)
- 수집기 실행: **성공** (stderr 없음). 다이제스트 첫 줄 `MW0602 (override · host=claude)` 정상
- 세션 차수: **491차** (원격 git 기준 최신이 490차)

---

## 0. 오늘 한눈에

> 이 절에는 약어·로그 태그·파일 경로·원칙 번호·단계 번호를 쓰지 않는다.

**오늘 거래를 시작해도 되는 상태입니다. 당장 위험한 문제는 없습니다.**

프로그램은 아침 8시 40분에 정상적으로 켜졌고, 지난 금요일에 남겨둔 미결제 물량은 없습니다.
주가 예측에 쓰는 인공지능 모델은 금요일 밤에 다시 학습한 것이 제대로 올라왔고,
아침 시장 분위기 판단(해외 지수·환율·변동성)도 8시 58분에 마쳐서 "위험 선호"로 결론이 났습니다.
설정값 26가지를 하나씩 대조했는데 누가 몰래 바꾼 흔적은 없었습니다.

**다만 신경 쓸 것이 네 가지 있습니다.**

첫째, 프로그램이 판단에 쓰는 핵심 재료 세 가지 중 하나("매수·매도 주문의 힘 불균형")가
**개장 직전까지 계속 무력화된 상태**였습니다. 지난 금요일에는 8시 48분에 풀렸는데 오늘은
9시가 넘도록 안 풀렸고, 개장 2분 뒤에야 정상으로 돌아왔습니다. 그 사이에 나온 판단은
재료 하나가 빠진 채로 내린 판단입니다.

둘째, 예측 확률을 실제 확률에 맞춰 보정하는 장치의 저장 파일이 **8월 11일 이후 갱신되지
않고 있습니다.** 이미 지난 금요일에 발견해 고쳐 놓았고, 그 수정이 실제로 작동하는지는
**오늘 장 마감 뒤에** 확인됩니다.

셋째, 점검 도구 자체가 **이미 없어진 마감일("8월 29일")을 아직 세 군데에서 인쇄하고
있습니다.** 지난 주말 회의에서 그 마감일을 없애고 다른 조건으로 바꾸기로 결정했는데,
도구는 옛날 날짜를 계속 보여줍니다. 이대로 두면 다음 점검이 없는 마감일에 쫓겨
잘못된 일을 하게 됩니다.

넷째, 이 장전 점검이 **개장보다 36초 늦게** 돌았습니다. 개장 전 자료와 개장 후 자료가
조금 섞였습니다. 큰 문제는 아니지만, 개장 전에 도는지를 3일 연속으로 확인하려던 계획은
오늘 다시 처음부터 세게 됐습니다.

**지금 사람이 해야 할 일은 없습니다.** 조치가 필요한 것은 맨 끝 「사용자 조치」 절에
모아 두었고, 급한 것은 하나도 없습니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| **장전** | **조건부 정상** | P0 0건. 기동·모델·매크로·설정 불변식 전부 정상. 단 CORE 피처 1종이 개장 시점까지 축퇴 미해소(1-1), 보정기 스냅샷 정체 지속(1-2), 스킬 문서의 죽은 기한 3곳(1-3) |
| 장중 | — | (장중 세션이 채운다) |
| 장후 | — | (장후 세션이 채운다) |

**절대원칙 준수 여부 (장전 시점 판정)**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | **해당 없음(장전)** — 이월 포지션 0 확인 | `08:41:02 [BrokerSync] startup sync 무포지션 확인(blank rows): FLAT -> FLAT (armistice cleared)` · `[Position] 저장 상태가 어제 데이터 — 무시` |
| ② | Circuit Breaker(CB, 자동 정지 장치) 5종 | **정상** — 미발동(장전이라 조건 자체가 없음) | 다이제스트 §12 `CB_state` = `NORMAL`×2587(7거래일). ⚠ CB②는 `CB_CONSEC_STOP_LIMIT=9999`로 **의도적 비활성**이며 미발동이 정상 |
| ③ | CORE 피처 호라이즌 그룹별 분리 | 🟠 **조건부** — 단기 그룹 CORE `ofi_norm`이 장전 전 구간 identity 강제 | **1-1 참조.** `[CORE준비도] trigger=A_WARMUP 축퇴 1/6 — ofi_norm×6hz` ×4회(08:47:59·08:49:59·08:55:00·08:59:00) |
| ④ | COM 콜백 내 `dynamicCall`·`emit` 금지 | **정상** | `0xC0000409` / `STACK_BUFFER` / 프로세스 무응답 로그 **0건**. `[FaultHandler] 활성화 PID=7128 행감지=30s` 정상 · 09:02까지 틱 수신 연속 |
| ⑤ | `GetRepeatCnt`/`GetCommData` 파라미터 구분 | **정상(간접)** | TR 수신 오류 로그 0건. `[CybosDailyPnlHeaders]` 21필드 정상 파싱 |
| ⑥ | 알파 리서치 봇 자동 통합 금지 | **정상** | 자동 통합·백테스트 자동 큐 로그 **0건**. 당일 새벽 26주 WFA 산출물(`ic_decay_catalog_20260824.json` 08:03)은 **사람이 돌린 오프라인 분석**이며 자동 통합 경로가 아니다 |

**설정 불변식** — 수집기 §3 표 **26행 전부 `일치` 또는 의도된 `값 확인`**. `불일치`·`미발견` **0건**.
차단 게이트 전수 29개 중 7개가 `False`이며 **전부 기록됨 또는 기능 토글**이다(신규 무기록 게이트 0건).

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**해당 없음.** 오늘 장전에서 P0로 올릴 항목은 없다.

### P1 — 정확성·관측 훼손

#### 1-1. 단기 CORE 피처 `ofi_norm`이 개장 직전까지 6개 호라이즌 전부 무력화된 채였다 — 전일 대비 악화

> ↩️ **12:50 정정 — `1-10`(장중 절) 참조.** 「개장 2분 뒤 해소」는 09:01~09:17의 **일시적**
> 해소였고, 09:22부터 12:38까지 재발해 장중 전 구간 고착이었다.

- **증상**: 장전 스케일러 재적합(refit) 4회 전부에서 CORE(핵심 판단 재료) 피처 `ofi_norm`
  (Order Flow Imbalance — 매수·매도 주문 유입의 불균형)이 **6개 호라이즌 전부**에서
  표준편차 ≈ 0으로 판정돼 `identity(0,1)` 강제 처리됐다. 즉 그 피처의 z-score가 상수처럼
  눌려 **판단에 기여하지 못하는 상태**로 개장을 맞았다.
- **근거**:
  ```
  2026-08-24 08:45:02 [WARNING] SIGNAL: [CORE준비도] trigger=A_WARMUP 축퇴 4/6 —
      above_vwap×6hz cvd_divergence×6hz ofi_norm×6hz opt_chain_pcr×6hz | 정상 진입 전 해소 여부를 확인하라
  2026-08-24 08:47:59 [WARNING] SIGNAL: [CORE준비도] trigger=A_WARMUP 축퇴 1/6 — ofi_norm×6hz | …
  2026-08-24 08:49:59 [WARNING] SIGNAL: [CORE준비도] trigger=A_WARMUP 축퇴 1/6 — ofi_norm×6hz | …
  2026-08-24 08:55:00 [WARNING] SIGNAL: [CORE준비도] trigger=A_WARMUP 축퇴 1/6 — ofi_norm×6hz | …
  2026-08-24 08:59:00 [WARNING] SIGNAL: [CORE준비도] trigger=A_WARMUP 축퇴 1/6 — ofi_norm×6hz | …
  2026-08-24 08:59:00 [WARNING] SIGNAL: [ScalerRefresh] 1m CORE 'ofi_norm' raw_std≈0(0.0298)
      → identity(0,1) 강제 (FLAT 100% 방지)     ← 1m·3m·5m·10m·15m·30m 전부 동일
  ```
  `logs/20260824_SIGNAL.log` · `[ScalerRefresh]` WARNING **54건**(08:45:02~08:59:00) ·
  `[CORE준비도]` WARNING **6건**
- **전일 대조(델타 — 이것이 오늘의 새 정보다)**:
  ```
  2026-08-21 08:45:15 [WARNING] [CORE준비도] A_WARMUP 축퇴 2/6 — cvd_divergence×6hz opt_chain_pcr×6hz
  2026-08-21 08:48:03 [INFO]    [CORE준비도] A_WARMUP 축퇴 0/6 — CORE 스케일 정상   ← 개장 12분 전 해소
  ```
  **08-21은 08:48에 완전 해소된 뒤 개장했다. 08-24는 08:59(마지막 장전 refit)까지 미해소였다.**
- **기준 위반**: CLAUDE.md **절대원칙 ③**(CORE 피처 — 호라이즌 그룹별 분리). 단기 그룹
  (1m·3m·5m)의 CORE 3종 중 「OFI 불균형 — 미통과 시 **등급 하락**」이 여기 해당한다.
  9단계 **STEP 4**(피처 생성) → **STEP 6**(앙상블 진입 판단 + 등급) 경로.
- **영향**: 오늘 장 초반의 매매 판단이, 판단 재료 세 가지 중 하나가 빠진 상태로 내려졌을
  수 있습니다. 손해가 곧바로 나는 것은 아니지만 등급이 실제보다 낮게 매겨져 들어갈 자리를
  놓치거나, 반대로 걸러졌어야 할 신호가 걸러지지 않을 수 있습니다.
- **영향(기술)**: `features/technical/ofi.py` 산출값의 장전 30봉 window `raw_std`가
  0.0270~0.0298로 임계 이하 → `strategy` 스케일러가 `identity(0,1)` 강제.
  `strategy/entry/checklist.py`의 단기 그룹 CORE 체크에서 `ofi` 항목이 상수 입력을 받으므로
  통과/미통과가 신호가 아니라 잡음으로 결정된다. `_CORE_MASK_EXEMPT_BY_HZ`(AutoMask 면제)는
  CORE를 마스킹에서 빼주지만 **스케일 축퇴는 면제 대상이 아니다**.
- **개장 후 즉시 후속(참고 — 장중 표본)**: `09:02:00 [CORE준비도] trigger=D_FORCE 축퇴 0/6 —
  CORE 스케일 정상`. **개장 2분 뒤 자동 해소됐다.** 다만 그 사이(09:00~09:02) 첫 파이프라인
  사이클이 축퇴 상태로 돌았다.
- **구조적 배경(신규 아님 — 재확인)**: CLAUDE.md 371차 각주가 *"`cvd_divergence`·`ofi_norm`
  학습표본의 91%가 정확히 0.0인 점질량은 분위수 비닝으로도 완전히 해소되지 않는 잔여 이슈"*로
  이미 등록해 두었다. 오늘 관측은 그 잔여 이슈가 **장전 구간에 실제로 발현한 사례**다.
  다이제스트 §12 `CORE축퇴_피처` 분포에서도 `ofi_norm×6hz`가 168표본 중 **103건(61.3%)** 으로
  최빈이다 — 상시적이다.
- **신규 여부**: **기존 등록 이슈의 신규 발현**. 축퇴 계측 자체는 470차 B4(`CORE준비도`) ·
  476차 G-2(`CORE축퇴_피처`)로 배선돼 있고 **섀도 관측 상태**다(차단으로 승격되지 않았다).
  오늘의 새 정보는 **「개장 시점까지 미해소」와 「전일 대비 악화」** 두 가지다.
- ⚠ **확정 결론 금지(313차 원칙)**: 관측 1거래일이다. "장전 축퇴가 당일 성과를 떨어뜨린다"는
  **아직 검정되지 않았다.** 470차 B4가 등록한 판정 절차(20거래일 축적 후 축퇴일의
  09:00~09:30 정확도를 **일자 단위**로 비교)를 따라야 하며, 그 전에 차단으로 승격하면
  317차 HurstGate가 진짜 신호의 72.3%를 오판 차단한 전례를 반복한다.

#### 1-2. 앙상블 확률 보정기 스냅샷이 9거래일째 갱신되지 않았다 — 🔄 지속(0821 1-1)

- **증상**: `data/ensemble_calibrator.pkl`의 최종 수정 시각이 **2026-08-11 15:40**에 멈춰
  있다. 기동 복원 로그는 매일 정상 출력되고 값도 정상 범위지만 **어제와 완전히 같은 값**이다.
- **근거**:
  ```
  2026-08-24 08:40:58 [INFO] SYSTEM: [Calibration] 앙상블 보정기 복원 완료
      n=1183 fitted=True degenerate=False unreachable=False span=0.02133 auc=0.574 out_max=0.3897
  ```
  파일 실측: `data/ensemble_calibrator.pkl` · 3,008바이트 · mtime **2026-08-11 15:40**
  다이제스트 §12c: `앙상블보정기_스냅샷` 관측 13거래일 중 **연속 동일 9일** · 사전등록 임계 **8** →
  🟠 **정체** 판정. 마지막 관측값 `n=1183 · fitted=True · out_max=0.3897` — 오늘 로그와 일치.
- **기준 위반**: 9단계 **STEP 6**(앙상블 진입 판단 + 등급)의 확률 보정 입력.
  CLAUDE.md 절대원칙 직접 위반은 아니다. 계측 4원칙 ②(미측정 ≠ 0) 계열의 「조용한 죽음」이다.
- **영향**: 예측 확률을 실제 맞은 비율에 맞춰 다듬는 장치가 **2주 전 자료로 멈춰 있습니다.**
  그동안 시장이 바뀌었다면 확률이 실제보다 높거나 낮게 나옵니다. 이미 지난 금요일에 고쳐
  놓았고, 그 수정이 작동하는지는 **오늘 장 마감 뒤**에 알 수 있습니다.
- **영향(기술)**: `PredictionCalibrator`/앙상블 Platt 보정기의 EOD 저장 경로가 끊긴 상태.
  0821 F-1이 저장 성공 로그(`[Calibration] 앙상블 보정기 저장 완료 n=`)와 실패 시
  「저장 건너뜀」 WARNING을 **양쪽 다** 배선했다 — 종전에는 부정 분기가 무로그였던 것이
  1-1(0821)의 핵심이었다.
- **신규 여부**: **지속.** 0821 리포트 1-1에서 최초 발견(당시 7거래일 정체), F-1 배포 완료.
  라이브 검증은 **P-1(486차)·P-2(486차)로 오늘 장후 판정 예정**이며, 장전 시점에 미갱신인
  것은 **예상된 상태다**(F-1은 EOD 경로에서 작동한다). 새 번호를 붙이지 않고 지속 처리한다.
- ⚠ **§12c가 오늘 처음 「정체」로 판정한 것**은 연속일이 임계 8을 넘긴 결과다(9일).
  **임계는 484차 G-1이 사전등록한 값이며 결과를 보고 조정하지 않는다**(313차 ④).

#### 1-3. 점검 스킬 문서 3곳이 **효력 종료된** CB② 재검토 기한 「2026-08-29」를 아직 인쇄한다

- **증상**: 2026-08-23 주간회의(489차 D2)가 CB②(Circuit Breaker 2단계 — 5분 내 손절 3연속
  시 당일 정지) 복원 기한을 **날짜에서 사건으로** 바꿨다. 종전 "재검토 2026-08-29"는
  **효력 종료**이고, 복원 시점은 **실전 전환 기준 ⑧(실전 자본 재설정) 해제와 동일 커밋**이다.
  `references/invariants.md`는 갱신됐으나 **다른 세 곳은 옛 날짜를 그대로 출력한다.**
- **근거** (2026-08-24 실측, grep 전수):
  | 파일 | 행 | 현재 문구 |
  |---|---|---|
  | `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py` | **168** | `"why": "…실투 전환 전 2~3 복원 필수. 재검토 기한 2026-08-29"` |
  | `.claude/skills/mireuk-daily-check/references/phases.md` | **45** | `- [ ] 한시 예외의 **재검토 기한이 지났는가** (CB② 2026-08-29 등)` |
  | `.claude/skills/mireuk-daily-check/references/report_template.md` | **255** | `\| CB② 복원 (…) \| 2026-08-29 \| \| \|` |

  갱신된 쪽(정본): `references/invariants.md` **153행·379행** —
  *"🔴 [489차 D2] 기한이 날짜 → 사건으로 바뀌었다. 종전 '재검토 2026-08-29'는 **효력 종료** —
  이 날짜를 기한으로 재인용하지 말 것."*

  그리고 **오늘 다이제스트가 실제로 그 죽은 날짜를 출력했다**:
  ```
  | `CB_CONSEC_STOP_LIMIT` | `9999` | `9999` | 일치 | 모의투자 한정 예외(CB② 사실상 비활성).
    실투 전환 전 2~3 복원 필수. 재검토 기한 2026-08-29 |
  ```
- **기준 위반**: CLAUDE.md 절대원칙 §2 CB② 항목(489차 D2 갱신본) · SKILL.md **함정 ①**
  (판정 ≠ 결정 — *"어떤 권고든 '이미 코드에 있는가'를 먼저 확인하라"* 의 거울상: 여기서는
  **이미 폐기된 기한이 도구에 남아 있다**).
- **영향**: 점검 도구가 **이미 없어진 마감일을 계속 보여줍니다.** 다음 점검이 그 날짜를
  믿으면 "8월 29일까지 자동 정지 장치를 되돌려야 한다"고 잘못 보고하게 되고, 실제로는
  그 장치를 되돌리는 시점이 **실전 자본을 정하는 때로 바뀌었기 때문에** 순서가 뒤집힙니다.
  자동 정지 장치를 잘못된 시점에 되돌리면 모의투자 표본이 다시 줄어듭니다.
- **영향(기술)**: `collect_evidence.py:168`의 `why` 문자열은 매일 §3 설정 불변식 표에
  렌더링된다. `phases.md:45`는 장전 체크리스트 A-3의 판정 문항 자체이므로 **점검자가
  이 문항을 그대로 수행하면 죽은 기한을 살아 있는 것으로 판정한다.**
  `report_template.md:255`는 §7「기한 도래 항목」 표의 예시 행이라 그대로 복사될 소지가 크다.
  실제로 **2026-08-23 리포트 §6·§8이 "기한 2026-08-29, 남은 거래일 5"를 사용자 조치
  1순위로 올렸다** — 그 시점(08-23 21:56)에는 489차 커밋(08-24 07:05) 전이라 **당시엔
  정당했다.** 오늘부터는 아니다.
- **신규 여부**: **신규**(489차 결정이 08-24 07:05에 커밋되면서 오늘 처음 성립한 괴리).

### P2 — 운영 부담·기술부채

#### 1-4. 장전 점검이 개장 **36초 뒤**에 실행됐다 — O-10 카운터 리셋(1/3 → 0/3)

- **증상**: 증거 수집기 발화 시각이 **09:00:36**으로, 개장(09:00:00) **36초 후**다.
  장전 표본과 장중 표본이 한 파일에 섞였다.
- **근거**: 다이제스트 §「장전 발화 마진」 —
  `마진 = 09:00:00 − 생성시각 = −36초 (개장 0분 36초 후)`
  | 날짜 | 발화 마진 |
  |---|---|
  | 2026-08-21 | **+12초** (개장 전) |
  | 2026-08-20 | −454초 (개장 7분 34초 후) |
  | 2026-08-19 | −424초 (개장 7분 4초 후) |
  | **2026-08-24** | **−36초 (개장 후)** |
- **기준 위반**: 관측 위생. 절대원칙 위반 아님. **O-10(0821 등재)** — *"다이제스트 발화 마진이
  **3거래일 연속 09:00 이전**"* 이 판정 조건이었고 08-21에 1/3이었다. 오늘 개장 후 발화로
  **카운터가 0/3으로 리셋**된다.
- **영향**: 개장 전에 찍혔어야 할 자료와 개장 직후 자료가 조금 섞였습니다. 오늘 판단이
  틀어질 정도는 아니지만, "장전 점검이 항상 개장 전에 돈다"를 확인하려던 3일짜리 시험은
  다시 처음부터 세게 됐습니다.
- **영향(기술)**: 다이제스트 §5 거래일 요약이 09:00:36까지의 거래를 포함한다(오늘은
  진입·청산 **전부 0건**이라 실질 혼입은 없다). §12 `CORE준비도` 등 매분 샘플 지표는
  개장 후 분(09:00)이 1분 섞였다.
- **처방 금지선**: 🔴 **cron을 08:58:30 이전으로 앞당기지 말 것.** `phases.md` A-2
  (08:55 매크로 → 레짐 확정) 증거를 잃는다 — 오늘 레짐 확정 실측 시각은 **08:58:06**이었다.
  개선은 발화 시각이 아니라 **워크스페이스 부팅·스킬 로드 지연**을 줄이는 쪽이어야 한다.
- **신규 여부**: **지속 관측(O-10, 0821 등재)** — 이상점 번호는 오늘 처음 부여한다.

#### 1-5. `GuardFair_유효`가 7거래일 42표본 100% `무효`로 고착 — 🔄 지속(0821 O-9)

- **증상**: 457차 `fair_valid`(모델 교체 시 신·구 모델의 공정 비교 유효성) 지표가
  **42건 전부 `무효`** 다. 모델 교체의 우열을 한 건도 판정하지 못하고 있다.
- **근거**: 다이제스트 §12 —
  `| GuardFair_유효 | 로그 | 🔴 고착 | 7 | 42 | — | 무효×42 |`
- **기준 위반**: 관측 계층. 절대원칙 위반 아님. 470차 C3'가 26주 WFA 재검증 항목으로
  「`GuardFair 무효율` 추세」를 등록해 둔 바로 그 축이다.
- **영향**: 새 모델과 옛 모델 중 어느 쪽이 나은지를 자동으로 가려주는 장치가 2주 가까이
  한 번도 답을 내지 못하고 있습니다. 매매에 직접 영향은 없지만, 모델을 바꿀 때마다
  더 나빠졌는지를 알 수 없습니다.
- **영향(기술)**: 457차 설계상 `fair_valid=무효`는 `판정 무영향(섀도)`로 표기되고 캠페인
  채널 [23]은 PASS를 낸다 — **죽은 것은 아니다.** 다만 100% 고착은 §12가 잡아내는
  292·303·371·468차 계열의 형태와 같다.
- **신규 여부**: **지속**(0821 O-9로 등재, 매 장후 누적 관측 중). 오늘 장전에 새로 판정할
  근거는 없다 — 표본이 EOD에만 늘어난다.

#### 1-6. 리포 루트에 산출물 2개가 떨어져 있고 미커밋 5건이 남아 있다

- **증상**: `git --no-optional-locks status --porcelain` 기준 untracked **5건**.
  그중 **2건이 리포 루트**에 있다.
  ```
  ?? docs/미륵이고도화3/26주WFA_20260824/
  ?? docs/미륵이고도화3/26주WFA_피처셋재검증_2026-08-24.md
  ?? gate_blocking_metrics.json      ← 리포 루트
  ?? gate_blocking_report.md         ← 리포 루트
  ?? scripts/loo_feature_ablation_purged_cv.py
  ```
- **기준 위반**: 위생 규약(`feedback_git_commit_scope` — 런타임 산출물 커밋 금지 / 산출물은
  지정 폴더에). 절대원칙 위반 아님.
- **영향**: 프로젝트 폴더 맨 위에 임시 결과 파일 두 개가 굴러다닙니다. 지금 당장 문제는
  없지만, 다음에 누가 정리하다가 실수로 지우거나 반대로 실수로 커밋할 수 있습니다.
- **영향(기술)**: `gate_blocking_metrics.json`·`gate_blocking_report.md`는 산출 경로가
  지정되지 않은 스크립트의 기본 출력으로 보인다. `docs/미륵이고도화3/26주WFA_*`와
  `scripts/loo_feature_ablation_purged_cv.py`는 **오늘 새벽 26주 WFA 피처셋 재검증
  (CLAUDE.md 455차 등록 항목)을 실제로 돌린 산출물**이며(`data/ic_decay/ic_decay_catalog_20260824.json`
  08:03:45, window 2026-07-01~08-21, n_days=36, n_real_features=75), 커밋 가치가 있다.
- **신규 여부**: 신규. ⚠ **이 마운트에서는 커밋을 시도하지 않는다** — `invariants.md` §0-B
  (`unlink` 금지 환경). 실제로 오늘 `git status` 한 번에
  `warning: unable to unlink '.git/index.lock': Operation not permitted`가 재현됐다.

### 확인 필요 (확정 아님)

- **호라이즌 확률 보정기 24개 전부 「축퇴 감지」 + FloorShadow 24/24 「판정 불일치」.**
  ```
  08:40:45 [WARNING] LEARNING: [Calibration] 축퇴 감지 — span=0.00007 auc=0.488 out_max=0.3125
      (기준 auc<0.53 and span<0.020, 기저율=0.3125 n=80) → 보정 미적용, raw 통과
  08:40:57 [INFO]    LEARNING: [Calibration][FloorShadow] 판정 불일치 — 정적하한 기준=False /
      실효하한(0.3300) 기준=True  out_max=0.3364. 실효 기준이 True면 현재 보정 적용 상태에서
      자동진입이 산술적으로 불가능하다는 뜻이다.
  ```
  오늘 **24/24(100%)**, 전일 08-21 **28/28(100%)**. `[Calibration]` WARNING 총 159건.
  **판정 보류 이유**: ① FloorShadow는 「보정이 적용됐다면」의 **반사실 섀도**이고, 축퇴가
  감지되면 실제 경로는 `보정 미적용, raw 통과`라 자동진입이 산술적으로 막히는 것은 아니다.
  ② 이 축퇴 임계 `DEGENERATE_AUC_MIN = 0.53`은 **490차 P1이 재개 게이트 앵커로 방금
  재사용한 상수**이며(`dev_memory/DECISION_LOG.md` 490차), 그 채널의 판정 창이
  **2026-08-25부터** 열린다. **무엇을 더 보면 닫히는가**: 08-25 EOD 캠페인 리포트의
  `[2-A]` AUC 표와 `[2-B]` 게이트(O-30). 그때까지 확정 결론 금지.
- **선물 투자자 수급 TR이 브로커에서 제공되지 않는다** — 상시 조건인지 재확인 필요.
  ```
  08:58:06 [INFO] SYSTEM: [CybosInvestorRaw] futures investor TR 후보 없음
      open_interest=152833 (FutureMst fallback)
  08:58:06 [INFO] DATA: [CybosInvestor] futures supported=False source=FutureMst_oi
      foreign=+0 individual=+0 institution=+0 … reason=Cybos 선물 투자자 TR 미발견; 미결제약정만 제공
  ```
  외국인·개인·기관 순매수가 전부 `+0`으로 들어온다. 장전 스케일러 refit의 잔존 z경고에
  `institution_futures_net`이 Phase1→2→3 내내 남은 것과 정합한다
  (`08:55:00 [PreMarket] Phase3 refit 완료 … 잔존=atr,avg_volume,institution_futures_net,microprice_bias`).
  **판정 보류 이유**: 브로커(Cybos) 제약인지 설정 문제인지 장전 로그만으로 구분되지 않는다.
  09:02에 프로그램매매는 `CpSvr8111`로 확보됐다(`arb=+299 nonarb=-162922`). **무엇을 더 보면
  닫히는가**: `[Capability] … investor=Y/N(fetch=0,runtime=N)` 의 `runtime=N` 이 설정에서
  온 것인지 코드 확인(`collection/broker/` 계열) — **장중 코드 열람은 금지가 아니므로
  장중 세션이 읽어도 된다. 변경은 장후.**
- **`CybosProbe` 경고 18건(08:58:06·08:58:36 2회 배치)** — `CoInitialize가 호출되지 않았습니다`
  및 `잘못된 클래스 문자열입니다`. 전일(08-21) 하루 총 **798건**과 비교하면 오늘 18건은
  **로그가 09:02까지밖에 없어서**이지 감소가 아니다(계측 4원칙 ② — 미측정 ≠ 0).
  **무엇을 더 보면 닫히는가**: 장후 다이제스트의 동일 태그 총건수.

### 이미 반영된 사안 (신규 아님)

> 이 절을 비워두지 않는다 — SKILL.md **함정 ①**(판정 ≠ 결정) 방지.

- ✅ **G-1(468차, `MODEL_LABEL_STATE_UNLOCK_ENABLED`) 라이브 검증은 이미 끝났다 — 오늘은
  2거래일 연속 재확인이다. 신규 안건으로 올리지 않는다.**
  ```
  2026-08-24 08:55:03 [INFO] SYSTEM: [EntryGate] 모델 레이블 체계 확인(장전)
      — 사이즈 제한 해제 (×0.6 → ×1.0) | 6/6 호라이즌 현행 레이블(fixed) 학습본
  ```
  판정 근거는 `main.py:1138` 주변 `_refresh_pre_retrain_state()` — `_pre_retrain_done`이
  **False일 때만** 이 로그가 나오므로, 이 줄의 출현 자체가 **G-1 경로가 해제를 수행했다**는
  뜻이다(F-1 마커로 이미 풀렸다면 함수가 조기 반환해 무로그다).
  `invariants.md` §2-B의 P-2 기준(`불일치 0/6` 출현 + F-1 경로 로그 미출현)을 만족한다.
  **최초 통과는 2026-08-21**이며 0821 리포트 312행에 ✅ 기록돼 있다. 이력:
  | 날짜 | 판정 |
  |---|---|
  | 08-14·08-18·08-19 | `미확인 … 불일치 6/6 — label_scheme없음(구버전)` |
  | 08-20 | `미확인 … 불일치 6/6 — label_scheme=null(기록경로 결함)` |
  | **08-21** | ✅ `확인 … 6/6 호라이즌 현행 레이블(fixed)` ← **최초 통과** |
  | **08-24** | ✅ 동일 (2거래일 연속) |
  ⚠ **그래도 실전 전환 기준 ⑧은 해제되지 않는다** — 남은 조건은 **실전 자본 재설정(사용자
  결정)** 이고, 489차 D6이 *"①이 충족되기 전에는 ⑧을 해제하지 않는다"* 는 선행조건을 추가했다.
- ✅ **CB② 미복원(`CB_CONSEC_STOP_LIMIT = 9999`)은 결함이 아니라 확정된 결정이다.**
  489차 D2가 기한을 날짜 → 사건(⑧ 해제와 동일 커밋)으로 바꿨고, 그 사이 캠페인 채널
  **[56] `cb2_restore_shadow`** 가 매주 반사실을 재계산한다(판정 창 2026-08-24~).
  ⚠ 복원값 **2와 3은 방향이 반대**이며 그 사후탐색 수치로 값을 고르지 말 것(313차 ④).
- ✅ **CB③-P4(`CB3_P4_GRADE_BLOCK_ENABLED=False`)·FP-CRITICAL
  (`FP_CRITICAL_GRADE_BLOCK_ENABLED=False`)·TOX-SEVERE-SPREAD
  (`TOXICITY_SEVERE_SPREAD_BLOCK_ENABLED=False`)는 전부 기록된 한시 예외**다.
  특히 TOX-SEVERE-SPREAD는 489차 D7이 **실전 전환 기준 ⑨에서 26주 WFA 재검증 항목으로
  이관**했다 — **실전 전환 선행조건이 아니다.**
- ✅ **`ZONE_ENTRY_BAN_ENFORCE=False`** 는 462차 P1-a 결정. 위반 코호트가 흑자(+596,858원)라
  채널 [53]이 `min_samples=20`에 도달할 때까지 False 유지. **판정 전 집행 금지.**
- ✅ **`MAX_CONTRACTS=3`이 오늘 한 번도 구속하지 않는 것**은 470차가 규명한 대로
  **브로커 증거금 상한이 2에서 먼저 자르기 때문**이다. 오늘 `[Capital]` 로그가
  `MAX_CONTRACTS=3 최대노출=164,010,000원(레버리지 3.28x)`을 찍었지만 실제 상한은
  `get_order_available_qty()`가 정한다. 새 발견이 아니다.
- ✅ **`[PreRetrain] 08:55 사전 재학습 스킵`은 정상이다.**
  ```
  08:55:03 [PreRetrain] EOD 재학습 날짜 복원: 2026-08-21 (3일 전) → PreRetrain 스킵 검토
  08:55:03 [PreRetrain] 08:55 사전 재학습 스킵 — 1영업일 전(2026-08-21) EOD 재학습 성공 (동일 데이터 중복 불필요)
  ```
  "3일 전"은 달력일, "1영업일 전"은 거래일 기준이며 **둘 다 맞다**(08-22 토·08-23 일 휴장).

---

## 2. Fix 작업 구현계획

> 🟢 **장전 국면이므로 코드 변경을 실행하지 않는다.** 라이브 프로세스(PID 7128)가 09:00부터
> 매분 루프를 돌고 있다. 아래는 **계획 수립**이며, 적용은 **15:10 강제청산 이후**를 권고한다.
> ⚠ 이 마운트는 `unlink`를 금지하므로 커밋은 `invariants.md` §0-B의 rename 우회로만 시도한다.

### F-1(491차). 스킬 문서 3곳의 죽은 CB② 기한을 489차 D2 결정으로 교체한다 — **P1** · 대응 이상점 **1-3**

- **원인 가설**: 489차가 결정을 반영할 때 `invariants.md`만 갱신했다. CB② 기한 문자열이
  **네 곳에 복제**돼 있었고 나머지 셋이 남았다. 「소비부만 옮기지 않았는가」
  (0821 등재 멀티PC 컨벤션 보강 안건)의 **같은 계열 실패**이며, 이번엔 방향이 반대다 —
  정본은 고쳤는데 사본이 안 고쳐졌다.
- **변경 파일**:
  - `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py` — **168행** `why` 문자열:
    `"…실투 전환 전 2~3 복원 필수. 재검토 기한 2026-08-29"`
    → `"…실투 전환 전 2~3 복원 필수. 🔴 [489차 D2] 기한은 날짜가 아니라 사건 — 복원 시점 = 실전 전환 기준 ⑧ 해제와 동일 커밋. 종전 '2026-08-29'는 효력 종료(재인용 금지). 그 사이 채널 [56] cb2_restore_shadow가 매주 반사실 재계산"`
  - `.claude/skills/mireuk-daily-check/references/phases.md` — **45행** A-3 체크 문항:
    `(CB② 2026-08-29 등)` → `(⚠ CB②는 날짜 기한이 아니다 — 489차 D2로 「⑧ 해제와 동일 커밋」으로 전환)`
  - `.claude/skills/mireuk-daily-check/references/report_template.md` — **255행** §7 예시 행의
    `2026-08-29` → `사건 기한(⑧ 해제와 동일 커밋) — 날짜 아님`
  - 사본 동기화: `config/dailycheck_targets.json`에는 이 문자열이 **없다**(실측 확인) —
    수집기 파이썬 상수만 고치면 된다.
- **회귀 위험**: **매우 낮다.** 문자열·주석·문서만 바뀐다. 라이브 코드 경로가 아니다
  (`.claude/skills/**`는 점검 도구이지 매매 런타임이 아니다).
  단 `collect_evidence.py`는 **표 렌더링 폭**에 영향을 주므로 문자열이 길어지면 §3 표가
  가로로 늘어난다 — 두 줄로 나누거나 축약할 것.
- **절대원칙 저촉 여부**: **없음.** 6종 중 어느 것도 건드리지 않는다. CB②의 **값
  (`9999`)은 손대지 않는다** — 이 fix는 기한 문구만 고친다.
- **런타임 주의**: 점검 스크립트는 표준 라이브러리만 쓰며 `py37_32`/`py310_64` 무관.
  COM 콜백 밖이다.
- **검증 방법**:
  ① `grep -rn "2026-08-29" .claude/skills/mireuk-daily-check/` → `invariants.md`의
     **「효력 종료」 문맥 2행만** 남는지 확인(그 두 행은 역사 기록이므로 지우지 않는다).
  ② 수집기 재실행 후 §3 표의 `CB_CONSEC_STOP_LIMIT` 행에 새 문구가 렌더링되는지 확인.
  ③ 다음 거래일(08-25) 장전 다이제스트에서 옛 날짜 미출현 확인 → **O-1(491차)**.
- **섀도 계측 필요 여부**: **불요.** 게이트·차단 로직 신설이 아니다.
- **적용 시점**: **장후.** 급하지 않지만 08-25 장전 다이제스트 전에 끝내는 것이 좋다 —
  그래야 죽은 날짜가 한 번 더 인쇄되지 않는다.
- **결정 필요 사항**: **없음.** 489차 D2가 이미 결정했고 이 fix는 그 결정을 도구에 반영할 뿐이다.

### F-2(491차). 리포 루트 산출물 2건에 출력 경로를 준다 — **P2** · 대응 이상점 **1-6**

- **원인 가설**: `gate_blocking_*` 산출 스크립트가 출력 경로를 인자로 받지 않고 CWD에 쓴다.
- **변경 파일**: 산출 스크립트 특정이 선행이다.
  `grep -rn "gate_blocking_metrics" scripts/ --include=*.py` 로 생산부를 찾은 뒤
  기본 출력을 `docs/미륵이고도화3/` 또는 `data/`(gitignore 대상)로 옮긴다.
  ⚠ **`docs/정기점검/금요일점검/` 로 보내지 말 것** — 그쪽은 「런타임 산출물 커밋 금지」의
  **의도적 예외** 구역이고, 두 PC가 서로를 보는 통로다. 성격이 다른 파일을 섞지 않는다.
- **회귀 위험**: 낮음. 다만 **다른 스크립트가 루트 경로로 읽고 있으면** 깨진다 —
  `grep -rn "gate_blocking" --include=*.py .` 로 소비부를 먼저 전수 확인할 것
  (「소비부만 옮기지 않았는가」의 역방향).
- **절대원칙 저촉 여부**: 없음.
- **런타임 주의**: 오프라인 분석 스크립트. 라이브 무관.
- **검증 방법**: 재실행 후 리포 루트에 파일이 생기지 않는지 + 기존 소비부가 정상 동작하는지.
- **섀도 계측 필요 여부**: 불요.
- **적용 시점**: 장후 또는 차주. 급하지 않다.
- **결정 필요 사항**: 출력 위치 — **권고: `docs/미륵이고도화3/`**(오늘 26주 WFA 산출물과
  같은 자리). 사용자 확인 불요 수준.

### F-3(491차). 26주 WFA 피처셋 재검증 산출물을 커밋한다 — **P2** · 대응 이상점 **1-6**

- **원인 가설**: 오늘 새벽 사람이 돌린 분석의 산출물이 아직 untracked다.
  `docs/미륵이고도화3/26주WFA_20260824/` · `26주WFA_피처셋재검증_2026-08-24.md` ·
  `scripts/loo_feature_ablation_purged_cv.py`.
- **변경 파일**: 없음(신규 파일 추가만).
- **회귀 위험**: 없음. 단 `scripts/loo_feature_ablation_purged_cv.py`가 **EOD 체인에
  연결돼 있지 않은지** 확인할 것 — 연결돼 있으면 그건 코드 변경이라 별건이다.
- **절대원칙 저촉 여부**: **없음.** 절대원칙 ⑥(알파 리서치 봇 자동 통합 금지)과 무관하다 —
  이것은 **사람이 명시적으로 돌린 오프라인 분석**이고, 자동 큐·자동 통합이 아니다.
- **런타임 주의**: `loo_feature_ablation_purged_cv.py`는 재학습 계열 분석이므로
  **`py310_64` 전용**일 가능성이 높다(191차 OOM 결정). 스크립트 상단에 명기할 것.
- **검증 방법**: `git --no-optional-locks status --porcelain` 이 비는지.
- **적용 시점**: 장후.
- **결정 필요 사항**: **있다** — 이 산출물은 CLAUDE.md 「주기적 재검증 항목」의
  **「호라이즌 방향예측 피처셋 재검증」(455차 등록)** 실행분으로 보인다.
  그 결과를 **26주 WFA 판정으로 채택할지**는 주간회의 소관이다. 이번 fix는 **파일 보존만**
  하고 판정하지 않는다. ⚠ 사후 데이터로 기준을 움직이지 않는다(313차 ④).

### 적용 순서와 커밋 계획

1. **커밋 ①** — F-1(스킬 문서 3곳 CB② 기한 교체) —
   `[MW0602] 491차: 죽은 CB② 기한 문구 3곳 교체 — 489차 D2 결정을 점검 도구에 반영`
2. **커밋 ②** — F-3(26주 WFA 산출물 보존) —
   `[MW0602] 491차: 26주 WFA 피처셋 재검증 산출물 보존 (판정 아님 — 주간회의 소관)`
3. **커밋 ③(차주 가능)** — F-2(출력 경로) —
   `[MW0602] 491차: gate_blocking 산출 경로를 리포 루트 밖으로`

> 🔴 **셋 다 장후 적용.** 장중에는 실행하지 않는다.
> 🔴 **커밋 시 `invariants.md` §0-B 우회로를 쓴다** — lock 파일을 **지우지 말고 rename**하고
> `git --no-optional-locks` 로 실행한다. 오늘도 `unlink: Operation not permitted`가 재현됐다.

---

## 3. 고도화 방안

> 당일 관측에서 출발한 것만 쓴다. 일반론 금지.

### G-1(491차). 「장전 CORE 축퇴가 개장 시점까지 남았는가」를 별도 축으로 계측한다

- **관측 근거**: 오늘 `[CORE준비도]` 축퇴가 08:45 `4/6` → 08:47 `1/6` → **08:59 `1/6`(미해소)**
  → 09:02 `0/6` 이었다. 전일 08-21은 08:48에 `0/6`으로 완전 해소된 뒤 개장했다.
  **현행 §12 `CORE준비도` 지표는 「하루 중 축퇴 개수의 분포」만 본다** — `1`×103 · `0`×59 …
  식이라 **개장 시점의 값**이라는 결정적 순간이 분포에 묻힌다.
  오늘 같은 케이스(장중에는 해소됐지만 첫 사이클은 축퇴 상태)를 현행 계측은
  「1이 좀 더 많은 날」로만 기록한다.
- **제안 내용**: 수집기 §12에 파생 축 **`CORE축퇴_개장시점`** 을 추가한다.
  값은 **09:00 직전 마지막 `A_WARMUP` 로그의 축퇴 개수**(오늘 = `1`, 08-21 = `0`).
  `sample_axis`는 분 단위가 아니라 **거래일 1샘플**이므로 관측률 판정 대상이 아니다.
  피처 이름 축(`CORE축퇴_피처`)도 같은 방식으로 개장시점 값을 남긴다.
- **기대 효과**: 「축퇴가 있었다」와 「축퇴한 채로 개장했다」를 도구가 가른다.
  20거래일 축적 후 470차 B4가 등록한 판정 절차(**축퇴일의 09:00~09:30 정확도를
  일자 단위로 비교**)에 **정확한 코호트 정의**를 제공한다 — 현행 분포로는 그 코호트를
  만들 수 없다.
- **비용·위험**: 수집기 파서에 파생 축 1개 추가(약 30줄). 라이브 코드 무변경.
  위험은 **오탐 방향으로 없다** — 관측 전용이고 차단 승격이 아니다.
  ⚠ 이 축을 근거로 **차단을 신설하지 말 것**. 470차 B4가 명시한 20거래일 절차가 선행이다.
- **선행 조건**: 없음. 1-1과 독립적으로 배선 가능하다.
- **우선순위 제안**: **이번 주.** 오늘이 코호트 1일차이므로 지금 배선해야 표본이 처음부터 쌓인다.

### G-2(491차). 스킬 문서 안의 「날짜 기한」을 기계가 훑게 한다

- **관측 근거**: 1-3에서 **같은 기한 문자열이 네 곳에 복제**돼 있었고 정본 하나만 갱신됐다.
  사람이 grep 해서 찾았다. 이 프로젝트가 이미 아는 실패 형태다 — CLAUDE.md가
  CB②·CB③-P4·FP-CRITICAL을 두고 반복해 경고한 *"재검토하기로 했는데 안 함"* 의
  **도구판 변종**이다(이번엔 결정이 있었는데 도구가 몰랐다).
- **제안 내용**: 수집기에 **§14 「문서 내 미래 날짜 스캔」** 을 신설한다.
  `.claude/skills/mireuk-daily-check/**` 와 `CLAUDE.md` 에서 `20\d\d-\d\d-\d\d` 패턴을 훑어
  ① **오늘 기준 30일 이내에 도래하는 날짜**와 ② **이미 지난 날짜**를 표로 낸다.
  각 행에 파일·행번호·주변 20자를 붙인다. **판정하지 않고 나열만 한다** — 판정은 사람 몫이다.
- **기대 효과**: 죽은 기한과 임박한 기한이 매일 눈에 띈다. 오늘 같은 괴리는 **다음 날 자동
  검출**된다.
- **비용·위험**: 정규식 스캔 1회(파일 수십 개, 밀리초 단위). **오탐이 많다** — 역사 기록의
  날짜(예: "2026-06-25 CORE 강등")가 전부 걸린다. 그래서 **적신호로 올리지 않고 §14
  참고 표로만** 둔다. §12의 `benign` 화이트리스트와 같은 취급이 필요해지면 그때 붙인다.
- **선행 조건**: **F-1이 먼저**다. 지금 배선하면 첫 실행에서 자기가 고칠 대상 3건을
  적신호처럼 뱉는다 — 고친 뒤에 켜야 목록이 신호가 된다.
- **우선순위 제안**: **이번 주(F-1 다음).**

### G-3(491차). 장전 발화 지연의 원인을 발화 시각이 아니라 **부팅 지연**에서 찾는다

- **관측 근거**: 오늘 발화 마진 **−36초**. 예약 작업 자체는 08:59대에 트리거됐을 것이나
  다이제스트 생성이 09:00:36이었다. 08-19·08-20은 −424초·−454초로 훨씬 나빴고 08-21만
  +12초였다 — **분산이 크다는 것이 곧 원인이 스케줄이 아니라 지연에 있다는 신호다.**
- **제안 내용**: 수집기 다이제스트 「장전 발화 마진」 블록에 **`부팅~수집 소요 시간`** 한 줄을
  더한다. 수집기 프로세스 시작 시각과 파일 저장 시각의 차이를 찍으면, 마진 악화가
  ① 스케줄 트리거 지연인지 ② 수집기 자체가 느린 것인지가 갈린다.
- **기대 효과**: O-10(3거래일 연속 09:00 이전) 판정이 실패할 때 **어디를 고쳐야 하는지**가
  같이 나온다. 지금은 실패 사실만 남는다.
- **비용·위험**: 두 줄. 위험 없음.
- **선행 조건**: 없음.
- **우선순위 제안**: **이번 주.** 🔴 **cron 시각을 앞당기는 처방과 함께 제안하지 말 것** —
  08:58:30 이전으로 당기면 `phases.md` A-2(08:55 매크로 → 레짐 확정) 증거를 잃는다.
  오늘 레짐 확정 실측은 **08:58:06**이었다.

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| **실전 전환 기준 ⑧** (사이징 재설계) | 조건 2(`MAX_CONTRACTS` 재검토) 431차 충족 · 선행조건 G-1(468차) **2026-08-21 통과** | **변경 없음 — 해제하지 않는다** | 남은 조건은 **실전 자본 재설정(사용자 결정)** 하나이고, 489차 D6이 *"①이 충족되기 전에는 ⑧을 해제하지 않는다"* 를 추가했다. ①은 0821 기준 **미충족**(최근 20거래일 −178,004원 · 양수일 8/20). 오늘 장전에 이를 뒤집을 증거는 없다 |
| **주기적 재검증 항목**(26주 WFA) | 「고착 지표 감시목록」(468차 G-2) · 「관측 계층 5종」(470차 C1'~C5') · 「분기편향 옵트인 목록」(475차 후속) | **G-1(491차) `CORE축퇴_개장시점` 축을 같은 목록에 신설 등록** | 468차 G-2가 등록한 사유 그대로다 — *"감시 대상 목록 자체가 낡는 것은 일일 점검이 못 잡는다."* 새 축은 배선 즉시 목록에 넣지 않으면 다음 26주 때 존재를 모른다 |
| **주기적 재검증 항목**(26주 WFA) | 「호라이즌 방향예측 피처셋 재검증」(455차 등록) | **오늘 새벽 실행분(`ic_decay_catalog_20260824.json`, window 2026-07-01~08-21, n_days=36, n_real_features=75, bonferroni_t=6.360, noise_floor_max_abs_t=2.462)이 그 항목의 실행 산출물인지 확인하고 등재** | 산출물이 untracked로 떠 있어(1-6) 다음 26주 때 "돌렸는지 여부"조차 알 수 없게 된다. ⚠ **등재는 보존이지 판정이 아니다** — L1→L2→L3 순차 재실행 완료 여부와 판정은 주간회의 소관 |
| **실전 전환 기준 ⑨**(TOX-SEVERE-SPREAD) | 489차 D7로 **26주 WFA 재검증 항목으로 이관 완료** | **변경 없음** | 이미 처분된 안건이다. 오늘 §12b가 `4일 연속 미도달(max 9.003 / 임계 20)`을 다시 찍었지만 그것은 **매주 재계산되는 판정**이지 미조치의 증거가 아니다(함정 ①) |

---

## 4. 진입 승패 사후검증 (장후 한정)

**해당 없음(장후 전용).** 장전 시점 진입·청산 **0건**(다이제스트 §5 거래일 요약: 진입체크
통과 0 · 진입 등록 0 · 체결 0 · 포지션 0 · 청산 레그 0 · 차단 0 · 사이저 호출 0).

> ⚠ 이 0은 **개장 36초 시점의 0**이다(1-4). "오늘 하루 0건"이 아니다.

---

## 5. 수익률 향상방안 (장후 한정)

**해당 없음(장후 전용).**

---

## 6. 다음 거래일 관측 예정 + 오늘 장중·장후에 닫을 항목

### 6-A. 앞 국면(0821 장후 · 0823) 이월 항목 — 장전 시점 처분

| 원 ID | 항목 | 장전 처분 | 근거 |
|---|---|---|---|
| **O-10**(0821) | 다이제스트 발화 마진 3거래일 연속 09:00 이전 | 🔴 **판정 — 실패(리셋 0/3)** | 오늘 **−36초**(개장 후). 1-4로 이상점화. 카운터 재시작 |
| **O-4**(0821) | `[Capital]` 괴리율 추이 | ⏸ **관측 계속** | 08-14 −40.6% → 08-18 −40.7% → 08-19 −40.8% → 08-20 −42.3% → 08-21 −42.1% → **08-24 −41.5%**(실제잔고 29,274,631원). **추세 반전 아님 — 횡보.** 판정 기준 미도달 |
| **O-3**(0821) | 런처 GUARD 연속일 | ⏸ **관측 계속** | 08-14·18·19·20·21·**24 전부** `기존 프로세스 종료 완료` **1회**. **6거래일 연속 동일** — 매 기동 시 이전 인스턴스가 살아 있다는 뜻. ⚠ 오늘 판정하지 않는다(무엇이 정상인지 기준이 등록돼 있지 않다) |
| **P-1**(486차) | `[Calibration] 앙상블 보정기 저장 완료 n=` 또는 「저장 건너뜀」 WARNING | ➡ **장후로 이월** | 15:40 EOD 경로. 장전에 판정 불가 |
| **P-2**(486차) | `data/ensemble_calibrator.pkl` mtime → 2026-08-24 갱신 | ➡ **장후로 이월** (장전 현황: **미갱신, 2026-08-11 15:40**) | 1-2로 지속 기록. F-1(0821)은 EOD에 작동하므로 **장전 미갱신은 예상된 상태** |
| **P-3**(486차) | `[DriftRetrain] state=…` 매분 1줄(미발동 분에도) | ➡ **장중으로 이월** | 09:00 이후 표본 필요 |
| **P-4**(486차) | 다이제스트 §5 `[MarginCap]` 건수 == §12 `MarginCap_state` 표본 | ➡ **장후로 이월** (장전 현황: §5 `조회 — · 축소 0 (구형식만)` · §12 `표본부족` 1일 27건) | 장전에는 증거금 조회 사이클이 거의 없다 |
| **O-1**(486차) | 예약 점검 `lastRunAt` vs 수집기 대상일 | ✅ **해소(오늘 조건)** | 오늘 대상일 `2026-08-24` = 실제 거래일. 486차 F-1(휴장일 판정) 배선 후 첫 거래일이며 「🗓 거래일 문맥」 블록이 정상 출력됐다(`거래일 여부: 거래일 ✅`) |
| **O-17**(0821) | `entry_horizon` 소비처 전수 | ➡ **장후로 이월** | 코드 조사 항목. 장중 코드 변경 금지 원칙과 무관하나(읽기만) 장후에 하는 것이 맞다 |
| **O-16 · O-5 · O-7 · O-8 · O-9 · O-18~O-23 · P-8 · P-9** | 5m 정확도 / 비데몬 스레드 / meta_action 승패 / 손절 준수율 / GuardFair 무효율 / Tier1 준수율 / 진입 방향 편향 / WeightCollapse 비율 / `sizing_trace.margin_cap_qty` / 롤링20일 net / `bar_pass≥1` | ➡ **전부 장후로 이월** | 전부 EOD 표본 기반. 기준 **무변경**(0821 §6p 정본). ⚠ O-9(GuardFair 무효율)는 장전 현황을 1-5에 기록했다 |
| **O-20 · O-21**(0821) | 월간 로그 정리 `OK` / 캠페인 metrics 필드 소멸 | ➡ **2026-08-28(금) EOD로 이월** | 금요일 전용 체인. 평일에 비는 것은 결함이 아니다 |
| **O-26~O-29**(489차) | `[56]` 행 출현 / `[44]`·`[46]` 📌 마커 / `[56]` 표본 진행 / `[46]` 신규 통과분 | ➡ **오늘 장후(EOD)로 이월** | 판정 창이 **2026-08-24부터** 열린다 — 오늘이 1일차다 |
| **O-30~O-32**(490차) | `[2-A]` AUC 표·`[2-B]` 게이트 출현 / 사이드카 `history` 누적 / `meta_entry_quality_prob` NULL 급증 없음 | ➡ **2026-08-25로 이월**(원 등록대로) | 490차가 08-25 판정 예정으로 등록했다. 오늘 앞당기지 않는다 |

### 6-B. 오늘 새로 등록하는 관측

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| **O-1(491차)** 🆕 | 다이제스트 §3 `CB_CONSEC_STOP_LIMIT` 행의 문구 | 「2026-08-29」 **미출현** + 「⑧ 해제와 동일 커밋」 출현. F-1(491차) 적용 후 | **2026-08-25 장전** |
| **O-2(491차)** 🆕 | `[CORE준비도]` 개장 직전 마지막 `A_WARMUP` 축퇴 개수 | 오늘 = **1**(`ofi_norm×6hz`), 08-21 = **0**. **20거래일 축적 후** 470차 B4 절차(축퇴일 09:00~09:30 정확도 일자단위 비교)로 판정. ⚠ **그 전에 확정 결론·차단 승격 금지**(313차) | **누적** — 매 장전 |
| **O-3(491차)** 🆕 | `[CORE준비도]` 개장 후 최초 `D_FORCE`/`B_OPEN` 축퇴 개수 | 오늘 09:02 = **0**(해소). 「장전 축퇴가 개장 후 스스로 풀리는가」의 표본. 풀리지 않는 날이 나오면 P1 승격 | **매 장중** |
| ↳ **O-3 재정의** | ↩️ **[2026-08-24 장후 · F-6(491차) 적용]** 위 행의 기준은 **폐기**됐다 — 「최초 1점」은 재발에 눈이 멀다(`1-10` 이 `1-1` 을 정정: 09:01 `0/6` ✅ → 실제 **21/28=75%** · 최장 연속 09:22~12:38). | 신규 기준: 개장 후 **전** refit 의 **축퇴율** + **최장 연속 축퇴 구간**. 정본은 `dev_memory/NEXT_TODO.md` 491차 관측 항목 | **매 장중** |
| **O-4(491차)** 🆕 | `[Calibration] 축퇴 감지` 건수 및 FloorShadow `판정 불일치` 비율 | 오늘 24/24(100%) · 08-21 28/28(100%). **490차 [2-B] 재개 게이트 판정창(08-25~)과 함께** 읽는다. ⚠ 단독으로 확정 결론 금지 | **2026-08-25 EOD** |
| **O-5(491차)** 🆕 | `[CybosProbe]` 경고 총건수 (장후 다이제스트 기준) | 08-19·20·21 각 **798건**과 같은 수준이면 **상시 조건 확정**(1-1 「확인 필요」 항목 종결). 크게 다르면 별건 | **오늘 장후** |

---

## 7. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| 🔴 ~~CB② 복원 (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) — 2026-08-29~~ | ❌ **효력 종료** | — | **489차 D2로 기한이 날짜 → 사건으로 전환됐다.** 복원 시점 = **실전 전환 기준 ⑧ 해제와 동일 커밋**. ⚠ **「8월 29일」을 기한으로 재인용하지 말 것.** 그 사이 캠페인 채널 **[56] `cb2_restore_shadow`** 가 매주 반사실을 재계산한다(판정 창 2026-08-24~). **도구 3곳이 아직 옛 날짜를 인쇄한다 → 1-3 · F-1(491차)** |
| **[46] Hurst 임계 승격** — `hurst_threshold_shadow.promotion_watch` | 판정 창 **2026-08-24~** | **오늘 1일차** | 사전등록 완료. `abs_0.39`의 **신규 통과분**이 왕복비용을 넘는가. ⚠ **문턱을 관측값에서 역산하지 않았다 — 사후 완화 금지** |
| **[55] 09:20~09:29 (E)안** — `early_window_gate_shadow.vol_condition_prereg` | 판정 창 **2026-08-24~** | **오늘 1일차** | 등급 조건을 ATR 조건(앵커 `ATR_MAX_ENTRY`)으로 바꾸면 나은가 |
| **[56] CB② 복원** — `cb2_restore_shadow` | 판정 창 **2026-08-24~** | **오늘 1일차** | 복원값 2·3 각각에서 CB②가 **손실을 막나 수익을 막나**. ⚠ **집계 단위는 포지션**(470차 C1) — 로그 `[CB] 연속 손절 N회`로 과거를 세지 말 것 |
| **[2-B] 스코어러 재개 게이트** — `meta_gate.reopen_gate` | 판정 창 **2026-08-25~** | 1거래일 | 490차 P1 사전등록. `auc_net_directional` ≥ 0.53(런타임 앵커) **4주 연속**. 현재 ⏳ INSUFFICIENT |
| **주간회의 2026-08-28 이월 안건** | 주간회의 | 4거래일 | ① **1-9 앙상블 등급 임계 재설계**(§12b **9일 연속 미도달**, 관측 max 0.5862 < 0.60) ② **F-8 채널 생산부 결손** — 487차 후속으로 (B) 확정·(A) 폐기됨, 잔여 확인만 ③ **멀티PC 컨벤션 보강**(「소비부만 옮기지 않았는가」) ④ **[P2] `auc_strong_directional` 청산 축 승격 여부**(490차 등재) |

> ⚠ **1-9(앙상블 등급 임계)는 2026-08-22 주간회의 안건이었으나 0823 회의에서 처분되지
> 않고 이월됐다.** 오늘 §12b가 `📌 미도달(9일 연속 — 전 관측일)`로 다시 찍은 것은
> **판정의 재계산이지 미조치의 증거가 아니다**(함정 ①). 신규 계측을 제안하지 않는다.

---

## 8. dev_memory 반영

- `dev_memory/DECISION_LOG.md` 추가 항목:
  `## 2026-08-24 (MW0602 491차 — 장전 점검: 죽은 CB② 기한 3곳 + 장전 CORE 축퇴 미해소)`
- `dev_memory/NEXT_TODO.md` 추가 체크박스: **Fix 3건**(F-1·F-2·F-3 / 491차) ·
  **고도화 3건**(G-1·G-2·G-3 / 491차) · **관측 5건**(O-1~O-5 / 491차)
- 완료 처리한 기존 항목: **O-1(486차)** ✅ 해소 · **O-10(0821)** 🔴 판정(실패·리셋)
- 세션 차수: **491차** — 원격(git) 기준 최신이 `490차`(`d187218`)이므로 그 다음
- 커밋 제목 접두: `[MW0602]`
- ⚠ **기존 내용을 덮어쓰지 않는다** — 두 파일 다 2.0MB·1.1MB 누적 기록이다

> 📄 **스킬 템플릿 메모**: `report_template.md` **255행**이 §7 예시 행에
> `CB② 복원 … 2026-08-29`을 하드코딩하고 있다. 489차 D2로 그 날짜는 효력 종료됐으므로
> **템플릿을 그대로 복사하면 죽은 기한이 매일 재생산된다.** F-1(491차)이 이를 포함한다.
> 오늘 리포트는 §7에서 그 행을 **취소선 + 효력 종료 명시**로 처리했다.

---

## 사용자 조치

> 🟢 **오늘 아침에 당장 하실 일은 없습니다.** 아래는 시간 나실 때 봐 주시면 되는 것들입니다.

| # | 지금 할 일 | 걸리는 시간 | 안 하면 생기는 일 |
|---|---|---|---|
| **1** | **오늘 장 마감(오후 3시 10분) 뒤에 "F-1 구현해"라고 말씀해 주세요.** 점검 도구 세 군데가 이미 없어진 마감일(8월 29일)을 계속 보여주고 있습니다. 지난 주말 회의에서 그 마감일을 없애고 "실전 자본을 정하는 때"로 바꾸기로 결정하셨는데, 도구만 못 따라왔습니다. | 제가 **약 10분**, 확인 포함 **20분** | 내일 아침 점검부터 없는 마감일이 다시 인쇄되고, 다음 점검이 그것을 근거로 "자동 정지 장치를 8월 29일까지 되돌려야 한다"고 잘못 보고할 수 있습니다 |
| **2** | (선택) **"G-1 구현해"라고 말씀해 주세요.** 오늘 판단 재료 하나가 개장 직전까지 무력화돼 있었는데, 지금 도구로는 "개장하는 순간에 그랬는지"가 기록되지 않습니다. 오늘이 그 기록의 1일차라 지금 만들어야 처음부터 쌓입니다. | 제가 **약 20분**, 확인 포함 **30분** | 20일치 자료를 모아 "이게 성적에 영향을 주는가"를 따져야 하는데, 그 20일이 오늘부터가 아니라 만드는 날부터 시작됩니다 |
| **3** | (선택) **정리 두 가지** — 프로젝트 폴더 맨 위에 임시 결과 파일 두 개(`gate_blocking_metrics.json`·`gate_blocking_report.md`)가 굴러다니고, 오늘 새벽에 돌리신 26주 점검 결과물이 아직 저장(커밋)되지 않았습니다. "F-2·F-3 구현해"라고 하시면 정리하겠습니다. | 제가 **약 15분** | 임시 파일이 계속 쌓이고, 26주 점검을 돌린 기록이 남지 않아 다음번에 "이미 돌렸는지"조차 알 수 없게 됩니다 |
| **4** | **이번 주 금요일 회의(8월 28일)에서 네 가지를 봐 주세요.** (1) 예측 확신도의 최고 등급 기준이 **9거래일 연속 한 번도 닿지 않았습니다** — 기준을 다시 정할지 (2) 다른 컴퓨터에서 코드를 가져올 때 "결과를 만드는 부분도 같이 왔는가"를 확인하는 규칙 추가 (3) 점검표 채널 두 개의 남은 확인 (4) 새로 만든 지표를 청산 판단에 쓸지 | **20분** | (1)은 지금 아침 9시 20분~29분 구간이 사실상 전면 진입 금지로 동작하고 있다는 뜻입니다. 나머지는 점검표가 잘못된 그림을 보여주는 문제입니다 |
| **5** | **가끔 부탁드리는 정리** — 윈도우 PowerShell에서 `Remove-Item .git\*.lock*, .git\__* -Force; git gc --prune=now` 를 한 번 돌려 주세요. 제가 있는 환경에서는 파일 삭제가 막혀 있어 제가 못 지웁니다. | **1분** | 커밋 흔적 파일이 계속 쌓입니다. 커밋 자체는 우회로로 되므로 급하지 않습니다 |

---

*본 점검은 예약 점검(자동 실행)으로 작성됐다. **장전 국면이므로 라이브 코드·설정 변경 0건.**
산출은 증거 다이제스트(`evidence_MW0602-20260824_pre.md`)와 이 보고서, 그리고 `dev_memory` 기록뿐이다.
장중·장후 세션은 이 파일 **끝에 이어 붙인다** — 위 장전 절의 본문은 고치지 않는다.*

---

# 장중(intra)

- 점검 시각: **12:50 KST** (증거 수집기 발화 2026-08-24 12:40:59) · PC `MW0602` · 브랜치 `dev` ✅
- HEAD `135c00f` (491차, 장전 점검 커밋) · 미커밋 **5건**(전부 untracked, 장전과 동일) · 당일 커밋 **3건**
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260824_intra.md` (82.5KB)
- 수집기 실행: **성공** (stderr 없음). 다이제스트 첫 줄 `MW0602 (override · host=claude)` 정상
- 🟢 **장중이므로 코드·설정 변경 0건. 커밋 0건. 읽기 전용 조사만 했다.**

> 📌 **번호 규약 메모 — `1-9`는 오늘 쓰지 않는다.**
> 이 프로젝트에서 `1-9`는 **2026-08-19에 등록된 「앙상블 등급 임계 미도달」 항목의 고유
> ID**로 굳어져 `CLAUDE.md` 확률 판단 기준 각주 · 수집기 §12b 안건 열 · 라이브 로그
> 문구(`[EntryGate] … 실질 전면 금지(1-9)`) · 2026-08-28 주간회의 안건에 걸쳐 인용된다.
> 하루 안의 연번(대원칙 B-②)과 충돌하므로, 오늘 장중 신규는 `1-7`·`1-8`·**`1-10`**을 쓴다.

---

## 0-i. 지금 한눈에

> 이 절에는 약어·로그 태그·파일 경로·원칙 번호·단계 번호를 쓰지 않는다.

**지금 위험한 상태는 아닙니다. 오늘 오전 장사는 잘 됐습니다.**

오전에 13번 들어가서 9번 이겼고, 수수료를 빼고 **약 21만 원을 벌었습니다**(굴리는 돈
5천만 원 기준으로 0.42% 정도). 프로그램은 아침 9시부터 낮 12시 40분까지 **한 분도 빠짐없이**
돌았고, 멈추거나 다시 켜진 흔적이 없습니다. 손실을 볼 때 미리 정해둔 만큼만 잃고 빠져나왔는지
확인했는데, 두 번 다 거의 정확히 지켰습니다.

**다만 새로 알게 된 문제가 셋 있습니다. 셋 다 "돈이 새는" 문제가 아니라 "프로그램이 스스로를
잘못 알고 있는" 문제입니다.**

첫째, **오전 장이 크게 어긋났을 때 스스로 다시 공부하도록 만들어 둔 기능이, 사실상 절대
켜지지 않는 상태**입니다. 그 기능은 "최근 성적이 나쁘고, 그 판단에 쓸 성적표가 15장 이상
모였을 때"만 작동하는데, 성적표를 모으는 조건이 너무 까다로워서 **하루에 3장밖에 안 쌓입니다.**
지난 20일 치를 세어 보니 조건을 통과하는 비율이 100번에 4~5번꼴이었습니다. 그러니 15장은
영영 못 채웁니다. 오늘 그 기능이 한 번도 안 켜진 것은 "오늘 시장이 괜찮아서"가 아니라
**애초에 켜질 수 없어서**입니다. 어제까지는 이걸 알 수 없었는데, 어제 새로 붙인 기록 장치가
오늘 처음 돌면서 드러났습니다.

둘째, **주문 수량이 줄어든 이유를 프로그램이 잘못 적고 있습니다.** 오늘 세 번, 계산상으로는
3계약을 살 수 있었는데 실제로는 2계약만 들어갔습니다. 진짜 이유는 **증권사가 담보 부족으로
2계약까지만 허용**한 것인데, 기록에는 "품질 판단 장치 때문에 줄었다"고 적혔습니다. 지난
금요일 점검에서 "이 기록에 담보 한도 칸이 없다"는 건 이미 찾아내서 고치기로 해 뒀는데,
그때는 "빈칸으로 남는다"고 봤습니다. 오늘 보니 **빈칸이 아니라 엉뚱한 답이 적힙니다.**
빈칸은 모른다는 뜻이라 안전하지만, 엉뚱한 답은 아는 줄 알게 만들어서 더 나쁩니다.

셋째, **오늘 아침에 "개장 2분 만에 풀렸다"고 보고했던 문제가 사실은 안 풀렸습니다.**
15분쯤 반짝 정상이었다가 9시 22분부터 낮 12시 38분까지 계속 같은 상태였습니다.
아침 판단이 틀렸던 것이고, 그 판단이 왜 틀렸는지는 아침 기록을 그대로 남겨 두었습니다.

**지금 사람이 해야 할 일은 없습니다.** 장이 열려 있는 동안에는 프로그램을 건드리지 않는 것이
원칙이라, 고치는 것은 전부 오후 3시 10분 이후로 미뤄 두었습니다. 급한 것은 하나도 없습니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| **장전** | **조건부 정상** | (장전 절 판정 유지 — 고치지 않는다) |
| **장중** | **조건부 정상** | P0 0건. 매분 루프 221/221분(100%) · 강제 X 규칙 위반 0건 · 장중 코드변경 0건 · 손절 준수율 정상. 단 관측 계층에 구조적 결손 3건(1-7·1-8·1-10) |
| 장후 | — | (장후 세션이 채운다) |

---

## 이월 처리 (장전 → 장중)

> 대원칙 B-②. 장전이 남긴 **모든** 항목을 처분한다. 지속 항목에 새 번호를 붙이지 않는다.

### 이상점 (1-1 ~ 1-6)

| 번호 | 항목 | 처분 | 근거 |
|---|---|---|---|
| **1-1** | 단기 CORE `ofi_norm` 개장 직전까지 6호라이즌 identity 강제 | ↩️ **정정 → `1-10`** · 🔄 지속 | 장전이 「09:02 축퇴 0/6 — 해소」로 읽었으나 **일시적**이었다. 09:22 재발 → 12:38까지 **21/28 refit(75%)이 `축퇴 1/6 — ofi_norm×6hz`**. 개장 후 `identity(0,1) 강제` **126건**. 상세 `1-10` |
| **1-2** | 앙상블 확률 보정기 스냅샷 9거래일 정체 | 🔄 **지속 · 장후 이월** | 갱신 경로는 15:40 EOD다. 장중에 판정 불가 — 판정 수단은 `P-1`(`[Calibration] … 저장 완료 n=`)·`P-2`(`data/ensemble_calibrator.pkl` mtime) |
| **1-3** | 스킬 문서 3곳이 효력 종료된 CB② 기한 「2026-08-29」 인쇄 | 🔄 **지속** | 오늘 장중 다이제스트 §3 `CB_CONSEC_STOP_LIMIT` 행이 **여전히** `재검토 기한 2026-08-29`를 인쇄한다. F-1(491차)은 **장중 적용 금지** — 장후 적용 대기. 미조치가 아니라 **적용 시점 대기**다 |
| **1-4** | 장전 점검이 개장 36초 뒤 실행 (O-10 리셋) | ✅ **해소(장중 조건)** | 장중 수집기는 12:40:59 발화 — 장중은 창이 09:00~15:10이라 발화 마진 개념이 없다. 카운터 재시작은 **내일 장전**부터 |
| **1-5** | `GuardFair_유효` 7거래일 42표본 100% `무효` | 🔄 **지속 · 장후 이월** | GuardFair(모델 교체 시 신·구 공정비교) 판정은 EOD 산출물이다. 장중 표본 없음 |
| **1-6** | 리포 루트 산출물 2건 + 미커밋 5건 | 🔄 **지속** | 12:40 현재 동일(`gate_blocking_metrics.json`·`gate_blocking_report.md` mtime 08-23 17:56, `26주WFA_*` 08-24 08:03~08:10). **장중 커밋 금지**라 F-2·F-3은 장후 대기 |

### 장전 「확인 필요」 3건

| 항목 | 처분 | 근거 |
|---|---|---|
| 호라이즌 확률 보정기 24개 전부 「축퇴 감지」 + FloorShadow 24/24 판정 불일치 | ⏸ **보류 유지 — 확정 결론 금지(313차)** | 장중에 새 표본 없음(`[Calibration]` 축퇴 로그는 08:40:45 배치 1회). **무엇을 더 보면 닫히는가**: 08-25 EOD 캠페인 리포트 `[2-A]` AUC 표 · `[2-B]` 재개 게이트(O-30). **언제 닫는가**: 2026-08-25 장후 |
| 선물 투자자 수급 TR 미제공 (`futures supported=False`) | ✅ **해소 — 장전 판단이 시점 한정이었다** | 09:20:02에 **`futures supported=True source=CpSysDib.CpSvrNew7221 foreign=-872 individual=-338 institution=+1244`** 로 정상 수급이 들어온다. 09:20:02 `fetch#21 futures_supported=True program_supported=True option_supported=True`. **상시 제약이 아니라 08:58 시점 프로브 미완이었다** — 코드 열람 불필요 |
| `CybosProbe` 경고 18건 (전일 798건 대비) | ⏸ **보류 유지 — 장후 이월(O-5/491차)** | 장중 12:40까지 누계를 다시 세는 것은 여전히 부분 표본이다(계측 4원칙 ② 미측정≠0). 판정은 장후 다이제스트 총건수로 |

### 장전 「이미 반영된 사안」 6건

전부 **✅ 유지 — 재론하지 않는다**(함정 ①). 장중에 반박 증거 없음.
특히 `ZONE_ENTRY_BAN_ENFORCE=False`는 오늘 11:57 진입 1건(`A급(원시X)` · `time❌`)으로
**실제 위반 코호트 표본이 1건 늘었다** — 채널 `[53] zone_ban_breach_watch`의 입력이며
**집행을 켜자는 신호로 읽지 않는다**(462차 결정, 위반 코호트 흑자).

### 장전이 등록한 관측 O-1 ~ O-5(491차) + 486차·0821 이월분

| ID | 처분 | 근거 |
|---|---|---|
| **O-1(491차)** 다이제스트 §3 CB② 문구 | ⏭ **이월(2026-08-25 장전)** | F-1 미적용(장중 금지) → 오늘 판정 불가. 원 등록대로 |
| **O-2(491차)** 장전 마지막 `A_WARMUP` 축퇴 개수 | ⏸ **누적 — 오늘분 기록 완료(=1)** | 20거래일 축적 전 확정 결론·차단 승격 금지(313차). 오늘 2일차 |
| **O-3(491차)** 개장 후 **최초** `D_FORCE`/`B_OPEN` 축퇴 개수 | 🔴 **판정 — 기준이 구조적으로 결함이다** | 오늘 「최초」=09:01 `B_OPEN 축퇴 0/6`, 09:02 `D_FORCE 축퇴 0/6` → 기준상 **✅ 해소**. 그러나 09:22부터 재발해 끝까지 고착했다. **「최초」 1점만 재는 기준은 재발에 구조적으로 눈이 먼다.** → `1-10`으로 이상점화 + 기준 재설계를 `G-4(491차)`로 제안 |
| **O-4(491차)** `[Calibration] 축퇴 감지` + FloorShadow 불일치 비율 | ⏸ **보류 — 2026-08-25 EOD** | 위 「확인 필요」와 동일. 단독 확정 결론 금지 |
| **O-5(491차)** `[CybosProbe]` 총건수 | ⏭ **이월(오늘 장후)** | 부분 표본 |
| **P-3(486차)** `[DriftRetrain] state=` 매분 1줄 | ✅ **판정 — 통과(첫 라이브일)** | 09:00~12:40 **220줄 / 221분**. 값 분포 `state=SKIP`×228(당일 전량). ⚠ 이 로그는 **오늘이 라이브 1일차**다 — 08-18~08-21 로그에 `[DriftRetrain]` **0건**(485차 F-4 배선 이후 첫 거래일). **그 첫 줄이 곧바로 `1-7`을 드러냈다** |
| **P-4(486차)** §5 `[MarginCap]` 건수 == §12 `MarginCap_state` 표본 | ✅ **판정 — 통과(장중 시점)** | §5 `조회 29 · 축소 3` == 원본 로그 `state=OK`×26 + `state=CAP`×3 = **29**. §12 누계 56 = 08-21분 27 + 오늘 29. **F-5(485차) 정규식 신·구 양식 대응이 라이브에서 맞는다.** 전일 갈렸던 `3 vs 27`은 해소. ⚠ 최종 확인은 장후 전량 기준 |
| **O-22(0821)** WeightCollapse 비율 (밴드 20.3~20.5%±5%p) | ✅ **판정 — 밴드 내(장중 시점)** | `[WeightCollapse] 실질 가중합 0` **45건** / `[Ensemble] dir=` **221건** = **20.4%**. 사전등록 밴드 정확히 중앙. ⚠ 최종은 장후 |
| **O-6(0821)** `entry_qty_final` == `trades.entry_qty` 대사 | ➡ **장후 이월 — 단 오늘은 대사 케이스가 있다** | 0821에는 `state=CAP` **0건**이라 「케이스 부재」였다. 오늘 **CAP 3건** 발생 → 대사 가능. DB 실측 `entry_qty_final=2`(10:14·10:25·10:36 3행 전부) — 장후에 `trades.entry_qty`와 맞출 것 |
| **O-23(0821)** `sizing_trace.margin_cap_qty` 대사 | 🔄 **지속 — 선행 R-1 미배선** | 필드가 아직 없다(`main.py:9750~9765` 실측: `max_contracts`·`max_entry_qty`만 존재). 대사 불가. **오늘 그 결손이 `None` 아닌 오답을 낳는다는 새 증거 확보 → `1-8`** |
| **O-17(0821)** `entry_horizon` 소비처 전수 · **O-16/O-5/O-7/O-8/O-9/O-18~O-21/P-1/P-2/P-8/P-9** | ➡ **전부 장후 이월(기준 무변경)** | EOD 표본·코드조사 항목. 0821 §6p 정본 유지 |
| **O-26~O-29(489차)** `[56]`·`[44]`·`[46]` 채널 | ➡ **오늘 장후(EOD) 이월** | 판정 창 1일차, EOD 체인 산출 |
| **O-30~O-32(490차)** `[2-A]`/`[2-B]`/사이드카 | ➡ **2026-08-25 이월** | 원 등록대로. 앞당기지 않는다 |

---

## 1-i. 장중 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**해당 없음.** 아래 셋을 실측으로 확인했다.

- **절대원칙 ③ 강제 X 규칙 위반 0건.** 진입 13건 전부 `vwap✅` — `grep "진입체크" … | grep -c "vwap❌"` = **0**.
  (`ofi❌`×10 · `cvd❌`×9 는 **등급 하락** 규칙이며 강제 X가 아니다 — CLAUDE.md 절대원칙 §3 표)
- **절대원칙 ④ COM 콜백 크래시 0건.** `0xC0000409`·`STACK_BUFFER` 0건. 매분 루프 무중단.
- **절대원칙 ⑥ 알파 리서치 봇 자동 통합 0건.** `자동 통합`·`auto_integrate`·`백테스트 큐` 전 로그 0건.
  당일 미커밋 산출물(`26주WFA_*` 08:03~08:10 · `gate_blocking_*` 08-23 17:56)은 **장 시작 전
  사람이 돌린 오프라인 분석**이며 자동 통합 경로가 아니다.

### P1 — 정확성·관측 훼손

#### 1-7. 장중 자동 재학습(STEP 3)의 발동 조건 두 개가 **구조적으로 도달 불가**다 — 표본 게이트가 입력을 굶긴다

- **증상**: 매분 9단계 중 **STEP 3**(GBM 배치 재학습)의 장중 트리거 `DriftRetrain`
  조건A(`5m 정확도<25%` **and** `n≥20` **and** 60분 경과)·조건B(`<15%` **and** `n≥15`
  **and** 30분 경과)가, 오늘 **228개 상태 샘플 전부에서 `n≤3`** 이라 성립 자체가 불가능했다.
  ```
  09:01:00 [INFO] SYSTEM: [DriftRetrain] state=SKIP acc5m=0.0% n=0 mins_since_rt=3914 halted=False running=False cooldown_ok=True cond_a=False cond_b=False
  12:48:00 [INFO] SYSTEM: [DriftRetrain] state=SKIP acc5m=0.0% n=3 mins_since_rt=4141 halted=False running=False cooldown_ok=True cond_a=False cond_b=False
  ```
  `n` 값 분포(당일 전량) — **`n=3`×208 · `n=2`×9 · `n=1`×5 · `n=0`×6.** 3시간 47분 동안
  성적표가 **3장**에서 멈췄다. `acc5m`은 **228/228 전부 `0.0%`**.

- **근거 — `n`이 3에서 멈추는 이유는 두 겹의 표본 게이트다** (`main.py:5749~5750` →
  `learning/online_learner.py:394 horizon_acc_samples()` → `:177 _acc_buf[horizon].append()`):
  1. **신뢰도 하한 `conf ≥ 0.52`** — `main.py:9792 _min_conf_sgd = 0.52` (P2-D, 저신뢰
     레이블 오염 차단). 이 값 미만 예측은 `continue`로 학습·집계에서 제외된다.
  2. **봉단위 dedup** — `main.py:9808` 같은 호라이즌은 자기 봉 길이(5m=5분) 미만 간격이면
     스킵. ⇒ 100분 롤링 버퍼(`ACCURACY_WINDOW`)에 **최대 20슬롯**뿐이다.

  **두 게이트를 곱하면 조건A(`n≥20`)는 20슬롯 전부가 `conf≥0.52`여야 성립한다(100%),
  조건B(`n≥15`)는 75%가 필요하다.** 실측 통과율은 그 근처에도 못 간다 —
  `data/db/predictions.db` `horizon='5m'` 직독, 최근 20거래일(2026-07-25~08-24):

  | 구간 | 5m 예측 n | `conf≥0.52` 통과 | 통과율 |
  |---|---|---|---|
  | 2026-08-24 (12:40까지) | 90 | 4 | **4.4%** |
  | 최근 20거래일 합계 | 2,649 | 121 | **4.57%** |
  | 최고일 (2026-08-12) | 147 | 39 | **26.5%** |
  | 최저일 (07-28 · 08-14) | 68 · 146 | 0 · 0 | **0.0%** |

  **가장 좋았던 날조차 26.5%로, 조건B가 요구하는 75%의 3분의 1이다.**

- **기준 위반**: `CLAUDE.md` **매분 실행 파이프라인 STEP 3** 및 그 470차 각주.
  각주는 *"당일 `[GBM] 배치 재학습` 로그 0건은 결함이 아니라 조건 미충족(=정상)이다"*
  라고 문서를 코드에 맞췄다. **그 문장은 여전히 옳지만 오늘 실측이 그것을 한 단계
  좁힌다 — 「조건 미충족」이 그날그날의 우연이 아니라 상수다.**
  절대원칙 위반은 아니다(STEP 3은 절대원칙 6종에 없다).

- **영향**: 오전에 시장 방향이 확 바뀌어 예측이 계속 빗나가도, **스스로 다시 공부해서
  따라잡는 기능이 켜지지 않습니다.** 오늘처럼 지수가 크게 내린 날은 그 기능이 있으라고
  만든 바로 그 상황인데, 켜질 수 없는 상태였습니다. 오늘 손해를 봤다는 뜻은 아닙니다
  (오늘은 오히려 벌었습니다). 안전장치 하나가 서류상으로만 있었다는 뜻입니다.

- **영향(기술)**: DriftRetrain 경로가 사실상 사문화(dead branch)돼 STEP 3의 장중 축이
  **주간(월 08:50) + EOD(`daily_close`) 두 정기 경로만** 남는다. 303차 후속이 기준
  호라이즌을 30m→5m으로 교체하며 근거로 든 *"5m은 앙상블에 실제 반영되는 활성
  호라이즌"* 은 맞지만, 그때 **5m의 `conf≥0.52` 통과율은 검토되지 않았다** — 그 교체는
  2026-07-08 무렵이고 CLAUDE.md 476차가 확정한 *"방향성 conf는 2026-06-01 이후 0.60에
  한 번도 닿지 않았다(p50 0.3478 · p99 0.5073)"* 는 분포 급변 **이후**의 사실이다.
  ⇒ **`1-9`(앙상블 A/B 등급 임계 미도달)와 같은 뿌리다: 2026-05 이전 confidence
  분포에 맞춰 박힌 상수가, 분포가 내려앉은 뒤에도 그대로 남아 분기를 죽였다.**
  같은 유형: 471차 F-1(15:10 1차 경로 6개월 도달 불가) · 474차(중기·장기 CORE 그룹
  6개월 도달 불가) · 476차 1-9(앙상블 A/B 약 3개월).

- ⚠ **표본 규율 (313차)**: 상태 샘플은 **오늘 하루(1거래일)** 뿐이다 — 485차 F-4가
  배선한 뒤 **첫 라이브일**이기 때문이다(08-18·19·20·21 로그 `[DriftRetrain]` **0건**).
  다만 위 결론의 뼈대는 1일 표본이 아니라 **① 코드가 명시한 두 게이트 ② 20거래일
  2,649건의 통과율 분포** 두 가지이고, 1일 상태 샘플은 그 예측을 확인해 준 것이다.
  그럼에도 **임계 변경을 오늘 제안하지 않는다** — 아래 F-4(491차)는 계측만 붙인다.

- ⚠ **`acc5m=0.0%`를 「5분 예측이 다 틀렸다」로 읽지 말 것.** `n≤3` 표본에서 나온
  값이고, `horizon_accuracy()`는 표본 부족(<5) 시 0.0을 반환하도록 설계돼 있다
  (`main.py:5747~5748` 주석: *"표본 부족(<5) 시 horizon_accuracy()가 0.0을 반환해도
  n 게이트가 먼저 막아 오발동 방지"*). **0.0%는 정확도가 아니라 「미측정」의 표기다.**
  계측 4원칙 ②(미측정 ≠ 0)의 교과서 사례이며, 이 표기 자체가 개선 대상이다(G-5).

#### 1-8. 수량이 줄어든 원인을 기록이 **틀리게** 지목한다 — 증거금 상한 축소를 품질 게이트로 오귀속

- **증상**: 오늘 사이저가 3계약을 냈다가 2계약으로 체결된 3건에서, `sizing_trace`의
  `binding_gate`(무엇이 수량을 구속했는가)가 **`meta`**로 기록됐다. 그러나 그 3건에서
  품질 게이트는 수량을 **전혀 줄이지 않았다** — 실제로 줄인 것은 **브로커 증거금 상한**이다.

- **근거 ①(로그, 같은 분 두 줄이 서로 다른 말을 한다)**:
  ```
  10:37:00 [INFO] TRADE:  [Sizer] … 신뢰도배수=0.6 레짐배수=1.0 안전배수=1.00(정상) → 3계약 (최소=1)
  10:37:00 [INFO] TRADE:  [MarginCap] state=CAP SHORT 산출=3 상한=2
  10:37:00 [INFO] TRADE:  [MarginCap] SHORT 산출=3계약 → 증거금상한=2계약으로 축소
  10:37:00 [INFO] SIGNAL: [SizerMatch] sizer=3계약 → actual=2계약 (gap=1) | binding=meta(0.90) | kelly=0.36 meta=0.90 tox=1.00 exec=1.00
  ```
  `[MarginCap] 산출=3` 이 **품질 게이트 통과 후 수량이 3이었다**는 뜻이다.
  같은 분 `[SizerMatch]` 는 그 `3→2` 격차를 **`binding=meta(0.90)`** 로 귀속한다.
  대조군 — 품질 게이트가 실제로 구속한 분은 `산출`이 이미 줄어 들어온다:
  ```
  10:03:00 [Sizer] … → 3계약   /   [MarginCap] state=OK SHORT 산출=2 상한=2
  ```

- **근거 ②(DB, 구조체 안에서 자기모순이 보인다)** — `data/db/predictions.db`
  `ensemble_decisions.sizing_trace` 직독(WAL 포함 스냅샷):

  | ts | qty_sizer_raw | qty_display | qty_auto | binding_gate | quality_min | max_contracts | max_entry_qty |
  |---|---|---|---|---|---|---|---|
  | 2026-08-24 10:14 | 3 | **3** | **2** | `meta` | 0.90 | 3 | 3 |
  | 2026-08-24 10:25 | 3 | **3** | **2** | `meta` | 0.90 | 3 | 3 |
  | 2026-08-24 10:36 | 3 | **3** | **2** | `meta` | 0.90 | 3 | 3 |
  | (대조) 10:33 | 3 | 2 | 2 | `hurst` | 0.50 | 3 | 3 |

  위 3행은 **`qty_display(3) > qty_auto(2)` 인데 기록된 두 상한(`max_contracts=3`·
  `max_entry_qty=3`) 중 어느 것도 2로 자르지 않는다.** 구조체 안에 격차를 설명하는 값이
  하나도 없는데 `binding_gate`만 `meta`라고 답한다. 대조행(10:33)은 `hurst=0.50` →
  `3×0.5=1.5→2`로 계보가 맞는다.

- **근거 ③(원인은 코드 한 줄)** — `main.py:9750`:
  ```python
  "binding_gate": (min(_sz_q, key=lambda k: _sz_q[k]) if _sz_q else None),
  ```
  `_sz_q`는 **품질군 배수(`_quality_mults`)만** 담는다. 증거금 상한은 후보에 아예 없다.
  ⇒ 증거금이 구속한 분에도 함수는 **품질군 중 가장 작은 것**을 답으로 내놓는다. 그것이
  `1.00`이 아닌 값이면(오늘 `meta=0.90`) **그럴듯한 오답**이 되고, 품질군이 비어 있으면
  `None`이 된다.

- **근거 ④(오늘만의 일이 아니다 — 계측 전 구간 재집계)**: `sizing_trace` 보유 157행
  (2026-08-18 09:38 ~ 08-24 11:56) 중 **`qty_display > qty_auto` 이면서 기록된 두 상한이
  모두 미구속인 「미설명 축소행」이 13행**이다.

  | 일자 | 미설명 축소행 | 그 중 `binding_gate` 라벨 |
  |---|---|---|
  | 2026-08-18 | 1 | `None`×1 |
  | 2026-08-19 | 2 | `None`×2 |
  | 2026-08-20 | 4 | `None`×2 · **`meta`×2** |
  | 2026-08-21 | 3 | `None`×2 · **`meta`×1** |
  | **2026-08-24** | **3** | **`meta`×3 (전량)** |
  | **합계** | **13** | `None`×7 · **`meta`×6** |

  전체 `binding_gate` 히스토그램은 `hurst`×51 · `tox`×47 · `None`×31 · `meta`×28 이므로
  **`meta` 28건 중 6건(21.4%)이 오귀속**이다.

- **기준 위반**: `CLAUDE.md` 실전 전환 기준 **⑧**의 근거 채널 `[28] sizing_inversion_watch`.
  그 채널의 `structural_reason`은 `scripts/generate_validation_campaign_report.py:2544`에서
  `binding_gate`를 읽어 `binding_gate_hist`를 만든다. 수집기 §12 고착 지표의
  `binding_gate` 행도 같은 값을 센다. 9단계로는 **STEP 7**(진입 실행) 계측이다.

- **⚠ 함정 ① 확인 — 신규 안건이 아니다. 등록된 항목의 전제 정정 + 격상이다.**
  2026-08-21 리포트 `R-1`이 **이미** `sizing_trace`에 `margin_cap_qty`·`margin_binding`
  두 필드를 추가하자고 제안했고, `dev_memory/NEXT_TODO.md:15139`에 `O-23`으로 등록돼
  있다. **미배선 확인**: `grep -rn "margin_cap_qty" --include=*.py` → main.py **0건**,
  `tests/test_485_sizing_trace_margin.py` **미생성**. 즉 **적용 대기 상태**이지 미조치가
  아니다. 오늘이 바꾸는 것은 딱 하나 —
  > 0821 R-1 원문: *"오늘 CASE-04의 binding을 `None`으로 기록했다(**틀린 것이 아니라
  > 볼 수 없는 것**)"*
  ↩️ **그 전제가 정정된다. 2026-08-20 시점에 이미 `meta` 오답 2건이 있었고, 오늘은 3건
  전량이 오답이다. 「볼 수 없는 것」이 아니라 「틀린 것」이 실제로 기록되고 있다.**
  ⬆️ 그래서 R-1의 심각도를 **P2 → P1**로 올리고, 필드 2개 추가에 **불변식 테스트 1개**를
  더한다(F-5).

- **영향**: 주문 수량이 줄어들 때마다 "왜 줄었는지"가 장부에 적히는데, **담보가 모자라서
  줄어든 것을 "판단 장치가 깎았다"고 잘못 적고 있습니다.** 나중에 "진짜 돈을 넣어도
  되는가"를 따질 때 바로 이 장부를 근거로 씁니다. 빈칸이면 "모른다"고 알기라도 하는데,
  틀린 답이 적혀 있으면 안다고 착각한 채로 결정하게 됩니다.

- **영향(기술)**: `[28]`의 `structural_reason`이 증거금 구속을 품질 게이트 구속으로
  분류 → 실전 전환 기준 ⑧ 해제 판단의 입력이 편향된다. 470차가 규명한
  *"⑧ 해제 → 증거금 상한 상승 → `qty≥3` 발생 → [28] 표본 축적 → 판정"* 순서에서,
  ⑧ 해제 직후 증거금이 binding이 되는 빈도가 오르는 **바로 그 시점에 오귀속률이 함께
  오른다.** 477차 후속 F-1(`entry_qty_final`)·G-2(`MarginCap_state` 무조건 샘플)가
  «수량»과 «상태»는 복구했지만 **«귀속»은 아직 복구되지 않았다.**

#### 1-10. 장전 CORE 축퇴는 「개장 후 해소」가 아니었다 — 15분 반짝 뒤 장중 전 구간 재발 (↩️ `1-1` 정정)

> `1-9`는 쓰지 않는다(이 절 머리 「번호 규약 메모」 참조).

- **증상**: 장전 절 `1-1`과 `§0` 넷째 문단은 `ofi_norm` 축퇴를 *"개장 2분 뒤에야 정상으로
  돌아왔습니다"* 로 기록했다. **09:01~09:17 구간(refit 7회)만 그랬다.** 09:22부터 12:38까지
  다시 축퇴 상태이며, 그 사이 해소된 적이 없다.

- **근거 (오늘 `[CORE준비도]` 전량 — 개장 후 28회 중 21회가 축퇴)**:
  ```
  09:01:00 [CORE준비도] trigger=B_OPEN  축퇴 0/6 — CORE 스케일 정상      ← 장전이 본 지점
  09:02:00 [CORE준비도] trigger=D_FORCE 축퇴 0/6 — CORE 스케일 정상
  …(09:07·09:10·09:12·09:16·09:17 전부 0/6)…
  09:22:00 [CORE준비도] trigger=D_FORCE 축퇴 1/6 — ofi_norm×6hz | 정상 진입 전 해소 여부를 확인하라   ← 재발
  09:30·09:31·09:50·10:11·10:31·10:32·10:53·11:14·11:18·11:23·11:28·11:31·11:33·11:34·11:38·11:54·11:55·12:16·12:31·12:38
      → 전부 `축퇴 1/6 — ofi_norm×6hz`
  ```
  개장 후 `[ScalerRefresh] … CORE 'ofi_norm' raw_std≈0(0.0337) → identity(0,1) 강제
  (FLAT 100% 방지)` **126건**. 마지막 관측 12:38:00(10m·15m·30m).

- **근거 (다일 대조 — 오늘이 예외가 아니다. 08-21이 예외였다)**:

  | 거래일 | 개장 후 refit | 그 중 축퇴 ≥1 | 비율 |
  |---|---|---|---|
  | 2026-08-18 | 37 | 25 | 68% |
  | 2026-08-19 | 36 | 36 | **100%** |
  | 2026-08-20 | 36 | 26 | 72% |
  | 2026-08-21 | 33 | **0** | **0%** |
  | 2026-08-24 (12:40까지) | 28 | 21 | 75% |

- **기준 위반**: `CLAUDE.md` **절대원칙 §3**(CORE 피처 — 호라이즌 그룹별 분리).
  `ofi_norm`은 **단기 그룹(1m·3m·5m) CORE**이고 미통과 규칙은 **등급 하락**이다
  (강제 X는 `vwap_position`뿐). 오늘 진입 13건은 전부 `entry_horizon` 3m·5m = 단기 그룹.
  ⇒ **위반은 아니다** — 강제 X 규칙은 지켜졌고(위 P0 절), 등급 하락도 규칙대로 걸렸다
  (진입 13건 중 `ofi❌` 10건, `C급` 6건). 걸린 것은 **관측 기준**이지 절대원칙이 아니다.
  9단계로는 **STEP 4**(피처 생성) → **STEP 5**(예측) 경로.

- **⚠ 인과를 단정하지 않는다.** `identity(0,1) 강제`는 **모델 입력 스케일러**(STEP 5)를
  중립화하는 것이고, 체크리스트 `5_ofi`(STEP 6)는 별도 경로다. 오늘 `ofi❌`가 진입 13건 중
  10건인 것과 축퇴 고착은 **정합적이되 인과 증명이 아니다.** 확정 결론 금지(313차).

- **영향**: 프로그램이 방향을 고를 때 쓰는 세 가지 핵심 재료 중 하나가, 오늘 오전 내내
  사실상 빠진 채로 판단이 이뤄졌습니다. **오늘 성적이 나빴다는 뜻은 아닙니다** — 오히려
  벌었습니다. 문제는 아침 보고가 "괜찮아졌다"고 적었다는 것이고, 그 판단이 틀렸다는 것을
  지금 알게 됐다는 점입니다.

- **영향(기술)**: `O-3(491차)`의 판정 기준이 *"개장 후 **최초** `D_FORCE`/`B_OPEN` 축퇴
  개수"* 라 **1점 표본**이다. 오늘 그 1점(09:01·09:02)은 0/6이었고 기준상 ✅ 해소로
  닫힌다 — **재발에 구조적으로 눈이 멀었다.** 468차 G-2가 등록한
  *"안전장치가 한쪽 값에 붙박여 죽어 있는데 매번 사람이 뒤늦게 발견했다"* 의
  거울상이다(이쪽은 「한 번 좋았던 값만 보고 닫는다」). 수집기 §12
  `CORE준비도`·`CORE축퇴_피처`는 **하루 전체 분포**를 세므로 이 결손을 보완하지만,
  「장전 상태가 개장 이후까지 이어졌는가」라는 **O-3의 질문 자체**는 여전히 안 잰다.

### P2 — 운영 부담·기술부채

#### 1-11. 같은 이름 `acc30m`이 두 원천에서 서로 다른 값을 낸다 — 인용 시 원천 표기 필요

- **증상**: 같은 30분 정확도를 두 곳이 다르게 읽는다.
  ```
  10:37:00 [INFO]  SYSTEM: [Contrarian] ARMED | 조건 2/3 | acc30m=0.1% streak=10 regime=RISK_ON
  12:40:02 [DEBUG] DEBUG:  [DBG-CB] state=NORMAL consec_stops=0 acc30m=53.3% latency=0.214s
  ```
- **근거**: `CLAUDE.md` 절대원칙 §3 각주가 두 소비처를 명시한다 — `main.py:5539`가
  30m 채점을 `circuit_breaker.record_accuracy()`에 먹이고(=`[DBG-CB] acc30m`),
  `main.py:6203`이 `pred_buffer.recent_accuracy("30m", …)`로 Contrarian 판단을 만든다
  (=`[Contrarian] acc30m`). **버퍼가 다르므로 값이 다른 것은 설계상 정상**이다.
  당일 `[DBG-CB] acc30m` 분포: `53.3%`×54 · `0.0%`×39 · `9.5%`×30 · `43.3%`×27 ·
  `15.4%`×23 · `13.6%`×15 · `100.0%`×9 · `56.7%`×3 — 변동한다(고착 아님).
- **기준 위반**: 없다. **명명 규약**의 문제다. 461차가 `mdd_pct`(자본 대비 vs peak 대비)에서
  같은 사고를 겪고 `mdd_pct_of_capital`로 이름을 갈랐다 — 그 교훈이 이 축엔 적용되지 않았다.
- **영향**: 보고서나 화면에서 "30분 정확도"라는 같은 말이 두 가지 다른 숫자를 가리킬 수
  있습니다. 지금 손해가 나는 건 아니지만, 나중에 둘을 섞어 쓰면 잘못된 결론이 나옵니다.
- **영향(기술)**: CB③(30분 정확도 기반 당일 정지)의 임계 `CB_ACCURACY_MIN_30M=0.28`은
  **CB 버퍼 기준**이다. Contrarian 버퍼 값으로 CB③를 논하면 461차 `mdd_pct` 사고의 재판이 된다.
- ⚠ **확정 결론 금지(313차)**: `[Contrarian]` 로그는 오늘 **1건**뿐이다. 두 버퍼의 계통적
  차이인지 소표본 변동인지 판별 불가 → `O-8(491차)`로 등록만 한다.

#### 1-12. 30m 온라인 학습기가 하루에 4번 「단방향 붕괴 → 자동 리셋」을 반복한다 — CB③의 입력원이다

- **증상**: 30m SGD(온라인 학습기)가 예측을 한 방향으로만 쏟아내 자동 리셋된 것이 오늘 5건
  (그 중 30m이 4건, 12:42 포함 5건째).
  ```
  09:43:00 [OnlineLearner] 30m SGD DN붕괴 자동 복구 (≥80% 12분 지속) → 모델·스케일러 리셋
  11:12:00 [OnlineLearner] 30m SGD DN붕괴 자동 복구 …
  11:42:00 [OnlineLearner] 30m SGD UP붕괴 자동 복구 …
  12:12:00 [OnlineLearner] 30m SGD DN붕괴 자동 복구 …
  12:42:00 [OnlineLearner] 30m SGD DN붕괴 자동 복구 …
  ```
  약 30분 주기다. 전 호라이즌 합계 8건(5m 1 · 15m 2 · 30m 5).
- **근거·대조**: `grep -c "30m SGD" 20260821_LEARNING.log` = **6**(전일 전체) vs 오늘 **5**
  (12:40까지). ⇒ **신규 현상이 아니라 상시 상태다.**
- **기준 위반**: 없다(자동 복구는 설계된 방어다). 다만 `CLAUDE.md` 절대원칙 §3 각주
  *"30m 학습을 중단하면 안 된다 — CB③의 유일한 입력원이다"* 와 맞물린다.
- **영향**: 30분짜리 예측을 담당하는 부분이 하루에 네댓 번 백지에서 다시 시작합니다.
  그 예측은 "오늘 장사를 멈춰야 하나"를 판단하는 자동 정지 장치의 유일한 재료입니다.
- **영향(기술)**: 리셋 직후 30m 예측은 콜드스타트 상태이므로, `[DBG-CB] acc30m`이
  `0.0%`(39분)와 `100.0%`(9분)를 오가는 것과 정합적이다. CB③ 임계 0.28에 대해
  **정지 여부가 리셋 타이밍에 좌우될 수 있다**. 오늘 `CB_state`는 전 구간 `NORMAL`이라
  실해는 없었다.
- ⚠ **확정 결론 금지(313차)**: 2거래일 대조뿐이고, 리셋과 CB③ 판정의 결합은 미검증.
  `O-9(491차)`로 등록 — 20거래일 누적 후 「리셋 직후 30분 내 `acc30m<0.28` 출현률」로 판정.

---

## 2-i. Fix 작업 구현계획 (장중 — **수립만 하고 적용하지 않는다**)

> 🟢 **라이브 프로세스가 09:00부터 매분 루프를 돌고 있다(중단 없음, 커버리지 100%).**
> 아래 전부 **15:10 강제청산 이후** 적용을 권고한다. 오늘 장중 코드 변경 **0건**을 실측 확인했다
> (`find . -name "*.py" -newermt "2026-08-24 09:00"` → **0건**).
> ⚠ 커밋은 `invariants.md` §0-B의 rename 우회로만 시도한다(이 마운트는 `unlink` 금지).
> ⚠ **장전이 세운 F-1·F-2·F-3(491차)은 그대로 유효하다.** 아래는 그 뒤에 붙는다.

### F-4(491차). 장중 자동 재학습 조건의 **도달 가능성**을 매일 계측한다 — **P1** · 대응 이상점 **1-7**

| 항목 | 내용 |
|---|---|
| **원칙** | 🔴 **임계도 조건식도 건드리지 않는다.** `_min_conf_sgd=0.52`·`n≥20`·`n≥15`는 **전부 무변경**. 표본 게이트 완화는 **학습 위생 정책 변경**이고 490차 P0(학습 위생 3종)와 정면으로 얽힌다 — 주간회의 소관 |
| **변경 대상** | ① `main.py:5791` `[DriftRetrain]` 상태 샘플에 **필드 2개 추가** — `n_cap=%d`(그 시점 100분 롤링 창의 dedup 이론 상한 = `100 // HORIZONS['5m']` = 20)와 `conf_pass_rate=%.3f`(당일 누계 `conf≥0.52` 통과율). 조건식은 읽지 않는다, **로그 문자열만 늘린다** ② 수집기 §12b(임계-분포 대조)에 임계쌍 **`DriftRetrain조건B(n≥15)`** 추가 — `max(관측 n)` vs `15`. 파일 `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py`, `threshold_pairs` |
| **왜 계측부터인가** | §12b는 `앙상블A(conf≥0.70)`·`TOX스프레드(≥20틱)`를 이미 그렇게 다룬다 — **"임계에 닿지 않는다"가 매일 자동으로 보이게** 하고, 처분은 주간회의로 올린다. 1-7은 그 목록에 들어가야 할 세 번째 항목이다 |
| **회귀 위험** | 낮음. 로그 문자열 확장 + 수집기 설정 추가. **판정식·집행 경로 무변경** |
| **검증** | `tests/test_491_driftretrain_reachability.py` — (a) `[DriftRetrain]` 줄에 `n_cap=`·`conf_pass_rate=` 존재 (b) `n_cap == 100 // HORIZONS['5m']` 불변식 (c) 수집기 §12b가 새 임계쌍을 렌더 |
| **판정 예정** | 20거래일 누적 후 §12b가 `📌 미도달(N일 연속)`을 안정적으로 찍으면 **2026-09-26 주간회의 안건**으로 올린다. ⚠ 그 전에 임계 완화 금지 |

### F-5(491차). `sizing_trace`에 증거금 축을 넣고 **오답을 못 쓰게 막는다** — **P1** · 대응 이상점 **1-8** · 0821 `R-1` 격상판

| 항목 | 내용 |
|---|---|
| **원칙** | ⚠ **사이징 산출식은 건드리지 않는다.** 계측 축만 손댄다(0821 R-1 원칙 승계) |
| **변경 ①(0821 R-1 그대로)** | `main.py:9750~9765` `decision["sizing_trace"]`에 `margin_cap_qty`(그 분 `get_order_available_qty()` 반환값, 미조회 분은 **NULL** — 계측 4원칙 ②)와 `margin_binding`(bool) **2필드 추가** |
| **변경 ②(오늘 추가분 — 이것이 격상의 실체)** | `binding_gate` 산출을 **오답 불가**로 바꾼다. `main.py:9750` 현행 `min(_sz_q, …)`는 품질군 argmin을 무조건 답으로 낸다. → **`qty_display > qty_auto` 인데 어떤 품질 배수로도 그 격차가 설명되지 않으면 `binding_gate = "margin"`**(또는 `margin_binding` 미확정 시 `None`)을 쓰도록 분기. ⚠ 품질군이 실제로 구속한 경우의 동작은 **완전 무변경** |
| **변경 ③** | `scripts/generate_validation_campaign_report.py:2544~2560` `[28]` `binding_gate_hist`에 `margin` 버킷을 인식시킨다. 수집기 §12 `binding_gate` 행의 `왜 보는가` 문구에 **"`margin` 출현은 증거금이 구속한 것 — ⑧ 해제 논의의 직접 입력"** 한 줄 추가 |
| **소급** | 🔴 **과거 157행을 소급 재라벨하지 않는다.** 계측 4원칙 ②. 대신 `measured_since` 마커로 **2026-08-25~** 를 새 계열로 표기하고, 그 이전 `meta` 28건 중 6건이 오귀속임을 `DECISION_LOG`에 남긴다 |
| **회귀 위험** | 중. `binding_gate` 값 도메인이 늘어 `[28]`·§12 소비처 2곳이 영향받는다. **차단·사이징 경로에는 닿지 않는다** |
| **검증** | `tests/test_491_sizing_trace_margin.py` — (a) 두 필드 존재 + NULL 규약 (b) **불변식**: `qty_display > qty_auto` 이고 `quality_min × qty_display` 반올림이 `qty_display`와 같으면 `binding_gate != <품질게이트명>` (오늘 3행이 이 테스트를 **깨야** 한다 — 회귀 고정) (c) 상한 미구속 확인 |
| **라이브 검증** | 다음 `state=CAP` 발생일에 `sizing_trace.margin_cap_qty == trades.entry_qty` **대사** → **O-23 종결**. 오늘 이미 대사 케이스 3건이 발생했으므로 배선 후 첫 CAP일은 멀지 않다 |
| **표본 규율** | 🔴 이 Fix는 **처방이 아니라 등록**이다(`invariants.md` §4-3). **`[28]`의 합격선·`min_samples=20`·`min_days` 전부 무변경.** 0821 R-1이 사전등록한 재판정 조건(**`state=CAP` 진입 20건·5거래일 도달 시 `margin_binding` 코호트 일자단위 부호검정**)도 그대로 승계한다 — 문턱을 낮춰 앞당기지 않는다 |

### F-6(491차). `O-3(491차)`의 판정 기준을 「최초 1점」에서 「개장 후 전 구간」으로 바꾼다 — **P2** · 대응 이상점 **1-10**

| 항목 | 내용 |
|---|---|
| **변경 대상** | 문서 1곳뿐이다 — 오늘 리포트 §6-B의 `O-3(491차)` 정의와 `dev_memory/NEXT_TODO.md` 491차 관측 항목. **코드 변경 없음** |
| **바꾸는 내용** | 「개장 후 **최초** `D_FORCE`/`B_OPEN` 축퇴 개수」 → 「개장 후 **전** refit의 축퇴율(축퇴≥1 건수 / 총 refit) **와** 최장 연속 축퇴 구간」. 오늘 실측 **21/28 = 75% · 최장 연속 09:22~12:38** |
| **왜** | 「최초」는 1점 표본이라 **재발에 구조적으로 눈이 멀다.** 오늘 정확히 그 사고가 났다(09:01 0/6 → 기준상 ✅ → 실제로는 3시간 16분 고착) |
| **회귀 위험** | 없음(문서). 단 **O-2(491차)와 축이 겹치지 않게** 할 것 — O-2는 **장전** 마지막 값, O-3은 **개장 후** 분포 |
| **검증** | 내일(2026-08-25) 장중 점검이 O-3을 축퇴율로 보고하는지 |

### 적용 순서와 커밋 계획 (전부 15:10 이후)

1. **F-1(491차)** 스킬 문서 죽은 CB② 기한 → 장전 계획 그대로 (P1, 선행 없음)
2. **F-5(491차)** `sizing_trace` 증거금 축 + `binding_gate` 오답 차단 (P1, 회귀 테스트 필수)
3. **F-4(491차)** DriftRetrain 도달성 계측 (P1, 로그·수집기)
4. **F-6(491차)** O-3 기준 재정의 (P2, 문서)
5. **F-2·F-3(491차)** 리포 루트 정리 · 26주 WFA 산출물 커밋 (P2)

- 커밋 제목 접두 **`[MW0602]`**, 세션 차수 **491차** 유지(같은 날 같은 세션 계열).
- ⚠ **재학습 코드에는 닿지 않는다** — F-4는 `main.py` 로그 문자열과 수집기만 만진다.
  `retrain_intraday.py`·`retrain_eod.py`는 **`py310_64` 전용**이며 이번 범위 밖(191차).
- ⚠ **COM 콜백 안에 `dynamicCall`·`emit`을 넣지 않는다**(절대원칙 ④). 위 5건 전부 콜백 밖.

---

## 3-i. 고도화 방안 (당일 관측에서 출발)

> ⚠ 장전이 이미 세운 **G-1·G-2·G-3(491차)** 은 유효하다. 아래는 장중 관측에서 나온 것만.
> ⚠ `config/settings.py:VALIDATION_CAMPAIGN_DECISIONS` 19건 · 수집기 §13 확정 결정
> 레지스트리와 대조했다 — **아래 어느 것도 그 표의 키와 같은 질문이 아니다**(함정 ①).

### G-4(491차). 「도달 불가 분기」를 사람 손이 아니라 **§12b가** 찾게 한다

- **출발점**: 오늘 1-7은 **어제 붙인 계측이 첫날 곧바로 드러냈다**. 반대로 471차 F-1(6개월)·
  474차(6개월)·476차 1-9(3개월)는 전부 사람이 우연히 찾았다. 차이는 **무조건 상태 샘플이
  있었는가** 하나다.
- **제안**: §12b(임계-분포 대조)의 임계쌍을 **손으로 등록하는 목록**에서 **코드에서 뽑는
  목록**으로 옮긴다. `config/settings.py`의 `*_MIN`/`*_MAX`/`*_LIMIT`/`*_TICKS` 상수와
  그것을 받는 DB 컬럼·로그 필드를 이름 규칙으로 짝지어, 매일 `max(관측) vs 임계`를 찍는다.
  짝을 못 지으면 **`미짝지음`으로 표시**한다(무기록과 같은 취급).
- **왜 26주가 아니라 지금인가**: 468차 G-2가 *"감시 대상 목록 자체가 낡는 것은 일일 점검이
  못 잡는다"* 를 26주 항목으로 등록했다. 그것은 **기존 지표의 노후**를 다루고, 이쪽은
  **새 임계가 목록에 안 들어오는 것**을 다룬다. 1-7이 오늘 §12b에 없었던 이유가 그것이다.
- ⚠ **관측 전용. 판정 기준 무변경.** 오탐이 많을 것이므로 §12처럼 `benign` 화이트리스트를 둔다.
- **선행**: F-4(491차)가 손으로 등록하는 첫 사례를 만든 뒤에 자동화한다 — 순서를 바꾸지 않는다.

### G-5(491차). 「미측정 0.0%」와 「진짜 0.0%」를 로그에서 구분한다

- **출발점**: 오늘 `[DriftRetrain] acc5m=0.0%`가 228/228 전부였고, 그 값은 **정확도가 아니라
  표본 부족(<5)의 표기**였다(`main.py:5747` 주석이 그렇게 설명한다). 점검자가 이 줄만 보면
  **"5분 예측이 하루 종일 다 틀렸다"** 로 읽는다 — 실제로 이 리포트를 쓰면서 한 번 그렇게
  읽을 뻔했다.
- **제안**: 표본 부족 시 `acc5m=0.0%`가 아니라 **`acc5m=n/a(n<5)`** 로 찍는다.
  적용 범위는 `horizon_accuracy()` 계열 소비처 전부(`[DriftRetrain]`·`[SGD] 50분정확도`·
  `[DBG-CB] acc30m`). **반환값은 바꾸지 않는다 — 표기만 바꾼다**(조건식이 `0.0`을 읽는
  경로가 있으므로).
- **왜**: 계측 4원칙 ②(미측정 ≠ 0)가 **데이터 축에는 적용됐는데 로그 문자열 축에는
  적용되지 않았다.** 470차가 `sizing_trace` NULL 규약으로 같은 일을 했고, 477차 G-1
  `measured_since` 마커도 같은 계열이다.
- ⚠ 오늘 1-12에서 `[DBG-CB] acc30m=0.0%`×39·`100.0%`×9를 관측했는데, 그 중 몇이
  「미측정」인지 **지금은 구분할 수 없다.** 이 제안은 그 구분을 만든다.

### G-6(491차). 진입 방향 100% 편향을 「그날 시장」과 나눠 보는 축을 만든다

- **출발점**: 오늘 진입 **13건 전부 SHORT**다. 앙상블 방향도 `dir=-1`×110 vs `dir=+1`×21
  (전일 08-21은 `-1`×124 vs `+1`×88으로 균형). 오늘 지수는 시가 1087.98 → 11:04 진입가
  1061.42까지 내렸으므로 **하락장에서 숏 편향은 정합적**이고, 결과도 net **+209,082원**이다.
- **제안**: `O-19(0821)` 「진입 방향 편향」을 **당일 지수 방향으로 정규화**한 축으로 바꾼다 —
  `숏 진입 비율 − (당일 음봉 분 비율)`. 0이면 시장을 따라간 것, 크게 양수면 편향이다.
- **왜 지금인가**: 100% 한 방향은 그 자체로는 편향의 증거가 아니다. 그런데 지금 축은
  비율만 재므로 **하락일마다 "편향 100%"가 찍혀 늑대소년이 된다.** 1-12의 30m SGD
  단방향 붕괴(하루 5회)와 혼동될 여지도 있다 — 그쪽은 학습기 내부, 이쪽은 진입 결과다.
- ⚠ **관측 축 재정의일 뿐 차단·게이트 신설이 아니다.** 합격선 없음(추세만 본다).
- ⚠ 확정 결론 금지(313차) — 오늘 1거래일 표본이다.

### 로드맵·실전전환 기준 반영 제안

- **실전 전환 기준 ⑧**: 1-8이 ⑧의 근거 채널 `[28]`의 **입력 무결성** 문제이므로,
  ⑧ 해제 체크리스트에 **"`sizing_trace.binding_gate`에 `margin` 버킷이 존재하고
  `qty_display>qty_auto` 미설명행이 0인가"** 를 F-5 배포 후 선행 확인으로 추가할 것을
  제안한다. ⚠ **⑧의 해제 조건 자체는 바꾸지 않는다**(489차 D6의 선행조건 ①·④ 유지).
- **CLAUDE.md STEP 3 각주(470차)**: 1-7 확정 시 *"조건 미충족(=정상)"* 옆에
  **"단 조건A·B는 `conf≥0.52` 표본 게이트로 인해 현행 confidence 분포에서 도달 불가"** 를
  병기할 것을 제안한다 — 468차 CB③·476차 등급 각주와 **같은 형식**(문서를 실측에 맞추고
  코드는 바꾸지 않는다). ⚠ **주간회의 소관.** 오늘 CLAUDE.md를 고치지 않는다.

---

## 4-i. 매분 9단계 판정 (09:00 ~ 12:40)

| STEP | 내용 | 판정 | 근거 |
|---|---|---|---|
| **루프 생존** | 매분 루프 커버리지 | ✅ **정상** | `20260824_SYSTEM.log` **221/221분 = 100.0%** · 08:55~12:40 **10분 이상 공백 0건** · 3분 이상 공백도 관측 안 됨 · 프로세스 무응답·재기동 흔적 0 |
| **STEP 1** | 과거 예측 채점 (T-1·3·5·10·15·30) | ✅ **정상** | `[DBG-F6] horizons: 1m:-1@37% \| 5m:+0@52% \| 10m:-1@39% \| 30m:-1@45%` 매분 · **T-30 채점 지속 확인**(`[DBG-CB] acc30m` 이 8개 값으로 변동 — 고착 아님). CLAUDE.md 474차 각주(30m은 CB③의 유일한 입력원) 준수 |
| **STEP 2** | SGD 온라인 자가학습 | 🟠 **조건부** | `[SGD] N건 학습` **238건** 정상 발행. 단 30m 단방향 붕괴·리셋 **5회**(1-12) · 5m 정확도 버퍼 `n≤3` 고착(1-7) |
| **STEP 3** | GBM 배치 재학습 (조건부 트리거) | 🔴 **이상 — 조건 도달 불가** | `[DriftRetrain] state=SKIP` **228/228**. `cond_a=False cond_b=False` 전량. **1-7.** ⚠ 주간(월 08:50)·EOD 정기 경로는 별개이며 오늘 08:55 `[PreRetrain] 스킵`은 정상(장전 절 기록) |
| **STEP 4** | 피처 생성 | 🟠 **조건부** | NaN·결측 로그 **0건**. 수급 TR은 09:20:02에 `supported=True`로 정상화(이월표). 단 `ofi_norm` identity 강제 **126건**(1-10) |
| **STEP 5** | 멀티 호라이즌 예측 | ✅ **정상** | `[Ensemble] dir=` **221건**(= 루프 분수). 1·3·5·10·15·30m 발행. `WeightCollapse` 45건 = **20.4%** — `O-22` 사전등록 밴드(20.3~20.5%±5%p) **내** |
| **STEP 6** | 앙상블 진입 판단·등급 | ✅ **정상(규칙 준수)** | 등급 `A급(원시C)`×6 · `A급(원시X)`×1 · `C급`×6. **`vwap❌` 진입 0건** → 절대원칙 ③ 강제 X 준수. 차단 48건/26종 — 최다 `등급X — 미통과: 2_confidence`×19(39.6%), 다음 `청산 후 쿨다운`×12 · `OPEN_VOLATILE 시가이격 과다`×6 · `JointGateBlock`×2. **한 게이트 압도 없음**(316차 HurstGate 63% 같은 패턴 아님) |
| **STEP 7** | 진입 실행 | 🟠 **조건부** | 진입 13건 전부 체결(`[체결진입]` 13). 사이저 3계약 출력 10회 → 실제 최대 2계약. 실효 상한: 품질게이트 `[SizerMatch]` 4건 · 증거금 `[MarginCap] state=CAP` 3건. **귀속 오류 1-8** |
| **STEP 8** | 청산 트리거 5단계 | ✅ **정상** | 순서 유지 — 하드스톱(`[TickStop]`×9) → 손절계단화(`[TickLossTier1]`×3 · `[TickLossTier1Qty1]`×6) → TP1~TP3(`[TickTP1]`×11 · `[SingleContractTP1]`×9 · `[IntrabarTPSchedule]`×2). 15:10·15:18은 **시각 미도래** |
| **STEP 9** | 예측 DB 저장 | ✅ **정상** | `ensemble_decisions` 당일 `sizing_trace` 비-NULL **29행** = `[Sizer]` 호출 29건과 일치. `predictions` 5m **90행** |

### 손절 준수율 (417차 축 — 유일하게 유의했던 관계)

> 「하드스톱」 라벨 13건 중 **진짜 손절은 2건**이다. 나머지 8건은 `[TP1보호]` = TP1 도달 후
> 보호 스톱 = **이익 청산**이며, 손절로 세면 안 된다(465차 `tp1_reached`, 468차 로그 태그).

| 진입 | 계약 | 진입 평균가 | 의도 손절가 | 의도 폭 | 실현(최종 레그) | **준수율** |
|---|---|---|---|---|---|---|
| 10:37 | 2 | 1072.13 | 1075.49 | 3.36pt | −3.35pt | **0.997** |
| 10:50 | 1 | 1071.50 | 1074.08 | 2.58pt | −2.70pt | **1.047** |

⇒ 초과 4.7%가 최대. **417차가 지목한 "큰 포지션이 손절선을 못 지킨다"(1계약 29.4% vs
3계약+ 68.8%)의 재현 없음.** ⚠ **n=2 — 확정 결론 금지(313차).** 장후에 전량 재집계할 것.

### 손익 (12:40 기준 · **포지션 단위**)

- **포지션 13건**(청산 레그 18건 = 최종 13 + 부분 5) · 승 **9** / 종료 13 = **69%**
- 합계 **+4.76pt** · gross **+238,000원** − 수수료 **28,918원** = **net +209,082원**
  (`SIZING_TARGET_CAPITAL_KRW` **5천만원 대비 +0.42%**)
- 수수료/|gross| = **12%**
- 종가 포지션: 12:02:20 최종청산 이후 **FLAT**
- ⚠ **부분청산 5건 합계 −189,051원은 포지션 손익의 일부다** — 최종청산만 더하면 어긋난다.
- ⚠ **오늘 하루로 실전 전환 기준 ①(4주 통산 양수)을 논하지 말 것.** 489차 D6이 최근
  20거래일 **−178,004원**·양수일 **8/20**으로 **①·④ 미충족**을 확정했다.

---

## 5-i. 절대원칙 6종 판정 (장중 시점 · 12:40 기준)

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | **시각 미도래** | 12:40 현재 FLAT. 1차 경로 하트비트(`[SchedForceExit] … bar_pass=N회`)는 15:11 발행 — `P-9` 장후 이월 |
| ② | Circuit Breaker 5종 | **정상** | `[DBG-CB] state=NORMAL` 전 구간. **CB②**: `[CB] 연속 손절 1·2·3회`(10:33:55·10:39:59·10:50:51) 카운터는 정상 증가, `CB_CONSEC_STOP_LIMIT=9999`라 **정지 없음 = 의도된 상태**. 부분청산 3건은 `연속 손절 카운터 미증가 (포지션 단위 집계, 470차 C1)`로 올바르게 제외됨 ✅. **CB⑤**: `latency` 최대 **0.507초** — `CB_PIPE_PAUSE_MS=5,000`(5초)의 10%. **CB③**: `acc30m` 변동 정상(1-11·1-12 참조), 차단은 `CB3_P4_GRADE_BLOCK_ENABLED=False`로 의도적 비활성 |
| ③ | CORE 피처 호라이즌 그룹별 분리 | 🟠 **조건부 — 규칙은 준수, 재료는 약화** | **강제 X(`vwap_position`) 위반 0건** ✅. 등급 하락 규칙도 정상 작동(`ofi❌`×10 → `C급`×6). 단 `ofi_norm`이 개장 후 refit 21/28에서 identity 강제(**1-10**). 진입 13건 전부 3m·5m = **단기 그룹** — 중기·장기 그룹은 474차대로 도달 불가(정상) |
| ④ | COM 콜백 내 `dynamicCall`·`emit` 금지 | **정상** | `0xC0000409`·`STATUS_STACK_BUFFER_OVERRUN`·`STACK_BUFFER` **0건**. 프로세스 무응답 0. 매분 루프 무중단 221/221 |
| ⑤ | `GetRepeatCnt`/`GetCommData` 파라미터 구분 | **정상(간접)** | TR 수신 오류 0건. 09:20:02 `[CybosInvestor] fetch#21 futures_supported=True program_supported=True option_supported=True` 정상 파싱 |
| ⑥ | 알파 리서치 봇 자동 통합 금지 | **정상** | `자동 통합`·`auto_integrate`·`백테스트 큐` 0건. 미커밋 산출물은 전부 **장 시작 전** 생성(08-23 17:56 · 08-24 07:56~08:10) |

**장중 금지사항 준수** — 코드 배포 0건(`*.py` mtime ≥09:00 **0건**) · 재기동 0건 · git 커밋 0건 ·
설정 변경 0건(다이제스트 §3 **26행 전부 일치/값 확인**, `불일치`·`미발견` 0건).

---

## 6-i. 장후 관측 예정 (장중 등록분)

| ID | 관측 대상 | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| **O-6(491차)** 🆕 | `[DriftRetrain]` `n` 최댓값 (당일) | `max(n) ≥ 15` 이면 **1-7 반증**(조건B 도달 가능). 오늘 `max=3`. **20거래일 누적 후** §12b 방식으로 판정. ⚠ **그 전에 임계 완화 금지 — 문턱을 관측값에서 역산하지 않는다**(사전등록 ④) | **누적** — 매 장중·장후 |
| **O-7(491차)** 🆕 | `sizing_trace` 「미설명 축소행」 (`qty_display>qty_auto` & 기록 두 상한 미구속) | F-5 배포 후 **0행**이어야 한다. 배포 전에는 건수만 기록(오늘 3행 · 계측 전구간 13행) | **F-5 배포 다음 거래일** |
| **O-8(491차)** 🆕 | `[Contrarian] acc30m` vs `[DBG-CB] acc30m` 동시 관측 쌍 | 두 값의 차이가 **계통적**(항상 한쪽이 큼)인지 **소표본 변동**인지. 오늘 쌍 **1건**뿐 — 확정 결론 금지(313차) | **20거래일 누적 후** |
| **O-9(491차)** 🆕 | 30m SGD 리셋 직후 30분 내 `acc30m<0.28` 출현률 | CB③ 임계 접촉이 리셋 타이밍에 좌우되는가. 오늘 리셋 5회·CB③ 발동 0회 | **20거래일 누적 후** |
| **O-10(491차)** 🆕 | 개장 후 `[CORE준비도]` 축퇴율 (F-6으로 O-3 대체) | 오늘 **21/28 = 75%** · 최장 연속 09:22~12:38. 08-18 68% / 08-19 100% / 08-20 72% / 08-21 0%. **20거래일 축적 후** 470차 B4 절차(축퇴일 09:00~09:30 정확도 일자단위 비교). ⚠ **차단 승격 금지** | **누적** — 매 장중 |
| **O-11(491차)** 🆕 | 진입 방향 편향 − 당일 음봉 분 비율 (G-6 축) | 오늘 숏 100%(13/13). 정규화 축은 G-6 구현 후. 그 전에는 원시 비율만 기록 | **누적** |

**장후에 반드시 닫을 이월분** — `1-2`(P-1·P-2 보정기 스냅샷) · `1-5`(GuardFair 무효율/O-9(0821)) ·
`1-3`(F-1 적용 여부) · `1-6`(F-2·F-3) · `O-4·O-5(491차)` · `O-6·O-17·O-23(0821)` ·
`O-26~O-29(489차)` · `P-4`(전량 기준 재확인) · `P-8`(롤링 20일 net) · `P-9`(`bar_pass≥1`).

---

## 7-i. 사용자 조치 (장중분 — 장후 `§9p-3`에서 하루치로 통합됩니다)

> 🟢 **지금 하실 일은 없습니다.** 장이 열려 있는 동안은 프로그램을 건드리지 않는 것이
> 원칙이라, 전부 오후 3시 10분 이후 일입니다. 오늘 오전 성적은 좋습니다(13번 중 9번 성공,
> 수수료 빼고 약 21만 원).

| # | 지금 할 일 | 걸리는 시간 | 안 하면 생기는 일 |
|---|---|---|---|
| **1** | **장 마감(오후 3시 10분) 뒤에 "F-5 구현해"라고 말씀해 주세요.** 주문 수량이 줄어든 이유를 장부가 **틀리게** 적고 있습니다. 담보가 모자라 줄어든 것을 "판단 장치가 깎았다"고 적습니다. 오늘 세 번 다 그랬습니다. | 제가 **약 30분**, 회귀 테스트 포함 **45분** | 나중에 "진짜 돈을 넣어도 되는가"를 이 장부로 판단하는데, 틀린 답을 근거로 결정하게 됩니다. 게다가 실제 자금을 키우는 순간 이 오기록이 **더 자주** 발생합니다 |
| **2** | **이어서 "F-4 구현해"라고 말씀해 주세요.** 오전 장이 크게 어긋났을 때 스스로 다시 공부하는 기능이 **사실상 절대 켜지지 않는** 상태입니다. 지금 당장 고치자는 게 아니라, **매일 눈에 보이게 만들자**는 것입니다. | 제가 **약 20분**, 확인 포함 **30분** | 이 사실이 다시 안 보이게 됩니다. 같은 종류의 문제를 지난번엔 6개월, 그 전엔 3개월 동안 아무도 몰랐습니다 |
| **3** | (선택) **"F-6 구현해"** — 아침에 "문제가 풀렸다"고 잘못 보고하게 만든 판정 기준을 고칩니다. 문서만 고치는 일입니다. | 제가 **약 10분** | 내일 아침에도 똑같이 "풀렸다"고 잘못 보고할 수 있습니다 |
| **4** | **장전에 부탁드린 1·2·3번(F-1·G-1·F-2·F-3)은 그대로 유효합니다.** 순서는 F-1 → F-5 → F-4 → F-6 → F-2·F-3을 권합니다. | (장전 안내와 동일) | (장전 안내와 동일) |
| **5** | **이번 주 금요일 회의(8월 28일) 안건이 하나 늘었습니다.** 예측 확신도가 낮아진 뒤로 **기준값 여러 개가 함께 무력화됐다**는 것이 오늘 두 번째 사례로 확인됐습니다(아침 9시 20분대 진입 금지에 이어, 장중 자동 재학습까지). 개별로 고치지 말고 **한 묶음으로 다시 정하실지** 봐 주세요. | **10분 추가** | 세 번째, 네 번째 사례가 같은 방식으로 하나씩 발견됩니다 |

---

*장중 점검은 예약 점검(자동 실행)으로 작성됐다. **라이브 코드·설정·커밋 변경 0건**을 실측 확인했다
(`*.py` mtime ≥09:00 0건 · 다이제스트 §3 불변식 26행 전부 일치 · git HEAD `135c00f` 무변동).
산출은 증거 다이제스트(`evidence_MW0602-20260824_intra.md`)와 이 절뿐이다.
`dev_memory` 갱신은 **장후 점검에서 일괄**로 한다(장중 커밋 금지).
장후 세션은 이 파일 **끝에 이어 붙인다** — 장전·장중 절의 본문은 고치지 않는다.*

---

# 장후(post)

- 점검 시각: **16:33 KST** (증거 수집기 발화 2026-08-24 16:33:00) · PC `MW0602` · 브랜치 `dev` ✅
- HEAD `135c00f` (491차, 장전 점검 커밋 — 장중 이후 무변동) · 미커밋 **7건** · 당일 커밋 **3건**
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260824_post.md` (92.3KB)
- 수집기 실행: **성공** (stderr 없음). 다이제스트 첫 줄 `MW0602 (override · host=claude)` 정상
- 세션 차수: **492차** (원격 git 기준 최신이 491차 `135c00f`)
- 🟢 **이 점검도 읽기 전용이다. 코드·설정·커밋 변경 0건.**

> 📌 **번호 규약 (장중 절 머리 메모 유지)** — `1-9`는 오늘 쓰지 않는다(2026-08-19 등록된
> 「앙상블 등급 임계 미도달」 고유 ID). 장후 신규는 **`1-13`·`1-14`·`1-15`** 를 쓴다.

---

## 0-p. 오늘 하루 한눈에

> 이 절에는 약어·로그 태그·파일 경로·원칙 번호·단계 번호를 쓰지 않는다.

**오늘은 이겼습니다. 그리고 지켜야 할 것을 다 지켰습니다.**

24번 들어가서 17번 이겼고, 수수료를 뺀 최종 손익은 **+68,089원**입니다(굴리는 돈 5천만 원
기준 **+0.14%**). 프로그램은 아침 9시부터 오후 3시 10분까지 **371분 전부** 한 분도 빠짐없이
돌았고, 멈추거나 다시 켜진 적이 없습니다. 오후 3시 10분에 넘겨서는 안 되는 물량은 애초에
하나도 없었습니다 — 마지막 거래가 오후 2시 40분에 끝났고, 3시 11분 점검에서 "정리할 것 없음"이
정상적으로 확인됐습니다. 장 마감 뒤 인공지능 모델 6개를 다시 학습시키는 작업도 2분 38초 만에
전부 성공했습니다.

**좋은 소식이 하나 더 있습니다.** 지난 금요일부터 사흘 내내 보고드렸던 문제 — 예측 확률을
실제에 맞춰 다듬는 장치가 8월 11일 이후 갱신되지 않던 것 — 이 **오늘 마감 때 정상적으로
갱신됐습니다.** 금요일에 해 둔 수정이 실제로 작동한다는 것이 확인됐습니다. 9거래일 만입니다.

**다만 오늘 새로 찾은 문제가 하나 있습니다. 성격은 지금까지와 같습니다.**

프로그램에는 "요즘 예측이 자꾸 틀리면 공부 속도를 높여라"라는 자동 조절 장치가 있습니다.
그 장치가 **최근 9거래일 중 6일이나 "속도를 높이겠다"고 기록을 남겼는데, 실제로는 단 한 번도
높아지지 않았습니다.** 이미 올릴 수 있는 최대치에 도달해 있었기 때문입니다. 기록만 보면
매일 대응하고 있는 것처럼 보이지만 실제로는 아무 일도 일어나지 않았습니다. 반대로 내리는
길도 막혀 있습니다 — 내리려면 성적이 58% 이상이어야 하는데 최근 10일 최고 성적이 40%입니다.
**즉 그 손잡이는 끝까지 돌아간 채 굳어 있고, 돌아올 길도 없습니다.**

이건 오늘 처음 생긴 문제가 아니라 **적어도 2주 전부터 그랬던 것이 오늘 처음 보인 것**입니다.
그리고 아침·점심에 보고드린 두 가지 문제(자동 재공부 기능이 켜질 수 없는 것, 예측 확신도
기준이 너무 높게 박혀 있는 것)와 **뿌리가 같습니다** — 예전 성적을 기준으로 정해 둔 숫자가,
성적이 내려앉은 뒤에도 그대로 남아 장치를 무력화한 것입니다. 오늘로 같은 유형이 셋이 됐습니다.

**지금 사람이 해야 할 일은 두 가지뿐이고, 둘 다 급하지 않습니다.** 맨 끝 「사용자 조치」 절에
모아 두었습니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| **장전** | **조건부 정상** | (장전 절 판정 유지 — 고치지 않는다) |
| **장중** | **조건부 정상** | (장중 절 판정 유지 — 고치지 않는다) |
| **장후** | **조건부 정상** | P0 **0건**. 절대원칙 6종 **전부 준수**. 매분 루프 371/371분(100%) · 종가 포지션 0 · EOD 재학습 6/6 성공 · 3원 대사 24=24=24 일치. 단 관측 계층 결손 **신규 1건(1-13)** + 지속 6건 |
| **당일 최종** | **조건부 정상 (수익)** | 포지션 24건 승 17 · **net +68,089원**(자본 대비 +0.14%). 절대원칙 위반 0. 한시예외 4종 전부 의도대로. ⚠ **상위 1포지션을 빼면 −99,311원** — 하루 흑자를 추세로 읽지 말 것 |

---

## 0p-C. 이월 처리 (장중 → 장후)

> 대원칙 B-②. 장전·장중이 남긴 **모든** 항목을 처분한다. 지속 항목에 새 번호를 붙이지 않는다.

### 이상점 1-1 ~ 1-12

| 번호 | 항목 | 처분 | 근거 |
|---|---|---|---|
| **1-1** | 단기 CORE `ofi_norm` 장전 축퇴 | 🔄 **지속** (정정본은 `1-10`) | 장전 절이 이미 `1-10`으로 정정 포인터를 달았다. 장후에 새 판단 없음 — 원 번호 유지 |
| **1-2** | 앙상블 확률 보정기 스냅샷 9거래일 정체 | ✅ **해소 — 라이브 검증 완료** | `data/ensemble_calibrator.pkl` mtime **2026-08-24 15:40:03** · 크기 3,008 → **5,299바이트** · 로그 `15:40:03 [Calibration] 앙상블 보정기 저장 완료 **n=1478**`(장전 복원값 n=1183 대비 +295). **0821 F-1(저장 성공/실패 양방향 로그 배선)이 EOD 경로에서 작동함이 실측 확인됐다.** `P-1`·`P-2` 동시 판정 통과 |
| **1-3** | 스킬 문서 3곳의 죽은 CB② 기한 「2026-08-29」 | 🔄 **지속 — 적용 대기** | 장후 다이제스트 §3 `CB_CONSEC_STOP_LIMIT` 행이 **여전히** `재검토 기한 2026-08-29`를 인쇄한다(오늘 3회 연속: 장전·장중·장후). F-1(491차)은 **미적용**. 미조치가 아니라 사용자 지시 대기 |
| **1-4** | 장전 점검이 개장 36초 뒤 실행 | ✅ **해소(국면 조건)** | 장후는 발화 마진 개념이 없다. `O-10(0821)` 카운터 재시작은 **내일 장전**부터 |
| **1-5** | `GuardFair_유효` 100% `무효` 고착 | 🔄 **지속 · ⬆ 표본 증가** | 오늘 EOD가 6호라이즌 전부 `무효` 추가 → **9거래일 54표본 100% 무효**. 사유는 전부 동일: `현행이 홀드아웃 학습함 — train_end=2026-08-21 14:39 >= holdout_start=2026-08-14 12:18`. 457차 설계상 `판정 무영향(섀도)`이라 채널 [23]은 PASS — **죽은 것은 아니다.** 470차 C3'가 26주 WFA 항목으로 등록한 「무효율 추세」의 표본 |
| **1-6** | 리포 루트 산출물 + 미커밋 | 🔄 **지속 · ⬆ 5건 → 7건** | 오늘 `weekly_prompt.txt` 수정(15:09)과 오늘자 리포트 md가 추가돼 **7건**. 리포 루트 산출물 2건(`gate_blocking_metrics.json`·`gate_blocking_report.md`)은 동일 |
| **1-7** | 장중 자동 재학습 조건 구조적 도달 불가 | 🔄 **지속 · 전일 표본으로 확정 강화** | 전일 `[DriftRetrain]` **369건 전부 `cond_a=False cond_b=False`**. `n` 분포 `n=3`×340 · `n=4`×9 · `n=2`×9 · `n=1`×5 · `n=0`×6 → **당일 최댓값 n=4**(조건B 요구 15의 27%). `acc5m` 369/369 전부 `0.0%`. ⚠ 여전히 **라이브 1일차** — 임계 변경 제안 금지 |
| **1-8** | 수량 축소 원인 오귀속(증거금 → 품질게이트) | 🔄 **지속 · ⬆ 3행 → 5행** | 전일 `sizing_trace` 미설명 축소행 **5행**(10:14·10:25·10:36·14:35·14:55) **전부 `binding_gate='meta'`**. 계측 전구간(08-18~08-24 · 181행) 누계 **15행**(`None`×7 · **`meta`×8**). `meta` 34건 중 **8건(23.5%) 오귀속**. `margin_cap_qty` 필드 **0행** → F-5 미배선 재확인 |
| **1-10** | 개장 후 CORE 축퇴 재발·고착 | 🔄 **지속 · ⬆ 75% → 80.6%** | 전일 기준 개장 후 `[CORE준비도]` **36회 중 29회 축퇴(80.6%)**(장중 12:40 시점 21/28=75%). `identity(0,1) 강제` 장중 126건 → **전일 228건**. 마지막 관측 `15:02:01 D_FORCE 축퇴 1/6 — ofi_norm×6hz`. **EOD(15:47:40)에도 `trigger=E_EOD 축퇴 1/6 — ofi_norm×6hz`** — 내일 장전으로 이월된다 |
| **1-11** | 같은 이름 `acc30m` 두 원천 불일치 | 🔄 **지속 — 표본 미증가** | 전일 `[Contrarian]` 로그 **1건**뿐(10:37). 쌍 표본 1건 → **확정 결론 금지(313차)** 유지. `O-8(491차)` 등록 상태 |
| **1-12** | 30m 온라인 학습기 단방향 붕괴·리셋 반복 | 🔄 **지속 · 전일 집계** | 전일 SGD 붕괴 자동복구 **12건** — `30m` **7건**(DN 5 · UP 2) · `15m` 4건 · `5m` 1건. 장중 12:40 시점 30m 5건 → 오후에 2건 추가. 전일(08-21) 30m 6건과 같은 수준 = **상시 상태**. `O-9(491차)` 유지 |

### 장전·장중 「확인 필요」 항목

| 항목 | 처분 | 근거 |
|---|---|---|
| 호라이즌 확률 보정기 24개 「축퇴 감지」 + FloorShadow 판정 불일치 | ⏸ **보류 유지 — 확정 결론 금지(313차)** | 전일 `[Calibration]` 축퇴 WARNING **174건**(08:40:45~14:23:00). **무엇을 더 보면 닫히는가**: 2026-08-25 EOD 캠페인 리포트 `[2-A]` AUC 표 · `[2-B]` 재개 게이트. **언제 닫는가**: 490차가 등록한 대로 **2026-08-25 장후**. 앞당기지 않는다 |
| 선물 투자자 수급 TR 미제공 | ✅ **해소(장중에 이미 종결)** | 09:20:02 `futures supported=True source=CpSysDib.CpSvrNew7221`. 재론하지 않는다 |
| `CybosProbe` 경고 건수 | ✅ **해소 — 상시 조건 확정** | 전일 총 **798건**(`logs/20260824_PROBE.log`). 전일(08-21)도 **798건**으로 **완전 동일**. 장전이 본 18건은 `PROBE.log`가 다이제스트 대상 8/13에서 제외돼 다른 로그의 부분 표본을 센 결과였다 — **감소가 아니라 계측 범위 차이**(계측 4원칙 ②). `O-5(491차)` **종결** |

### 장전 「이미 반영된 사안」 6건

전부 **✅ 유지 — 재론하지 않는다**(함정 ①). 장후에 반박 증거 없음.
`ZONE_ENTRY_BAN_ENFORCE=False` 관련 오늘 위반 코호트는 **1건**(11:57 진입, `OTHER` 존)이며
그 포지션은 **−57,188원(손실)** 이다 — 채널 `[53] zone_ban_breach_watch` 표본 1건 추가.
⚠ **이 1건으로 「집행을 켜야 한다」로 읽지 말 것**(462차 결정, 판정 창 미도달, n=1).

### 관측 항목 O-* / P-* 전량 처분

| ID | 처분 | 근거 |
|---|---|---|
| **P-1**(486차) 보정기 저장 로그 | ✅ **판정 — 통과** | `15:40:03 [Calibration] 앙상블 보정기 저장 완료 n=1478` 출현. 「저장 건너뜀」 WARNING 미출현 |
| **P-2**(486차) `ensemble_calibrator.pkl` mtime | ✅ **판정 — 통과** | mtime `2026-08-24 15:40:03` · 5,299바이트. **1-2 해소의 직접 근거** |
| **P-3**(486차) `[DriftRetrain]` 매분 1줄 | ✅ **판정 — 통과(전일 기준)** | **369줄 / 371분**(09:00~15:10). 미기록 2분은 15:09·15:10 마감 구간 |
| **P-4**(486차) `[MarginCap]` 건수 대사 | ✅ **판정 — 통과** | 다이제스트 §5 `조회 53 · 축소 5` == 원본 로그 `state=OK`×48 + `state=CAP`×5 = **53**. 477차 후속 G-2(무조건 상태 샘플) 라이브 정합 확인 |
| **P-8**(0821) 롤링 20거래일 net | ✅ **판정 — 기록 완료** | `trades.db` 포지션 단위 최근 20거래일 **−107,232원**. 일일 전략 리포트 배너 `롤링20일: 누적 -107234원`과 **2원 차**(반올림) — 대사 일치. ⚠ **여전히 음수다** — 489차 D6의 ①·④ 미충족 판정은 유지된다 |
| **P-9**(0821) `bar_pass≥1` | ✅ **판정 — 통과** | `15:11:02 [SchedForceExit] 15:11 점검 — status=FLAT engine=0ct broker_cached=0ct **bar_pass=2회** → 청산 대상 없음(정상)`. 471차 F-2 하트비트 5거래일 연속 `≥1(생존)` |
| **O-6**(0821) `entry_qty_final` == `trades.entry_qty` 대사 | ✅ **판정 — 통과(대사 가능 3/3)** | 증거금 캡 5건 중 진입까지 간 3건 전부 일치 — 10:14 `final=2` ↔ 10:15 진입 `entry_qty=2` · 10:25 ↔ 10:26 · 10:36 ↔ 10:37. 나머지 2건(14:35·14:55)은 진입 미발생(대사 대상 아님). **477차 후속 F-1 라이브 검증 완료** |
| **O-17**(0821) `entry_horizon` 소비처 전수 | ⏭ **이월(2026-08-25 장후)** | 코드 조사 항목. 오늘 표본 조사에 시간을 썼다. **무엇을 보면 닫히는가**: `grep -rn "entry_horizon" --include=*.py` 전수와 474차 도달성 테스트 대조 |
| **O-22**(0821) WeightCollapse 비율 | ✅ **판정 — 밴드 내** | `[WeightCollapse] 실질 가중합 0` **75건** / `[Ensemble] dir=` **364건** = **20.6%**. 사전등록 밴드 20.3~20.5%±5%p **내** |
| **O-23**(0821) `sizing_trace.margin_cap_qty` 대사 | 🔄 **지속 — 선행 F-5 미배선** | 필드 부재 재확인(181행 중 0행). `1-8`이 그 결손의 결과를 매일 5행씩 만든다 |
| **O-3**(0821) 런처 GUARD 연속일 | ⏸ **관측 계속 — 7거래일 연속** | `logs/Mireuk_batch/launcher_20260824_084001_24126.log` — `[GUARD] 기존 프로세스 종료 완료` **1회**. 08-14·18·19·20·21·24 전부 동일. ⚠ **판정 기준이 아직 등록돼 있지 않다** — 무엇이 정상인지 정하기 전에는 판정하지 않는다 |
| **O-4**(0821) `[Capital]` 괴리율 | ⏸ **관측 계속 — 횡보** | 08-14 −40.6% → 08-18 −40.7% → 08-19 −40.8% → 08-20 −42.3% → 08-21 −42.1% → **08-24 −41.5%**(실제잔고 29,274,631원, 장중 최종 29,329,947원). **추세 반전 아님** |
| **O-5**(491차) `CybosProbe` 총건수 | ✅ **판정 — 종결** | 위 「확인 필요」 표 참조. 798 == 798 |
| **O-1**(491차) 다이제스트 §3 CB② 문구 | ⏭ **이월(2026-08-25 장전)** | F-1 미적용 → 판정 불가. 원 등록대로 |
| **O-2**(491차) 장전 마지막 축퇴 개수 | ⏸ **누적 2일차 — 오늘분 `1`** | 20거래일 축적 전 확정 결론·차단 승격 금지(313차) |
| **O-3**(491차) 개장 후 최초 축퇴 개수 | 🔴 **판정 완료(장중) — F-6으로 대체** | 장중이 「최초 1점 기준은 재발에 눈이 멀었다」로 판정. `O-10(491차)`가 승계 |
| **O-4**(491차) `[Calibration]` 축퇴·FloorShadow | ⏸ **보류 — 2026-08-25 EOD** | 위 「확인 필요」와 동일 |
| **O-6**(491차) `[DriftRetrain]` `n` 최댓값 | ⏸ **누적 1일차 — 오늘 `max(n)=4`** | 조건B 요구 15 대비 27%. ⚠ **문턱을 관측값에서 역산하지 말 것**(사전등록 ④) |
| **O-7**(491차) 미설명 축소행 | ⏸ **누적 — 오늘 5행 · 전구간 15행** | F-5 배포 후 **0행**이어야 한다. 배포 전이므로 건수만 기록 |
| **O-8**(491차) `acc30m` 쌍 | ⏸ **누적 — 오늘 쌍 1건** | 20거래일 필요. 확정 결론 금지 |
| **O-9**(491차) 30m SGD 리셋 직후 CB③ 접촉 | ⏸ **누적 — 오늘 리셋 7회 · CB③ 발동 0회** | 20거래일 필요 |
| **O-10**(491차) 개장 후 축퇴율 | ⏸ **누적 2일차 — 오늘 29/36 = 80.6%** | 08-18 68% / 08-19 100% / 08-20 72% / 08-21 0% / **08-24 80.6%**. 20거래일 후 470차 B4 절차. ⚠ **차단 승격 금지** |
| **O-11**(491차) 진입 방향 편향 | ⏸ **누적 — 오늘 숏 100%(24/24 포지션 · 30/30 레그)** | 정규화 축(G-6)은 미구현. 원시 비율만 기록. ⚠ 오늘 지수가 1087.98 → 1053대로 크게 밀린 날이라 **편향과 시장을 아직 못 가른다** |
| **O-26~O-29**(489차) `[56]`·`[44]`·`[46]` 채널 | ⏭ **이월(2026-08-28 금)** — **판정 불가 사유가 있다** | 판정 창은 **오늘(08-24)** 열렸으나 그 판정을 **렌더링하는 캠페인 리포트는 금요일 전용 체인**이다(`scripts/eod_retrain.py:63` *"기본 None = 금요일에만 자동 실행"*). 오늘 EOD는 P8에서 정상 종료했다 — **결함 아님.** 이 구조적 시차 자체를 `1-15`로 기록한다 |
| **O-30~O-32**(490차) `[2-A]`/`[2-B]`/사이드카 | ⏭ **이월(2026-08-25)** | 원 등록대로. 앞당기지 않는다 |
| **O-20 · O-21**(0821) 월간 로그 정리 / 캠페인 metrics 필드 | ⏭ **이월(2026-08-28 금)** | 금요일 전용 체인. 평일에 비는 것은 결함이 아니다 |
| **O-16 · O-7 · O-8 · O-9 · O-18 · O-19**(0821) | ✅ **판정 또는 지속 — 아래 개별 기록** | `O-9`(GuardFair 무효율)=`1-5`로 기록 · `O-8`(손절 준수율)=§4p-E로 판정 · `O-7`(meta_action 승패)=§4p 표로 판정 · `O-16`(5m 정확도)=`1-7`에 흡수 · `O-18`(Tier1 준수율)=§4p 청산 2축 표 · `O-19`(진입 방향 편향)=`O-11(491차)`로 승계 |

---

## 1-p. 장후 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**해당 없음.** 절대원칙 6종 전부 준수를 실측 확인했다(§6p-3 표).
특히 **절대원칙 ①(오버나이트 금지)** 은 아래로 확인했다 —

```
14:40:40 [체결청산] SHORT 1계약  ← 당일 마지막 청산
15:11:02 [SchedForceExit] 15:11 점검 — status=FLAT engine=0ct broker_cached=0ct
         bar_pass=2회 → 청산 대상 없음(정상)
15:16:02 [System] 대기 중 | … | 포지션=FLAT | 15:16:02      ← 15:18 안전망 구간 재확인
```

⚠ **「청산 로그 없음」과 「청산할 것이 없었음」은 다르다**(`phases.md` C-1).
오늘은 **후자**다 — 마지막 포지션이 14:40:40에 닫혔고 15:10 시점 보유 0계약이었다.
따라서 **실전 전환 기준 ②의 선행 확인사항(15:10 강제청산 경로 실집행 1회)은 오늘도 미충족**이며,
이는 2026-06-10 이래 계속이다. 1차 경로의 **생존**은 하트비트로 확인되지만(`bar_pass=2회`),
**주문이 실제로 나가는지는 471차 G-3 리허설로만 확인된다.**

### P1 — 정확성·관측 훼손

#### 1-13. 🆕 학습률 자동 조절기가 **상한에 붙박여** 매일 「올림」을 기록하지만 아무것도 바뀌지 않는다 — 게다가 내려올 길도 막혔다

- **증상**: `DriftAdjuster`(SGD 온라인 학습기의 학습률 alpha를 정확도 추이로 자동 조정하는
  장치)가 오늘 EOD에 `DRIFT_UP`(학습률 상향) 액션을 기록했으나 **alpha 값은 변하지 않았다.**
  ```
  2026-08-24 15:40:03 [WARNING] LEARNING: [DriftAdjuster] 3일 연속 정확도 50% 미만 → alpha 0.01000→0.01000
  2026-08-24 15:40:03 [INFO]    LEARNING: [DriftAdjuster] acc=0.0% → alpha=0.01000 (DRIFT_UP) | 이력=['39%', '11%', '8%', '17%', '0%']
  ```
  화살표 좌우가 **같은 값**이다.

- **근거 ① — 코드가 상한을 명시한다** (`learning/self_learning/drift_adjuster.py`):
  ```python
  ALPHA_DEFAULT = 0.001
  ALPHA_MIN     = 0.0001
  ALPHA_MAX     = 0.01          # ← 현재 alpha와 정확히 같다
  ALPHA_UP_FACTOR = 1.5
  # :141
  self._alpha = min(self._alpha * ALPHA_UP_FACTOR, ALPHA_MAX)   # min(0.015, 0.01) = 0.01
  ```
  ⇒ `DRIFT_UP` 분기는 **수학적으로 무연산(no-op)** 이다. 그런데 로그는 `WARNING` 레벨로
  「조치했다」처럼 출력된다.

- **근거 ② — 9거래일 중 6일이 이 무연산 액션이었고, alpha는 그 전부터 상한이었다**
  (`logs/*_LEARNING.log` 전수):

  | 거래일 | 액션 | alpha |
  |---|---|---|
  | 08-11 | `SKIP_LOW_SAMPLE`(n=7<15) | 0.01000 (로드 시점에 **이미** 상한) |
  | 08-12 | `SKIP_LOW_SAMPLE`(n=7<15) | 0.01000 |
  | **08-13** | **`DRIFT_UP`** | 0.01000 → 0.01000 |
  | **08-14** | **`DRIFT_UP`** | 0.01000 → 0.01000 |
  | 08-18 | `SKIP_LOW_SAMPLE`(n=13<15) | 0.01000 |
  | 08-19 | `SKIP_LOW_SAMPLE`(n=14<15) | 0.01000 |
  | **08-20** | **`DRIFT_UP`** | 0.01000 → 0.01000 |
  | **08-21** | **`DRIFT_UP`** | 0.01000 → 0.01000 |
  | **08-24** | **`DRIFT_UP`** | 0.01000 → 0.01000 |

  08-11 **기동 시 로드값이 이미 `alpha=0.01000, 마지막 액션=DRIFT_UP`** 이었다 —
  즉 상한 도달은 **2주 이상 전**이며 오늘 발생한 일이 아니다.

- **근거 ③ — 내려오는 길이 관측상 도달 불가다.** 상태 파일 `data/drift_adjuster_state.json`
  실측 (10일 롤링 이력):
  ```json
  "alpha": 0.01,
  "acc_history": [0.2833, 0.3333, 0.4048, 0.2317, 0.2556, 0.3867, 0.1111, 0.0833, 0.1667, 0.0],
  "last_action": "DRIFT_UP"
  ```
  하향 조건은 `RECOVERY_THRESHOLD = 0.58` **2일 연속**(`:150~152`)인데
  **10일 최댓값이 0.4048**이다. 0.58은커녕 0.50에도 닿은 적이 없다.
  게다가 `_adjust_alpha()`는 **드리프트 분기를 먼저 평가하고 `return`** 하므로
  (`:136~146` → `:148`), 3일 연속 <50%인 동안은 **회복 분기가 실행조차 되지 않는다.**

- **기준 위반**: 9단계 **STEP 2**(SGD 온라인 자가학습)의 학습률 제어.
  **절대원칙 위반은 아니다**(6종에 없다). 위반한 것은 `CLAUDE.md` 「주기적 재검증 항목」
  **468차 G-2(고착 지표 점검)** 가 등록한 규율 — *"안전장치가 한쪽 값에 붙박여 죽어 있는데
  매번 사람이 뒤늦게 발견했다"* 그 형태다. 292차(CB③-P4 상시 RESTRICTED)·303차
  (FP-CRITICAL 상시 CRITICAL)·371차(PSI 메가빈)와 **같은 계열**이다.

- **영향**: 예측이 계속 틀릴 때 "공부 속도를 높여서 빨리 따라잡자"는 자동 장치가 있는데,
  **그 손잡이가 이미 끝까지 돌아가 있어 더 돌릴 곳이 없습니다.** 그런데 기록에는 매번
  "돌렸다"고 적힙니다. 사람이 기록만 보면 대응이 되고 있다고 믿게 됩니다. 반대로 성적이
  좋아졌을 때 속도를 다시 낮추는 길도, 요구 성적이 너무 높아 사실상 막혀 있습니다.
  지금 손해가 나고 있다는 뜻은 아닙니다 — **장치 하나가 서류상으로만 살아 있다는 뜻입니다.**

- **영향(기술)**: SGD alpha가 `ALPHA_DEFAULT`의 **10배**인 `ALPHA_MAX=0.01`에 고정된 채
  `online_learner.set_alpha()`로 매일 재주입된다. 학습률이 상시 최대라는 것은
  **최근 표본에 대한 과반응(과적합) 쪽으로 편향된 상태가 상시**라는 뜻이고, 이는 1-12에서
  관측한 **30m SGD 단방향 붕괴 하루 7회**(`DN붕괴 5 · UP붕괴 2`)와 **정합적**이다.
  ⚠ **정합적이라는 것이 인과 증명은 아니다** — 아래 표본 규율 참조.

- **⚠ 함정 ① 확인 — 신규 안건이다.** `config/settings.py:VALIDATION_CAMPAIGN_DECISIONS`
  19개 채널 키 전수 대조 결과 `drift_adjuster`·`alpha`·`sgd_lr` 계열 **없음**.
  `dev_memory/DECISION_LOG.md` grep에서도 「alpha 상한」 관련 결정 없음.
  다만 **288차가 인접 문제를 이미 고쳤다** — `main.py:4590~4597` 주석:
  *"장중 GBM 재학습마다 SGD acc_buf가 매번 초기화되어 … DriftAdjuster도 매번
  '표본부족→스킵' 고착"* → 그때는 **스킵 고착**을 고쳤고, 지금은 **상한 고착**이다.
  같은 장치의 **다른 죽음**이다.

- ⚠ **확정 결론 금지(313차) — 고치자고 제안하지 않는다.**
  ① alpha 상한 0.01이 **잘못된 값이라는 증거는 없다.** 상한 자체는 폭주 방지 장치다.
  ② 오늘 `acc=0.0%`가 이력에 들어간 것이 **진짜 0%인지 「미측정 0」인지 구분되지 않았다** —
     `online_learner.recent_accuracy()`는 버퍼가 전부 비면 **0.5**를 반환하므로(`:370`)
     0.0은 「버퍼가 0으로 찼다」는 뜻이어야 하지만, 1-7이 밝힌 `conf≥0.52` 표본 게이트와
     같은 뿌리일 가능성이 남는다. **이력 자체가 오염됐다면 조정 로직을 고쳐도 소용없다.**
  ③ 따라서 F-7(492차)은 **계측만** 붙인다 — 「상한 고착」과 「도달 불가 하향」을 매일
     보이게 하고, `acc` 원천을 로그에 병기한다. **임계·상한은 건드리지 않는다.**

### P2 — 운영 부담·기술부채

#### 1-14. 🆕 EOD 마감에서 「DB 정리 실패」와 「DB 정리 성공」이 **같은 초에** 나란히 기록된다

- **증상**: 15:40:03에 두 줄이 연속으로 나온다. 앞은 실패(WARNING), 뒤는 성공(INFO)이다.
  ```
  2026-08-24 15:40:03 [WARNING] LEARNING: [Retrain] DB pruning 실패: database table is locked
  2026-08-24 15:40:03 [INFO]    LEARNING: [GBM]     DB pruning: 3,810행 삭제 (52주 초과분)
  ```
- **근거**: 태그가 다르다(`[Retrain]` vs `[GBM]`) — **서로 다른 두 정리 경로**가 같은 마감
  시퀀스에서 연달아 돌고, 한쪽만 SQLite 락에 걸렸다. 전일 EOD 재학습(15:45~15:47)은
  정상 완주했고 `data/eod_retrain_done_20260824.txt`도 정상 기록됐다 → **실해 없음.**
- **기준 위반**: 없다. 위생·관측 문제다. 9단계로는 **STEP 9**(예측 DB 저장) 인접 경로.
- **영향**: 마감 정리 작업 하나가 "실패했다"고 남았는데, 바로 다음 줄에서 다른 경로가
  성공했습니다. 오늘 결과에는 아무 영향이 없지만, **다음에 진짜로 정리가 안 됐을 때
  이 로그만 보고는 구분할 수 없습니다.**
- **영향(기술)**: `database table is locked`는 WAL 모드에서 다른 커넥션이 쓰기 중일 때 난다.
  15:40:03은 `daily_close()`가 `[MetaConf] 상태 저장`·`[ExtremityCorrector] 재적합`을
  동시에 도는 구간이다. 실패한 `[Retrain]` 경로가 **무엇을 지우려던 것인지**가 로그에
  없어 재시도 필요 여부를 판단할 수 없다.
- **신규 여부**: **신규 관측.** ⚠ **1거래일 표본** — 전일(08-21) 동일 로그 유무를 확인하지
  않았다. `O-13(492차)`로 등록만 하고 조치를 제안하지 않는다.

#### 1-15. 🆕 사전등록 판정 창은 **오늘 열렸는데**, 그 판정을 보여주는 리포트는 **금요일에만 생성된다**

- **증상**: 0823 주간회의(489차)가 채널 **[46] Hurst 임계 승격** · **[55] 09:20~09:29 (E)안** ·
  **[56] CB② 복원**의 판정 창을 **2026-08-24~** 로 사전등록했다. 오늘이 1일차다.
  그러나 그 채널의 표본 진행·판정을 렌더링하는 검증 캠페인 리포트는 **오늘 생성되지 않았다.**
- **근거**:
  ```
  scripts/eod_retrain.py:63   # [260705 검증 캠페인] 주간 스텝 — 기본 None = 금요일에만 자동 실행
  scripts/eod_retrain.py:139  # ── 주간 스텝 (금요일 자동 / --campaign 강제) ──
  ```
  오늘 EOD 로그는 `[P8] session_state … 기록 완료`로 **정상 종료**했고 캠페인 스텝은 돌지
  않았다. `docs/정기점검/금요일점검/MW0602/` 최신 산출물은 **`..._20260821.*`** 이다.
- **기준 위반**: 없다 — `phases.md` C-5가 *"캠페인 리포트 생성은 금요일 전용 체인이다.
  금요일이 아닌 날 이 절이 비는 것을 적신호로 올리지 말 것"* 이라고 **명시**한다.
  **결함이 아니라 일정 구조**다.
- **영향**: 주말 회의에서 "월요일부터 지켜보자"고 정한 세 가지가, 실제로는 **금요일에야
  처음 눈에 보입니다.** 나흘 동안 아무도 진행 상황을 못 보는 것이고, 그 사이에 뭔가
  잘못 쌓여도 알 수 없습니다.
- **영향(기술)**: 사전등록 판정 창(`start_date`)의 시간 해상도가 **일 단위**인데 관측
  수단의 주기는 **주 단위**다. `[56] cb2_restore_shadow`는 `scripts/cb2_restore_shadow.py`가
  독립 실행 가능하므로 일일 점검에서 부분 확인은 가능하나, **사전등록된 판정 경로가 아니다** —
  일일 점검이 임의로 돌려서 판정하면 458차 D6과 같은 사전등록 위반이 된다.
- ⚠ **처방을 제안하지 않는다.** 판정 주기를 바꾸는 것은 **사전등록 변경**이며 주간회의
  소관이다(§9-4 검증 시계 리셋 대상). 오늘은 **사실만 기록**하고 `G-8(492차)`로
  「일일 점검이 판정하지 않고 *표본 진행만* 보여주는 축」을 제안한다.

---

## 2-p. Fix 작업 구현계획

> 🟢 **장 마감(15:10) 이후이므로 코드 변경이 가능한 시간대다.** 다만 이 점검은 예약 실행이며
> **사용자 지시 없이 코드를 바꾸지 않는다.** 아래는 계획이다.
> ⚠ 이 마운트는 `unlink`를 금지하므로 커밋은 `invariants.md` §0-B의 rename 우회로만 시도한다.

### 미적용 이월 (장전·장중 수립분 — 재수립하지 않는다)

| ID | 내용 | 우선순위 | 상태 |
|---|---|---|---|
| **F-1**(491차) | 스킬 문서 3곳의 죽은 CB② 기한 교체 | P1 | ⏳ **적용 대기** (1-3 지속) |
| **F-5**(491차) | `sizing_trace`에 증거금 축 추가 + 불변식 테스트 | P1 | ⏳ **적용 대기** (1-8 지속·악화) |
| **F-4**(491차) | 장중 재학습 조건 도달 가능성 매일 계측 | P1 | ⏳ **적용 대기** (1-7 지속·확정 강화) |
| **F-6**(491차) | `O-3` 판정 기준 재설계(최초 1점 → 전 구간) | P2 | ⏳ **적용 대기** (1-10 지속·악화) |
| **F-2·F-3**(491차) | 리포 루트 산출물 정리 + 26주 WFA 산출물 커밋 | P2 | ⏳ **적용 대기** (1-6 지속·7건으로 증가) |

### F-7(492차). 학습률 조절기의 「상한 고착」과 「도달 불가 하향」을 매일 보이게 한다 — **P1** · 대응 이상점 **1-13**

**바꾸지 않는 것 (명시)**: `ALPHA_MAX`·`ALPHA_UP_FACTOR`·`DRIFT_THRESHOLD`·
`RECOVERY_THRESHOLD`·`MIN_SAMPLES_REQUIRED` **전부 무변경.** 임계를 관측값에서 역산하지
않는다(사전등록 ④). 이 Fix는 **계측만** 붙인다.

| 파일 | 변경 | 내용 |
|---|---|---|
| `learning/self_learning/drift_adjuster.py:136~146` | 로그 문구 | `DRIFT_UP` 시 `old == self._alpha`면 **`(상한 포화 — 무연산)`** 를 문구에 붙이고 레벨을 `WARNING` → `INFO`로 내린다. 진짜 상향된 날만 `WARNING`이 남게 한다 |
| 같은 파일 `_adjust_alpha()` 반환 | 액션명 분리 | 반환값을 `DRIFT_UP` / **`DRIFT_UP_SATURATED`** 로 가른다. 대시보드(`dashboard/main_dashboard.py:4873`)가 이미 액션명을 읽고 있으므로 표시가 자동으로 갈린다 |
| 같은 파일 `record_accuracy()` 로그 | 원천 병기 | `acc=%.1f%%` 옆에 **`n=%d src=online_learner.recent_accuracy`** 를 붙인다. 1-13 ⚠②의 「진짜 0% vs 미측정 0」을 다음 세션이 구분할 수 있게 한다 |
| `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py` §12 | 고착 지표 2행 추가 | `sgd_alpha`(값 분포) · `drift_action`(액션 분포). ⚠ `sgd_alpha`는 **`benign`이 아니다** — 상한 고착이 정상이라는 판단이 아직 없다 |
| `tests/test_492_drift_adjuster_saturation.py` 🆕 | 불변식 | ① `ALPHA_MAX`에서 `DRIFT_UP` 호출 시 alpha 불변 + 액션이 `DRIFT_UP_SATURATED` ② `RECOVERY_THRESHOLD > 최근 10일 acc 최댓값`이면 하향 분기 미도달임을 명시적으로 고정 — **이 테스트가 깨지는 날이 「하향 경로가 살아난 날」이다** |

- **회귀 위험**: 낮음. 값 변경 없음. 다만 대시보드가 `DRIFT_UP` 문자열을 **정확 일치**로
  비교한다면 `DRIFT_UP_SATURATED`가 미표시될 수 있다 → `main_dashboard.py:4873`·`:5113`
  두 곳을 `startswith("DRIFT_UP")`으로 함께 고친다.
- **검증**: 다음 거래일 EOD 로그에 `(상한 포화 — 무연산)`과 `n=`·`src=`가 출현하고,
  §12에 `sgd_alpha` 행이 `0.01000` 단일값으로 뜨는가 → `O-12(492차)`.
- **소요**: 구현 25분 · 회귀 테스트 포함 40분.

### F-8(492차). EOD 마감의 두 DB 정리 경로를 구분 가능하게 만든다 — **P2** · 대응 이상점 **1-14**

| 파일 | 변경 |
|---|---|
| `learning/batch_retrainer.py` (`[Retrain] DB pruning 실패` 발행부) | 실패 메시지에 **대상 테이블명과 삭제 예정 행수**를 넣는다. 현재는 `실패: database table is locked`뿐이라 재시도 필요 여부를 판단할 수 없다 |
| 같은 곳 | 락 실패 시 **1회 재시도(1초 후)** 후에도 실패하면 그때만 `WARNING`. 성공하면 `INFO`로 정정 로그 |

- **회귀 위험**: 낮음. ⚠ **재시도 루프를 넣지 말 것** — `daily_close()`는 15:40:03 한 초에
  여러 저장이 몰리는 구간이고, 여기서 대기가 길어지면 15:45 EOD 재학습 체인(`[WaitDC]`
  최대 20분 대기)의 여유를 갉아먹는다.
- **검증**: 다음 거래일 EOD에 대상 테이블명이 출현하는가. ⚠ **1거래일 표본이므로 재현
  자체가 안 될 수 있다** — 그때는 `O-13(492차)` 관측만 계속한다.
- **소요**: 15분.

### 적용 순서와 커밋 계획

```
① F-1 (P1, 10분)   — 죽은 기한 3곳. 가장 싸고, 내일 장전이 바로 오염된다
② F-5 (P1, 30분)   — 오귀속. 오늘 5행 추가로 전구간 15행. ⑧ 해제 전에 반드시
③ F-4 (P1, 20분)   — 재학습 도달 가능성 계측
④ F-7 (P1, 25분)   — 학습률 상한 고착 계측  ← 오늘 신규
⑤ F-6 (P2, 10분)   — 판정 기준 재설계(문서)
⑥ F-8 (P2, 15분)   — DB 정리 로그 분리      ← 오늘 신규
⑦ F-2·F-3 (P2, 15분) — 산출물 정리·커밋
커밋 제목: [MW0602] 492차: …
```

⚠ **②③④는 전부 「계측만 붙이고 임계는 안 건드린다」** — 316차 HurstGate가 진짜 신호의
72.3%를 오판 차단한 전례 때문이다. 게이트·차단 로직 신설은 섀도 계측을 거친 뒤 승격한다.

---

## 3-p. 고도화 방안 (당일 관측에서 출발)

> ⚠ 아래를 쓰기 전에 다이제스트 **§13 확정 결정 레지스트리 19건**과
> `config/settings.py:VALIDATION_CAMPAIGN` 채널 키를 대조했다(`phases.md` C-6).
> **오늘 §4p에서 나온 「등급 역전」·「KellySkip 코호트 우위」 두 관측은 각각
> `grade_ev_inversion`(부결 확정 2026-08-01) · `kelly_skip`(차단 도입 보류, 3관문
> 사전등록 2026-08-08)에 이미 등록된 질문이므로 신규 계측을 제안하지 않는다.**
> 해당 채널의 표본으로만 기록한다.

### 미적용 이월 (장전·장중 수립분)

| ID | 내용 | 상태 |
|---|---|---|
| **G-1**(491차) | 장전 CORE 축퇴가 개장 시점까지 남았는가를 별도 축으로 | ⏳ 대기 |
| **G-2**(491차) | 스킬 문서 안의 「날짜 기한」을 기계가 훑게 | ⏳ 대기 |
| **G-3**(491차) | 장전 발화 지연 원인을 부팅 지연에서 찾기 | ⏳ 대기 |
| **G-4**(491차) | 「도달 불가 분기」를 §12b가 자동으로 찾게 | ⏳ 대기 — **오늘 1-13이 세 번째 사례** |
| **G-5**(491차) | 「미측정 0.0%」와 「진짜 0.0%」를 로그에서 구분 | ⏳ 대기 — **오늘 1-13 ⚠②가 같은 문제** |
| **G-6**(491차) | 진입 방향 편향을 「그날 시장」과 나눠 보는 축 | ⏳ 대기 — **오늘 숏 100%(24/24)로 표본 확대** |

### G-7(492차). 「상수 고착 목록」을 하나로 모은다 — G-4의 상위 묶음

**당일 관측에서 출발**: 오늘 하루에 같은 유형이 **셋** 확인됐다.

| # | 상수 | 값 | 관측 실적 | 발견 |
|---|---|---|---|---|
| 1-7 | `_min_conf_sgd` | 0.52 | 5m 예측 20거래일 통과율 **4.57%** → `n≥15` 도달 불가 | 오늘 장중 |
| 1-9 | 앙상블 A/B 임계 | 0.70 / 0.60 | 방향성 conf **10거래일 max 0.5862** | 2026-08-19 |
| **1-13** | `RECOVERY_THRESHOLD` | 0.58 | 일일 정확도 **10일 max 0.4048** | **오늘 장후** |

셋 다 **2026-05 이전 confidence·정확도 분포에 맞춰 박힌 상수**이고, 셋 다 분포가 내려앉은
뒤에도 그대로 남아 분기를 죽였다. 여기에 471차 F-1(15:10 1차 경로 6개월)·474차(중기·장기
CORE 그룹 6개월)를 더하면 **다섯**이다. **매번 사람이 우연히 발견했다.**

**제안**: `config/settings.py`에 `THRESHOLD_REACHABILITY_REGISTRY`(가칭)를 두고
`(상수명, 값, 그 값을 받는 관측 원천, 사전등록 관측창)` 4튜플로 등록한다.
수집기 §12b가 지금 3쌍(앙상블A·앙상블B·TOX스프레드)을 하드코딩하고 있는데, 그것을
**레지스트리 순회**로 바꾼다. 새 임계를 넣는 사람이 레지스트리에 한 줄 추가하면
**다음 날부터 자동으로 도달성이 감시된다.**

- ⚠ **판정 기준을 만들지 않는다.** §12b는 관측 전용이고 그 성격을 유지한다.
- ⚠ **1-13을 이 레지스트리 근거로 임계 인하를 제안하는 것으로 읽지 말 것.**
  레지스트리는 「보이게 한다」이지 「고친다」가 아니다.
- **선행**: G-4(491차)와 같은 코드를 건드린다 → **G-4를 먼저 하고 G-7로 확장**한다.
- **소요**: G-4 이후 40분.

### G-8(492차). 사전등록 판정 창의 **표본 진행**을 일일 점검이 보여준다 (판정은 하지 않는다)

**당일 관측에서 출발**: 1-15 — `[46]`·`[55]`·`[56]` 판정 창이 오늘 열렸는데
그것을 보는 수단은 금요일에만 돈다.

**제안**: 수집기에 §13-b「사전등록 판정 창 — 표본 진행」 표를 추가한다.
`config/settings.py:VALIDATION_CAMPAIGN`에서 `start_date <= 오늘 <= (판정 미완)` 인 채널을
골라 **`채널 · 판정창 시작일 · 경과 거래일 · 현재 표본 / min_samples`** 네 열만 낸다.

- 🔴 **판정·권고·합격선을 절대 출력하지 않는다.** 판정은 금요일 캠페인 체인의 몫이다.
  일일 점검이 판정하면 **사전등록 위반**이며 458차 D6과 같은 사고다.
- 왜 필요한가: `[10] TP2 홀드`가 **7주 연속 0건**이었던 것처럼 **구조적 미발생**이면
  기다려도 채워지지 않는다. 주 단위로만 보면 그것을 알아채는 데 몇 주가 더 걸린다.
- ⚠ 표본 수를 세는 것 자체가 **일부 채널에서는 DB 재구성이 필요**하다(예: `[56]`은
  `trades.db`에서 포지션 단위로 재구성). 비용이 크면 **표본 수 대신 「원천 행수」만**
  세는 축소판으로 시작할 것.
- **소요**: 축소판 30분 · 전량 90분.

### 로드맵·실전전환 기준 반영 제안

| 대상 | 제안 | 근거 |
|---|---|---|
| **실전 전환 기준 ⑧** | F-5(증거금 축) 배선을 **⑧ 해제의 선행조건으로 명시** | 1-8이 오늘 5행 추가(전구간 15행, `meta` 34건 중 8건 = 23.5% 오귀속). 470차가 규명한 순서 *"⑧ 해제 → 증거금 상한 상승 → qty≥3 발생 → [28] 표본 축적"* 에서, **오귀속률이 오르는 시점과 [28]이 깨어나는 시점이 정확히 같다** |
| **26주 WFA 재검증 항목** | 「관측 계층 5종」(470차)에 **C6' 「상수 도달성 레지스트리 갱신」** 추가 | G-7. 468차 G-2가 *"감시 대상 목록 자체가 낡는 것은 일일 점검이 못 잡는다"* 로 등록한 것과 같은 이유 |
| **26주 WFA 재검증 항목** | 「관측 계층 5종」 C3'(GuardFair 무효율)에 **오늘 실측 추가** | 1-5 — **9거래일 54표본 100% 무효.** 사유가 6/6 전부 `현행이 홀드아웃 학습함`으로 동일하다. 470차 C3'가 *"1일 표본이라 판정하지 않았다 — 무효율의 추세를 볼 것"* 이라 했고, 이제 9일이다. ⚠ **아직 판정하지 않는다** — 판정 기준(무효율 몇 %가 문제인가)이 등록돼 있지 않다 |
| **주간회의 2026-08-28** | 안건 ⑤ 추가 — **「상수 고착」을 개별이 아니라 한 묶음으로 처분할지** | 장중 §7-i 5번의 연장. 오늘 세 번째 사례(1-13)가 나왔다 |

---

## 4-p. 진입 승패 사후검증

> 절차는 `references/postmortem.md`. **EOD·P8 완료 확인 후 시작했다** —
> `data/session_state.json`의 `p8_last_success_date=2026-08-24` · `eod_retrain_ok_date=2026-08-24`.

### 4p-0. 3원 대사 (포지션 단위)

| 원천 | 값 |
|---|---|
| 로그 `[Position] 진입` | **24** |
| `ensemble_decisions.entry_executed=1` | **24** |
| `COUNT(DISTINCT trades.entry_ts)` (`entry_source='SYSTEM_AUTO'`) | **24** |

✅ **일치.** `trades` 행수는 **30**(청산 레그)이며 이는 불일치가 아니다 — 부분청산 6건이
별도 행이 된 결과다.

### 4p-1. 오늘의 손익은 「TP1을 밟았는가」 하나로 갈렸다

**포지션 단위 · `net_pnl_krw`**

| 축 | 구간 | n | 승 | 승률 | net | 건당 |
|---|---|---|---|---|---|---|
| **TP1 도달** | **도달(1)** | 19 | 17 | **89.5%** | **+840,242원** | +44,223원 |
| **TP1 도달** | **미도달(0)** | 5 | 0 | **0.0%** | **−772,153원** | −154,431원 |

TP1(1차 이익실현 목표)에 닿은 19건은 **하나도 크게 잃지 않았고**(보호 스톱이 이익을 잠갔다),
닿지 못한 5건이 **전부** 손실이며 그 합이 오늘 총이익의 92%를 상쇄했다.
⇒ **오늘 성적표는 방향 예측의 성적이 아니라 「TP1까지 갔는가」의 성적이다.**

**청산 2축(468차 G-3 — 트리거 / 결과)** — 레그 30건

| 트리거 | 결과 | 건수 |
|---|---|---|
| `보호트레일` | **이익** | **14** |
| `보호트레일` | 손실 | 1 |
| `하드스톱` | 손실 | 5 |
| `손절1차` | 손실 | 4 (전부 부분청산) |
| `TP2` | 이익 | 4 |
| `TP1` | 이익 | 2 |

⚠ **`하드스톱` 라벨로 손절을 세면 안 된다.** 「보호트레일」 15건은 TP1 도달 후의 보호 스톱이며
그중 14건이 **이익 청산**이다. **진짜 손절은 `하드스톱` 5건**이다(465차 `tp1_reached`,
468차 로그 태그).

### 4p-2. 요인 태그별 분해 (A~F)

**A. 등급** — ⚠ 이 축은 확정 결정 `grade_ev_inversion`(부결, 2026-08-01)의 질문이다. **신규 계측 제안 금지, 표본만 기록.**

| 등급 | n | 승 | 승률 | net | 건당 |
|---|---|---|---|---|---|
| 최종 `A` (전부 `원시C` 승격분 14 + `원시X` 1) | 15 | 10 | 66.7% | **−131,558원** | −8,771원 |
| 최종 `C` | 9 | 7 | 77.8% | **+199,647원** | +22,183원 |
| 원시 `C` | 23 | 17 | 73.9% | +125,277원 | +5,447원 |
| 원시 `X` | 1 | 0 | 0.0% | −57,188원 | −57,188원 |

오늘은 **역전**(A가 C보다 나쁨)이지만, 30일 기준 일일 전략 리포트는 반대다 —
`등급별 순EV(30일): A=+5,269원(155건,승61%) C=−13,789원(67건,승66%)`.
⚠ **n=24 · 1거래일. 확정 결론 금지(313차).**
⚠ 앙상블 **원시 `A` 0건** — CLAUDE.md 476차 각주(2026-07-30 이래 `raw_grade='A'` 0건)와 정합.

**B. 호라이즌**

| 호라이즌 | n | 승 | 승률 | net | 건당 |
|---|---|---|---|---|---|
| `1m` | 1 | 1 | 100% | +17,412원 | +17,412원 |
| `3m` | 16 | 13 | **81.2%** | **+220,045원** | +13,753원 |
| `5m` | 7 | 3 | 42.9% | **−169,368원** | −24,195원 |

⚠ 전부 **단기 그룹**(1m·3m·5m)이다 — 중기·장기 그룹은 474차대로 **구조적 도달 불가**이며
이는 정상이다. ⚠ n=7의 5m을 두고 결론 내지 말 것.

**C. Hurst 레짐**

| Hurst | n | 승 | 승률 | net | 건당 |
|---|---|---|---|---|---|
| `mean-revert` | 12 | 10 | 83.3% | +351,922원 | +29,327원 |
| `neutral` | 9 | 5 | 55.6% | **−421,426원** | −46,825원 |
| `trend` | 3 | 2 | 66.7% | +137,593원 | +45,864원 |

**D. 계약 수** — ⚠ **포지션 단위**다. 2026-08-01 §9-3의 청산 레그 단위 통계 4종은 **재인용 금지**(417차 정정).

| 계약 | n | 승 | 승률 | net | 건당 |
|---|---|---|---|---|---|
| 1계약 | 18 | 15 | 83.3% | **+489,353원** | +27,186원 |
| 2계약 | 6 | 2 | 33.3% | **−421,264원** | −70,211원 |

⚠ **n=6. 확정 결론 금지.** 2계약 6건 중 5건이 10:15~11:57 오전 급락 구간에 몰려 있어
**계약 수 효과와 시간대 효과가 분리되지 않았다.** 417차가 밝힌 진짜 축은 **손절 준수율**이며,
그 축은 아래 E에서 오늘 **역전되지 않았다**.

**E. 손절 준수율** (417차 축 — 유일하게 유의했던 관계) — 진짜 손절 5건 전량

| 진입 | 계약 | 체결평균가 | 의도 손절가 | 의도 폭 | 실현(최종 레그) | **준수율** |
|---|---|---|---|---|---|---|
| 10:37 | 2 | 1072.13pt | 1075.49pt | 3.36pt | −3.35pt | **0.997** |
| 10:50 | 1 | 1071.50pt | 1074.08pt | 2.58pt | −2.70pt | **1.047** |
| 13:11 | 2 | 1054.17pt | 1056.57pt | 2.40pt | −2.29pt | **0.954** |
| 13:50 | 1 | 1058.16pt | 1059.87pt | 1.71pt | −1.82pt | **1.064** |
| 14:37 | 1 | 1054.00pt | 1056.15pt | 2.15pt | −2.00pt | **0.930** |

- 최대 초과 **6.4%**(13:50, 1계약). 평균 **1.0**.
- **1계약 3건 평균 1.014 / 2계약 2건 평균 0.976** — **2계약이 오히려 더 잘 지켰다.**
- ⇒ **417차가 지목한 「큰 포지션이 손절선을 못 지킨다」(1계약 29.4% vs 3계약+ 68.8%)의
  재현 없음.** 오늘 최대 계약이 2라 그 구간 자체가 없었던 것도 있다.
- ⚠ **n=5. 확정 결론 금지(313차).**

**F. MetaGate 액션** — ⚠ `meta_gate` 확정 결정(탈락 확정, 2026-08-01 / 2026-08-12 재측정 대상)의 질문. 표본만 기록.

| `meta_action` | n | 승 | 승률 | net | 건당 |
|---|---|---|---|---|---|
| `take` | 15 | 11 | 73.3% | +127,605원 | +8,507원 |
| `reduce` | 9 | 6 | 66.7% | −59,516원 | −6,613원 |

**보조: KellySkip 권고 코호트** — ⚠ `kelly_skip` 확정 결정(차단 도입 보류, 3관문 사전등록, 2026-08-08)의 질문. **신규 계측 제안 금지.**

| `kelly_advised_skip` | n | 승 | 승률 | net | 건당 |
|---|---|---|---|---|---|
| 1 (건너뛰라고 권고) | 8 | 7 | **87.5%** | **+436,226원** | +54,528원 |
| 0 | 16 | 10 | 62.5% | −368,137원 | −23,009원 |

**오늘은 「건너뛰라」던 8건이 오히려 오늘 이익 전부를 냈다.** 채널 [8]의 표본으로만 기록하며,
**차단 도입 논의를 재개하지 않는다**(3관문 사전등록 진행 중).

### 4p-3. 313차 다섯 갈래 적용

| 갈래 | 적용 |
|---|---|
| **일자단위 판정** | 오늘은 **1거래일**이다. 위 모든 분해는 **일 내(intra-day) 층화**이므로 일자단위 검정이 아니다 → **어떤 축에도 확정 결론을 내지 않았다** |
| **overlap 보정** | 24포지션 중 동시보유 없음(전부 순차 — 청산 후 쿨다운 12건 기록). 중복 노출 보정 불필요 |
| **이상치 분해** | 🔴 **필수 기록** — 전체 **+68,089원** / **최고 1포지션 제거 시 −99,311원** / 상하위 1건씩 제거 시 +161,905원 / 상위 2건 제거 시 −261,719원. **오늘 흑자는 10:26 포지션(+118,764원) 1건에 의존한다.** 489차 D6이 캠페인 전체에서 *"상위 2일을 빼면 −1,679,994원"* 이라 한 것과 **같은 구조가 하루 안에서도 나타난다** |
| **사전등록(기준 불변)** | 오늘 어떤 합격선도 만들거나 바꾸지 않았다. §13 레지스트리 19건 대조 완료 |
| **부호 일관성** | 등급 축이 **오늘과 30일이 반대 부호**(A: 오늘 −8,771원 / 30일 +5,269원) → **단일 근거로 쓸 수 없다.** `grade_ev_inversion` 부결 결정이 유지되는 이유와 정합 |

---

## 5-p. 수익률 향상방안

> ⚠ **오늘 흑자를 근거로 전략을 바꾸자는 제안을 하지 않는다.** 489차 D6이 실전 전환 기준
> ①·④를 **미충족**으로 확정했고(최근 20거래일 **−107,232원** — 오늘 갱신값), 오늘 +68,089원은
> 상위 1건을 빼면 적자다(§4p-3).

아래는 **당일 관측에서만** 출발한 것이며 전부 **계측 선행**을 전제한다.

1. **TP1 도달 여부가 오늘 손익을 전부 설명한다(89.5% vs 0.0%).**
   → 이미 사전등록 채널이 있다: `tp1_geometry_shadow`(현행 유지 확정, 2026-08-08) ·
   `tp1_protect_offset_shadow`(미적용 유지, `atr_lock_0.75`만 감시 후보 승격, 2026-08-08) ·
   `quantile_tp_shadow`(승격 폐기, 2026-08-08). 🔴 **신규 계측을 제안하지 않는다** —
   0818 R-1이 정확히 이 자리에서 사고를 냈다. 오늘 표본은 그 채널들의 입력으로만 쓴다.

2. **`5m` 호라이즌이 오늘 유일한 적자 호라이즌이다(7건 −169,368원, 승률 42.9%).**
   → 그런데 `5m`은 1-7이 밝힌 대로 **장중 자동 재학습 트리거의 기준 호라이즌**이기도 하며,
   그 표본 게이트(`conf≥0.52` 통과율 4.57%)가 사실상 닫혀 있다. **같은 호라이즌이 성과에서도
   최하위이고 자가교정 경로에서도 굶고 있다.** ⚠ **인과를 단정하지 않는다** — n=7이다.
   → **F-4(491차) 계측이 먼저다.** 20거래일 축적 후 「5m 통과율 낮은 날의 5m 성과」를
   일자단위로 볼 것. 그 전에는 어떤 조치도 제안하지 않는다.

3. **진입 24건이 전부 SHORT이고, 오늘 지수는 1087.98 → 1053대(약 −3.2%)로 밀렸다.**
   → 편향인지 시장인지 **오늘 자료로는 못 가른다.** G-6(491차, 「그날 음봉 분 비율」로
   정규화)이 그 축이며 미구현이다. `O-11(491차)` 누적만 계속한다.
   ⚠ 방향 편향을 「잘 맞혔다」로 읽지 말 것 — `[Bias⚠] 5m 적중=33%(10/30) UP=0 DN=18 FL=12
   [DN편향⚠ 60%]`(15:09) 는 **5m 예측이 하루 종일 UP을 한 번도 내지 않았음**을 보여준다.
   맞는 방향으로 편향된 날과 편향이 없는 날은 다르다.

4. **비용 축**: gross **+116,000원** − 수수료 **47,911원** = net **+68,089원**.
   **수수료가 gross의 41%** 다. 24포지션 중 15건이 `보호트레일`로 **작은 이익**(+0.20~+0.76pt)에
   닫혔고, 그런 건의 왕복 수수료(1계약 약 1,580~1,620원)가 이익을 크게 갉아먹는다.
   → ⚠ 이것도 `tp1_protect_offset_shadow` 계열 질문이다. **신규 제안 금지.**
   오늘 실측(수수료/gross 41%)을 그 채널 표본으로 기록한다.

---

## 6-p. 종합 완성본 — 오늘 하루를 닫는다

### 6p-1. 종합 판정표

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| **장전** | **조건부 정상** | P0 0건. 기동·모델·매크로·설정 불변식 정상. CORE 축퇴 미해소(1-1) · 보정기 정체(1-2) · 죽은 기한 3곳(1-3) |
| **장중** | **조건부 정상** | P0 0건. 매분 루프 221/221분 · 강제 X 위반 0 · 코드변경 0. 관측 계층 결손 3건(1-7·1-8·1-10) |
| **장후** | **조건부 정상** | P0 0건. **절대원칙 6종 전부 준수.** 매분 루프 371/371분(100%) · 종가 포지션 0 · EOD 6/6 성공 · 3원 대사 24=24=24. 신규 결손 1건(1-13) |
| **당일 최종** | **조건부 정상 (수익)** | 포지션 24건 승 17(70.8%) · **net +68,089원**(자본 5천만원 대비 **+0.14%**). 절대원칙 위반 **0건**. ⚠ 상위 1포지션 제거 시 **−99,311원** · 롤링 20거래일 **−107,232원** |

### 6p-2. 당일 이상점 통합 대장

| # | 항목 | 발견 국면 | 최초→최종 심각도 | 최종 처분 |
|---|---|---|---|---|
| **1-1** | 단기 CORE `ofi_norm` 장전 축퇴 미해소 | 장전 | P1 → P1 | 🔄 지속 (정정본 `1-10`) |
| **1-2** | 앙상블 확률 보정기 스냅샷 9거래일 정체 | 장전(0821 승계) | P1 → — | ✅ **해소** — pkl mtime 15:40:03 · `저장 완료 n=1478` |
| **1-3** | 스킬 문서 3곳의 죽은 CB② 기한 | 장전 | P1 → P1 | 🔄 지속 (F-1 적용 대기) |
| **1-4** | 장전 점검 개장 36초 뒤 실행 | 장전 | P2 → — | ✅ 해소(국면 조건). O-10(0821) 카운터 내일 재시작 |
| **1-5** | `GuardFair_유효` 100% 무효 고착 | 장전(0821 O-9) | P2 → P2 | 🔄 지속 ⬆ **9거래일 54표본** |
| **1-6** | 리포 루트 산출물 + 미커밋 | 장전 | P2 → P2 | 🔄 지속 ⬆ **5건 → 7건** |
| **1-7** | 장중 자동 재학습 조건 구조적 도달 불가 | 장중 | P1 → P1 | 🔄 지속 — 전일 **369/369** `cond=False`, `max(n)=4` |
| **1-8** | 수량 축소 원인 오귀속 | 장중 (0821 R-1 격상) | P2→**P1** → P1 | 🔄 지속 ⬆ **3행 → 5행**, 전구간 15행 |
| **1-10** | 개장 후 CORE 축퇴 재발·고착 (↩️ 1-1 정정) | 장중 | P1 → P1 | 🔄 지속 ⬆ **75% → 80.6%**, EOD까지 이어짐 |
| **1-11** | 같은 이름 `acc30m` 두 원천 불일치 | 장중 | P2 → P2 | 🔄 지속 — 쌍 표본 1건, 확정 결론 금지 |
| **1-12** | 30m 온라인 학습기 단방향 붕괴 반복 | 장중 | P2 → P2 | 🔄 지속 — 전일 30m **7회** |
| **1-13** | 🆕 학습률 조절기 상한 고착 + 하향 경로 도달 불가 | **장후** | — → **P1** | 🆕 신규 — F-7·G-7 |
| **1-14** | 🆕 EOD DB 정리 실패/성공 동시 기록 | **장후** | — → **P2** | 🆕 신규 — F-8 · O-13 |
| **1-15** | 🆕 판정 창은 열렸는데 리포트는 금요일 전용 | **장후** | — → **P2** | 🆕 신규 — G-8. **결함 아님**(일정 구조) |

> `1-9`는 오늘 쓰지 않았다 — 2026-08-19 등록 「앙상블 등급 임계 미도달」의 고유 ID다.
> §12b가 오늘 `📌 미도달(10일 연속)`로 재출력했으나 이는 **판정의 재계산이지 미조치의 증거가
> 아니다**(함정 ①). 2026-08-28 주간회의 안건으로 이월 상태 그대로다.

**집계**: P0 **0건** · P1 **6건**(1-1·1-3·1-7·1-8·1-10·**1-13**) · P2 **7건**
(1-5·1-6·1-11·1-12·**1-14**·**1-15** + 해소된 1-4) · **해소 3건**(1-2·1-4 + 확인필요 2건).

### 6p-3. 종가 기준 최종 손익 · 절대원칙 판정

**손익 (포지션 단위 — 청산 레그 단위 아님)**

| 항목 | 값 |
|---|---|
| 포지션 | **24건** (청산 레그 **30건** = 최종 24 + 부분 6) |
| 승 / 종료 | **17 / 24 = 70.8%** |
| 합계 pt | **+2.32pt** (코스피200 선물 지수 포인트, gross 기준) |
| gross | **+116,000원** |
| 수수료 | **−47,911원** (gross의 **41%**) |
| **net 실현손익** | **+68,089원** (`SIZING_TARGET_CAPITAL_KRW` 5천만원 대비 **+0.14%**) |
| 검산 | 로그 배너 `15:40:02 일일 마감 \| 승=17 패=7 PnL=+68,089원` — **차 0원** ✅ |
| 평가손익 | **0원** (종가 포지션 없음) |
| **MDD** | **3.64% (자본 대비)** / 147.4% (peak 대비) — 낙폭 1,819,662원. 🔴 **판정에는 자본 대비만 쓴다**(461차) |
| 종가 포지션 | **0계약** (FLAT). 마지막 청산 14:40:40 |
| 진입 방향 | **SHORT 24 / LONG 0** (100% 숏) |
| 계약수 분포 | 1계약 18 · 2계약 6 (3계약 이상 **0건** — 470차대로 증거금 상한이 2에서 먼저 자름) |
| 차단 | **88건 / 40종** — 최다 `등급X — 미통과: 2_confidence`×37(42%) |
| 사이저 호출 | **53건** (출력 1계약×34 · 2계약×6 · **3계약×13**) |
| 실효 상한 | 품질게이트 `[SizerMatch]` **5건** · 증거금 `[MarginCap] state=CAP` **5건** |
| Circuit Breaker | 이벤트 **12건** — `연속 손절 1·2·3회` 카운터 정상 증가, **정지 0회**(`CB_CONSEC_STOP_LIMIT=9999`, 의도된 상태) |
| 매분 루프 커버리지 | **371/371분 = 100.0%** (09:00~15:10) · 10분 이상 공백 **0건** |
| 예측 DB | `ensemble_decisions` **369행** · `entry_executed=1` **24행** · `predictions` **1,253행**(레이블 완료 **1,214행 = 96.9%**) · `sizing_trace` **53행** = `[Sizer]` 호출 53건 ✅ |

**절대원칙 6종 — 최종 판정**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | **15:10 강제청산 (오버나이트 금지)** | ✅ **준수** | 종가 포지션 **0계약**. 마지막 청산 `14:40:40`. `15:11:02 [SchedForceExit] … status=FLAT engine=0ct broker_cached=0ct bar_pass=2회 → 청산 대상 없음(정상)`. 15:16:02 `포지션=FLAT` 재확인. ⚠ **「청산할 것이 없었다」이지 「청산을 실행했다」가 아니다** — 실전 전환 기준 ②의 선행 확인(실집행 1회)은 **여전히 미충족**(2026-06-10 이래) |
| ② | **Circuit Breaker 5종** | ✅ **준수** | CB① 신호 반전 정지 0 · **CB②** 카운터 정상 증가(`연속 손절 1·2·3회`, 부분청산 4건은 `카운터 미증가(포지션 단위, 470차 C1)`로 올바르게 제외 ✅), 정지 0회는 `9999` 예외로 **의도된 상태** · **CB③** `[DBG-CB] acc30m` 8개 값으로 변동(고착 아님), 차단은 `CB3_P4_GRADE_BLOCK_ENABLED=False`로 의도적 비활성 · CB④ ATR 정지 0 · **CB⑤** 파이프 지연 최대 0.507초 = `CB_PIPE_PAUSE_MS` 5,000ms의 **10%**. §12 `CB_state` 전 구간 `NORMAL` |
| ③ | **CORE 피처 호라이즌 그룹별 분리** | 🟠 **조건부 준수 — 규칙은 지켰고 재료가 약했다** | **강제 X 규칙(`vwap_position`) 위반 0건** ✅ — 진입 24건 전부 `vwap✅`. 등급 하락 규칙도 정상 작동(`ofi❌`×16 · `cvd❌`×13 → `C급`×9). 진입 전부 `1m/3m/5m` = **단기 그룹**(중기·장기는 474차대로 도달 불가 = 정상). **단 `ofi_norm`이 개장 후 refit 29/36(80.6%)에서 identity 강제**(1-10) — **위반이 아니라 관측 기준 미달**이다 |
| ④ | **COM 콜백 내 `dynamicCall`·`emit` 금지** | ✅ **준수** | `0xC0000409` · `STATUS_STACK_BUFFER_OVERRUN` · `STACK_BUFFER` **0건**. 프로세스 무응답·재기동 0. `[FaultHandler] 활성화 PID=7128 행감지=30s` 정상. 매분 루프 무중단 371/371 |
| ⑤ | **`GetRepeatCnt`/`GetCommData` 파라미터 구분** | ✅ **준수(간접)** | TR 수신 오류 0건. `09:20:02 [CybosInvestor] fetch#21 futures_supported=True program_supported=True option_supported=True` 정상 파싱. `[CybosDailyPnl]`×162 정상 |
| ⑥ | **알파 리서치 봇 자동 통합 금지** | ✅ **준수** | `자동 통합`·`auto_integrate`·`백테스트 큐` **0건**. 미커밋 산출물(`26주WFA_*` 08:03~08:10 · `gate_blocking_*` 08-23 17:56)은 전부 **장 시작 전 사람이 돌린 오프라인 분석** |

**한시 예외 4종 — 적용 현황 (전부 의도대로, 무단 변경 0)**

| 예외 | 값 | 상태 |
|---|---|---|
| CB② `CB_CONSEC_STOP_LIMIT` | `9999` | ✅ 의도된 비활성. 기한은 **날짜 → 사건**(⑧ 해제와 동일 커밋, 489차 D2). **「2026-08-29」 재인용 금지** |
| CB③-P4 `CB3_P4_GRADE_BLOCK_ENABLED` | `False` | ✅ 의도된 비활성(296·297차) |
| FP-CRITICAL `FP_CRITICAL_GRADE_BLOCK_ENABLED` | `False` | ✅ 의도된 비활성(371차 재설계 후 라이브 관찰 중) |
| TOX-SEVERE-SPREAD `TOXICITY_SEVERE_SPREAD_BLOCK_ENABLED` | `False` | ✅ 의도된 비활성. **489차 D7로 실전 전환 기준 ⑨ → 26주 WFA 재검증 항목 이관.** 오늘 §12b `TOX스프레드(진입≥20틱)` 관측 max **9.003틱** — 📌 미도달 5일 연속 |

**설정 불변식** — 다이제스트 §3 **26행 전부 `일치` 또는 의도된 `값 확인`**. `불일치`·`미발견` **0건**.
차단 게이트 전수 **29개 중 7개 `False`** — 전부 **기록됨 또는 기능 토글**(신규 무기록 게이트 0건).

**매분 9단계 — 최종 판정**

| STEP | 판정 | 근거 |
|---|---|---|
| **루프 생존** | ✅ 정상 | 371/371분 = **100.0%** · 08:55~15:12 10분 이상 공백 **0건** |
| **STEP 1** 과거 예측 채점 | ✅ 정상 | `predictions` 레이블 완료 **1,214/1,253행(96.9%)**. T-30 채점 지속(30m 369행) — CB③ 입력원 생존 ✅ |
| **STEP 2** SGD 온라인 자가학습 | 🟠 조건부 | `[SGD] N건 학습` 정상 발행(368건). 단 30m 단방향 붕괴 **7회**(1-12) · 5m 정확도 버퍼 `n≤4` 고착(1-7) · **학습률 상한 고착**(1-13 🆕) |
| **STEP 3** GBM 배치 재학습 | 🔴 **이상 — 장중 축 조건 도달 불가** | `[DriftRetrain] state=SKIP` **369/369**, `cond_a=False cond_b=False` 전량(1-7). ⚠ 주간(월 08:50)·EOD 정기 경로는 **별개이며 정상** — 오늘 EOD 재학습 **6/6 교체 성공** |
| **STEP 4** 피처 생성 | 🟠 조건부 | NaN·결측 로그 0건. 수급 TR 09:20:02 정상화. 단 `ofi_norm` identity 강제 **228건**(1-10) |
| **STEP 5** 멀티 호라이즌 예측 | ✅ 정상 | `[Ensemble] dir=` **364건**. `WeightCollapse` 75건 = **20.6%** — 사전등록 밴드 내(`O-22` 통과) |
| **STEP 6** 앙상블 진입 판단·등급 | ✅ 정상(규칙 준수) | `vwap❌` 진입 **0건** → 절대원칙 ③ 강제 X 준수. 차단 88건/40종, 최다 `2_confidence` 37건(42%) — **한 게이트 압도 없음**(316차 HurstGate 63% 패턴 아님) |
| **STEP 7** 진입 실행 | 🟠 조건부 | 진입 24건 전부 체결. 실효 상한: 품질게이트 5 · 증거금 5. **귀속 오류 5행**(1-8) |
| **STEP 8** 청산 트리거 5단계 | ✅ 정상 | 순서 유지 — 하드스톱 5 → 손절1차 4 → TP1 2 · TP2 4 · 보호트레일 15 → **15:10 청산 대상 없음(정상)** → 15:18 안전망 `포지션=FLAT` |
| **STEP 9** 예측 DB 저장 | ✅ 정상 | `ensemble_decisions` 369행 · `sizing_trace` 53행 = `[Sizer]` 53건 ✅. ⚠ 마감 DB 정리에 실패/성공 동시 기록(1-14) |

**EOD 재학습 (`phases.md` C-3) — 전항 통과**

| 확인 | 결과 |
|---|---|
| `py310_64` 실행 | ✅ `Python : 3.10.20 64-bit` · `sklearn 1.0.2` · `numpy 1.26.4` — **정상이며 이상 환경이 아니다**(191차 결정) |
| `pickle protocol=4` | ✅ `learning/batch_retrainer.py:1226·1229·1312·1332·1334` 전부 `protocol=4` (py37_32 로드 호환) |
| OOM 없이 완주 | ✅ 39,755행 × 105피처 · 로드 22.0s + 재학습 135.8s = **158.0s**. 호라이즌 교체 **6/6** · 가드보류 **0** |
| RF 학습 | ✅ 6/6 `ready=True` · OOB 1m 50.9% / 3m 47.7% / 5m 46.9% / 10m 51.2% / 15m 52.6% / 30m 59.4% |
| SelfCheck | ✅ `label_state ok=True detail=6/6 호라이즌 현행 레이블(fixed) 학습본` |
| **P8 스케일러 재적합** | ✅ `n=500봉 elapsed=0.03s horizons=6개`. `data/session_state.json` — `p8_last_success_date=2026-08-24` · `eod_retrain_ok_date=2026-08-24` **둘 다 오늘** |
| 완료 마커 | ✅ `data/eod_retrain_done_20260824.txt` (15:47:39) |
| RegimeFingerprint | ✅ 학습분포 갱신 **17,977행 기준** · CORE 3피처 전부 확보 |
| CUSUM | ✅ 이벤트 9,812/39,755(24.7%) < 30% → 전체 사용 |
| ⚠ GuardFair | 🔴 **6/6 전부 `무효`** — 1-5. 판정 무영향(섀도)이므로 교체는 정상 진행됐다 |
| ⚠ P8 직후 | `[CORE준비도] trigger=E_EOD 축퇴 1/6 — ofi_norm×6hz` — **내일 장전으로 이월**(1-10) |

**검증 캠페인 (`phases.md` C-5)**

- 오늘은 **월요일**이다. 캠페인 리포트 생성은 **금요일 전용 체인**이며(`scripts/eod_retrain.py:63`)
  오늘 EOD는 P8에서 **정상 종료**했다. **「캠페인 리포트 없음」은 결함이 아니다**(476차 명시).
- 최신 산출물은 `docs/정기점검/금요일점검/MW0602/validation_campaign_report_20260821.md`
  (169.2KB, 08-21 15:48) — **날짜본으로 보존됨**, 고정 파일명 덮어쓰기 **없음** ✅.
  `UNKNOWN/` 폴더 생성 **없음** ✅. FIFO(`VALIDATION_REPORT_KEEP_WEEKS=4`) 정상 ✅.
- 함정 ① 대조: 다이제스트 §13 **확정 결정 19건** 전수 확인. 오늘 §3-p·§5-p에서
  `grade_ev_inversion` · `kelly_skip` · `tp1_*` 3계열 4채널이 **이미 결정된 질문**임을 확인해
  **신규 계측 제안을 하지 않았다.**
- ⚠ 판정 창이 오늘 열린 3채널(`[46]`·`[55]`·`[56]`)은 **금요일에야 렌더링된다** → `1-15`.

---

## 7-p. 다음 거래일 관측 예정 (제4부)

### 7p-A. 오늘 등록하는 신규 관측

| ID | 관측 대상 | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| **O-12(492차)** 🆕 | `[DriftAdjuster]` 액션·alpha | F-7 배포 후 `DRIFT_UP_SATURATED` + `(상한 포화 — 무연산)` 출현, `n=`·`src=` 병기. 배포 전에는 **alpha 값 분포만 기록**(오늘 `0.01000` 단일값 · 9거래일 연속) | **F-7 배포 다음 거래일** |
| **O-13(492차)** 🆕 | `[Retrain] DB pruning 실패: database table is locked` | **재현 여부.** 오늘 1건. 3거래일 중 2일 이상 재현되면 상시 조건 → F-8 우선순위 상향. 1회성이면 종결 | **2026-08-27**(3거래일 누적) |
| **O-14(492차)** 🆕 | `[Bias⚠] 5m … UP=n DN=n FL=n` 의 UP 건수 | 오늘 **UP=0**(30건 중). 방향 편향이 예측 단계에서 나오는지 진입 단계에서 나오는지 가른다. `O-11(491차)`의 상류 축. **20거래일 누적 후** 일자단위 비교. ⚠ **확정 결론 금지** | **누적** — 매 장후 |
| **O-15(492차)** 🆕 | `TP1 도달` 코호트 승률·건당손익 | 오늘 도달 19건 89.5% +44,223원 / 미도달 5건 0.0% −154,431원. ⚠ **`tp1_*` 확정 결정 3건이 이미 있는 질문** — 신규 계측 제안 금지, **표본만 누적**해 그 채널에 넘긴다 | **누적** — 매 장후 |

### 7p-B. 이월 (기준 무변경)

| ID | 판정 예정일 | 오늘 현황 |
|---|---|---|
| `O-1`(491차) 다이제스트 §3 CB② 문구 | **2026-08-25 장전** | F-1 미적용 → 오늘도 죽은 날짜 인쇄 |
| `O-2`(491차) 장전 마지막 축퇴 개수 | 누적(2일차) | 오늘 `1` |
| `O-4`(491차) `[Calibration]` 축퇴·FloorShadow | **2026-08-25 EOD** | 전일 174건 |
| `O-6`(491차) `[DriftRetrain]` `max(n)` | 누적(1일차) | **오늘 4** (조건B 요구 15) |
| `O-7`(491차) 미설명 축소행 | F-5 배포 다음 거래일 | 오늘 **5행** · 전구간 15행 |
| `O-8`(491차) `acc30m` 쌍 | 20거래일 누적 | 오늘 쌍 1건 |
| `O-9`(491차) 30m SGD 리셋 후 CB③ 접촉 | 20거래일 누적 | 오늘 리셋 7회 · CB③ 0회 |
| `O-10`(491차) 개장 후 축퇴율 | 누적(2일차) | **오늘 29/36 = 80.6%** |
| `O-11`(491차) 진입 방향 편향 | 누적 | **오늘 숏 100%(24/24)** |
| `O-3`(0821) 런처 GUARD 연속일 | 판정 기준 미등록 | **7거래일 연속** — 기준부터 정할 것 |
| `O-4`(0821) `[Capital]` 괴리율 | 관측 계속 | **−41.5%** (횡보) |
| `O-17`(0821) `entry_horizon` 소비처 전수 | **2026-08-25 장후** | 미착수 |
| `O-23`(0821) `sizing_trace.margin_cap_qty` | F-5 배포 후 | 필드 부재(181행 중 0행) |
| `O-20·O-21`(0821) 월간 로그 정리 / 캠페인 metrics | **2026-08-28(금)** | 금요일 전용 체인 |
| `O-26~O-29`(489차) `[56]`·`[44]`·`[46]` | **2026-08-28(금)** | 판정 창 1일차. **렌더링 수단이 금요일 전용**(1-15) |
| `O-30~O-32`(490차) `[2-A]`·`[2-B]`·사이드카 | **2026-08-25** | 원 등록대로 |

### 7p-C. 기한 도래 항목

| 항목 | 기한 | 남은 거래일 | 조치 |
|---|---|---|---|
| 🔴 ~~CB② 복원 — 2026-08-29~~ | ❌ **효력 종료** | — | **489차 D2로 날짜 → 사건 전환.** 복원 시점 = **실전 전환 기준 ⑧ 해제와 동일 커밋.** ⚠ **「8월 29일」 재인용 금지.** 도구 3곳이 아직 옛 날짜를 인쇄한다 → **1-3 · F-1** |
| `[2-B]` 스코어러 재개 게이트 | 판정 창 **2026-08-25~** | **1** | 490차 P1 사전등록. `auc_net_directional ≥ 0.53` **4주 연속**. 현재 ⏳ INSUFFICIENT |
| `[46]`·`[55]`·`[56]` | 판정 창 **2026-08-24~** | 오늘 1일차 | 표본 진행은 **금요일 리포트에서** 처음 보인다(1-15). ⚠ **문턱을 관측값에서 역산하지 말 것** |
| **주간회의 2026-08-28** | 주간회의 | **4** | ① 1-9 앙상블 등급 임계 재설계(§12b **10일 연속 미도달**, max 0.5862) ② F-8 채널 생산부 결손 잔여 확인 ③ 멀티PC 컨벤션 보강 ④ `auc_strong_directional` 청산 축 승격 여부 ⑤ 🆕 **「상수 고착」 3건(1-7·1-9·1-13)을 개별이 아니라 한 묶음으로 처분할지** |

---

## 8-p. dev_memory 반영 (제5부)

- `dev_memory/DECISION_LOG.md` 추가:
  `## 2026-08-24 (MW0602 492차 — 장후 점검: 학습률 조절기 상한 고착 + 보정기 정체 해소)`
- `dev_memory/NEXT_TODO.md` 추가: **Fix 2건**(F-7·F-8 / 492차) · **고도화 2건**(G-7·G-8 / 492차) ·
  **관측 4건**(O-12~O-15 / 492차)
- 완료 처리: **`1-2` ✅ 해소**(0821 F-1 라이브 검증 완료) · **`P-1`·`P-2`·`P-3`·`P-4`·`P-8`·`P-9`
  ✅ 판정 통과** · **`O-5`(491차) ✅ 종결** · **`O-6`(0821)·`O-22`(0821) ✅ 판정 통과**
- 세션 차수: **492차** — 원격(git) 기준 최신이 `491차`(`135c00f`)이므로 그 다음
- 커밋 제목 접두: `[MW0602]`
- ⚠ **기존 내용을 덮어쓰지 않는다** — 두 파일 다 2.0MB·1.1MB 누적 기록이다

> 📄 **스킬 템플릿 메모 (장전 491차 메모 유지·보강)**: `report_template.md` **255행**의
> `CB② 복원 … 2026-08-29` 하드코딩은 **오늘도 그대로**이며, `phases.md:45`·
> `collect_evidence.py:168`도 같다. 오늘 다이제스트 §3이 세 번째로 죽은 날짜를 인쇄했다.
> **F-1(491차)이 이 셋을 함께 고친다.** 이 보고서는 §7p-C에서 취소선 + 효력 종료로 처리했다.
>
> 📄 **대원칙 A·B 준수 메모**: 이 파일은 **하루 한 파일 · append 전용**으로 작성됐다.
> `-pre`/`-intra`/`-post` 접미사 파일을 만들지 않았고, 장전·장중 절의 본문을 고치거나
> 지우지 않았다(정정은 `1-10`에서, 앞 절에는 장중 세션이 남긴 포인터 한 줄만).

---

## 사용자 조치

> 🟢 **오늘은 잘 끝났습니다.** 24번 중 17번 이겨서 수수료 빼고 **68,089원** 벌었고,
> 지켜야 할 규칙 여섯 가지를 **전부 지켰습니다.** 오후 3시 10분에 넘길 물량도 없었습니다.
> 지난 사흘간 말씀드리던 "예측 확률 다듬는 장치가 멈춰 있다"는 문제는 **오늘 저절로
> 풀렸습니다** — 금요일에 해 둔 수정이 실제로 작동했습니다.
>
> ⚠ 다만 **오늘 번 돈은 가장 잘된 거래 한 건에 의존합니다.** 그 한 건을 빼면 오히려
> 99,311원 손해입니다. 최근 20거래일 통산도 여전히 **−107,232원**입니다.
> **오늘 하루로 "잘 되고 있다"고 판단하지 말아 주세요.**

| # | 지금 할 일 | 걸리는 시간 | 안 하면 생기는 일 |
|---|---|---|---|
| **1** | **"F-1 · F-5 · F-4 순서로 구현해"라고 말씀해 주세요.** 장 마감 후라 지금 고칠 수 있습니다. 세 가지입니다 — (1) 점검 도구 세 군데가 **이미 없어진 마감일(8월 29일)** 을 오늘도 세 번째로 인쇄했습니다. (2) 주문 수량이 줄어든 이유를 장부가 **틀리게** 적는 것이 오늘 다섯 번 더 늘어 **누적 15번**이 됐습니다. (3) 오전에 예측이 어긋났을 때 스스로 다시 공부하는 기능이 **켜질 수 없는** 상태인 것을 매일 보이게 만듭니다. | 제가 **약 60분**, 회귀 테스트 포함 **90분** | (1) 내일 아침 점검이 없는 마감일을 근거로 잘못 보고합니다. (2) 나중에 "진짜 돈을 넣어도 되는가"를 이 장부로 판단하는데, 자금을 키우는 순간 이 오기록이 **더 자주** 발생합니다. (3) 같은 종류의 문제를 지난번엔 6개월, 그 전엔 3개월 동안 아무도 몰랐습니다 |
| **2** | **(오늘 새로 찾은 것) "F-7 구현해"라고 말씀해 주세요.** 예측이 자꾸 틀릴 때 "공부 속도를 높여라"라는 자동 장치가, **이미 최대치에 도달해 아무것도 못 하면서 매일 '높였다'고 기록**하고 있습니다. 최근 9일 중 6일이 그랬습니다. 속도를 되돌리는 길도 막혀 있습니다. **지금 고치자는 게 아니라, 이 상태가 매일 눈에 보이게만 만들자는 것입니다.** 손잡이 자체는 건드리지 않습니다. | 제가 **약 25분**, 확인 포함 **40분** | 기록만 보면 매일 대응하고 있는 것처럼 보여서, 언젠가 "왜 안 고쳐졌지"를 물을 때까지 아무도 모릅니다. 오늘까지 **2주 이상** 그랬습니다 |
| **3** | (선택) **"F-6 · F-8 · F-2 · F-3 구현해"** — 작은 것 네 가지입니다. 아침 판정 기준 수정, 마감 정리 로그 구분, 프로젝트 폴더에 굴러다니는 임시 파일 정리, 그리고 오늘 새벽에 돌리신 26주 점검 결과물 저장입니다. | 제가 **약 40분** | 임시 파일이 계속 쌓이고(오늘 5건 → 7건으로 늘었습니다), 26주 점검을 돌린 기록이 남지 않습니다 |
| **4** | **이번 주 금요일 회의(8월 28일)에 안건이 하나 더 늘었습니다 — 다섯 가지가 됐습니다.** 새로 는 것은 이것입니다: **예전 성적을 기준으로 정해 둔 숫자들이, 성적이 내려앉은 뒤에도 그대로 남아 장치를 무력화한 사례가 오늘로 셋이 됐습니다.** 하나씩 고치지 마시고 **한 묶음으로 다시 정하실지** 봐 주세요. | **30분** (기존 20분 + 10분) | 네 번째, 다섯 번째 사례가 앞으로도 하나씩 우연히 발견됩니다. 이미 다섯 건이 그렇게 발견됐고, 각각 3~6개월씩 아무도 몰랐습니다 |
| **5** | **가끔 부탁드리는 정리** — 윈도우 PowerShell에서 `Remove-Item .git\*.lock*, .git\__* -Force; git gc --prune=now` 를 한 번 돌려 주세요. 제가 있는 환경에서는 파일 삭제가 막혀 있어 제가 못 지웁니다. | **1분** | 커밋 흔적 파일이 계속 쌓입니다. 커밋 자체는 우회로로 되므로 급하지 않습니다 |

---

*장후 점검은 예약 점검(자동 실행)으로 작성됐다. **라이브 코드·설정·커밋 변경 0건**을 실측
확인했다(git HEAD `135c00f` 무변동 · 다이제스트 §3 불변식 26행 전부 일치 · `*.py` 변경 0).
산출은 증거 다이제스트(`evidence_MW0602-20260824_post.md`)와 이 절, 그리고 `dev_memory` 기록뿐이다.*

*✅ **자체 검증** — 이 파일 하나만 읽으면 2026-08-24 08:40 기동부터 15:47 EOD 재학습 완료까지가
전부 재구성된다: 기동·매크로·설정 불변식(장전) → 매분 9단계·진입 13건·차단·사이징(장중) →
전일 손익·절대원칙 6종·EOD 재학습·사후검증·다음 할 일(장후). 이상점 번호 `1-1`~`1-15`는
하루 안에서 유일하며, 지속 항목에 새 번호를 붙이지 않았다.*
