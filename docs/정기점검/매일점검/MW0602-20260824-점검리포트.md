# 미륵이 일일 점검 — MW0602 / 2026-08-24 (월요일 · 거래일)

> 📄 **하루 한 파일이다.** 이 파일은 장전(pre) 세션이 만들었다.
> 장중은 `0i-C` 이월 처리표부터, 장후는 `0p-C` 이월 처리표부터 **같은 파일 끝에 이어 붙인다.**
> 앞 국면 절의 본문은 고치거나 지우지 않는다 — 정정은 뒤 국면 절에서 하고 앞 절에는 포인터 한 줄만 단다.

---

# 장전(pre)

- 점검 시각: **09:00 KST** (증거 수집기 발화 2026-08-24 09:00:36) · PC `MW0602` · 브랜치 `dev` ✅
- HEAD `d187218` · 미커밋 **5건**(전부 untracked) · 당일 커밋 **2건**(`cfd16bb` 489차 07:05 · `d187218` 490차 07:44)
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260824_pre.md` (62.0KB)
- 수집기 실행: **성공** (stderr 없음). 다이제스트 첫 줄 `MW0602 (override · host=claude)` 정상
- 세션 차수: **491차** (원격 git 기준 최신이 490차)

---

## 0. 오늘 한눈에

> 이 절에는 약어·로그 태그·파일 경로·원칙 번호·단계 번호를 쓰지 않는다.

**오늘 거래를 시작해도 되는 상태입니다. 당장 위험한 문제는 없습니다.**

프로그램은 아침 8시 40분에 정상적으로 켜졌고, 지난 금요일에 남겨둔 미결제 물량은 없습니다.
주가 예측에 쓰는 인공지능 모델은 금요일 밤에 다시 학습한 것이 제대로 올라왔고,
아침 시장 분위기 판단(해외 지수·환율·변동성)도 8시 58분에 마쳐서 "위험 선호"로 결론이 났습니다.
설정값 26가지를 하나씩 대조했는데 누가 몰래 바꾼 흔적은 없었습니다.

**다만 신경 쓸 것이 네 가지 있습니다.**

첫째, 프로그램이 판단에 쓰는 핵심 재료 세 가지 중 하나("매수·매도 주문의 힘 불균형")가
**개장 직전까지 계속 무력화된 상태**였습니다. 지난 금요일에는 8시 48분에 풀렸는데 오늘은
9시가 넘도록 안 풀렸고, 개장 2분 뒤에야 정상으로 돌아왔습니다. 그 사이에 나온 판단은
재료 하나가 빠진 채로 내린 판단입니다.

둘째, 예측 확률을 실제 확률에 맞춰 보정하는 장치의 저장 파일이 **8월 11일 이후 갱신되지
않고 있습니다.** 이미 지난 금요일에 발견해 고쳐 놓았고, 그 수정이 실제로 작동하는지는
**오늘 장 마감 뒤에** 확인됩니다.

셋째, 점검 도구 자체가 **이미 없어진 마감일("8월 29일")을 아직 세 군데에서 인쇄하고
있습니다.** 지난 주말 회의에서 그 마감일을 없애고 다른 조건으로 바꾸기로 결정했는데,
도구는 옛날 날짜를 계속 보여줍니다. 이대로 두면 다음 점검이 없는 마감일에 쫓겨
잘못된 일을 하게 됩니다.

넷째, 이 장전 점검이 **개장보다 36초 늦게** 돌았습니다. 개장 전 자료와 개장 후 자료가
조금 섞였습니다. 큰 문제는 아니지만, 개장 전에 도는지를 3일 연속으로 확인하려던 계획은
오늘 다시 처음부터 세게 됐습니다.

**지금 사람이 해야 할 일은 없습니다.** 조치가 필요한 것은 맨 끝 「사용자 조치」 절에
모아 두었고, 급한 것은 하나도 없습니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| **장전** | **조건부 정상** | P0 0건. 기동·모델·매크로·설정 불변식 전부 정상. 단 CORE 피처 1종이 개장 시점까지 축퇴 미해소(1-1), 보정기 스냅샷 정체 지속(1-2), 스킬 문서의 죽은 기한 3곳(1-3) |
| 장중 | — | (장중 세션이 채운다) |
| 장후 | — | (장후 세션이 채운다) |

**절대원칙 준수 여부 (장전 시점 판정)**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | **해당 없음(장전)** — 이월 포지션 0 확인 | `08:41:02 [BrokerSync] startup sync 무포지션 확인(blank rows): FLAT -> FLAT (armistice cleared)` · `[Position] 저장 상태가 어제 데이터 — 무시` |
| ② | Circuit Breaker(CB, 자동 정지 장치) 5종 | **정상** — 미발동(장전이라 조건 자체가 없음) | 다이제스트 §12 `CB_state` = `NORMAL`×2587(7거래일). ⚠ CB②는 `CB_CONSEC_STOP_LIMIT=9999`로 **의도적 비활성**이며 미발동이 정상 |
| ③ | CORE 피처 호라이즌 그룹별 분리 | 🟠 **조건부** — 단기 그룹 CORE `ofi_norm`이 장전 전 구간 identity 강제 | **1-1 참조.** `[CORE준비도] trigger=A_WARMUP 축퇴 1/6 — ofi_norm×6hz` ×4회(08:47:59·08:49:59·08:55:00·08:59:00) |
| ④ | COM 콜백 내 `dynamicCall`·`emit` 금지 | **정상** | `0xC0000409` / `STACK_BUFFER` / 프로세스 무응답 로그 **0건**. `[FaultHandler] 활성화 PID=7128 행감지=30s` 정상 · 09:02까지 틱 수신 연속 |
| ⑤ | `GetRepeatCnt`/`GetCommData` 파라미터 구분 | **정상(간접)** | TR 수신 오류 로그 0건. `[CybosDailyPnlHeaders]` 21필드 정상 파싱 |
| ⑥ | 알파 리서치 봇 자동 통합 금지 | **정상** | 자동 통합·백테스트 자동 큐 로그 **0건**. 당일 새벽 26주 WFA 산출물(`ic_decay_catalog_20260824.json` 08:03)은 **사람이 돌린 오프라인 분석**이며 자동 통합 경로가 아니다 |

**설정 불변식** — 수집기 §3 표 **26행 전부 `일치` 또는 의도된 `값 확인`**. `불일치`·`미발견` **0건**.
차단 게이트 전수 29개 중 7개가 `False`이며 **전부 기록됨 또는 기능 토글**이다(신규 무기록 게이트 0건).

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**해당 없음.** 오늘 장전에서 P0로 올릴 항목은 없다.

### P1 — 정확성·관측 훼손

#### 1-1. 단기 CORE 피처 `ofi_norm`이 개장 직전까지 6개 호라이즌 전부 무력화된 채였다 — 전일 대비 악화

- **증상**: 장전 스케일러 재적합(refit) 4회 전부에서 CORE(핵심 판단 재료) 피처 `ofi_norm`
  (Order Flow Imbalance — 매수·매도 주문 유입의 불균형)이 **6개 호라이즌 전부**에서
  표준편차 ≈ 0으로 판정돼 `identity(0,1)` 강제 처리됐다. 즉 그 피처의 z-score가 상수처럼
  눌려 **판단에 기여하지 못하는 상태**로 개장을 맞았다.
- **근거**:
  ```
  2026-08-24 08:45:02 [WARNING] SIGNAL: [CORE준비도] trigger=A_WARMUP 축퇴 4/6 —
      above_vwap×6hz cvd_divergence×6hz ofi_norm×6hz opt_chain_pcr×6hz | 정상 진입 전 해소 여부를 확인하라
  2026-08-24 08:47:59 [WARNING] SIGNAL: [CORE준비도] trigger=A_WARMUP 축퇴 1/6 — ofi_norm×6hz | …
  2026-08-24 08:49:59 [WARNING] SIGNAL: [CORE준비도] trigger=A_WARMUP 축퇴 1/6 — ofi_norm×6hz | …
  2026-08-24 08:55:00 [WARNING] SIGNAL: [CORE준비도] trigger=A_WARMUP 축퇴 1/6 — ofi_norm×6hz | …
  2026-08-24 08:59:00 [WARNING] SIGNAL: [CORE준비도] trigger=A_WARMUP 축퇴 1/6 — ofi_norm×6hz | …
  2026-08-24 08:59:00 [WARNING] SIGNAL: [ScalerRefresh] 1m CORE 'ofi_norm' raw_std≈0(0.0298)
      → identity(0,1) 강제 (FLAT 100% 방지)     ← 1m·3m·5m·10m·15m·30m 전부 동일
  ```
  `logs/20260824_SIGNAL.log` · `[ScalerRefresh]` WARNING **54건**(08:45:02~08:59:00) ·
  `[CORE준비도]` WARNING **6건**
- **전일 대조(델타 — 이것이 오늘의 새 정보다)**:
  ```
  2026-08-21 08:45:15 [WARNING] [CORE준비도] A_WARMUP 축퇴 2/6 — cvd_divergence×6hz opt_chain_pcr×6hz
  2026-08-21 08:48:03 [INFO]    [CORE준비도] A_WARMUP 축퇴 0/6 — CORE 스케일 정상   ← 개장 12분 전 해소
  ```
  **08-21은 08:48에 완전 해소된 뒤 개장했다. 08-24는 08:59(마지막 장전 refit)까지 미해소였다.**
- **기준 위반**: CLAUDE.md **절대원칙 ③**(CORE 피처 — 호라이즌 그룹별 분리). 단기 그룹
  (1m·3m·5m)의 CORE 3종 중 「OFI 불균형 — 미통과 시 **등급 하락**」이 여기 해당한다.
  9단계 **STEP 4**(피처 생성) → **STEP 6**(앙상블 진입 판단 + 등급) 경로.
- **영향**: 오늘 장 초반의 매매 판단이, 판단 재료 세 가지 중 하나가 빠진 상태로 내려졌을
  수 있습니다. 손해가 곧바로 나는 것은 아니지만 등급이 실제보다 낮게 매겨져 들어갈 자리를
  놓치거나, 반대로 걸러졌어야 할 신호가 걸러지지 않을 수 있습니다.
- **영향(기술)**: `features/technical/ofi.py` 산출값의 장전 30봉 window `raw_std`가
  0.0270~0.0298로 임계 이하 → `strategy` 스케일러가 `identity(0,1)` 강제.
  `strategy/entry/checklist.py`의 단기 그룹 CORE 체크에서 `ofi` 항목이 상수 입력을 받으므로
  통과/미통과가 신호가 아니라 잡음으로 결정된다. `_CORE_MASK_EXEMPT_BY_HZ`(AutoMask 면제)는
  CORE를 마스킹에서 빼주지만 **스케일 축퇴는 면제 대상이 아니다**.
- **개장 후 즉시 후속(참고 — 장중 표본)**: `09:02:00 [CORE준비도] trigger=D_FORCE 축퇴 0/6 —
  CORE 스케일 정상`. **개장 2분 뒤 자동 해소됐다.** 다만 그 사이(09:00~09:02) 첫 파이프라인
  사이클이 축퇴 상태로 돌았다.
- **구조적 배경(신규 아님 — 재확인)**: CLAUDE.md 371차 각주가 *"`cvd_divergence`·`ofi_norm`
  학습표본의 91%가 정확히 0.0인 점질량은 분위수 비닝으로도 완전히 해소되지 않는 잔여 이슈"*로
  이미 등록해 두었다. 오늘 관측은 그 잔여 이슈가 **장전 구간에 실제로 발현한 사례**다.
  다이제스트 §12 `CORE축퇴_피처` 분포에서도 `ofi_norm×6hz`가 168표본 중 **103건(61.3%)** 으로
  최빈이다 — 상시적이다.
- **신규 여부**: **기존 등록 이슈의 신규 발현**. 축퇴 계측 자체는 470차 B4(`CORE준비도`) ·
  476차 G-2(`CORE축퇴_피처`)로 배선돼 있고 **섀도 관측 상태**다(차단으로 승격되지 않았다).
  오늘의 새 정보는 **「개장 시점까지 미해소」와 「전일 대비 악화」** 두 가지다.
- ⚠ **확정 결론 금지(313차 원칙)**: 관측 1거래일이다. "장전 축퇴가 당일 성과를 떨어뜨린다"는
  **아직 검정되지 않았다.** 470차 B4가 등록한 판정 절차(20거래일 축적 후 축퇴일의
  09:00~09:30 정확도를 **일자 단위**로 비교)를 따라야 하며, 그 전에 차단으로 승격하면
  317차 HurstGate가 진짜 신호의 72.3%를 오판 차단한 전례를 반복한다.

#### 1-2. 앙상블 확률 보정기 스냅샷이 9거래일째 갱신되지 않았다 — 🔄 지속(0821 1-1)

- **증상**: `data/ensemble_calibrator.pkl`의 최종 수정 시각이 **2026-08-11 15:40**에 멈춰
  있다. 기동 복원 로그는 매일 정상 출력되고 값도 정상 범위지만 **어제와 완전히 같은 값**이다.
- **근거**:
  ```
  2026-08-24 08:40:58 [INFO] SYSTEM: [Calibration] 앙상블 보정기 복원 완료
      n=1183 fitted=True degenerate=False unreachable=False span=0.02133 auc=0.574 out_max=0.3897
  ```
  파일 실측: `data/ensemble_calibrator.pkl` · 3,008바이트 · mtime **2026-08-11 15:40**
  다이제스트 §12c: `앙상블보정기_스냅샷` 관측 13거래일 중 **연속 동일 9일** · 사전등록 임계 **8** →
  🟠 **정체** 판정. 마지막 관측값 `n=1183 · fitted=True · out_max=0.3897` — 오늘 로그와 일치.
- **기준 위반**: 9단계 **STEP 6**(앙상블 진입 판단 + 등급)의 확률 보정 입력.
  CLAUDE.md 절대원칙 직접 위반은 아니다. 계측 4원칙 ②(미측정 ≠ 0) 계열의 「조용한 죽음」이다.
- **영향**: 예측 확률을 실제 맞은 비율에 맞춰 다듬는 장치가 **2주 전 자료로 멈춰 있습니다.**
  그동안 시장이 바뀌었다면 확률이 실제보다 높거나 낮게 나옵니다. 이미 지난 금요일에 고쳐
  놓았고, 그 수정이 작동하는지는 **오늘 장 마감 뒤**에 알 수 있습니다.
- **영향(기술)**: `PredictionCalibrator`/앙상블 Platt 보정기의 EOD 저장 경로가 끊긴 상태.
  0821 F-1이 저장 성공 로그(`[Calibration] 앙상블 보정기 저장 완료 n=`)와 실패 시
  「저장 건너뜀」 WARNING을 **양쪽 다** 배선했다 — 종전에는 부정 분기가 무로그였던 것이
  1-1(0821)의 핵심이었다.
- **신규 여부**: **지속.** 0821 리포트 1-1에서 최초 발견(당시 7거래일 정체), F-1 배포 완료.
  라이브 검증은 **P-1(486차)·P-2(486차)로 오늘 장후 판정 예정**이며, 장전 시점에 미갱신인
  것은 **예상된 상태다**(F-1은 EOD 경로에서 작동한다). 새 번호를 붙이지 않고 지속 처리한다.
- ⚠ **§12c가 오늘 처음 「정체」로 판정한 것**은 연속일이 임계 8을 넘긴 결과다(9일).
  **임계는 484차 G-1이 사전등록한 값이며 결과를 보고 조정하지 않는다**(313차 ④).

#### 1-3. 점검 스킬 문서 3곳이 **효력 종료된** CB② 재검토 기한 「2026-08-29」를 아직 인쇄한다

- **증상**: 2026-08-23 주간회의(489차 D2)가 CB②(Circuit Breaker 2단계 — 5분 내 손절 3연속
  시 당일 정지) 복원 기한을 **날짜에서 사건으로** 바꿨다. 종전 "재검토 2026-08-29"는
  **효력 종료**이고, 복원 시점은 **실전 전환 기준 ⑧(실전 자본 재설정) 해제와 동일 커밋**이다.
  `references/invariants.md`는 갱신됐으나 **다른 세 곳은 옛 날짜를 그대로 출력한다.**
- **근거** (2026-08-24 실측, grep 전수):
  | 파일 | 행 | 현재 문구 |
  |---|---|---|
  | `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py` | **168** | `"why": "…실투 전환 전 2~3 복원 필수. 재검토 기한 2026-08-29"` |
  | `.claude/skills/mireuk-daily-check/references/phases.md` | **45** | `- [ ] 한시 예외의 **재검토 기한이 지났는가** (CB② 2026-08-29 등)` |
  | `.claude/skills/mireuk-daily-check/references/report_template.md` | **255** | `\| CB② 복원 (…) \| 2026-08-29 \| \| \|` |

  갱신된 쪽(정본): `references/invariants.md` **153행·379행** —
  *"🔴 [489차 D2] 기한이 날짜 → 사건으로 바뀌었다. 종전 '재검토 2026-08-29'는 **효력 종료** —
  이 날짜를 기한으로 재인용하지 말 것."*

  그리고 **오늘 다이제스트가 실제로 그 죽은 날짜를 출력했다**:
  ```
  | `CB_CONSEC_STOP_LIMIT` | `9999` | `9999` | 일치 | 모의투자 한정 예외(CB② 사실상 비활성).
    실투 전환 전 2~3 복원 필수. 재검토 기한 2026-08-29 |
  ```
- **기준 위반**: CLAUDE.md 절대원칙 §2 CB② 항목(489차 D2 갱신본) · SKILL.md **함정 ①**
  (판정 ≠ 결정 — *"어떤 권고든 '이미 코드에 있는가'를 먼저 확인하라"* 의 거울상: 여기서는
  **이미 폐기된 기한이 도구에 남아 있다**).
- **영향**: 점검 도구가 **이미 없어진 마감일을 계속 보여줍니다.** 다음 점검이 그 날짜를
  믿으면 "8월 29일까지 자동 정지 장치를 되돌려야 한다"고 잘못 보고하게 되고, 실제로는
  그 장치를 되돌리는 시점이 **실전 자본을 정하는 때로 바뀌었기 때문에** 순서가 뒤집힙니다.
  자동 정지 장치를 잘못된 시점에 되돌리면 모의투자 표본이 다시 줄어듭니다.
- **영향(기술)**: `collect_evidence.py:168`의 `why` 문자열은 매일 §3 설정 불변식 표에
  렌더링된다. `phases.md:45`는 장전 체크리스트 A-3의 판정 문항 자체이므로 **점검자가
  이 문항을 그대로 수행하면 죽은 기한을 살아 있는 것으로 판정한다.**
  `report_template.md:255`는 §7「기한 도래 항목」 표의 예시 행이라 그대로 복사될 소지가 크다.
  실제로 **2026-08-23 리포트 §6·§8이 "기한 2026-08-29, 남은 거래일 5"를 사용자 조치
  1순위로 올렸다** — 그 시점(08-23 21:56)에는 489차 커밋(08-24 07:05) 전이라 **당시엔
  정당했다.** 오늘부터는 아니다.
- **신규 여부**: **신규**(489차 결정이 08-24 07:05에 커밋되면서 오늘 처음 성립한 괴리).

### P2 — 운영 부담·기술부채

#### 1-4. 장전 점검이 개장 **36초 뒤**에 실행됐다 — O-10 카운터 리셋(1/3 → 0/3)

- **증상**: 증거 수집기 발화 시각이 **09:00:36**으로, 개장(09:00:00) **36초 후**다.
  장전 표본과 장중 표본이 한 파일에 섞였다.
- **근거**: 다이제스트 §「장전 발화 마진」 —
  `마진 = 09:00:00 − 생성시각 = −36초 (개장 0분 36초 후)`
  | 날짜 | 발화 마진 |
  |---|---|
  | 2026-08-21 | **+12초** (개장 전) |
  | 2026-08-20 | −454초 (개장 7분 34초 후) |
  | 2026-08-19 | −424초 (개장 7분 4초 후) |
  | **2026-08-24** | **−36초 (개장 후)** |
- **기준 위반**: 관측 위생. 절대원칙 위반 아님. **O-10(0821 등재)** — *"다이제스트 발화 마진이
  **3거래일 연속 09:00 이전**"* 이 판정 조건이었고 08-21에 1/3이었다. 오늘 개장 후 발화로
  **카운터가 0/3으로 리셋**된다.
- **영향**: 개장 전에 찍혔어야 할 자료와 개장 직후 자료가 조금 섞였습니다. 오늘 판단이
  틀어질 정도는 아니지만, "장전 점검이 항상 개장 전에 돈다"를 확인하려던 3일짜리 시험은
  다시 처음부터 세게 됐습니다.
- **영향(기술)**: 다이제스트 §5 거래일 요약이 09:00:36까지의 거래를 포함한다(오늘은
  진입·청산 **전부 0건**이라 실질 혼입은 없다). §12 `CORE준비도` 등 매분 샘플 지표는
  개장 후 분(09:00)이 1분 섞였다.
- **처방 금지선**: 🔴 **cron을 08:58:30 이전으로 앞당기지 말 것.** `phases.md` A-2
  (08:55 매크로 → 레짐 확정) 증거를 잃는다 — 오늘 레짐 확정 실측 시각은 **08:58:06**이었다.
  개선은 발화 시각이 아니라 **워크스페이스 부팅·스킬 로드 지연**을 줄이는 쪽이어야 한다.
- **신규 여부**: **지속 관측(O-10, 0821 등재)** — 이상점 번호는 오늘 처음 부여한다.

#### 1-5. `GuardFair_유효`가 7거래일 42표본 100% `무효`로 고착 — 🔄 지속(0821 O-9)

- **증상**: 457차 `fair_valid`(모델 교체 시 신·구 모델의 공정 비교 유효성) 지표가
  **42건 전부 `무효`** 다. 모델 교체의 우열을 한 건도 판정하지 못하고 있다.
- **근거**: 다이제스트 §12 —
  `| GuardFair_유효 | 로그 | 🔴 고착 | 7 | 42 | — | 무효×42 |`
- **기준 위반**: 관측 계층. 절대원칙 위반 아님. 470차 C3'가 26주 WFA 재검증 항목으로
  「`GuardFair 무효율` 추세」를 등록해 둔 바로 그 축이다.
- **영향**: 새 모델과 옛 모델 중 어느 쪽이 나은지를 자동으로 가려주는 장치가 2주 가까이
  한 번도 답을 내지 못하고 있습니다. 매매에 직접 영향은 없지만, 모델을 바꿀 때마다
  더 나빠졌는지를 알 수 없습니다.
- **영향(기술)**: 457차 설계상 `fair_valid=무효`는 `판정 무영향(섀도)`로 표기되고 캠페인
  채널 [23]은 PASS를 낸다 — **죽은 것은 아니다.** 다만 100% 고착은 §12가 잡아내는
  292·303·371·468차 계열의 형태와 같다.
- **신규 여부**: **지속**(0821 O-9로 등재, 매 장후 누적 관측 중). 오늘 장전에 새로 판정할
  근거는 없다 — 표본이 EOD에만 늘어난다.

#### 1-6. 리포 루트에 산출물 2개가 떨어져 있고 미커밋 5건이 남아 있다

- **증상**: `git --no-optional-locks status --porcelain` 기준 untracked **5건**.
  그중 **2건이 리포 루트**에 있다.
  ```
  ?? docs/미륵이고도화3/26주WFA_20260824/
  ?? docs/미륵이고도화3/26주WFA_피처셋재검증_2026-08-24.md
  ?? gate_blocking_metrics.json      ← 리포 루트
  ?? gate_blocking_report.md         ← 리포 루트
  ?? scripts/loo_feature_ablation_purged_cv.py
  ```
- **기준 위반**: 위생 규약(`feedback_git_commit_scope` — 런타임 산출물 커밋 금지 / 산출물은
  지정 폴더에). 절대원칙 위반 아님.
- **영향**: 프로젝트 폴더 맨 위에 임시 결과 파일 두 개가 굴러다닙니다. 지금 당장 문제는
  없지만, 다음에 누가 정리하다가 실수로 지우거나 반대로 실수로 커밋할 수 있습니다.
- **영향(기술)**: `gate_blocking_metrics.json`·`gate_blocking_report.md`는 산출 경로가
  지정되지 않은 스크립트의 기본 출력으로 보인다. `docs/미륵이고도화3/26주WFA_*`와
  `scripts/loo_feature_ablation_purged_cv.py`는 **오늘 새벽 26주 WFA 피처셋 재검증
  (CLAUDE.md 455차 등록 항목)을 실제로 돌린 산출물**이며(`data/ic_decay/ic_decay_catalog_20260824.json`
  08:03:45, window 2026-07-01~08-21, n_days=36, n_real_features=75), 커밋 가치가 있다.
- **신규 여부**: 신규. ⚠ **이 마운트에서는 커밋을 시도하지 않는다** — `invariants.md` §0-B
  (`unlink` 금지 환경). 실제로 오늘 `git status` 한 번에
  `warning: unable to unlink '.git/index.lock': Operation not permitted`가 재현됐다.

### 확인 필요 (확정 아님)

- **호라이즌 확률 보정기 24개 전부 「축퇴 감지」 + FloorShadow 24/24 「판정 불일치」.**
  ```
  08:40:45 [WARNING] LEARNING: [Calibration] 축퇴 감지 — span=0.00007 auc=0.488 out_max=0.3125
      (기준 auc<0.53 and span<0.020, 기저율=0.3125 n=80) → 보정 미적용, raw 통과
  08:40:57 [INFO]    LEARNING: [Calibration][FloorShadow] 판정 불일치 — 정적하한 기준=False /
      실효하한(0.3300) 기준=True  out_max=0.3364. 실효 기준이 True면 현재 보정 적용 상태에서
      자동진입이 산술적으로 불가능하다는 뜻이다.
  ```
  오늘 **24/24(100%)**, 전일 08-21 **28/28(100%)**. `[Calibration]` WARNING 총 159건.
  **판정 보류 이유**: ① FloorShadow는 「보정이 적용됐다면」의 **반사실 섀도**이고, 축퇴가
  감지되면 실제 경로는 `보정 미적용, raw 통과`라 자동진입이 산술적으로 막히는 것은 아니다.
  ② 이 축퇴 임계 `DEGENERATE_AUC_MIN = 0.53`은 **490차 P1이 재개 게이트 앵커로 방금
  재사용한 상수**이며(`dev_memory/DECISION_LOG.md` 490차), 그 채널의 판정 창이
  **2026-08-25부터** 열린다. **무엇을 더 보면 닫히는가**: 08-25 EOD 캠페인 리포트의
  `[2-A]` AUC 표와 `[2-B]` 게이트(O-30). 그때까지 확정 결론 금지.
- **선물 투자자 수급 TR이 브로커에서 제공되지 않는다** — 상시 조건인지 재확인 필요.
  ```
  08:58:06 [INFO] SYSTEM: [CybosInvestorRaw] futures investor TR 후보 없음
      open_interest=152833 (FutureMst fallback)
  08:58:06 [INFO] DATA: [CybosInvestor] futures supported=False source=FutureMst_oi
      foreign=+0 individual=+0 institution=+0 … reason=Cybos 선물 투자자 TR 미발견; 미결제약정만 제공
  ```
  외국인·개인·기관 순매수가 전부 `+0`으로 들어온다. 장전 스케일러 refit의 잔존 z경고에
  `institution_futures_net`이 Phase1→2→3 내내 남은 것과 정합한다
  (`08:55:00 [PreMarket] Phase3 refit 완료 … 잔존=atr,avg_volume,institution_futures_net,microprice_bias`).
  **판정 보류 이유**: 브로커(Cybos) 제약인지 설정 문제인지 장전 로그만으로 구분되지 않는다.
  09:02에 프로그램매매는 `CpSvr8111`로 확보됐다(`arb=+299 nonarb=-162922`). **무엇을 더 보면
  닫히는가**: `[Capability] … investor=Y/N(fetch=0,runtime=N)` 의 `runtime=N` 이 설정에서
  온 것인지 코드 확인(`collection/broker/` 계열) — **장중 코드 열람은 금지가 아니므로
  장중 세션이 읽어도 된다. 변경은 장후.**
- **`CybosProbe` 경고 18건(08:58:06·08:58:36 2회 배치)** — `CoInitialize가 호출되지 않았습니다`
  및 `잘못된 클래스 문자열입니다`. 전일(08-21) 하루 총 **798건**과 비교하면 오늘 18건은
  **로그가 09:02까지밖에 없어서**이지 감소가 아니다(계측 4원칙 ② — 미측정 ≠ 0).
  **무엇을 더 보면 닫히는가**: 장후 다이제스트의 동일 태그 총건수.

### 이미 반영된 사안 (신규 아님)

> 이 절을 비워두지 않는다 — SKILL.md **함정 ①**(판정 ≠ 결정) 방지.

- ✅ **G-1(468차, `MODEL_LABEL_STATE_UNLOCK_ENABLED`) 라이브 검증은 이미 끝났다 — 오늘은
  2거래일 연속 재확인이다. 신규 안건으로 올리지 않는다.**
  ```
  2026-08-24 08:55:03 [INFO] SYSTEM: [EntryGate] 모델 레이블 체계 확인(장전)
      — 사이즈 제한 해제 (×0.6 → ×1.0) | 6/6 호라이즌 현행 레이블(fixed) 학습본
  ```
  판정 근거는 `main.py:1138` 주변 `_refresh_pre_retrain_state()` — `_pre_retrain_done`이
  **False일 때만** 이 로그가 나오므로, 이 줄의 출현 자체가 **G-1 경로가 해제를 수행했다**는
  뜻이다(F-1 마커로 이미 풀렸다면 함수가 조기 반환해 무로그다).
  `invariants.md` §2-B의 P-2 기준(`불일치 0/6` 출현 + F-1 경로 로그 미출현)을 만족한다.
  **최초 통과는 2026-08-21**이며 0821 리포트 312행에 ✅ 기록돼 있다. 이력:
  | 날짜 | 판정 |
  |---|---|
  | 08-14·08-18·08-19 | `미확인 … 불일치 6/6 — label_scheme없음(구버전)` |
  | 08-20 | `미확인 … 불일치 6/6 — label_scheme=null(기록경로 결함)` |
  | **08-21** | ✅ `확인 … 6/6 호라이즌 현행 레이블(fixed)` ← **최초 통과** |
  | **08-24** | ✅ 동일 (2거래일 연속) |
  ⚠ **그래도 실전 전환 기준 ⑧은 해제되지 않는다** — 남은 조건은 **실전 자본 재설정(사용자
  결정)** 이고, 489차 D6이 *"①이 충족되기 전에는 ⑧을 해제하지 않는다"* 는 선행조건을 추가했다.
- ✅ **CB② 미복원(`CB_CONSEC_STOP_LIMIT = 9999`)은 결함이 아니라 확정된 결정이다.**
  489차 D2가 기한을 날짜 → 사건(⑧ 해제와 동일 커밋)으로 바꿨고, 그 사이 캠페인 채널
  **[56] `cb2_restore_shadow`** 가 매주 반사실을 재계산한다(판정 창 2026-08-24~).
  ⚠ 복원값 **2와 3은 방향이 반대**이며 그 사후탐색 수치로 값을 고르지 말 것(313차 ④).
- ✅ **CB③-P4(`CB3_P4_GRADE_BLOCK_ENABLED=False`)·FP-CRITICAL
  (`FP_CRITICAL_GRADE_BLOCK_ENABLED=False`)·TOX-SEVERE-SPREAD
  (`TOXICITY_SEVERE_SPREAD_BLOCK_ENABLED=False`)는 전부 기록된 한시 예외**다.
  특히 TOX-SEVERE-SPREAD는 489차 D7이 **실전 전환 기준 ⑨에서 26주 WFA 재검증 항목으로
  이관**했다 — **실전 전환 선행조건이 아니다.**
- ✅ **`ZONE_ENTRY_BAN_ENFORCE=False`** 는 462차 P1-a 결정. 위반 코호트가 흑자(+596,858원)라
  채널 [53]이 `min_samples=20`에 도달할 때까지 False 유지. **판정 전 집행 금지.**
- ✅ **`MAX_CONTRACTS=3`이 오늘 한 번도 구속하지 않는 것**은 470차가 규명한 대로
  **브로커 증거금 상한이 2에서 먼저 자르기 때문**이다. 오늘 `[Capital]` 로그가
  `MAX_CONTRACTS=3 최대노출=164,010,000원(레버리지 3.28x)`을 찍었지만 실제 상한은
  `get_order_available_qty()`가 정한다. 새 발견이 아니다.
- ✅ **`[PreRetrain] 08:55 사전 재학습 스킵`은 정상이다.**
  ```
  08:55:03 [PreRetrain] EOD 재학습 날짜 복원: 2026-08-21 (3일 전) → PreRetrain 스킵 검토
  08:55:03 [PreRetrain] 08:55 사전 재학습 스킵 — 1영업일 전(2026-08-21) EOD 재학습 성공 (동일 데이터 중복 불필요)
  ```
  "3일 전"은 달력일, "1영업일 전"은 거래일 기준이며 **둘 다 맞다**(08-22 토·08-23 일 휴장).

---

## 2. Fix 작업 구현계획

> 🟢 **장전 국면이므로 코드 변경을 실행하지 않는다.** 라이브 프로세스(PID 7128)가 09:00부터
> 매분 루프를 돌고 있다. 아래는 **계획 수립**이며, 적용은 **15:10 강제청산 이후**를 권고한다.
> ⚠ 이 마운트는 `unlink`를 금지하므로 커밋은 `invariants.md` §0-B의 rename 우회로만 시도한다.

### F-1(491차). 스킬 문서 3곳의 죽은 CB② 기한을 489차 D2 결정으로 교체한다 — **P1** · 대응 이상점 **1-3**

- **원인 가설**: 489차가 결정을 반영할 때 `invariants.md`만 갱신했다. CB② 기한 문자열이
  **네 곳에 복제**돼 있었고 나머지 셋이 남았다. 「소비부만 옮기지 않았는가」
  (0821 등재 멀티PC 컨벤션 보강 안건)의 **같은 계열 실패**이며, 이번엔 방향이 반대다 —
  정본은 고쳤는데 사본이 안 고쳐졌다.
- **변경 파일**:
  - `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py` — **168행** `why` 문자열:
    `"…실투 전환 전 2~3 복원 필수. 재검토 기한 2026-08-29"`
    → `"…실투 전환 전 2~3 복원 필수. 🔴 [489차 D2] 기한은 날짜가 아니라 사건 — 복원 시점 = 실전 전환 기준 ⑧ 해제와 동일 커밋. 종전 '2026-08-29'는 효력 종료(재인용 금지). 그 사이 채널 [56] cb2_restore_shadow가 매주 반사실 재계산"`
  - `.claude/skills/mireuk-daily-check/references/phases.md` — **45행** A-3 체크 문항:
    `(CB② 2026-08-29 등)` → `(⚠ CB②는 날짜 기한이 아니다 — 489차 D2로 「⑧ 해제와 동일 커밋」으로 전환)`
  - `.claude/skills/mireuk-daily-check/references/report_template.md` — **255행** §7 예시 행의
    `2026-08-29` → `사건 기한(⑧ 해제와 동일 커밋) — 날짜 아님`
  - 사본 동기화: `config/dailycheck_targets.json`에는 이 문자열이 **없다**(실측 확인) —
    수집기 파이썬 상수만 고치면 된다.
- **회귀 위험**: **매우 낮다.** 문자열·주석·문서만 바뀐다. 라이브 코드 경로가 아니다
  (`.claude/skills/**`는 점검 도구이지 매매 런타임이 아니다).
  단 `collect_evidence.py`는 **표 렌더링 폭**에 영향을 주므로 문자열이 길어지면 §3 표가
  가로로 늘어난다 — 두 줄로 나누거나 축약할 것.
- **절대원칙 저촉 여부**: **없음.** 6종 중 어느 것도 건드리지 않는다. CB②의 **값
  (`9999`)은 손대지 않는다** — 이 fix는 기한 문구만 고친다.
- **런타임 주의**: 점검 스크립트는 표준 라이브러리만 쓰며 `py37_32`/`py310_64` 무관.
  COM 콜백 밖이다.
- **검증 방법**:
  ① `grep -rn "2026-08-29" .claude/skills/mireuk-daily-check/` → `invariants.md`의
     **「효력 종료」 문맥 2행만** 남는지 확인(그 두 행은 역사 기록이므로 지우지 않는다).
  ② 수집기 재실행 후 §3 표의 `CB_CONSEC_STOP_LIMIT` 행에 새 문구가 렌더링되는지 확인.
  ③ 다음 거래일(08-25) 장전 다이제스트에서 옛 날짜 미출현 확인 → **O-1(491차)**.
- **섀도 계측 필요 여부**: **불요.** 게이트·차단 로직 신설이 아니다.
- **적용 시점**: **장후.** 급하지 않지만 08-25 장전 다이제스트 전에 끝내는 것이 좋다 —
  그래야 죽은 날짜가 한 번 더 인쇄되지 않는다.
- **결정 필요 사항**: **없음.** 489차 D2가 이미 결정했고 이 fix는 그 결정을 도구에 반영할 뿐이다.

### F-2(491차). 리포 루트 산출물 2건에 출력 경로를 준다 — **P2** · 대응 이상점 **1-6**

- **원인 가설**: `gate_blocking_*` 산출 스크립트가 출력 경로를 인자로 받지 않고 CWD에 쓴다.
- **변경 파일**: 산출 스크립트 특정이 선행이다.
  `grep -rn "gate_blocking_metrics" scripts/ --include=*.py` 로 생산부를 찾은 뒤
  기본 출력을 `docs/미륵이고도화3/` 또는 `data/`(gitignore 대상)로 옮긴다.
  ⚠ **`docs/정기점검/금요일점검/` 로 보내지 말 것** — 그쪽은 「런타임 산출물 커밋 금지」의
  **의도적 예외** 구역이고, 두 PC가 서로를 보는 통로다. 성격이 다른 파일을 섞지 않는다.
- **회귀 위험**: 낮음. 다만 **다른 스크립트가 루트 경로로 읽고 있으면** 깨진다 —
  `grep -rn "gate_blocking" --include=*.py .` 로 소비부를 먼저 전수 확인할 것
  (「소비부만 옮기지 않았는가」의 역방향).
- **절대원칙 저촉 여부**: 없음.
- **런타임 주의**: 오프라인 분석 스크립트. 라이브 무관.
- **검증 방법**: 재실행 후 리포 루트에 파일이 생기지 않는지 + 기존 소비부가 정상 동작하는지.
- **섀도 계측 필요 여부**: 불요.
- **적용 시점**: 장후 또는 차주. 급하지 않다.
- **결정 필요 사항**: 출력 위치 — **권고: `docs/미륵이고도화3/`**(오늘 26주 WFA 산출물과
  같은 자리). 사용자 확인 불요 수준.

### F-3(491차). 26주 WFA 피처셋 재검증 산출물을 커밋한다 — **P2** · 대응 이상점 **1-6**

- **원인 가설**: 오늘 새벽 사람이 돌린 분석의 산출물이 아직 untracked다.
  `docs/미륵이고도화3/26주WFA_20260824/` · `26주WFA_피처셋재검증_2026-08-24.md` ·
  `scripts/loo_feature_ablation_purged_cv.py`.
- **변경 파일**: 없음(신규 파일 추가만).
- **회귀 위험**: 없음. 단 `scripts/loo_feature_ablation_purged_cv.py`가 **EOD 체인에
  연결돼 있지 않은지** 확인할 것 — 연결돼 있으면 그건 코드 변경이라 별건이다.
- **절대원칙 저촉 여부**: **없음.** 절대원칙 ⑥(알파 리서치 봇 자동 통합 금지)과 무관하다 —
  이것은 **사람이 명시적으로 돌린 오프라인 분석**이고, 자동 큐·자동 통합이 아니다.
- **런타임 주의**: `loo_feature_ablation_purged_cv.py`는 재학습 계열 분석이므로
  **`py310_64` 전용**일 가능성이 높다(191차 OOM 결정). 스크립트 상단에 명기할 것.
- **검증 방법**: `git --no-optional-locks status --porcelain` 이 비는지.
- **적용 시점**: 장후.
- **결정 필요 사항**: **있다** — 이 산출물은 CLAUDE.md 「주기적 재검증 항목」의
  **「호라이즌 방향예측 피처셋 재검증」(455차 등록)** 실행분으로 보인다.
  그 결과를 **26주 WFA 판정으로 채택할지**는 주간회의 소관이다. 이번 fix는 **파일 보존만**
  하고 판정하지 않는다. ⚠ 사후 데이터로 기준을 움직이지 않는다(313차 ④).

### 적용 순서와 커밋 계획

1. **커밋 ①** — F-1(스킬 문서 3곳 CB② 기한 교체) —
   `[MW0602] 491차: 죽은 CB② 기한 문구 3곳 교체 — 489차 D2 결정을 점검 도구에 반영`
2. **커밋 ②** — F-3(26주 WFA 산출물 보존) —
   `[MW0602] 491차: 26주 WFA 피처셋 재검증 산출물 보존 (판정 아님 — 주간회의 소관)`
3. **커밋 ③(차주 가능)** — F-2(출력 경로) —
   `[MW0602] 491차: gate_blocking 산출 경로를 리포 루트 밖으로`

> 🔴 **셋 다 장후 적용.** 장중에는 실행하지 않는다.
> 🔴 **커밋 시 `invariants.md` §0-B 우회로를 쓴다** — lock 파일을 **지우지 말고 rename**하고
> `git --no-optional-locks` 로 실행한다. 오늘도 `unlink: Operation not permitted`가 재현됐다.

---

## 3. 고도화 방안

> 당일 관측에서 출발한 것만 쓴다. 일반론 금지.

### G-1(491차). 「장전 CORE 축퇴가 개장 시점까지 남았는가」를 별도 축으로 계측한다

- **관측 근거**: 오늘 `[CORE준비도]` 축퇴가 08:45 `4/6` → 08:47 `1/6` → **08:59 `1/6`(미해소)**
  → 09:02 `0/6` 이었다. 전일 08-21은 08:48에 `0/6`으로 완전 해소된 뒤 개장했다.
  **현행 §12 `CORE준비도` 지표는 「하루 중 축퇴 개수의 분포」만 본다** — `1`×103 · `0`×59 …
  식이라 **개장 시점의 값**이라는 결정적 순간이 분포에 묻힌다.
  오늘 같은 케이스(장중에는 해소됐지만 첫 사이클은 축퇴 상태)를 현행 계측은
  「1이 좀 더 많은 날」로만 기록한다.
- **제안 내용**: 수집기 §12에 파생 축 **`CORE축퇴_개장시점`** 을 추가한다.
  값은 **09:00 직전 마지막 `A_WARMUP` 로그의 축퇴 개수**(오늘 = `1`, 08-21 = `0`).
  `sample_axis`는 분 단위가 아니라 **거래일 1샘플**이므로 관측률 판정 대상이 아니다.
  피처 이름 축(`CORE축퇴_피처`)도 같은 방식으로 개장시점 값을 남긴다.
- **기대 효과**: 「축퇴가 있었다」와 「축퇴한 채로 개장했다」를 도구가 가른다.
  20거래일 축적 후 470차 B4가 등록한 판정 절차(**축퇴일의 09:00~09:30 정확도를
  일자 단위로 비교**)에 **정확한 코호트 정의**를 제공한다 — 현행 분포로는 그 코호트를
  만들 수 없다.
- **비용·위험**: 수집기 파서에 파생 축 1개 추가(약 30줄). 라이브 코드 무변경.
  위험은 **오탐 방향으로 없다** — 관측 전용이고 차단 승격이 아니다.
  ⚠ 이 축을 근거로 **차단을 신설하지 말 것**. 470차 B4가 명시한 20거래일 절차가 선행이다.
- **선행 조건**: 없음. 1-1과 독립적으로 배선 가능하다.
- **우선순위 제안**: **이번 주.** 오늘이 코호트 1일차이므로 지금 배선해야 표본이 처음부터 쌓인다.

### G-2(491차). 스킬 문서 안의 「날짜 기한」을 기계가 훑게 한다

- **관측 근거**: 1-3에서 **같은 기한 문자열이 네 곳에 복제**돼 있었고 정본 하나만 갱신됐다.
  사람이 grep 해서 찾았다. 이 프로젝트가 이미 아는 실패 형태다 — CLAUDE.md가
  CB②·CB③-P4·FP-CRITICAL을 두고 반복해 경고한 *"재검토하기로 했는데 안 함"* 의
  **도구판 변종**이다(이번엔 결정이 있었는데 도구가 몰랐다).
- **제안 내용**: 수집기에 **§14 「문서 내 미래 날짜 스캔」** 을 신설한다.
  `.claude/skills/mireuk-daily-check/**` 와 `CLAUDE.md` 에서 `20\d\d-\d\d-\d\d` 패턴을 훑어
  ① **오늘 기준 30일 이내에 도래하는 날짜**와 ② **이미 지난 날짜**를 표로 낸다.
  각 행에 파일·행번호·주변 20자를 붙인다. **판정하지 않고 나열만 한다** — 판정은 사람 몫이다.
- **기대 효과**: 죽은 기한과 임박한 기한이 매일 눈에 띈다. 오늘 같은 괴리는 **다음 날 자동
  검출**된다.
- **비용·위험**: 정규식 스캔 1회(파일 수십 개, 밀리초 단위). **오탐이 많다** — 역사 기록의
  날짜(예: "2026-06-25 CORE 강등")가 전부 걸린다. 그래서 **적신호로 올리지 않고 §14
  참고 표로만** 둔다. §12의 `benign` 화이트리스트와 같은 취급이 필요해지면 그때 붙인다.
- **선행 조건**: **F-1이 먼저**다. 지금 배선하면 첫 실행에서 자기가 고칠 대상 3건을
  적신호처럼 뱉는다 — 고친 뒤에 켜야 목록이 신호가 된다.
- **우선순위 제안**: **이번 주(F-1 다음).**

### G-3(491차). 장전 발화 지연의 원인을 발화 시각이 아니라 **부팅 지연**에서 찾는다

- **관측 근거**: 오늘 발화 마진 **−36초**. 예약 작업 자체는 08:59대에 트리거됐을 것이나
  다이제스트 생성이 09:00:36이었다. 08-19·08-20은 −424초·−454초로 훨씬 나빴고 08-21만
  +12초였다 — **분산이 크다는 것이 곧 원인이 스케줄이 아니라 지연에 있다는 신호다.**
- **제안 내용**: 수집기 다이제스트 「장전 발화 마진」 블록에 **`부팅~수집 소요 시간`** 한 줄을
  더한다. 수집기 프로세스 시작 시각과 파일 저장 시각의 차이를 찍으면, 마진 악화가
  ① 스케줄 트리거 지연인지 ② 수집기 자체가 느린 것인지가 갈린다.
- **기대 효과**: O-10(3거래일 연속 09:00 이전) 판정이 실패할 때 **어디를 고쳐야 하는지**가
  같이 나온다. 지금은 실패 사실만 남는다.
- **비용·위험**: 두 줄. 위험 없음.
- **선행 조건**: 없음.
- **우선순위 제안**: **이번 주.** 🔴 **cron 시각을 앞당기는 처방과 함께 제안하지 말 것** —
  08:58:30 이전으로 당기면 `phases.md` A-2(08:55 매크로 → 레짐 확정) 증거를 잃는다.
  오늘 레짐 확정 실측은 **08:58:06**이었다.

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| **실전 전환 기준 ⑧** (사이징 재설계) | 조건 2(`MAX_CONTRACTS` 재검토) 431차 충족 · 선행조건 G-1(468차) **2026-08-21 통과** | **변경 없음 — 해제하지 않는다** | 남은 조건은 **실전 자본 재설정(사용자 결정)** 하나이고, 489차 D6이 *"①이 충족되기 전에는 ⑧을 해제하지 않는다"* 를 추가했다. ①은 0821 기준 **미충족**(최근 20거래일 −178,004원 · 양수일 8/20). 오늘 장전에 이를 뒤집을 증거는 없다 |
| **주기적 재검증 항목**(26주 WFA) | 「고착 지표 감시목록」(468차 G-2) · 「관측 계층 5종」(470차 C1'~C5') · 「분기편향 옵트인 목록」(475차 후속) | **G-1(491차) `CORE축퇴_개장시점` 축을 같은 목록에 신설 등록** | 468차 G-2가 등록한 사유 그대로다 — *"감시 대상 목록 자체가 낡는 것은 일일 점검이 못 잡는다."* 새 축은 배선 즉시 목록에 넣지 않으면 다음 26주 때 존재를 모른다 |
| **주기적 재검증 항목**(26주 WFA) | 「호라이즌 방향예측 피처셋 재검증」(455차 등록) | **오늘 새벽 실행분(`ic_decay_catalog_20260824.json`, window 2026-07-01~08-21, n_days=36, n_real_features=75, bonferroni_t=6.360, noise_floor_max_abs_t=2.462)이 그 항목의 실행 산출물인지 확인하고 등재** | 산출물이 untracked로 떠 있어(1-6) 다음 26주 때 "돌렸는지 여부"조차 알 수 없게 된다. ⚠ **등재는 보존이지 판정이 아니다** — L1→L2→L3 순차 재실행 완료 여부와 판정은 주간회의 소관 |
| **실전 전환 기준 ⑨**(TOX-SEVERE-SPREAD) | 489차 D7로 **26주 WFA 재검증 항목으로 이관 완료** | **변경 없음** | 이미 처분된 안건이다. 오늘 §12b가 `4일 연속 미도달(max 9.003 / 임계 20)`을 다시 찍었지만 그것은 **매주 재계산되는 판정**이지 미조치의 증거가 아니다(함정 ①) |

---

## 4. 진입 승패 사후검증 (장후 한정)

**해당 없음(장후 전용).** 장전 시점 진입·청산 **0건**(다이제스트 §5 거래일 요약: 진입체크
통과 0 · 진입 등록 0 · 체결 0 · 포지션 0 · 청산 레그 0 · 차단 0 · 사이저 호출 0).

> ⚠ 이 0은 **개장 36초 시점의 0**이다(1-4). "오늘 하루 0건"이 아니다.

---

## 5. 수익률 향상방안 (장후 한정)

**해당 없음(장후 전용).**

---

## 6. 다음 거래일 관측 예정 + 오늘 장중·장후에 닫을 항목

### 6-A. 앞 국면(0821 장후 · 0823) 이월 항목 — 장전 시점 처분

| 원 ID | 항목 | 장전 처분 | 근거 |
|---|---|---|---|
| **O-10**(0821) | 다이제스트 발화 마진 3거래일 연속 09:00 이전 | 🔴 **판정 — 실패(리셋 0/3)** | 오늘 **−36초**(개장 후). 1-4로 이상점화. 카운터 재시작 |
| **O-4**(0821) | `[Capital]` 괴리율 추이 | ⏸ **관측 계속** | 08-14 −40.6% → 08-18 −40.7% → 08-19 −40.8% → 08-20 −42.3% → 08-21 −42.1% → **08-24 −41.5%**(실제잔고 29,274,631원). **추세 반전 아님 — 횡보.** 판정 기준 미도달 |
| **O-3**(0821) | 런처 GUARD 연속일 | ⏸ **관측 계속** | 08-14·18·19·20·21·**24 전부** `기존 프로세스 종료 완료` **1회**. **6거래일 연속 동일** — 매 기동 시 이전 인스턴스가 살아 있다는 뜻. ⚠ 오늘 판정하지 않는다(무엇이 정상인지 기준이 등록돼 있지 않다) |
| **P-1**(486차) | `[Calibration] 앙상블 보정기 저장 완료 n=` 또는 「저장 건너뜀」 WARNING | ➡ **장후로 이월** | 15:40 EOD 경로. 장전에 판정 불가 |
| **P-2**(486차) | `data/ensemble_calibrator.pkl` mtime → 2026-08-24 갱신 | ➡ **장후로 이월** (장전 현황: **미갱신, 2026-08-11 15:40**) | 1-2로 지속 기록. F-1(0821)은 EOD에 작동하므로 **장전 미갱신은 예상된 상태** |
| **P-3**(486차) | `[DriftRetrain] state=…` 매분 1줄(미발동 분에도) | ➡ **장중으로 이월** | 09:00 이후 표본 필요 |
| **P-4**(486차) | 다이제스트 §5 `[MarginCap]` 건수 == §12 `MarginCap_state` 표본 | ➡ **장후로 이월** (장전 현황: §5 `조회 — · 축소 0 (구형식만)` · §12 `표본부족` 1일 27건) | 장전에는 증거금 조회 사이클이 거의 없다 |
| **O-1**(486차) | 예약 점검 `lastRunAt` vs 수집기 대상일 | ✅ **해소(오늘 조건)** | 오늘 대상일 `2026-08-24` = 실제 거래일. 486차 F-1(휴장일 판정) 배선 후 첫 거래일이며 「🗓 거래일 문맥」 블록이 정상 출력됐다(`거래일 여부: 거래일 ✅`) |
| **O-17**(0821) | `entry_horizon` 소비처 전수 | ➡ **장후로 이월** | 코드 조사 항목. 장중 코드 변경 금지 원칙과 무관하나(읽기만) 장후에 하는 것이 맞다 |
| **O-16 · O-5 · O-7 · O-8 · O-9 · O-18~O-23 · P-8 · P-9** | 5m 정확도 / 비데몬 스레드 / meta_action 승패 / 손절 준수율 / GuardFair 무효율 / Tier1 준수율 / 진입 방향 편향 / WeightCollapse 비율 / `sizing_trace.margin_cap_qty` / 롤링20일 net / `bar_pass≥1` | ➡ **전부 장후로 이월** | 전부 EOD 표본 기반. 기준 **무변경**(0821 §6p 정본). ⚠ O-9(GuardFair 무효율)는 장전 현황을 1-5에 기록했다 |
| **O-20 · O-21**(0821) | 월간 로그 정리 `OK` / 캠페인 metrics 필드 소멸 | ➡ **2026-08-28(금) EOD로 이월** | 금요일 전용 체인. 평일에 비는 것은 결함이 아니다 |
| **O-26~O-29**(489차) | `[56]` 행 출현 / `[44]`·`[46]` 📌 마커 / `[56]` 표본 진행 / `[46]` 신규 통과분 | ➡ **오늘 장후(EOD)로 이월** | 판정 창이 **2026-08-24부터** 열린다 — 오늘이 1일차다 |
| **O-30~O-32**(490차) | `[2-A]` AUC 표·`[2-B]` 게이트 출현 / 사이드카 `history` 누적 / `meta_entry_quality_prob` NULL 급증 없음 | ➡ **2026-08-25로 이월**(원 등록대로) | 490차가 08-25 판정 예정으로 등록했다. 오늘 앞당기지 않는다 |

### 6-B. 오늘 새로 등록하는 관측

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| **O-1(491차)** 🆕 | 다이제스트 §3 `CB_CONSEC_STOP_LIMIT` 행의 문구 | 「2026-08-29」 **미출현** + 「⑧ 해제와 동일 커밋」 출현. F-1(491차) 적용 후 | **2026-08-25 장전** |
| **O-2(491차)** 🆕 | `[CORE준비도]` 개장 직전 마지막 `A_WARMUP` 축퇴 개수 | 오늘 = **1**(`ofi_norm×6hz`), 08-21 = **0**. **20거래일 축적 후** 470차 B4 절차(축퇴일 09:00~09:30 정확도 일자단위 비교)로 판정. ⚠ **그 전에 확정 결론·차단 승격 금지**(313차) | **누적** — 매 장전 |
| **O-3(491차)** 🆕 | `[CORE준비도]` 개장 후 최초 `D_FORCE`/`B_OPEN` 축퇴 개수 | 오늘 09:02 = **0**(해소). 「장전 축퇴가 개장 후 스스로 풀리는가」의 표본. 풀리지 않는 날이 나오면 P1 승격 | **매 장중** |
| **O-4(491차)** 🆕 | `[Calibration] 축퇴 감지` 건수 및 FloorShadow `판정 불일치` 비율 | 오늘 24/24(100%) · 08-21 28/28(100%). **490차 [2-B] 재개 게이트 판정창(08-25~)과 함께** 읽는다. ⚠ 단독으로 확정 결론 금지 | **2026-08-25 EOD** |
| **O-5(491차)** 🆕 | `[CybosProbe]` 경고 총건수 (장후 다이제스트 기준) | 08-19·20·21 각 **798건**과 같은 수준이면 **상시 조건 확정**(1-1 「확인 필요」 항목 종결). 크게 다르면 별건 | **오늘 장후** |

---

## 7. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| 🔴 ~~CB② 복원 (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) — 2026-08-29~~ | ❌ **효력 종료** | — | **489차 D2로 기한이 날짜 → 사건으로 전환됐다.** 복원 시점 = **실전 전환 기준 ⑧ 해제와 동일 커밋**. ⚠ **「8월 29일」을 기한으로 재인용하지 말 것.** 그 사이 캠페인 채널 **[56] `cb2_restore_shadow`** 가 매주 반사실을 재계산한다(판정 창 2026-08-24~). **도구 3곳이 아직 옛 날짜를 인쇄한다 → 1-3 · F-1(491차)** |
| **[46] Hurst 임계 승격** — `hurst_threshold_shadow.promotion_watch` | 판정 창 **2026-08-24~** | **오늘 1일차** | 사전등록 완료. `abs_0.39`의 **신규 통과분**이 왕복비용을 넘는가. ⚠ **문턱을 관측값에서 역산하지 않았다 — 사후 완화 금지** |
| **[55] 09:20~09:29 (E)안** — `early_window_gate_shadow.vol_condition_prereg` | 판정 창 **2026-08-24~** | **오늘 1일차** | 등급 조건을 ATR 조건(앵커 `ATR_MAX_ENTRY`)으로 바꾸면 나은가 |
| **[56] CB② 복원** — `cb2_restore_shadow` | 판정 창 **2026-08-24~** | **오늘 1일차** | 복원값 2·3 각각에서 CB②가 **손실을 막나 수익을 막나**. ⚠ **집계 단위는 포지션**(470차 C1) — 로그 `[CB] 연속 손절 N회`로 과거를 세지 말 것 |
| **[2-B] 스코어러 재개 게이트** — `meta_gate.reopen_gate` | 판정 창 **2026-08-25~** | 1거래일 | 490차 P1 사전등록. `auc_net_directional` ≥ 0.53(런타임 앵커) **4주 연속**. 현재 ⏳ INSUFFICIENT |
| **주간회의 2026-08-28 이월 안건** | 주간회의 | 4거래일 | ① **1-9 앙상블 등급 임계 재설계**(§12b **9일 연속 미도달**, 관측 max 0.5862 < 0.60) ② **F-8 채널 생산부 결손** — 487차 후속으로 (B) 확정·(A) 폐기됨, 잔여 확인만 ③ **멀티PC 컨벤션 보강**(「소비부만 옮기지 않았는가」) ④ **[P2] `auc_strong_directional` 청산 축 승격 여부**(490차 등재) |

> ⚠ **1-9(앙상블 등급 임계)는 2026-08-22 주간회의 안건이었으나 0823 회의에서 처분되지
> 않고 이월됐다.** 오늘 §12b가 `📌 미도달(9일 연속 — 전 관측일)`로 다시 찍은 것은
> **판정의 재계산이지 미조치의 증거가 아니다**(함정 ①). 신규 계측을 제안하지 않는다.

---

## 8. dev_memory 반영

- `dev_memory/DECISION_LOG.md` 추가 항목:
  `## 2026-08-24 (MW0602 491차 — 장전 점검: 죽은 CB② 기한 3곳 + 장전 CORE 축퇴 미해소)`
- `dev_memory/NEXT_TODO.md` 추가 체크박스: **Fix 3건**(F-1·F-2·F-3 / 491차) ·
  **고도화 3건**(G-1·G-2·G-3 / 491차) · **관측 5건**(O-1~O-5 / 491차)
- 완료 처리한 기존 항목: **O-1(486차)** ✅ 해소 · **O-10(0821)** 🔴 판정(실패·리셋)
- 세션 차수: **491차** — 원격(git) 기준 최신이 `490차`(`d187218`)이므로 그 다음
- 커밋 제목 접두: `[MW0602]`
- ⚠ **기존 내용을 덮어쓰지 않는다** — 두 파일 다 2.0MB·1.1MB 누적 기록이다

> 📄 **스킬 템플릿 메모**: `report_template.md` **255행**이 §7 예시 행에
> `CB② 복원 … 2026-08-29`을 하드코딩하고 있다. 489차 D2로 그 날짜는 효력 종료됐으므로
> **템플릿을 그대로 복사하면 죽은 기한이 매일 재생산된다.** F-1(491차)이 이를 포함한다.
> 오늘 리포트는 §7에서 그 행을 **취소선 + 효력 종료 명시**로 처리했다.

---

## 사용자 조치

> 🟢 **오늘 아침에 당장 하실 일은 없습니다.** 아래는 시간 나실 때 봐 주시면 되는 것들입니다.

| # | 지금 할 일 | 걸리는 시간 | 안 하면 생기는 일 |
|---|---|---|---|
| **1** | **오늘 장 마감(오후 3시 10분) 뒤에 "F-1 구현해"라고 말씀해 주세요.** 점검 도구 세 군데가 이미 없어진 마감일(8월 29일)을 계속 보여주고 있습니다. 지난 주말 회의에서 그 마감일을 없애고 "실전 자본을 정하는 때"로 바꾸기로 결정하셨는데, 도구만 못 따라왔습니다. | 제가 **약 10분**, 확인 포함 **20분** | 내일 아침 점검부터 없는 마감일이 다시 인쇄되고, 다음 점검이 그것을 근거로 "자동 정지 장치를 8월 29일까지 되돌려야 한다"고 잘못 보고할 수 있습니다 |
| **2** | (선택) **"G-1 구현해"라고 말씀해 주세요.** 오늘 판단 재료 하나가 개장 직전까지 무력화돼 있었는데, 지금 도구로는 "개장하는 순간에 그랬는지"가 기록되지 않습니다. 오늘이 그 기록의 1일차라 지금 만들어야 처음부터 쌓입니다. | 제가 **약 20분**, 확인 포함 **30분** | 20일치 자료를 모아 "이게 성적에 영향을 주는가"를 따져야 하는데, 그 20일이 오늘부터가 아니라 만드는 날부터 시작됩니다 |
| **3** | (선택) **정리 두 가지** — 프로젝트 폴더 맨 위에 임시 결과 파일 두 개(`gate_blocking_metrics.json`·`gate_blocking_report.md`)가 굴러다니고, 오늘 새벽에 돌리신 26주 점검 결과물이 아직 저장(커밋)되지 않았습니다. "F-2·F-3 구현해"라고 하시면 정리하겠습니다. | 제가 **약 15분** | 임시 파일이 계속 쌓이고, 26주 점검을 돌린 기록이 남지 않아 다음번에 "이미 돌렸는지"조차 알 수 없게 됩니다 |
| **4** | **이번 주 금요일 회의(8월 28일)에서 네 가지를 봐 주세요.** (1) 예측 확신도의 최고 등급 기준이 **9거래일 연속 한 번도 닿지 않았습니다** — 기준을 다시 정할지 (2) 다른 컴퓨터에서 코드를 가져올 때 "결과를 만드는 부분도 같이 왔는가"를 확인하는 규칙 추가 (3) 점검표 채널 두 개의 남은 확인 (4) 새로 만든 지표를 청산 판단에 쓸지 | **20분** | (1)은 지금 아침 9시 20분~29분 구간이 사실상 전면 진입 금지로 동작하고 있다는 뜻입니다. 나머지는 점검표가 잘못된 그림을 보여주는 문제입니다 |
| **5** | **가끔 부탁드리는 정리** — 윈도우 PowerShell에서 `Remove-Item .git\*.lock*, .git\__* -Force; git gc --prune=now` 를 한 번 돌려 주세요. 제가 있는 환경에서는 파일 삭제가 막혀 있어 제가 못 지웁니다. | **1분** | 커밋 흔적 파일이 계속 쌓입니다. 커밋 자체는 우회로로 되므로 급하지 않습니다 |

---

*본 점검은 예약 점검(자동 실행)으로 작성됐다. **장전 국면이므로 라이브 코드·설정 변경 0건.**
산출은 증거 다이제스트(`evidence_MW0602-20260824_pre.md`)와 이 보고서, 그리고 `dev_memory` 기록뿐이다.
장중·장후 세션은 이 파일 **끝에 이어 붙인다** — 위 장전 절의 본문은 고치지 않는다.*
