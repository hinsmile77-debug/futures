# 미륵이 일일 점검 — MW0601 / 2026-08-17 / 장중(intra)

- 점검 시각: 16:49~17:1x KST · PC `MW0601` · 브랜치 `v9-dev` (정상, behind 0)
- HEAD `e995764` (08-17 15:18) · 당일 커밋 1건 · 미커밋 **10건 + 미추적 25건**
  (수집기 표기 `453건`은 오탐 — §1 P1-1)
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260817_intra.md`
- 실행 형태: 코웍 예약작업(리눅스 샌드박스). `v9-dev` 세대 수집기에 `--pc` 인자가 없어
  `platform.node()`를 `DeskTop-MW0601`로 주입해 실행했다. **로그 전용 수집기 · 라이브 DB 미접근.**

> 🔴 **오늘은 KRX 휴장일이다** — 광복절 대체공휴일
> (`config/krx_holidays.py:52` `"2026-08-17", # 광복절 대체공휴일 (월)`).
> "매분 9단계가 설계대로 도는가"라는 장중 질문은 오늘 성립하지 않는다. 범위를 ① 휴장 대기·
> 종료 경로가 설계대로였는가 ② 휴장일에만 드러나는 배치·계측 결함, 두 가지로 바꿨다.

> ⚠ **중복 처리 원칙 — 오늘 장전 세션이 16:57에 이미 리포트를 냈다**
> (`MW0601-20260817-점검리포트-pre.md`, 이상점 6건 · Fix F-1~F-4 · 고도화 G-1~G-3).
> 예약 3국면이 16:48~16:55에 몰려 실행돼(§1 P2-3) 장전 세션도 **마감 후 로그 전량**을 봤다.
> 따라서 본 보고서는 **장전 세션이 이미 기록한 항목을 재보고하지 않고**, §1 "장전 세션이
> 이미 기록한 것" 절에 한 줄씩 남기고 **본 세션이 더한 증거만** 덧붙인다.
> 신규 이상점은 **P1 1건 · P2 3건**이고, Fix·고도화 번호는 장전 세션과 충돌하지 않게
> **F-5부터 · G-4부터** 이어 붙였다.

## 0. 한 줄 결론

**P0 없음. 휴장 판정 → 유휴 대기 → 정상 종료 경로는 설계대로 동작했고 절대원칙 위반 흔적은
0건이다.** 본 세션이 새로 세운 것은 ① 장전 세션이 "거짓"이라고만 적어둔 수집기 미커밋
오탐의 **원인 특정과 즉시 적용 가능한 정규화 수정안**, ② `_BACKFILL.log` 0바이트라는
**기록되지 않은 배치 실행 흔적** 2건이다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | (별도 세션) | `MW0601-20260817-점검리포트-pre.md` (16:57) |
| 장중 | **해당 없음(휴장) · 대기·종료 경로 정상** | 08:40:01 기동 → 08:51:16~12:51:16 "공휴일·휴장일" 하트비트 **49회**(5분 주기, 전부 `포지션=FLAT`) → 12:54:57 `[CLEAN EXIT]` · `user_close` · RestartCnt=0. 진입체크·진입·체결·청산·차단·사이저 **전부 0건** |
| 장후 | (별도 세션) | `evidence_MW0601-20260817_post.md` (16:55) |

**절대원칙 준수 여부**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | **해당 없음** | 포지션 사상 0건, `강제청산` 문자열 0건. 471차 F-1R 리허설은 휴장으로 **08-18 이월** |
| ② | Circuit Breaker 5종 | **해당 없음(발동 0)** | `CircuitBreaker`·`HALT` 0건. 파이프라인이 돌지 않았으므로 **실전전환기준 ②의 근거로 쓸 수 없다** |
| ③ | CORE 피처 호라이즌 그룹별 | **해당 없음** | `[진입체크]` 0건 → 체크리스트 미발동. `vwap` 미통과 진입 0건 |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | **위반 흔적 없음 (본 세션 신규 확인)** | 당일 로그 12종 전량에서 `0xC0000409`·`STATUS_STACK_BUFFER_OVERRUN`·`MemoryError` **0건**. `logs/crash_fault.log` 마지막 줄 `[CLEAN EXIT] 2026-08-17T12:54:57  PID=22184` — 마지막 스택은 `dashboard/main_dashboard.py:10531 closeEvent` (정상 종료 경로) |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | **위반 흔적 없음** | 관련 예외 0건. `08:41:16 [LiveDBG] request_futures_balance 완료 총 109ms account=333044256` 정상 수신 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | **위반 흔적 없음** | 당일 로그 전체에서 `알파` **0건** |

**설정 불변식** (수집기 §3) — 21행 `일치`, 5행 `미발견 ⚠`.
`미발견` 5행은 장전 세션 1-1이 이미 판정했다(브랜치 격차, MW0601 회귀 아님). §1 하단 참조.

**장중 금지(phases.md B-7) 판정** — **위반 없음.**
당일 코드 편집은 전부 라이브 프로세스 종료(12:54:57) **이후**다:
`dashboard/main_dashboard.py` 15:04:47 · `strategy/ops/phase5_gate_status.py` 15:06:42 ·
`tests/conftest.py` 15:48:51 · `learning/prediction_buffer.py` 16:00:12 ·
`config/settings.py` 16:11:31 · `CLAUDE.md` 16:11:23.
당일 유일 커밋 `e995764`도 15:18(종료 후). 알파봇 자동통합 흔적 0건.

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**없음.**

### P1 — 정확성·관측 훼손

#### 1-1. [신규·원인특정] 수집기 미커밋 건수 오탐의 원인은 CRLF이며, `git -c core.autocrlf=true` 한 줄로 정규화된다

- **증상**: 수집기 §2가 `미커밋 453건`, §11 적신호 7번이 `미커밋 변경 453건`으로 뜬다.
  실제 변경은 **10건(+미추적 25건)** 이다.
  장전 세션 1-4는 이것을 *"거짓이다"* 라고 적고 CRLF를 원인으로 지목했으나
  **Fix 항목으로 등록하지는 않았다**(F-1~F-4에 수집기 git 호출부 수정이 없다).
  본 세션은 원인을 재현·측정하고 수정안을 확정했다.
- **근거**:
  ```
  $ git diff --numstat -- utils/time_utils.py utils/logger.py
  231  231  utils/time_utils.py     ← 추가·삭제 행수가 동일 = 전문 재기록(의미 변경 0)
  174  174  utils/logger.py
  $ git diff --stat | tail -1
  430 files changed, 241400 insertions(+), 241016 deletions(-)
  $ head -2 utils/time_utils.py | cat -A        → import datetime^M$   (작업트리 CRLF)
  $ git show HEAD:utils/time_utils.py | cat -A  → import datetime$     (블롭 LF)
  $ git config core.autocrlf                    → (미설정)
  $ git -c core.autocrlf=true  status --short | wc -l → 35
  $ git -c core.autocrlf=input status --short | wc -l → 35
  ```
  정규화 후 실제 수정 10건: `CLAUDE.md` · `config/settings.py` · `dev_memory/DECISION_LOG.md` ·
  `dev_memory/NEXT_TODO.md` · `docs/Ref/앙상블진입판단_진입실행_흐름상세.md` ·
  `docs/정기점검/금요일점검/weekly_prompt.txt` · `learning/prediction_buffer.py` ·
  `tests/conftest.py` · `tests/test_457_guard_meta_and_scope.py` · `utils/db_utils.py`
  (= 473차 작업분. 장전 1-4 목록과 일치).
- **기준 위반**: 계측 4원칙 ③(탈락·오탐 가시화). 함정 ③(멀티PC·브랜치)의 1차 탐지 수단이
  `git status`인데, 이 숫자가 오염되면 2026-08-12 브랜치 오전환류 사고의 탐지력이 떨어진다.
- **영향**: 진짜 미커밋 10건이 453이라는 숫자에 묻힌다. 장전 세션이 미커밋 목록을 손으로
  분류해야 했던 비용이 매 점검마다 발생한다.
- **신규 여부**: 원인 지목은 장전 1-4에 있음 / **재현·측정·수정안은 신규**(F-5).
- **범위 한정**: 이 오탐은 **코웍 리눅스 샌드박스 실행에 한정될 가능성이 높다.**
  윈도우 git은 보통 `core.autocrlf=true`가 켜져 있다 — 그러나 **미검증**이므로
  F-5는 "정규화를 강제"하는 방향(양쪽에서 같은 결과)으로 설계했다.

### P2 — 운영 부담·기술부채

#### 1-2. [신규] `20260817_BACKFILL.log` 0바이트 — 15:11:13에 열렸는데 한 줄도 남지 않았다

- **증상**: 다른 0바이트 채널들은 로그 매니저가 기동 시각 **08:40:39**에 만든 것인데,
  `BACKFILL`만 **15:11:13**에 생성됐고 내용이 0바이트다. 즉 본체 종료(12:54:57) 후
  **별도 프로세스가 15:11에 이 채널을 열고 아무 것도 쓰지 않았다.**
- **근거**:
  ```
  $ ls -la --time-style=+%H:%M:%S logs/20260817_*.log
  0      08:40:39  logs/20260817_DATA.log        ← 본체 기동 시 일괄 생성
  0      08:40:39  logs/20260817_HEALTH.log
  0      08:40:39  logs/20260817_MICRO.log
  0      15:11:13  logs/20260817_BACKFILL.log    ← 혼자 다른 시각
  ```
  이 파일의 **유일한 기록자**는 `scripts/aggregate_and_backfill.py:37-47`
  (`logging.basicConfig(handlers=[… FileHandler("{}_BACKFILL.log")])` — 모듈 임포트 시점에
  파일 생성). 첫 로그 호출은 `main()` 안쪽 `logger.info("DB 스키마 초기화 완료")`(line 124)다.
  따라서 임포트는 됐고 **line 124 도달 전에 종료**됐다 —
  후보 구간은 `from config.settings import RAW_DATA_DB` / `from features.bar_aggregator import
  BarAggregator` / `os.path.exists(RAW_DATA_DB)` / `init_raw_data_db()` / argparse다.
- **기준 위반**: 계측 4원칙 ④(폴백 가시화)와 ②(미측정 ≠ 0). 실행됐는지·실패했는지
  **로그만으로 판별 불가**하다. FP-CRITICAL·`spread_extreme_shadow`가 각각 2개월·1개월간
  "죽은 계측"이었던 것과 같은 패턴이다(있는데 안 남는다).
- **영향**: 휴장일 백필의 성패를 확인할 수 없다. 거래일에 같은 일이 나면
  `raw_features_horizon` 결손을 **다음 EOD 재학습 실패까지 모른다.**
- **판정 보류(313차 원칙)**: 종료 지점은 특정하지 못했다. 예외 트레이스백이
  stdout으로만 나갔을 가능성이 높다(핸들러에 `StreamHandler(sys.stdout)`가 함께 있다).
  누가 15:11에 이 스크립트를 띄웠는지도 미확인 — 스케줄러인지 사용자인지 로그에 없다.

#### 1-3. [신규·확장] 예약 3국면이 16:48~16:55에 몰려 실행 — 장전 세션이 보지 못한 `post` 세션까지 포함

- **증상**: 장전 세션 1-6이 `pre`(16:48)·`intra`(16:49) 2건을 지적했다. 그 시점에는 없던
  세 번째가 확인된다.
  ```
  evidence_MW0601-20260817_pre.md   생성 16:48:00  세션 determined-affectionate-lamport  PC **UNKNOWN** (host=claude)
  evidence_MW0601-20260817_intra.md 생성 16:49:16  세션 epic-compassionate-mendel        PC MW0601 (override)
  evidence_MW0601-20260817_post.md  생성 16:55:24  세션 dazzling-charming-mendel         PC MW0601
  ```
- **본 세션이 더한 것 2가지**:
  1. **3국면 전부**가 7분 안에 돌았다 — `post`까지 겹치므로 세 보고서가 같은 로그를 읽는다.
     휴장일이라 특히 중복이 크고, 실제로 본 보고서가 장전 리포트와 이상점 4건이 겹쳤다.
  2. **`pre` 다이제스트는 헤더가 `PC UNKNOWN (claude)`인데 파일명은 `MW0601`이다** —
     헤더와 파일명이 어긋난다. 지금은 사람이 알지만, 나중에 헤더만 보면 어느 PC의 관찰인지
     알 수 없다(SKILL.md가 `--pc` 누락을 두고 경고한 바로 그 사고 형태).
- **영향**: 국면 분리가 무의미해진다("오늘 거래할 자격이 되는가"를 마감 6시간 뒤에 묻는다).
  세션이 서로를 모르고 같은 결론을 3번 쓴다.
- **기준**: 스킬 §1(국면·날짜 확정), `--pc` 규약.
- **신규 여부**: 장전 1-6의 **확장**. `O-1`(다음 거래일 실행 시각 관측)이 이미 등록돼 있다.

#### 1-4. [신규] 런처 정상 종료를 `일시적 크래시`로 서술한다 — 동작은 옳고 문구만 틀렸다

- **증상**: 사용자 종료(정상)인데 런처가 크래시 문구를 찍고, 그 다음 줄에서 정상 종료로
  올바르게 판정한다.
- **근거**:
  ```
  2026-08-17 12:51:16 [INFO] SYSTEM: [System] 대기 중 | 공휴일·휴장일 — 다음 KRX 거래일 08:45 재개 | 레짐=NEUTRAL | 포지션=FLAT | 12:51:16
  [AUTO-RESTART] main.py 가 255분 실행됨 -- 일시적 크래시, 카운터 초기화.
    Mireuk 종료. RestartCnt=0 Runtime=255min
  [AUTO-RESTART] 정상 종료 감지 -- 이유: user_close -- 재시작 안 함
  [AUTO-RESTART] 재시작이 필요하면 start_mireuk.bat 를 다시 실행하세요.
  ```
  `logs/Mireuk_batch/launcher_20260817_084001_15180.log` 말미
- **평가**: **재시작 억제 동작 자체는 정상이다**(`user_close` 인식 → 재시작 안 함 →
  무한 재기동 없음). 문제는 서술이다 — 크래시가 없었는데 크래시로 적는다.
- **영향**: 진짜 크래시 로그를 사후에 검색할 때 이 문구가 오탐으로 잡힌다.
  오늘도 `crash_fault.log`(2.8MB)를 열어 `[CLEAN EXIT]`을 확인해야 크래시가 아님을 확정할 수 있었다.
- **곁가지(무해, 소음)**: 런처 로그에 matplotlib 한글 폰트 경고가 섞인다 —
  `Glyph 45936 (\N{HANGUL SYLLABLE DE}) missing from current font.` 11행 연속.

### 확인 필요 (확정 아님)

- 1-2의 `aggregate_and_backfill.py` 종료 지점 — 휴장일 조건으로 재현해야 특정된다.
  누가 15:11에 띄웠는지(스케줄러/사용자)도 확인 필요.
- 1-1의 CRLF 오탐이 **윈도우 직접 실행**에서도 나는지 — 사용자가 윈도우 셸에서
  `git status --short` 행수를 1회 세면 판정된다.
- `LEARNING` 채널 `Calibration` WARNING **143건**은 **이상 아님**으로 판단했고,
  이는 장전 세션의 판정과 같다(08:41:00~08:41:09 기동 복원 스윕, `n=90`에서 축퇴 해소,
  `08:41:09 보정기 복원 완료 (n=888 method=platt fitted=True degenerate=False unreachable=False
  span=0.00630 auc=0.550 out_max=0.3479)`로 수렴). **남는 문제는 판정이 아니라 로그 레벨**이다 → §3 G-4.
- `[SHAP] 주간 심사 완료 | 하락피처=1개 | 교체후보=3개 | CORE안전=⚠️` (08:41:14) —
  `CORE안전=⚠️`의 의미와 대상 피처는 이 로그 1행으로 판정 불가.
  **장후/주간 세션에서 SHAP 심사 상세를 봐야 한다**(휴장일 기동 시 심사가 도는 것이
  의도인지도 함께 — 신규 표본이 없으므로 전일과 같은 결론일 가능성).

### 이미 반영된 사안 (신규 아님)

| 항목 | 어디에 기록됐나 | 본 세션이 더한 것 |
|---|---|---|
| 불변식 감시표 5행 `미발견` (462·468차 상수가 `origin/dev` 전용) | 장전 1-1 · Fix F-1 · DECISION_LOG 오늘자 | 없음(재확인만). **MW0601 회귀 아님 · 브랜치 정상 · 2026-08-12 사고와 무관** |
| EOD 재학습 `[WaitDC]` 15:46:34 이후 정지 + 휴장일 게이트 부재 | 장전 1-2 · Fix F-2 | 보조 증거 3종: `data/daily_close_done_20260817.txt` **부재** · `data/eod_retrain_done_20260817.txt` **부재** · `model/horizons/*.pkl` 최종 **08-14 15:45~47(미교체 = 휴장일 정상)** |
| `TimeRouter`가 휴장일에도 시간대 전환 4회 | 장전 1-3 · Fix F-4 | 수집기 §7이 그 2건을 "09:00 정규장 개장·매분 루프 시작" 앵커 창에 담아 **루프가 돈 것처럼 보이게 한다**는 점 |
| 473차 변경분 미커밋(라이브 경로 3파일 포함) | 장전 1-4 · Fix F-3 | 정규화 후 정확한 10건 목록(1-1) |
| 정기점검 산출물 16종 untracked | 장전 1-5 | 오늘 새로 3종 추가(`evidence_..._{pre,intra,post}.md`) → **25종**으로 증가 |
| 예약 점검 16:48~16:49 몰림 | 장전 1-6 · 관측 O-1 | `post` 16:55 포함 3국면 · `pre` 헤더 PC UNKNOWN (1-3) |
| CB②`9999` / CB③-P4 `False` / FP-CRITICAL `False` / TOX-SEVERE `False` | CLAUDE.md 한시예외 | 전부 기대값 일치 — 의도된 상태 |
| 실전전환 ⑨ TOX-SEVERE 처분(473차 F-8 `INSUFFICIENT`, ETA 7.1개월) | CLAUDE.md · NEXT_TODO 주간회의 안건 | 없음 |

---

## 2. Fix 작업 구현계획

> **본 계획은 수립만 한다.** 적용은 **장후 창**에서 한다(오늘 라이브 프로세스는 12:54:57에
> 이미 종료됐지만, 규약대로 장중 국면 보고서는 적용하지 않는다).
> ⚠ **선행: 장전 세션 F-3(473차 변경분 커밋).** 미커밋 10건에 `config/settings.py` 등
> 라이브 경로가 포함돼 있어, 그 커밋 전에 다른 파일을 고치면 473차 변경과 섞인다.
> 번호는 장전 세션 F-1~F-4에 이어 **F-5부터** 붙였다.

### F-5. 수집기 git 호출부 CRLF 정규화 + `--pc` 인자 — P1 · 대응 이상점 1-1, 1-3

- **원인 가설**: 수집기가 `git status --short` / `git diff`를 그대로 호출한다. 리눅스
  샌드박스에는 `core.autocrlf`가 없어 CRLF 작업트리 전체가 수정으로 보인다.
  윈도우 실행 시에는 보통 `true`라 안 보였고, 코웍 예약작업이 도입된 뒤 드러났다.
- **변경 파일**:
  - `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py`
    - git 서브프로세스 호출부 전량: `["git", …]` → `["git", "-c", "core.autocrlf=true", …]`
      (실측: 453 → 35건). **읽기 전용 옵션이므로 작업트리를 건드리지 않는다.**
    - §2 집계: 수정(` M`)과 미추적(`??`)을 **분리 표기** —
      `미커밋 수정 N건 · 미추적 M건`. §11 적신호는 **수정분만** 센다
      (미추적은 점검 산출물이 매일 늘어 상시 점등된다).
    - `pc_id()`: 우선순위 `--pc` 인자 > `MIREUK_PC_ID` 환경변수 > 호스트명.
      override 시 헤더에 `MW0601 (override · host=claude)` 표기.
      **SKILL.md는 이미 이 인자를 전제하는데 `v9-dev` 수집기에는 없다**
      (오늘 `platform.node()` 주입으로 우회).
- **회귀 위험**: 낮음. 윈도우 직접 실행에서 결과가 바뀌지 않는지 1회 대조 필요
  (이미 `true`면 무변화). `-c`는 전역 설정을 쓰지 않는다.
- **절대원칙 저촉 여부**: 없음(점검 도구, 매매 경로 무관).
- **런타임 주의**: 표준 라이브러리만 사용 — py37_32/py310_64 무관. COM 콜백 아님.
- **검증**: `v9-dev` 재실행 시 §2가 `수정 10건 · 미추적 25건`, §11 적신호에서 미커밋 항목 소멸.
  `--pc MW0601` 지정 시 헤더 override 표기. 윈도우에서 1회 교차 확인.
- **섀도 계측 필요 여부**: 불필요.
- **적용 시점**: **장후.** ⚠ **장전 F-1(브랜치 인식 감시표)과 같은 파일·같은 함수군이다 —
  반드시 한 커밋으로 묶어라.** 따로 나가면 같은 파일을 두 번 만지며 충돌한다.
- **결정 필요 사항**: 없음(권고: 그대로 적용).

### F-6. 백필 스크립트 기동·종료 흔적 로깅 — P2 · 대응 이상점 1-2

- **원인 가설**: `logging.basicConfig`가 파일을 만드는 시점과 첫 `logger.info` 사이에
  임포트·DB 초기화가 있어, 그 구간에서 죽으면 **파일은 생기고 내용은 없다.**
  예외 트레이스백은 `StreamHandler(sys.stdout)`로만 나가 콘솔과 함께 사라진다.
- **변경 파일**:
  - `scripts/aggregate_and_backfill.py` — `logging.basicConfig` 직후(모듈 최상단,
    `main()` 진입 전): `logger.info("[Start] pid=%s argv=%s", os.getpid(), sys.argv)`.
  - 같은 파일 — `if __name__ == "__main__":` 를 `try/except`로 감싸
    `logger.exception("[Fatal] 백필 중단")` + `logger.info("[Done] …")`.
    **파일 핸들러에 트레이스백이 남는 것이 이 Fix의 목적**이다.
- **회귀 위험**: 없음(로그 2~3행 추가, 로직 무변경).
- **절대원칙 저촉 여부**: 없음.
- **런타임 주의**: 이 스크립트는 py37_32 경로로 돈다(`features.bar_aggregator` 임포트).
  `os`·`sys`는 이미 임포트돼 있는지 확인할 것.
- **검증**: 다음 실행에서 `_BACKFILL.log`가 0바이트가 아니고 첫 줄이 `[Start] pid=… argv=…`.
  ⚠ **0바이트가 아니게 되는 것만으로 성공이 아니다** — `[Done]` 또는 `[Fatal]` 중
  하나가 반드시 찍혀야 한다(계측 4원칙 ④).
- **적용 시점**: 장후.
- **결정 필요 사항**: 15:11 기동 주체(스케줄러 등록 여부) 확인 — 사용자 확인 필요.

### F-7. 런처 정상 종료 문구 정정 — P2 · 대응 이상점 1-4

- **변경 파일**: `start_mireuk.bat`(런처 재시작 판정 블록) — 정상 종료(`user_close`) 경로에서
  `일시적 크래시, 카운터 초기화` 문구를 쓰지 않도록 출력만 분기.
  예: `정상 종료(user_close) -- 재시작 카운터 초기화`.
- **회귀 위험**: 🔴 **재시작 판정 로직을 건드리지 말 것.** 오늘 동작은 옳았다
  (`RestartCnt=0`, 재시작 안 함). **문자열만 바꾼다.**
- **절대원칙 저촉 여부**: 없음.
- **검증**: 다음 수동 종료 시 런처 로그에 `일시적 크래시` 미출현 + `재시작 안 함` 유지.
- **적용 시점**: 장후(최저 우선순위 — 문구 문제다).

### 적용 순서와 커밋 계획

1. **커밋 ①** — 장전 F-3 (473차 변경분). **선행 필수.**
2. **커밋 ②** — 장전 F-1 + 본 F-5 (같은 파일: `collect_evidence.py` · `invariants.md`).
   `[MW0601] 474차: 수집기 불변식 브랜치 스코프 + git CRLF 오탐 제거 + --pc 인자`
3. **커밋 ③** — 장전 F-2 (EOD 휴장일 게이트 + WaitDC 흔적) — `py310_64` 검증 동반.
4. **커밋 ④** — 본 F-6 + F-7 + 장전 F-4 (TimeRouter). 관측·문구 계열 묶음.
   `[MW0601] 474차: 백필 기동/종료 로깅 + 런처 종료 문구 + TimeRouter 거래일 인식`

---

## 3. 고도화 방안

> 장전 세션 G-1(수집기 휴장일 배지) · G-2(재학습 완주 마커) · G-3(브랜치 격차 상시 노출)에
> 이어 **G-4부터** 붙인다. 중복 제안 없음.

### G-4. 기동 복원 스윕의 `Calibration` 경고를 WARNING에서 내린다

- **관측 근거**: 오늘 `LEARNING` 채널 WARNING **143건 전부**가 `Calibration` 기동 복원
  스윕(08:41:00~08:41:09)이고, 같은 스윕이 `보정기 복원 완료 (… degenerate=False
  unreachable=False)`로 정상 종료했다. **정상 종료한 절차가 WARNING 143건을 남긴다.**
  장전 세션도 "정상 동작"으로 판정했다 — 즉 **두 세션이 독립적으로 같은 소음을 걸러내는
  비용을 지불했다.**
- **제안 내용**: 부트스트랩 n 증가 루프(복원 스윕) 구간의 `축퇴 감지`·`하한 도달불가`는
  `DEBUG`/`INFO`로 내리고, **스윕 종료 후 최종 상태만 WARNING 판정**한다.
  또는 스윕을 1행으로 집계: `축퇴 감지 n=80~85 3건 / 해소 n=90 → 최종 degenerate=False`.
  🔴 **임계·판정 로직은 건드리지 않는다 — 로그 레벨과 집계만 바꾼다**
  (축퇴 가드는 403차 이후 conf 스케일에 직접 영향을 주므로 판정 변경은 별건이다).
- **기대 효과**: **장중 실시간 축퇴(진짜 위험 신호)가 기동 소음에 묻히지 않는다.**
  수집기 §11 적신호 6번(`축퇴 8건`)이 실질 신호가 된다. 매 점검 세션이 143건을
  손으로 무해 판정하는 작업이 사라진다.
- **비용·위험**: 낮음(로깅 레벨). 위험은 "복원 실패를 조용히 넘기는 것" 하나뿐이므로
  **최종 상태 WARNING은 반드시 남긴다**(계측 4원칙 ④).
- **선행 조건**: 없음.
- **우선순위 제안**: 이번 주.

### G-5. 휴장일을 "배치 잡 정합 점검일"로 규정한다 (`phases.md` D절 신설)

- **관측 근거**: 오늘 나온 실질 이상점은 **거래일에는 구조적으로 드러나지 않는 것들**이다.
  EOD `[WaitDC]` 무한대기는 거래일엔 `daily_close()`가 마커를 써서 가려지고,
  `_BACKFILL.log` 0바이트는 거래일엔 뒤이은 정상 로그에 덮여 눈에 띄지 않는다.
  휴장일은 **라이브 리스크 0인 상태에서 스케줄 의존성만 남기는 유일한 창**이다.
- **제안 내용**: `references/phases.md`에 **D절 "휴장일(non-trading day)"** 신설 —
  ⓐ `data/daily_close_done_{d}.txt` **부재 확인**(있으면 그게 이상)
  ⓑ 15:45 EOD 재학습이 **스킵 로그 1행으로 끝났는가**(장전 F-2 적용 후 판정 항목이 된다)
  ⓒ 백필·캠페인 체인이 휴장 보정으로 **직전 거래일에 이월됐는가**
  ⓓ `model/horizons/*.pkl`·완료 마커 날짜가 **직전 거래일로 유지**되는가
     (오늘: pkl 08-14 15:45~47 유지 = 정상. **이 값이 오늘 날짜면 휴장일 재학습이
     실제로 일어난 것**이므로 P0에 가깝다)
  ⓔ 런처가 유휴 대기 중 무엇을 쥐고 있는가(COM 세션·DB 핸들)
  ⓕ 각 배치 로그가 **0바이트가 아닌가**(1-2가 만든 항목).
- **기대 효과**: 연 15~18일의 휴장일이 관측 공백에서 **배치 회귀 검증일**로 바뀐다.
  오늘로 치면 ⓓ가 "정상"임을 근거로 남길 수 있었고, ⓕ가 1-2를 자동으로 잡았을 것이다.
- **비용·위험**: 문서 1절. 위험은 "휴장일이니 아무것도 안 본다"로 굳는 것 —
  장전 G-1(수집기 휴장일 배지)이 §11 적신호를 억제하지 않기로 한 것과 같은 취지로,
  **D절은 축소가 아니라 대상 교체**임을 문서에 못 박는다.
- **선행 조건**: 장전 F-2(휴장일 게이트) — ⓑ가 판정 가능해지는 시점.
- **우선순위 제안**: 이번 주 (장전 G-1과 같은 커밋).

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| 휴장일 배치 점검 D절 | 미등록 | `references/phases.md` D절 신설 (G-5) | 연 15~18일의 관측 창을 회귀 검증에 쓴다 |
| 26주 WFA 재검증 항목 | CLAUDE.md 4항목 | **변경 제안 없음** | 오늘 관측은 파라미터 드리프트 축이 아니다 |
| 실전 전환 기준 | ①~⑨ | **변경 제안 없음** | 휴장일 관측은 ①~⑨ 어느 조건의 근거도 되지 못한다. 특히 **CB 발동 0건을 ②의 근거로 쓰지 말 것** — 파이프라인 자체가 돌지 않았다 |

---

## 4. 진입 승패 사후검증 (장후 한정)

**해당 없음(장후 전용).** 덧붙여 오늘은 **휴장일로 진입 0건**이므로 장후 세션에서도
케이스 카드는 생성되지 않는다.

## 5. 3원 대사 · DB 검증 (장후 한정)

**해당 없음(장후 전용).** 본 세션은 지시대로 **라이브 DB에 어떤 분석 쿼리도 돌리지 않았다** —
수집기는 로그 전용이고, DB는 `ls`로 파일 mtime만 확인했다
(2026-08-10 13:47 CB⑤ 자가유발 전례 회피. 그 발동은 자가유발이므로 실전전환기준 ②의 근거 아님).

---

## 부록. 오늘 관측 원표 (휴장일 기준선)

| 항목 | 값 |
|---|---|
| 거래일 여부 | **휴장** (광복절 대체공휴일, `krx_holidays.py:52`) |
| 런처 기동 | 08:40:01 (`launcher_20260817_084001_15180.log`) |
| 본체 PID / 실행시간 | 22184 / 255분 |
| 종료 | 12:54:57 `[CLEAN EXIT]` · `user_close` · RestartCnt=0 · 재시작 안 함 |
| 휴장 하트비트 | 08:51:16~12:51:16 · 5분 주기 **49회** · 전부 `레짐=NEUTRAL 포지션=FLAT` |
| 세션 재기동 | `[Session] 재기동 #1 | cause=STARTUP` (최초 기동, 이상 아님) |
| 진입체크/진입/체결/청산/차단/사이저 | 0 / 0 / 0 / 0 / 0 / 0 |
| CB 발동 | 0건 (`CircuitBreaker`·`HALT` 0) |
| 메인 스레드 블로킹 | 2건 · 최대 **2,781ms** (CB⑤ 임계 5,000ms 미달 · 08:41:19 기동 버스트) |
| `TimeRouter` 전환 | 4회 (09:00 GAP_OPEN / 09:05 OPEN_VOLATILE / 10:30 STABLE_TREND / 11:50 OTHER) |
| DynMC 기동 복원 | 7구간 (GAP_OPEN 0.670→0.434 · OPEN_VOLATILE 0.600→0.422 · STABLE_TREND 0.540→0.413 …) |
| SHAP 주간 심사 | 08:41:14 완료 · 하락피처 1개 · 교체후보 3개 · **CORE안전=⚠️** (상세 미확인) |
| 모델 pkl 최종 | **08-14 15:45~15:47** (미교체 = 휴장일 정상) |
| 완료 마커 | `daily_close_done_20260817.txt` **부재** · `eod_retrain_done_20260817.txt` **부재** |
| 크래시·COM 패턴 | `0xC0000409`·`STATUS_STACK_BUFFER_OVERRUN`·`MemoryError`·`dynamicCall` **0건** |
| 로그 채널 이상 | `20260817_BACKFILL.log` 0바이트(15:11:13 생성) — §1 1-2 |
| 당일 코드 편집 | 전부 본체 종료(12:54:57) 이후 · 최종 `config/settings.py` 16:11:31 |

> conf·min_conf 관련 지표는 **오늘 산출값이 없다**(예측 0건). CLAUDE.md "확률 판단 기준"
> 표와의 대조는 수행하지 않았다 — 그 표는 2026-07-31 conf 스케일 변경 이전의 설계 원안이며
> 현행 판정 기준이 아니다(`CONF_SCALE_BREAKS`, DynMC p65).
