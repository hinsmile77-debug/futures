# 미륵이 일일 점검 — MW0601 / 2026-09-02

# 장전(pre)

- 점검 시각: 08:59~09:05 KST · PC `MW0601` · 브랜치 `v9-dev` (정상 — MW0602는 `dev`)
- HEAD `a3f70ab` · 미커밋 514건(전량 EOL 파생·코드 변경 0건, `core.autocrlf` 미설정 — CRLF/LF 표시 차이일 뿐 실질 변경 아님) · 당일 커밋 0건
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260902_pre.md`
- `.git/index.lock` 없음(수집기 자가점검: 이 수집 실행이 락을 만들지 않았다)
- 병행 세션 확인: 오늘(2026-09-02 00:00~) 커밋 0건, `docs/정기점검/매일점검/` 최신 파일은 09-01 장후 리포트 — 병행 세션 없음

## 0. 오늘 한눈에

**어제 저녁 리포트가 "지금 즉시 계좌를 확인해서 정리하라"고 경고했던, 출처를 알 수 없는
매수 3계약이 정리되지 않은 채 오늘 아침까지 남아 있었습니다. 프로그램이 다시 켜지면서
그 사실을 확인했고, 마침 가격이 크게 떨어져 있어 08시 45분에 자동으로 손절 처리됐습니다.
이 손실은 이미 확정됐고 금액은 약 530만 원(투자 원금의 10.6%)입니다.**

프로그램 자체가 오작동한 것은 아닙니다 — 오히려 위험한 상황(주인을 알 수 없는 큰 손실
포지션)을 정상적으로 인식해서 정해진 손절 규칙대로 즉시 정리했습니다. 문제는 두 가지입니다.
첫째, 이 포지션이 애초에 어디서 왔는지가 아직도 밝혀지지 않았습니다(어제부터 이어진 미해결
사안입니다). 둘째, 어제 저녁 사람이 직접 정리해 달라는 요청이 실행되지 않았습니다(또는
실행이 늦었습니다). 지금 이 시각 기준으로 계좌에는 포지션이 없고(현금 상태), 추가로
위험에 노출된 것은 없습니다. 그 밖의 개장 준비 과정(프로그램 기동, 접속, 모델 로드, 오늘
새벽 자동 재학습, 시장 상황 판정)은 특별한 문제 없이 정상적으로 끝났습니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | **이상(P0)** | 전일 미해소 포지션이 오늘 아침 자동 손절로 -5,299,668원(자본대비 -10.60%) 확정. 개장 준비 절차 자체는 정상 |
| 장중 | (진행 전 — 이 절은 장전 시점 작성) | |
| 장후 | (진행 전) | |

**절대원칙 준수 여부(장전 시점)**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 오버나이트 금지(15:10 강제청산) | **🔴 위반(어제 09-01 발생분의 오늘 아침 실현)** | 09-01 이상점 1-6이 청산되지 않은 채 자정을 넘겼고, 오늘 08:45 그 결과로 손실이 확정됐다 — 아래 1-1 |
| ② | Circuit Breaker 5종 | 부분 준수(기존 한시예외 지속) | `CB_CONSEC_STOP_LIMIT=9999`(CB② 사실상 비활성) 재검토 기한 2026-08-29 초과, 계속 미결 — §7 |
| ③ | CORE 피처 호라이즌 그룹별 | 해당 없음(구조 점검 대상, 장전 관측 불가) | — |
| ④ | COM 콜백 dynamicCall·emit 금지 | 준수 | `0xC0000409`·STACK_BUFFER 로그 0건 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 구분 | 해당 없음(코드 정합성 점검, 로그로 직접 관측 불가) | — |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 준수(위반 근거 없음) | — |

**설정 불변식** — 수집기 §3 표 26행 전부 `일치`. 불일치 0건. (CB② 등 한시예외는 "꺼진 상태가
정상"이며 §7에서 기한만 별도로 다룬다.)

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

#### 1-1. 전일(09-01) 미해소 미상 매수 3계약이 오늘 08:45 자동 손절로 -5,299,668원(자본대비 -10.60%) 확정

- **증상**: 어제(2026-09-01) 장후 리포트가 P0(이상점 1-6)로 등록했던 "15:34:46 원인불명 매수
  3계약, 청산 시도 없이 밤새 보유"가 오늘 아침까지 정리되지 않았다. 프로그램이 08:40에
  재기동하면서 브로커 계좌에 그 포지션이 그대로 남아 있는 것을 확인했고, 4분 뒤 하드스톱
  (손절)이 걸려 3계약 전량이 자동 청산됐다. 청산가는 진입가(1076.00) 대비 약 35.1포인트
  낮은 1040.7~1041.0대 — 즉 이 포지션은 어젯밤~오늘 아침 사이 큰 폭의 가격 하락에
  그대로 노출돼 있었다.
- **근거(로그 인용 + 시각)**:
  ```
  2026-09-02 08:40:49 [INFO] TRADE: [Position] 저장 상태가 어제 데이터 — 무시
  2026-09-02 08:41:09 [CRITICAL] SYSTEM: [BrokerSync] startup sync 완료: FLAT -> LONG 3계약 @ 1076.00
  2026-09-02 08:41:09 [WARNING] TRADE: [PositionFallback] entry_horizon 미설정 → TP1 배수 폴백 1.00 적용
      status=LONG qty=3 entry=1076.00 — 진입 경로가 파라미터를 넘기지 않았다(F-5 대상)
  2026-09-02 08:41:09 [WARNING] TRADE: [Position] 브로커 기준 동기화: LONG 3계약 @ 1076.0 | 손절=1075.25
  2026-09-02 08:45:09 [INFO] TRADE: [TickStop-S0C] 하드스톱(틱) LONG 3ct tick=1041.44 stop=1075.25 → 주문 전송
  2026-09-02 08:45:10 [WARNING] SYSTEM: [ExitFillFlow] fill_price=1040.74 fill_qty=1 (1/3)
  2026-09-02 08:45:10 [WARNING] SYSTEM: [ExitFillFlow] fill_price=1040.98 fill_qty=1 (2/3)
  2026-09-02 08:45:11 [WARNING] SYSTEM: [ExitFillFlow] fill_price=1040.92 fill_qty=1 (3/3, after='FLAT')
  2026-09-02 08:45:10 [INFO] TRADE: [청산 완료] PnL=-105.36pt | 포지션 합계 -5,299,668원 (레그 3)
  2026-09-02 08:45:11 [WARNING] SYSTEM: [PendingOrder] clear {... agg_exit_pnl_krw: -5299668.0, agg_flushed: True}
  ```
  `logs/20260902_TRADE.log` · `logs/20260902_WARN.log` · `data/position_state.json`
  (마지막 저장 08:45:10.956978, `status: FLAT`) · 전일 근거는
  `docs/정기점검/매일점검/MW0601-20260901-점검리포트.md` 1-6(원 항목, 1029~1102행).
- **기준 위반**: `CLAUDE.md` 절대원칙 §1(오버나이트 금지 — 15:10 전 포지션 무조건 청산,
  수익/손실 무관). 매분 9단계 STEP 8(청산 트리거 감시)이 어제 15:34:46 이후 재실행되지
  않아 이 포지션이 밤새 방치된 것이 직접 원인 — 어제 리포트가 이미 이 기준 위반을
  확정했고, 오늘은 그 **금전적 결과가 실현**된 것이다.
- **영향**: 어제 확인을 요청했던 매수 포지션이 오늘 아침 결국 큰 손실로 마감됐습니다.
  프로그램은 위험한 상황을 정상적으로 인지하고 정해진 손절 규칙대로 즉시 정리했으므로
  추가로 위험에 노출된 것은 없습니다(현재 포지션 없음, 현금 상태). 다만 이 손실 자체는
  이미 확정된 것이라 되돌릴 수 없고, 애초에 이 포지션이 왜 생겼는지는 여전히 밝혀지지
  않았습니다 — 같은 일이 또 생기지 않는다는 보장이 없습니다.
- **영향(기술)**: 포지션 단위 손익 -5,299,668원 / -105.36pt(3계약, 평균 -35.12pt/계약),
  자본(`SIZING_TARGET_CAPITAL_KRW=50,000,000`) 대비 **-10.60%**. 08-31 유사 사고
  (505차, LONG 4계약 -5,461,928원/-10.92%, 브로커 거부 실패 주문이 유령 진입으로 둔갑한
  경우)와 손실 규모가 비슷하지만, **발생 경로는 다르다** — 08-31은 실패한 청산 주문이
  브로커에서 신규 진입으로 처리된 결함이었고, 오늘은 09-01 15:34:46에 실제로 열린
  정체불명 매수 포지션이 밤새 방치된 것이다(둘 다 궁극 원인은 09-01 리포트가 지목한
  "원인불명 외부/MANUAL 진입" 문제로 수렴한다 — 09-01 이상점 1-1/O-i4). **오늘의 청산
  실행 자체는 정상 동작했다** — 08-31처럼 주문이 거부되지 않았고(511차 F-19/F-21/F-22
  배포 후 첫 실사용), 3계약이 08:45:10~11 약 1초 안에 전량 체결됐다(§1-3 참조,
  O-p3 관측 판정 완료로 처리).
- **신규 여부**: **신규 아님 — 전일 P0(이상점 1-6)의 직접 귀결.** 근본 원인(정체불명
  외부진입, O-i4)은 09-01부터 미확정 상태로 이월 중이며, 오늘도 확정하지 못했다
  (아래 "확인 필요" 참조 — 08:45 이후는 라이브 DB 분석 금지 구간이라 장전 세션에서는
  DB 조회로 발생원을 추적할 수 없다).

### P1 — 정확성·관측 훼손

#### 1-2. `PositionFallback`(entry_horizon 미설정) — 09-01부터 지속되는 동일 결함이 오늘도 재현

- **증상**: 브로커 동기화로 포지션을 인식할 때 `entry_horizon`이 설정되지 않아 TP1 배수가
  설계값 대신 폴백값(1.00, 호라이즌별 설계값의 최대 2배)으로 적용됐다.
- **근거**: `2026-09-02 08:41:09 [WARNING] TRADE: [PositionFallback] entry_horizon
  미설정 → TP1 배수 폴백 1.00 적용 (호라이즌별 설계값의 최대 2배). status=LONG qty=3
  entry=1076.00`. `logs/20260902_TRADE.log`.
- **기준 위반**: 계측 4원칙 ④(폴백 가시화) — 로그 자체는 폴백 사실을 명시하고 있어
  원칙을 지키고 있으나, 근본 결함(진입 경로가 entry_horizon을 넘기지 않음)은 09-01부터
  미해결이다.
- **영향**: 이번 포지션은 어차피 정상 청산 규칙이 아니라 손절로 끝났기 때문에 TP1 배수가
  잘못 적용된 것이 이번엔 실제 손익에 영향을 주지 않았습니다. 다만 같은 결함이 정상
  진입에서 발생하면 이익 실현 폭이 설계와 달라질 수 있습니다.
- **영향(기술)**: 09-01 리포트 1-2(P2)와 동일 원인. F-5(entry_horizon 전달 수정)로
  등록돼 있으나 아직 구현되지 않았다. 오늘은 외부/이월 포지션 브로커 동기화 경로에서도
  같은 결함이 재현됨을 추가로 확인 — **엔진 자체 진입 경로 + 브로커 동기화 경로 둘 다**
  이 결함을 갖는다는 뜻이라 F-5 범위를 넓혀야 할 수 있다(코드 확인 필요, 장후 이월).
- **신규 여부**: 신규 아님 — 09-01 이상점 1-2의 재현.

#### 1-3. 청산 주문 3계약 전량 정상 체결 — 511차 Fix 첫 라이브 확인(O-p3 판정 완료)

- **증상(관측)**: 09-01까지는 청산 주문 거부가 163건 발생(09-01 이상점 1-3, P0)했고
  그 수정(511차 F-19/F-21/F-22, 커밋 `a06cd05`)이 코드에는 반영됐지만 재기동 전이라
  실효가 없었다. 오늘 08:40 재기동 이후 이 수정이 처음으로 라이브에서 시험됐다.
- **근거**: `08:45:09 [ExitSendOrderResult] ret=0` → `08:45:10~11 ExitFillFlow ×3`
  (전량 정상 체결, 거부·재시도 로그 0건) → `08:45:11 [PendingOrder] clear
  agg_flushed=True`. `logs/20260902_WARN.log`.
- **기준 위반**: 해당 없음(정상 동작 확인 — 이상점이 아니라 관측 판정).
- **영향**: 오늘 아침의 대규모 손절 주문이 지연이나 거부 없이 1초 안에 전량 처리됐습니다.
  09-01에 있었던 "손절 주문이 반복 거부되는" 문제가 이번엔 나타나지 않았습니다.
- **영향(기술)**: `09-01 NEXT_TODO O-p3`("511차 Fix 재기동 후 라이브 동작 확인")를
  **판정 완료 — 정상**으로 닫는다. 다만 이번 케이스는 `EXIT_FULL`(하드스톱) 단일 주문
  경로였고, 09-01에 문제가 됐던 "청산가능수량 선점"(외부 미체결 주문과의 경합) 상황은
  오늘 재현되지 않았다 — 그 경합 조건까지 완전히 검증된 것은 아니다.
- **신규 여부**: 신규 아님 — 09-01 O-p3 관측 항목의 판정.

### P2 — 운영 부담·기술부채

#### 1-4. 증거 다이제스트 정합성 불일치 — 귀속 실패 레그 3행(-5,299,668원 전액)

- **증상**: 증거 수집기 §5가 오늘 청산을 "레그합 -5,299,668 = 포지션합 +0 → 불일치"로
  표시했다. 이 포지션에 대한 "진입" 로그가 없어(어제 15:34:46에 열렸으므로 오늘 로그
  범위 밖) 수집기의 포지션 조립 로직이 이 청산을 어떤 진입에도 묶지 못했다.
- **근거**: evidence §5, "정합성: 레그합 -5,299,668 = 포지션합 +0 → 불일치 ⚠ ·
  `[청산 완료]` 1건 = 조립 포지션 0건 → 불일치 ⚠ · 귀속 실패 레그 3행 ⚠(진입 로그
  없는 이월 포지션 가능)". `docs/정기점검/매일점검/evidence_MW0601-20260902_pre.md`.
- **기준 위반**: 계측 4원칙 ①(단위 명시)·③(탈락 가시화) — 수집기 자체는 규약대로
  불일치와 귀속 실패 건수를 명시했으므로 수집기 결함은 아니다. 다만 이 상태로 사람이
  §5 표만 보면 "오늘 포지션 0건·손익 0원"으로 오독할 위험이 있다(실제로는 -530만원의
  실현 손실이 있었다).
- **영향**: 자동으로 만들어지는 요약표만 보면 오늘 아침 큰 손실이 있었다는 사실이
  드러나지 않습니다. 이 리포트처럼 원본 로그를 직접 확인해야만 정확한 손익을 알 수
  있습니다.
- **영향(기술)**: 09-01 리포트가 이미 이 패턴(귀속 실패 레그, 당시 -54,538원 규모)을
  NEXT_TODO에 등록했다 — 오늘은 같은 결함이 훨씬 큰 금액(-5,299,668원 전액)으로
  재현됐다. 근본 원인은 이월 포지션에 진입 로그가 없다는 구조적 한계이며, DB
  (`trades`/`ensemble_decisions`)에는 청산 레코드가 남아 있을 가능성이 높아 로그 대신
  DB 3원 대사로 보완 가능할 것 — 단 지금(08:45 이후)은 라이브 DB 분석 금지 구간이라
  장후로 이월한다.
- **신규 여부**: 기존 TODO(`NEXT_TODO.md:18519` "레그합-포지션합 불일치(-54,538원) 원인
  특정")의 재현·확대 사례.

### 확인 필요 (확정 아님)

- **오늘 손실 포지션(1-1)의 발생원**: 09-01 1-1/O-i4("원인불명 외부/MANUAL 진입 발생원 —
  HTS 수동주문인지 다른 기기 접속인지")가 여전히 미확정이다. 08:45 이후는 라이브 DB
  분석 금지 구간(CLAUDE.md)이라 장전 세션에서는 `trades.db`/`predictions.db` 조회로
  이 포지션의 등급·`grade`·`entry_horizon` 원본 기록을 추적할 수 없다. **장후에 반드시
  확인** — `trades` 테이블에서 오늘 새벽~08:45 구간 또는 09-01 15:34:46 진입의
  `grade` 값을 조회.
- **어제 사용자가 수동으로 계좌를 정리했는지 여부**: 09-01 리포트가 "지금 즉시 매도"를
  요청했다. 오늘 BrokerSync가 포지션이 그대로 남아 있었음을 확인했으므로, 사용자가
  매도하지 못했거나(계좌 접근 지연 등) 매도가 체결 전에 시장이 다시 움직였을 가능성이
  있다 — **이것은 시스템 로그만으로는 판정할 수 없고 사용자 확인이 필요하다**(§ 사용자
  조치 참조).

### 이미 반영된 사안 (신규 아님)

- **`[CybosProbe] CoInitialize가 호출되지 않았습니다` 10건(08:58:13·08:58:43)**: 기존
  등록 P2 F-3(`NEXT_TODO.md:15342`, 2026-08-19 2-1 이월분)와 동일 패턴 — Cybos 선물
  투자자 수급 탐침 워커의 `pythoncom.CoInitialize()` 누락. 실거래 경로와 무관(탐침
  전용). 신규 안건 아님.
- **`ConfFloorGuard`(자동진입 하한 도달 불가) 09:00:01 1건**: "보정기 출력상한 0.3479
  < 필요 0.4240" — 개장 직후 보정기 워밍업 구간에서 반복적으로 관측돼 온 패턴이다
  (`DECISION_LOG.md:27306` 등에 "개장 첫 분 보정기 워밍업, 이후 축퇴 해소로 복귀.
  상시 아님"으로 이미 특성화됨). 오늘도 1건뿐이고 그 직후(09:06 전후 예상) 축퇴 해소
  여부는 장중에 확인한다 — **O-p1로 관측 등록**(아래).

---

## 2. Fix 작업 구현계획

### F-1. `daily_close()` 진입 전 잔여 포지션 강제청산 통합 — P0 · 대응 이상점 1-1

**이미 09-01 리포트가 P0-2로 계획을 낸 항목이다(함정① 확인 완료 — `grep`으로
`main.py:daily_close()`를 다시 확인했으나 아직 구현되지 않았다).** 오늘 사고가 이 Fix의
필요성을 재확인시켰을 뿐, 내용은 09-01 계획과 동일하다.

- **원인 가설**: 09-01 리포트 1140~1165행 그대로 — `daily_close()`가 마감 직전 잔여
  포지션 존재 여부를 확인하지 않고, `force_flat_guard.py`가 15:12 단발 실행이라 그
  이후 생긴 포지션을 구조적으로 보지 못한다.
- **변경 파일**: `main.py: daily_close()` 진입부(잔여 포지션이면 강제 청산 경로 동기
  호출) / `scripts/force_flat_guard.py`(15:10~15:39 반복 스케줄 — 이 부분은 09-01
  514차 F-B로 이미 `["15:20","15:30","15:39"]` 추가 배포됐고 다음 재기동부터 실효).
- **회귀 위험**: 마감 절차 지연, 청산 재거부 시 무한 대기 위험 — 반드시 상한 시도
  횟수·타임아웃 필요.
- **절대원칙 저촉 여부**: 절대원칙 §1을 오히려 강화하는 방향. §4(COM 콜백 내
  dynamicCall·emit 금지) 준수 유지 필요.
- **런타임 주의**: `main.py` 메인 스레드, py37_32.
- **검증 방법**: 마감 직전 인위적으로 포지션을 열어두는 테스트 하네스로 재현.
- **섀도 계측 필요 여부**: 필요 없음(기존 청산 경로 재사용, 호출 시점만 변경).
- **적용 시점**: **자동조치 C등급(주문·청산 실행 경로 변경)** — 승인 전에는 구현하지
  않는다. 장전이므로 계획만 재확인.
- **결정 필요 사항**: 주간회의 승인 여부. 09-01 리포트가 이미 상정했으나 09-02까지
  미승인 상태 — 오늘 사고(실현 손실 -530만원)를 근거로 승인 우선순위를 높일 것을
  권고한다.

### F-2. `entry_horizon` 미전달 — 엔진 진입 경로뿐 아니라 브로커 동기화 경로도 포함하도록 범위 확장 — P1 · 대응 이상점 1-2

- **원인 가설**: 09-01 리포트가 F-5로 등록한 결함이 엔진 자체 진입뿐 아니라 브로커
  동기화(재기동 시 이월 포지션 인식) 경로에서도 재현된다.
- **변경 파일**: `grep -n "PositionFallback" main.py`로 호출 지점 확인 후 특정(장후
  이월 — 지금은 코드 확인만 하고 변경하지 않는다).
- **회귀 위험**: 낮음(로깅·배수 계산 로직).
- **절대원칙 저촉 여부**: 없음.
- **런타임 주의**: py37_32, COM 콜백 밖.
- **검증 방법**: 다음 이월 포지션 발생 시 `entry_horizon` 필드가 채워지는지 확인.
- **섀도 계측 필요 여부**: 불필요.
- **적용 시점**: 장후 코드 확인 후 결정.
- **결정 필요 사항**: 없음(P1, 승인 불요).

### 적용 순서와 커밋 계획

장전 세션이므로 **오늘은 코드를 변경하지 않는다.** F-1은 주간회의 안건으로, F-2는 장후
코드 확인 항목으로 이월한다.

---

## 3. 고도화 방안

### G-1. 재기동 시 브로커 잔량 이상(FLAT 아님) 감지를 실시간 경보로 승격

- **관측 근거**: 오늘 08:41:09 `[BrokerSync] startup sync 완료: FLAT -> LONG 3계약`이
  CRITICAL 레벨로 이미 로그에 남았다. 이 시점부터 하드스톱 체결(08:45:11)까지 약 4분의
  창이 있었다 — 이 창 안에 사람이 알림을 받았다면 가격 상황을 보고 수동으로 더 빨리
  대응(즉시 시장가 청산 등)할 여지가 있었다.
- **제안 내용**: `[BrokerSync] startup sync 완료: FLAT -> {포지션}` 로그 지점에 09-01
  514차 F-C(외부 진입 실시간 경보, `main.py` 외부진입 동기화 지점)와 같은 방식의 경보
  훅을 추가 — 재기동 시 브로커 잔량이 FLAT이 아니면 즉시 경보.
- **기대 효과**: 오늘과 같은 상황에서 사람이 시스템 자동 손절(4분 뒤)보다 먼저 대응할
  기회를 얻는다.
- **비용·위험**: 낮음(알림만, 매매 경로 미변경). Slack 미사용(사용자 결정, 09-01
  DECISION_LOG 근거) — 경보 채널은 기존과 동일하게 대시보드 「경보」 탭.
- **선행 조건**: 없음(F-C와 같은 통로 재사용).
- **우선순위 제안**: 이번 주 — F-1(P0-2) 승인이 늦어질 경우의 임시 안전판 성격.

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| CB② 복원(`CB_CONSEC_STOP_LIMIT` 9999→2~3) | 재검토 기한 2026-08-29(초과 4일) | 이번 주 주간회의에서 반드시 재론 | CLAUDE.md 실전 전환 기준 ⑤, 초과 상태 방치 중 |

---

## 4. 진입 승패 사후검증 (장후 한정)

해당 없음(장후 전용) — 장전 세션.

## 5. 수익률 향상방안 (장후 한정)

해당 없음(장후 전용) — 장전 세션.

---

## 6. 다음 거래일 관측 예정 / 오늘 장중·장후 관측 예정

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| O-p1 | `ConfFloorGuard` 09:00:01 이후 축퇴 해소 여부 | `[Calibration] 축퇴 해소` 로그가 몇 분 안에 뒤따르는가 (기존 패턴은 개장 첫 분 한정) | 오늘 장중 |
| O-p2 | 1-1 손실 포지션의 발생원(등급·경로) | `trades`/`ensemble_decisions`에서 09-01 15:34:46 진입 또는 오늘 이월분의 `grade` 확인 (08:45 이후 DB 분석 금지 — 장후에만 가능) | 오늘 장후 |
| O-p3 | (판정 완료 — 위 1-3 참조) | — | 완료 |
| O-p4 | 사용자가 09-01 저녁 지시대로 포지션을 정리하려 시도했는지 | 사용자 확인(로그로는 판정 불가) | 사용자 응답 시 |

## 7. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| CB② 복원(`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | 2026-08-29 | **초과 4일** | 주간회의 재상정 필요(2026-08-01 결산에서 이미 1회 연기됨) |

---

## 8. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-09-02 (MW0601 51X차 — 장전 점검: 전일 미해소
  포지션 실현손실 -530만원)` — append 완료(아래 실행).
- `NEXT_TODO.md` 추가 체크박스: 4건(F-1 승인 촉구, F-2 코드 확인, O-p2 DB 확인, 사용자
  확인 요청) — append 완료(아래 실행).
- 완료 처리한 기존 항목: 09-01 `O-p3`(511차 Fix 라이브 확인) → 판정 완료로 표시.

## 이 절이 발급한 번호

이 절이 발급한 번호: 이상점 **1-1~1-4** · 관측 **O-p1~O-p4**(O-p3은 이 절에서 즉시 판정
완료).

---

## 사용자 조치 — 지금 확인이 필요한 것

| # | 할 일 | 배경 | 걸리는 시간 | 안 하면 생기는 일 |
|---|---|---|---|---|
| 1 | 어제(09-01) 저녁 "매수 3계약 즉시 매도" 요청을 실행하려 했는지, 왜 반영되지 않았는지 알려주세요 | 오늘 아침 그 포지션이 그대로 남아 있다가 자동 손절로 -530만 원이 확정됐습니다(이상점 1-1) | 대화 한 번 | 같은 경로(정체불명 매매 발생 → 정리 지연 → 손실)가 반복될 수 있음 — 원인을 모르면 재발 방지가 불가능합니다 |
| 2 | 어제 리포트의 "정체불명 매매 38건(-163만원)이 본인 것인지" 질문에 아직 답이 없습니다 | 오늘 손실 포지션도 같은 뿌리(원인불명 외부/MANUAL 진입, O-i4)일 가능성이 높습니다 | 1분 | 발생원을 못 찾으면 계속 미해결 상태로 남고, 다음에도 같은 방식으로 큰 손실이 날 수 있습니다 |
| 3 | 대신증권 계좌 상태를 한 번 더 직접 확인해 주세요(현재 시스템상으로는 포지션 없음/FLAT으로 확인됨) | 시스템 기록과 실제 계좌가 어긋난 전례(08-31)가 있습니다 | 2분 | 시스템이 놓친 잔여 포지션이 있으면 또 하루를 넘길 위험 |

---

# 장중(intra)

- 점검 시각: 12:26~12:30 KST · PC `MW0601` · 브랜치 `v9-dev` (정상)
- HEAD `a3f70ab` · 미커밋 518건(실질 변경 2건 — `dev_memory/DECISION_LOG.md`·`NEXT_TODO.md`뿐, 코드(.py) 변경 0건, 나머지는 CRLF/LF 표시 차이) · 당일 커밋 0건
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260902_intra.md`
- `.git/index.lock` 없음(수집기 자가점검: 이 수집 실행이 락을 만들지 않았다)
- 병행 세션 확인: 오늘(2026-09-02 00:00~) 커밋 0건, `docs/정기점검/매일점검/` 최신 파일은 이 세션이 방금 만든 증거 다이제스트뿐 — 병행 세션 없음
- ⚠ **이 절은 장중(12:26 KST) 시점 작성이다.** 정규장은 15:10(강제청산)·15:35(마감)까지 계속되므로, 이 절이 다루는 것은 09:00~12:27 구간뿐이고 나머지는 장후 절이 다룬다.

## 0i-C. 이월 처리 — 장전 절 항목 처분

| 원 번호 | 제목(줄임) | 원 국면 | 처분 | 근거 |
|---|---|---|---|---|
| 1-1 | 전일 미해소 매수 3계약, 오늘 08:45 자동손절 -530만원 확정 | 장전 | ✅ 해소(사건 자체는 종결 — 추가 위험 없음) | 손실은 이미 확정됐고 되돌릴 수 없다. `data/position_state.json` 재확인(12:26 기준 `status: FLAT`) — 이후 재발 없음. 단 **발생원(O-p2)은 여전히 미확정**이라 "왜 이런 일이 생겼는가"는 장후로 이월 |
| 1-2 | `PositionFallback`(entry_horizon 미설정) | 장전 | 🔄 지속(단, 원인 범위가 좁혀졌다) | 아래 §1i-2 참조 — 오늘 정상 엔진 진입 2건(10:19·10:49)은 `horizon=1m`/`horizon=3m`이 각각 정확히 찍혔다. **결함은 브로커 동기화(이월 포지션 인식) 경로에만 있고 엔진 자체 진입 경로는 정상**임을 장중 관측이 추가로 확인했다 — F-2 범위를 "브로커 동기화 경로 한정"으로 좁힐 근거 |
| 1-3 | 511차 Fix(청산 주문 거부 대응) 첫 라이브 확인 | 장전 | ✅ 완료(그대로 유지) | 오늘 장중 신규 청산 4건(TP1 부분청산 2 + 하드스톱 2)도 거부·재시도 로그 0건 — 계속 정상 |
| 1-4 | 증거 다이제스트 정합성 불일치(귀속 실패 레그 3행) | 장전 | 🔄 지속(동일 원인 재확인, 신규 아님) | 장중 증거 §5도 같은 이유로 "레그합 -5,263,654 = 포지션합 +36,014 → 불일치"를 찍었다 — **원인은 1-1과 동일**(이월 포지션에 진입 로그가 없음). 오늘 새로 생긴 불일치가 아니라 하루 종일 누적 로그가 그 사건 하나를 계속 반영하는 것뿐이다 |
| O-p1 | `ConfFloorGuard` 축퇴 해소 여부 | 장전 | **판정 완료 — 과거 패턴에서 벗어났다** | 아래 §1i-1 참조. "개장 첫 분만 그렇다"던 과거 패턴과 달리 오늘은 09:00~10:49(1시간49분)·10:55~수집시점(1시간31분 이상) 두 구간에 걸쳐 총 3시간 넘게 자동진입 하한 미달 상태가 이어졌다 |
| O-p2 | 오늘 손실 포지션(1-1)의 발생원(등급·경로) | 장전 | 판정 보류(사유: 장중 라이브 DB 분석 금지 — CLAUDE.md) | `trades`/`ensemble_decisions` 조회는 08:45 이후 정규장 중 금지 구간이라 이 절에서 할 수 없다. **장후로 이월** |
| O-p3 | 511차 Fix 라이브 동작 확인 | 장전 | (이미 판정 완료 — 변경 없음) | 위 1-3과 동일 |
| O-p4 | 사용자가 09-01 저녁 지시대로 포지션을 정리하려 시도했는지 | 장전 | 판정 보류(사유: 사용자 응답 대기) | 시스템 로그로는 판정 불가. 사용자 응답 없으면 장후에도 미해결로 이월 |

**미처분 0건.**

## 0i. 오늘 한눈에 (장중 갱신)

지금까지(오전 9시부터 낮 12시반까지) 시스템은 문제없이 돌아가고 있습니다. 오늘 아침
확정된 큰 손실(약 530만 원, 어제부터 이어진 사건) 이후로는 작은 매매 두 건만 있었고
결과는 이익입니다(합계 약 3만6천 원). 지금 이 순간 보유 중인 포지션은 없습니다.

다만 두 가지를 계속 지켜봐야 합니다. 첫째, 프로그램이 잠깐(약 57초) 완전히 멈춘 순간이
한 번 있었습니다 — 다행히 그때는 포지션이 없어서 실제 손해로 이어지지는 않았지만, 최근
2주 중 가장 길게 멈춘 기록입니다. 둘째, 프로그램이 자동으로 진입하기 위한 "확신도" 기준을
오전 내내 거의 통과하지 못했습니다 — 오늘 시장 자체가 방향성이 약한 날이라 그런 것인지,
프로그램의 판단 능력에 문제가 생긴 것인지는 아직 구분되지 않았습니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 이상(P0) | 전일 미해소 포지션 자동 손절 -5,299,668원(자본대비 -10.60%) 확정. 개장 준비 절차는 정상 |
| 장중 | **조건부 정상** | 09:00~12:27 구간 매매·설계 절차 자체는 정상(체크리스트·청산·CB 이상 없음). 다만 (a) 57초 메인 스레드 정지 1건(최근 2주 최대) (b) 자동진입 확신도 기준 미달 상태가 예상보다 훨씬 길게 지속 — 둘 다 즉각적 손실로 이어지지는 않았으나 관찰 필요 |
| 장후 | (진행 전) | |

**절대원칙 준수 여부(장중 시점, 09:00~12:27 구간)**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 오버나이트 금지(15:10 강제청산) | 해당 없음(아직 15:10 전) | 장후에 최종 판정 |
| ② | Circuit Breaker 5종 | 준수 | CB② 카운터 2건 발생(정지는 안 함 — `CB_CONSEC_STOP_LIMIT=9999` 한시예외, 설계대로). CB①③④⑤ 발동 0건 |
| ③ | CORE 피처 호라이즌 그룹별 | 준수 | 오늘 진입 2건 모두 체크리스트 미통과 항목에 `vwap` 없음(강제 X 대상 아님) — §1i-3 |
| ④ | COM 콜백 dynamicCall·emit 금지 | 준수 | `0xC0000409`·STACK_BUFFER 로그 0건 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 구분 | 해당 없음(코드 정합성 점검, 로그로 직접 관측 불가) | — |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 준수 | 챌린저·자동통합 관련 로그 0건 |

**설정 불변식** — 수집기 §3 표 26행 전부 `일치`. 불일치 0건(장전과 동일).

---

## 1i. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

없음(장중 구간 신규 P0 없음).

### P1 — 정확성·관측 훼손

#### 1-5. 메인 스레드 57초(56,875ms) 완전 정지 — 최근 2주 관측 최대치

- **증상**: 11:28:57에 프로그램의 메인 스레드(화면·주문·데이터 수신을 처리하는 중심
  실행 흐름)가 56.875초 동안 완전히 멈췄다. 그동안 신규 데이터 처리·화면 갱신·주문
  처리가 전부 정지됐다. 프로그램 자체가 죽거나 재시작하지는 않았고, 멈췄던 시간이
  지나자 다시 정상적으로 움직이기 시작했다.
- **근거(로그 인용 + 시각)**:
  ```
  2026-09-02 11:28:57 [ERROR] SYSTEM: [LiveDBG] _tick_header 간격 56875ms — 메인 스레드
  블로킹 발생 | pipe_elapsed=0 watchdog_alerted=[] | [MainStall] stall_ms=56875 band=ALERT
  since_pipe_s=0.1
  ```
  스택 스냅샷(`logs/mainstall_traceback_20260902.log` 84행): 메인 스레드가
  `collection/cybos/api_connector.py:1595 _pump_messages`(Cybos COM 메시지 펌프) 안에서
  멈춰 있었다 — Python 코드 레벨이 아니라 Windows COM API 호출 대기로 추정된다.
  `logs/20260902_WARN.log` · `logs/mainstall_traceback_20260902.log`.
- **기준 위반**: 계측 4원칙과 직접 충돌하지는 않으나, CLAUDE.md 절대원칙 §2 CB⑤(API 지연
  5초 초과 → 즉시 청산)가 재는 것은 **파이프라인(STEP0~8) 경과시간**이고 이 정지는
  **메인 스레드 전체 정지시간**이라 단위가 다르다 — CB⑤도 FZ-1 워치독(180초 기준)도 이
  구간을 못 본다(482차 F-3가 이미 이 사각지대를 2주간 섀도 관찰 중).
- **영향**: 이 57초 동안 포지션이 있었다면 가격이 급변해도 프로그램이 전혀 반응하지
  못했을 것입니다. 다행히 이번엔 보유 포지션이 없어(직전 청산 10:51, 다음 진입 없음)
  실제 손해로 이어지지 않았습니다. 다만 포지션이 있는 순간에 이런 정지가 나면 위험할
  수 있습니다.
- **영향(기술)**: 최근 2주 장중 최대 메인 스레드 정지 기록과 비교하면 오늘이 압도적으로
  크다 — 08-20 4,750ms / 08-21 9,922ms / 08-24 20,985ms(종전 최대) / 08-25 8,625ms /
  08-26 8,141ms / 08-27 12,500ms / 08-31 7,718ms / 09-01 6,250ms → **09-02 56,875ms
  (종전 최대 대비 2.7배)**. 표본 8거래일 — 확정 결론 금지, 다만 이례적 이상치로 등록.
  포지션 FLAT 구간(마지막 청산 10:51:02, 이 정지 11:28:57)이라 실손해는 0.
- **신규 여부**: 신규(오늘 최초 관측). 482차 F-3 섀도 계측(`MAIN_THREAD_STALL_*`) 대상
  구간의 새 데이터 포인트로 등록.

### P2 — 운영 부담·기술부채

#### 1-6. `ConfFloorGuard`(자동진입 확신도 하한 미달) — 과거 "개장 첫 분 한정" 패턴과 다르게 장시간 지속

- **증상**: 자동진입을 위해서는 신뢰도 보정기 출력이 최소 확신도(`min_conf`)를 넘어야
  하는데, 그 상한 자체가 min_conf에 못 미치는 상태가 오전 내내 대부분 이어졌다. 09:00:01
  에 처음 걸렸고, 10:49:00에 잠깐(약 6분) 풀렸다가 10:55:00에 다시 걸렸다. 이후 이
  증거 수집 시점(12:26)까지 다시 풀렸다는 로그가 없다 — 즉 최소 09:00~10:49(1시간49분)
  + 10:55~12:26(1시간31분) 두 구간, 총 3시간이 넘는 시간 동안 자동진입 하한을 넘을 수
  있는 신호가 구조적으로 나올 수 없는 상태였다.
- **근거(로그 인용 + 시각)**:
  ```
  2026-09-02 09:00:01 [WARNING] SIGNAL: [ConfFloorGuard] 자동진입 하한 도달 불가 —
  보정기 출력상한 0.3479 < 필요 0.4240 (conf_floor=0.330, min_conf=0.424, span=0.0063)
  2026-09-02 10:49:00 [INFO]    SIGNAL: [ConfFloorGuard] 하한 도달 가능 복구 —
  출력상한 0.3807 ≥ 필요 0.3720
  2026-09-02 10:55:00 [WARNING] SIGNAL: [ConfFloorGuard] 자동진입 하한 도달 불가 —
  보정기 출력상한 0.3715 < 필요 0.3720 (conf_floor=0.330, min_conf=0.372, span=0.0135)
  ```
  `logs/20260902_SIGNAL.log`. 정황 증거(같은 원인일 가능성): 오늘 레짐은 09:00~12:27
  전 구간 `regime=NEUTRAL` 207/207 — 방향성 자체가 약한 하루였을 가능성. 오늘 신호
  평균 확신도(`conf=`) 501개 표본 평균 **40.6%**. 차단 사유 44건 중 28건(63.6%)이
  "등급X — `2_confidence` 미통과" 단일 사유로 압도적 1위(§1i-3 참조) — ConfFloorGuard와
  같은 근본 원인(낮은 확신도)을 가리키는 정황이 겹친다.
- **기준 위반**: 없음(ConfFloorGuard는 설계대로 동작 — 확신도가 낮을 때 진입을 막는 것이
  그 목적이다). 다만 09-01 이전 리포트가 특성화해 둔 "개장 첫 분 워밍업, 이후 축퇴
  해소로 복귀. 상시 아님"이라는 과거 패턴과는 지속 시간이 명백히 다르다.
- **영향**: 오전 내내 프로그램이 스스로 확신을 못 가져서 자동매매를 거의 하지 않은
  것으로 보입니다. 오늘 실제로 매매가 2건뿐이었던 것도 이 때문일 가능성이 높습니다.
  이것이 "오늘 시장 자체가 방향 없이 조용했다"는 정상적인 이유 때문인지, 판단 프로그램
  쪽에 문제가 생긴 것인지는 로그만으로는 확정할 수 없습니다(원인 규명에는 예측 데이터
  베이스 조회가 필요한데, 장중에는 조회가 금지돼 있습니다).
- **신규 여부**: 신규 관측(패턴 이탈). 09-01 이전 리포트의 "상시 아님" 서술은 무효화하지
  않는다 — 표본이 1거래일뿐이라 "상시로 바뀌었다"고 단정하지 않는다(313차 원칙, 확정
  결론 금지).

### 확인 필요 (확정 아님)

- **1-6의 원인**: 시장 자체의 저신호 국면(NEUTRAL 레짐 지속)인지, 보정기(Calibration)
  쪽 결함인지 아직 구분되지 않는다. **장후 확인** — `predictions` 테이블에서 오늘
  보정기 출력 분포를 최근 5거래일과 비교.
- **O-p2 이월**: 1-1 손실 포지션의 발생원. 08:45 이후 라이브 DB 분석 금지 구간이라
  장중에도 확인 불가 — **장후 필수 확인** (재이월).
- **O-p4 이월**: 사용자가 09-01 저녁 지시대로 포지션 정리를 시도했는지. 사용자 응답
  대기 — 미응답 시 장후에도 이월.

### 이미 반영된 사안 (신규 아님)

- **사이저 최대 3계약 → 실제 진입 최대 2계약(오늘 §5)**: 실전 전환 기준 ⑧의
  `sizing_inversion_watch` 채널이 이미 추적 중인 알려진 패턴(417차·431차)이다.
  오늘 배수 조합은 `conf=0.6 regime=0.8 safe=1.00`로 고정 — 신규 안건 아님.
- **`Calibration` 축퇴 감지/해소 반복(105건/101건)**: 기존에 이미 알려진 정상 동작
  패턴(개장 직후 보정기 워밍업 반복) — 08-19 등 과거 리포트에 동일 패턴 기록.
  신규 안건 아님.
- **`ConstOut`(3m 상수 출력) 2회(09:35·11:59) → GBM 재학습 2회 이벤트 구동**: CLAUDE.md
  절대원칙 §매분 9단계 STEP3 정정 주석(483차)대로 이벤트 구동이 정상 — "30분마다"가
  아니다. 재학습 2건 모두 `returncode` 이상 없이 완료(09:36:22·12:00:23, 각 18~19초).
  신규 안건 아님.

---

## 2i. Fix 작업 구현계획

장중 세션이므로 **오늘은 코드를 변경하지 않는다.** 신규 Fix 없음 — 09-01·09-02 장전이
이미 낸 F-1(P0-2, daily_close 잔여 포지션 강제청산)·F-2(entry_horizon 미전달)는 위
이월표에서 처분했을 뿐 계획 내용 변경 없음.

### 적용 순서와 커밋 계획

장중이므로 계획 없음(신규 Fix 없음). 기존 F-1·F-2는 장후 세션이 이어받는다.

---

## 3i. 고도화 방안

### G-2. 메인 스레드 정지 실시간 경보(임계값 초과 시)

- **관측 근거**: 오늘 56.9초 정지(1-5)가 최근 2주 최대치를 2.7배 경신했는데, 이 정보는
  다음 정기점검(장중/장후)에서만 드러난다 — 정지가 발생한 그 시점에는 아무도 알지
  못한다.
- **제안 내용**: `[MainStall]` 로그의 `band=ALERT`(현재 임계 기준 초과) 판정 지점에
  경보 훅을 추가 — 09-01 514차 F-C(외부 진입 실시간 경보)와 같은 방식으로 대시보드
  「경보」 탭에 표시.
- **기대 효과**: 포지션 보유 중에 이런 정지가 나면 사람이 더 빨리 인지하고 대응(수동
  개입 등)할 수 있다.
- **비용·위험**: 낮음(알림만, 매매 경로 미변경). 482차 F-3 섀도 계측 인프라 재사용 가능.
- **선행 조건**: 없음.
- **우선순위 제안**: 26주 WFA 주기(482차 F-3 섀도 관찰 종료 시점)에 맞춰 검토 — 지금은
  표본 1건뿐이라 임계값을 정할 근거가 부족하다.

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| CB② 복원(`CB_CONSEC_STOP_LIMIT` 9999→2~3) | 재검토 기한 2026-08-29(초과 4일, 장전과 동일) | 이번 주 주간회의에서 반드시 재론(장전과 동일 권고 유지) | CLAUDE.md 실전 전환 기준 ⑤ |

---

## 4i. 진입 승패 사후검증 (장후 한정)

해당 없음(장후 전용) — 장중 세션.

## 5i. 수익률 향상방안 (장후 한정)

해당 없음(장후 전용) — 장중 세션.

---

## 6i. 다음 거래일 관측 예정 / 오늘 장후 관측 예정

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| O-i1 | 1-6(ConfFloorGuard 장기지속)의 원인 — 시장 저신호 vs 보정기 결함 | `predictions` 테이블 보정기 출력 분포를 최근 5거래일과 비교(장후 DB 조회 가능 시점에) | 오늘 장후 |
| O-i2 | 1-5(메인 스레드 57초 정지) 재발 여부 및 규모 추이 | 482차 F-3 섀도 계측 누적치 대조, 다음 정지가 5초 초과·30초 초과 중 어디에 속하는가 | 다음 거래일 이후 누적 관찰(26주 주기) |
| O-p2(재이월) | 1-1 손실 포지션 발생원(등급·경로) | `trades`/`ensemble_decisions`에서 09-01 15:34:46 진입의 `grade` 확인 | 오늘 장후 |
| O-p4(재이월) | 사용자가 09-01 저녁 지시대로 포지션을 정리하려 시도했는지 | 사용자 확인(로그로는 판정 불가) | 사용자 응답 시 |

## 7i. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| CB② 복원(`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | 2026-08-29 | **초과 4일**(장전과 동일 — 오늘 안에 날짜 변화 없음) | 주간회의 재상정 필요 |

---

## 8i. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-09-02 (MW0601 516차 — 장중 점검: 메인 스레드
  57초 정지·ConfFloorGuard 장기지속 관측)` — append 완료(아래 실행).
- `NEXT_TODO.md` 추가 체크박스: 4건(O-i1·O-i2 신규 관측, O-p2·O-p4 재이월) — append
  완료(아래 실행).
- 완료 처리한 기존 항목: O-p1(ConfFloorGuard 판정 완료 — 패턴 이탈로 판정), O-p3(변경
  없음, 계속 판정 완료 유지).

## 이 절이 발급한 번호

이 절이 발급한 번호: 이상점 **1-5~1-6** · 관측 **O-i1~O-i2**(O-p1은 이 절에서 판정
완료로 처분, O-p2·O-p4는 재이월).

---

## 사용자 조치 — 장중 갱신 (기존 항목 유지 + 신규 없음)

장전 절의 「사용자 조치」 3항목은 아직 응답이 없어 **그대로 유효**하다(위 이월 처리표
O-p4 참조). 장중 세션에서 새로 추가되는 사용자 조치는 없다 — 오늘 낮 12시반 기준으로
사람이 지금 당장 판단해야 할 새로운 사안은 없다(⚠ 즉시판단 대상 없음).

---

# 장후(post)

- 점검 시각: 16:17~17:40 KST · PC `MW0601` · 브랜치 `v9-dev` (정상)
- HEAD `a3f70ab` · 미커밋 519건(실질 변경 미측정 — `git diff` 실패, git 호출 규약대로
  `--no-optional-locks` 사용) · 당일 커밋 0건 · **`.git/index.lock` 없음**(수집기 자가점검:
  이 수집 실행이 락을 만들지 않았다)
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260902_post.md`
- 병행 세션 확인: 오늘(2026-09-02 00:00~) 커밋 0건. `docs/정기점검/매일점검/` 최신 파일은
  이 세션이 만든 오늘자 증거 다이제스트뿐 — **딥다이브·특별조사 파일 없음, 병행 세션 없음**
- 이 절은 **EOD(15:40 일일 마감) + P8(15:45~ 스케일러 재적합)** 을 다 포함해 하루를
  종합한다. 이 시각(15:35 이후)은 라이브 DB 분석 금지 구간이 아니므로
  `data/db/trades.db`·`data/db/predictions.db` 를 직접 조회했다(읽기 전용).

## 0p-C. 이월 처리 — 장전·장중 절 항목 처분

| 원 번호 | 제목(줄임) | 원 국면 | 처분 | 근거 |
|---|---|---|---|---|
| 1-1 | 전일 미해소 매수 3계약, 08:45 자동손절 -530만원 | 장전 | ✅ 해소(유지) | 장중에 이미 사건 종결 처리됨. 오늘 DB 조회로 발생원(O-p2)까지 추가 확인 완료(아래) |
| 1-2 | `PositionFallback`(entry_horizon 미설정) | 장전→장중 | ↩️ **정정 — 더 큰 문제로 재정의** | 장중은 "브로커 동기화 경로 한정"으로 범위를 좁혔으나, 오늘 DB 조회 결과 이 결함은 **`entry_horizon` 하나만의 문제가 아니라 `entry_source` 필드 자체가 세션 내내 고착되는 더 큰 결함**의 일부였다(신규 1-9 참조). 1-2 자체는 여기서 닫고 1-9로 승계한다 |
| 1-3 | 511차 Fix 첫 라이브 확인 | 장전 | ✅ 완료(유지) | 오늘 전체(6개 청산 레그) 거부·재시도 로그 0건 유지 |
| 1-4 | 증거 다이제스트 정합성 불일치(귀속 실패 레그) | 장전→장중 | ✅ **해소 — 도구 한계로 재규정, 데이터 손실 아님** | `trades.db` 직접 조회 결과 해당 포지션(id=468)은 손익 -5,299,668원으로 **정확히 기록되어 있다**. 다이제스트가 "불일치"로 표시한 이유는 로그 전용 조립 로직이 "오늘자 진입 로그가 없는 이월 포지션"을 못 묶기 때문 — 데이터가 없는 게 아니라 **집계 도구의 설계 범위 밖**이다. 고도화 방안으로 하단 G-4 등록 |
| 1-5 | 메인 스레드 57초(56,875ms) 정지 | 장중 | 🔄 **지속 — 12:26 이후 추가 사례 1건** | 14:12:06에 6,875ms 정지가 추가로 발생(오늘 두 번째로 큰 값). 26주 WFA 주기 누적 관찰 대상(482차 F-3)에 새 데이터 포인트로 반영. 오늘 5초 초과 총 7건, 최대 56,875ms — 변경 없음 |
| 1-6 | `ConfFloorGuard` 장기지속 원인 미확정 | 장중 | ✅ **판정(가설, 확정 아님) — 시장 저신호 쪽에 무게** | 아래 §1p-3 참조. `weight_collapsed` 제외 평균 확신도가 최근 4거래일과 사실상 동일(0.3705 vs 0.3648~0.3698) — 313차 원칙상 1일 대조로 확정하지 않는다 |
| O-p1 | (판정 완료, 08-31 이전 항목 아님 — 장중에서 이미 판정) | 장중 | 변경 없음 | — |
| O-p2 | 1-1 손실 포지션 발생원(등급·경로) | 장전→장중 | ✅ **판정 완료 — DB 확인** | `ensemble_decisions`에서 09-01 15:20:00~15:40:00 구간 전수 조회 결과, **그날 마지막 판단 행은 15:08:00이고 그 이후 행이 전혀 없다.** 진입 시각(15:34:46)에 대응하는 시스템 판단 행이 **0건** — 정상 앙상블 경로로 생성된 진입이 아니다. `trades.id=468`의 `grade='BROKER'`(합성값)·`entry_horizon=''`·`checklist_pass_count=NULL`도 이를 뒷받침한다. **외부/정체불명 기원 가설이 강화됐고, 시스템 자체 버그(숨은 자동진입 경로)라는 가설은 배제됐다** — 단 "누가·어떻게 주문을 냈는가"는 여전히 미확정(사용자 확인 필요) |
| O-p3 | 511차 Fix 라이브 확인 | 장전 | 변경 없음(완료 유지) | — |
| O-p4 | 사용자가 09-01 저녁 지시대로 포지션을 정리하려 시도했는지 | 장전→장중 | 🔄 **지속 — 응답 대기** | 사용자 응답 없음. 사용자 조치 절에 재게재 |
| O-i1 | `ConfFloorGuard` 원인(시장 vs 보정기) | 장중 | ✅ 판정(가설) — 위 1-6과 동일 | — |
| O-i2 | 메인 스레드 정지 재발 여부 | 장중 | 🔄 지속 — 위 1-5 참조 | — |

**미처분 0건.**

## 0p. 오늘 한눈에 (최종)

오늘 하루를 한 문장으로 말하면 이렇습니다 — **어제 생긴 큰 손실이 오늘 아침 확정됐고,
그 뒤로는 프로그램이 대체로 정상적으로 작동했지만, 마감 정산 과정에서 "돈이 맞아떨어지지
않는" 문제가 새로 발견됐습니다.**

먼저 손익부터 말씀드리면, 오늘 계좌에서 실제로 빠져나간 돈(증권사가 계산한 최종 금액)은
약 **556만 원**입니다. 이 중 530만 원은 어제 저녁부터 이어진 미해결 매수 포지션이 오늘
아침 8시 45분에 자동으로 손절 처리되면서 확정된 손실이고(장전 리포트에서 이미 말씀드린
내용입니다), 나머지는 오늘 낮 동안 프로그램이 정상적으로 진행한 세 번의 매매에서 생긴
작은 손익들을 합친 것입니다(그 세 번의 매매만 놓고 보면 결과가 엇갈렸습니다 — 계약
수량 기준으로는 이익, 수수료까지 뺀 실제 손에 남는 돈 기준으로는 손실이었습니다).

여기서 프로그램 내부적으로 계산한 손실 금액(약 527만 원)과 증권사가 실제로 계산한
손실 금액(약 556만 원) 사이에 약 30만 원의 차이가 있다는 것이 오늘 새로 발견된
문제입니다. 이 차이는 마감 시점에 시스템이 스스로 감지해서 경고를 띄웠습니다 — 즉
"뭔가 이상하다"는 것을 시스템이 이미 알고 있었다는 뜻입니다. 원인은 아직 확실하지
않지만, 지난주 금요일(8월 31일)에도 비슷한 종류의 불일치(그때는 약 9만 원)가 있었고
오늘 다시 나타난 것이라, 완전히 새로운 문제라기보다는 **이미 알고 있던 문제가 더 큰
규모로 재발**한 것에 가깝습니다. 실제 사고인지, 계산 방식의 사소한 차이인지는 아직
단정할 수 없습니다.

한 가지 더, 이 점검 자체를 진행하는 과정에서 컴퓨터 관리 파일(버전 관리용 잠금
파일) 하나가 남는 작은 부작용이 있었습니다. 매매나 손익과는 전혀 무관하지만, 사용자가
직접 파일 하나를 지워줘야 다음 작업이 매끄럽게 됩니다(맨 아래 사용자 조치 5번).

그 외에는, 낮 동안 컴퓨터가 한 번 57초 동안 완전히 멈춘 일이 있었지만(장중 리포트에서
말씀드린 내용) 그때 포지션이 없어서 실제 피해는 없었고, 오늘 마감(오후 3시 40분)과
그 이후 자동 재학습도 모두 정상적으로 완료됐습니다. 오후 3시 10분 기준으로 남아있는
포지션도 없어서, "장을 마치기 전에 반드시 포지션을 정리한다"는 원칙도 오늘 자체
거래분에 한해서는 잘 지켜졌습니다.

**요약하면: 오늘의 큰 손실(530만 원)은 어제 문제의 결과이지 오늘 새로 생긴 문제가
아니며, 오늘 새로 발견된 것은 "마감 정산 금액이 30만 원 어긋난다"는 회계상의 이상
신호입니다. 지금 당장 위험한 상태는 아니지만, 확인이 필요합니다.**

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 이상(P0) | 전일 미해소 포지션 자동 손절 -5,299,668원(자본대비 -10.60%) 확정. 개장 준비 절차는 정상 |
| 장중 | 조건부 정상 | 09:00~12:27 구간 매매·설계 절차 정상. 메인 스레드 57초 정지(포지션 없어 무해) · 확신도 하한 장기 미달 관찰 |
| 장후 | **조건부 정상(신규 이상 1건 P1)** | 15:10 강제청산·EOD·P8 전부 정상 이행. 다만 마감 정산에서 브로커 실측 net과 엔진 계산 net이 허용범위를 넘어 불일치(NetRecon MISMATCH, 08-31에 이어 2번째 사례) — 정확성 문제이지 오늘 거래 자체의 위험은 아님 |

**절대원칙 준수 여부(장후 최종 시점)**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 오버나이트 금지(15:10 강제청산) | 🔴 **위반(어제 발생분) — 오늘 자체 거래는 준수** | 어제(09-01) 15:34:46 진입분이 밤새 방치된 것이 원위반. 오늘은 13:35:34 이후 포지션 없음, 15:11:08 `[SchedForceExit]` "청산 대상 없음(정상)" 확인 |
| ② | Circuit Breaker 5종 | 부분 준수(한시예외 지속) | CB② 카운터(연속손절 300초창) 2건 발생, `CB_CONSEC_STOP_LIMIT=9999`라 정지 없음(설계대로). CB①③④⑤ 발동 0건. CB③ 판정가능시간 65분/370분(18%) |
| ③ | CORE 피처 호라이즌 그룹별 | 준수(단기 그룹만 해당) | 오늘 진입 3건 전부 1m/3m — 중기·장기 그룹은 474차 확정대로 구조적 도달 불가(설계상 정상) |
| ④ | COM 콜백 dynamicCall·emit 금지 | 준수 | `0xC0000409` 로그 0건 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 구분 | 해당 없음(코드 정합성, 로그로 직접 관측 불가) | — |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 준수 | 관련 로그 0건 |

**설정 불변식** — 수집기 §3 표 26행 전부 `일치`. 불일치 0건(하루 종일 동일).

---

## 1p. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

없음(장후 구간 신규 P0 없음 — 1-1은 장전 발생분으로 이미 처분 완료).

### P1 — 정확성·관측 훼손

#### 1-7. (지속·재현) 하드스톱 분할체결이 `partial_1_done`을 세워 오늘도 손실 1건이 `TRAIL_AFTER_TP1`로 오분류 — F-10 대상 6번째 사례

- **증상**: 08:45 하드스톱으로 청산된 어제 이월 포지션(3계약, id=468)의 `exit_stage`가
  `TRAIL_AFTER_TP1`(TP1 이후 보호 트레일=이익 확정 청산이라는 뜻)로 기록됐다. 그런데
  이 포지션에는 **TP1 부분청산 이벤트 자체가 오늘 하루 동안 한 번도 없었다** — 하드스톱
  주문 하나가 거래소에서 1계약씩 3번(1/3, 2/3, 3/3)에 걸쳐 체결됐을 뿐이다. 시스템은 첫
  체결(1계약)만으로 "부분청산 목표 수량에 도달했다"고 오판해 내부 플래그를 세웠고,
  마지막 체결에서 그 플래그를 보고 라벨을 잘못 붙였다.
- **근거(로그 인용 + 시각 + DB)**:
  ```
  2026-09-02 08:45:10 [WARNING] SYSTEM: [ExitFillFlow] fill_price=1040.74 fill_qty=1 (1/3)
  2026-09-02 08:45:10 [WARNING] SYSTEM: [ExitFillFlow] fill_price=1040.98 fill_qty=1 (2/3)
  2026-09-02 08:45:11 [WARNING] SYSTEM: [ExitFillFlow] fill_price=1040.92 fill_qty=1 (3/3, after='FLAT')
  2026-09-02 15:40:09 [WARNING] SYSTEM: [ExitStageRecon] 오늘 TRAIL_AFTER_TP1 4레그 / 4포지션 중
    TP 이벤트 대응 2 · 단일계약 보호전환(설계) 1 · 미대응 1 ⚠ 미대응 합계 -5,299,668원
    (진입 08:41:09). 이 레그들은 라벨상 「TP1 뒤 트레일(이익 청산)」이지만 그 포지션에
    TP가 난 적이 없다 — 손절률 분모·분자가 오염된다
  ```
  `logs/20260902_WARN.log` · `data/db/trades.db: trades.id=468 (exit_stage='TRAIL_AFTER_TP1',
  exit_reason='하드스톱(틱)', pnl_krw=-5299668)`.
  **이 진단은 시스템이 스스로 낸 것이다** — 사람이 DB를 파서 찾은 게 아니라, 07차
  이후 이미 배선된 `[ExitStageRecon]` 섀도 로그(G-5, 507차)가 매일 자동으로 계산해
  마감 로그에 찍는다.
- **기준 위반**: 계측 4원칙 ①(단위 명시)·postmortem.md §3-1(승패 판정을 `exit_reason`
  만으로 세지 말 것). **이미 2026-08-31에 동일 결함이 1-19(P1)로 등록돼 있다**
  (원인: `strategy/position/position_tracker.py:_sync_partial_progress()`가 "닫힌 수량이
  목표치에 도달했는가"만 보고 "왜 닫혔는가"는 안 봄. `main.py`가 EXIT_FULL 주문의 중간
  체결에도 `apply_exit_fill()`을 그대로 호출해 이 함수가 매번 도는 구조). **오늘은 신규
  발견이 아니라 이미 계획(F-10)까지 나온 결함의 재현이다** — 함정① 확인 완료.
- **영향**: 오늘 하루로는 실제 손익에 영향이 없습니다(라벨이 잘못 붙어도 이미 확정된
  손실 금액 자체는 정확합니다). 다만 이 라벨은 "손절이 잘 지켜지고 있는가"를 판단하는
  통계에 쓰이는데, 진짜 손절이 "이익 보호" 것처럼 위장되면 그 통계가 실제보다 좋아
  보이게 됩니다.
- **영향(기술)**: 08-31 5건(-6,977,391원) + 오늘 1건(-5,299,668원) = **누적 6포지션·2거래일
  -12,277,059원이 `TRAIL_AFTER_TP1`로 오분류된 상태로 방치돼 있다.** 이 라벨은
  postmortem.md §3-1의 승패 판정 1순위 기준이고, 417차가 발견한 유일하게 유의했던
  요인(손절폭 초과율)의 입력이기도 하다. 사전등록 승격 기준(`min_samples=` 실질은
  "10거래일 중 3일 이상 발생")까지는 아직 1일이 더 필요하다 — **누적 대장 P5-06 갱신**
  (오늘 세션에서 완료, 아래 제5부).
- **신규 여부**: **신규 아님 — 1-19(2026-08-31, F-10 계획)의 재현·확대.** F-10은
  여전히 "계획만" 상태이며 오늘 구현하지 않는다(장후 세션 코드 변경 원칙과 무관하게,
  F-10 자체가 아직 승인 대기 상태).

#### 1-8. (지속·재현) 마감 net 대사 2번째 불일치 — 잔차 +298,643원(허용 ±13,289), 지배축은 gross

- **증상**: 15:40:09 마감 시점에 시스템이 스스로 계산한 손익(엔진 net)과 증권사가
  실제로 집계한 손익(브로커 net, 예탁금 차이 기준)을 대조했더니 허용 오차를 크게
  넘는 차이가 났다. 시스템은 이 사실을 **ERROR 레벨로 자동 감지·기록**했다.
- **근거(로그 인용 + 시각)**:
  ```
  2026-09-02 15:40:09 [ERROR] SYSTEM: [NetRecon] 🔴 net 불일치 — 엔진 -5,265,803원 vs
    브로커 -5,564,446원 (잔차 +298,643원, 허용 ±13,289)
             ├ gross 축  : 엔진 -5,183,000 vs 브로커 -5,498,000 → 기여 +315,000 (105%)
             └ 수수료 축 : 엔진 82,803 vs 브로커 66,446 → 기여 -16,357 (5%, 배수 0.80)
             ⇒ 지배 축 = gross. 수수료율 재보정 전에 체결 대사를 먼저 볼 것
  2026-09-02 15:40:08 [WARNING] SYSTEM: [BrokerPnl] gross 불일치 — broker -5,498,000원
    [TR수신 2026-09-02 13:35:36] vs engine -5,183,000원 (차 -315,000원). 체결 누락 또는
    브로커 미정산 가능 — 확인 필요
  ```
  `logs/20260902_WARN.log` · `data/db/trades.db: daily_broker_pnl`(date='2026-09-02':
  `pnl_krw=-5498000, commission_krw=82803, pnl_net_krw=-5265803, deposit_cash_krw=42172727,
  next_day_deposit_cash_krw=36608281, broker_net_krw=-5564446, broker_net_source='live',
  deposit_snapshot_ts='2026-09-02 13:35:36'`).
  DB 직접 검증: `trades` 테이블 오늘 6개 레그 합계 gross=-5,183,000 / net=-5,265,804 —
  **엔진 쪽 숫자는 `trades` 테이블과 정확히 일치한다(엔진 계산 자체는 내부적으로
  일관됨).** 차이는 오로지 "엔진이 아는 거래 내역"과 "증권사가 실제로 정산한 금액"
  사이에서 난다.
- **기준 위반**: 계측 4원칙 ⑤(대사는 모든 축을 걸어라). **이미 2026-08-31에
  1-24(P1·확인 필요)로 등록된 것과 같은 패턴의 재발**이다(그날은 잔차 -91,461원,
  지배축도 gross 77%). F-14(대사 로그를 축 분해 형태로 바꾸는 것)는 이미 배포돼
  **정상 작동 중**임을 오늘 확인했다(위 인용의 `├ gross 축 / └ 수수료 축 / ⇒ 지배 축`
  3줄이 F-14의 결과물이다) — 이 부분은 ✅ 확인 완료. 남은 것은 **gross 축 차이 자체의
  원인 규명**(08-31 결정문이 "원인 확정은 표본 1일이라 다음 관측으로 이월"이라 명시한
  바로 그 후속 관측이 오늘이다).
- **영향**: 오늘 마감 리포트·대시보드에 찍히는 "당일 손익 -527만 원"이라는 숫자는
  프로그램 내부 계산일 뿐이고, 증권사 계좌에서 실제로 줄어든 돈은 그보다 30만 원 더
  많은 -556만 원입니다. 실전 전환 여부를 판단할 때는 반드시 증권사 실측 금액을 써야
  하며(이미 그렇게 하도록 정해져 있습니다), 그 기준으로도 오늘은 손실입니다.
- **영향(기술)**: **종가 기준 최종 손익은 반드시 `daily_broker_pnl.broker_net_krw`
  (-5,564,446원)를 판정 원천으로 쓴다**(493차 F-1·F-4, `fetch_daily_net_for_verdict()`).
  아래 종합 절의 손익 수치도 이 값을 우선한다. 원인 후보는 미확정 — ⓐ 08-31과 달리
  **거래량(오늘 6레그) 대비 차액(-315,000원) 비율이 08-31(27레그, -70,000원)보다
  훨씬 크다**는 점에서 단순 수수료율 문제(거래량에 비례)로는 설명이 약하다. ⓑ 오늘
  이월 포지션(id=468)이 유일하게 "오늘자 진입 기록이 없는" 거래라는 점에서, 이
  포지션의 원 진입(09-01 15:34:46, 정체불명 기원 — 위 O-p2)과 관련된 계좌상의 별도
  현금흐름(체결 누락분·정산 시차 등)이 섞여 있을 가능성을 배제할 수 없다 — **확정
  아님, O-t1(다음 관측)로 이월**. ⓒ `deposit_snapshot_ts`가 13:35:36으로 15:10보다
  이르지만, 그 시각 이후 포지션 변화가 없었으므로(마지막 Chejan 이벤트 13:35:34)
  스냅샷 자체가 낡아서 생긴 차이는 아니다.
- **신규 여부**: **신규 아님 — 1-24(2026-08-31)와 같은 계열의 2번째 사례.** 표본
  2거래일, 둘 다 지배축=gross라는 공통점은 있으나 313차 원칙상 확정 결론 금지
  (거래량 대비 차액 방향이 08-31과 반대로 움직여 "거래량에 비례하는 단순 요율 문제"
  가설과는 오히려 배치된다).

#### 1-9. (신규) `entry_source`가 세션 내내 고착돼 정상 엔진 진입 3건이 전부 "브로커 복구" 로 오표기

- **증상**: 오늘 정상적으로 앙상블 판단을 거쳐 체결된 3건(10:19·10:49·13:34)이 전부
  `trades.entry_source='BROKER_SYNC_RECOVERY'`로 기록됐다. 이 값은 원래 "브로커
  잔고에서 강제로 복구한 포지션"을 표시하는 값인데, 이 세 건은 그런 것이 아니라
  `ensemble_decisions`에 정상적인 판단 근거(등급·확신도·메타게이트 결과)가 전부 남아
  있는 **진짜 시스템 자동진입**이다.
- **근거(로그 인용 + 시각 + DB)**: `ensemble_decisions`에서 오늘 `entry_executed=1`인
  행은 정확히 3건이며, 셋 다 등급·확신도가 온전하다.
  ```
  10:18:00  grade=C conf=43.6%  meta_action=take   meta_gate_horizon=1m  entry_qty=2
  10:48:00  grade=X conf=37.3%  meta_action=reduce meta_gate_horizon=3m  entry_qty=2
  13:33:00  grade=C conf=41.5%  meta_action=take   meta_gate_horizon=3m  entry_qty=1
  ```
  그런데 이 세 판단과 짝지어지는 `trades` 행(id=469~473, 체결시각 10:19·10:49·13:34)은
  전부 `entry_source='BROKER_SYNC_RECOVERY'`로 저장됐다.
  코드 확인(`main.py`): `self._entry_source`는 인스턴스 속성으로, `__init__`에서
  `"SYSTEM_AUTO"`로 **한 번만** 초기화된다(1135행). 이후 `"BROKER_SYNC_RECOVERY"`
  (16276·16498·17828·17940행), `"GHOST_PENDING_MISS"`(16137행),
  `"OPERATOR_MANUAL"`(3347행), `"OPERATOR_RESTORE"`(18306행)로 바뀌는 지점은
  **여러 곳**에 있지만, 이 값을 다시 `"SYSTEM_AUTO"`로 되돌리는 코드는 **어디에도
  없다.** 오늘 08:41:09에 `[BrokerSync] startup sync`가 한 번 발동해 이 값이
  `"BROKER_SYNC_RECOVERY"`로 바뀐 뒤, 그날 하루 종일 모든 후속 거래(설령 정상
  진입이라도)가 이 낡은 값을 그대로 물려받았다.
- **기준 위반**: 계측 4원칙 ④(폴백 가시화 — "이미 알고 있던 값"인 것처럼 조용히
  방치되는 상태 플래그). 08-31에 이미 `entry_source`가 **어디서 세팅되는지**는
  전수 확인됐으나(DECISION_LOG "함정① 정정 — F-9의 절반은 이미 배포돼 있었다"),
  그때는 **리셋되지 않는다는 사실 자체는 확인되지 않았다** — 그날은 진짜 시스템
  자동진입이 0건이라 대조할 표본이 없었다. 오늘 처음으로, 같은 세션 안에 "복구
  이벤트 이후의 정상 신규 진입"이 있어서 이 결함이 드러났다. 함정① 확인 완료(F-9
  카탈로그를 재사용했을 뿐, 새 조사가 아니다).
- **영향**: 오늘 거래 자체에는 영향이 없었습니다(체결·손익 계산은 정상입니다). 다만
  이 값은 "이 매매가 시스템이 스스로 판단해서 넣은 것인지, 어쩔 수 없이 브로커
  잔고에 맞춰 억지로 끼워 맞춘 것인지"를 구분하는 데 쓰이는데, 그 구분이 깨지면 나중에
  "시스템이 얼마나 잘 하고 있는가"를 평가하는 통계에서 진짜 좋은 성과가 누락될 수
  있습니다.
- **영향(기술)**: `scripts/generate_validation_campaign_report.py` 등 검증 캠페인
  스크립트 다수가 `entry_source='SYSTEM_AUTO'`를 명시적으로 필터링해 "진짜 시스템
  자동진입" 채널의 표본을 센다(예: 방향 편향 채널, `entry_source_filter` 파라미터).
  이 버그가 있는 세션에서는 **진짜 SYSTEM_AUTO 표본이 통째로 그 필터에서 빠지고**,
  min_samples 도달이 그만큼 늦어진다 — 이미 여러 캠페인 채널이 표본 부족으로 판정
  보류 중인 상황(TOX-SEVERE-SPREAD 등)과 같은 계열의 표본 누수다. 영향 범위는
  "BrokerSync·GHOST_PENDING_MISS·OPERATOR_MANUAL·OPERATOR_RESTORE 이벤트가 하루
  중 한 번이라도 발생한 세션의 그 이후 모든 거래"로 추정되며, 얼마나 자주 이런
  세션이 있었는지는 이번 세션에서 정량화하지 못했다(과거 전체 스캔 필요, 아래 Fix
  절 F-3의 검증 항목).
- **신규 여부**: **신규.** 함정①에 따라 `entry_source`가 **세팅되는 지점**은
  08-31에 이미 카탈로그화됐음을 확인했으나, **리셋되지 않는다는 이번 결함**은
  그 카탈로그에 없던 새 관찰이다.

### P2 — 운영 부담·기술부채

#### 1-10. 수익률향상 누적 대장 — P5-01·02·03·04·05·07·08 표본 재검증 지연(09-01·09-02 연속)

- **증상**: `docs/정기점검/수익률향상_누적대장.md`가 08-31 이후 갱신되지 않고 있다가
  09-01 장후가 "P2 이상점 후보"로만 표시하고 실제 편집은 미뤘다(09-01 리포트 §5-B).
  오늘 세션이 대장을 열어 **P5-06(exit_stage 오분류)·P5-09(외부진입 경보, 09-01
  이관분)** 두 건은 실제 표본을 갱신·신규 등록했으나, 나머지 6개 방안(P5-01~05·07·08)은
  이번 세션의 시간 제약으로 재검증하지 못했다.
- **근거**: `docs/정기점검/수익률향상_누적대장.md` 갱신 이력에 "2026-09-02 (MW0601
  51X차 장후): P5-06 표본 갱신 · P5-09 신규 등록. ⚠ P5-01~05·07·08은 이번 세션에서
  재검증하지 못했다"를 직접 기록해 뒀다.
- **기준 위반**: 대장 자신의 검증 조항(postmortem.md §5-B, 방안 5) — "미갱신이
  확인되면 그 자체를 P2 이상점으로 올린다".
- **영향**: 사용자에게 직접적인 영향은 없습니다. 다만 이 방안들 중 일부는 이미
  표본이 판정 기준에 도달했을 수도 있는데, 확인하지 않으면 그 시점을 놓칠 수
  있습니다.
- **영향(기술)**: 다음 장후 세션이 반드시 6개 방안 전체를 재검증할 것 — 특히
  P5-04(점심 블랙아웃)는 08-31 기준 "약 10건 부족"으로 가장 표본 도달에 가까웠다.
- **신규 여부**: 부분 지속(09-01의 "P2 후보"를 오늘 정식 P2로 등록, 일부만 해소).

### 확인 필요 (확정 아님)

- **1-8의 gross 축 차이 -315,000원의 정확한 원인** — O-t1로 이월(다음 26주 재검증
  또는 표본 축적 시). `scripts/commission_rate_recon.py --verify` 재실행 권고(단
  판정식은 변경하지 않음).
- **O-p2의 "누가·어떻게" 부분** — 시스템 경로가 아니라는 것은 확인됐으나, 실제 주체
  (HTS 수동 주문 / 제3자 접근 / 다른 프로그램)는 로그·DB로 확인 불가 — 사용자 확인
  필수(사용자 조치 절 참조).
- **O-p4** — 사용자가 09-01 저녁 지시를 실행하려 했는지, 여전히 응답 대기.

### 이미 반영된 사안 (신규 아님)

- **CB② 카운터 2건(연속손절 300초창)**: 설계대로 카운트만 되고 정지는 안 함
  (`CB_CONSEC_STOP_LIMIT=9999` 한시예외, 재검토 기한 2026-08-29 초과 지속 — §7 참조).
- **F-11(승패 표기 gross/net 병기)이 오늘도 정상 작동**: `일일 마감 | 승=3 패=1
  (gross pt 기준) | net 기준 1승 3패 | PnL=-5,265,803원(net)` — 08-31에 등록된
  F-11이 실효 중임을 재확인. 다만 "어느 축이 공식 승률인가"는 여전히 주간회의
  안건(1-20 원 결정 유지, 변경 없음).
- **ScalerFloor·ConstOut·WeightCollapse 반복**: 기존에 이미 특성화된 정상 패턴
  (개장 직후/재적합 시 반복 발생). 오늘 발생 빈도(각 1,608·6·82건)도 평소 범위.

---

## 2p. Fix 작업 구현계획

### F-3. `_entry_source`를 정상 신규 진입 시점에 `"SYSTEM_AUTO"`로 되돌린다 — P1 · 대응 이상점 1-9

- **원인 가설**: `main.py`의 `self._entry_source`가 상태 플래그인데, `"BROKER_SYNC_
  RECOVERY"`/`"GHOST_PENDING_MISS"`/`"OPERATOR_MANUAL"`/`"OPERATOR_RESTORE"`로 바뀌는
  지점은 여러 곳(1135·3347·16137·16276·16498·17828·17940·18306행)인데, 정상 진입
  체결 경로(`[체결진입]`/`[Position] 진입` 로그를 내는 지점)에서 이 값을
  `"SYSTEM_AUTO"`로 다시 세팅하는 코드가 없다.
- **변경 파일**: `main.py` — `[진입체크]` 통과 후 실제 주문을 내는 지점(체크리스트
  A/B/C등급 진입 실행부, `entry_qty`가 확정되는 근처)에 `self._entry_source =
  "SYSTEM_AUTO"` 를 명시적으로 세팅. `grep -n "\[주문요청\]"` 으로 정확한 삽입
  지점을 먼저 확인할 것.
- **회귀 위험**: 낮음(라벨링 전용, 매매 로직 미변경). 단 `_entry_source`를 읽는
  다른 소비처(`main.py:3578` `trades` INSERT)가 있으므로 세팅 시점이 INSERT
  **이전**이어야 한다.
- **절대원칙 저촉 여부**: 없음.
- **런타임 주의**: py37_32, 메인 스레드(COM 콜백 아님 — 확인 필요).
- **검증 방법**: `tests/test_5XX_entry_source_reset.py` 신설 — BrokerSync 이벤트
  발생 후 정상 진입이 이어지는 시나리오를 픽스처로 고정(오늘 09:41→10:19 순서를
  그대로 재현). 과거 영향 범위 정량화를 위해 `trades.db` 전체를 스캔해 "같은
  세션 안에서 복구 이벤트 이후 발생한 entry_executed=1 매칭 진입 중
  entry_source가 SYSTEM_AUTO가 아닌 건수"를 별도로 집계(회고적 조사, 코드
  변경과 무관하게 지금도 실행 가능 — 다음 세션 권고).
- **섀도 계측 필요 여부**: 불필요(순수 라벨 버그 수정).
- **적용 시점**: **장후 세션이므로 계획만.** 라이브 프로세스가 아직 돌고 있지
  않은 시간대(예약 실행 종료 후)라 해도, 이 스킬의 "장전·장중에만 실행 금지"
  규정과 별개로 **사용자 승인 없이 코드를 배포하지 않는다**는 상위 원칙을 따른다.
- **결정 필요 사항**: 없음(P1, 코드 리뷰 후 다음 세션에 구현 가능한 단순 수정).

### F-4 (참고, 신규 Fix 아님). F-10·F-14는 이미 계획·배포 상태 — 오늘은 추가 증거만

- F-10(exit_stage 다중체결 오분류)은 08-31에 이미 "계획만" 결정됨 — 오늘은 6번째
  사례를 추가했을 뿐 계획을 바꾸지 않는다.
- F-14(NetRecon 축 분해 로그)는 이미 배포·정상 작동 확인 — 추가 구현 불필요.
  남은 것은 원인 규명(O-t1, 통계적 관측)뿐 코드 변경 항목이 아니다.

### 적용 순서와 커밋 계획

장후 세션이므로 **오늘은 코드를 변경하지 않는다.** F-3은 다음 개발 세션(장 마감 후
비거래 시간)에서 구현·테스트 후 커밋 권고. F-1(daily_close 잔여 포지션 자동청산,
09-01·09-02 장전에서 이미 상정)은 여전히 주간회의 승인 대기 — 오늘 사고가 반복되지
않았다는 사실이 승인을 늦출 근거는 아니다(우연히 FLAT였을 뿐 구조적 해결이 아님).

---

## 3p. 고도화 방안

### G-3. 재기동 시 브로커 잔량 이상 실시간 경보 — 장전 G-1의 재확인(변경 없음)

오늘도 이 경보 훅은 배선되지 않은 채였다(08:41:09 CRITICAL 로그는 있었지만 실시간
알림 훅은 없음). 장전 리포트의 제안 그대로 유지 — 우선순위 재확인만 한다.

### G-4. 증거 다이제스트가 "오늘자 진입 로그가 없는 이월 포지션"을 인식하도록 개선

- **관측 근거**: 1-4(해소) — 오늘 다이제스트가 실재하는 -5,299,668원 손실을
  "귀속 실패"로 표시해, 다이제스트만 본 사람은 오늘 손익을 오독할 위험이 있었다
  (이 리포트 작성자는 DB를 직접 대조해 바로잡았다).
- **제안 내용**: `collect_evidence.py`의 §5 포지션 조립 로직에서, 청산 레그의
  `entry_ts`가 오늘 날짜가 아니면 "이월 포지션(전일 이전 진입)"으로 별도 표시하고
  손익은 그대로 합산한다.
- **기대 효과**: 로그만 보고도 "이월 포지션이 있었다"와 정확한 손익을 동시에 알
  수 있게 된다.
- **비용·위험**: 낮음(수집기는 읽기 전용 도구, 라이브 매매에 영향 없음).
- **선행 조건**: 없음.
- **우선순위 제안**: P2 — 급하지 않지만 반복되는 시각적 오독 위험을 줄인다.

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| CB② 복원(`CB_CONSEC_STOP_LIMIT` 9999→2~3) | 재검토 기한 2026-08-29(초과 5일) | 이번 주 주간회의에서 반드시 재론(3일 연속 동일 권고) | CLAUDE.md 실전 전환 기준 ⑤ |
| F-10(exit_stage 다중체결 오분류) 승인 검토 | "3일 이상 발생 시 검토" 조건에 2/필요일 도달 | 다음 발생 시 즉시 승격 검토 착수 | 누적 오분류 금액이 이미 -1,227만원으로 확대됨 |

---

## 4p. 진입 승패 사후검증 (EOD+P8)

### 선행조건 확인

- EOD(`daily_close`) 완료: `data/daily_close_done_20260902.txt` = `2026-09-02T15:40:13`.
- P8(스케일러 재적합) 완료: `data/eod_retrain_done_20260902.txt`에
  `horizons_replaced: 5/6`, `retrain_eod.log`에 `[P8] 스케일러 재적합 완료
  n=500봉 elapsed=0.05s` + `[P8] session_state p8_last_success_date +
  eod_retrain_ok_date 기록 완료` (15:48:54). **둘 다 확인 — 정상.**
  (15m 1개 호라이즌만 EOD 모델가드가 acc 하락 0.0449>0.03로 교체 보류 — 참고용 저장,
  구모델 유지. 정상 방어 동작이며 새 이상점 아님)
- 브랜치 컬럼 확인: `PRAGMA table_info(trades)` 결과 `exit_stage` 존재(`v9-dev` 정상),
  `tp1_reached`는 이 브랜치에 없음 — 승패 판정 1순위 기준(`exit_stage`)을 사용했다.

### 오늘 할일 대조 (요구1 보강)

- `dev_memory/NEXT_TODO.md` 미완료 체크박스: 2,292건(누적). 오늘 세션 관련분은
  F-1(P0-2 승인 촉구, 미승인 지속) · F-2(entry_horizon 코드 확인, 오늘 1-2→1-9로
  재정의되며 해소) · O-p2(오늘 판정 완료) · O-p4(지속).
- 함정① 게이트: 이번 세션이 새로 제안한 F-3(entry_source 리셋)은 `grep -n
  "_entry_source"`로 기존 코드 전수를 먼저 확인한 뒤 냈다 — 리셋 로직 부재를
  코드로 직접 확인, 이미 있는 것을 다시 제안한 것이 아니다.

### 데이터 소스 3원 대사

| 원천 | 값 |
|---|---|
| 로그 `[Position] 진입` | 3건 |
| DB(판정) `ensemble_decisions.entry_executed=1` | 3건 |
| DB(체결) `trades` `COUNT(DISTINCT entry_ts)`(오늘 진입분만) | 3건 |

**일치.** 별도로 `trades`에는 오늘 날짜로 청산된 레그가 총 4개 진입(entry_ts) 묶음·6개
레그가 있으나, 그중 1건(08:41:09)은 어제 진입한 이월 포지션이 BrokerSync로 오늘
날짜에 합성 등록된 것이라 "오늘의 신규 시스템 진입" 카운트에서 제외했다(§1-4 해소
근거와 동일 — 불일치가 아니라 설명 가능한 차이).

### 케이스 단위 딥다이브

```
[CASE-00] 진입 08:41:09(합성, 원 진입 09-01 15:34:46) LONG · 3계약
          (등급=BROKER·합성 · horizon 미상 · entry_source=BROKER_SYNC_RECOVERY)
          → 08:45:10 하드스톱(틱) 전량청산(3계약, 1/3·2/3·3/3 분할체결)
          포지션 합 -105.36pt / -5,299,668원 · 보유 시간 산정불가(원 진입시각 전일)
 진입근거   원 진입에 대응하는 ensemble_decisions 행 없음(09-01 15:08:00 이후 판단
            공백, 26분 뒤 미상 경로로 체결) — O-p2 판정 완료, 시스템 자동진입 아님
 예측 vs 실현  해당 없음(시스템 판단 자체가 없었던 진입)
 승패판정   ❌패 — `exit_stage='TRAIL_AFTER_TP1'`로 기록돼 있으나 **오분류**(1-7,
            F-10 대상). 실제로는 TP1 이벤트 없는 순수 손절(`INITIAL_STOP`에 가깝다)
 주 요인    F 외생 이벤트(정체불명 외부/미상 경로 진입 — 09-01부터 이어지는 미해결 사안)
 부 요인    E 체결·집행 — 진입가(1076.00) 대비 청산가(1040.74~1040.98) 35.1pt 갭은
            오버나이트 방치로 인한 시장 노출이지 슬리피지가 아니다(절대원칙 §1 위반의
            직접 귀결)
 반사실     "어제 15:40 daily_close 시점에 잔여 포지션 자동청산이 있었다면?" — F-1
            (P0-2, daily_close 통합) 채널 관할, 여기선 관찰만

[CASE-01] 진입 10:19:00 LONG · 2계약 (등급 A/원시C · horizon=1m · hurst=mean-revert)
          → 10:21:08 TP1 부분청산 33% (+0.33pt, +6,212원)
          → 10:21:18 하드스톱(틱)[TP1 보호전환 후] (-0.03pt, -11,788원)
          포지션 합 +0.30pt(gross 승) / -5,576원(net 패) · 보유 2분18초
 진입근거   `[진입체크] LONG→LONG 2계약 A급(원시C) | sign✅ conf✅ vwap✅ cvd❌ ofi✅
            fore❌ prev❌ time✅ risk❌ chas✅ coun✅ | conf=43.5%`,
            `[Sizer] 기본리스크=1,500,000 신뢰도배수=0.6 레짐배수=0.8 안전배수=1.00 → 3계약
            산출(최소=1)` → 실제 체결 2계약(게이트 배수 적용 후)
 예측 vs 실현  ensemble_decisions(10:18:00): conf=0.4355 / meta_action=take /
            meta_confidence=0.4797 / quantile_expected_pt=+0.031 → 실현 방향(LONG, 상승)
            과 부호 일치, 다만 목표폭(0.031pt)보다 실제 변동폭은 컸다(TP1까지 +0.58pt)
 승패판정   ✅승(gross, +0.30pt) / ❌패(net, -5,576원) — `exit_stage='TRAIL_AFTER_TP1'`은
            **정상 분류**(TP1 실제 발생, id=469 확인)
 주 요인    C 청산 규칙 — TP1 이후 보호 스톱이 진입가 근처(1048.65)로 좁게 설정돼
            반락에 곧바로 걸림(TP1 체결 10:21:08 → 보호스톱 체결 10:21:18, 10초)
 부 요인    E 체결 — 왕복 수수료(합 20,576원)가 gross 이익(16,500원)보다 커서 net
            손실로 전환(손익분기 미달 사례, 08-31 1-20/F-11과 동일 유형)
 반사실     "보호 스톱을 1~2틱 더 넓혔다면?" — 표본 부족, 관찰만

[CASE-02] 진입 10:49:01 SHORT · 2계약 (등급 A/원시X · horizon=3m · hurst=mean-revert)
          → 10:50:16 TP1 부분청산 33% (+0.79pt, +29,295원)
          → 10:51:02 하드스톱[TP1 보호전환 후] (+0.45pt, +12,295원)
          포지션 합 +1.24pt / +41,590원 · 보유 2분1초
 진입근거   `[진입체크] SHORT→SHORT 2계약 A급(원시X) | 전항목 통과(risk❌만 미통과) |
            conf=37.3%`. `raw_grade=X`이나 `grade=A`로 상향 — 전략 리포트의
            "등급상향경로(앙상블X→체크리스트통과) 1건"이 이 케이스로 추정된다
 예측 vs 실현  ensemble_decisions(10:48:00): conf=0.3728(raw 0.3322) / meta_action=reduce
            / meta_size_mult=0.5 / quantile_expected_pt=-0.084 → SHORT 유리 예측과
            실현 방향(하락) 일치, 목표폭 근사
 승패판정   ✅승(gross·net 모두) — `exit_stage='TRAIL_AFTER_TP1'` **정상 분류**(TP1
            실제 발생, id=471 확인). 오늘 유일한 완전 승리 포지션
 주 요인    A 방향예측 적중(quantile 부호·메타게이트 판단 모두 일치)
 부 요인    C 청산 규칙 — TP1 이후 보호 트레일이 이익을 지키며 청산(설계 의도대로 작동)
 반사실     해당 없음(모범 사례)

[CASE-03] 진입 13:34:01 SHORT · 1계약 (등급 A/원시C · horizon=3m · hurst=mean-revert)
          → 13:34:53 TP1 보호전환(단일계약, 회계상 확정 33% @ +0.50pt, 물리적 분할 없음)
          → 13:35:34 하드스톱(틱) (+0.16pt, -2,150원)
          포지션 합 +0.16pt(gross 승) / -2,150원(net 패) · 보유 1분33초
 진입근거   `[진입체크] SHORT→SHORT 1계약 A급(원시C) | ofi❌만 미통과 | conf=41.5%`,
            `[Sizer] … [ConfShadow: 1.2→2계약]`(섀도 사이저는 2계약을 제안했으나
            실제 산출은 1계약 — 표본 부족, 313차 관찰만)
 예측 vs 실현  ensemble_decisions(13:33:00): conf=0.4147 / meta_action=take /
            quantile_expected_pt=+0.036 → 부호 일치
 승패판정   ✅승(gross, +0.16pt) / ❌패(net, -2,150원) — `exit_stage='TRAIL_AFTER_TP1'`은
            **정상 분류(설계상 단일계약 경로)**: `_tp1_armed()`가 아니라
            `arm_tp1_single_contract*()`가 물리적 분할 없이 스톱만 당겨 올린 경우로,
            `[ExitStageRecon]`이 "단일계약 보호전환(설계) 1건"으로 이미 구분해 준 케이스
 주 요인    C 청산 규칙 — 1계약이라 TP1 물리적 분할이 불가능해 "보호 트레일" 하나로
            수렴, 좁은 보호폭 때문에 net 손익분기(0.204pt) 미만에서 청산
 부 요인    E 체결 — 왕복 수수료(10,150원)가 gross 이익(8,000원)을 초과
 반사실     해당 없음(설계된 경로가 정상 작동)
```

### 집계표

| 요인 | 승(gross) | 패(gross) | 승(net) | 패(net) | 순손익(net) | 평균 보유(분) | 관측 거래일 수 |
|---|---|---|---|---|---|---|---|
| 오늘 4포지션 전체 | 3 | 1 | 1 | 3 | -5,265,804원 | 산정불가(CASE-00 제외 시 1.9분) | 1 |
| CASE-00(이월/미상 기원) 제외 3건 | 3 | 0 | 1 | 2 | +33,864원 | 1.9분 | 1 |

- 단위는 **포지션**(레그 아님). 승패 판정은 §3-1 기준(`exit_stage`)으로 했다.
- **313차 적용**: ① 판정 단위=일자, 오늘은 관측 1일뿐 — 어떤 결론도 확정하지 않는다.
  ② overlap 보정 — 오늘 3건은 서로 겹치지 않는 별개 시간대(10:19·10:49·13:34)라
  중첩 없음. ③ 이상치 분해 — CASE-00을 빼면 오늘은 net -5,576+41,590-2,150=
  **+33,864원 흑자**였다. 즉 오늘 시스템 자체의 정상 매매는 이익이었고, 손실은
  전적으로 이월 사고(CASE-00)에서 왔다 — "3승1패인데 순손실"이라는 표면적 결과는
  1건의 비정상 케이스가 만든 착시다. ④ 사전등록 — 표본 부족으로 어떤 파라미터
  변경도 제안하지 않는다. ⑤ 부호 일관성 — 표본 1일이라 판정 대상 아님.
- **함정② 재인용 금지 확인**: 사이징(D) 축은 오늘 이렇다 할 이상 없음(3계약 상한
  이내), 2026-08-01 §9-3 통계나 2026-06-25 SHAP=0 판정을 인용하지 않았다.

---

## 5p. 수익률 향상방안

### 5-B. 누적 대장 확인·갱신 (완료)

`docs/정기점검/수익률향상_누적대장.md`를 열어 다음을 완료했다(위 1-10에 상세):

- **P5-06** 표본 갱신: 5포지션/1거래일 → **6포지션/2거래일(누계 -12,277,059원)**.
  판정식·합격선 변경 없음(458차 D6 준수). 오늘 신규 6칸을 다시 쓰지 않았다 — 표본
  칸만 갱신.
- **P5-09** 신규 등록: 09-01 리포트가 「P5-신규 — 외부/MANUAL 진입 실시간 경보」로
  본문에만 적어두고 대장 편집을 미뤘던 것을 오늘 정식 행으로 옮겼다(6칸은 09-01
  리포트에 이미 있음 — 대장은 색인이므로 표만 옮기고 근거는 원 리포트를 가리킨다).
- P5-01~05·07·08은 시간 제약으로 재검증하지 못했고, 그 사실 자체를 1-10(P2)으로
  등록했다(위 참조) — 대장 미갱신을 조용히 넘기지 않았다.

### 오늘 신규 방안 없음

오늘 발견한 세 가지(1-7/1-8/1-9)는 전부 **계측·라벨링 정확성 버그**이지 "표본을
쌓아 판정할 통계적 가설"이 아니다 — postmortem.md §5의 "방안"(수익률 개선 가설)
범주에 들지 않고, 위 제2부 Fix 계획(F-3) 및 기존 F-10·F-14로 계속 추적한다.
일반론적 신규 방안은 내지 않는다(SKILL.md §5 — 일반론 금지).

---

## 6p. 다음 거래일 관측 예정

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| O-t1 | 1-8 net 대사 gross 축 차이(-315,000원)의 원인 | `commission_rate_recon.py --verify` 재실행 + 3번째 사례 발생 시 방향·배수 패턴 대조 | 다음 불일치 발생 시 또는 26주 주기 |
| O-t2 | F-10 대상(exit_stage 다중체결 오분류) 3번째 사례 발생 여부 | `[ExitStageRecon]` "미대응" 로그 재출현 — 3일째 발생 시 F-10 승인 검토 착수 | 다음 발생일 |
| O-t3 | `entry_source` 오표기 과거 영향 범위 | `trades.db` 전체 스캔(회고) — BrokerSync/GHOST/MANUAL 이벤트 이후 세션 내 SYSTEM_AUTO 미표기 건수 | 다음 세션(코드 변경 없이도 조사 가능) |
| O-p4(재이월) | 사용자가 09-01 저녁 지시대로 포지션을 정리하려 시도했는지 | 사용자 응답 시 | 사용자 응답 시 |
| P5-01~05·07·08 | 누적 대장 재검증 | 다음 장후가 반드시 6개 방안 표본을 다시 확인 | 다음 거래일 |

## 7p. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| CB② 복원(`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | 2026-08-29 | **초과 5일** | 주간회의 재상정 필요(3일 연속 동일 권고 — 방치 장기화 우려) |

## 8p. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-09-02 (MW0601 51X차 — 장후 점검: 종합)` —
  아래 append 완료.
- `NEXT_TODO.md` 추가 체크박스: F-3(entry_source 리셋) · O-t1~O-t3(신규 관측) ·
  1-10 누적대장 재검증 잔여 6건 — 아래 append 완료.
- 완료 처리한 기존 항목: O-p2(오늘 DB로 최종 판정), O-p3(변경 없음, 완료 유지).

## 이 절이 발급한 번호

이 절이 발급한 번호: 이상점 **1-7~1-11** · 관측 **O-t1~O-t3**.

#### 1-11. (신규, P2) 이번 점검 세션이 `.git/index.lock`을 남김 — 스테일 확정, 회수 실패

- **증상**: 이 세션이 git 상태를 확인하는 과정에서 `.git/index.lock`(0바이트)이
  생겼다. `python scripts/git_lock_guard.py --check` 결과 `STALE`(실행 중인 git
  프로세스 0개, 0.4시간 경과)로 안전하게 회수 가능한 상태였으나, `--reclaim` 실행이
  `Operation not permitted`로 실패했다 — 리눅스 샌드박스에서 마운트 경유로 파일을
  지우려 할 때 발생하는 알려진 제약(SKILL.md §0 git 호출 규약에 이미 문서화된 실패
  유형)이다.
- **근거**: `git_lock_guard.py --check` 출력 `futures STALE 스테일 확정 — 0바이트 ·
  0.4시간 · git 프로세스 0개`. `--reclaim` 출력 `STALE 회수 실패:
  [Errno 1] Operation not permitted`.
- **기준 위반**: 없음(설계된 안전장치가 정상적으로 "지우면 안 되는 상태인가"를
  판별했고, 판별 결과 "지워도 되지만 권한이 없다"는 것을 정확히 보고했다 — 오진이
  아니다).
- **영향**: 지금 당장 매매나 손익에 영향은 없습니다. 다만 이 파일이 남아있는 채로
  다음에 이 폴더에서 커밋을 시도하면 실패할 수 있습니다.
- **영향(기술)**: `--no-optional-locks`를 이번 세션 전체에서 일관되게 사용했음에도
  락이 생겼다 — 아마도 `collect_evidence.py` 내부의 특정 git 호출 하나가 이 옵션을
  놓쳤을 가능성이 있다(수집기 자가점검은 "이 실행이 만들지 않았다"고 보고했으나
  타이밍상 이 실행 구간과 겹친다 — 확정 아님, 코드 조사 필요). 사용자가 직접
  탐색기/터미널에서 파일을 지우면 즉시 해소된다.
- **신규 여부**: 신규(이번 세션 한정 관측).

---

# 종합 — 오늘 (2026-09-02, MW0601)

## ① 종합 판정표

| 국면 | 판정 | 한 줄 요약 |
|---|---|---|
| 장전(08:59~09:05) | 이상(P0) | 전일 미해소 매수 3계약이 08:45 자동 손절로 확정 -530만원. 개장 준비는 정상 |
| 장중(12:26~12:30) | 조건부 정상 | 매매·설계 절차 정상. 57초 메인 스레드 정지(무해)·확신도 하한 장기 미달 관찰 |
| 장후(16:17~) | 조건부 정상(P1 신규 1건 · P1 재현 2건) | 15:10 강제청산·EOD·P8 전부 정상. 마감 정산 net 불일치(2번째 재현)·exit_stage 오분류(6번째 재현)·entry_source 고착(신규) 발견 — 전부 정확성/계측 문제, 오늘 거래 실행 자체의 위험은 아님 |
| **오늘 최종** | **이상(P0, 전일 귀속) + 정확성 이슈 다수(P1)** | 손실의 실체는 전일 사고의 오늘 실현. 오늘 자체 신규 시스템 매매는 소액 흑자(+33,864원). 다만 계측·라벨링 결함이 세 건 드러나 데이터 신뢰도에 유의가 필요 |

## ② 당일 이상점 통합 대장

| 번호 | 제목(줄임) | 발견 국면 | 최초 심각도 | 최종 심각도 | 최종 처분 |
|---|---|---|---|---|---|
| 1-1 | 전일 미해소 포지션 자동손절 -530만원 | 장전 | P0 | P0 | ✅ 해소(사건 종결, 발생원 O-p2까지 확인) |
| 1-2 | `PositionFallback` entry_horizon 미설정 | 장전 | P1 | — | ↩️ 정정(1-9로 재정의·승계) |
| 1-3 | 511차 Fix 첫 라이브 확인 | 장전 | (관측) | (관측) | ✅ 완료(정상 확인) |
| 1-4 | 증거 다이제스트 정합성 불일치 | 장전 | P2 | — | ✅ 해소(도구 한계로 재규정, 데이터 손실 아님) |
| 1-5 | 메인 스레드 57초 정지 | 장중 | P1 | P1 | 🔄 지속(26주 주기 누적 관찰, 14:12 추가 사례) |
| 1-6 | `ConfFloorGuard` 장기지속 | 장중 | P2 | P2 | ✅ 판정(가설 — 시장 저신호 쪽에 무게, 확정 아님) |
| 1-7 | exit_stage 다중체결 오분류(6번째 사례) | 장후 | P1 | P1 | 🔄 지속(F-10 대상, 3일째 발생 시 승인 검토) |
| 1-8 | net 대사 불일치(2번째 사례) | 장후 | P1 | P1 | 🔄 지속(O-t1로 원인 규명 이월) |
| 1-9 | `entry_source` 세션 고착 | 장후 | P1 | P1 | 🆕 신규(F-3 Fix 계획 등록) |
| 1-10 | 누적 대장 재검증 지연 | 장후 | P2 | P2 | 🔄 지속(6개 방안 미검증, 다음 세션 필수) |
| 1-11 | 점검 세션이 `.git/index.lock` 남김(회수 실패) | 장후 | P2 | P2 | 🆕 신규(사용자 조치 5번으로 이관) |

**미처분 0건.**

## ③ 종가 기준 최종 손익

> ⚠ 판정 원천은 **브로커 실측 net**(`daily_broker_pnl.broker_net_krw`)이다(493차
> F-1·F-4). 엔진 net은 참고값이며, 오늘은 둘의 차이 자체가 이상점(1-8)이다.

| 항목 | 값 |
|---|---|
| **최종 손익(브로커 실측, 판정 원천)** | **-5,564,446원**(자본 5천만원 대비 **-11.13%**) |
| 참고: 엔진 계산 net | -5,265,803원(자본 대비 -10.53%) — 1-8 사유로 브로커 실측보다 30만원 낙관적 |
| 참고: 브로커 gross | -5,498,000원 |
| 참고: 엔진 commission | 82,803원 |
| MDD(당일 단독 정의 없음 — 다거래일 개념) | 롤링 20일 자본대비 **26.5%**(재인용 가능) / peak대비 675.1%(⚠ 461차 재인용 금지 대상, 판정에 쓰지 않음) |
| 진입·청산 건수(포지션 단위) | 진입 4건(이월 1 + 신규 3) · 청산 4건 |
| 진입·청산 건수(청산 레그 단위) | 레그 6건(부분청산 2 · 전량청산 4) |
| 승률(포지션, gross 축) | 3승 1패(75%) |
| 승률(포지션, net 축) | 1승 3패(25%) — ⚠ 두 축이 정반대로 갈린다. 원인은 손익분기(0.204pt) 미만 소폭 이익이 수수료로 잠식되는 구조(CASE-01·03), 08-31 1-20/F-11과 동일 유형이지 신규 결함 아님 |
| 종가 포지션 | 0계약(FLAT) |
| 차단 건수 | 74건(28종, `2_confidence` 미통과 35건이 최다) |
| 사이저 호출 | 13건(최대 산출 3계약, 실제 진입 최대 2계약) |
| CB 발동 건수 | CB② 카운터 2건(정지 없음, 한시예외 설계대로) · CB①③④⑤ 0건 |

## ④ 절대원칙 판정 — 최종

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① 오버나이트 금지(15:10 강제청산) | 🔴 **위반(전일 09-01분, 오늘 실현)** — 오늘 자체 거래는 준수 | 09-01 15:34:46 진입분이 밤새 방치돼 오늘 08:45 손실 확정. 오늘 자체 매매는 13:35:34 이후 무포지션, 15:11:08 강제청산 점검 "청산 대상 없음(정상)" |
| ② Circuit Breaker 5종 | 부분 준수(한시예외 지속) | CB② 카운터만 발생, 정지 없음(설계대로, 재검토 기한 초과 5일) |
| ③ CORE 피처 호라이즌 그룹별 | 준수 | 오늘 진입 전부 단기(1m/3m) 그룹, 미통과 시 등급하락 규칙대로 처리 |
| ④ COM 콜백 dynamicCall·emit 금지 | 준수 | 크래시 로그 0건 |
| ⑤ GetRepeatCnt/GetCommData 파라미터 구분 | 해당 없음 | 코드 정합성 항목, 로그로 직접 관측 불가 |
| ⑥ 알파 리서치 봇 자동통합 금지 | 준수 | 관련 로그 0건 |

**한시예외 적용 현황**: CB②(9999, 재검토 기한 초과 5일) · CB③-P4(False) ·
FP-CRITICAL(False) · TOX-SEVERE-SPREAD(False, 26주 항목으로 이관됨, 정상) — 전부
설정대로 유지, 불일치 0건.

**9단계 STEP 중 걸린 것**: 없음 — 08:55 매크로 수집 → 09:00 개장 → 매분 STEP1~9 →
15:10 강제청산(자체 거래분 정상) → 15:18 안전망 → 15:40 EOD → 15:45 P8 전부 정상
이행. 매분 루프 커버리지 371/371분(100%), 10분 이상 공백 0건.

**15:10 강제청산 이행 여부**: ✅ 이행(오늘 자체 거래분 — 13:35:34에 이미 FLAT).

**EOD 재학습 성공 여부**: ✅ 성공(`horizons_replaced 5/6`, 15m만 모델가드 보류 —
정상 방어). **P8 성공**(`p8_last_success_date=2026-09-02`).

---

## 사용자 조치 — 오늘 전체 통합 (맨 마지막)

| # | 할 일 | 배경 | 걸리는 시간 | 안 하면 생기는 일 |
|---|---|---|---|---|
| 1 | 어제(09-01) 저녁 "매수 3계약 즉시 매도" 요청을 실행하려 했는지, 왜 반영되지 않았는지 알려주세요 | 그 포지션이 오늘 아침 자동 손절로 -530만 원 확정. DB 확인 결과 시스템 자체 버그로 생긴 진입이 아니라는 것은 확인됐지만, 실제 발생 경로(누가·어떻게 주문을 냈는지)는 여전히 모릅니다 | 대화 한 번 | 같은 경로가 반복될 수 있음 — 원인을 모르면 재발 방지가 불가능합니다 |
| 2 | 어제 리포트의 "정체불명 매매 38건(-163만원)이 본인 것인지"와 오늘 손실 포지션이 본인 것인지 알려주세요 | 오늘 DB 조회로 이 진입이 시스템의 정상 판단 경로를 거치지 않았다는 것이 확정됐습니다 — 외부에서 들어왔을 가능성이 더 커졌습니다 | 1분 | 발생원을 못 찾으면 계속 미해결 상태로 남고, 다음에도 같은 방식으로 큰 손실이 날 수 있습니다 |
| 3 | 대신증권 계좌 상태와 오늘 정산 내역(특히 실현손익 -556만원)을 한 번 더 직접 확인해 주세요 | 오늘 프로그램 내부 계산(-527만원)과 증권사 실제 정산(-556만원) 사이에 약 30만원 차이가 있고, 원인이 아직 확인되지 않았습니다 | 3분 | 원인 불명 상태로 방치하면 다음 정산에서 비슷한 차이가 또 나도 알아채기 어렵습니다 |
| 4 | (참고, 급하지 않음) `daily_close()` 진입 전 잔여 포지션 자동청산(F-1) 승인 여부를 검토해 주세요 | 이 기능이 있었다면 09-01 사고를 미리 막았을 수 있습니다. 이번 주로 3번째 상정입니다 | 검토 5분 | 같은 유형의 사고가 반복될 위험이 계속 남습니다 |
| 5 | 🔴 컴퓨터에서 직접 `C:\Users\82108\PycharmProjects\futures\.git\index.lock` 파일을 지워주세요 | 이 점검 세션이 git 상태를 확인하는 과정에서 빈 잠금 파일이 하나 생겼는데, `git_lock_guard.py --check`로 "스테일(정상적으로 회수 가능)"까지는 확인했지만, 이 점검이 실행되는 환경(리눅스 샌드박스)에서는 권한 문제로 세션이 직접 지우지 못했습니다(`Operation not permitted`) | 30초 | 이 파일이 남아있으면 다음에 이 폴더에서 git 명령(커밋 등)을 실행할 때 실패할 수 있습니다 |

커밋 대기: `docs/정기점검/수익률향상_누적대장.md`,
`docs/정기점검/매일점검/MW0601-20260902-점검리포트.md`,
`docs/정기점검/매일점검/evidence_MW0601-20260902_post.md`,
`dev_memory/DECISION_LOG.md`, `dev_memory/NEXT_TODO.md`

---

# 제4부. 장후 자동조치 구현 결과 (2026-09-02 17:25~18:0X · 자동 · MW0601 518차)

> 이 절은 **자동조치 예약작업**(`mireuk-postmarket-autofix`, 평일 17:23)이 붙였다.
> 위 장전·장중·장후 본문은 **한 글자도 고치지 않았다**(하루 한 파일 · 국면마다 append 규약).
> 이 예약은 점검과 달리 **커밋·푸시를 한다** — 2026-08-26 사용자 승인(494차 후속).

## ① 오늘 무엇을 했나 (평문)

리포트가 제안한 것 중 **세 가지를 실제로 고쳐서 저장**했습니다. 셋 다 **매매 방식은
건드리지 않았습니다** — 미륵이가 언제 사고 언제 파는지는 어제와 똑같습니다. 바뀐 것은
**기록이 정확해진 것**과 **사람이 위험을 더 빨리 알아채는 것** 두 가지뿐입니다.

| 무엇을 | 왜 | 안 했으면 |
|---|---|---|
| 진입 기록에 붙는 「누가 낸 주문인가」 표가 다시 정상으로 돌아오게 했다 | 한번 「외부 주문」으로 바뀌면 그날 내내 그대로 굳어, 그 뒤 미륵이가 스스로 낸 진입까지 남의 것으로 기록됐다 | 성적표에서 미륵이 자기 거래가 계속 누락된다 |
| 재기동했을 때 계좌에 포지션이 남아 있으면 **읽히는 경고**를 띄우게 했다 | 경고는 원래 떴지만 첫 단어가 "완료"라 성공 알림처럼 보였다. 오늘 아침 그 경고가 뜨고 4분 뒤 -530만원이 확정됐다 | 다음에도 4분을 그냥 흘려보낸다 |
| 점검 자료에서 **전날 넘어온 포지션의 손익**이 표에 나타나게 했다 | 오늘 -529만원이 표 어디에도 없고 "귀속 실패" 한 줄로만 남아, 자료만 본 사람은 오늘을 소액 흑자로 오해한다 | 매일 손익을 잘못 읽을 수 있다 |

**손대지 않은 것**: 마감 직전 남은 포지션 자동청산(F-1)입니다. 이건 실제로 주문을 내는
기능이라 **사용자 승인 없이는 자동조치가 만들지 않는다**는 규칙에 걸립니다. 아래 ④ 참조.

## ② 구현한 항목

### F-3. 진입 출처 라벨이 세션 내내 고착되던 것 (이상점 1-9 · P1)

- **변경**: `main.py` `_ts_execute_entry()` — 조기 반환 게이트 5종(서버구분·계좌불일치·
  broker sync·쿨다운·pending)을 **전부 통과한 뒤**, `_set_pending_order()`·
  `_send_broker_entry_order()` **직전**에 `self._entry_source = "SYSTEM_AUTO"` 1줄.
- 🔴 **신설이 아니라 복구다.** `_on_manual_entry_requested()`가 `_execute_entry()` 호출
  직후 `OPERATOR_MANUAL`로 덮어쓰면서 그 자리에 *"_execute_entry() 내부에서
  기본값(SYSTEM_AUTO)으로 설정되므로 호출 후 덮어씀"*(`main.py:3346`)이라고 적어두고
  있었다 — **설계 의도는 처음부터 이 지점이었고 구현만 없었다.**
- **왜 하필 그 지점인가**(둘 다 만족하는 유일한 자리):
  ① 게이트보다 **뒤** — 차단된 시도가 라벨을 되돌리면 그때 들고 있던 외부/복구 포지션이
     청산될 때 `SYSTEM_AUTO`로 오귀속된다.
  ② 주문 전송보다 **앞** — `BlockRequest()`가 COM 이벤트를 pump 하는 동안 체결 콜백이
     먼저 도착하는 경로(바로 아래 `Fix-PendingFirst` 주석의 그 레이스)가 낡은 라벨을 읽는다.
- **범위**: 리포트 계획은 "진입 실행부"였으나, 실물 확인 결과 진입 3경로(수동 `main.py:3345`
  · 자동 `10001` · C등급 실험 `10119`)가 **전부 `_execute_entry()` 하나를 지난다.**
  퍼널 1곳만 고치면 3경로가 함께 낫는다 — 진입부마다 넣지 않았다.
- ⚠ **소급 정정이 아니다.** 기존 행은 그대로다. `trades.entry_source` 시계열은
  **2026-09-02에 불연속**이며 앞뒤로 "SYSTEM_AUTO 건수"를 직접 비교하면 안 된다
  (461차 `mdd_pct`와 같은 유형). 사전등록 채널 다수가 `"entry_source": "SYSTEM_AUTO"`로
  표본을 거르므로 이 수정 **이후** 표본이 늘어나는 것은 정상이며, **합격선·판정식은
  무변경**이다(458차 D6 준수).

### G-1(=G-3). 재기동 시 브로커 잔량 경보 — 훅이 아니라 **문구**를 고쳤다

- 🔴 **리포트 전제의 절반이 틀렸다(함정① 적발).** 리포트는 *"CRITICAL 로그는 있었지만
  실시간 알림 훅은 없음"*이라 했으나, 코드 실측 결과 경로가 **이미 이어져 있었다**:
  `log_manager.system(…,"CRITICAL")` → SYSTEM 레이어 → `main.py:1272`
  `log_manager.subscribe("SYSTEM", …)` → `dashboard.append_sys_log_tagged()`
  (WARN/ERROR/CRITICAL 통과) → `log_panel.append("all","CRITICAL",…)` → **「2 경보」 탭**.
  → **훅을 새로 달았으면 같은 사건이 경보 탭에 두 줄로 떴을 것이다.** 달지 않았다.
- **진짜 결손은 문구였다.** 기존 줄은 `[BrokerSync] startup sync 완료: FLAT -> LONG 3계약` —
  **첫 단어가 "완료"**다. 경보 탭을 훑는 사람에게 성공 통지로 읽힌다. 오늘 08:41:09에
  실제로 이 줄이 떴고 08:45:11 하드스톱까지 4분이 있었으나 아무도 움직이지 않았다.
  **로그는 제 할 일을 했다 — 읽히지 않았을 뿐이다.**
- **변경**: 기존 줄은 **그대로 두고**, 그 아래 `before != after and qty > 0`일 때만
  ERROR 한 줄 추가 — 무슨 일인지 + 지금 무엇을 확인할지 + 안 하면 무엇이 걸리는지
  (514차 F-C `[ExternalEntry]`와 같은 형식). FLAT 정상 재기동에서는 뜨지 않는다.
- Slack 미사용(사용자 결정 — 개발단계 직접 모니터링).

### G-4. 이월 포지션 손익이 다이제스트에서 사라지던 것 (이상점 1-4 후속)

- 🔴 **리포트 제안은 이 도구에 적용할 수 없었다.** 제안은 *"청산 레그의 `entry_ts`가
  오늘이 아니면 이월로 표시"*였는데, **수집기는 DB를 읽지 않는다**(로그·설정·git 전용,
  SKILL.md §2) — `entry_ts`라는 필드가 여기 없다. 로그만으로 같은 것을 판정하는 방법이
  **"오늘 여는 이벤트가 없는 청산 레그"**, 즉 이미 계산돼 있던 `orphans`다.
  **제안의 의도를 로그 축으로 옮겨 구현했다.**
- **변경**: `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py` §5 —
  ⓐ 이월 블록 신설(건수·pt·원 + 레그 표) ⓑ 헤드라인 미포함 사실 명시 ⓒ **합계 병기**
  ⓓ 절대원칙 §1(전일이 FLAT으로 안 끝났다) 확인 지시 ⓔ 정합성 줄에 금액 동반.
- **합산하지 않고 병기했다.** 헤드라인에 그냥 더하면 이번엔 「오늘 엔진 성적」이 전일
  사고로 오염된다(507차 F-8이 출처별 소계를 따로 둔 것과 같은 이유). 세 값을 전부 보여준다.
- **실데이터 검증(오늘자 로그 재실행)**:
  ```
  이월 포지션(전일 이전 진입) 추정 — 청산 레그 3행 · -105.36pt · -5,299,668원
  위 포지션 표 합계(+33,864원)에 이 금액은 들어 있지 않다
  오늘 계좌에서 실제로 오간 돈 = -5,265,804원
  ```
  🔵 이 합계 **-5,265,804원**이 위 ③표의 엔진 net **-5,265,803원**과 1원(반올림) 차이로
  일치한다 — **로그 축만으로 엔진 net을 재현**하게 됐다는 뜻이다.

## ③ 검증

| 항목 | 결과 |
|---|---|
| 신규 `tests/test_518_entry_source_and_carryover.py` | **18 passed** |
| 전체 스위트 | **1,089 passed · 3 failed · 1 skipped · 4 xfailed** |
| 실패 3건 | **전량 선행 실패**(내 변경과 무관) — `test_483_git_lock_guard`(형제 프로젝트 fuoption 사본 대조 1건) · `test_504_pnl_history_creon_tab`(2건, PnL 탭 필터·브로커 계측) |
| 대조 | 09-01 상태파일 `1,071 passed · 3 failed` + 신규 18 = **1,089** — 정확히 일치, **신규 실패 0** |

**「라이브 반영 0」 불변식 테스트를 함께 넣었다**(§4, 3건):
`_entry_source`가 **DB INSERT 한 곳에서만** 읽히는지(분기 조건으로 승격되면 라벨이
매매 판단이 된다) · 진입 퍼널의 게이트·주문 호출 구조가 그대로인지 · G-1 경보 블록에
주문·청산 호출이 없는지.

⚠ **환경 이슈 2건 — 내 변경과 무관하며 오늘 새로 생긴 것이 아니다**:
① `tests/test_500_*.py` 5개는 모듈 최상단에서 `sys.exit(0)`을 부르는 **독립 실행
스크립트**라 pytest 수집 시 에러가 된다(각각 단독 실행하면 "✅ 전부 통과"를 찍는다).
② `tests/test_511_exit_order_reject.py`는 전체 실행 시 `win32com` 적재에서
`0xc0000139`(STATUS_ENTRYPOINT_NOT_FOUND)로 **프로세스가 하드 크래시**한다(단독 실행은
정상). 둘 다 09-01 상태파일의 제외 목록에 이미 올라 있던 항목이라 그대로 두었고,
전체 스위트는 **청크 분할**로 돌려 위 수치를 얻었다.

## ④ 구현하지 않은 것과 그 이유

| 항목 | 등급 | 이유 |
|---|---|---|
| **F-1** `daily_close()` 진입 전 잔여 포지션 강제청산 | **C** | **주문·청산 실행 경로 변경** + 리포트 자신이 *"자동조치 C등급 — 승인 전에는 구현하지 않는다"*라고 명시. 주간회의 승인 대기(3번째 상정) |
| **G-2** 메인 스레드 정지 실시간 경보 | **보류** | 리포트가 *"지금은 표본 1건뿐이라 임계값을 정할 근거가 부족"*하다며 26주 WFA 주기로 미뤘다 — 313차 원칙(표본 부족 시 확정 금지) |
| **F-2** `entry_horizon` 미전달(장전) | 흡수 | 이상점 1-2가 장후에 1-9로 **정정·승계**됐고, 그 처분이 F-3다 — 별건으로 구현하지 않았다 |
| **F-10 / F-14** | 해당 없음 | 리포트 F-4가 *"이미 계획·배포 상태, 추가 구현 불필요"*라고 스스로 적었다 |
| **CB② 복원** | **C** | 절대원칙 한시예외 토글 — 주간회의 전용(기한 초과 5일, 아래 ⑥) |

**제5부 신규 방안**: 리포트가 *"오늘 신규 방안 없음"*이라 적었으므로 구현 대상 0건.

## ⑤ 덤으로 고친 것

`dev_memory/DECISION_LOG.md`·`NEXT_TODO.md`의 세션 헤더가 **`51X차`라는 미해결
자리표시자**로 남아 있었다(장전 515·장중 516이므로 장후는 **517**). 세션 번호는 다음
세션이 이력을 찾는 열쇠라 `X`가 박혀 있으면 검색이 안 된다 — **517차로 확정**했다.
⚠ 리포트 본문(3곳)의 `51X차`는 **고치지 않았다** — 본문 수정 금지 규약. 이 절이 포인터다.

## ⑥ 사용자 조치 — 위 「사용자 조치」 5개 중 남은 것

| # | 상태 | 내용 |
|---|---|---|
| 1 | 🔴 **남음** | 09-01 저녁 "매수 3계약 매도" 지시가 왜 반영 안 됐는지 알려주세요 |
| 2 | 🔴 **남음** | 정체불명 매매 38건·오늘 손실 포지션이 본인 것인지 알려주세요 |
| 3 | 🔴 **남음** | 대신증권 계좌·오늘 정산(-556만원) 직접 확인 |
| 4 | 🔴 **남음** | F-1(마감 전 잔여 포지션 자동청산) 승인 여부 검토 — 자동조치는 손댈 수 없는 항목입니다 |
| 5 | ✅ **완료(자동조치가 처리)** | `.git/index.lock` — 이 세션이 지웠습니다. `git_lock_guard` 기준 스테일(0바이트·git 프로세스 0개)임을 확인한 뒤 회수했고, 이 PC는 윈도우 네이티브라 권한 문제 없이 지워졌습니다(리포트가 겪은 `Operation not permitted`는 리눅스 샌드박스 제약) |

**새로 생긴 사용자 조치**: 🔴 **미륵이 재기동**이 필요합니다(2분). F-3·G-1은 `main.py`
변경이라 **다음 재기동부터 실효**합니다. 재기동 전까지는 진입 출처 라벨이 계속 고착되고
재기동 경보도 예전 문구 그대로입니다.


---

# 제5부. 사용자 지시 구현 — CB② 복원 · 정지 경보 · 마감 잔여 자동청산 (MW0601 519차)

> 제4부(자동조치)가 **C등급으로 미뤘던 세 항목**을 사용자가 직접 지시해 구현한
> 결과다. 앞 절 본문은 고치지 않았다(하루 한 파일 · append 규약).
> 커밋 `d03b629`. 🔴 **재기동해야 실효된다.**

## ① 무엇이 바뀌었나 (평문)

| 무엇을 | 오늘 이전 | 지금 |
|---|---|---|
| **손절이 연달아 날 때** | 아무리 연달아 잃어도 그날 거래를 멈추지 않았습니다(모의투자용 임시 설정) | **5분 안에 서로 다른 3포지션을 손절하면 그날 거래를 멈춥니다** |
| **프로그램이 멈췄을 때** | 15초 이상 멈춰도 화면에는 아무것도 안 뜨고 파일 기록에만 남았습니다 | **「2 경보」 탭에 빨간 줄로 뜹니다** — 몇 초 멈췄는지, 무엇을 확인할지까지 |
| **마감(15:40)에 포지션이 남아 있을 때** | 경고만 하고 그대로 밤을 넘겼습니다 (어제가 그랬고 오늘 아침 -530만원이 됐습니다) | **자동으로 시장가 청산을 시도합니다** (최대 3번·12초, 안 되면 포기하고 알림) |

**공통으로 지킨 것**: 셋 다 **오래 붙잡지 않습니다.** 특히 마감 청산은 시간 제한이
있어서, 실패하더라도 마감 절차(다음날 쓸 모델 재학습·저장)를 멈추지 않습니다.
마감이 멈추면 손해가 더 크기 때문입니다.

## ② CB② 복원 — 9999 → 3

- **값 선택**: **3**. 절대원칙 ② 문구("5분 내 손절 3연속")와 일치하고, 실전 전환
  게이트 판정기(`_CB2_RESTORED_MAX = 3`)도 이 값을 「복원됨」으로 본다. 허용 범위
  2~3 중 **보수적인 쪽**이다(2면 5분 안에 두 포지션만 잃어도 그날이 끝난다).
- **왜 지금 해도 안전한가 — 선행조건은 489차에 이미 끝나 있었다.** 그때 계측이
  문구와 어긋나 있었다: ⓐ 시간창이 없어 하루 종일 흩어진 손절도 "연속"으로 쌓였고
  ⓑ 청산 **레그** 단위로 세어 한 포지션의 계단식 손절이 2카운트였다. 그 상태로
  한도를 되돌렸다면 **포지션 하나로 당일 정지**가 성립했다 — 규칙이 뜻한 바가 아니다.
  489차가 둘 다 고쳤고(300초 창 + `entry_ts` 중복제거), 호출부 4곳이 전부 키를
  넘기는 것을 오늘 다시 확인했다.
- 🔵 **오늘 실측으로 보면 복원해도 오늘은 안 멈췄다.** 이 리포트 ③표는 "CB② 카운터
  2건"이라 적었는데, 로그는 08:45:10과 10:21:18에 **각각 「연속 손절 1회」** 다 —
  96분 간격이라 5분 창이 사이에서 비웠다. **연속 카운터 최고치 1**, 한도가 2였어도
  정지 없었다. 이 복원은 "오늘부터 자주 멈춘다"는 뜻이 아니다.
  덤으로, 08:45의 3레그 청산(-5,299,668원)이 **1회로만** 세어진 것이 489차 중복제거가
  살아 있다는 직접 증거다.
- **기대값을 7곳 함께 옮겼다** — `settings.py` · 수집기 불변식 + §5 안내문 ·
  `test_489`(H) · `test_514` 토글표 · `CLAUDE.md`(절대원칙 §2 + 전환기준 ⑤) ·
  `invariants.md` ×2 · `report_template.md`. ⚠ 한 곳이라도 남으면 **다음 점검이
  정상을 이상으로 판정**한다(461차 F-3와 같은 유형).
- ⚠ **실전 전환 기준 ⑤는 아직 충족이 아니다.** v9 계획이 「복원 후 **정상 발동
  1회 확인**」을 함께 요구하므로 게이트는 여전히 `UNMEASURED`를 돌려준다.
  **발동을 인위적으로 만들지 말 것** — 5분 내 서로 다른 3포지션 손절은 드물다.

## ③ 메인 스레드 정지 → 「2 경보」 탭

- 🔴 **여기는 G-1과 원인이 다르다.** 오늘 자동조치가 고친 G-1(재기동 잔량 경보)은
  *"훅은 이미 있는데 문구가 안 읽혔다"* 였다. **여기는 훅 자체가 없었다** —
  `main_dashboard.py:75`의 `logger = logging.getLogger("SYSTEM")`는 **파일 로그
  전용**이고, 경보 탭은 `log_manager`를 타는 것만 받는다. 증상만 닮았지 다른 결함이다.
- **오늘 실측**: 11:28:57에 **56,875ms**(56.9초) 정지 — 직전 최대의 2.6배. 경보 탭은
  조용했고 사람이 안 것은 12:26 장중 점검이었다.
- **폭주 위험 없음**: ALERT(≥15초)는 전 로그 기간 **5건 / 5거래일**이다 —
  08-24 20,985 · 08-25 21,781 · 08-28 17,390 · 08-31 15,157 · 09-02 56,875ms.
  ⚠ 리포트 3i가 말한 *"표본 1건"* 은 **56.9초라는 크기**가 처음이라는 뜻이었지
  ALERT 건수가 1이라는 뜻이 아니었다 — 로그 실측으로 구분했다.
- ⚠ **임계(15초)는 바꾸지 않았다.** 재보정은 표본이 더 필요하고 **26주 WFA 항목**이다.
  바뀐 것은 *"이미 ALERT로 판정된 것을 화면에도 띄우는가"* 하나뿐이다.
  WARN(5~15초)은 **일부러 제외** — 오늘만 4건, 08-25 실측 28건이라 올리면 경보 탭이
  잡음으로 덮인다.
- 일일 상한 10건을 두되, **상한에 닿으면 닿았다는 사실을 한 줄 남기고** 그친다
  (조용히 끊지 않는다).

## ④ F-1 — 마감(15:40) 잔여 포지션 자동청산

- **메우는 구멍**: 15:10 강제청산과 15:18 안전망은 **그 뒤에 생긴 포지션을 구조적으로
  못 본다.** 어제 15:34:46에 들어온 외부 매수 3계약이 정확히 그 창으로 들어와 밤을
  넘겼고, 오늘 아침 08:45:11 하드스톱으로 **-5,299,668원**이 됐다.
  514차가 마감 진입부에 **탐지**를 넣었지만 경보뿐이라, 그 시각 사람이 화면 앞에
  없으면 아무 일도 일어나지 않았다. 이제 그 자리에서 **직접 청산한다.**
- **반드시 유한하게 끝난다**: 시도 **3회** + 벽시계 **12초**. 넘기면 포기하고 경보를
  남긴 뒤 **마감을 계속 진행한다** — 마감이 멈추면 다음날 쓸 모델 재학습·스케일러
  재적합·세션 저장이 통째로 날아가 피해가 더 크다.
- 🔵 **시한 12초는 우연이 아니다.** 정지 경보 임계(15초)보다 **짧게** 잡아,
  안전장치가 자기 자신 때문에 정지 경보를 울리는 모양을 피했다. 테스트가 이
  부등식(`청산 시한 < ALERT 임계`)을 고정한다.
- **새 주문 경로를 만들지 않았다** — 15:10·15:18이 쓰는 검증된
  `_ts_broker_direct_force_exit()`를 그대로 재사용한다. 대신 **읽기전용 프로브**를
  하나 신설했는데, 기존 함수의 반환값이 *"주문을 보냈는가"* 라서
  **「이미 FLAT」과 「조회 실패」를 같은 값으로 뭉개기** 때문이다. 그 둘을 구분하지
  못하면 "언제 그만둘지"를 판단할 수 없다.
- **가짜 브로커로 5가지 상황을 실제로 돌려봤다**:

  | 상황 | 결과 |
  |---|---|
  | 잔량 3계약·정상 체결 | **1시도 1주문 → 0계약, 청산 완료** |
  | 이미 FLAT | **0시도 0주문** — 불필요한 주문을 내지 않는다 |
  | 주문은 나가는데 체결 안 됨 | 3시도 후 `잔량 3계약`으로 **정직한 실패 보고** |
  | 잔고 조회 실패 | **주문 0건**, `잔량 **미측정**`으로 보고 — 눈감고 쏘지 않는다 |
  | 브로커가 주문 거부 | 3시도 0성공, 실패 보고 |

  전부 0.05초 내 종료, 예외 전파 0.
- ⚠ **이중 청산 위험 없음**: 같은 시간대에 주문 낼 수 있는 다른 경로는 외부 가드
  (`force_flat_guard.py`)뿐인데 그쪽은 **주문 권한이 꺼져 있고 미구현**이다.
  ⇒ 🔴 **그 플래그를 켜는 논의는 앞으로 F-1과 한 묶음이다** — 둘 다 켜면 15:39
  가드와 15:40 마감이 같은 잔량에 두 번 주문한다. 테스트가 이 조건을 고정한다.

## ⑤ 검증

| 항목 | 결과 |
|---|---|
| 신규 `tests/test_519_cb2_stall_alert_daily_close_exit.py` | **29 passed** (CB② 8 · 정지경보 6 · F-1 12 · 문서정합 3) |
| 전체 스위트 | **1,118 passed · 3 failed · 1 skipped · 4 xfailed** |
| 실패 3건 | **전량 선행**(내 변경과 무관) — `test_483`(형제 프로젝트 fuoption 사본 대조) · `test_504` ×2 |
| 대조 | 제4부 시점 1,089 + 신규 29 = **1,118** 정확 일치 — **신규 실패 0** |

**기존 가드 2개는 삭제하지 않고 범위를 옮겼다.** `test_514`의 토글표는 *"자동조치가
넘지 않을 선"* 을 재는 것이지 *"값이 영원히 고정"* 이 아니다 — 승인된 변경이 나면
기대값을 함께 옮기고 근거를 적어야 한다. 그러지 않으면 **가드가 승인된 결정을 계속
위반으로 신고한다.** 마찬가지로 "마감은 경보만 한다"는 테스트는
**"임시 주문 경로를 마감 안에 만들지 않는다"** 로 재정의했다.

## ⑥ 사용자 조치

| # | 할 일 | 걸리는 시간 | 안 하면 |
|---|---|---|---|
| 1 | 🔴 **미륵이 재기동** | 2분 | 셋 다 적용되지 않습니다 — 오늘 바뀐 것이 하나도 동작하지 않습니다 |
| 2 | 재기동 뒤 며칠간 **거래가 갑자기 줄지 않는지** 확인 | 매일 1분 | CB②가 자주 걸리면 진입 표본이 줄어 검증이 느려집니다. 그러면 값을 다시 조정할 수 있습니다 |
| 3 | (기존) 09-01 매도 지시 미반영 경위 · 매매 38건 본인 여부 · 증권사 정산 확인 | — | 제4부 ⑥과 동일 — 그대로 남아 있습니다 |

