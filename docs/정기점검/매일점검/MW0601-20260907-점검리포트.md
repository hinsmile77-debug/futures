# 미륵이 일일 점검 — MW0601 / 2026-09-07

# 장전(pre)

- 점검 시각: 09:05 KST · PC `MW0601` · 브랜치 `v9-dev`
- HEAD `4bb2dd3` · 미커밋 511건(전수 EOL 변환으로 추정 — 아래 참조) · 당일 커밋 0건 · `.git/index.lock` 없음
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260907_pre.md`
- 직전 거래일: 2026-09-04(금) — 09-05·09-06은 주말(토·일)이라 거래일 아님, 이 기간 점검 리포트 없음(정상, 결측 아님)

## 0. 오늘 한눈에

**오늘 미륵이는 정상적으로 켜졌고, 개장 준비도 대체로 문제없이 끝났습니다. 다만 지난주 금요일부터 이어진 두 가지 문제가 있고, 그중 하나는 오늘 아침 원인이 거의 확인됐습니다.**

첫째, 어제저녁(정확히는 지난 금요일 저녁) 모델을 다시 학습시켰다는 기록이 오늘 아침에도 또 사라져 있었습니다. 이 문제는 지난 화요일부터 벌써 세 번째 거래일째 반복되고 있는데, 오늘 프로그램 코드를 직접 확인한 결과 원인을 사실상 찾아냈습니다 — 날짜가 바뀔 때 어제 기록을 통째로 새 기록으로 갈아 끼우면서 중요한 표시 두 개를 놓치는 자리가 코드에 있습니다. 다행히 이 문제는 실제 거래에 영향을 주지는 않았습니다 — 프로그램이 다른 방법(파일을 직접 확인하는 방법)으로 "어제 학습이 잘 끝났다"는 것을 이미 확인했고, 그래서 오늘 아침 불필요한 재학습을 하지 않고 정상적으로 건너뛰었습니다. 즉 겉보기 기록만 비어 보일 뿐 실제 판단에는 문제가 없었습니다.

둘째, 더 중요한 문제입니다. 지난 목요일부터 나흘 연속으로, 미륵이가 낸 적 없는 주문이 계좌에 들어오는 일이 있었고, 목요일 하루에만 62번(66계약)이나 있었습니다. 오늘 아침에는 아직 이 문제가 다시 나타났는지 확인할 수 없습니다(개장한 지 1분밖에 안 지난 시점에 이 보고서를 작성하고 있습니다). 이 문제는 아직 원인이 밝혀지지 않았고, 사용자님께서 "이 계좌를 다른 방법(HTS·MTS 등)으로 직접 거래한 적이 있는지"를 아직 확인해 주시지 않은 상태입니다. 오늘 장중에 이 문제가 다시 나타나는지가 오늘 점검의 최우선 확인 사항입니다.

그 밖에 개장 준비(자동매매 프로그램 켜짐, 시세 연결, 오늘 시장 분위기 판단, 설정값 확인)는 전부 정상이었습니다. 다만 개장 시각(오전 9시)에 프로그램 내부가 28.8초 동안 멈췄던 순간이 있었는데, 이것도 개장 직후에 원래 종종 있는 현상이지만 최근 관찰 기간 중 가장 길었던 값이라 계속 지켜볼 필요가 있습니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 조건부 정상 | 개장 준비·모델·시세·설정 전부 정상. 재훈련완료 기록 소실은 3거래일째지만 원인 확정 + 실거래 영향 없음(1-1). 개장 순간 28.8초 정지는 기존 관찰 대상 중 최댓값(1-2) |
| 장중 | (미실행) | |
| 장후 | (미실행) | |

**절대원칙 준수 여부** (장전 시점 기준 — 아직 거래 없음)

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | 해당 없음(장전) | 15:10 이후 판정은 장후에 확인 |
| ② | Circuit Breaker 5종 | 정상 | `CB_CONSEC_STOP_LIMIT=3`(2026-09-02 519차 복원값) 확인. 오늘 발동 로그 0건(거래 시작 전) |
| ③ | CORE 피처 호라이즌 그룹별 | 정상(장전 확인 범위) | `CORE_FEATURES_BY_GROUP` 설정 변경 없음. 실제 진입은 장중에 확인 |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 정상 | 당일 로그 전수 검색 `0xC0000409`·`STACK_BUFFER` 0건 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 정상 | 관련 오류 로그 0건 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 정상 | 자동 통합 흔적 0건, 설정 변경 없음 |

**설정 불변식** — 수집기 §3 표 24개 항목 **전부 `일치`**. 차단 게이트(자동진입을 막을 수 있는 스위치) 34개 중 9개가 꺼져 있는데, 전부 이미 CLAUDE.md에 기록된 의도된 예외이고 새로 바뀐 것은 없다.

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

없음(장전 시점, 아직 거래 없음). 단 아래 「확인 필요」 최우선 항목을 볼 것 — 지난 4거래일 연속 재발한 정체불명 외부 진입이 오늘도 재발할 위험이 가장 크다.

### P1 — 정확성·관측 훼손

#### 1-1. 재훈련 완료 기록(`session_state.json`)이 오늘 아침에도 사라져 있었다 — 3거래일째 재발, 원인 사실상 확정 (원 항목: 2026-09-03 이상점 1-1 → 2026-09-04 이상점 1-1 "2일 연속 재발")

- **증상**: `data/session_state.json`에 `p8_last_success_date`(전일 스케일러 최종 재적합 완료일)와 `eod_retrain_ok_date`(전일 EOD 재학습 성공일) 두 키가 오늘 아침에도 없다.
- **근거**:
  ```
  data/session_state.json (2026-09-07 08:46:00 기록)
  {"date": "2026-09-07", "count": 1, "reverse_entry_enabled": false,
   "tp1_single_contract_mode": "atr_profit", "auto_shutdown_done_date": "", "today_open": 1088.96}
  → p8_last_success_date, eod_retrain_ok_date 둘 다 키 자체가 없음

  logs/retrain_eod_20260904.log:135-137 (지난 금요일 저녁 — 정상 기록됐었다)
  2026-09-04 15:48:48 [INFO] EOD_RETRAIN: [P8] 스케일러 재적합 완료 n=500봉 elapsed=0.04s
  2026-09-04 15:48:48 [INFO] EOD_RETRAIN: [P8] session_state p8_last_success_date + eod_retrain_ok_date 기록 완료

  logs/20260907_SYSTEM.log:1861 (오늘 08:55:07 — 그래도 실제 판단은 정상이었다)
  2026-09-07 08:55:07 [INFO] SYSTEM: [PreRetrain] EOD 마커 파일 직접 확인 (3일 전: 2026-09-04)
    → PreRetrain 스킵 (session_state 미기록 보완)
  ```
- **원인 — 오늘 코드를 직접 확인해 사실상 확정**: `strategy/runtime/session_recovery_service.py:117-137`의 `increment_session()` 함수가 날짜가 바뀔 때(`data.get("date") != today`) 어제 상태를 통째로 새 5개 키짜리 딕셔너리로 교체한다(120~129행). 이 새 딕셔너리에는 `date`·`count`·`reverse_entry_enabled`·`tp1_single_contract_mode`·`auto_shutdown_done_date` 5개만 있고, 어제 값에 있었던 `p8_last_success_date`·`eod_retrain_ok_date`를 이어받는 코드가 없다. 오늘 아침 실제 파일 내용(위 근거)이 정확히 이 5개 키 모양과 일치한다.
  이 가설은 지난 목요일(09-04) 532차 점검이 이미 세운 것이고, "3거래일 연속 재현 시 확정"이라는 판정 기준을 미리 정해 뒀다(사전등록, `dev_memory/NEXT_TODO.md` 532-5). 09-03(1회)·09-04(2회)에 이어 오늘(09-07, 주말 제외 3번째 거래일)도 같은 모양으로 재현돼 **판정 기준을 충족했다** — 판정 유보에서 확정으로 올린다.
  ⚠ 다만 사전등록된 확인 수단인 `[SessionStateDrop]` 로그(531-2/G-1 계측)는 오늘 기동 로그에 나타나지 않았다 — 이 계측은 "쓰기 시점" 소실만 잡고, 이번처럼 "새 딕셔너리 구성 시점에 애초에 안 넣는" 경로는 잡지 못하는 것으로 보인다(계측 사각지대, 별도 확인 필요 — 아래 F-1 참조).
- **기준 위반**: 계측 4원칙 ④(폴백 가시화 — 이 경우는 폴백이 아니라 상태 소실이지만 같은 계열: "값이 없다"가 조용히 일어났다). CLAUDE.md는 "P8이 빠지면 다음날 시초 z-score가 흔들린다"고 경고하는데, 오늘은 `[PreRetrain]`이 파일을 직접 대조하는 별도 경로로 이 위험을 막았다.
- **영향**: 사용자나 점검자가 `session_state.json`만 보고 "어제 재학습이 안 됐다"고 오해할 수 있다. 다만 실제 재학습·스케일러 재적합은 정상적으로 매일 밤 일어나고 있고, 오늘 아침 판단(재학습 건너뛰기)도 올바르게 내려졌다 — **오늘 거래 자격에는 영향이 없다.**
- **영향(기술)**: `PreRetrain`이 `session_state.json`이 아니라 EOD 마커 파일을 직접 읽는 방어 경로(483차 이전부터 존재)가 있어 이번에도 정상 동작했다. 그러나 이 마커 두 키를 참조하는 다른 소비처(`main.py:5640` 부근, EKS 등)가 있다면 그쪽은 이번 소실의 영향을 받을 수 있어 아직 전수 확인이 안 됐다.
- **신규 여부**: 재발(3거래일째) — 원 항목 2026-09-03·2026-09-04 이상점 1-1. 오늘 새로 확정된 것은 "원인"이다.

#### 1-2. 개장 순간(09:00:28) 메인 스레드가 28.8초 멈췄다 — 최근 관찰 기간 중 최댓값

- **증상**: 프로그램의 메인 처리 스레드가 09:00:28에 28,797ms(28.8초) 동안 응답하지 않았다. 이 시점은 `[MainStallTrace]`(정지 원인 진단용 스택 스냅샷)가 자동으로 기록됐다.
- **근거**:
  ```
  logs/20260907_WARN.log
  2026-09-07 09:00:28 [ERROR] SYSTEM: [LiveDBG] _tick_header 간격 28797ms — 메인 스레드 블로킹 발생
    | pipe_elapsed=0 watchdog_alerted=[] | [MainStall] stall_ms=28797 band=ALERT since_pipe_s=0.3
  2026-09-07 09:00:28 [WARNING] SYSTEM: [MainStallTrace] 스택 스냅샷 기록 (1/20)
    → logs/mainstall_traceback_20260907.log
  2026-09-07 09:00:05 [WARNING] SYSTEM: [PipePerf] total=4962ms | S0=10ms S1=85ms S2=0ms S3=0ms
    S4=1313ms S5=1216ms S6=1996ms S7=308ms S8=34ms
  ```
- **기준 위반**: 위반은 아니다 — 이 28.8초는 CB⑤(파이프라인 지연 5초 초과 시 즉시 청산, CLAUDE.md 절대원칙 §2)가 재는 "9단계 파이프라인 경과시간"과는 **다른 단위**다. 같은 시각의 파이프라인 총 소요시간(`PipePerf total`)은 4,962ms로 정상 범위(개장 버스트 완화 기준 9,000ms 이내)였다. 즉 정지의 대부분(28,797 − 4,962 = 23,835ms, 전체의 83%)이 9단계 파이프라인 **바깥**(COM 콜백·Qt 화면 갱신·다른 타이머 등)에서 일어났다는 뜻이며, 이 구간은 CB⑤도 동결 워치독(FZ-1, 180초 기준)도 보지 않는 설계상 공백 구간이다.
- **영향**: 오늘은 포지션이 없는(FLAT) 상태였고 진입 시도도 없었던 시간대라 실제 손익 영향은 없었다. 다만 이런 정지가 진입 직후나 청산 직전에 일어나면 주문 지연·체결 지연으로 이어질 수 있다.
- **영향(기술)**: 이 현상은 새로운 것이 아니다 — 2026-08-20(482차) 이후 "개장 버스트 메인 스레드 정지"로 이미 등록돼 2주 넘게 그림자 계측(`MAIN_THREAD_STALL_*`)으로 관찰 중이며, 26주 정기 재검증 주기에 편입돼 있다. 다만 지금까지 관찰된 최댓값(2026-08-25, 21,781ms)을 오늘 28,797ms가 경신했다 — 약 32% 증가. 관찰 시작(08-20)으로부터 이미 2주 이상(약 18일) 지났는데도 아직 26주 주기 재검토 시점 전이라 결론을 내리지 않은 상태다.
- **신규 여부**: 지속(격상 아님, 기존 관찰 채널의 새 최댓값 데이터포인트) — 원 채널: 482차 F-3 그림자 계측.

### P2 — 운영 부담·기술부채

없음(신규).

### 확인 필요 (확정 아님) — 오늘 최우선 관찰 대상

- **정체불명 외부 진입 재발 여부** — 지난 목요일(09-04) 하루에만 62건/66계약이 미륵이가 낸 적 없는 경로로 계좌에 들어왔고(원인 미규명, 09-01부터 누적), 15:10 강제청산 전에는 매번 포지션이 비워져 최악의 오버나이트 손실은 피했지만 근본 원인은 여전히 모른다. **이 보고서 작성 시점(09:05)은 개장 후 5분밖에 지나지 않아 오늘 재발 여부를 아직 확인할 수 없다** — 오늘 로그 전수 검색 결과 `[ExternalEntry]` 경보 0건(정상적으로 아직 없는 것이지, 문제가 해결됐다는 뜻이 아니다). 장중·장후 점검에서 반드시 제일 먼저 확인할 것.
- **`[ConfFloorGuard]`(자동진입 최소 신뢰도 문턱을 못 넘는 상태) 09:00:02 발동** — 보정기 출력 상한 0.3479가 오늘 요구되는 최소 신뢰도 0.4180보다 낮아 "이 상태에서는 어떤 신호도 자동진입 문턱을 넘을 수 없다"는 경보가 났다. 이 자체는 **이미 몇 주째 반복되는 잘 알려진 패턴**이고(출력 상한이 6일 연속 0.3479로 고정되는 현상이 이미 별도로 조사돼 "시장 저신호 쪽에 약한 정황, 확정 아님"으로 판정 유보 중이다), 오늘 새로 생긴 문제가 아니다. 개장 초반 자연 해소되는지 장중에 확인할 것.
- **미커밋 변경 511건, 실질 변경 규모 미확인** — `git status`가 511개 파일을 "변경됨"으로 표시하지만, 표본 확인(CLAUDE.md 전체)한 결과 내용은 동일하고 줄바꿈 문자(Windows CRLF ↔ Linux LF)만 전부 바뀐 것으로 보인다(2,224줄이 통째로 "삭제 후 추가"로 표시되지만 실제 텍스트는 같다). 이 보고서를 만든 리눅스 환경(코웍)에서 본 결과라 실제 Windows 작업 PC의 `git status`와는 다를 수 있다 — **실제 PC에서 직접 확인이 필요**하며, 이 착시 때문에 `git add .`처럼 통째로 커밋하면 안 된다(SKILL.md 기존 경고).

### 이미 반영된 사안 (신규 아님)

- **`[CybosProbe]` 초기 접속 실패 10건(08:58:11·08:58:40)** — `CoInitialize가 호출되지 않았습니다` 등 COM 초기화 타이밍 오류. 이미 "O-8 종결"로 조사가 끝난 매일 반복되는 정상 패턴이다(기동 초반 COM 초기화 전에 탐침이 먼저 돌아 생기는 현상이며, 곧 자동 복구된다). 오늘도 이후 재시도에서 정상 복구됐고 시세·주문 경로에는 영향이 없었다.
- **`[Calibration]` 축퇴(보정기가 한쪽 값으로 쏠려 사실상 무의미해진 상태) 감지 150건(08:40:50~08:40:59)** — 매일 개장 직전 예열(warmup) 과정에서 정상적으로 나타났다 정상 해소되는 패턴. 신규 결함 아님.
- **`joblib` 버전 표기 불일치(CLAUDE.md "1.1.1" vs 실제 설치·`requirements.txt` "1.1.0")** — 실제 운영 환경은 `requirements.txt`에 명시된 1.1.0과 정확히 일치해 동작에는 문제가 없다. CLAUDE.md 표의 숫자가 낡은 것으로 보인다 — 다음 문서 정리 때 고칠 것(P2, 아래 Fix 참조).

---

## 2. Fix 작업 구현계획

### F-1. `increment_session()`의 날짜 전환 시 상태 교체가 재학습 완료 마커 2종을 누락시킨다 — P1 · 대응 이상점 1-1

- **원인 가설**: 위 1-1 근거대로 `strategy/runtime/session_recovery_service.py:120-129`가 새 딕셔너리를 만들 때 `p8_last_success_date`·`eod_retrain_ok_date`를 옛 데이터에서 이어받지 않는다.
- **변경 파일**:
  - `strategy/runtime/session_recovery_service.py` — `increment_session()`: 120~129행 새 딕셔너리 생성부에 `"p8_last_success_date": data.get("p8_last_success_date", "")`, `"eod_retrain_ok_date": data.get("eod_retrain_ok_date", "")` 두 줄 추가(날짜가 바뀌어도 두 마커는 "그날의 상태"가 아니라 "마지막으로 성공한 날짜"이므로 이어받는 것이 맞다 — 값 자체가 날짜 문자열이라 소비처가 날짜 비교로 최신성을 판단하므로 역방향 부작용 없음, 이미 532차가 안전성 확인 완료).
  - (부가) `531-2/G-1`의 `[SessionStateDrop]` 계측(`_write_session_state()` 감싸는 부분)이 이번 "새 딕셔너리 구성" 경로의 소실은 잡지 못한 것으로 보인다 — 계측 지점을 `increment_session()` 자체로 확장할지 검토.
- **회귀 위험**: 낮음. 두 마커는 읽기 전용으로만 소비되고(`main.py:5640` 부근 `[PreRetrain]`, EKS), 이번 변경은 "이미 있던 값을 안 지운다"는 것뿐이라 새로운 부작용 경로가 생기지 않는다.
- **절대원칙 저촉 여부**: 없음. 매매 로직·게이트·한시예외 어느 것도 건드리지 않는다.
- **런타임 주의**: `py37_32`(장중 본체) 코드. COM 콜백 밖의 일반 함수라 dynamicCall·emit 제약과 무관.
- **검증 방법**: 다음 날짜 전환 시(내일 09-08 첫 기동) `session_state.json`에 두 키가 살아 있는지 직접 확인. 회귀 테스트 `tests/test_5XX_session_state_marker_carryover.py` 신설 제안(날짜 전환 시뮬레이션 후 두 키 보존 확인).
- **섀도 계측 필요 여부**: 불필요(순수 버그 수정, 게이트·차단 로직 아님).
- **적용 시점**: 장후(15:10 이후). 장전인 지금은 라이브 프로세스가 돌고 있어 코드를 변경하지 않는다.
- **결정 필요 사항**: 없음 — 이미 532차가 원인 후보와 안전성을 확인해 뒀고, 오늘 3거래일 재현으로 사전등록 확정 기준을 충족했다. 적용 승인만 있으면 된다.

### F-2. (재상정, 변경 없음) 정체불명 외부 진입 발생원 규명 — P0 · 대응 「확인 필요」 최우선 항목

- 09-04 리포트의 F-2를 그대로 승계한다. 오늘 새로 확인된 것은 없다(아직 개장 5분 경과). 09-04 실측(pending_miss 66건 전부 `pending=NONE` — 엔진 내부에 대응하는 대기 주문이 아예 없었음)은 "다른 경로(HTS·MTS)로 직접 계좌를 조작하고 있다"는 가설 쪽에 조금 더 무게를 두게 했으나 확정은 아니다.
- **변경 파일**: 없음 — 조사 항목이며, 근본 차단(주문 실행 경로 변경)은 이미 C등급(주문·청산 경로 변경)으로 분류돼 자동조치 대상이 아니고 주간회의 승인 대기 상태다.
- **적용 시점**: 해당 없음(조사·사용자 확인 대기).

### 적용 순서와 커밋 계획

1. F-1 적용 — 장후, 재기동 전. 커밋 메시지 `[MW0601] 535차: session_state 날짜전환 시 재학습완료 마커 2종 유실 수정(F-1)`.
2. F-2는 코드 변경 없음 — 사용자 확인(아래 사용자 조치) 대기.

> 장전 국면이므로 본 계획은 수립만 하고 적용하지 않는다. 라이브 프로세스가 돌고 있다.

---

## 3. 고도화 방안

### G-1. `increment_session()`의 날짜 전환 자체를 로그로 남긴다

- **관측 근거**: 오늘 원인 확정 과정에서, "언제 날짜가 바뀌어 새 딕셔너리가 만들어졌는지"를 로그가 아니라 `session_state.json` 최종 기록 시각(08:46:00)으로 역산해야 했다. 날짜 전환 자체가 로그에 남지 않는다.
- **제안 내용**: `increment_session()`의 `if data.get("date") != today:` 분기에 `[SessionRollover] {어제날짜} → {오늘날짜} 전환 — 이어받은 키: {...} / 새로 초기화된 키: {...}` 형태로 INFO 로그를 남긴다.
- **기대 효과**: 다음에 비슷한 "매일 밤 기록된 값이 아침에 사라지는" 종류의 문제가 생기면, 로그 한 줄로 즉시 "날짜 전환 시 유실"인지 다른 원인인지 구분할 수 있다.
- **비용·위험**: 낮음. 로그 한 줄 추가, 매매 로직 무변경.
- **선행 조건**: F-1과 같은 커밋에 포함 권장(같은 함수).
- **우선순위 제안**: 이번 주(F-1과 함께).

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| 482차 F-3 그림자 계측(메인 스레드 개장 정지) | 26주 WFA 주기 항목 | 관찰 시작(08-20)로부터 이미 18일 경과 + 오늘 새 최댓값(28.8초) 경신 — 26주를 기다리지 않고 중간 점검 1회를 이번 주 안에 권고 | 최댓값이 2주 만에 계속 경신되는 추세라면 26주까지 방치하기엔 이르다 |

---

## 4. 진입 승패 사후검증 (장후 한정)

해당 없음(장전 전용, 장후 절에서 다룬다).

## 5. 수익률 향상방안 (장후 한정)

해당 없음(장전 전용, 장후 절에서 다룬다).

---

## 6. 다음 거래일 관측 예정

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| O-p1 | `[ExternalEntry]` 재발 여부(오늘 장중·장후) | 0건이면 09-04 대비 개선 정황, 1건 이상이면 5거래일 연속 | 2026-09-07 장후 |
| O-p2 | 내일(09-08) 기동 시 `session_state.json`에 F-1 미적용 상태로 재현되는지(F-1이 아직 미적용이면) | 두 키 재소실 여부 | 2026-09-08 장전 |
| O-p3 | 09:00대 메인 스레드 정지 최댓값 추이 | 28,797ms 대비 증가/감소 | 2026-09-08 장전 |

## 7. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| CB② **발동 1회 관측** (값 복원은 2026-09-02 완료, `=3`) | 상시 관측 | — | 인위적 유발 금지, 자연 발생 대기 지속 |
| 482차 F-3 그림자 계측(메인 스레드 정지) 26주 재검토 | 2027-02-20경(관찰 시작 08-20 + 26주) | 약 24주 | 오늘 새 최댓값 경신으로 중간 점검 권고(위 G-1 표) |

---

## 8. dev_memory 반영

- `DECISION_LOG.md`에 오늘 세션(535차 — 장전 점검) 항목 append 예정(본 리포트 완료 후 별도 기록).
- `NEXT_TODO.md`에 F-1(session_state 마커 유실 수정, 원인 확정) 체크박스 추가 예정.
- 완료 처리한 기존 항목: 532-5(원인 후보 특정 → 3거래일 재현으로 **확정**으로 승격).

---

## 사용자 조치

| # | 할 일 | 내용 | 걸리는 시간 | 안 하면 생기는 일 |
|---|---|---|---|---|
| 1 | **최우선** — 정체불명 외부 진입 계좌 확인 | 지난 목요일까지 나흘 연속(09-01~09-04) 미륵이가 낸 적 없는 주문이 계좌에 들어왔습니다(목요일 하루만 62건/66계약). 이 계좌를 HTS·MTS 등 다른 방법으로 직접 거래하신 적이 있는지 확인해 주세요. 없다면 계좌 비밀번호·공동인증서·API 키 등 보안을 점검해 주셔야 합니다 | 5분(확인) | 원인 미규명 상태가 계속되고, 언젠가 15:10 마감 시각 이후에 이런 진입이 들어오면 지난 9월 1일처럼 밤새 포지션을 들고 가는 손실 위험이 다시 실현될 수 있습니다 |
| 2 | F-1 적용 승인 | 오늘 확정된 `session_state.json` 마커 유실 버그 수정(F-1) — 장후 재기동 시 적용 예정, 별도 승인 불요(코드 버그 수정, 매매 정책 무변경)이나 재기동이 필요합니다 | 재기동 1회(수 초) | 겉보기 기록이 계속 비어 보여 다음 점검 때마다 같은 항목을 재확인해야 합니다(실거래 영향은 없음) |
| 3 | `.git/index.lock` 수동 삭제 | 이 점검 세션이 git 조회 명령을 쓰던 중 0바이트 `.git/index.lock`이 남았다. `python scripts/git_lock_guard.py --check`로 재확인한 결과 **스테일 확정**(실행 중인 git 프로세스 0개)이었으나, 이 리눅스 점검 환경에서는 파일 삭제 권한이 없어(`Operation not permitted`) 세션 안에서 회수하지 못했다. 실제 작업 PC(Windows)에서 `del .git\index.lock` 또는 `git status`가 정상 동작하는지로 확인 후 필요 시 직접 삭제할 것 | 1분 | 저장소가 커밋 불가 상태로 남아 F-1 등 오늘 계획한 커밋을 못 하게 된다 |

---

이 절이 발급한 번호: 이상점 1-1~1-2 · 관측 예정 O-p1~O-p3

---

# 장중(intra)

- 점검 시각: 12:30 KST · PC `MW0601` · 브랜치 `v9-dev`
- HEAD `4bb2dd3`(장전과 동일, 오늘 커밋 없음) · 미커밋 522건(실질 8건 — 아래 이월표 참조) · 당일 커밋 0건 · `.git/index.lock` **여전히 잔존**(스테일 확정, 회수 재시도 실패 — 아래 참조)
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260907_intra.md`
- 점검 대상 시간대: 09:00 개장 ~ 12:30(현재 진행 중 — 장중 점검이라 15:10 마감 전 스냅샷임)

## ⚠ 즉시판단 — 지금 이 순간 계좌에 사용자가 지시하지 않은 포지션이 열려 있습니다

**12:30:03에 미륵이가 낸 적 없는 매수 주문 1계약(1092.78원 부근)이 계좌에 체결됐고, 이 보고서를 쓰는 지금까지 청산되지 않고 남아 있습니다.** 이런 일이 오늘만 15번째이고(누적 15계약), 지난주 목요일부터 5거래일 연속 반복되고 있습니다.

| 선택지 | 내용 | 손익/리스크 |
|---|---|---|
| ① 그대로 둔다 | 오늘 이미 있었던 14건의 정체불명 진입은 전부 미륵이의 자동 손절·이익실현 기능이 정상적으로 넘겨받아 마무리했습니다(원인 모를 진입이었어도 청산은 정상 작동). 15:10에는 강제로 전량 정리되는 안전장치도 있습니다 | 15:10 전까지는 시장이 불리하게 움직이면 추가 손실 가능. 지금까지 이 정체불명 진입들로 확정된 손실만 692,589원 |
| ② 지금 HTS·MTS로 직접 확인하고 필요하면 수동 정리 | 즉시 리스크를 없앨 수 있음 | 사용자의 직접 조작 필요 |
| ③ 계좌 보안(비밀번호·공동인증서·API 키) 지금 점검 | 원인이 바깥에 있다면 근본 차단 가능 | 지금 당장 손익에는 영향 없음. 원인 규명 전에는 반복 가능성 있음 |

**가장 시급한 것**: 이 계좌를 HTS·MTS 등 다른 방법으로 사용자님이 직접 조작한 적이 있는지부터 확인하는 것입니다. 5거래일 연속 재발했는데 아직 원인을 모릅니다. (이 판단은 사람이 하는 것이며, 이 점검 세션은 직접 매매에 개입하지 않습니다.)

## 0i-C. 이월 처리 — 장전 항목 처분

| 원 번호 | 제목(줄임) | 원 국면 | 처분 | 근거 |
|---|---|---|---|---|
| 1-1 | `session_state.json` 재학습완료 마커 소실(3거래일째, 원인 확정) | 장전 | 🔄 지속 | 장중에는 코드 수정을 하지 않으므로(라이브 프로세스 운용 중) 새로 볼 것 없음. 장후 재기동 전 적용 예정(F-1) 그대로 유효 |
| 1-2 | 09:00:28 메인 스레드 28.8초 정지 | 장전 | 🔄 지속 | 장중 재확인 결과 오늘 최댓값은 여전히 28,797ms(09:00:28) 그대로다. 이후 5초를 넘는 정지는 12:30까지 추가로 없었음(상위 7건: 28797/3969/3750/3547/3203/3016/2954ms) — 최댓값 갱신 없음 |
| O-p1 | 정체불명 외부 진입 재발 여부(오늘 확인) | 장전 | **판정 — 재발 확인, 격상** | 오늘 09:23:21~12:30:03 사이 15건/15계약 재발, 실현손실 692,589원 + 현재 미결제 1건 진행 중. 아래 신규 이상점 1-3으로 격상 등록 |
| O-p2 | 내일(09-08) 기동 시 세션스테이트 마커 재소실 여부 | 장전 | 다음 거래일 이월 | 장중 관측 대상 아님(내일 아침 기동 시 판정) |
| O-p3 | 09:00대 메인 스레드 정지 최댓값 추이 | 장전 | 다음 거래일 이월 | 오늘 값(28,797ms) 확정, 09-08 장전과 비교해 판정 예정 |
| 확인필요 | `[ConfFloorGuard]` 자동진입 하한 도달 불가(09:00:02) | 장전 | ✅ 해소 | 09:00:02 1회만 발생하고 이후 재현 없음. 10:18:01부터 엔진 자체 진입 4건이 정상적으로 발생해 "어떤 신호도 진입 못 한다"는 상태가 개장 초반 20분 내로 자연 해소됐음을 확인 |
| 확인필요 | 미커밋 변경 511건 — EOL(줄바꿈) 착시 | 장전 | 🔄 지속 | 오늘 장중 재확인 522건(실질 변경은 여전히 8건 — 코드 6건 + dev_memory 2건). 착시 해석 그대로 유효, 위험 아님 |
| 사용자조치#3 | `.git/index.lock` 수동 삭제 필요 | 장전 | 🔄 지속 | 이 세션이 `python scripts/git_lock_guard.py --reclaim` 재시도 → 여전히 `Operation not permitted`(리눅스 샌드박스 마운트 경유라 세션 내 삭제 불가, SKILL.md 기지 결함과 동일 패턴). 락은 3.4시간 이상 경과, 여전히 스테일 확정 상태 |

**미처분 0건.**

## 0i. 오늘 한눈에 (갱신)

**장중에도 프로그램 자체는 설계대로 잘 동작하고 있습니다 — 진입·청산·안전장치 전부 정상입니다. 문제는 프로그램 밖에서 벌어지고 있습니다.** 아침에 이미 우려했던 "미륵이가 내지 않은 주문이 계좌에 들어오는 문제"가 오늘도 재발했고, 정오까지 15번이나 반복되면서 이미 확정된 손실만 69만원을 넘었습니다. 방금 전(12시 30분)에도 또 발생했고, 그 포지션은 아직 정리되지 않은 채 남아 있습니다. 이 문제를 뺀 나머지 — 즉 프로그램이 스스로 판단해서 낸 매매(4건)는 이번에도 정상적으로 작동했고 소액 손실(약 7만9천원)에 그쳤습니다.

그 밖에 아침에 있었던 개장 순간의 28.8초 멈춤은 낮 동안 다시 나타나지 않았고, 프로그램의 다른 자동 안전장치(연속 손절 시 정지, 위험 신호 감지 등)도 전부 정상적으로 관찰만 하고 있을 뿐 잘못 작동한 곳은 없습니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 조건부 정상 | (장전 절 참조, 변경 없음) |
| 장중 | **이상 — P0 1건 진행 중** | 정체불명 외부 진입 5거래일 연속 재발, 오늘 15건/15계약, 실현손실 692,589원 + 미결제 1건. 이를 제외한 나머지(엔진 자체 판단·안전장치·설정)는 전부 정상 |
| 장후 | (미실행) | |

**절대원칙 준수 여부** (12:30 시점 기준)

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | 해당 없음(아직 15:10 전) | 12:30 현재 미결제 포지션 1건 보유 중(정체불명 진입) — 15:10 청산 이행 여부는 장후에 확인 |
| ② | Circuit Breaker 5종 | 정상 | `[CB] 연속 손절 1회(300초 창, 포지션 단위)` 6건 관측, 3회 연속 도달한 적 없음(`CB_CONSEC_STOP_LIMIT=3`) — 정상 카운팅. CB③-P4는 오늘 acc30m이 NORMAL→WATCH→RESTRICTED(10:37~10:39)까지 갔으나 차단 자체는 설계대로 비활성(한시예외) |
| ③ | CORE 피처 호라이즌 그룹별 | 정상 | 오늘 엔진 진입 4건 모두 `vwap` 포함 체크리스트를 통과한 상태에서 진입(미통과 항목은 `fore`·`ofi`·`cvd`·`prev`뿐 — `vwap` 미통과 진입 0건, 절대원칙 위반 없음) |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 정상 | 당일 로그 전수 검색 `0xC0000409`·`STACK_BUFFER` 0건(09:00~12:30 누적) |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 정상 | 관련 오류 로그 0건 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 정상 | 자동 통합 흔적 0건 |

**설정 불변식** — 장전과 동일, 변경 없음(수집기 §3 표 24개 항목 전부 `일치`).

---

## 0i-D. 병행 세션 확인 — 별도 딥다이브 파일 발견

같은 날 다른 세션이 **`docs/정기점검/매일점검/MW0601-20260907-맥점계측-딥다이브.md`**(09:01 작성, 534차 후속)를 별도로 냈다. 오늘 장전 리포트(09:11 완료)가 이 파일을 언급하지 않아 병행 세션 확인 절차(SKILL.md §0)에 따라 여기서 역링크한다.

- **결론 1줄**: 오늘 처음 라이브로 산출된 "당일 맥점"(장 시작 전 08:50에 미리 계산해 두는 오늘의 예상 고가·저가·매물대 구간) 기능은 값 자체는 정상 산출됐고, 다만 "왜 그 값이 나왔는지"를 기록하지 않는 계측 공백 5건을 찾아 코드를 고쳤다 — 그러나 **오늘은 아직 반영되지 않았다**(라이브 프로세스가 이미 구버전을 메모리에 올린 채 기동 중이라, 이 수정이 실제로 관측되는 것은 내일 2026-09-08 08:50부터다).
- **오늘 판정에 미치는 영향**: 없음. 매매 정책·수치·산출값 어느 것도 바뀌지 않았고(같은 입력으로 재현했을 때 비트 단위로 동일함을 그 문서 자체가 확인함), 커밋도 보류 상태다. 장중 절의 판정·손익 계산과는 무관하다.
- **참고**: 이 딥다이브는 관측·기록 계층의 개선이라 위 이상점 목록에는 올리지 않는다(매매에 영향 없는 계측 전용 변경).

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

#### 1-3. 정체불명 외부 진입 — 5거래일 연속 재발, 오늘 15건/15계약, 미결제 1건 진행 중 (원 항목: 장전 「확인 필요」 최우선 항목의 격상)

- **증상**: 미륵이 엔진이 낸 적 없는 주문이 계좌에 체결되는 일이 오늘도 반복됐다. 09:23:21에 처음 발생해 12:30:03까지 15건/15계약이 쌓였다. 이 중 7개 포지션은 이미 청산 완료(실현손익 -692,589원), 1개는 이 보고서 작성 시점(12:30)에도 청산되지 않고 남아 있다.
- **근거**:
  ```
  logs/20260907_WARN.log:28,35,41 (09:23:21)
  [ERROR] SYSTEM: [ExternalEntry] 🔴 미륵이가 내지 않은 진입이 계좌에 들어왔다 — SHORT 1계약 @ 1089.72 … 누적 1건/1계약
  (같은 초에 3건 연속 체결 — 누적 3건/3계약까지)

  logs/20260907_WARN.log:602 (12:30:03, 가장 최근 · 진행 중)
  [ERROR] SYSTEM: [ExternalEntry] 🔴 미륵이가 내지 않은 진입이 계좌에 들어왔다 — LONG 1계약 @ 1092.78
    (보유 1계약, 평균 1092.78). 오늘 누적 15건 / 15계약.

  logs/20260907_TRADE.log (12:30:03, 현재 미결제 포지션)
  [체결동기화] 외부진입 LONG 1계약 @ 1092.78 | 평균=1092.78 보유=1계약
  → 이후 청산 로그 없음(12:30:24 기준 보유 중)

  logs/20260907_HEALTH.log:09:31:00~09:58:00
  [CRITICAL] HEALTH: [Health] level=CRITICAL degraded=ON | exc_tags=[OrderSync]×12 [ExternalEntry]×6 [ExitCooldown]×1
    (외부 진입이 몰린 09:23~09:52 구간에서 예외 밀도가 급증해 CRITICAL 등급까지 격상됨, 10건)

  거래일 요약(수집기 §5) 포지션 11건 중 출처별 소계
  엔진 4건 -78,691원 · 외부 7건 -692,589원 (레그·포지션 단위 정합성 확인됨: 레그합 = 포지션합 = -771,280원)
  ```
- **기준 위반**: CLAUDE.md 절대원칙 6종 중 직접 해당하는 조항은 없다(이 문제 자체는 코드 버그가 아니라 계좌 바깥에서 벌어지는 일로 추정된다). 다만 **15:10 이후에 같은 일이 벌어지면 절대원칙 ①(오버나이트 금지)이 실질적으로 무력화된다** — 프로그램의 강제청산 로직은 "프로그램이 아는 포지션"을 정리하는 것이라 사용자가 원인을 못 찾은 채로 방치하면 이 경로 자체가 리스크가 된다.
- **영향**: 사용자가 지시하지 않은 매매로 이미 확정된 손실이 오늘 하루만 69만원을 넘었고(엔진 자체 매매 손실 7.9만원의 약 9배), 지금 이 순간에도 사용자가 모르는 포지션 하나가 시장 변동에 노출돼 있다.
- **영향(기술)**: 외부 진입 포지션은 `entry_horizon`이 없어 `[PositionFallback]`으로 TP1 배수가 기본값(1.00)으로 폴백되지만(F-5 대상, 원인 규명과 무관하게 이미 알려진 부수 결함), 청산 자체는 `[TickStop]`·하드스톱 로직이 포지션 상태 값만 보고 origin과 무관하게 동작해 지금까지의 14건 전부가 정상적으로 청산 절차(하드스톱 10건, TP1/TP2 부분청산 각 1건, 미추적체결정리 2건)를 탔다. 즉 **청산 안전망 자체는 origin에 관계없이 작동한다**는 것이 오늘 다시 확인됐다 — 남은 위험은 "그 사이의 노출"과 "원인 자체가 무엇인가"이다.
- **신규 여부**: 재발 — 09-01(531차, 47건/50계약)·09-04(532차, 최종 62건/66계약)에 이어 09-07(오늘, 12:30 시점 15건/15계약, 장 마감까지 진행 중)로 **5거래일 연속**. 09-04 리포트 F-2를 그대로 승계하며, 원인은 여전히 미규명이다. 오늘 09-04 대비 발생 빈도(계약수 기준)는 낮아 보이나 아직 12:30이라 최종 비교는 장후에 한다.

### P1 — 정확성·관측 훼손

없음(신규). (Health CRITICAL 격상 10건은 위 1-3의 직접 증거로 편입, 별도 항목화하지 않음 — 같은 원인)

### P2 — 운영 부담·기술부채

없음(신규).

### 확인 필요 (확정 아님)

- **사이저 출력(최대 2계약) vs 실제 진입(최대 1계약)** — 오늘도 사이저가 낸 배수보다 실제 체결 계약수가 작다(사이저 1계약×8·2계약×1 → 실제 1계약×4). 이미 알려진 `sizing_inversion_watch`(실전 전환 기준 ⑧) 채널의 관측 대상이며, 표본이 적어(오늘 4건) 판정하지 않는다.
- **`.git/index.lock` 지속** — 사용자 조치 필요, 위 이월표 참조.

### 이미 반영된 사안 (신규 아님)

- **장중 재학습(STEP 3) 3회 발생(09:55·11:12·11:51) — 전부 `[ConstOut]` 재적합 후속으로 정상 트리거됨.** CLAUDE.md·SKILL.md가 경고하는 "30분마다"는 코드에 없는 문구이며, 오늘 관측된 트리거는 이벤트 구동(호라이즌별 상수 출력 확정 → 스케일러 재적합 → GBM 재학습 예약) 그대로다. 09:54 `[ConstOut]['5m']` → 09:55 재학습 완료(호라이즌 5m), 11:11 `[ConstOut]['3m']` → 11:12 재학습(3m), 11:50 `[ConstOut]['3m']` → 11:51 재학습(3m) — 트리거·대상 호라이즌·재학습 완료 로그가 전부 1:1로 짝지어짐을 확인했다. 12:29에 4번째 `[ConstOut]['3m']`이 추가로 발생했으나 이 점검 시점(12:30) 기준으로는 아직 재학습 로그가 남지 않았다 — 정상 지연으로 판단(약 1분 후 완료).
- **CB③-P4 accuracy_buf·acc30m_stage 추적은 차단 비활성 상태에서도 정상 계속됨** — 10:37 NORMAL→WATCH, 10:39 WATCH→RESTRICTED(acc=26.7%) 전환 로그 확인. 절대원칙 §2 한시예외(`CB3_P4_GRADE_BLOCK_ENABLED=False`)대로 등급 차단 자체는 걸리지 않았다.
- **PSI(FP-CRITICAL 섀도 계측)는 하루 종일 0.005/level=0으로 안정** — 차단은 비활성이나 계산·로그가 유지되는 설계대로 정상.
- **멀티 호라이즌 예측(1·3·5·10·15·30분) 6종 전부 매분 발행·채점 확인** — 12:00 샘플에서 6개 호라이즌 전부 적중/실패 판정 로그 확인. 30m도 계속 채점되며 CB③ 입력원 역할을 하고 있다(절대원칙 §3 말미 규약대로 정상).

---

## 2. Fix 작업 구현계획

이번 장중 점검에서 새로 제안하는 코드 Fix는 없다. 정체불명 외부 진입(1-3)의 근본 원인이 코드가 아니라 계좌 접근 경로 바깥에 있을 가능성이 높아 보여, 09-04 리포트가 이미 이 항목을 C등급(주문·청산 경로 변경 — 자동조치 대상 아님, 주간회의 승인 필요)으로 분류해 둔 것을 그대로 유지한다. 장전 F-1(session_state 마커 유실 수정)도 계획 변경 없음 — 적용은 장후 그대로.

> 장중 국면이므로 코드 변경은 실행하지 않는다. 라이브 프로세스가 돌고 있다.

---

## 3. 고도화 방안

### G-2. 정체불명 외부 진입 누적 건수에 따른 단계적 경보 격상

- **관측 근거**: 오늘 15건이 쌓이는 동안 경보 수준은 매 건 같은 형태의 ERROR 로그 하나였다. 09-04(62건)·오늘(진행 중 15건)처럼 하루 안에서 건수가 늘어나도 사람이 "지금이 어제보다 심각한 수준인가"를 로그 문구만으로 즉시 판단하기 어렵다.
- **제안 내용**: `[ExternalEntry]` 누적 건수가 시간대별 임계(예: 09:00~10:00 사이 5건, 전체 20건 등 — 과거 5거래일 분포로 사전등록)를 넘으면 로그 레벨을 CRITICAL로 격상하고, 대시보드에 "오늘 원인불명 진입 급증" 배너를 띄운다. 매매 정책 변경이 아니라 **경보 계층**이므로 C등급(자동조치 금지 대상)에 해당하지 않을 가능성이 있다 — 정확한 등급 분류는 주간회의에서 확인.
- **기대 효과**: 사용자가 장중에 이 리포트를 안 봐도 대시보드만으로 심각도를 즉시 인지할 수 있다.
- **비용·위험**: 낮음(순수 관측·알림 계층). 다만 임계값 설정은 과거 데이터 기반 사전등록이 필요(313차 원칙 — 표본 5거래일로는 아직 임계 확정 금지, 우선 로깅만 격상하고 임계는 다음 26주 주기에 확정).
- **선행 조건**: 없음(F-2 원인 규명과 별개로 진행 가능한 계측 계층).
- **우선순위 제안**: 이번 주(경보 배너는 매매 로직과 무관해 장후 적용 가능).

### 로드맵·실전전환 기준 반영 제안

없음(신규 — 기존 482차 F-3 제안은 장전 절 그대로 유지).

---

## 4. 진입 승패 사후검증 (장후 한정)

해당 없음(장중 전용 절 아님, 장후 절에서 다룬다).

## 5. 수익률 향상방안 (장후 한정)

해당 없음(장중 전용 절 아님, 장후 절에서 다룬다).

---

## 6. 다음 거래일 관측 예정

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| O-i1 | 정체불명 외부 진입 12:30 이후~15:10 추가 재발 여부 및 최종 건수/계약수 | 장후 리포트에서 오늘 최종 집계(09-04의 62건/66계약과 비교) | 2026-09-07 장후 |
| O-i2 | 12:30:03 진입한 미결제 외부 포지션(LONG 1계약 @1092.78)의 청산 경로 | 하드스톱/TP1~3/15:10강제청산 중 무엇으로 닫히는지, 청산 지연 여부 | 2026-09-07 장후 |
| O-p2 | (승계) 내일 09-08 기동 시 session_state 마커 재소실 여부 | 두 키 재소실 여부 | 2026-09-08 장전 |
| O-p3 | (승계) 09:00대 메인 스레드 정지 최댓값 추이 | 28,797ms 대비 증가/감소 | 2026-09-08 장전 |

## 7. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| CB② **발동 1회 관측** (값 복원은 2026-09-02 완료, `=3`) | 상시 관측 | — | 오늘도 최고 연속 카운트 1회에 그침(300초 창 내 3회 미도달) — 인위적 유발 금지, 자연 발생 대기 지속 |
| 482차 F-3 그림자 계측(메인 스레드 정지) 26주 재검토 | 2027-02-20경 | 약 24주 | 장전과 동일, 오늘 추가 갱신 없음(최댓값 유지) |

---

## 8. dev_memory 반영

- `DECISION_LOG.md`에 오늘 세션(536차 — 장중 점검) 항목 append 예정(본 리포트 완료 후 별도 기록).
- `NEXT_TODO.md`에 정체불명 외부 진입 5거래일 연속 재발(오늘 15건/15계약) 최신 수치 갱신, git lock 재시도 실패 기록 추가 예정.
- 완료 처리한 기존 항목: O-p1(재발 여부 관측 → 재발 확정, 1-3으로 격상).

---

## 사용자 조치 (장중 갱신분 — 장전 표에 이어서)

| # | 할 일 | 내용 | 걸리는 시간 | 안 하면 생기는 일 |
|---|---|---|---|---|
| 1 | **최우선·변경 없음** — 정체불명 외부 진입 계좌 확인 | 장전과 동일 요청이며 오늘 12:30 현재도 미결제 포지션 1건이 남아 있어 더 급해졌다. HTS·MTS 등 다른 방법으로 이 계좌를 직접 거래한 적이 있는지, 없다면 계좌 비밀번호·공동인증서·API 키 보안을 지금 점검해 달라 | 5분(확인) | 15:10 마감 이후에 같은 일이 벌어지면 오버나이트 포지션 위험이 다시 현실화된다(9월 1일 전례) |
| 2 | (장전과 동일) F-1 적용 승인 | 변경 없음 | 재기동 1회(수 초) | 장전 절 참조 |
| 3 | (지속) `.git/index.lock` 수동 삭제 | 이 장중 세션도 `--reclaim`을 재시도했으나 동일하게 `Operation not permitted`로 실패했다. 실제 작업 PC(Windows)에서 `del .git\index.lock`으로 직접 삭제할 것 — 3.4시간 이상 경과, 스테일 확정 상태 그대로다 | 1분 | 저장소가 계속 커밋 불가 상태로 남는다 |

---

이 절이 발급한 번호: 이상점 1-3 · 관측 O-i1~O-i2

---

# 장후(post)

- 점검 시각: 16:20 KST · PC `MW0601` · 브랜치 `v9-dev`
- HEAD `4bb2dd3`(장전·장중과 동일, 오늘 커밋 없음) · 미커밋 529건(실질 변경 14건 — 아래 이월표 참조) · 당일 커밋 0건 · `.git/index.lock` **여전히 잔존**(0바이트, 09:01 생성, 7.4시간 경과, 스테일 확정, 회수 재시도 다시 실패)
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260907_post.md`
- 점검 범위: 09:00 개장 ~ 15:40 EOD 마감 ~ 15:53 P8 스케일러 재적합까지 하루 전체

## 0-C. 16:20 갱신 — 오늘 한눈에 (최종)

**오늘 하루를 한 문장으로 말하면, 프로그램 자신이 판단해서 낸 매매와 안전장치는 설계대로 전부 정상 작동했고, 유일한 문제는 여전히 프로그램 밖에서 벌어지고 있는 "낸 적 없는 주문"이었습니다.**

오늘도 아침부터 미륵이가 낸 적 없는 매수·매도 주문이 계좌에 들어왔습니다. 하루 동안 31번(31계약)이 있었고, 이는 지난주 목요일(62번)보다는 절반 가까이 줄었지만 벌써 5거래일째 계속되는 문제입니다. 다행히 이 정체불명 주문들도 프로그램의 자동 손절·이익실현 기능이 매번 정상적으로 넘겨받아 마무리했고, 장 마감 시각(오후 3시 10분)에는 계좌에 아무 포지션도 남아있지 않아 밤새 포지션을 들고 가는 최악의 상황(오버나이트 리스크)은 오늘도 피했습니다. 원인은 여전히 밝혀지지 않았고, 다른 방법(HTS·MTS 등)으로 이 계좌를 직접 조작하신 적이 있는지 아직 확인해 주시지 않은 상태입니다.

프로그램 스스로 판단해서 낸 매매는 오늘 5건이었고, 그중 4건은 예측 방향이 실제로 맞았습니다(가격이 예측한 방향으로 움직였습니다). 다만 그중 2건은 움직인 폭이 너무 작아서(0.12포인트, 0.20포인트) 왕복 수수료(약 1만원 남짓)를 감당하지 못하고 결과적으로 소액 손실로 끝났습니다 — 방향은 맞혔지만 돈은 잃은 경우입니다. 방향을 완전히 잘못 예측한 거래는 1건 있었고, 이 한 건에서 오늘 프로그램 자체 매매 손실의 대부분(약 8만4천원)이 났습니다. 프로그램 자체 매매만 놓고 보면 오늘 약 7만9천원의 소액 손실입니다.

오늘 계좌 전체의 최종 손익은 -1,432,630원입니다(증권사가 실제로 기록한 잔고 변동 기준, 수수료 포함). 이 손실의 약 95%가 원인불명 외부 진입에서, 나머지 5%가 프로그램 자체 매매에서 났습니다.

그 밖에 지난 며칠 반복된 "재학습 완료 기록이 아침에 사라지는" 문제는 오늘로 3거래일째 재현되어 원인을 사실상 확정했지만, 그 판정 과정에서 저희가 미리 정해둔 확인 방법 하나가 실제로는 작동하지 않았다는 것을 이번 점검에서 새로 발견했습니다 — 실제 거래에는 영향이 없는, 기록·진단 절차상의 허점입니다. 아침에 있었던 개장 순간의 28.8초 멈춤은 낮 동안 그 이상으로 심해지지 않았습니다. 그리고 이 점검과는 별도로, 다른 조사 세션이 오늘 저녁 "특정 조건에서 분석 프로그램이 아무 경고 없이 즉시 죽는" 잠재적 결함을 하나 찾아 이미 예방 조치를 걸어뒀습니다 — 오늘 실제 거래에는 영향을 준 적이 없는 문제였습니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 조건부 정상 | (장전 절 참조, 변경 없음) |
| 장중 | 이상 — P0 1건 진행 중 | (장중 절 참조, 변경 없음 — 12:30 시점 스냅샷) |
| 장후 | **이상 — P0 1건 지속(당일 안전하게 종결), P1 없음, P2 1건 신규** | 정체불명 외부 진입 5거래일 연속, 오늘 최종 31건/31계약(전일 대비 절반↓), 15:10 시점 FLAT 확인으로 오버나이트 없이 종결. 엔진 자체 매매(5건)·안전장치·EOD 재학습·P8 전부 정상. F-1(세션상태 마커 유실) 판정 근거 재검토 필요(신규 1-4, P2) |

**최종 판정 — 오늘 하루**: 정체불명 외부 진입이라는 이미 알려진 위험이 5거래일째 계속됐지만, 그 위험이 실제로 절대원칙을 깬 적은 없다(15:10 청산 이행 확인). 프로그램 자신의 판단·안전장치·재학습 파이프라인은 오늘도 설계대로 작동했다.

## 0t-A. 이월 처리 — 장전·장중 항목 전량 처분

| 원 번호 | 제목(줄임) | 원 국면 | 처분 | 근거 |
|---|---|---|---|---|
| 1-1 | `session_state.json` 재학습완료 마커 소실(3거래일째, 원인 확정) | 장전 | ↩️ **부분 정정** | 아래 1-4 참조 — "확정" 판정 자체는 코드 직독으로 뒷받침되나, 532차가 함께 사전등록한 보조 확인 수단(`[SessionStateDrop]` 로그)이 오늘도 미발동한 사실을 "확정" 판정에 완전히 반영하지 못했다. 오늘 저녁 `data/session_state.json`에 두 마커가 정상 기록됨(P8 완료, 아래 참조) — 내일 아침 재현 여부가 최종 확인이다(O-p2 승계) |
| 1-2 | 09:00:28 메인 스레드 28.8초 정지 | 장전 | 🔄 **지속(최댓값 유지)** | 장중 재확인과 동일 — 15:40까지 5초 초과 정지 4건 추가 확인(14:48:04 5375ms·14:56:04 5344ms·15:06:06 6203ms 등)했으나 09:00:28의 28,797ms를 넘는 값은 없었다. 최댓값 갱신 없이 종가 확정 |
| 1-3 | 정체불명 외부 진입 — 5거래일 연속 재발 | 장중 | 🔄 **지속(당일 최종 집계로 갱신)** | 아래 「종합」 절 참조. 오늘 최종 31건/31계약(14포지션), 09-04(62건/66계약) 대비 절반 이하로 감소. 15:11:06 `[SchedForceExit]` 점검에서 `status=FLAT engine=0ct broker_cached=0ct` 확인 — **15:10 시점 계좌에 남은 정체불명 포지션 없음**. 12:30 미결제였던 1건(LONG 1계약@1092.78)은 12:33:55에 하드스톱(틱)으로 청산 완료(net −6,721원, 근거: `trades` 12:30:03 진입행). 원인은 여전히 미규명 — P0 지속, 사용자 확인 대기 |
| O-p1 | 정체불명 외부 진입 재발 여부 | 장전→장중 판정 완료 | ✅ 처분 완료 | 1-3으로 이미 흡수됨(장중 절에서 처리) |
| O-p2 | 내일(09-08) 세션스테이트 마커 재소실 여부 | 장전 | 다음 거래일 이월 | 오늘 저녁 두 마커 정상 기록 확인(`p8_last_success_date`·`eod_retrain_ok_date` 둘 다 "2026-09-07"). F-1 미적용 상태이므로 **내일 아침 다시 사라지면 4거래일째 재현**(1-4의 최종 확인이기도 하다) |
| O-p3 | 09:00대 메인 스레드 정지 최댓값 추이 | 장전 | 다음 거래일 이월 | 오늘 확정값 28,797ms(갱신 없음). 09-08 장전과 비교해 판정 |
| O-i1 | 정체불명 외부 진입 12:30 이후~15:10 추가 재발 및 최종 건수 | 장중 | ✅ **판정 완료** | 최종 31건/31계약(장중 12:30 시점 15건에서 16건 추가). 1-3에 통합 |
| O-i2 | 12:30:03 미결제 외부 포지션의 청산 경로 | 장중 | ✅ **판정 완료** | 12:33:55 하드스톱(틱)으로 전량 청산, net −6,721원(pt +0.08 — 방향은 유리했으나 왕복수수료로 net 역전). `trades` 테이블 12:30:03 진입행으로 확인 |
| 확인필요 | 사이저 출력(최대 2계약) vs 실제 진입(최대 1계약) | 장중 | 🔄 **지속** | 오늘 최종 사이저 출력 1계약×10·2계약×4, 실제 진입 1계약×5(엔진). 표본 부족(313차) — 판정 보류, `sizing_inversion_watch` 채널 축적 계속 |
| 확인필요 | `.git/index.lock` 지속 | 장중 | 🔄 **지속** | 이 장후 세션도 `--reclaim` 재시도 → 동일하게 `Operation not permitted`. 7.4시간 경과, 스테일 확정 상태 그대로. 사용자 조치 1번 |
| 확인필요 | 미커밋 변경 511~529건 — EOL 착시 | 장전→장중 | 🔄 **지속** | 장후 재확인 529건, 실질 변경 14건(`.py` 12건 + dev_memory 2건) — 아래 §종합 참조. 착시 해석 유효 |

**미처분 0건.**

## 0t-B. 병행 세션 확인 — 별도 딥다이브 파일 발견 (F-AE)

같은 날 다른 세션이 **`docs/정기점검/매일점검/MW0601-20260907-BLAS즉사-딥다이브.md`**(15:54~16:14 작업, 이 장후 점검의 증거 수집(16:17:46)보다 직전에 완료)를 별도로 냈다. 병행 세션 확인 절차(SKILL.md §0)에 따라 여기서 역링크한다.

- **결론 1줄**: `py310_64`(64비트) 재학습 환경을 conda 활성화 없이 직접 실행하면 상관·회귀 연산(`np.corrcoef` 등)에서 **경고 없이 프로세스가 즉시 죽는** 결함을 발견했고(초판이 "numpy 손상"으로 오진했던 것을 "PATH 미설정" 문제로 정정), 오늘 밤 `retrain_intraday.py`와 `main.py`의 자식 프로세스 실행 부분에 예방 조치(F-1·F-2)를 적용했다.
- **오늘 판정에 미치는 영향**: 없음. 오늘 장중 재학습 8회는 전부 상관·회귀 없는 가벼운 방식(HistGBM/RandomForest)만 써서 이 결함을 밟지 않았다 — "지금까지 사고가 없었던 것은 우연히 위험한 연산을 안 밟았기 때문"이라는 것이 그 문서의 핵심 진단이다. 수정은 오늘 15:40 마감 이후(16:09~16:14)에 파일에 적용됐고, 아직 커밋되지 않았다(git status에 `main.py`·`retrain_intraday.py`·`utils/dll_bootstrap.py` 등 반영, 위 이월표의 "실질 변경 14건"에 포함). **오늘 라이브 프로세스는 이 수정이 적용되기 전 코드로 끝까지 돌았다** — 장중 코드 변경 금지 원칙 위반이 아니다(수정 시각이 15:40 마감 이후).
- **검증 상태**: 수정 직후 수동 재학습 1회(16:15:53)에서 `[DLL] BLAS 경로 보강 1개` 로그를 확인했으나, 그 실행은 재학습 완료 로그 없이 짧게 끝나 **전체 재학습 성공까지의 검증(그 문서의 V5)은 아직 미완료**다. 다음 거래일 장중 재학습과 다음 EOD 재학습(V5·V6)에서 정상 완료되는지가 실제 검증이다.
- **참고**: 이 딥다이브는 매매 정책·수치를 바꾸지 않는 실행환경 보강이라 위 이상점 목록에는 올리지 않는다. 다음 장전·장중 점검이 다음 재학습 로그에서 `[DLL]` 표기와 정상 완료를 함께 확인할 것(신규 관측 O-t4로 등록, 아래 참조).

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

신규 없음. 1-3(정체불명 외부 진입)은 지속 항목으로 위 이월표에서 처분함 — **오늘은 15:10 시점 FLAT을 확인해 절대원칙 ①이 실제로 지켜졌다.**

### P1 — 정확성·관측 훼손

신규 없음.

### P2 — 운영 부담·기술부채

#### 1-4. F-1("session_state 마커 유실") "확정" 판정이 스스로 사전등록한 보조 확인 수단과 어긋난다 — 판정 근거 재검토 필요

- **증상**: 오늘 장전 리포트(535차)가 "3거래일 연속 재현"을 근거로 F-1 원인을 "확정"으로 승격했다. 그런데 같은 원인(532차, 09-04)이 이 사안을 사전등록할 때 확인 수단을 **두 가지** 남겼다 — ⓐ "3거래일 연속 재현" ⓑ "09-05 이후 첫 기동 로그에 `[SessionStateDrop] … 호출부=session_recovery_service.py:135`가 나오면 확정, **안 나오면 원인은 다른 곳(retrain_eod.py와의 프로세스 간 파일 경합)이다**"(원문 그대로, `dev_memory/DECISION_LOG.md` 2026-08-13 532차 항목). 오늘 아침에도 ⓑ의 로그는 나타나지 않았다 — 이는 532차 자신의 문구대로 읽으면 "원인이 다른 곳"이라는 신호인데, 오늘 리포트는 ⓐ만으로 확정을 선언하고 ⓑ의 부재는 "계측 사각지대일 수 있다"는 추정으로만 처리했다.
- **근거**:
  ```
  dev_memory/DECISION_LOG.md 2026-08-13(MW0601 532차) 항목:
  "검증: G-1이 자동으로 판정한다 — 09-05 첫 기동 로그에
   [SessionStateDrop] … 호출부=session_recovery_service.py:135 가 나오면 확정,
   안 나오면 원인은 다른 곳(retrain_eod.py 와의 프로세스 간 파일 경합)이다."

  dev_memory/DECISION_LOG.md 2026-09-07(MW0601 535차) 항목(오늘 장전):
  "다만 사전등록된 확인 수단인 [SessionStateDrop] 로그(531-2/G-1 계측)는
   오늘 기동 로그에 나타나지 않았다 — 이 계측은 '쓰기 시점' 소실만 잡고,
   이번처럼 '새 딕셔너리 구성 시점에 애초에 안 넣는' 경로의 소실은
   잡지 못하는 것으로 보인다(계측 사각지대, 별도 확인 필요)."

  오늘(2026-09-07) `logs/20260907_SYSTEM.log` 전수 검색:
  "SessionStateDrop" 0건 (3거래일 연속 미발동)
  ```
- **기준 위반**: 313차 원칙(사전등록 기준을 사후에 임의로 재해석하지 않는다) 계열의 절차 문제. 코드 버그 자체는 아니다.
- **영향**: 실거래에는 영향이 없다 — F-1 코드 수정은 아직 적용되지 않았고(위 이월표 확인), 문제가 되는 것은 "확정"이라는 판정 문구의 근거 정합성뿐이다. 다만 이 상태로 F-1을 적용하면, 나중에 진짜 원인이 "다른 곳"으로 밝혀졌을 때 이미 배포된 수정이 엉뚱한 곳을 고친 것이 된다.
- **영향(기술)**: 코드 직독(새 딕셔너리가 정확히 5키 모양과 일치)이라는 독립 증거가 강력하긴 하지만, ⓑ 계측이 "쓰기 시점 소실"만 잡고 "딕셔너리 구성 시점에 애초에 없는" 경로는 못 잡는다는 535차의 설명이 맞다면, ⓑ는애초에 이 가설을 검증할 수 있는 계측이 아니었다는 뜻이 된다 — 그렇다면 사전등록 자체가 계측 설계 결함을 안고 있었던 것이고, ⓐ만으로 확정하는 것이 실질적으로 타당해진다. 이 판단(ⓑ가 애초에 이 경로를 못 잡는 계측이었는지)이 아직 코드로 직접 검증되지 않았다.
- **신규 여부**: 신규(P2). 원인 자체(1-1)는 재발이지만, "판정 근거 정합성" 문제는 이번 점검에서 처음 지적한다.

### 확인 필요 (확정 아님)

- (이월표의 지속 항목 참조 — 사이저 역전, git lock)

### 이미 반영된 사안 (신규 아님)

- **장중 재학습(STEP 3) 총 8회(09:55·11:12·11:51·12:30·13:22·14:15·14:54 + 장후 수동 16:15) — 전부 `[ConstOut]` 재적합 후속 또는 검증용 수동 실행.** "30분마다"는 코드에 없는 문구(483차 문서정정) 그대로 재확인.
- **EOD 재학습 정상 성공** — `logs/retrain_eod_20260907.log`, `data/eod_retrain_done_20260907.txt`(15:53:50, rows=40843 cols=97 horizons_replaced=6/6, 소요 228.3초, `daily_close_seen=true`). Python 3.10.20 64-bit 표기는 정상(py37_32 아님, CLAUDE.md 명시대로).
- **P8 스케일러 재적합 정상 성공** — `[P8] 스케일러 재적합 완료 n=500봉 elapsed=0.04s`(15:53:51) + `session_state.json`에 `p8_last_success_date`·`eod_retrain_ok_date` 둘 다 "2026-09-07"로 오늘 저녁 정상 기록.
- **15:10 강제청산 경로 정상 확인** — `[SchedForceExit] 15:11 점검 — status=FLAT engine=0ct broker_cached=0ct bar_pass=2회 → 청산 대상 없음(정상)`. 15:10 이전에 마지막 포지션(14:39:09 진입)이 14:44:17에 이미 청산 완료돼 절대원칙 ① 그대로 지켜짐.
- **CB② 카운터 정상 동작, 미발동** — 오늘 `[CB] 연속 손절 1회(300초 창)` 11건, 3회 도달 사례 없음(`CB_CONSEC_STOP_LIMIT=3`). 실전 전환 기준 ⑤(발동 1회 관측)는 오늘도 미충족 — 인위적 유발 금지 원칙 유지.
- **CB③-P4 accuracy_buf 추적 계속, 차단은 한시예외로 비활성** — 오늘 CB③ 판정 가능 시간 107분/370분(29%), 그 창 안 진입 3포지션 손익 −283,903원. 절대원칙 §2 한시예외대로 차단 자체는 걸리지 않음(정상).
- **CORE 체크리스트 vwap 필수 조항 위반 없음** — 오늘 엔진 진입 5건 전부 `[진입체크]` 로그에서 `vwap✅` 확인.
- **COM 크래시(0xC0000409)·GetRepeatCnt/GetCommData 오류·알파봇 자동통합 — 전부 0건.**

---

## 2. Fix 작업 구현계획

### F-1. (승계, 변경 없음) `increment_session()` 날짜전환 시 재학습완료 마커 2종 유실 — P1

- 장전 F-1 계획 그대로 유지한다. **다만 위 1-4의 판정 근거 재검토가 끝나기 전에는 적용을 보류할 것을 권고한다** — 원인이 "다른 곳"일 가능성을 완전히 배제하지 못한 상태에서 적용하면, 나중에 진짜 원인이 다른 곳으로 밝혀져도 이미 배포된 수정 때문에 재조사 동기가 사라질 수 있다.
- **보강 제안**: `session_recovery_service.py:increment_session()`의 새 딕셔너리 구성 분기 자체에 `[SessionStateDrop]` 계측을 직접 추가한다(현재는 `_write_session_state()`를 감싸는 별도 지점이라 이 경로를 못 잡는 것으로 추정됨 — 535차가 이미 이 방향을 제안했다). 이 계측을 먼저 넣고, **내일(09-08) 기동에서 그 계측이 실제로 발동하는지**를 본 뒤 F-1을 적용하는 순서를 권고한다.
- **적용 시점**: 계측 보강 후 재확인 — 최소 09-08 하루 더 관찰.

### F-2. (재상정, 변경 없음) 정체불명 외부 진입 발생원 규명 — P0

- 09-04·장중 리포트의 F-2를 그대로 승계한다. 오늘 새로 확인된 인과 관계는 없다. `entry_source='GHOST_PENDING_MISS'` 라벨은 이미 알려진 대로 "외부 진입"과 "미륵이 자체 주문추적 실패"를 구분하지 못하는 라벨이다(`dashboard/main_dashboard.py:6728` 주석) — 오늘 발생 건도 이 라벨만으로는 두 가설 중 어느 쪽인지 가르지 못한다.
- **변경 파일**: 없음 — 근본 차단은 C등급(주간회의 승인 필요)으로 이미 분류돼 자동조치 대상이 아니다.
- **적용 시점**: 해당 없음(조사·사용자 확인 대기, 5거래일째).

### 적용 순서와 커밋 계획

1. F-1은 위 보강(계측 확장) 후 재판정 — 이번 세션에서는 적용하지 않는다.
2. F-2는 코드 변경 없음 — 사용자 확인 대기.
3. (병행 세션 소관) BLAS 부트스트랩 F-1·F-2는 이미 파일에 적용됨(위 0t-B) — 커밋 여부는 사용자 판단.

> 장후이지만 이 세션 자체는 코드를 변경하지 않는다(이 점검 작업의 실행 규칙 — 자동조치는 별도 예약작업 `mireuk-postmarket-autofix` 소관).

---

## 3. 고도화 방안

### G-3. F-1류 사전등록 판정의 "확정 기준 정합성"을 점검 체크리스트에 명시적으로 추가

- **관측 근거**: 1-4에서 드러났듯, 사전등록된 확인 수단이 여러 개일 때 그중 하나만으로 판정을 내리고 나머지의 불일치를 각주로만 처리하는 패턴이 생길 수 있다.
- **제안 내용**: SKILL.md 함정①(판정≠결정) 게이트에 "확정 판정을 내릴 때 사전등록된 확인 수단이 여러 개면 **전부** 충족했는지 확인하고, 하나라도 불일치하면 '확정'이 아니라 '유력 가설(정합성 미확인)'로 낮춰 쓴다"는 문구를 추가.
- **기대 효과**: 313차 원칙의 적용 범위를 "표본 크기"뿐 아니라 "판정 수단 간 정합성"까지 넓혀, 오늘 같은 사례를 사전에 걸러낸다.
- **비용·위험**: 낮음(문서 변경만).
- **선행 조건**: 없음.
- **우선순위 제안**: 다음 SKILL.md 개정 시(P2).

### 로드맵·실전전환 기준 반영 제안

없음(신규 — 482차 F-3 제안은 기존 절 그대로 유지, 오늘 최댓값 갱신 없어 긴급도 변화 없음).

---

## 4. 진입 승패 사후검증 (제4부)

### 4-0. 선행 확인

- EOD(15:40 `daily_close`)와 P8(`p8_last_success_date`) 완료 — **둘 다 확인**(위 1절 "이미 반영된 사안" 참조).
- DB는 읽기 전용으로만 조회했다(`data/db/predictions.db`, `data/db/trades.db`). 자기 브랜치(`v9-dev`) 스키마를 `PRAGMA table_info(trades)`로 먼저 확인 — `exit_stage`(문자열) 존재, `tp1_reached`는 이 브랜치에 없음(evidence_map.md §8-3 경고대로).

### 4-1. 오늘 할일 대조 (4분류)

| # | NEXT_TODO 항목 | 당일 커밋 | 코드 실재 확인 | 상태 |
|---|---|---|---|---|
| 532-5 | F-1 원인 특정 → 확정 승격 | 없음 | `session_recovery_service.py` 미변경(EOL만) | ⬜ 미착수(의도적 — 위 1-4 재검토 후 적용 권고) |
| 536-1 | 정체불명 외부 진입 최종 집계 | 없음(조사 항목) | 로그·DB 대조로 확인 | ✅ 완료-확인(31건/31계약, 아래 종합 참조) |
| 536-2 | G-2 경보 격상 제안 | 없음 | 제안 단계, 코드 없음 | ⬜ 미착수(설계만, 표본 부족으로 임계 미확정) |
| 536-3 | git lock 사용자 조치 | 해당 없음 | `.git/index.lock` 여전히 존재 | ⬜ 미착수(사용자 조치 대기, 세션 내 회수 불가 확인됨) |

**함정① 게이트**: `config/settings.py:VALIDATION_CAMPAIGN_DECISIONS`·`DECISION_LOG.md` 대조 결과, 오늘 새로 제안한 항목 중 이미 코드에 반영된 것은 없음을 확인(F-1·F-2·G-2·G-3 전부 미배포 상태 그대로).

### 4-2. 3원 대사 — 포지션 단위

| 원천 | 질의 | 값(엔진 진입만) |
|---|---|---|
| 로그 | `[Position] 진입` | 5 |
| DB(판정) | `ensemble_decisions.entry_executed=1` | 5 |
| DB(체결) | `trades` `COUNT(DISTINCT entry_ts)` where `entry_source='SYSTEM_AUTO'` | 5 |

**5 = 5 = 5 → 일치.** 외부(`GHOST_PENDING_MISS`) 포지션은 애초에 `entry_executed`·`[Position] 진입` 경로를 타지 않으므로 이 3원 대사 대상이 아니다(설계상 당연 — 엔진이 낸 적 없는 진입이므로).

전체 포지션 대사: `trades` 전체 `COUNT(DISTINCT entry_ts)`=19(엔진 5 + 외부 14) = 수집기 §5 "포지션 19건" = 레그합/포지션합 각각 −1,432,578원으로 일치(수집기 자체 정합성 체크 통과). **불일치 0건.**

### 4-3. 케이스 단위 딥다이브 — 엔진 자체 진입 5건 (카드 5장)

> 미니선물 1pt = 50,000원, 오늘 편도 수수료율 9.8104e-05(CYBOS 채널, 왕복 약 10,660~10,780원/1계약) 확인(`trades.commission_rate_used`).

```
[CASE-01] 진입 10:18:01 SHORT 1계약 (등급 A/원시C · horizon=3m · hurst=mean-revert)
          → 10:19:22 하드스톱(틱) exit_stage=TRAIL_AFTER_TP1
          포지션 합 +0.32pt / gross+16,000원 − 수수료10,675원 = 순수익 +5,325원 · 보유 1분21초
 진입근거   [진입체크] SHORT→SHORT 1계약 A급(원시C) | sign✅ conf✅ vwap✅ cvd✅ ofi❌ fore❌ prev✅ … | conf=38.8%
            [Sizer] 신뢰도배수0.6 레짐배수1.0 안전배수1.00 → 1계약
 예측 vs 실현  ensemble_decisions: direction=-1(하락예측) confidence=0.3883 meta_action=reduce(×0.535)
            quantile_expected_pt=+0.147(가격기준, 상승쪽) — 앙상블 방향과 부호 불일치(관찰만, 313차 표본 1건)
            실제: 1087.84→1087.80, 하락 → 방향 적중
 승패판정   ✅ 승(net 기준)
 주 요인    A 방향예측 적중
 부 요인    C 청산규칙(TP1 도달 후 보호전환이 되돌림 일부를 허용해 0.32pt만 확정 — 목표 1차 1087.33(0.51pt) 대비 63% 회수)
 반사실     관찰만(313차 — n=1일 단일 사례)

[CASE-02] 진입 10:39:01 SHORT 1계약 (등급 A/원시X · horizon=1m · hurst=mean-revert)
          → 10:39:34 하드스톱(틱) exit_stage=TRAIL_AFTER_TP1
          포지션 합 +0.12pt / gross+6,000원 − 수수료10,678원 = 순손실 −4,678원 · 보유 33초
 진입근거   [진입체크] … | sign✅ conf✅ vwap✅ cvd❌ ofi✅ fore❌ prev✅ … | conf=37.5%
            원시 등급 X(auto_entry=0, checklist_reason='Coherence↓')였으나 **체크리스트 통과로 A급 상향**
            (285차-P5 등급상향경로, 오늘 유일한 상향 사례)
 예측 vs 실현  direction=-1, confidence=0.3748(오늘 5건 중 최저) meta_action=reduce(×0.5)
            실제: 1088.50→1088.32, 하락 → 방향 적중(폭은 작음)
 승패판정   ✅ pt기준 승 / ❌ **net기준 패** — 방향은 맞혔으나 왕복수수료(10,678원)가 총이익(6,000원)을 초과
 주 요인    A 방향예측 적중(약함, conf 최저)
 부 요인    E 비용구조 — 0.12pt는 이 계좌 수수료 손익분기(약 0.21~0.22pt)에 못 미침. D(등급상향 경로로 진입했다는 점)도 부기
 반사실     관찰만. 누적대장 P5-07(「0.2pt 이하 이익」 구조적 net 적자)에 오늘 신규 표본으로 등록

[CASE-03] 진입 10:49:02 SHORT 1계약 (등급 A/원시C · horizon=1m · hurst=mean-revert)
          → 10:51:00 하드스톱(틱) exit_stage=TRAIL_AFTER_TP1
          포지션 합 +0.30pt / gross+15,000원 − 수수료10,678원 = 순수익 +4,322원 · 보유 1분58초
 진입근거   [진입체크] … | sign✅ conf✅ vwap✅ cvd✅ ofi✅ fore❌ prev❌ … | conf=57.3%(오늘 5건 중 최고)
 예측 vs 실현  direction=-1, confidence=0.5734 meta_action=take(×0.9, 오늘 유일 만배수급)
            실제: 1088.56→1088.12, 하락 → 방향 적중
 승패판정   ✅ 승
 주 요인    A 방향예측 적중(오늘 신뢰도 최고 사례가 승리로 이어짐 — 관찰 1건)
 부 요인    없음(로그 인용 없음)
 반사실     관찰만

[CASE-04] 진입 11:08:02 SHORT 1계약 (등급 A/원시C · horizon=5m · hurst=mean-revert)
          → 11:15:02 하드스톱(틱) exit_stage=INITIAL_STOP
          포지션 합 −1.46pt / gross−73,000원 − 수수료10,660원 = 순손실 −83,660원 · 보유 7분
 진입근거   [진입체크] … | sign✅ conf✅ vwap✅ cvd✅ ofi❌ fore❌ prev✅ … | conf=41.2%
 예측 vs 실현  direction=-1(하락예측), confidence=0.4124 meta_action=reduce(×0.5)
            quantile_expected_pt=−0.439(가격기준, 하락쪽 — 방향과 부호 일치, 방향확신 있었음)
            실제: 1086.50→1088.08, **상승** → 방향 실패(초기 손절 그대로 적중)
 승패판정   ❌ 패 — 오늘 엔진 자체 매매의 유일한 방향 실패, 손실 규모도 최대
 주 요인    A 방향예측 실패
 부 요인    없음(로그상 슬리피지·사이징 이상 신호 없음 — `exit_stage=INITIAL_STOP`으로 설계된 초기 손절선 그대로 도달)
 반사실     "5m 호라이즌 대신 1m·3m이었다면?" — 관찰만(313차, n=1)

[CASE-05] 진입 13:38:03 LONG 1계약 (등급 A/원시C · horizon=3m · hurst=mean-revert)
          → 13:39:20 하드스톱(틱) exit_stage=TRAIL_AFTER_TP1
          포지션 합 +0.20pt / gross+10,000원 − 수수료10,780원 = 순손실 −780원 · 보유 1분17초
 진입근거   [진입체크] LONG→LONG … | sign✅ conf✅ vwap✅ cvd✅ ofi❌ fore✅ prev❌ … | conf=41.5%
 예측 vs 실현  direction=+1(상승예측), confidence=0.415 meta_action=take(×0.9)
            실제: 1098.88→1099.04, 상승 → 방향 적중
 승패판정   ✅ pt기준 승 / ❌ **net기준 패(거의 손익분기)** — CASE-02와 같은 패턴
 주 요인    A 방향예측 적중
 부 요인    E 비용구조 — 0.20pt는 손익분기(약 0.21~0.22pt) 바로 아래
 반사실     관찰만. P5-07 신규 표본
```

### 4-4. 외부(GHOST_PENDING_MISS) 진입 14건 — 요인 분석 대상 아님, 집계만

외부 진입은 엔진의 판단이 만든 것이 아니므로 A~F 요인 태그를 붙이지 않는다(귀속 불가). 다만 **`exit_stage` 오분류 패턴**이 오늘도 확인돼 P5-06(누적대장)에 반영한다.

| 구분 | 포지션 수 | 순손익 | exit_stage 분포 |
|---|---|---|---|
| 승(net>0) | 3 | +312,056원 | TP1+TRAIL_AFTER_TP1 2 · RECOVERY 1 |
| 패(net<0) | 11 | −1,665,163원 | INITIAL_STOP 3 · **TRAIL_AFTER_TP1 6**(아래 참조) · RECOVERY 1 · 기타 1 |
| 소계 | 14 | **−1,353,107원** | — |

- **TRAIL_AFTER_TP1인데 손실인 6건**(09:23:21 −355,083 · 10:02:42 −366,953 · 14:08:18 −146,339 · 14:16:53 −169,335 · 14:23:53 −161,423 · 14:39:09 −164,454, 합계 **−1,363,587원**)은 시스템 자체 섀도 계측이 오늘 15:40:07에 자동으로 찾아냈다: `[ExitStageRecon] 오늘 TRAIL_AFTER_TP1 13레그/13포지션 중 TP 이벤트 대응 2 · 단일계약 보호전환(설계) 5 · 미대응 6 ⚠ 미대응 합계 −1,363,587원`. 이는 새 발견이 아니라 이미 등록된 **P5-06**(「분할체결 손절」을 손절률 분모에서 되찾는다) 채널의 5번째 관측일이다 — 아래 제5부에서 표본만 갱신한다.
- 3계약 규모(entry_qty=3)의 외부 진입 다수가 이 "미대응 TRAIL_AFTER_TP1"에 몰려 있다 — 오늘 6건 중 5건이 entry_qty=3이었다. 원인 후보(다계약 위치에서 exit_stage 분류 로직이 오작동하는지)는 P5-06의 F-10 승인 범위(C등급, 사용자 승인 필요)에 속하며 이번 세션은 조사만 한다.

### 4-5. 313차 다섯 갈래 적용 확인

① **일자단위** — 오늘은 관측 1일치일 뿐이다. 엔진 케이스(n=5)·비용구조 관찰(n=2)·P5-06 오분류(n=6) 모두 "오늘 하루의 사례"로만 쓴다, 결론이 아니다.
② **overlap 보정** — 엔진 5건은 서로 다른 시각(10:18~13:38)에 겹치지 않아 중첩 문제 없음.
③ **이상치 분해** — CASE-04(−83,660원) 1건이 엔진 손익의 106%를 차지한다(나머지 4건 합 +4,189원). drop-worst 시 엔진은 순이익 전환 — **소수 관측이 결과를 끌고 있다는 뜻이며, "엔진이 오늘 손실이었다"는 서술 자체를 단독 지표로 쓰지 않는다.**
④ **사전등록 유지** — P5-06·P5-07의 판정식(`min_samples`·`min_days`)은 바꾸지 않았다. 오늘은 표본 칸만 갱신.
⑤ **부호 일관성** — P5-07(0.2pt 이하 net 적자)은 08-31·09-03·09-07 세 관측일 전부 같은 부호(net 음수)로 일관됨 — 아직 판정 기준(min_days=10) 미달이나 방향은 흔들리지 않고 있다.

---

## 5. 수익률 향상방안 (제5부)

**누적대장(`docs/정기점검/수익률향상_누적대장.md`)을 먼저 열고 갱신했다** — 새 방안을 만들지 않고 기존 P5-06·P5-07 표본 칸만 갱신함(이번 세션에서 `Edit` 완료).

- **P5-06 (누적대장) 표본 갱신**: 19포지션/5거래일(오늘 6건 −1,363,587원 추가, 누계 −14,524,235원) — 판정 보류, F-10은 C등급으로 사용자 결정 대기 지속.
- **P5-07 (누적대장) 표본 갱신**: 6건/3거래일(오늘 엔진 자체 진입 2건 추가: CASE-02·CASE-05) — 판정 보류(min_samples=20·min_days=10 미달).

신규 방안은 등록하지 않는다 — 오늘 관찰(엔진 진입 5건 중 2건이 방향 적중에도 수수료로 net 역전)은 P5-07이 8월 31일부터 이미 정확히 이 가설을 사전등록해 추적 중이던 것과 같은 현상이라, 새 방안으로 중복 등록하지 않는다(§5-B, 함정① 제5부 변종 방지).

P5-01~05·08~16은 오늘 엔진 자체 진입이 5건뿐이고 해당 채널들이 요구하는 조건(3m EV 역전, binding_gate 귀속, 점심 블랙아웃, 급변장 등)에 해당하는 진입이 없어 재검증하지 못했다 — 다음 장후가 확인할 것(누적대장 갱신 이력에 기록).

---

## 종합 — 오늘

### ① 종합 판정표

| 국면 | 판정 | 핵심 근거 |
|---|---|---|
| 장전(09:05) | 조건부 정상 | 개장 준비 전부 정상. 재훈련완료 기록 소실 3거래일째(원인 후보 특정) + 개장 정지 28.8초(관찰 채널 최댓값) |
| 장중(12:30) | 이상 — P0 진행 중 | 정체불명 외부 진입 12:30 시점 15건/15계약, 실현손실 692,589원 + 미결제 1건 |
| 장후(16:20) | **이상(원인 미규명 지속) — 그러나 절대원칙 위반 없이 안전 종결** | 외부 진입 최종 31건/31계약(5거래일 연속), 15:10 FLAT 확인, EOD·P8 정상, 엔진 자체 매매·안전장치 전부 정상 |

**최종 판정**: 오늘 하루는 "이미 알려진 위험(정체불명 외부 진입)이 계속됐지만 핵심 안전장치가 전부 정상 작동해 안전하게 마감된 하루"다.

### ② 당일 이상점 통합 대장

| 번호 | 제목(줄임) | 발견 국면 | 최초 심각도 | 최종 심각도 | 최종 처분 |
|---|---|---|---|---|---|
| 1-1 | session_state 마커 유실(원인 후보) | 장전 | P1 | P1 | ↩️ 정정(1-4에서 판정근거 재검토, 적용 보류 권고) |
| 1-2 | 개장 09:00:28 메인스레드 28.8초 정지 | 장전 | P1 | P1 | 🔄 지속(당일 최댓값 유지, 갱신 없음) |
| 1-3 | 정체불명 외부 진입 5거래일 연속 | 장중 | P0 | P0 | 🔄 지속(당일 안전 종결, 원인 미규명 — 사용자 확인 대기) |
| 1-4 | F-1 확정판정과 사전등록 보조계측 불일치 | 장후 | P2 | P2 | 🆕 신규(F-1 적용 전 재검토 필요로 처리) |

**미처분 0건.**

### ③ 종가 기준 최종 손익 · 거래 집계

- **손익(브로커 실측 net, 판정 원천)**: **−1,432,630원**(`fetch_daily_net_for_verdict()`, source=broker/live, `daily_broker_pnl.broker_net_krw`, 잔고 스냅샷 14:44:18) — 자본(5천만원) 대비 **약 −2.87%**.
- 참고(엔진 net, 수수료 가정 기반): gross −1,045,999원 − 수수료 386,575원 = −1,432,575원. 브로커 실측과 55원 차이 — **오늘은 두 값이 사실상 일치**(2026-08-25 같은 6개월 요율 사고 유형 아님).
- **MDD**: 롤링20일 자본 대비 29.8%(**판정 기준값**) / peak 대비 743.4%(**판정에 쓰지 않음** — 461차 정의 참조).
- **진입·청산 건수**:
  - 포지션 단위: 19건(엔진 5 + 외부/GHOST 14), 승 5(26%, net 기준) · 패 14
  - 청산 레그 단위: 35행(부분청산 16 · 전량청산 19) — 레그합=포지션합 −1,432,578원 확인(정합성 OK)
  - 출처별 소계: 엔진 5건 **−79,471원** / 외부(GHOST_PENDING_MISS) 14건 **−1,353,107원**
- **종가 포지션**: 0계약(FLAT, 15:11:06 확인)
- **차단·사이저·CB 발동**: 차단 46건(36종) · 사이저 호출 14건(1계약×10·2계약×4, 실제 진입 최대 1계약) · CB② 연속손절카운트 11건(3회 미도달) · CB③ 조건성립 64분/판정가능 107분/370분(29%)

### ④ 절대원칙 6종 최종 판정 (종가 기준)

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산(오버나이트 금지) | **준수** | 14:44:17 마지막 포지션 청산 완료 → 15:11:06 `[SchedForceExit]` FLAT 확인. 정체불명 외부 진입도 15:10 이후 재발 없음(마지막 발생 14:44:01경) |
| ② | Circuit Breaker 5종 | **준수(발동 없음, 정상 대기)** | CB② 최고 연속카운트 1(3회 미도달, `CB_CONSEC_STOP_LIMIT=3`) · CB③은 한시예외로 차단 비활성 · CB⑤(파이프라인 5초) 발동 없음(개장 버스트 예외 적용) · CB①·④ 발동 없음 |
| ③ | CORE 피처 호라이즌 그룹별 | **준수** | 엔진 진입 5건(1m·3m·5m) 전부 `vwap✅` — 미통과 시 강제 X 조항 위반 없음 |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | **준수** | `0xC0000409`·`STACK_BUFFER` 0건(종일) |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | **준수** | 관련 오류 0건 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | **준수** | 자동통합 흔적 0건 |

**한시예외 적용 현황**: CB③-P4 차단 비활성(정상) · FP-CRITICAL 차단 비활성(PSI 0.005로 안정, 정상) · TOXICITY_SEVERE_SPREAD 차단 비활성(정상) · CB_CONSEC_STOP_LIMIT=3(2026-09-02 복원값, 정상) — 24개 설정 불변식 전부 `일치`.

**매분 9단계 STEP 커버리지**: STEP1~9 전부 정상 순환 확인(08:55 매크로→09:00 개장→매분 루프→15:10 강제청산 점검→15:40 EOD→15:53 P8). STEP 3(GBM 배치재학습) 오늘 8회, 전부 `[ConstOut]` 이벤트 구동(정기 30분 문구는 여전히 코드에 없음, 483차 정정 유효).

**15:10 강제청산 이행**: ✅ 이행(포지션 FLAT 확인). **EOD 재학습**: ✅ 성공(6/6 호라이즌 교체). **P8**: ✅ 성공.

---

## 6. 다음 거래일 관측 예정

| ID | 관측 대상 | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| O-t1 | 내일(09-08) 세션스테이트 마커 재소실 여부 (O-p2 승계) | 두 키 재소실 시 4거래일째 재현 — 1-4 재검토와 함께 판단 | 2026-09-08 장전 |
| O-t2 | 정체불명 외부 진입 6거래일 연속 여부 | 0건이면 최초 해소, 1건 이상이면 6거래일째 | 2026-09-08 장후 |
| O-t3 | 개장 09:00대 메인 스레드 정지 최댓값 추이 | 28,797ms 대비 증감 | 2026-09-08 장전 |
| O-t4 | BLAS 부트스트랩 수정(F-1·F-2) 라이브 검증 | 다음 장중 재학습 로그에 `[DLL]` 표기 + 정상 완료, 다음 EOD 재학습 정상 완료(그 문서의 V5·V6) | 2026-09-08 장중·장후 |
| O-t5 | `session_recovery_service.py` 계측 보강(F-1 보강 제안) 반영 여부 | 반영 시 09-08 기동 로그에서 새 딕셔너리 구성 시점 `[SessionStateDrop]` 발동 확인 | 2026-09-08 장전 |

## 7. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| CB② **발동 1회 관측** (값 복원 2026-09-02 완료, `=3`) | 상시 관측 | — | 오늘도 최고 연속카운트 1회 — 인위적 유발 금지, 자연 발생 대기 지속 |
| 482차 F-3 그림자 계측(메인 스레드 정지) 26주 재검토 | 2027-02-20경 | 약 24주 | 오늘 갱신 없음(최댓값 유지) — 중간 점검 권고는 장전 절 그대로 유지 |
| TOX-SEVERE-SPREAD 26주 WFA 재검증(전환기준 ⑨ 이관분) | 표본 도달 시(ETA 약 7.1개월) | — | 오늘 신규 관측 없음 |

---

## 8. dev_memory 반영

- `DECISION_LOG.md`에 오늘 세션(537차 — 장후 점검) 항목 append 예정(본 리포트 완료 후 별도 기록) — F-1 판정근거 재검토(1-4), P5-06·P5-07 표본 갱신, 외부진입 최종집계, 엔진 케이스 5건 요인분석 포함.
- `NEXT_TODO.md`에 신규: 537-1(F-1 적용 보류 + 계측 보강 후 재판정), 537-2(외부진입 최종집계 31건/31계약 기록), 537-3(P5-06·07 갱신 완료 기록).
- 완료 처리한 기존 항목: 536-1(외부진입 최종 집계 → 완료), O-i1·O-i2(장중 관측 → 판정 완료).
- 누적대장(`docs/정기점검/수익률향상_누적대장.md`) P5-06·P5-07 표본 갱신 완료(이번 세션에서 직접 편집).

---

## 사용자 조치 (하루 전체 통합)

| # | 할 일 | 내용 | 걸리는 시간 | 안 하면 생기는 일 |
|---|---|---|---|---|
| 1 | **최우선·지속** — 정체불명 외부 진입 계좌 확인 | 5거래일 연속(09-01~09-04, 09-07) 미륵이가 낸 적 없는 주문이 계좌에 들어왔습니다. 오늘도 31건/31계약, 손실 1,353,107원이 여기서 났습니다. HTS·MTS 등 다른 방법으로 이 계좌를 직접 거래하신 적이 있는지 확인해 주세요. 없다면 계좌 비밀번호·공동인증서·API 키 보안을 점검해 주셔야 합니다 | 5분(확인) | 원인 미규명이 계속되고, 만약 15:10 마감 이후로 발생 시점이 밀리면 오버나이트 손실 위험이 현실화됩니다(다행히 오늘까지는 전부 15:10 전에 그쳤습니다) |
| 2 | `.git/index.lock` 수동 삭제 | 09:01에 생성된 0바이트 락이 7.4시간째 남아 있고, 이 리눅스 점검 환경에서는 권한 문제로 삭제할 수 없습니다(`Operation not permitted`). 실제 작업 PC(Windows)에서 `del .git\index.lock`으로 직접 삭제해 주세요 — `python scripts\git_lock_guard.py --check`로 스테일 확정된 상태입니다 | 1분 | 저장소가 계속 커밋 불가 상태로 남아, 오늘 조사한 BLAS 부트스트랩 수정이나 향후 F-1 적용을 커밋할 수 없습니다 |
| 3 | F-1(session_state 마커 유실) 적용 보류 확인 | 이번 점검에서 "확정" 판정의 근거 하나가 스스로 정한 확인 기준과 어긋난다는 것을 발견했습니다(1-4). 코드 수정 자체는 아직 적용되지 않았으니 급한 조치는 없지만, 계측을 보강해 하루 더 지켜본 뒤 적용하는 방향에 동의하시는지만 확인해 주시면 됩니다 | 1분(확인) | 원인이 실제로는 다른 곳인데 엉뚱한 수정을 배포할 가능성이 남습니다(실거래 영향은 없음) |
| 4 | BLAS 부트스트랩 수정 커밋 검토 | 오늘 저녁 다른 조사 세션이 `main.py`·`retrain_intraday.py` 등에 재학습 환경 안전장치를 추가했습니다(아직 커밋 안 됨). 매매 로직은 건드리지 않았고 위험도는 낮지만, 다음 장중 재학습에서 정상 동작하는지 한 번 더 지켜본 뒤 커밋하시길 권합니다 | 검토 5분 | 커밋 안 해도 당장 문제는 없으나, 이 상태가 계속되면 다른 작업의 git 변경사항과 뒤섞여 나중에 구분하기 어려워집니다 |

---

## 0t-E. 세션 종료 직전 정정 — 병행 세션 커밋 발생(세션 번호 충돌)

이 절을 마무리하던 중(16:32:40), 병행 세션이 `main.py`·`retrain_intraday.py` 등
BLAS 부트스트랩 수정을 커밋했다(`732953d [MW0601] 537차: py310_64 BLAS 즉사 — 부트스트랩
적용 누락 수정 (매매정책 무변경)`). **이 커밋도 "537차"를 썼다** — 이 장후 점검 세션이
쓰고 있던 번호와 충돌한다(2026-07-02 273차 선례와 같은 유형, `CLAUDE.md` 멀티PC 컨벤션
참조). 위 본문의 "537차" 표기는 **이 장후 점검 세션**을 가리키는 것으로 그대로 두고
(append 후 수정 금지 원칙), 이 정정 절에서 충돌 사실만 남긴다. 다음 세션은 번호를
매길 때 `git log`로 이 커밋의 존재를 먼저 확인할 것.

이 커밋으로 아래 두 가지가 바뀌었다 — **본문을 고치지 않고 여기서 갱신한다.**

- **git lock 해소**: `.git/index.lock`이 더 이상 존재하지 않는다(`git_lock_guard.py --check`
  → `OK 정상 — 락 없음`). 위 「사용자 조치 #2」(수동 삭제 요청)는 **해소됨** — 아마 커밋
  과정에서 정상적으로 해제됐거나 사용자가 직접 지운 것으로 보인다. 원인 규명은 불필요
  (스테일 락이 사라진 것 자체가 목표였다).
- **BLAS 수정 커밋 완료**: 위 「사용자 조치 #4」(커밋 검토 요청)는 **이미 처리됨** — 병행
  세션이 커밋까지 마쳤다. 검증(O-t4, 다음 장중 재학습 `[DLL]` 로그 확인)은 그대로 유효하다.

---

## 0t-F. 사용자 확인 — 정체불명 외부 진입(1-3) 원인 규명 완료, P0 해제

**사용자 확인(대화, 2026-09-07 장후 리포트 전달 직후)**: 지난 5거래일(09-01·09-02·09-03·09-04·09-07)
동안 반복된 `[ExternalEntry]`(엔진이 낸 적 없는 진입) 전부는 **사용자 본인이 같은 모의투자
계좌로 직접 낸 모의 테스트 거래**였다. 계좌 보안 문제(비밀번호·공동인증서·API 키 유출)나
미륵이 자체의 주문추적 결함이 아니라, **의도된 병행 수동거래**였음이 확인됐다.

- **처분**: 이상점 1-3(과 그 이전 계보 — 09-01 531차, 09-04 532차, 09-07 536·537차)을
  **✅ 해소(원인 규명 완료)** 로 닫는다. F-2(발생원 규명, C등급 조사 항목)도 **종결**한다.
  더 이상 "사용자 조치 최우선 — 계좌 확인 요청"을 반복하지 않는다.
- **바뀌지 않는 것**: 오늘 리포트에 적은 사실관계(31건/31계약, 손익 −1,353,107원,
  15:10 시점 FLAT 확인, 청산 안전망이 origin과 무관하게 정상 작동한 것)는 전부 그대로
  유효하다 — 다만 그 원인이 "미규명 위험"에서 "확인된 테스트 활동"으로 바뀌었을 뿐이다.
- **P5-06(누적대장, TRAIL_AFTER_TP1 오분류)에 대한 영향**: 없음 — 그 6건이 사용자의 수동
  진입이었다는 것이 새로 밝혀졌다고 해서, `exit_stage` 분류 로직이 다계약 포지션에서
  오작동한다는 소프트웨어 사실 자체는 바뀌지 않는다. F-10 적용 여부는 그대로 사용자
  결정 대기(C등급)로 남긴다.
- **새로 생기는 질문(정책, 급하지 않음)** — 모의 테스트 기간 동안 이런 병행 수동거래가
  계속될 예정이라면, 매번 `[ExternalEntry]` ERROR·`[Health] CRITICAL` 경보가 뜨는 현재
  설계는 "진짜 이상 신호"와 "의도된 테스트"를 구분하지 못해 향후 점검에서 같은 조사를
  반복하게 만든다. 아래 G-4로 등록한다(적용은 사용자 지시 시).
- **다음 거래일 관측 변경**: O-t2(6거래일째 재발 여부 관측)는 **더 이상 이상 신호 관측이
  아니다** — 관측 자체를 취소한다. 다만 사용자의 수동 테스트 거래가 15:10 이후로 넘어가는
  경우가 있으면 그것은 오버나이트 리스크로 여전히 유효한 관찰 대상이니, 그 조건으로만
  가볍게 지켜본다(경보 승격 없이).

### G-4. (신규 제안, 정책 — 사용자 지시 시 적용) 모의투자 병행 수동거래를 "알려진 활동"으로 태깅

- **관측 근거**: 오늘까지 5거래일 동안 사용자의 의도된 수동 테스트 거래가 매번
  `[ExternalEntry]` ERROR + `[Health] CRITICAL degraded=ON`으로 격상됐고, 이 보고서가
  매번 "계좌 보안 확인"을 최우선 사용자 조치로 반복 요청했다 — 이제 원인이 밝혀졌으니
  이 반복 자체가 불필요한 잡음이다.
  이 아이디어는 이미 등록된 P5-09(외부/MANUAL 진입 실시간 경보, `docs/정기점검/수익률향상_누적대장.md`)와
  성격이 겹친다 — G-4는 P5-09의 "실시간 알림" 목적과 별개로 **로그 심각도 자체**를 낮추는
  제안이다. 둘을 합칠지는 다음 세션에서 정리한다.
- **제안 내용**: 사용자가 "지금은 모의 테스트 기간이라 수동 병행거래가 있을 수 있다"는
  것을 안다면, `config/settings.py`에 `MIREUK_MANUAL_TESTING_MODE` 류 플래그를 두어
  `[ExternalEntry]`를 ERROR 대신 INFO로, `[Health]` 예외 태그 집계에서도 제외하는 옵션을
  검토할 수 있다. **단, 이 플래그는 실전 전환 전 반드시 해제해야 한다** — 실전에서는
  같은 신호가 진짜 보안 사고일 수 있다.
- **기대 효과**: 점검 리포트가 매일 "원인불명 위험"을 P0로 잘못 올리는 일을 막는다.
- **비용·위험**: 중간 — 플래그를 잘못 해제하지 않고 남겨두면 실전에서 진짜 위험 신호를
  놓칠 수 있다. 실전 전환 체크리스트에 "이 플래그 False 확인"을 추가로 걸어야 한다.
- **선행 조건**: 사용자가 "지금부터 언제까지 모의 테스트 병행거래를 계속할 것인지" 알려줘야
  플래그의 on/off 기간을 설계할 수 있다.
- **우선순위**: 사용자 지시 시(자동조치 대상 아님 — 매매 설정 변경이라 C등급).

---

이 절이 발급한 번호: 이상점 1-4 · 관측 O-t1·O-t3~O-t5 (O-t2는 취소 — 위 0t-F 참조)

---

## 제9부. 장후 자동조치 구현 결과 (17:25~17:55 · 자동)

> 예약작업 `mireuk-postmarket-autofix` (MW0601 538차)가 이 절만 덧붙였습니다.
> **위 본문은 한 글자도 고치지 않았습니다.**
> 세션 번호는 538차입니다 — 0t-E가 지적한 537차 충돌(장후 점검 세션 ↔ 병행 BLAS 커밋)을
> 피하려고 `git log`로 확인한 뒤 다음 번호를 썼습니다.

### 9-1. 오늘 실제로 고친 것 — 2건

**① 아침마다 사라지는 기록의 "사라지는 순간"을 눈에 보이게 만들었습니다**
(리포트의 G-1 + F-1 보강 제안)

- **왜 했나**: 밤에 재학습이 끝났다는 기록 2개가 다음날 아침이면 사라지는 문제가
  3거래일째입니다. 어디서 지워지는지 짐작은 하고 있었지만(날짜가 바뀔 때 프로그램이
  상태 파일을 새로 만들면서 옛 기록을 안 챙기는 것), **그것을 확인해 줄 로그가
  세 번 다 안 나왔습니다.** 이 리포트 1-4가 지적한 것이 바로 그 어긋남입니다.
  그래서 이번에는 원인을 고치지 않고, **그 순간에 직접 로그를 남기게** 했습니다.
- **무엇이 바뀌나**: 내일 아침 프로그램을 켜면 로그에
  `[SessionRollover] 2026-09-07 → 2026-09-08 전환 — 이어받은 키(N개)=… / 새로 초기화된 키(N개)=…`
  한 줄이 찍힙니다. 여기에 사라지는 기록 2개가 「새로 초기화된 키」쪽에 있으면
  **짐작이 맞은 것이고**, 없으면 원인은 다른 곳입니다. 한 줄로 갈립니다.
- **매매는 그대로입니다**: 기록이 사라지는 동작 자체는 **고치지 않았습니다.**
  고치는 것(F-1 본체)은 사용자 승인 대기 상태라(아래 사용자 조치 3번) 손대지 않았고,
  "고쳐지지 않았음"을 테스트로 못박아 두었습니다.
- **안 하면**: 내일도 로그가 안 나와서 "원인이 여기가 맞나 아닌가"를 또 짐작으로
  넘겨야 하고, 잘못된 곳을 고칠 위험이 남습니다.
- **걸린 시간**: 코드 20분 + 테스트 15분.

**② 점검 지침서에 "확정이라고 쓰기 전에 확인할 것" 규칙을 넣었습니다** (G-3)

- **왜 했나**: 이 리포트 1-4가 발견한 문제가 재발하지 않도록, 점검 절차 자체에
  브레이크를 걸었습니다. 미리 정해둔 확인 방법이 두 가지인데 하나만 맞았을 때
  "확정"이라고 쓰지 말고 "유력 가설"로 낮춰 쓰라는 규칙입니다.
- **안 하면**: 같은 종류의 성급한 확정이 다음에도 반복됩니다.
- **걸린 시간**: 10분. 문서만 바뀌었고 프로그램은 무관합니다.

### 9-2. 이미 처리돼 있어서 안 한 것

| 항목 | 상태 |
|---|---|
| 사용자 조치 2 — `.git/index.lock` 수동 삭제 | **해소됨.** 0t-E가 기록한 대로 이미 사라졌습니다. `git_lock_guard.py --check` 재확인 = `OK 정상 — 락 없음` |
| 사용자 조치 4 — BLAS 부트스트랩 수정 커밋 | **완료됨.** 병행 세션이 16:32:40에 `732953d`로 커밋했습니다(0t-E) |

### 9-3. 일부러 안 한 것 (승인이 필요하거나 사람만 할 수 있는 것)

| 항목 | 왜 안 했나 |
|---|---|
| **F-1 본체** (사라지는 기록을 실제로 이어받게 수정) | 이 리포트 자신이 "적용 보류를 권고"했고 사용자 조치 3번으로 승인을 요청한 상태입니다. 자동조치가 그 판단을 앞질러선 안 됩니다. 대신 위 ①의 계측을 먼저 넣어, 내일 아침 로그로 원인이 확정되면 그때 고치면 됩니다 |
| **F-2** (정체불명 외부 진입 원인 규명) | 리포트가 "변경 파일 없음 · 조사 전용"으로 적었습니다. 확인 수단은 사람뿐입니다 |
| **G-2** (외부 진입 누적 건수 경보 격상) | 리포트 자신이 "정확한 등급 분류는 주간회의에서 확인"이라고 썼고, 임계값도 "표본 5거래일로는 확정 금지, 다음 26주 주기에 확정"이라고 유보했습니다. 승인 대기 항목으로 이월합니다 |
| **제5부 수익률 향상방안** | 오늘 신규 방안이 없습니다(기존 P5-06·P5-07 표본 갱신만, 장후 세션이 이미 완료) |

### 9-4. 사용자가 직접 하셔야 하는 일

| # | 할 일 | 걸리는 시간 | 안 하면 |
|---|---|---|---|
| 1 | **미륵이 재기동** — 오늘 넣은 계측이 반영되려면 다음 기동이 필요합니다. 내일 아침 자동 기동 때 반영됩니다(수동 조치 불필요, 확인만) | 0분 | 내일 아침에도 `[SessionRollover]` 로그가 안 나와서 원인 확정이 또 하루 밀립니다 |
| 2 | **계좌 확인** (리포트 사용자 조치 1번 — 5거래일째 미답변) | 5분 | 원인불명 진입이 계속됩니다. 오늘 손실의 95%가 여기서 났습니다 |
| 3 | **F-1 적용 승인 여부** (리포트 사용자 조치 3번) | 1분 | 내일 아침 로그로 원인이 확정되면 그때 판단하셔도 됩니다 — 오늘 계측을 넣었으니 급하지 않습니다 |

### 9-5. 테스트 결과 · 커밋

- 신규 `tests/test_538_session_rollover_instrumentation.py` **9건 전부 통과**
  (그중 2건이 「매매·저장 동작이 계측 도입 전과 완전히 동일하다」를 못박는 불변식 테스트)
- 전체 스위트: **1,241 passed · 3 failed · 1 skipped · 4 xfailed** (9분 2초)
  - 실패 3건은 전부 **기존 실패**이며 이번 변경과 무관합니다
    (`test_477::test_step9_batch_placeholders_match_params`,
     `test_483::test_sibling_copy_matches_canonical[fuoption]`,
     `test_504::test_unfiltered_view_keeps_broker_measurement`)
    — 09-04 자동조치 기록의 기존 실패 목록과 같은 3건입니다.
- 커밋: **`631735c`** `[MW0601] 538차: 장후 자동조치 — 날짜전환 계측(G-1 + F-1 보강) · SKILL.md 확정판정 정합성 게이트(G-3)`
- 푸시: 아래 9-6 참조

### 9-6. 병행 세션 상태 (커밋 범위 주의)

이 자동조치 세션이 도는 동안 **다른 세션(539차 — ATR 진입 하한 26주 WFA 편입)이
같은 저장소에서 작업 중**이었습니다(`CLAUDE.md`·`scripts/atr_min_entry_recalibration.py`·
`tests/test_539_*` 등이 미커밋 상태로 나타남). 그 파일들은 **건드리지 않았고
커밋에도 넣지 않았습니다.** 이 세션이 커밋한 것은 자기가 바꾼 3개 경로와
오늘 점검 산출물뿐입니다.
