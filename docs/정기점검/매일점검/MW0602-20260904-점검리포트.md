# 미륵이 일일 점검 — MW0602 / 2026-09-04

# 장전(pre)

- 점검 시각: 09:06 KST · PC `MW0602`
- HEAD `7bd2e12` · 미커밋 0건(EOL 차이 제외 실질 변경 기준) · 당일 커밋 2건(장전 점검 이전, 스윙 피처 작업 — 527차)
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260904_pre.md`
- 정본 SKILL.md 직접 확인: `rev: 2026-08-26e` — 이번 세션에 로드된 사본과 **일치**(사본 드리프트 없음)

## 0. 한 줄 결론

**오늘 거래를 시작해도 됩니다. 위험한 문제는 없었습니다.** 브랜치·설정·전일 마감 작업(모델 재학습)이 전부 정상이고, 오늘 아침 시장 상황 판단(레짐 확정)도 개장 전에 끝났습니다. 이 점검 자체가 장 시작 22초 뒤에 돌아 오늘 거래는 아직 0건이며, 이 시점까지 뭔가 잘못됐다는 신호도 없습니다. 다만 점검 프로그램이 요 며칠 계속 개장 직후에 도는 문제(사람 결정 대기)와 어제까지 이어지던 "판정을 못 내린 관측 항목" 하나를 오늘 아침 로그로 마무리했습니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 정상(조건부) | P0 0건. 브랜치·설정 26행 전부 일치. 전일 EOD 재학습 성공 확인(재학습 스킵은 정상). 레짐 RISK_ON 확정(08:58:20). 진입 0건(개장 22초 경과 시점) |
| 장중 | (진행 예정) | |
| 장후 | (진행 예정) | |

**절대원칙 준수 여부** (장전 시점 — 아직 거래 없음)

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | 해당없음(장전) | 아직 포지션 없음. 전일 잔고 데이터는 "어제 것"으로 무시 처리됨(`[Position] 저장 상태가 어제 데이터 — 무시`, 정상) |
| ② | Circuit Breaker 5종 | 준수 | 오늘 CB 로그 0건(발동 없음, 정상). `CB_CONSEC_STOP_LIMIT=9999`(CB②, 모의투자 한정 예외) 값 일치 |
| ③ | CORE 피처 호라이즌 그룹별 | 준수(관찰 지속) | 진입 없어 판정 대상 없음. 개장 전 일부 CORE 피처가 "값이 너무 안 움직인다"고 판단돼 임시 처리됐다가 대부분 풀림, `ofi_norm` 1개만 마지막 관측(08:58:54)까지 지속(1-1) |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 해당없음 | 코드 변경 없음 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 해당없음 | 코드 변경 없음 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 준수 | 흔적 없음 |

**설정 불변식** — 수집기 §3 표 26개 행 전부 `일치`. 이상 없음. 차단 게이트 인벤토리 32개 중 8개 꺼짐 — 전부 기록된 한시예외와 일치.

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

없음.

### P1 — 정확성·관측 훼손

없음 신규. (아래 "확인 필요"에 판정 진행 중인 기존 관측 항목을 정리했습니다.)

### P2 — 운영 부담·기술부채

#### 1-1. 개장 전 일부 신호(CORE 피처)가 "변동이 없다"고 판단돼 잠깐 얼어붙었다가 대부분 풀림 (지속)

- **증상**: 08:45경 above_vwap·cvd_divergence·ofi_norm 3개 신호가 "너무 안 움직인다"고 판단돼 임시로 고정값 처리됐고, 이후 순차적으로 풀려 08:47:55에는 ofi_norm 1개만 남았습니다. ofi_norm은 08:58:54(장전 마지막 관측 시점)까지 계속 고정 상태였습니다.
- **근거**:
  ```
  logs/20260904_SIGNAL.log
  08:45:16 [WARNING] [CORE준비도] trigger=A_WARMUP 축퇴 3/6 — above_vwap×6hz cvd_divergence×6hz ofi_norm×6hz
  08:47:55 [WARNING] [CORE준비도] trigger=A_WARMUP 축퇴 1/6 — ofi_norm×6hz
  08:49:53 [WARNING] [CORE준비도] trigger=A_WARMUP 축퇴 1/6 — ofi_norm×6hz
  08:54:53 [WARNING] [CORE준비도] trigger=A_WARMUP 축퇴 1/6 — ofi_norm×6hz
  08:58:54 [WARNING] SIGNAL: [CORE준비도] trigger=A_WARMUP 축퇴 1/6 — ofi_norm×6hz
  08:58:54 [WARNING] SIGNAL: [ScalerRefresh] 15m CORE 'ofi_norm' raw_std≈0(0.0278) → identity(0,1) 강제
  08:58:54 [WARNING] SIGNAL: [ScalerRefresh] 30m CORE 'ofi_norm' raw_std≈0(0.0278) → identity(0,1) 강제
  ```
- **기준 위반**: 없음 — 이 계측은 절대원칙 3(CORE 피처 강제)을 차단하는 장치가 아니라 관찰 전용(섀도)이다.
- **영향**: 개장 직전 특정 신호 하나가 반응이 둔한 상태로 시작하는 건 흔한 일이라 오늘 하루만으로는 문제라고 볼 수 없습니다. 다만 장이 열린 뒤 실제로 풀리는지는 장중에 확인이 필요합니다.
- **영향(기술)**: 470차 B4가 설계한 `CORE준비도`/`CORE축퇴_피처` 섀도 채널의 정상 범위 내 관측치. `ofi_norm`은 §12 고착 지표 표에서도 최빈값(176/339)으로 이미 추적 중 — 신규 아님. 개장 후 해소 여부는 파생 지표 `CORE축퇴_개장해소`(494차 G-1)가 매일 판정한다.
- **신규 여부**: 기존 관찰 채널(470차 B4, 494차 G-1) — 신규 등록 없음. 장중 절에서 개장 후 해소 확인.

#### 1-2. 장전 점검 실행이 개장 22초 뒤에 됨 (지속, F-2 사용자 승인 대기)

- **증상**: 예약된 장전 점검이 09:00:21(개장 22초 후)에 실행됐습니다. 최근 5거래일 모두 개장 이후 실행(09-03 −22초, 09-02 −228초, 09-01 −94초, 08-31 −7초, 08-27 −30초) — 장전/장중 표본이 섞일 수 있는 구조입니다.
- **근거**: `evidence_MW0602-20260904_pre.md` §「장전 발화 마진」 — 생성 09:00:21, 마진 −22초.
- **기준 위반**: 없음(운영 일정 문제이지 코드 결함 아님).
- **영향**: 이 점검 결과에 "오늘 이 시점까지 이미 일어난 거래"가 섞여 들어올 수 있습니다. 다행히 오늘은 이 시점까지 진입이 0건이라 실제 영향은 없었습니다.
- **영향(기술)**: 수집기 `_is_tight()`는 `sec<0`(개장 후 실행)을 "마진 빠듯함" 연속 판정에서 제외한다 — 이건 "장전 점검이 아예 늦게 도는" 별도 사건이다. 501차가 예약 시각을 **08:58:40**으로 조정하는 안(F-2, P2)을 이미 제안했으나 사용자 승인 대기 상태로 남아 있다.
- **신규 여부**: 기존 TODO(`F-2`, 501차) — 재제안 아님.

#### 1-3. 런타임 라이브러리 버전이 로그에 안 남음 (지속)

- **증상**: `phases.md` A-1이 scipy 1.5.4 / scikit-learn 1.0.2 / joblib 1.1.1 확인을 요구하지만, 오늘 로그 전체에 이 버전 문자열이 0건입니다. 32-bit Python 사용 여부만 런처 로그로 확인됩니다(`[INFO] 32-bit Python 확인: ...py37_32\python.exe`).
- **기준 위반**: 없음(위반이 아니라 "확인 안 됨" — 계측 4원칙 ②).
- **영향**: 라이브러리 버전이 실수로 바뀌어도 지금은 아무도 모르고 지나갈 수 있습니다. 지금 당장 문제가 생긴 것은 아닙니다.
- **영향(기술)**: 501차가 Fix `F-6`(`[Runtime]` 무조건 상태 샘플 1줄, `F-2`와 한 커밋)을 이미 계획해뒀으나 아직 배포되지 않음.
- **신규 여부**: 기존 TODO(`F-6`, 501차) — 재제안 아님.

### 확인 필요 (확정 아님)

- **`O-54` 판정 완료(PASS)**: "기동 시각 08:40 자동 경로 복귀"가 오늘 정상 확인됐습니다 — `[GUARD] 장전 [0840] -- 10초 후 자동 Y (스케줄러 자동 기동)`. 08-31 등록 이후 처음 도래한 판정일(다음 거래일 장전)에 정상 회복을 확인해 **종결**합니다.
- **`O-39`·`O-50`(브로커 순손익 상태 검증) — 아직 판정 불가, 장중·장후로 이월**: 오늘 08:41:16 로그에 `[BrokerNet] state=SKIP_NON_TRADING date=2026-09-04`가 찍혀 있는데, 이건 당일 예측이 아직 없는 장전 상태에서는 **정상**입니다(501차 등록문에 "장전 SKIP_NON_TRADING은 정상"이라고 명시돼 있음). 실제 판정은 장중·장후에 `state=OK`로 바뀌는지 봐야 합니다. 08-31 등록 후 4거래일째 판정을 못 내린 상태라 — 오늘 장중·장후 세션이 반드시 판정할 것.
- **`launcher_guard` 고착 지표 10일 연속 "발화"는 새 문제가 아닙니다**: §11 자동 적신호가 최근 10거래일 100% "발화"를 보여주지만, 이 지표의 사전등록 판정 규칙("10거래일 연속 → P1 승격")은 **이미 판정 보류로 확정**돼 있습니다 — 원인이 런처 스크립트의 `!=` 문법 결함(F-1)이라 분자 자체가 오염된 것으로 규명됐고(500차), "F-1 적용 전에는 P1 승격 근거로 쓰지 않는다"고 09-03 세션이 명시했습니다(`O-17` 판정 보류 유지). 여기서 재등록하지 않습니다 — 함정①.

### 이미 반영된 사안 (신규 아님)

- **CB② 사실상 비활성(`CB_CONSEC_STOP_LIMIT=9999`)**: 모의투자 한정 예외로 이미 결정되어 있고, 오늘 설정값도 `9999`로 정확히 일치합니다(2026-08-23 489차 D2 — 복원 시점은 날짜가 아니라 "실전 자본 재설정" 사건에 연동).
- **런처 GUARD `!=` 결함(`F-1`)**: 2026-08-26(500차)에 이미 원인 규명 완료, 수정안도 마련됐으나 **라이브 런처의 실제 동작을 바꾸는 변경**이라 사용자 결정 대기 중입니다(`O-67`로 계속 관찰, 아래 사용자 조치 참조). 오늘 새로 조사할 것 없음.
- **전일 EOD 재학습 08:55 스킵**: `[PreRetrain] 08:55 사전 재학습 스킵 — 1영업일 전(2026-09-03) EOD 재학습 성공` — CLAUDE.md에 이미 "정상"으로 명시된 동작. 이상 아님.

---

## 2. Fix 작업 구현계획

> 장전이므로 **아무 것도 실행하지 않습니다.** 아래는 계획과 사람이 결정할 사항만 정리합니다(신규 Fix 없음 — 기존 F-1·F-2·F-6가 사용자 결정/승인 대기 상태로 이어짐).

### 적용 순서와 커밋 계획

해당 없음 — 오늘 장전 세션에서 새로 낸 Fix가 없습니다. 기존 `F-1`(런처 GUARD 수정), `F-2`(예약 시각 08:58:40), `F-6`(런타임 버전 로그)는 전부 사용자 결정/승인 대기 상태로 이월됩니다.

---

## 3. 고도화 방안

당일 관측에서 새로 낼 만한 것이 없습니다 — 오늘 장전 관측은 전부 기존에 이미 등록된 항목(`F-1`·`F-2`·`F-6`·`O-39`·`O-50`)의 진행 상황 확인이었습니다.

---

## 4. 진입 승패 사후검증 (장후 한정)

해당 없음(장후 전용).

## 5. 수익률 향상방안 (장후 한정)

해당 없음(장후 전용).

---

## 6. 다음 거래일 관측 예정

오늘 새로 등록하는 관측 항목은 없습니다(다음 관측 ID는 `O-72`로 유지). 기존 항목 진행 상황:

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| `O-39`·`O-50` | `[BrokerNet] state=OK` 전이 + `daily_broker_pnl.broker_net_krw` 당일 적재 | 09:00~15:10 중 ≥1건 `state=OK` + EOD 후 당일 행 생성 | 오늘 장중+장후 (08-31 등록, 4거래일째 이월) |
| `O-67` | `F-1` 사용자 결정 여부 | 결정 이후 첫 기동일 로그가 `count=0 + decide=clear`로 전환되는지 | 사용자 결정 시점 (지속 이월) |

## 7. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| CB② 복원 (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | **사건 기한**(⑧ 실전 자본 재설정 해제와 동일 커밋) — 날짜 아님 | — | 489차 D2, 오늘 값 `9999` 일치 확인 |

---

## 8. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-09-04 (MW0602 528차 — 일일 점검: 장전)`
- `NEXT_TODO.md` 추가 체크박스: `O-54` 판정 완료 표시(`- [x]`), 나머지는 기존 항목 진행 상황 갱신
- 완료 처리한 기존 항목: `O-54`(기동 자동 경로 복귀 — PASS 판정)

### 이월 처리 요약 (오늘 아침 시점 기존 항목 상태)

| 원 번호 | 제목(줄임) | 처분 | 근거 |
|---|---|---|---|
| `O-54` | 기동 08:40 자동 경로 복귀 | ✅ 해소(판정 완료) | 오늘 `[GUARD] 장전 [0840] -- 10초 후 자동 Y` 정상 |
| `O-39`·`O-50` | 브로커 순손익 상태 검증 | 🔄 지속 | 장전 SKIP_NON_TRADING은 정상, 장중·장후 판정 필요 |
| `O-67`(`F-1`) | 런처 GUARD `!=` 수정 사용자 결정 | 🔄 지속 | 미결, 다음 거래일 이월 |
| `1-3`(런타임 버전 미기록) | `F-6` 미배포 | 🔄 지속 | 변화 없음 |
| `C-5` | 증권사 화면 VKOSPI 대조 | 🔄 지속 | **17회째 이월**, 사용자 몫 |
| 점검 지침서 앱 사본 갱신 | `save_skill overwrite:true` | 🔄 지속 | **17회째 이월**, 사용자 몫 |

미처분 0건.

---

## 사용자 조치

| # | 할 일 | 명령·경로 | 기한 |
|---|---|---|---|
| 1 | `F-1` 결정 — 런처 GUARD `!=` 결함을 지금처럼 방치할지(매일 오탐 종료 동작), 고쳐서 진짜 중복일 때만 종료하게 할지. 2~3분 | `start_mireuk.bat`(:457 부근)·`start_mireuk_CREON.bat`(:399/:434 부근) | 사용자 편할 때 |
| 2 | `F-2` 승인 — 장전 예약 실행 시각을 09:00:21(개장 후)에서 08:58:40(개장 전)으로 조정할지. 1분 | 예약작업 설정 | 사용자 편할 때 |
| 3 | 증권사(대신 Cybos Plus) HTS 화면에서 VKOSPI 실값 1회 대조(`C-5`) — 17회째 이월. 2~3분 | — | 여유 될 때 |
| 4 | 점검 지침서 앱 저장 스킬 갱신(`save_skill overwrite:true`, 정본 `rev: 2026-08-26e`) — 17회째 이월 | Claude 세션에서 `save_skill` 호출 | 여유 될 때 |

커밋 대기: `docs/정기점검/매일점검/MW0602-20260904-점검리포트.md`(신규 생성) · `dev_memory/DECISION_LOG.md`(append) · `dev_memory/NEXT_TODO.md`(append). 제외(gitignore): `evidence_MW0602-20260904_pre.md`.

---

# 장중(intra)

- 점검 시각: 12:43 KST · PC `MW0602` · 매분 루프 창 09:00~12:41 기준 관측
- HEAD `7bd2e12`(장전과 동일, 장중 신규 커밋 없음) · 미커밋 3건(`DECISION_LOG.md`·`NEXT_TODO.md`·이 리포트 파일 — 장전 산출물, 예약 실행 규약대로 미커밋 유지)
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260904_intra.md`
- 병행 세션 확인: 오늘 커밋은 07:34·08:02 두 건뿐(장전 점검 이전, 527차 스윙 피처 작업) — 장전 이후 신규 커밋·별도 산출물 없음. 다만 07:55에 별도 조사 파일이 있었다 → 아래 역링크 참조.
- 인덱스락: 없음(실행 시작·렌더 2점 확인)

## 0. 한 줄 결론

**지금까지 거래는 정상입니다. 위험한 문제는 없었습니다.** 오늘 3번 진입해 2번 이기고 1번 졌고, 합쳐서 16만 9천 원을 벌었습니다. 개장 직후 잠깐 반응이 둔했던 신호 하나(장전에 지적한 항목)는 09시 30분에 완전히 풀렸습니다. 진입한 세 건 모두 필수 확인 항목(주가 위치 판단)을 통과한 상태에서 들어갔습니다. 다만 프로그램이 주문 수량을 계산할 때 원래 내려던 수량(3계약)보다 적게(2계약) 나가는 일이 여러 번 있었는데, 이것은 새 문제가 아니라 계좌 증거금이 부족해 자동으로 줄어드는 이미 알려진 현상입니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 정상(조건부) | (장전 절 참조) |
| 장중 | **정상** | P0 0건. 진입 3건(승 2/패 1) +169,062원. 체크리스트 필수 항목(주가 위치) 전건 통과. CORE 신호 축퇴 09:30 해소. 매분 루프 커버리지 100%(222/222분) |
| 장후 | (진행 예정) | |

**절대원칙 준수 여부** (12:41 시점)

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | 진행 중(아직 시각 전) | 현재 포지션 없음(3건 모두 청산 완료). 15:10 도래 시 장후에서 판정 |
| ② | Circuit Breaker 5종 | 준수 | CB 이벤트 1건(손실 레그 카운터 기록, 정지 아님). `CB_CONSEC_STOP_LIMIT=9999`(모의투자 한정 예외) 유지 확인 |
| ③ | CORE 피처 호라이즌 그룹별(강제 X) | **준수** | 진입 3건 전부 `vwap✅` 체크리스트 통과(10:09:53·10:19:53·10:53:53 `[진입체크]` 원문 확인). 미통과 항목은 `fore`·`cvd`·`prev`뿐(등급 하락 대상, 강제 아님) — **P0 없음** |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 해당없음 | 코드 변경 없음 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 해당없음 | 코드 변경 없음 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 준수 | 흔적 없음 |

**매분 루프**: 09:00~12:41 커버리지 **222/222분(100.0%)**, 10분 이상 공백 0건. 메인 스레드 블로킹 1건(2453ms, 08:41:18 — 장 시작 전 브로커 잔고 조회 중, 5초 미만이라 CB⑤ 대상 아님).

---

## 1. 이월 처리표 (장전 → 장중)

| 원 번호 | 제목(줄임) | 처분 | 근거 |
|---|---|---|---|
| `1-1` | CORE 축퇴(ofi_norm) 개장 후 해소 여부 | ✅ **해소** | `[CORE준비도]` 09:00:53까지도 축퇴 1/6 지속됐으나 **09:30:53에 축퇴 0/6(CORE 스케일 정상)**로 완전 해소, 이후 12:38:55까지 전부 0/6 유지. 첫 진입(10:09:53)은 해소 이후라 영향 없음 |
| `1-2` | 장전 점검 22초 늦게 실행 (F-2 승인 대기) | 🔄 지속 | 변화 없음. 오늘 진입 0건 시점 실행이라 실제 영향 없었음(장전 절 판단 유지) |
| `1-3` | 런타임 라이브러리 버전 미기록 (F-6 미배포) | 🔄 지속 | 변화 없음 |
| `O-54` | 기동 08:40 자동 경로 복귀 | ✅ 해소(장전에서 이미 종결) | — |
| `O-39`·`O-50` | 브로커 순손익 상태 검증 | ✅ **판정 완료(PASS)** | 10:10:54 첫 `[BrokerNet] state=OK date=2026-09-04 dep=29247153 next=29319169 net=72016`. 이후 10:23:54·10:54:54에도 `state=OK` 갱신, 최종 net=169,067원 — `trades` 집계 net(+169,062원)과 5원 이내 일치(집계 시점 차이). 501차 요율 정정 이후 판정 원천이 라이브에서 정상 작동함을 오늘 첫 확인. 08-31 등록 후 4거래일째 미판정이던 것을 **오늘 종결** |
| `launcher_guard` 10일 연속 발화 | (§11 자동 적신호) | 🔄 지속(판정 보류 유지) | 함정① — F-1 적용 전 P1 승격 근거로 안 씀(09-03 확정 유지). 재등록하지 않음 |
| `O-55` (SizerMatch 분모 DB 고정) | tox 바인딩 게이트 80% 초과 여부 | 🔄 지속(판정 보류) | 오늘 로그 기준 바인딩 게이트 분포는 `hurst`×2·`meta`×2·`margin`×1로 `tox` 0건 — 오늘 표본만으로는 판정 재료가 아님. DB `sizing_trace` 누적 대조는 장후로 이월(장중 라이브 DB 전수 스캔 금지 원칙) |

**미처분 0건.**

**병행 산출물 역링크** — `docs/정기점검/매일점검/MW0602-20260904-스윙피처_도입_및_VWAP-TrendGate_상호작용_검토.md`(07:40~08:30 작성, 527차): N봉 스윙 고점·저점 피처를 `feature_builder`에 배선 완료(라이브 로직 무변경, 커밋 완료 `7bd2e12`). 핵심 발견은 "많이 올라왔으니 조심"이 분봉 전체에서는 맞지만 실제 진입 표본에서는 반대(신선한 추세 극단 진입이 오히려 흑자)라는 것 — 게이트에 바로 반영하지 않고 섀도 채널 `[60]`으로 먼저 관찰하기로 함. **오늘 장중 판정에 영향 없음**(관찰만 추가, 차단 로직 무변경).

---

## 2. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

없음.

### P1 — 정확성·관측 훼손

없음.

### P2 — 운영 부담·기술부채

#### 2-1. 사이저가 3계약을 내면 증거금이 먼저 2계약으로 자른다 (지속 관찰, 신규 아님)

- **증상**: 오늘 사이저(포지션 크기 계산기)가 9번 호출돼 매번 "3계약"을 산출했지만, 실제로 주문이 나간 것은 전부 2계약이었습니다. 09:54:53 등 9차례 모두 `[MarginCap] state=OK LONG 산출=2 상한=2`로 증거금 한도가 먼저 2계약으로 줄였습니다.
- **근거**:
  ```
  logs/20260904_TRADE.log
  09:54:53 [INFO] [Sizer] 미니선물 실효잔고=50,000,000(실제잔고=29,247,153) 기본리스크=1,500,000 신뢰도배수=0.6 레짐배수=1.0 안전배수=1.00(정상) → 3계약 (최소=1)
  09:54:53 [INFO] [MarginCap] state=OK LONG 산출=2 상한=2
  ```
  실효 상한 분해(§5): 품질게이트 `[SizerMatch]` 5건(3→2), 증거금 `[MarginCap]` 1건(3→2).
- **기준 위반**: 없음 — 설계대로 동작(증거금 부족 시 자동 축소는 정상 안전장치).
- **영향**: 오늘 거래에는 문제가 없었습니다. 다만 이 상태가 계속되는 한, "계약 수를 3개까지 늘렸을 때 성과가 어떤지"를 확인하는 별도 관찰 장치가 표본을 전혀 쌓지 못합니다.
- **영향(기술)**: CLAUDE.md 실전 전환 기준 ⑧ 각주(470차)가 정확히 지적한 구조 — 모의 목표자본 5천만원이 실제잔고(2,900만원대)보다 커서 증거금이 `MAX_CONTRACTS=3`보다 먼저 구속한다. `[28] sizing_inversion_watch` 채널은 이 상태에서 구조적으로 qty≥3 표본을 못 쌓는다(431차 이후 74포지션 전부 qty≤2, 오늘도 동일 패턴 지속). 이미 등록된 문제이며 해제 조건은 ⑧(실전 자본 재설정)과 묶여 있다.
- **신규 여부**: 기존 관찰(470차, 491차 O-55). 재등록 아님.

### 확인 필요 (확정 아님)

- **손절 준수율 1건 표본** — 10:19:54 진입(SHORT 2계약)이 10:20:14 부분손절(-0.94pt)을 거쳐 10:23:54 하드스톱[TP1보호]로 최종 -23,984원 마감했습니다. 표본이 1건뿐이라 확정 결론은 낼 수 없습니다(313차 원칙) — 장후 승패 사후검증에서 나머지 2건과 함께 요인 귀인할 것.
- **10시대 진입 3건 전부** — `O-70`(526차 후속6, "10시대 진입 열위" 관측)과 같은 시간대에 오늘 진입이 몰렸습니다(10:09·10:19·10:53). 표본 3건으로는 판정 재료가 안 되나, 장후에 이 관측 채널의 누적치에 오늘 표본을 반영할 것.

### 이미 반영된 사안 (신규 아님)

- **T-30(30분) 채점 정상 지속** — CB③의 유일한 입력원인 30m 예측 채점이 09:30:53부터 매분 정상 발행 중(`✓/✗ 30m 예측 적중/실패`). CLAUDE.md 474차 각주대로 채점 유지 확인.
- **DriftRetrain(STEP 3) 조건 미충족으로 재학습 0회** — `state=SKIP acc5m=0.0% n=0`이 반복되는 것은 표본 부족(n=0)으로 조건 미충족일 뿐 정상. 기존 관측 `O-6`(491차)과 동일 패턴, 재등록 안 함.
- **[Brier] 과신 경고 / [SHAP] 슬로우 감지 / degraded=ON / MemoryError** — 오늘 로그 전체에서 0건. 이상 없음.
- **macro_vix 스케일러 플로어 강제(ScalerFloor 654건)** — CLAUDE.md에 이미 "일봉 VIX→분봉 상수" 알려진 동작, 신규 아님.

---

## 3. Fix 작업 구현계획

> 장중이므로 **아무 것도 실행하지 않습니다.** 라이브 프로세스가 돌고 있습니다. 신규 Fix 없음 — 기존 `F-1`·`F-2`·`F-6`는 장전과 동일하게 사용자 결정/승인 대기 상태로 이월됩니다.

---

## 4. 고도화 방안

당일 관측에서 새로 낼 것이 없습니다. 오늘 장중 관측은 기존 채널(`sizing_inversion_watch`, `O-55`, `O-70`)의 진행 상황 확인이었습니다.

---

## 5. 진입 승패 사후검증 (장후 한정)

해당 없음(장후 전용).

## 6. 수익률 향상방안 (장후 한정)

해당 없음(장후 전용).

---

## 7. 다음 거래일 관측 예정 (갱신)

| ID | 판정 | 비고 |
|---|---|---|
| `O-39`·`O-50` | ✅ **오늘 판정 완료(PASS)** | 위 이월표 참조. 다음 거래일부터는 일상 관측(§12b 임계 대조)으로 격하 가능 |
| `O-67`(`F-1`) | 🔄 지속 | 사용자 결정 시점까지 이월 |
| `O-55` | 🔄 지속(판정 보류) | 장후 DB `sizing_trace` 대조로 이월 |
| `O-70` | 🔄 지속 | 오늘 10시대 진입 3건 표본 추가, 장후 누적 반영 |

---

## 사용자 조치 (장전과 동일 — 변경 없음)

| # | 할 일 | 명령·경로 | 기한 |
|---|---|---|---|
| 1 | `F-1` 결정 — 런처 GUARD `!=` 결함 방치/수정 여부. 2~3분 | `start_mireuk.bat`(:457 부근)·`start_mireuk_CREON.bat`(:399/:434 부근) | 사용자 편할 때 |
| 2 | `F-2` 승인 — 장전 예약 실행 시각 08:58:40 조정 여부. 1분 | 예약작업 설정 | 사용자 편할 때 |
| 3 | 증권사 HTS 화면 VKOSPI 실값 1회 대조(`C-5`) — 17회째 이월. 2~3분 | — | 여유 될 때 |
| 4 | 점검 지침서 앱 저장 스킬 갱신(`save_skill overwrite:true`, 정본 `rev: 2026-08-26e`) — 17회째 이월 | Claude 세션에서 `save_skill` 호출 | 여유 될 때 |

커밋 대기: `docs/정기점검/매일점검/MW0602-20260904-점검리포트.md`(장중 절 append) · `dev_memory/DECISION_LOG.md`(append) · `dev_memory/NEXT_TODO.md`(append, O-39·O-50 판정완료 반영). 제외(gitignore): `evidence_MW0602-20260904_intra.md`.

---

# 장후(post) — 종합 완성본

- 점검 시각: 16:33 KST · PC `MW0602` · EOD+P8 범위(15:40 마감 + 15:47 스케일러 재적합)까지 확인
- HEAD `7bd2e12`(장전·장중과 동일, 오늘 신규 커밋 없음) · 미커밋 19건(정기점검 산출물 4주 보관정리 회전분 12건 + dev_memory 2건 + 오늘 리포트류 5건 — 전부 예상된 변경, 코드 변경 0건)
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260904_post.md` (`--skill-rev 2026-08-26e` 명시 제출 — §2 정본 rev와 **일치**, 정본 rev 무결성도 정합)
- 병행 세션 확인: 장중 절(12:43) 이후 신규 커밋 없음. `ls -lt`로 12:45 이후 새 산출물도 없음 — 이번 장후가 오늘의 마지막 세션.
- 인덱스락: 없음(2점 확인)

## 0. 한 줄 결론

**오늘 하루 전체가 안전하게 끝났습니다. 위험한 문제는 없었습니다.** 오늘 세 번 거래해서 두 번 이기고 한 번 졌고, 수수료를 빼고 16만 9천 원을 벌었습니다. 오후 3시 10분에 남아있던 포지션은 없어서(이미 다 청산된 뒤였습니다) 밤새 위험을 떠안는 상황도 없었습니다. 하루 마감 작업(모델 재학습)과 그 뒤 스케일 재조정 작업도 모두 정상적으로 끝났습니다. 다만 매주 금요일마다 도는 파라미터 재점검 장치 세 개가 오늘 "지금 설정값이 최근 시장 흐름과 꽤 벌어졌다"는 경보를 울렸습니다 — 자동으로 바뀌는 건 아니고 사람이 검토해서 반영 여부를 정하는 장치라서, 아래 "사용자 조치"에 정리해뒀습니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 정상(조건부) | P0 0건. 브랜치·설정 일치. 전일 EOD 정상. 레짐 RISK_ON 확정 |
| 장중 | 정상 | P0 0건. 진입 3건(승2/패1) +169,062원. 체크리스트 필수 항목 전건 통과 |
| **장후** | **정상** | P0 0건. 15:10 시점 포지션 0(강제청산 대상 없음, 정상). EOD 15:40 완료·P8 15:47 완료. 매분 루프 09:00~15:10 371/371분(100.0%). 종가 net **+169,062원** |
| **최종** | **정상 — 오늘 절대원칙 위반 0건, P0 0건** | |

---

## 1. 이월 처리표 (장중 → 장후)

| 원 번호 | 제목(줄임) | 처분 | 근거 |
|---|---|---|---|
| `1-1` | CORE 축퇴(ofi_norm) 개장 후 해소 | ✅ 해소(장중에서 이미 종결) | — |
| `1-2` | 장전 예약 22초 지연(F-2 승인 대기) | 🔄 지속 | 변화 없음. 다음 거래일 이월 |
| `1-3` | 런타임 라이브러리 버전 미기록(F-6 미배포) | 🔄 지속 | 변화 없음 |
| `O-54` | 기동 08:40 자동 경로 복귀 | ✅ 해소(장전에서 이미 종결) | — |
| `O-39`·`O-50` | 브로커 순손익 상태 검증 | ✅ 판정 완료(PASS, 장중에서 이미 종결) | 장후 EOD 최종값으로 재확인 — `net=169,067` vs `trades` 집계 `+169,062` (차 5원, 집계 시점 차이) 재일치 확인 |
| `O-55` | SizerMatch 판정분모(tox 바인딩 비율) DB 고정 | 🔄 지속(판정 보류, 표본 추가) | `ensemble_decisions.sizing_trace` 오늘 13건 직접 조회: `binding=hurst`×9·`meta`×3·`margin`×1·**`tox`×0**. 08-31 등록 당시 분모 정의(`sizing_trace` 보유 행)로 오늘도 tox 0% — 여전히 min_samples 미달, 누적 판정은 다음 26주 주기/캠페인 채널로 이월 |
| `O-70` | 10시대 진입 열위 관측 | 🔄 지속(표본 추가) | 오늘 진입 3건 전부 10시대(10:09·10:19·10:53) — 누적 반영. 오늘만으로는 판정 재료 아님(313차) |
| `O-67`(`F-1`) | 런처 GUARD `!=` 결함 사용자 결정 | 🔄 지속 | 미결, 다음 거래일 이월 |
| `launcher_guard` 10일 연속 발화 | (§11 자동 적신호) | 🔄 지속(판정 보류 유지) | 함정① — F-1 적용 전 P1 승격 근거로 안 씀(기존 확정 유지) |
| 병행 산출물(스윙피처 조사) | 07:40~08:30, 527차 | ✅ 처리 완료(장중에서 역링크 append) | 오늘 판정에 영향 없음, 재확인 불필요 |

**미처분 0건.**

---

## 2. 이상점 정리보고 (장후 신규분)

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

없음.

### P1 — 정확성·관측 훼손

없음 신규.

### P2 — 운영 부담·기술부채

#### 3-1. 매주 금요일 파라미터 재점검 장치 3종이 전부 "갱신 필요"를 알림 (신규, 사람 검토 필요)

- **증상**: 미륵이는 매주 금요일 마감 때 "지금 쓰는 임계값이 최근 시장과 여전히 맞는지" 스스로 점검하는 장치 세 개를 돌립니다. 오늘(금요일) 이 세 장치가 전부 "지금 값과 최근 시장 흐름 사이 차이가 커졌다"고 알렸습니다. 자동으로 값을 바꾸지는 않습니다 — 사람이 보고 결정하도록 일부러 그렇게 설계돼 있습니다.
- **근거**:
  ```
  logs/20260904_WARN.log
  15:40:25 [WARNING] [ThresholdRecal] 경보 발생: {'1m': 'UPDATE', '3m': 'UPDATE', '5m': 'UPDATE', '10m': 'UPDATE', '15m': 'UPDATE', '30m': 'UPDATE'}
  15:40:25 [WARNING] [ATRCeilingRecal] UPDATE — floor 3.5→2.0pt (-43%), ceiling 6.0→3.0pt (-50%) — Slack 알림 발송, 수동 검토 필요
  15:40:26 [WARNING] [EntryHorizonRecal] UPDATE — 경계 3.2/4.4 → 재계산 5.8/8.217 (δ+81%/+87%), 버킷비중(1m/3m/5m)=1%/11%/88% — 수동 검토 필요
  ```
- **기준 위반**: 없음 — 375차·386차가 만든 정상 설계(자동반영 금지, 매주 감시하며 사람이 판단). 지금 이 상수들(`ENTRY_HORIZON_B1/B2`, ATR 상하한)이 "틀렸다"는 뜻이 아니라 "다시 봐야 할 때가 됐다"는 뜻입니다.
- **영향**: 지금 당장 거래에 문제가 생긴 것은 아닙니다. 다만 재계산값 방향이 꽤 큽니다 — 진입 호라이즌 경계가 대폭 올라가면(3.2→5.8, 4.4→8.217) 앞으로 진입 판단이 1분/3분 위주에서 5분 위주(버킷비중 88%)로 크게 쏠릴 수 있고, ATR 상하한이 내려가면(-43%~-50%) 변동성 부족으로 막히는 진입이 늘어날 수 있습니다. 검토를 오래 미루면 실제 시장과 더 벌어진 채로 운영하게 됩니다.
- **영향(기술)**: `ENTRY_HORIZON_B1`(3.2)·`ENTRY_HORIZON_B2`(4.4)는 374·387차가 재보정한 값이고, `ATR_EXPIRY_CEILING`류는 303·386차 계열. 둘 다 "정적 상수가 몇 주 만에 시장과 어긋난다"는 같은 실패를 막으려고 만든 감시 장치(`entry_horizon_recalibrator.py`, `atr_ceiling_monitor` 계열)이며 자동 반영 없음이 설계 원칙 자체다. `entry_horizon_recalibrator.recent_log()`로 1회성 튐인지 추세인지 먼저 확인한 뒤 반영 여부를 결정하는 절차가 이미 문서화돼 있다(`dev_memory/DECISION_LOG.md` 375차·386차 How to apply).
- **신규 여부**: 계측 자체는 기존(375·303·386차) — 다만 **오늘 경보가 실제로 발생한 것은 신규 관찰**이라 사용자 조치로 올린다.

#### 3-2. 오늘 EOD 재학습에서 1m·3m 모델 교체가 모델가드에 걸려 보류됨 (지속 관찰, O-59 표본)

- **증상**: 오늘 EOD 재학습이 6개 호라이즌 중 5m·10m·15m·30m 4개는 교체했지만, 1m과 3m은 "새 모델이 기존보다 정확도가 떨어진다"는 안전장치에 걸려 교체를 보류하고 기존 모델을 유지했습니다.
- **근거**:
  ```
  logs/retrain_eod_20260904.log
  15:45:33 [WARNING] [Retrain] 1m 교체 보류(EOD 모델가드) — acc 하락 0.0271 > 허용 0.0250 (new=0.3570 old=0.3841) — 참고용 저장, 구모델 유지
  15:45:36 [WARNING] [Retrain] 3m 교체 보류(EOD 모델가드) — acc 하락 0.0485 > 허용 0.0250 (new=0.3552 old=0.4037) — 참고용 저장, 구모델 유지
  ```
  `data/eod_retrain_done_20260904.txt`: `horizons_replaced: 4/6`
- **기준 위반**: 없음 — 모델가드가 설계대로 나쁜 모델 교체를 막은 것(정상 방어 동작).
- **영향**: 내일도 1m·3m은 오늘 이전의(더 오래된) 모델로 예측합니다. 나쁜 일은 아니지만, 이 상태가 계속되면 그 두 호라이즌은 최신 시장 데이터를 학습에 반영하지 못한 채 멈춰 있게 됩니다.
- **영향(기술)**: `O-59`(526차 후속9 등록, 판정일 2026-09-07)가 "3m 교체 보류 연속 일수 5거래일 누적"을 감시 중 — 오늘이 그 표본에 하루 추가됐다(누적 진행 상황은 다음 판정일에 종합). 모델가드 허용치(0.0250) 자체를 낮추자는 제안은 하지 않는다 — 매매 정책 변경이며 1일 관측은 근거가 못 된다(기존 결정 유지).
- **신규 여부**: 기존 관측(`O-59`) — 표본 추가일 뿐 재등록 아님.

### 확인 필요 (확정 아님)

- **`O-58` 오탐 정정**: §11 자동 적신호가 "판정 기준 로그 `[Retrain]`가 오늘 자 로그에 이미 있다"고 표시했으나, 직접 확인 결과 오늘 로그의 `[Retrain]` 태그는 전부 **호라이즌별 배치 재학습**(O-58과 무관한 일상 동작)이었다. O-58의 실제 판정 대상 로그는 `[Retrain] DB pruning`이고, 오늘 로그에는 `[스킵] DB 행 삭제 비활성 — --allow-db-prune 을 붙여야 후보가 된다`만 있다 — **판정 조건 미충족, 정상**. §11 스캐너가 태그 이름의 부분일치(`[Retrain]`)로 오탐한 것으로 규명. O-58은 원래 등록대로 **2026-09-07(다음 월요일 EOD)까지 이월**한다.
- **CASE-02(10:19 진입, -23,984원) 요인 귀인**: 아래 제4부 참조. 표본 1건이라 확정 결론 내지 않음(313차).

### 이미 반영된 사안 (신규 아님)

- **CB③-P4 30m 정확도 단계 전환(NORMAL→RESTRICTED→WATCH→NORMAL, 10:22~14:56)**: `CB3_P4_GRADE_BLOCK_ENABLED=False`라 등급 차단에는 영향 없음(296·297차 한시예외). 섀도 관측대로 정상.
- **`OTHER` 존(11:50~13:00) 진입 0건**: 462차 P1-a가 이미 등록한 시간대 정의 공백(진입 금지)의 정상 결과. [53] 채널 판정 대기 중, 재등록 안 함.
- **`mc-conf 괴리` 경보(오늘 35분 < 5일평균 42분 < 하한 60분)**: 기존 추적 채널(MC_CONF_GAP_ALERT). 재등록 안 함, 추이만 누적.
- **GuardFair 6/6 전 호라이즌 무효**: 457차 설계상 판정 무영향(섀도). 26주 WFA 「고착 지표 감시목록」에 이미 등재. 재등록 안 함.

---

## 3. Fix 작업 구현계획

> 오늘 새로 낸 Fix 없음. 기존 이월 항목(`F-1`·`F-2`·`F-6`)은 전부 사용자 결정/승인 대기 상태를 유지하며, 신규로는 **재보정 모니터 3종 검토**(3-1)만 사용자 결정 사항으로 추가한다. 코드 변경 제안 없음 — 세 모니터 모두 "자동 반영 금지"가 설계 원칙이라 지금 코드를 고칠 대상이 아니다.

---

## 4. 고도화 방안

당일 관측에서 새로 낼 만한 일반 고도화 제안은 없다. 오늘 얻은 것은 기존 채널(`O-55`·`O-59`·`O-70`)의 표본 추가와 재보정 모니터 경보 확인이 전부다 — 제5부에서 표본 기반 항목만 낸다.

---

## 5. 진입 승패 사후검증 (제4부)

### 5-0. 3원 대사 (딥다이브 전 선행 확인)

| 원천 | 질의 | 값 |
|---|---|---|
| 로그 | `[Position] 진입` | 3 |
| DB(판정) | `ensemble_decisions.entry_executed=1` | 3 |
| DB(체결) | `trades.COUNT(DISTINCT entry_ts)` | 3 |
| DB(체결, 레그) | `trades.COUNT(*)` | 6(부분 3 + 최종 3 — 불일치 아님) |

**포지션 3 = 3 = 3 — 일치.** 관측 훼손 없음.

### 5-1. 케이스 단위 딥다이브

```
[CASE-01] 진입 10:09:53 SHORT · 2계약 (등급 A/원시C · horizon=3m · hurst=neutral)
          → 10:10:08 TP1 부분청산 33% (+0.76pt) → 10:10:53 하드스톱[TP1보호] (+0.84pt, tp1_reached=1)
          포지션 합 +1.60pt / +76,020원 · 보유 60초
 진입근거   [진입체크] "SHORT→SHORT 2계약 A급(원시C) | sign✅ conf✅ vwap✅ cvd❌ ofi✅ fore❌ prev❌ … | conf=37.4%"
            [Sizer] 3계약 산출 → [MarginCap] state=OK 산출=2 상한=2 (SizerMatch 경로로 이미 2, MarginCap은 확인만)
 예측 vs 실현  ensemble_decisions: confidence=0.3742(min_conf_effective=0.371 — 임계 바로 위) meta_action=reduce(×0.632)
            predictions(3m, ts=10:09): direction=-1 confidence=0.382 actual=0 correct=0 (3분 뒤 시점 라벨은 "보합"으로 채점)
 승패판정   ✅승 — `하드스톱`이지만 `tp1_reached=1`인 TP1 보호 트레일 이익 청산(진짜 손절 아님)
 주 요인    C 청산 규칙 — 진입 15초 만에 TP1 도달 후 보호 전환, 3분 라벨(actual=0)이 나오기 훨씬 전에 인트라바 경로로 이익 확정
 부 요인    E 체결 — 체결가 1047.12~1047.16(진입 요청가 1047.38 대비 유리한 체결, 슬리피지 역방향)
 반사실     "TP1을 못 만나고 3분을 다 채웠다면?" — 3분 라벨 actual=0(보합)이라 무승부 근처였을 가능성. 관찰만, 판단 보류(313차)

[CASE-02] 진입 10:19:54 SHORT · 2계약 (등급 A/원시C · horizon=3m · hurst=neutral)
          → 10:20:14 손절1차 조기축소 (-0.94pt) → 10:23:54 하드스톱[TP1보호] (+0.54pt, tp1_reached=1)
          포지션 합 -0.40pt / -23,984원 · 보유 4분
 진입근거   [진입체크] "SHORT→SHORT 2계약 A급(원시C) | sign✅ conf✅ vwap✅ cvd✅ ofi✅ fore❌ prev✅ … | conf=41.4%"
            [Sizer] 3계약 산출 → **[MarginCap] state=CAP 산출=3 상한=2** (오늘 유일하게 증거금이 직접 구속한 사례,
            `sizing_trace.binding_gate="margin"`과 로그가 정확히 일치 — 대사 확인됨)
 예측 vs 실현  ensemble_decisions: confidence=0.4143(min_conf_effective=0.371) meta_action=reduce(×0.5)
            predictions(진입 시각 근접): 10m@10:20 direction=-1 correct=1, 5m@10:20 direction=-1 correct=1,
            1m@10:20 correct=0, 3m@10:20 direction=0 actual=-1 correct=0(가장 가까운 3m 행은 진입 1분 뒤 것)
 승패판정   ❌패 — 손절1차 조기축소로 1계약 먼저 손실 확정(-48,992원), 남은 1계약은 이후 유리하게 되돌아와
            보호청산(+25,008원)됐지만 합산 순손실
 주 요인    C 청산 규칙 — 손절1차 조기축소(틱 단위 방어 스톱)가 진입 20초 만에 역행 틱(1049.52)에 걸려
            1계약을 먼저 잘라냈고, 그 직후 가격이 유리한 방향(1048.04)으로 되돌아왔다
 부 요인    A 방향예측 — 중기(10m·5m)는 하락 적중, 단기(1m)는 불일치 — 혼재된 신호에서 짧은 역행이 먼저 온 경우
 반사실     "손절1차가 한 틱 더 넓었다면?" — [25]류 청산 오프셋 채널의 관할이며 여기서는 관찰만.
            표본 1건, 313차 원칙상 이 사례 하나로 손절1차 폭을 조정할 근거는 안 된다

[CASE-03] 진입 10:53:53 SHORT · 2계약 (등급 A/원시C · horizon=3m · hurst=mean-revert)
          → 10:54:14 TP1 부분청산 33% (+0.57pt) → 10:54:54 하드스톱[TP1보호] (+1.85pt, tp1_reached=1)
          포지션 합 +2.42pt / +117,026원 · 보유 61초 · 오늘 최대 수익 케이스
 진입근거   [진입체크] "SHORT→SHORT 2계약 A급(원시C) | sign✅ conf✅ vwap✅ cvd❌ ofi✅ fore❌ prev❌ … | conf=40.5%"
            [Sizer] 3계약 산출 → [MarginCap] state=OK 산출=2 상한=2
 예측 vs 실현  ensemble_decisions: confidence=0.4047(min_conf_effective=0.364) meta_action=reduce(×0.532)
            predictions(3m, ts=10:53): direction=-1 confidence=0.3438 actual=-1 correct=1 — 3분 라벨도 방향 적중
 승패판정   ✅승 — 방향예측 적중 + TP1 보호 전환까지 깔끔하게 이어진 케이스
 주 요인    A 방향예측 적중 (3m 라벨까지 확인됨 — 오늘 세 건 중 유일하게 라벨 자체가 방향 일치)
 부 요인    C 청산 규칙 — TP1 보호 전환이 손익을 더 키움(+0.57pt 부분 → 최종 +1.85pt)
 반사실     없음 — 방향·청산 모두 설계대로 작동한 케이스
```

### 5-2. 집계표 (313차 적용)

| 요인 | 승 | 패 | 순손익 | 평균 보유(분) | 관측 거래일 수 |
|---|---|---|---|---|---|
| A(방향적중, 주) | 1 | 0 | +117,026원 | 1.0 | 1 |
| C(청산규칙, 주) | 1 | 1 | +76,020 / -23,984원 | 2.5 | 1 |

- 포지션 단위. 승패 판정은 `tp1_reached` 확인 후(§3-1 기준)로 했다.
- **313차 다섯 갈래 적용**: ① 판정 단위 일자 — 오늘 관측일 **1일**뿐이라 어떤 요인도 결론으로 못 올림. ② overlap — 표본 3건 모두 비중첩(진입~청산 완전 분리 시간대). ③ 이상치 분해 — CASE-03 하나가 순손익의 69%를 차지(+117,026/169,062) → 소수 관측이 하루 손익을 끌고 있다는 뜻, 하루 단독으로 "청산 규칙이 좋다/나쁘다" 결론 내지 않음. ④ 사전등록 — 새 임계·파라미터 변경 제안 없음, 기존 채널(`O-59`·`O-55`)로 표본만 누적. ⑤ 부호 일관성 — 단일 PC·단일일이라 교차 비교 대상 없음, "이 부호가 다른 날에도 재현되는지"는 아직 모름.
- **결론**: 오늘 표본(n=3, 관측일=1)으로는 어떤 파라미터 변경도 근거가 되지 못한다. CASE-02가 보여준 "손절1차 조기축소가 역행 틱을 먼저 잡고 그 뒤 유리하게 되돌아온" 패턴은 **가설로만 등록**하고, 기존 청산-오프셋 계열 채널([25] 등)의 표본이 쌓일 때 함께 재확인한다.

---

## 6. 수익률 향상방안 (제5부)

| # | 근거 | 변경 대상 | 기대효과 | 회귀 위험 | 검증 | 표본 상태 |
|---|---|---|---|---|---|---|
| 1 | CASE-02(손절1차 조기축소가 역행 틱 하나에 걸려 유리한 되돌림 직전 손실 확정) | (제안 없음 — 관찰 등록만) `strategy/exit/loss_tier1.py` 계열의 조기축소 트리거 폭 | 산정 불가(표본 1건) | 조기축소 폭을 넓히면 진짜 추세 손절의 지연·확대 위험 | 기존 [25]류 청산-오프셋 섀도 채널에 오늘 케이스를 관찰로 추가, 최소 20건 누적 후 사전등록 판정 | n=1, 관측일=1 — **313차 가드 미통과, 파라미터 변경 근거 아님. 관찰만 등록** |
| 2 | 3-1(재보정 모니터 3종 UPDATE 경보) | `model/ensemble_decision.py: ENTRY_HORIZON_B1/B2`, ATR 상하한 상수(설계상 사람이 수동 반영) | 산정 불가(반영 여부 자체가 사용자 결정 사항) | 반영 시 진입 호라이즌 분포가 5분 위주로 크게 이동(버킷비중 1%/11%/88%) — 과소 반응이든 과잉 반응이든 매매 정책을 바꾸는 변경 | `entry_horizon_recalibrator.recent_log()`로 1회성 튐인지 추세인지 먼저 확인 후 반영 결정 (기존 절차) | 오늘 1회 경보 — 추세 여부 미확인. **파라미터 변경 근거로 쓰지 않음, 사용자 검토 대상으로만 이관** |

> 두 항목 모두 "당장 코드를 고치자"가 아니라 "표본/추세를 더 보고 결정하자"다 — 오늘 하루의 표본 크기(진입 3건, 관측 1일)로는 그 이상을 주장할 근거가 없다(313차).

---

## 7. 종합 판정표 · 당일 이상점 통합 대장

### 7-1. 종합 판정표

| 국면 | 판정 | P0 | P1 | P2 |
|---|---|---|---|---|
| 장전 | 정상(조건부) | 0 | 0 | 3(1-1~1-3, 지속 채널) |
| 장중 | 정상 | 0 | 0 | 1(2-1, 지속 채널) |
| 장후 | 정상 | 0 | 0 | 2(3-1·3-2, 신규 관찰) |
| **최종** | **정상 — 절대원칙 위반 0건** | **0** | **0** | **6건(누적, 전부 지속/관찰 — 즉시 조치 요구 P0·P1 없음)** |

### 7-2. 당일 이상점 통합 대장

| 번호 | 제목(줄임) | 발견 국면 | 최초 심각도 | 최종 심각도 | 최종 처분 |
|---|---|---|---|---|---|
| `1-1` | CORE 축퇴(ofi_norm) 개장 후 해소 | 장전 | P2 | — | ✅ 해소(09:30 확인, 장중) |
| `1-2` | 장전 예약 22초 지연(F-2) | 장전 | P2 | P2 | 🔄 지속(사용자 승인 대기) |
| `1-3` | 런타임 버전 미기록(F-6) | 장전 | P2 | P2 | 🔄 지속(미배포) |
| `O-54` | 기동 08:40 자동 경로 복귀 | 장전 | 관측 | — | ✅ 해소(판정 완료 PASS, 장전) |
| `O-39`·`O-50` | 브로커 순손익 상태 검증 | 장전(이월 4거래일째) | 관측 | — | ✅ 판정 완료(PASS, 장중·장후 재확인) |
| `2-1` | 사이저 3계약→실체결 2계약(증거금/품질게이트 구속) | 장중 | P2 | P2 | 🔄 지속(⑧ 해제와 연동된 기존 구조, 재확인만) |
| `O-55` | SizerMatch 판정분모(tox 바인딩) | 장중(이월) | 관측 | — | 🔄 지속 판정 보류(오늘 tox 0/13 표본 추가) |
| `O-70` | 10시대 진입 열위 관측 | 장중(이월) | 관측 | — | 🔄 지속(오늘 3건 표본 추가) |
| `3-1` | 재보정 모니터 3종 UPDATE 경보(금요일 정례) | 장후(신규) | P2 | P2 | 🆕 신규 — 사용자 검토 필요 |
| `3-2` | EOD 1m·3m 모델 교체 모델가드 보류 | 장후 | P2 | P2 | 🔄 지속(`O-59` 표본 추가, 판정일 09-07) |
| `O-58` | EOD DB pruning 재시도 판정 | (사전등록, 08-31) | 관측 | — | 🔄 지속(§11 오탐 정정, 판정일 09-07 불변) |
| `launcher_guard` 10일 연속 발화 | (자동 적신호) | 장전(이월) | 관측 | — | 🔄 지속(판정 보류, 함정①) |
| `O-67`(`F-1`) | 런처 GUARD `!=` 결함 사용자 결정 | 장전(이월) | P2 | P2 | 🔄 지속(사용자 결정 대기) |
| `C-5` | 증권사 화면 VKOSPI 대조 | (다회 이월) | P2 | P2 | 🔄 지속(18회째 이월, 사용자 몫) |
| 점검 지침서 앱 사본 갱신 | `save_skill overwrite:true` | (다회 이월) | P2 | P2 | 🔄 지속(18회째 이월, 사용자 몫) |

미처분 0건.

### 7-3. 당일 최종 손익·절대원칙 6종 최종 판정 (종가 기준)

**손익 (원천: 브로커 실측 net 우선)**

| 항목 | 값 |
|---|---|
| 판정 원천 | **브로커 실측**(`utils/db_utils.py:fetch_daily_net_for_verdict()`, `daily_broker_pnl.broker_net_krw`) — `[BrokerNet] state=OK` 최종 net=169,067원 |
| 엔진 net(대조용) | `trades` 집계 net=169,062원 (차 5원, 집계 시점 차이 — 재확인 완료) |
| gross | +181,000원(2.42+1.60-0.40=3.62pt, pt→원 환산) |
| 수수료 | 11,938원(수수료율 세대: `commission_rate_used` 493·495차 채널 요율, 2026-08-25/08-30 이후 불연속 구간과 무관 — 오늘은 정상 세대) |
| 자본 대비 손익률 | +0.34%(목표자본 5천만원 기준) |
| MDD | 자본 대비 2.13% / peak 대비 232.2%(peak 대비는 인용 시 반드시 병기) |
| 롤링 20거래일 | 누적 -437,928원, Sharpe -1.09, 낙폭 1,065,986원 |
| 진입/청산 건수 | **포지션 3건** / 청산 레그 6건(부분 3 + 최종 3) |
| 종가 포지션 | 0계약(전량 청산 완료, 15:10 이전) |
| 차단 건수 | 77건(42종) |
| 사이저 축소 | SizerMatch 6건 + MarginCap 1건(전부 3→2) |
| CB 발동 | 손실 레그 카운터 1회(정지 없음, `CB_CONSEC_STOP_LIMIT=9999` 한시예외 유지) |
| 한시예외 적용 | CB②(9999)·CB③-P4(False)·FP-CRITICAL(False)·TOX-SEVERE-SPREAD(False) — 전부 설정값 일치 |
| 걸린 STEP | STEP1~9 전부 정상, STEP3(GBM 재학습)은 장중 조건 미충족으로 0회(정상), EOD 재학습은 15:45~16:06 정상 |

**절대원칙 6종 최종 판정**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산(오버나이트 금지) | **준수** | `[SchedForceExit] 15:11 점검 — status=FLAT … 청산 대상 없음(정상)`. 마지막 청산 10:54:54, 15:10 시점 포지션 0. `exit_reason='15:10 강제청산'` 0건은 "안 지켜짐"이 아니라 "청산할 것이 없었음"(evidence_map.md §8-4 경고대로 DB가 아닌 로그로 확인) |
| ② | Circuit Breaker 5종 | **준수** | 발동 0건(정상), CB② 한시예외 값 `9999` 유지 확인 |
| ③ | CORE 피처 호라이즌 그룹별(강제 X) | **준수** | 진입 3건 전부 `[진입체크]` 원문 `vwap✅` 확인, 강제 항목 위반 0건 |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 해당없음 | 코드 변경 없음 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 해당없음 | 코드 변경 없음 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | **준수** | 흔적 없음 |

**15:10 강제청산 이행 여부**: 해당 없음(포지션 없어 집행 필요 자체가 없었음) — 절대원칙 위반 아님.
**EOD 재학습 성공 여부**: ✅ 성공 (`data/eod_retrain_done_20260904.txt`: rows=40073, cols=105, horizons_replaced=4/6, `selfcheck_label_state ok=True`).
**P8 성공 여부**: ✅ 성공 (`session_state.json: p8_last_success_date=2026-09-04, eod_retrain_ok_date=2026-09-04`, `[P8] 스케일러 재적합 완료 n=500봉 elapsed=0.03s`).

---

## 8. 다음 거래일 관측 예정 (갱신)

| ID | 판정 | 비고 |
|---|---|---|
| `O-55` | 🔄 지속 | 오늘 tox 0/13(0%) 추가 — 누적 판정은 표본 더 필요 |
| `O-58` | 🔄 지속 | 판정일 2026-09-07(다음 월요일 EOD) 불변, 오늘 오탐 정정 완료 |
| `O-59` | 🔄 지속 | 오늘 1m·3m 교체 보류 추가 — 판정일 2026-09-07(5거래일 누적) |
| `O-67`(`F-1`) | 🔄 지속 | 사용자 결정 시점까지 이월 |
| `O-70` | 🔄 지속 | 오늘 3건 추가, 계속 10시대 집중 — 누적 재확인 예정 |
| `launcher_guard` | 🔄 지속(판정 보류) | 함정① — F-1 적용 전 재론 안 함 |
| 재보정 모니터 3종(3-1) | 🆕 신규 | 사용자 검토·반영 여부 결정 시 종결 |

다음 관측 ID = **`O-72`**(오늘 신규 O-* 없음, §11 채번 유지).

---

## 9. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-09-04 (MW0602 528차 후속 — 일일 점검: 장후 종합)`
- `NEXT_TODO.md`: `O-55`·`O-59`·`O-70` 표본 추가 반영, `O-58` 오탐 정정 기록, 재보정 모니터 3종(3-1) 사용자 결정 대기 항목 신규 등록
- **기존 내용 덮어쓰지 않음** — append만

---

## 사용자 조치 (오늘 하루 종합)

| # | 할 일 | 명령·경로 | 걸리는 시간 | 안 하면 생기는 일 | 기한 |
|---|---|---|---|---|---|
| 1 | **(신규) 재보정 모니터 3종 검토** — 진입 호라이즌 경계(3.2/4.4→5.8/8.217 후보)·ATR 상하한(-43%~-50% 후보)·레이블 임계 6종이 "최근 시장과 벌어졌다"는 경보. `entry_horizon_recalibrator.recent_log()`로 1회성인지 추세인지 확인 후 반영 여부 결정 | `docs/정기점검/LABEL_THRESHOLD_RECALIBRATION_GUIDE.md` 참조, `model/ensemble_decision.py` `ENTRY_HORIZON_B1/B2` | 10~15분(로그 추이 확인 포함) | 계속 미루면 실제 시장과 설정값이 더 벌어진 채로 운영됨(당장 위험은 아님) | 다음 주 중 |
| 2 | `F-1` 결정 — 런처 GUARD `!=` 결함 방치/수정 여부 | `start_mireuk.bat`(:457)·`start_mireuk_CREON.bat`(:399/:434) | 2~3분 | 매일 같은 오탐 알림이 반복됨(실피해 없음) | 사용자 편할 때 |
| 3 | `F-2` 승인 — 장전 예약 시각 08:58:40 조정 여부 | 예약작업 설정 | 1분 | 장전 점검이 계속 개장 후에 실행됨(오늘까지는 실질 영향 없었음) | 사용자 편할 때 |
| 4 | 증권사 HTS 화면 VKOSPI 실값 1회 대조(`C-5`) — 18회째 이월 | — | 2~3분 | 계측 채널 하나가 검증 안 된 채로 계속 남음 | 여유 될 때 |
| 5 | 점검 지침서 앱 저장 스킬 갱신(`save_skill overwrite:true`, 정본 `rev: 2026-08-26e`) — 18회째 이월 | Claude 세션에서 `save_skill` 호출 | 1분 | 다음 세션이 낡은 사본으로 돌 위험(오늘은 정본과 일치 확인됨) | 여유 될 때 |

커밋 대기: `docs/정기점검/매일점검/MW0602-20260904-점검리포트.md`(장후 절 append, 종합 완성) · `dev_memory/DECISION_LOG.md`(append) · `dev_memory/NEXT_TODO.md`(append). 제외(gitignore): `evidence_MW0602-20260904_post.md`.

`.git/index.lock` 등 잔여 락 없음(세션 종료 시점 재확인 완료).

---

## 장후 자동조치 구현 결과 (2026-09-04 17:52 · 자동)

> 예약작업 `mireuk-postmarket-autofix` 가 위 리포트의 「사용자 조치」·Fix·고도화·
> 수익률 향상방안 절을 읽고 자동 실행한 결과다. **위 본문은 고치지 않았다**(append 규약).

### 한 줄 결론

오늘 리포트가 낸 **사용자 조치 5건은 전부 사람이 결정할 일**이라 자동으로 손댈 것이
없었다. 대신 리포트 §2 「확인 필요」가 **원인까지 규명해 둔 계측 오탐 1건**을 고쳤다 —
어제 이 자동조치 세션이 만든 점검 도구가 오늘 첫 오탐을 냈고, 그 자리에서 고쳤다.

### 1. 구현한 것 (1건)

**§11 「미판정 관측」 스캐너의 부분일치 오탐 차단** — `[Retrain]` 하나로 판정하지 않는다.

- **왜 했나.** 어제(09-03) 이 세션이 만든 기능이 오늘 첫 오탐을 냈다. `O-58` 의 판정
  조건은 *EOD DB 정리*(`[Retrain] DB pruning`)인데, 스캐너가 백틱 안에서 **태그
  `[Retrain]` 만** 떼어내 "오늘 로그에 있나"를 봤다. 그런데 그 태그는 **매일 찍히는
  호라이즌별 재학습**(`[Retrain] 3m 교체 보류`)에도 들어 있다. 그래서 아무 관계 없는
  줄에 걸려 "판정 조건 충족"으로 올라왔고, 사람이 로그를 직접 열어보고서야 오탐임을
  확인했다(위 §2 「확인 필요」).
- **안 고치면.** 매일 같은 거짓 알림이 뜬다. 그러면 사람이 그 줄을 무시하게 되고,
  **진짜 판정할 때가 온 항목도 같이 묻힌다.** 이 프로젝트가 반복해서 당한 실패
  (FP-CRITICAL 2개월, TOX 죽은 섀도 한 달)와 같은 계열이다.
- **어떻게 고쳤나 — 문턱을 올린 게 아니라 등급을 나눴다.** "그럼 한정어(`DB pruning`)
  까지 다 맞아야 한다"로 조이는 게 자연스러운 반응이지만, 실측해 보니 한정어의 상당수는
  `NORMAL → CRASH | day=-0.09% atr=1.00` 처럼 **등록 당시 사건의 예시**라 두 번 다시
  나오지 않는다. 전부 강제했다면 이 기능은 오늘부터 조용히 **0건**을 냈을 것이다 —
  반대 방향의 같은 고장이다. 그래서:
  - 한정어까지 **같은 한 줄에서** 맞으면 `strong` → 종전 문구 그대로 "판정 조건 충족".
  - 태그만 맞으면 `weak` → **"충족 아님"이 기본 해석**이라고 명시하고, 무엇이 없었는지
    (`DB pruning`)를 함께 적는다. 행을 지우지는 않는다 — 억제가 아니라 가시화다.
  - `count=N` 의 `N` 같은 **자리표시자는 요구에서 뺀다**(그걸 요구하면 진짜 발화도 못 찾는다).
  - 태그와 한정어가 **다른 줄**에 흩어져 있으면 `strong` 이 아니다 — 파일 전체
    substring 대조가 오탐의 직접 원인이었다.
- **걸린 시간.** 약 25분(원인 재현 → 구현 → 회귀 테스트 7종 → 실 수집기 1회 실행).
- **변경 파일**: `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py`
  (`tag_requirements()` 신설 + `scan_pending_observations()` 대조부 + §11 렌더 2곳) ·
  `tests/test_528_pending_obs_qualifier.py`(신규).
- **등급**: **A(계측·도구)**. 수집기는 로그를 읽어 리포트를 쓸 뿐이며 **매매 판단·주문·
  청산·설정값 어디에도 닿지 않는다.** 읽기 전용 불변식을 테스트가 고정한다.
- **⚠ 이 항목은 「사용자 조치」 목록에 없었다.** 자동조치 규격상 항목 출처는 그 절이지만,
  ① 리포트가 §2에서 **원인까지 규명**해 뒀고 ② 대상이 어제 이 세션이 만든 도구 자신이며
  ③ A등급이라 매매에 닿지 않아 처리했다. 범위를 넓힌 판단이므로 여기 명시해 둔다.

### 2. 하지 않은 것과 이유

| 항목 | 왜 안 했나 |
|---|---|
| 사용자 조치 1 — **재보정 모니터 3종 검토** | **C등급**. 대상이 `ENTRY_HORIZON_B1/B2`·ATR 상하한이라 자동조치 금지 목록에 정면으로 해당하고, 리포트 자신이 "자동반영 금지가 설계 원칙"·"반영 여부는 사용자 결정"이라 적었다. 이미 `NEXT_TODO.md` 에 체크박스로 등록돼 있다 |
| 사용자 조치 2 — `F-1` 런처 GUARD `!=` | **사용자 결정 대기**. 09-02·09-03에 이어 3일째 이월(`O-67` 로 관찰 중) |
| 사용자 조치 3 — `F-2` 예약 시각 조정 | **사용자 손 작업**(예약작업 설정) + 승인 대기 |
| 사용자 조치 4 — VKOSPI HTS 대조(`C-5`) | **사용자 손 작업**. 18회째 이월 |
| 사용자 조치 5 — 지침서 앱 사본 갱신 | **사용자 손 작업**(`save_skill` 호출). 18회째 이월 |
| 제5부 수익률 향상방안 1 — 손절1차 조기축소 폭 | **C등급 2중**. ① 대상이 `strategy/exit/` 청산 실행 경로(자동조치 범위 밖) ② 리포트 자신이 "n=1, 313차 가드 미통과, 관찰만 등록"이라 판정 |
| 제5부 2 — 재보정 모니터 반영 | 위 사용자 조치 1과 같은 건 |
| Fix 절 · 고도화 절 | 리포트가 **"신규 없음"** 으로 자체 판정 |

### 3. 사용자 몫으로 남은 것

리포트 §「사용자 조치」 5건 그대로다(위 표). 자동조치가 대신할 수 있는 것은 없다.

**추가로 하나 — 이건 리포트에 안 적혀 있던 것이다.** 오늘 EOD가 금요일 주간 산출물을
새로 만들면서 4주 보관 규칙(FIFO)에 따라 오래된 것들을 지웠는데, 그 결과가 **커밋되지
않은 채로 남아 있다**:

- 새 파일 6개(`..._20260904.*`)가 미추적, 예전 파일 10개(0801·0802·0807·0810분)가 삭제 상태.
- **이번 자동조치는 손대지 않았다** — 리포트의 「커밋 대기」 목록에 없었고, 10개 파일을
  지우는 커밋은 자동으로 낼 성격이 아니다.
- 이 폴더는 원래 **다른 PC가 서로의 주간 리포트를 보라고** 커밋 대상으로 옮긴 곳인데
  (CLAUDE.md 「주간 산출물 위치 규약」), 멀티PC 조율이 2026-08-23에 폐기된 뒤로는
  실제로 **08-23 이후 한 번도 커밋되지 않았다.** 즉 규약과 관행이 어긋난 상태로 3주가
  지났다. **계속 커밋할지, 규약에서 빼고 로컬 산출물로 둘지 결정이 필요하다**(5분).
  `NEXT_TODO.md` 에 등록해 뒀다.

### 4. 테스트

| 대상 | 결과 |
|---|---|
| `tests/test_528_pending_obs_qualifier.py`(신규, 불변식 7종) | **7/7 통과** |
| `tests/test_526_pending_obs_age.py`(어제 만든 기존 9종) | **9/9 통과** — 탐지력 회귀 없음 |
| `test_475_collector_db_axis` · `test_486_collector_holiday` · `test_494_observability_hygiene` · `test_494_core_degen_cause_axis` · `test_494_crash_signature_and_blockreq` · `test_485_monthly_cleanup_encoding` · `test_507_psi_baseline_redefinition_marker` | **전부 통과** |
| 실 수집기 1회 실행(`--phase post`, 임시 경로 출력) | 정상 93.3KB, §11 새 문구 렌더 확인 |

실행 환경은 **py37_32**(런타임과 동일). 오늘 자 실 데이터에서는 `strong` 0건 ·
`weak` 0건인데, **`O-58` 이 오늘 장후 세션에서 이미 `- [x]` 로 종결됐기 때문**이다
(최종 상태 판정이 정상 동작). 오탐 재현은 고정 입력 픽스처로 회귀에 박아 뒀다.

### 5. 커밋

- 커밋: `9ccbe2d` — `[MW0602] 528차 후속2: 장후 자동조치 — §11 미판정 관측 부분일치 오탐 차단`
- 푸시: `origin dev`
- 담은 파일: 수집기 1 · 신규 테스트 1 · 오늘 리포트(이 절) · `dev_memory` 2
- **제외**: `evidence_MW0602-20260904_*.md`(gitignore) · 금요일점검 폴더 FIFO 변동(위 §3)
- `.git/index.lock` 없음(종료 시 재확인).
