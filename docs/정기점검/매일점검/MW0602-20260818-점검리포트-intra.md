# 미륵이 일일 점검 — MW0602 / 2026-08-18 / 장중(intra)

- 점검 시각: **16:31 KST** · PC `MW0602` · 브랜치 `dev` ✅ (운영 브랜치 일치)
- HEAD `90c66cb` · 미커밋 3건(16:31 스냅샷) · 당일 커밋 0건
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260818_intra.md`
- 수집기: `--phase intra --pc MW0602 --out-auto` 정상 완주 (stderr 없음).
  다이제스트 첫 줄 `MW0602 (override · host=claude)` 확인 — `UNKNOWN` 아님.

> ⚠ **국면 표기와 실행 시각이 어긋난다.** 이 리포트는 `intra`로 예약됐으나 실행은
> **16:31**, 즉 15:10 강제청산·15:40 마감·15:47 EOD가 **모두 끝난 뒤**다. 따라서
> 본 점검은 "장중에 개입하는 점검"이 아니라 **당일 세션 전체 로그를 소급 심문한 것**이며,
> 장후 산출물(EOD 로그·사이드카·완료 마커)도 시야에 들어와 있다. 이 어긋남 자체를
> 이상점 **1-7**로 올린다.

> ⚠ **장중 규약대로 코드 변경·재기동·git 커밋을 일절 하지 않았다.** 읽기 전용 조사와
> 리포트 작성만 수행했다. §2의 모든 Fix는 **계획**이며 적용 시점을 명시한다.

---

## 0. 한 줄 결론

**절대원칙 6종 전부 준수, P0 0건. 매분 루프는 371/371분(100%) 완주했고 진입·청산·사이징
3원 대사가 포지션 단위로 완전히 일치한다. 다만 오늘은 "감시 장치가 감시를 못 하고 있다"가
네 건 겹쳐 나온 날이다 — G-1 사이드카·ConfFloorGuard 상태샘플·전략판정 배너·A3 유령점검이
모두 값은 정상인데 관측이 죽어 있다.**

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 정상 | 08:40 자동 기동, 08:55 매크로·EntryGate 정상. 설정 불변식 25행 전부 `일치` |
| 장중 | **정상 (관측 결함 4건)** | 루프 371/371분 100%, 공백 0건, 진입 18·청산 18 완결, CB HALT 0회, 5초 초과 블로킹 0건. 이상점은 전부 **계측 축** |
| 장후 | 정상 | 15:11 `[SchedForceExit]` FLAT 확인, 15:40 마감, 15:47 EOD `6/6` 교체, P8 스탬프 2종 기록 (※ 별도 post 점검 소관) |

### 절대원칙 준수 여부

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | ✅ **준수(공회전)** | `15:11:15 [SchedForceExit] 15:11 점검 — status=FLAT engine=0ct broker_cached=0ct bar_pass=2회 → 청산 대상 없음(정상)`. 마지막 포지션이 14:45:51에 닫혀 **청산할 것이 없었다**. `trades.exit_reason LIKE '%15:10%'` 전기간 **0건** — 실집행은 여전히 미확인(F-1R 이월) |
| ② | Circuit Breaker 5종 | ✅ 준수 | HALT 0회. `[CB] 연속 손절` 1→2→3회 기록되나 `CB_CONSEC_STOP_LIMIT=9999`(한시예외)라 정지 없음이 **정상**. acc30m은 0.0%~100% 사이를 오가 CB③ 입력 생존 확인 (고착 아님) |
| ③ | CORE 피처 호라이즌 그룹별 | ✅ 준수 | 진입 18건 **전부 `vwap✅`** (`grep -c "진입체크.*vwap❌"` = **0**). 강제 X 위반 없음. 진입 호라이즌 1m×2·3m×13·5m×3 — 전부 단기 그룹(474차 도달성 구조와 일치) |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | ✅ 준수 | `0xC0000409`·`STACK_BUFFER`·`access violation` **0건** |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | ✅ 준수 | TR 오류·파라미터 예외 로그 없음 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | ✅ 준수 | 자동 통합 흔적 **0건** |

### 설정 불변식

수집기 §3 표 **25행 전부 `일치`** (또는 의도된 `값 확인`). `불일치`·`미발견` **0건**.
차단 게이트 전수 29개 중 7개 `False`이며 **7개 모두 `기록됨` 또는 `기능토글`** — 미기록 게이트 0건.

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**해당 없음.** 절대원칙 6종 전부 준수, 미체결 잔여 포지션 0, CB HALT 0회,
5초 초과 파이프 지연 0건(최대 2,297ms), 크래시 0건.

---

### P1 — 정확성·관측 훼손

#### 1-1. ConfFloorGuard 상태 샘플(470차 L2)이 **진입 허용 구간에서 통째로 죽어 있다** — 신규

- **증상**: 사전등록 기준(`dev_memory/NEXT_TODO.md:12840`)은
  `grep -c "ConfFloorGuard] state=" ≈ 파이프라인 분수(370±5)`인데, 오늘 실측 **80건(21.6%)**.
  그 80건이 **전부 `ZONE_BLACKOUT`**이다. 진입 허용 290분에서는 **한 줄도 안 나왔다.**
- **근거**:
  ```
  2026-08-18 11:50:58 [INFO] SIGNAL: [ConfFloorGuard] state=ZONE_BLACKOUT 출력상한=N/A 필요=0.6200 (conf_floor=0.330, min_conf=0.620, zone_entry=False)
  2026-08-18 15:09:00 [INFO] SIGNAL: [ConfFloorGuard] state=ZONE_BLACKOUT 출력상한=N/A 필요=0.9700 (conf_floor=0.330, min_conf=0.970, zone_entry=False)
  ```
  `logs/20260818_SIGNAL.log` · 80건 · 11:50:58~15:09:00.
  **시각 분포가 결정적이다**: 11시대 10 + 12시대 60 + 15시대 10 = 80.
  이는 `OTHER` 70분(11:50~13:00) + `EXIT_ONLY` 10분(15:00~15:10)과 **정확히 일치**한다.
- **원인(코드 실측)**: `model/ensemble_decision.py:_sample_conf_floor_state()`가
  `_cal = getattr(self, "calibrator", None)`을 읽는데, `self.calibrator`는
  **`MultiHorizonCalibrator`**(`:294`에서 main.py가 주입)다. 이 클래스의
  `is_fitted(horizon)`은 **인자를 받는 메서드**(`learning/calibration.py:791`)라
  `not _cal.is_fitted`가 항상 False(바운드 메서드는 truthy)이고, 이어지는
  `_cal.output_max`는 **이 클래스에 존재하지 않는 속성**이다(`output_max`는
  `PredictionCalibrator:735`에만 있다). → `AttributeError` → 함수 말미
  `except Exception: logger.debug(...)`에 흡수되어 **INFO 한 줄도 안 남는다.**
  블랙아웃 구간만 살아남는 이유는 그 분기가 계산기를 만지기 **전에**
  `state = "ZONE_BLACKOUT"`으로 단락되기 때문이다.
  바로 한 줄 위 엣지 검사기 `_check_conf_floor_consistency()`(`:353`)는
  **`self.ensemble_calibrator`**를 쓴다 — 두 함수가 서로 다른 객체를 본다.
- **기준 위반**: 9단계 STEP 6(진입 판단) 관측. CLAUDE.md 26주 재검증 **C2'**
  *"⚠ 조건부 로그(임계 초과 시에만 출력)는 넣지 말 것 — 100% 고착이 구조적으로 보장된다"*.
- **영향**: ① 470차 L2가 고치려던 문제(2026-08-14 `10:36 봉쇄 경고 → 복구 로그 없이 11:25 진입`,
  즉 "봉쇄 구간 몇 분"을 세면 틀린 값이 나오는 것)가 **그대로 남아 있다.**
  ② 수집기 §12 `ConfFloor` 채널은 관측일이 쌓이는 즉시 `🔴 고착 ZONE_BLACKOUT 100%`로
  올라가는데, 그것은 **설계상 보장된 오탐**이다. `benign` 목록에 `OK`만 있어 걸러지지도 않는다.
  ③ 거래 손익 영향 **없음** — 이 함수는 순수 계측이며 진입 판단에 관여하지 않는다.
- **신규 여부**: **신규**. 사전등록 체크(`NEXT_TODO:12840`, O-p5)는 있었으나 원인은 미규명이었다.
  470차 L2 배포 이후 첫 정상 거래일이 오늘이다(08-15 주말·08-17 휴장).

#### 1-2. CB② 문서-코드 드리프트 — 절대원칙 §2의 **"5분 내"라는 시간창이 구현에 없다** — 신규

- **증상**: CLAUDE.md 절대원칙 ②는 *"5분 내 손절 3연속 → 당일 정지"*이고
  `config/settings.py:5050` 주석도 그 문구를 그대로 인용한다. 그러나 구현
  `safety/circuit_breaker.py:record_stop_loss()`에는 **경과시간 조건이 없다** —
  중간에 이익 포지션이 끼지 않는 한 카운터가 무한정 누적된다.
- **근거**: 오늘 카운터가 3에 도달하기까지 **3시간 26분**이 걸렸다.
  ```
  2026-08-18 10:10:58 [WARNING] SYSTEM: [CB] 연속 손절 1회
  2026-08-18 11:38:38 [WARNING] SYSTEM: [CB] 연속 손절 2회   ← 87분 뒤
  2026-08-18 13:05:30 [WARNING] SYSTEM: [CB] 연속 손절 2회
  2026-08-18 13:36:20 [WARNING] SYSTEM: [CB] 연속 손절 3회   ← 최초 손절로부터 206분
  ```
  `logs/20260818_WARN.log`. 코드에는 `self._consec_stops += 1` → `if self._consec_stops >= CB_CONSEC_STOP_LIMIT` 뿐이고 타임스탬프를 보지 않는다.
- **기준 위반**: CLAUDE.md 절대원칙 ②. `references/invariants.md` §3-B가 이미
  같은 계열 드리프트 2건(CB③ 35%↔0.28 **해소**, CB⑤ API↔파이프 **미해소**)을 등재하고 있는데
  **CB②의 시간창 드리프트는 그 표에 없다.**
- **영향**: 오늘은 `CB_CONSEC_STOP_LIMIT=9999`(한시예외)라 **실동작 영향 0**.
  위험은 **2026-08-29 CB② 복원 재검토**다 — 그 자리에서 `2~3`을 넣으면
  "5분 내"라는 문서상의 좁은 조건이 아니라 **하루 종일 누적되는 넓은 조건**으로 작동한다.
  오늘 데이터의 카운터팩추얼: `limit=3` 복원 시 **13:36:20에 당일 HALT**,
  이후 10포지션 **+657,691원**이 미실현되어 당일 손익이 **-25,084원 → -682,775원**이 된다.
  > ⚠ **313차 원칙 — 확정 결론 금지.** 관측 **1거래일**이고, HALT가 걸렸다면 그 뒤
  > 시장·게이트 상태가 그대로였으리라는 보장이 없다(반사실 추정). "복원하면 손해다"가
  > 아니라 **"복원 전에 시간창 의미를 확정해야 한다"**가 결론이다.
- **신규 여부**: **신규**. `dev_memory` 전문 검색에서 CB② 시간창 관련 기록 0건.
  단 470차 C1(레그→포지션 단위 집계)이 같은 함수를 이미 손봤으므로 **회귀는 아니다.**

#### 1-3. 전략판정 배너가 조건부 로그라 §12 `전략판정` 고착 지표가 **구조적 오탐**이다 — 신규

- **증상**: 수집기 §11 자동 적신호 **#10**이
  `고착 지표 전략판정 — UNDERPERFORM 100% (7건 / 7일)`을 올렸다. 그런데
  **오늘 판정은 UNDERPERFORM이 아니다.**
  ```
  data/daily_reports/strategy_report_20260818_154016.txt
    판정    : INSUFFICIENT
    사유    : 데이터 부족 (60일) — 5일 이후 재판정. 관찰 지속.
  ```
  §12가 이걸 못 본 이유는 표본 창이 아니라 **로그에 안 찍혔기 때문**이다:
  `grep "판정\s*:" logs/20260818_{WARN,SYSTEM}.log` → **0건** (08-14는 1건 있었다).
- **원인(코드 실측)**: `main.py:11517`
  ```python
  if _action in (ACTION_ROLLBACK_REVIEW, ACTION_REPLACE_CANDIDATE):
      log_manager.system(_alert, "WARNING")
  ```
  `INSUFFICIENT` → `ACTION_WATCH`, `NORMAL`/`OUTPERFORM` → `ACTION_KEEP`
  (`strategy/ops/verdict_engine.py:112~128`) → **어느 쪽도 로그를 남기지 않는다.**
  §12의 `전략판정` 패턴은 `files: ["_WARN","_SYSTEM"]`을 훑으므로
  **`UNDERPERFORM`(과 롤백)만 기록될 수 있다** — 100% 고착이 수학적으로 보장된다.
- **기준 위반**: CLAUDE.md 26주 재검증 **C2'** 동일 조항(조건부 로그 금지).
  9단계 밖의 Phase5 운영 축.
- **영향**: ① 적신호 #10은 **오탐**이며, 이대로 두면 다음 세션이 "판정식이 무의미해졌다"는
  틀린 진단을 승계한다. ② 더 나쁜 것은 **반대 방향 은폐**다 — 오늘 실제로 판정이
  7거래일 만에 바뀌었는데(아래 「이미 반영된 사안」 참조) §12는 그 **진짜 상태 변화를
  볼 수 없었다.** ③ 사전등록 관측 **O-p6**(`[전략 상태 경보]` 배너 MDD 분모가 자본 기준인지)도
  같은 이유로 **답할 수 없었다** — 배너가 아예 없다.
- **신규 여부**: **신규**(원인 규명). §12가 `🔴 고착`으로 표시한 것 자체는 08-13부터 있었으나
  "왜 한 값만 나오는가"가 규명된 적은 없다.

#### 1-4. G-1 사이드카 `label_scheme` 불일치 6/6 — **기지 항목, 예측 적중** (신규 아님)

- **증상**: 오늘 08:55 장전 판정이 `불일치 6/6`으로 재현됐고, 오늘 EOD가 새로 쓴
  사이드카 6개도 전부 `label_scheme: null`이다.
- **근거**:
  ```
  2026-08-18 08:55:13 [INFO] SYSTEM: [EntryGate] 모델 레이블 체계 미확인(장전) — 사이즈 제한 유지 (×0.6) | 불일치 6/6 — 1m:label_scheme없음(구버전), 3m:label_scheme없음(구버전), 5m:label_scheme없음(구버전) …
  2026-08-18 08:55:13 [INFO] SYSTEM: [EntryGate] 전일 EOD 모델 적재 확인 — 사이즈 제한 해제 (×0.6 → ×1.0, 재학습 불요)
  ```
  사이드카 실측(2026-08-18 15:45~15:46 기록, 6/6):
  `{"horizon":"3m","written_at":"2026-08-18 15:45:41","source":"eod", … ,"label_scheme":null,"label_param":null}`
- **판정**: 이것은 **사전등록 관측 O-p1의 정상 결과**다 —
  `dev_memory/NEXT_TODO.md:57` *"F-pre2-1 미적용이면 **6/6 재현**이 정상이다(진단이 옳다는 증거)"*.
  **F-pre2-1은 아직 미착수**이므로 재현이 맞다. **O-p2**(사이드카 → `['fixed']*6`)는 **미충족**.
- **기준 위반 없음 / 라이브 위험 없음**: 폴백이 보수 방향(모르면 제한 유지)이고,
  오늘 `×0.6`은 **F-1 경로**(전일 EOD 모델 적재 확인)가 풀었다.
  ⚠ **이것을 G-1 검증으로 읽지 말 것** — `invariants.md` §2-B 470차 D1 규약.
- **막히는 것**: **실전 전환 기준 ⑧**의 선행조건(G-1 라이브 확인)이 오늘도 미충족.
  **I-3**(`불일치 0/6` **＋** F-1 마커 없이도 해제) 두 조건 다 미달.
- **신규 여부**: **기존 TODO** — `F-pre2-1 (P1)`, `O-p1`, `O-p2`, `O-p3`, `I-3`.
  원인(`retrain_eod.py:320`이 `retrain_now(X=X, y_dict=y_dict)`로 호출 → `batch_retrainer.py:507-509`가
  `_last_label_scheme=None`으로 초기화)도 `DECISION_LOG.md:29,1132`에 이미 적혀 있다.
  **신규 안건으로 올리지 않는다(함정 ①).**

---

### P2 — 운영 부담·기술부채

#### 1-5. 470차 A3 유령 프로세스 점검이 **프로덕션 EOD 경로에 배선돼 있지 않다** — 신규

- **증상**: 사전등록 관측 **O-p7**(`[유령 프로세스]` EOD 체인 점검 첫 실행)이 **미관측**.
  오늘 EOD는 정상 완주했는데 `logs/eod_ghost_check.log`에 **2026-08-18 행이 없다.**
- **근거**:
  ```
  logs/eod_ghost_check.log (마지막 2행)
  2026-08-14 18:36:09 [EodGhostCheck] main.py 잔존 프로세스 0 — 정상 종료 확인
  2026-08-14 19:47:34 [EodGhostCheck] main.py 잔존 프로세스 0 — 정상 종료 확인 (매칭: name~python & cmdline~main.py)
  ```
  두 행 모두 **18~19시대** — 470차 작업 세션의 수동 실행으로 보인다(EOD는 15:45~15:47).
- **원인 가설(확정 아님)**: `eod_ghost_check.ps1` 호출은 **`EOD_RETRAIN.bat:32`에만** 있고,
  그 배치는 `python scripts\eod_retrain.py --phase2 --weeks 10`을 부른다.
  그런데 오늘 실제로 돈 것은 **`retrain_eod.py`(리포 루트)**다 —
  로그가 `weeks_back=26, phase2=False`이고 `[ScalerRefresh] … retrain_eod.py P8`을 찍는다.
  즉 **작업 스케줄러가 부르는 것은 `EOD_RETRAIN.bat`이 아니다.**
  ⚠ Windows 작업 스케줄러 설정은 repo 밖이라 **repo 증거만으로는 확정 불가**.
- **영향**: 470차 A3가 잡으려던 것(*"5거래일 연속 08:40 런처 GUARD가 살아있는 main.py를 발견"*)이
  **저녁에 기록되지 않고 다음 날 아침에야 드러나는 상태**로 되돌아가 있다.
  거래 손익 영향 없음.
- **신규 여부**: **신규**(O-p7의 답).

#### 1-6. `.git/index.lock` 0바이트 스테일 락 **재발** — 재발

- **증상**: `-rwx------ 0 Aug 18 16:32 .git/index.lock` 존재.
- **근거**: mtime **16:32**가 오늘 점검 수집기 3본(pre/intra/post 증거파일 전부 16:32)과 일치.
- **기준**: 사전등록 체크 `dev_memory/NEXT_TODO.md:12846`
  *"`.git/index.lock` 재발 여부 — 0814 08:50 예약 점검이 0바이트 스테일 락을 남겨
  그날 종일 git 쓰기가 불가능했다(470차 후속2에서 제거)."*
- **영향**: 이 상태로 두면 **오늘 밤 장후 Fix 커밋이 막힌다.** 470차 후속2와 동일 증상.
- **신규 여부**: **재발**. 원 제거 = 470차 후속2. 예약 점검이 락을 남기는 구조라면
  **매일 재발**한다는 그때의 예측이 맞았다.

#### 1-7. 장중 점검이 **장중에 돌지 않았다** — 2일 연속(MW0602 축 신규)

- **증상**: `--phase intra` 예약작업이 **16:31 KST**에 실행됐다. 증거파일 3본
  (`_pre` `_intra` `_post`)이 전부 **16:32**로 찍혀 pre/intra/post 세 세션이 겹쳐 돌았다.
- **근거**: 다이제스트 헤더 `생성 2026-08-18 16:31:59 KST`.
  `ls docs/정기점검/매일점검/ | grep 20260818` → 증거 3본 모두 `Aug 18 16:32`.
- **기준**: 사전등록 **O-7** *"예약 점검 시각(C-3) — 장전 점검이 08:57±5분에 도는지.
  오늘(08-17)은 pre 16:48 / intra 16:49 / post 16:55로 몰렸다. **2일 연속이면 재현성 확정.**"*
  → **2일 연속 확인. 재현성 확정.**
  ⚠ O-7은 MW0601이 등록했고 예약작업은 PC별로 따로 등록된다(함정 ③) —
  본 항목은 **MW0602 축의 독립 관측**이며 MW0601 항목을 닫지 않는다.
- **영향**: ① 장중 점검의 존재 이유(진행 중인 세션의 이상을 **그날 안에** 잡는 것)가
  소멸한다 — 16:31에 발견한 이상은 이미 손쓸 수 없다. ② 세 세션 동시 실행은
  `dev_memory/*.md`(1.8MB) 동시 append 위험과 1-6의 락 재발로 직결된다
  (**G-3 직렬화 항목**이 정확히 이것을 위해 등록돼 있다).
- **신규 여부**: MW0602 축 **신규**, MW0601 축 **재현 2일차**.

---

### 확인 필요 (확정 아님)

- **손절 준수율이 오늘도 1을 넘었다 — 특히 2계약 건에서.**
  진짜 손절 5건(`하드스톱(틱) [TP1미도달]`)의 `실현손실 ÷ 의도손절폭`:

  | 진입 | 계약 | 의도손절폭 | 실현손실 | 준수율 |
  |---|---|---|---|---|
  | 10:08:58 | **2** | 2.58pt | 3.98pt | **154.3%** |
  | 11:35:58 | 1 | 4.69pt | 4.78pt | 101.9% |
  | 13:04:58 | 1 | 2.87pt | 2.70pt | 94.1% |
  | 13:34:58 | 1 | 2.60pt | 2.50pt | 96.2% |
  | 13:45:58 | 1 | 2.27pt | 2.40pt | 105.7% |

  평균 110.4% · **5건 중 3건이 손절폭 초과**. 1계약 4건 평균 99.5% vs 2계약 1건 154.3%.
  417차 재분해가 유일하게 유의하다고 본 축(진입수량 ↔ 손절폭 초과비율)과 **방향이 같다.**
  → ⚠ **313차 원칙 — 확정 결론 금지.** n=5, 관측 **1거래일**, 2계약 표본 **1건**.
  판정하려면 `min_days` 기준으로 거래일을 더 쌓아야 한다. (승패를 `exit_reason`만으로
  세지 않았다 — `하드스톱` 18건 중 8건은 `[TP1보호]` 이익 청산이라 제외했다.)

- **⑨ TOX-SEVERE-SPREAD 표본**: 오늘 `spread_ticks ≥ 20` 분봉 **1건**
  (`2026-08-18 11:33:00`, **28.0028틱**, `entry_executed=0`). 진입이 아니므로
  473차 F-8 Phase A의 **진입 6포지션 / 4거래일**은 **불변**이다.
  오늘 분포: `<8` 331 · `8~12` 35 · `12~20` 2 · `≥20` 1 (총 369분).
  → ⑨는 여전히 `INSUFFICIENT`. **문턱을 낮춰 판정하지 말 것**(458차 D6).

- **L3 `[TP비율]` 로그 미출현**: 오늘 부분청산 2건은 전부 `손절1차 조기축소`이고
  **TP1 부분익절은 0건**이었다(최종청산 18건 = `[TP1보호]`×8 · `[TP1미도달]`×5 · `TP2(전량)`×5).
  즉 **트리거 조건 자체가 성립하지 않았다** — 결함이 아니라 **판정 보류**다.

---

### 이미 반영된 사안 (신규 아님) — 함정 ① 방지

이 절을 비워두지 않았다. 아래는 **오늘 눈에 띄지만 이미 코드/결정에 반영된 것**이다.

| 관측 | 왜 정상인가 |
|---|---|
| **`[GBM] 배치 재학습` 0건** | CLAUDE.md 470차 문서-코드 정합: STEP 3은 "30분마다"가 아니라 **조건부**다(5m 정확도 <25%/n≥20/60분, 또는 <15%/n≥15/30분). 오늘 조건 미충족 = **정상**. 2026-08-14 점검이 이 문구로 STEP 3을 "판정 불가"로 올린 전례가 있다 |
| **`[CB] 연속 손절 3회`에도 정지 없음** | `CB_CONSEC_STOP_LIMIT=9999` 한시예외. **발동 안 하는 것이 정상**(invariants §2). 별건인 시간창 드리프트는 1-2에 분리 |
| **`WeightCollapse` 75건** | 75/369 = **20.3%**. 사전등록 밴드 **O-8 = 20.3% ± 5%p** 정중앙. 8거래일 75~80건으로 안정(08-06 75 · 08-11 80 · 08-14 75 · 08-18 75). 이탈 아님 |
| **`FP_CRITICAL`·`CB3_P4`·`TOX-SEVERE-SPREAD` 차단 꺼짐** | 전부 CLAUDE.md 등재 한시예외. 수집기 §3 전부 `일치` |
| **`JointGateEdge` 0건** | 발동 조건은 `meta=reduce & tox=reduce & |joint-0.50| ≤ 0.02`(`main.py:9078-9082`). 오늘 실제 조합은 `meta=0.50 tox=0.70 joint=0.350`(margin -0.15)로 **밖**이다. 결함 아님 |
| **L1 `존금지` 접두** | 11:50~13:00 `[ZeroDiag]` **70/70 = 100%** 접두 부착. 470차 L1 **PASS** |
| **진입 18건 전부 SHORT** | 앙상블 방향 자체가 `dir=-1` **181분** vs `dir=+1` 48분이고 당일 지수는 1132 → 1079(**-53pt**, 레인지 73.1pt = 5일평균 32.3pt의 2.3배). 직전 거래일들은 양방향(08-12 LONG 5·SHORT 1). **레짐 정합이며 방향 고착 아님** |
| **전략판정이 UNDERPERFORM → INSUFFICIENT로 바뀐 것** | **470차 R1(`222397b`, 2026-08-14 19:07 커밋)의 설계된 첫 라이브 효과**다. 그 커밋이 `config/strategy_registry.py:_is_seed_baseline()`을 넣어 *"WFA 기준선이 QA seed면 판정하지 않는다 — 모르는 것을 UNDERPERFORM으로 단정하지 않는다"*로 바꿨다. 커밋 시각이 08-14 EOD(15:40) **이후**이고 08-17이 휴장이므로 **오늘이 첫 적용일**이다. **정상 동작이며 신규 이상점이 아니다.** (다만 배너가 안 찍혀 §12가 못 본 것이 1-3) |

---

## 2. Fix 작업 구현계획

> 🔴 **본 계획은 수립만 하고 적용하지 않는다.** 점검 시각(16:31)에 라이브 본체는 이미
> 종료됐으나(15:40:31 `[Shutdown] 자동 종료`), 장중 국면 규약과 "동시 세션 3본이
> 돌고 있다"(1-7)는 사실 때문에 **git 쓰기를 일절 하지 않았다.**
> 적용은 **F-0 선행 후, 오늘 밤 장후 세션에서** 일괄 수행할 것.

### F-0. `.git/index.lock` 제거 — P2 · 대응 이상점 1-6 · **다른 모든 Fix의 선행조건**

- **원인 가설**: 예약 점검 세션(샌드박스)이 `git status` 계열 호출 후 0바이트 락을 남긴다.
- **조치**: `del .git\index.lock`(Windows) — **단, 다른 git 프로세스가 없음을 확인한 뒤.**
  스테일 판정 기준은 471차 전례대로 **mtime 10분 초과**.
- **회귀 위험**: 진짜 실행 중인 git 작업을 죽일 수 있다 → 삭제 전 `tasklist | findstr git` 확인.
- **절대원칙 저촉**: 없음.
- **런타임**: 해당 없음(셸).
- **검증**: `git status`가 즉시 응답 · `git commit` 성공.
- **적용 시점**: **장후 세션 최초 단계.** 이것 없이는 F-1~F-3 커밋이 불가능하다.

### F-1. `_sample_conf_floor_state`의 계산기 참조 수정 — P1 · 대응 이상점 1-1

- **원인 가설**: 470차 L2 신설 시 바로 위 함수(`_check_conf_floor_consistency`)가 쓰는
  `self.ensemble_calibrator` 대신 `self.calibrator`를 적었다. 예외가
  `logger.debug`로 흡수돼 **한 달 가까이 조용히 죽어 있었다** — FP-CRITICAL(303차)·
  TOX 섀도(311차)와 **같은 실패 패턴**이다.
- **변경 파일**:
  - `model/ensemble_decision.py` — `_sample_conf_floor_state()`:
    ① `_cal = getattr(self, "calibrator", None)` → **`self.ensemble_calibrator`**로 교체
       (엣지 검사기와 동일 객체를 보게 한다).
    ② `except Exception` 흡수 레벨을 `logger.debug` → **`logger.warning` 1회성**으로
       승격(같은 예외 반복 시 억제). **조용한 죽음을 다시 만들지 않는 것이 이 fix의 핵심이다.**
    ③ 분당 1회 dedupe(`_conf_floor_sample_minute`)는 **그대로 둔다.**
- **회귀 위험**: 낮음. 이 함수는 순수 계측이며 반환값이 없고 진입 판단에 관여하지 않는다.
  로그량이 80행/일 → **약 370행/일**로 증가한다(설계 의도값, docstring이 명시).
- **절대원칙 저촉 여부**: 6종 중 어느 것에도 저촉되지 않음. COM 콜백 밖이며
  `dynamicCall`·`emit` 미사용(절대원칙 ④ 무관).
- **런타임 주의**: `py37_32`(장중 본체) — **f-string·walrus 금지 구역은 아니나**
  기존 파일 스타일(`%` 포매팅)을 따를 것.
- **검증**: 다음 거래일 `grep -c "ConfFloorGuard] state=" logs/{d}_SIGNAL.log` **≥ 365**이고
  값 분포에 **`OK` 또는 `BLOCKED`가 등장**할 것(`ZONE_BLACKOUT` 단일이면 미해결).
  회귀 테스트 신설 권고: `tests/test_475_conf_floor_state_sample.py` —
  ① 블랙아웃이 아닌 상태에서 `state ∈ {OK, BLOCKED, RAW, NO_CAL, UNKNOWN}`가 나오는지
  ② `MultiHorizonCalibrator`를 주입해도 예외로 침묵하지 않는지.
- **섀도 계측 필요 여부**: **불필요** — 게이트 신설이 아니라 기존 계측의 버그 수정이다.
- **적용 시점**: **장후.** 라이브 본체 재기동이 필요한 코드 경로다.
- **결정 필요 사항**: 없음(권고: 그대로 적용).

### F-2. §12 `전략판정` 고착 지표의 입력을 **무조건 상태 샘플**로 교체 — P1 · 대응 이상점 1-3

- **원인 가설**: 판정 배너가 "조치가 필요한 판정일 때만" 찍히는 **경보**인데,
  §12가 그것을 **상태 지표**로 소비하고 있다. 지표 설계 오류이지 판정식 오류가 아니다.
- **변경 파일** (둘 중 **①을 권고**):
  - **① 생산 측 (권고)** `main.py:11504~11520` — `if _action in (…)` **밖에**
    무조건 1줄 추가: `[전략상태] verdict=%s action=%s drift=%d live_days=%d`
    (레벨 `INFO`, 하루 1회). 경보 WARNING은 **그대로 둔다 — 추가이지 대체가 아니다**
    (470차 L2가 세운 관례).
  - **②** `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py` +
    `config/dailycheck_targets.json` — `전략판정` 패턴을 위 새 로그로 교체하고
    `benign`에 `INSUFFICIENT`·`NORMAL`·`OUTPERFORM` 추가.
    ⚠ 수집기는 **표준 라이브러리 전용 · py3.7 호환**(f-string·walrus 금지).
- **회귀 위험**: 매우 낮음(로그 1줄/일 추가). ②만 하고 ①을 빼면 **여전히 못 본다** —
  로그가 없는 것을 패턴으로 잡을 수는 없다.
- **절대원칙 저촉 여부**: 없음.
- **런타임 주의**: `main.py` = `py37_32`. 해당 블록은 `daily_close()` 경로이며
  COM 콜백 안이 아니다.
- **검증**: 다음 거래일 15:40 로그에 `[전략상태] verdict=INSUFFICIENT …` 1줄.
  수집기 §12 `전략판정` 표본이 `관측일 = 거래일 수`와 같아지고
  적신호 #10이 `⚪ 정상고착(기록됨)` 또는 `✅ 변동`으로 강등.
- **적용 시점**: **장후.**
- **결정 필요 사항**: `[전략상태]`를 `benign` 처리할지 감시 유지할지 —
  **권고: `INSUFFICIENT`를 `benign`에 넣되 `UNDERPERFORM` 연속은 계속 감시.**

### F-3. `[EodGhostCheck]`를 프로덕션 EOD 경로로 이설 — P2 · 대응 이상점 1-5

- **원인 가설**: A3가 `EOD_RETRAIN.bat`에만 배선됐는데 작업 스케줄러는
  `retrain_eod.py`(리포 루트)를 부른다.
- **선행 조사(코드 변경 전 필수)**: Windows 작업 스케줄러의 EOD 작업
  (`Maitreya_EODretrain` 등) **Actions 탭 실제 명령줄**을 확인할 것.
  repo 안에서는 확정할 수 없다. 스케줄러가 정말 `.bat`을 부르는데도 로그가 없다면
  **원인이 전혀 다르다**(PowerShell 실행 정책 등) — 그 경우 F-3을 중단하고 재진단한다.
- **변경 파일** (조사 결과가 가설과 일치할 때만):
  - `retrain_eod.py` — `main()` 진입 직후, **`subprocess`로 `scripts/eod_ghost_check.ps1` 호출**
    (실패해도 EOD를 막지 않도록 `try/except` + 항상 exit 0. `.ps1`이 이미 always-exit-0).
  - `EOD_RETRAIN.bat:32` — 호출은 **남겨둔다**(수동 실행 경로도 유효).
    중복 실행되면 로그가 2줄 되지만 무해하다.
- **회귀 위험**: 낮음. 단 **EOD 본작업 앞에 두면 PowerShell 지연이 재학습을 늦춘다** →
  `_wait_for_daily_close()` **이후, 데이터 로드 이전**에 둘 것.
- **절대원칙 저촉 여부**: 없음.
- **런타임 주의**: `retrain_eod.py` = **`py310_64` 전용**(191차). `py37_32`로 돌리지 말 것.
- **검증**: 다음 거래일 `logs/eod_ghost_check.log`에 **15:4x 행** 1줄.
- **적용 시점**: **장후 — 단 선행 조사 완료 후.** 조사 없이 손대면 배선을 하나 더 늘릴 뿐이다.

### F-4. CB② 시간창 드리프트 **문서 등재** (코드 무변경) — P1 · 대응 이상점 1-2

- **원인 가설**: 없음(설계 의도 불명확이 핵심). **어느 쪽이 옳은지 지금 정하지 않는다.**
- **변경 파일** (문서만):
  - `.claude/skills/mireuk-daily-check/references/invariants.md` **§3-B 표에 3번째 행 추가** —
    `CB② "5분 내 손절 3연속"` ↔ `record_stop_loss()`에 시간창 없음 · **미해소**.
  - `dev_memory/NEXT_TODO.md` — **2026-08-29 CB② 재검토 항목의 선행조건**으로 등록:
    *"복원 전에 '5분 내'의 의미를 확정할 것. 문서를 코드에 맞출지(시간창 없음),
    코드를 문서에 맞출지(윈도우 추가) 결정 없이 `2~3`을 넣지 말 것."*
- **회귀 위험**: 없음(문서).
- **절대원칙 저촉 여부**: 없음. ⚠ **절대원칙 ② 자체를 바꾸자는 제안이 아니다** —
  문구와 구현의 괴리를 **등재**하는 것이다(468차 CB③ 병기와 동일 성격).
- **검증**: 08-29 재검토 세션이 이 항목을 보고 시작하는가.
- **적용 시점**: **장후.**
- **결정 필요 사항**: **있음 — 08-29 주간회의 안건.** 권고: 오늘 카운터팩추얼
  (13:36 HALT 시 -657,691원)을 **판정 근거가 아니라 표본 1건**으로 제출하고,
  복원 전 최소 **10거래일치 `[CB] 연속 손절` 타임스탬프 간격 분포**를 먼저 집계할 것.

### 적용 순서와 커밋 계획

1. **F-0** 락 제거 (커밋 아님, 선행 조치)
2. **커밋 ①** — F-1 + F-2① — `[MW0602] 475차: 관측 계층 복구 — ConfFloorGuard 상태샘플 계산기 오참조 + 전략판정 무조건 상태 로그`
3. **커밋 ②** — F-2② + F-4 — `[MW0602] 475차 후속: 수집기 §12 전략판정 패턴 교체 + CB② 시간창 드리프트 등재`
4. **커밋 ③** — F-3 (선행 조사 통과 시에만) — `[MW0602] 475차 후속2: EodGhostCheck 프로덕션 EOD 경로 이설`

> ⚠ **F-pre2-1(G-1 label_scheme)은 이 계획에 넣지 않았다.** 이미 승인 대기 중인
> 기존 항목이고 *"먼저 착수할 것"*으로 표시돼 있다 — **중복 착수 금지**(NEXT_TODO 470차 후속6).
> 다만 위 커밋들과 **같은 세션에서 처리하면 검증 신호가 섞인다**: F-pre2-1의 검증은
> 다음 날 `불일치 0/6`이고, F-1의 검증도 다음 날 로그다. **커밋을 분리하고
> 리포트에서 축을 나눠 읽을 것.**

---

## 3. 고도화 방안

### G-1. `except` 흡수 로그 레벨 감사 — "조용한 죽음" 3중 재발에 대한 구조적 대응

- **관측 근거**: 오늘 하루에 **같은 실패 패턴이 3건** 나왔다.
  ① 1-1 `_sample_conf_floor_state` — `AttributeError` → `logger.debug` → 한 달간 침묵.
  ② 1-3 전략판정 배너 — 조건부 로그라 한 값만 기록.
  ③ 1-5 A3 — 안 불리는 배치 파일에 배선.
  선행 사례도 같다: FP-CRITICAL(303차, PSI=0.0 2개월) · TOX 섀도(311차, 한 달 죽은 섀도) ·
  474차(mid/long CORE 도달 불가 6개월) · 471차 F-1(15:10 1차 경로 도달 불가 6개월).
- **제안 내용**: `scripts/audit_silent_except.py` 신설 —
  `except Exception … logger.debug(` 패턴을 AST로 훑어
  **① 해당 함수가 로그를 남기는 것이 유일한 목적인가**(반환값 없음 + 부수효과 없음)
  **② 그 로그가 최근 N거래일 로그에 실제로 존재하는가**를 대조해
  "죽었을 가능성이 있는 계측" 목록을 낸다. 주간 리포트에 1개 절로 첨부.
- **기대 효과**: 정량 — 오늘 3건 중 **①은 확실히, ③은 부분적으로** 이 도구로 사전 검출된다.
  검출 지연을 "한 달~6개월"에서 **"1주"**로 줄이는 것이 목표.
- **비용·위험**: 초기 구현 반나절 수준. **오탐이 많을 것이다**(정상적으로 조건부인 로그가 다수)
  → §12와 같은 `benign` 화이트리스트 필수. **차단 로직이 아니므로 섀도 계측 불필요.**
- **선행 조건**: F-1(같은 계열 버그를 먼저 고쳐야 도구의 첫 판정이 깨끗하다).
- **우선순위 제안**: **이번 주.**

### G-2. `CORE준비도`·`ConfFloor` 계열에 `since`(배포일) 필드 — 이미 등록된 F-pre2-3의 근거 보강

- **관측 근거**: 오늘 §12에서 `ConfFloor`(표본 80)와 `CORE준비도`(표본 42)가 모두
  `⚪ 표본부족 / 관측일 1`로 떴다. 그런데 **`ConfFloor`의 "표본부족"은 배포일이 최근이라
  그런 게 아니라 1-1의 버그 때문**이다 — 두 원인이 같은 라벨로 뭉개져 있다.
- **제안 내용**: F-pre2-3(이미 등록됨)에 **판정 라벨 하나 추가** —
  `신규(미관측)` 외에 **`부분관측(기대분수 대비 X%)`**. 기대 분수는
  `dailycheck_targets.json`에 지표별 `expected_per_day`(예: `ConfFloor: 370`)로 둔다.
  오늘이면 `21.6%`가 찍혀 **눈에 걸렸을 것이다.**
- **기대 효과**: 1-1 같은 "절반만 살아있는 계측"을 §12가 **자동으로** 올린다.
  오늘은 사람이 사전등록 메모(`NEXT_TODO:12840`)를 기억해서 겨우 잡았다.
- **비용·위험**: 수집기 소폭 수정. ⚠ 표준 라이브러리 전용 · **py3.7 호환**.
- **선행 조건**: F-pre2-3(중복 구현 금지 — **같은 커밋에서 함께 처리할 것**).
- **우선순위 제안**: **이번 주**(F-pre2-3에 흡수).

### G-3. 점검 세션 직렬화 — 이미 등록된 항목의 **재현 확인**

- **관측 근거**: 오늘 pre/intra/post 증거 3본이 **전부 16:32**에 생성됐고
  `.git/index.lock`이 같은 시각에 재발했다(1-6·1-7). 08-17에 이어 **2일 연속**.
- **제안 내용**: 신규 제안 없음 — `dev_memory/NEXT_TODO.md` **G-3**
  (`dev_memory/.dailycheck.lock`, 스테일 mtime 10분) 그대로. **오늘 증거를 근거로
  우선순위만 "이번 주" → "즉시"로 올릴 것을 제안한다.**
- **기대 효과**: 1-6의 매일 재발과 `dev_memory` 동시 append 유실(MW0602 `483d41a` 전례) 차단.
- **비용·위험**: 낮음. ⚠ 잠금 실패 시 **조각 파일로 떨어뜨리고 다음 세션이 흡수**하는
  설계여야 한다 — 그냥 실패시키면 그날 점검이 통째로 사라진다.
- **선행 조건**: 없음.
- **우선순위 제안**: **즉시.**

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| 실전 전환 ⑤ (CB② 복원, 기한 **2026-08-29**) | "재검토 2026-08-29" | **선행조건 1건 추가** — *"'5분 내'의 의미 확정"* | 1-2. 시간창 없는 구현에 `2~3`을 넣으면 문서가 약속한 것보다 **훨씬 넓게** 발동한다. 오늘 실측 카운터 도달 소요 **206분** |
| 실전 전환 ⑧ (사이징 재설계) | "남은 해제 조건은 조건 1(실전 자본 재설정)뿐 + G-1 라이브 확인 선행" | **변경 없음** | 1-4. G-1은 오늘도 미충족(`불일치 6/6`). F-pre2-1 착수 전에는 진전 없음 |
| 실전 전환 ⑨ (TOX-SEVERE-SPREAD) | `INSUFFICIENT`, ETA 약 7.1개월 | **변경 없음** | 오늘 `≥20틱` 분봉 1건 추가됐으나 **진입이 아니다**. 진입 표본 6포지션/4거래일 불변 |
| 26주 재검증 **C1'**(`edge_only_indicators`) | 미구현, F-pre2-4가 설계 입력 | **등록부 첫 항목에 `[ConfFloorGuard]` 페어 추가** | 1-1이 정확히 C1'가 잡으려는 유형이며, 470차 L2가 그 대응이었는데 그 대응이 죽었다 |
| 26주 재검증 **C2'**(§12 사이징 축 3종) | 미구현 | **`전략판정`을 "조건부 로그" 사례로 명시** | 1-3. C2'가 *"조건부 로그는 넣지 말 것"*이라 경고한 바로 그 형태가 **이미 목록 안에 들어와 있다** |

---

## 4. 진입 승패 사후검증 (장후 한정)

**해당 없음(장후 전용).** 단, 장중 점검 범위에서 확인 가능한 **3원 대사만** 아래 옮긴다 —
장후 세션이 이 값에서 출발할 수 있도록.

**4-2. 3원 대사 (포지션 단위) — ✅ 완전 일치**

| 원천 | 질의 | 값 |
|---|---|---|
| 로그 | `[Position] 진입` | **18** |
| `ensemble_decisions` | `entry_executed=1` (`predictions.db`) | **18** |
| `trades` | `COUNT(DISTINCT entry_ts)` | **18** |
| (참고) `trades` | `COUNT(*)` — 청산 레그 | 20 (부분 2 + 최종 18) |

레그 20 > 포지션 18은 **불일치가 아니다**(부분청산 2건).
`ensemble_decisions` 당일 행수 **369** = 매분 루프 커버리지 371분과 정합.

---

## 5. 수익률 향상방안 (장후 한정)

**해당 없음(장후 전용).** 오늘 관측 중 §5로 넘길 씨앗은 「확인 필요」의 **손절 준수율**
1건이며, 6칸 중 「표본 상태」가 `n=5 / 관측일=1 / 313차 미통과`라 **방안이 아니라 아이디어**다.
장후 세션에서 요인 태그 **E**로 다룰 것.

---

## 6. 다음 거래일(2026-08-19) 관측 예정

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| **N-1** | `grep -c "ConfFloorGuard] state=" logs/20260819_SIGNAL.log` | F-1 적용 시 **≥365** 이고 값에 `OK`/`BLOCKED` 등장. 미적용이면 **80±10 재현**(진단 적중 증거) | 08-19 |
| **N-2** | `[전략상태] verdict=` (F-2① 신설) | 15:40 이후 1줄 존재. 없으면 F-2① 미적용 | 08-19 |
| **N-3** | `[EntryGate] … 불일치 N/6` (O-p1 승계) | F-pre2-1 미적용이면 **6/6 재현이 정상**. 적용 후 첫 EOD 거쳤으면 **0/6** | 08-19 08:55 |
| **N-4** | 사이드카 `model/horizons/gbm_*_meta.json:label_scheme` (O-p2 승계) | `['fixed']*6`이면 F-pre2-1 성공. `[null]*6`이면 미적용 | 08-19 EOD 후 |
| **N-5** | `logs/eod_ghost_check.log` 08-19 행 (O-p7 승계) | **15:4x 행 존재**. 없으면 1-5 재현 확정 | 08-19 |
| **N-6** | `data/session_state.json` P8 키 2종 (**O-3**) | 08-19 08:41 기동 **후에도** `p8_last_success_date`·`eod_retrain_ok_date` 잔존. 사라지면 `main.py` 기동 경로가 범인 확정 | 08-19 09:00 |
| **N-7** | `.git/index.lock` (1-6) | 예약 점검 후 재생성되는가. 3일 연속이면 **G-3 즉시 착수 확정** | 08-19 |
| **N-8** | 예약 점검 실행 시각 (1-7 / O-7) | `intra`가 09:00~15:10 안에 도는가. **3일 연속 이탈이면 예약 설정 자체를 손볼 것** | 08-19 |
| **N-9** | `[CB] 연속 손절` 타임스탬프 간격 | 1-2 근거 축적. **10거래일 모일 때까지 판정 금지** | 08-29 |
| **N-10** | `[SchedForceExit] … bar_pass=N회` (F-1H 승계) | `bar_pass ≥ 1`. **0이면 471차 F-1 경로 회귀** | 08-19 15:11 |
| **N-11** | `[TP비율] TP1 설정=…% 실효=…%` (L3, 오늘 트리거 미성립) | **TP1 부분익절이 발생한 날에만** 판정 | 미정 |

---

## 7. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| **CB② 복원** (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | **2026-08-29** | **11일** | ⚠ **선행조건 추가 권고(F-4)** — "5분 내"의 의미를 먼저 확정. N-9로 10거래일 간격 분포 집계 시작. **오늘 시작하지 않으면 08-29에 표본이 없다** |
| CB③-P4 재검토 (⑥) | 30m 재도입/CB③ 기준 교체 시 | — | 기한 없음. 오늘 acc30m 정상 변동(고착 아님) 확인 |
| FP-CRITICAL 재검토 (⑦) | 라이브 관찰 중 | — | 오늘 PSI **0.034 (CLEAR)** · feat별 `cvd=0.238 vwap=0.034 ofi=0.006`. 371차 분위수 재설계 후 정상 구간 오르내림 관측 **누적 중** |
| G-1 라이브 확인 (⑧ 선행) | 기한 없음 | — | **오늘도 미충족.** F-pre2-1 착수가 유일한 진전 경로 |
| ⑨ TOX-SEVERE-SPREAD | ETA 약 7.1개월 | — | **주간회의 안건**(선행조건 유지 vs 26주 WFA 이관). 오늘 진입 표본 불변 |

---

## 8. dev_memory 반영

> 🔴 **본 장중 세션에서는 dev_memory를 갱신하지 않았다.** 스킬 규약(장중 커밋 금지) +
> 오늘 세션 3본 동시 실행(1-7)으로 `DECISION_LOG.md`(1.8MB) 동시 append 위험이 있다.
> **장후 세션에서 아래 내용을 일괄 반영할 것.**

- `DECISION_LOG.md` 추가 예정: `## 2026-08-18 (MW0602 475차 — 장중 점검)`
  - 1-1 ConfFloorGuard 계산기 오참조 (증상→원인→결정→Why→How to apply→검증)
  - 1-2 CB② 시간창 드리프트 (**결정: 등재만. 코드·임계 무변경**)
  - 1-3 전략판정 배너 조건부 로그 → §12 구조적 오탐
  - 1-5 A3 프로덕션 경로 미배선 (**선행 조사 필요 — 확정 아님으로 기록**)
  - 470차 R1 첫 라이브 적용 확인 (UNDERPERFORM → INSUFFICIENT, **설계대로**)
- `NEXT_TODO.md` 추가 예정 체크박스: **11건**(N-1~N-11) + F-0~F-4 + G-1~G-3
- 완료 처리 예정: **O-p1**(6/6 재현 확인) · **O-p5**(첫 관측, 단 부분관측) ·
  **O-p7**(미관측 — 원인 1-5로 이관) · **O-1/O-2/O-8/F-1H/F-4M** (아래 PASS 표)
- 세션 차수: **475차** (원격 기준 `90c66cb`=471차 이관 정합 수정 이후. ⚠ 장후 세션이
  같은 날 다른 차수를 붙이지 않도록 확인할 것 — 2026-07-02 273차 충돌 전례)

---

## 부록 A. 사전등록 관측 결과 — **PASS 항목**

이상점이 아니라서 §1에 없지만, **답이 나온 사전등록 질문**이다. 다음 세션이 다시 묻지 않도록 남긴다.

| ID | 질문 | 결과 |
|---|---|---|
| **O-1** | EOD 재학습 완주 (로그 ≥10KB + `6/6` + 마커) | ✅ **PASS** — 17.4KB · `horizons_replaced: 6/6` · `eod_retrain_done_20260818.txt` (15:47:44). ⚠ MW0602 관측이며 **MW0601의 08-17 중단 건을 닫지 않는다**(`logs/`·`data/`는 gitignore, PC별 로컬) |
| **O-2** | `session_state.json` P8 키 2종 재생성 | ✅ **PASS** — `p8_last_success_date=2026-08-18`, `eod_retrain_ok_date=2026-08-18` |
| **O-8** | 08:40 자동 기동 | ✅ **PASS** — `logs/Mireuk_batch/launcher_20260818_084001_2418.log` (1.9MB) |
| **F-1H** | `[SchedForceExit]` 하트비트 1일차, `bar_pass=0`이면 회귀 | ✅ **PASS** — `15:11:15 … bar_pass=2회` |
| **F-4M** | 스키마 마이그레이션 (473차 `spread_ticks`·`spread_extreme_shadow` + 471차 `sizing_trace`) | ✅ **PASS** — `ensemble_decisions` 56컬럼, 3종 전부 존재. 오늘 369행 중 `spread_ticks` **369/369**, `sizing_trace` **65/65**(=`[Sizer]` 호출 65회와 정확히 일치) |
| **L1** | 11:50~13:00 `[ZeroDiag]` `존금지` 접두 100% | ✅ **PASS** — **70/70** |
| **루프** | 매분 루프 커버리지 09:00~15:10 | ✅ **PASS** — **371/371분 (100.0%)**, 10분 이상 공백 **0건** |
| **CB⑤** | 파이프 지연 5초 초과 (`CB_PIPE_PAUSE_MS=5_000`) | ✅ **PASS** — 블로킹 2건, 최대 **2,297ms**, 5초 초과 **0건** |
| **O-p5** | `[ConfFloorGuard] state=` 첫 관측 | ⚠ **부분** — 찍히긴 했으나 80/370. → **이상점 1-1** |
| **O-p6** | `[전략 상태 경보]` 배너 MDD 분모 | ❌ **판정 불가** — 배너 자체가 없다. → **이상점 1-3** |
| **O-p7** | `[EodGhostCheck]` EOD 체인 첫 실행 | ❌ **미관측** → **이상점 1-5** |

## 부록 B. 사이저 vs 실제 체결 (⑧ / 채널 [28] 입력)

사이저 출력 **1계약×61 · 3계약×4** → 실제 진입 **1계약×16 · 2계약×2**.
`qty≥3` 체결 **0건**(431차 배포 이후 누적 부재 지속).

```
10:08:58 [SizerMatch] sizer=3계약 → actual=2계약 (gap=1) | binding=hurst(0.50) | kelly=0.60 meta=1.00(raw0.50·무정보폴백→중립) tox=1.00 exec=1.00
10:15:58 [SizerMatch] sizer=3계약 → actual=2계약 (gap=1) | binding=없음(안전군·상한·증거금 경로) | kelly=0.60 meta=1.00(raw0.50·무정보폴백→중립) tox=1.00 exec=1.00
10:15:58 [MarginCap]  SHORT 산출=3계약 → 증거금상한=2계약으로 축소
```

배수 조합은 **65/65 전부** `conf=0.6 regime=0.8 safe=1.00` — 신뢰도·레짐 배수가
사실상 상수로 작동한다(431차에 규명·계측 중인 **정상 고착**).
오늘 binding 축이 **2건 서로 다르다**(`hurst(0.50)` 1건 / `증거금` 1건) —
470차가 "MarginCap이 `MAX_CONTRACTS`보다 먼저 자른다"고 실측한 구조가 오늘도 재현됐다.
⇒ **[28]은 "표본이 쌓이는 중"이 아니라 "⑧ 해제 전까지 켜지지 않는 상태"** 라는 470차 결론 유지.

---

*본 리포트는 **읽기 전용 조사**로 작성됐다. 코드·설정·git 상태를 변경하지 않았다.
`docs/정기점검/매일점검/MW0602-20260818-점검리포트.md`(장전/장후 본)와 **다른 파일**이며
어느 것도 덮어쓰지 않았다.*
