# 미륵이 일일 점검 — MW0601 / 2026-08-18 / 장중(intra)

- 점검 시각: **13:49~14:00 KST** · PC `MW0601` · 브랜치 `v9-dev` (규약 일치)
- HEAD `7dc14bc` (474차) · 당일 커밋 **0건** · 미커밋 — 수집기 표시 455건은 **샌드박스 CRLF 아티팩트**(473차 후속2 §5 확정, 재인용 금지). 실질 변경은 08-17 세션의 미커밋 10파일 그대로
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260818_intra.md`
- 관측 구간: **08:40 기동 ~ 13:54** (장 마감 15:35까지 잔여 약 100분 — 본 리포트는 그 구간을 포함하지 않는다)

> ⚠ **본 점검은 코드를 변경하지 않았고 라이브 DB(`raw_data.db`·`predictions.db`·`trades.db`)에
> 어떤 쿼리도 돌리지 않았다.** 근거는 전량 `logs/` 텍스트다. DB가 필요한 판단(3원 대사·
> `ensemble_decisions` 반사실·F-4M 스키마 1일차)은 **장후로 이월**한다.

---

## 0. 한 줄 결론

**P0 없음.** 매분 9단계는 08:40 기동 이래 공백 없이 돌고 있고 절대원칙 위반도 없다 —
다만 오늘의 매매는 **13:19:59 ProfitGuard L1-Trail 래치로 사실상 종료**됐고(잔여 세션
신규진입 구조적 0), 그와 별개로 **CLAUDE.md 절대원칙 §2가 "살아 있다"고 적고 있는 CB③ HALT가
코드에서 완전히 비활성**이라는 사실이 확인됐다(P1, 신규).

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | **조건부** | 08:40:01 자동 기동 정상(O-8 해소). 단 장전 점검 세션이 08:57이 아니라 **13:44:15**에 돌았고 리포트 산출물이 없다(O-7 답 — §1 P2-2) |
| 장중 | **조건부** | 9단계 전 STEP 관측됨, 진입 7·청산 7·잔여 FLAT. CB③ HALT 문서-코드 불일치(P1-1), `_tick_header` 37.9초 신기록(P1-2), Degraded 선제차단 S5 변종(P1-3) |
| 장후 | 해당 없음 | 15:10 미도래 |

**절대원칙 준수 여부**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | **판정 유예** | 13:54 현재 15:10 미도래. 현재 포지션 **FLAT**(13:19:39 전량 청산). F-1H 하트비트 1일차는 장후 판정 |
| ② | Circuit Breaker 5종 | **조건부** | CB②·CB③-P4·CB⑤ 계측 정상 동작. **단 CB③ HALT 트리거는 코드상 완전 비활성** — §1 P1-1 |
| ③ | CORE 피처 호라이즌 그룹별 | **준수** | 진입 7건 전부 `vwap✅`. 강제 X 조건(vwap 미통과 진입) **0건**. `entry_horizon`은 1m×1·3m×2·5m×4로 단기 그룹만 사용(474차 도달성 기술과 일치) |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | **준수** | `0xC0000409`·`STACK_BUFFER`·프로세스 무응답 로그 **0건**. `[FaultHandler] 활성화 PID=21660` 이후 크래시 흔적 없음 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | **준수** | TR 수신 예외 0건. `[CybosInvestorRaw]`×1,150 정상 수신 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | **준수** | 당일 로그에 알파봇 통합·백테스트 자동큐 흔적 **0건** |

**설정 불변식** — 수집기 §3에서 `불일치` **0건**. `미발견` 5종
(`MODEL_LABEL_STATE_UNLOCK_ENABLED` · `PRE_RETRAIN_DONE_BY_EOD_ENABLED` ·
`ZONE_ENTRY_BAN_ENFORCE` · `ZONE_ENTRY_BAN_SHADOW_ENABLED` ·
`PIPE_LATENCY_EXCLUDE_MODEL_SWAP`)은 **전부 `v9-dev`↔`origin/dev` 브랜치 격차가 원인**으로
473차 후속2 §5가 이미 확정했다(해당 커밋 10건 전부 `[MW0602]`, `v9-dev`에 0건).
**신규 안건 아님** — 처분은 주간회의.

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**0건.** 진입 7건 모두 `vwap✅`이라 절대원칙 ③의 강제 X 조건에 걸린 건이 없고,
오버나이트 위험(원칙 ①)은 현재 포지션 FLAT이라 성립하지 않는다.

---

### P1 — 정확성·관측 훼손

#### P1-1. CB③ HALT 트리거가 코드에서 완전 비활성인데, CLAUDE.md 절대원칙 §2는 살아 있다고 적고 있다 🆕

- **증상**: `acc30m`이 **12:00~13:00 한 시간 동안 0.0%~5.3%** 구간에 체류했다.
  `CB_ACCURACY_MIN_30M = 0.28` 기준이면 명백한 HALT 구간인데 `state=NORMAL`이 유지됐고
  당일 정지도, CRITICAL 로그도, Slack 알림도 없었다.
- **근거**:
  ```
  2026-08-18 12:16:59 [DEBUG] [DBG-CB] state=NORMAL consec_stops=0 acc30m=4.8% latency=0.430s
  2026-08-18 12:30:59 [DEBUG] [DBG-CB] state=NORMAL consec_stops=0 acc30m=3.8% latency=0.295s
  2026-08-18 12:40:59 [DEBUG] [DBG-CB] state=NORMAL consec_stops=0 acc30m=3.3% latency=0.314s
  2026-08-18 12:39:59 [WARNING] SYSTEM: [CB③-P4] acc30m 단계 전환: NORMAL → RESTRICTED (acc=3.3%)
  ```
  `logs/20260818_DEBUG.log` · `logs/20260818_WARN.log`

  코드 실측 — `safety/circuit_breaker.py:315~334`:
  ```python
  # CB③ acc30m HALT 트리거 — 비활성화(2026-06-25) → 30m 퇴역 확정(296차, 2026-07-06)
  if acc < effective_min:
      # HALT/경고 발동 없음 — 로그만 기록
      logger.debug("[CB③ 비활성] acc30m=%.1f%% < %.0f%% n=%d ...")
  ```
- **기준 위반**: 문서-코드 드리프트. CLAUDE.md 절대원칙 §2는 CB③을 5종 중 하나로 **집행
  주체**로 기술하고(461차 F-3이 "**코드는 바뀌지 않았다. 문서가 코드를 못 따라간 것이다**"라고
  임계값만 정정), 실전 전환 기준 ②의 471차 주석은 "**CB①~⑤는 라이브 발동이 확인됐다**"고
  단언한다. `references/invariants.md` §3-B의 문서-코드 드리프트 표에도 이 항목이 없다.
  CLAUDE.md 한시예외 목록이 담고 있는 것은 `CB3_P4_GRADE_BLOCK_ENABLED`(**등급 차단**)뿐이며,
  **HALT 트리거 자체의 비활성은 어디에도 등재돼 있지 않다.**
- **영향**: 세 갈래로 번진다.
  1. **실전 전환 기준 ②가 잘못 충족될 수 있다.** CB③은 296차(2026-07-06) 이래 발동이
     구조적으로 불가능하므로 "CB 1회 이상 정상 작동"을 CB③으로 셀 수 없다.
     이는 471차 F-1(15:10 1차 경로가 6개월간 구조적 도달 불가)과 **같은 유형**이다.
  2. **전제가 이미 다른 판단에 쓰였다.** 08-17 장전 리포트가 EOD 미완주 위험을
     *"모델 미교체 → CB③ HALT로 이어지는 기존 사고 경로"* 로 서술했고
     `references/phases.md` A-1도 같은 문구를 싣고 있다. 그 사고 경로는 **현재 닫혀 있다** —
     위험의 성질이 "당일 정지"가 아니라 "**정지 없이 열화된 모델로 계속 매매**"로 바뀐다.
     후자가 더 나쁘다.
  3. CLAUDE.md 474차 항목 *"30m 학습을 중단하면 안 된다 — CB③의 유일한 입력원이다"* 는
     **부분적으로만** 옳다. 30m이 먹이는 것은 `accuracy_buf` → **P4 stage 추적과
     Contrarian 판단**이지 HALT가 아니다(HALT 분기는 도달해도 `logger.debug`만 찍는다).
     이 구분을 안 하면 ⑥의 처분 논의가 잘못된 전제 위에서 돈다.
- **신규 여부**: **신규.** `dev_memory/NEXT_TODO.md:7538`에 296차 당시의 검증 항목
  (*"CB③ HALT 미발동 — `[CB③ 비활성]` DEBUG 로그 확인"*)이 남아 있어 **사실 자체는
  당시 의도된 것**이지만, 그것이 CLAUDE.md 절대원칙·전환기준·`invariants.md` 어디에도
  등재되지 않았다는 점은 지금까지 어느 점검에서도 지적되지 않았다.
  ⚠ **이것은 결함이 아니라 문서 결손이다** — 코드를 되돌리자는 제안이 아니다.

#### P1-2. `_tick_header` 메인 스레드 정지 **37,875ms** — 종전 최대의 3.3배 신기록 🆕

- **증상**: 13:28:36에 메인 스레드가 **37.9초** 멈췄다. 하루 18건 중 5초 초과 4건.
- **근거**:
  ```
  2026-08-18 12:17:12 [WARNING] SYSTEM: [LiveDBG] _tick_header 간격 14422ms — 메인 스레드 블로킹 발생 | pipe_elapsed=0
  2026-08-18 12:22:03 [WARNING] SYSTEM: [LiveDBG] _tick_header 간격  5203ms — 메인 스레드 블로킹 발생 | pipe_elapsed=0
  2026-08-18 12:30:04 [WARNING] SYSTEM: [LiveDBG] _tick_header 간격  5532ms — 메인 스레드 블로킹 발생 | pipe_elapsed=0
  2026-08-18 13:28:36 [WARNING] SYSTEM: [LiveDBG] _tick_header 간격 37875ms — 메인 스레드 블로킹 발생 | pipe_elapsed=0
  ```
  `logs/20260818_WARN.log` · 총 18건 · 08:41:24~13:36:03

  **일별 최대 추이** — 08-14 **9,125ms** / 08-13 **11,625ms**(NEXT_TODO O-7) / 08-12 8,172ms →
  **08-18 37,875ms**. 건수(4건)는 G-3 상향 기준 15건에 한참 못 미치지만 **크기가 자릿수를 넘었다.**
- **기준 위반**: 9단계 STEP 8(청산 트리거 감시)의 실효 전제. 파이프라인은 분 경계에서만
  돌지만 하드스톱·TickTP1은 **틱 단위 감시**이므로 메인 스레드 37.9초 정지는 그 구간의
  틱 감시가 통째로 비었다는 뜻이다.
- **영향**: 오늘은 **실해 없음** — 13:28:36 시점 포지션은 FLAT이었고(직전 청산 13:19:39),
  12:17·12:22·12:30도 전부 FLAT 구간이었다. 그러나 이는 **운이지 설계가 아니다.**
  같은 크기의 정지가 포지션 보유 중에 나면 ATR×1.5 손절선을 37.9초간 감시 못 한다.
  오늘 유일한 손실 포지션의 하드스톱 슬리피지가 0.12pt였던 것과 대비하면 위험 규모가 보인다.
- **신규 여부**: **기존 채널의 신규 최댓값.** `NEXT_TODO` G-3(수집기 적신호에서
  `_tick_header`와 `PipePerf total` 분리)·O-7·N-4로 이미 추적 중.
  ⚠ **"5초 초과인데 CB⑤ 미발동"은 정상**이다 — CB⑤는 `[PipePerf] total`을 보고
  `_tick_header` 간격은 별도 계측이다(08-14 intra 세션이 이미 확정). **그 부분을 다시
  이상점으로 올리지 않는다.** 여기서 새로 문제 삼는 것은 **크기**뿐이다.

#### P1-3. Degraded 선제차단이 **S5(1,630ms) 주도**로 발생 — 08-14 기술한 S0 자기유발 사슬의 새 변종 🆕

- **증상**: 12:18:59 Degraded 선제차단의 원인이 `cause=S5(1630ms)`다. 08-14 intra 세션이
  기록한 사슬 3회는 **전부 `S0`(모델 리로드) 단독**이었다.
- **근거**:
  ```
  2026-08-18 12:18:01 [WARNING] SYSTEM: [PipePerf] total=2797ms | S0=5ms S1=151ms S2=262ms S3=0ms S4=352ms S5=1630ms S6=198ms S7=196ms S8=3ms
  2026-08-18 12:18:59 [WARNING] SYSTEM: [HealthPolicy] Degraded 선제차단: streak=1.00+1.00 ≥ 2 (latency=2797ms quality=1.00 cache=0s exc10m=1) | cause=S5(1630ms)
  ```
  대조군(같은 날 S0 주도 3건):
  ```
  09:39:01 total=2826ms S0=2407ms → 이하 정상
  12:27:01 total=2625ms S0=1869ms → 12:27:59 선제차단 cause=S0(1869ms)
  13:33:01 total=2545ms S0=2113ms → 13:33:59 선제차단 cause=S0(2113ms)
  ```
  `logs/20260818_WARN.log`
- **기준 위반**: 9단계 STEP 5(멀티 호라이즌 예측). `S5`는 예측 단계이며 모델 교체와 무관하다.
- **영향**: **08-17 장전 F-1B가 체리픽 심사에 올린 `PIPE_LATENCY_EXCLUDE_MODEL_SWAP`
  (462차 P2, `origin/dev`에만 존재)로는 이 건을 막지 못한다.** 그 스위치는 정의상
  모델 교체 구간만 차감한다. 오늘 Degraded 선제차단 4건 중 **1건(25%)이 그 스위치의
  사정거리 밖**이다. 체리픽을 "선제차단이 사라진다"로 기대하면 어긋난다.
- **신규 여부**: **신규**(사슬 자체는 08-14 기록, 원인 축이 다른 변종은 처음).
  차단 3~4건이 실기회 손실이었는지는 `ensemble_decisions` 반사실이 필요 — **장후 판정**.

---

### P2 — 운영 부담·기술부채

#### P2-1. 수집기 §5 손익 집계가 **레그 단위**라 오늘 실현손익을 **59% 과소 표기**했다 — 재발·확대

- **증상**: 다이제스트 §5가 *"청산 7건 · 승 6 (86%) · 합계 +5.62pt (**+269,334원**)"* 로 냈다.
  포지션 단위 실제 실현손익은 **+661,668원**이다.
- **근거**: `[청산 완료]`는 **마지막 레그의 손익**이고 TP1 부분청산·손절1차 조기축소 레그가
  집계에서 통째로 빠진다.
  ```
  최종 레그 합  : +75,801 +104,805 +63,325 +69,330 +6,859 +117,353 -168,139 = +269,334
  부분청산 레그 : +42,801 +18,805 +90,325 +94,330 +113,859 +109,353 -77,139 = +392,334
  포지션 단위 합 = +661,668
  ```
  **교차검증 성공** — 엔진 자체 집계와 **원 단위까지 일치**한다:
  ```
  2026-08-18 13:48:59 [INFO] SIGNAL: [ProfitGuard][DebugPnL] source=broker used=+685,000원 engine=+661,668원 broker=+685,000원
  ```
  `logs/20260818_TRADE.log` · `logs/20260818_SIGNAL.log`
  (broker −engine = 23,332원 차는 미결제·수수료 반영 시차로 추정 — 장후 확인 대상)
- **기준 위반**: **계측 4원칙 ①**(단위 명시 — 손익·승패 집계는 포지션 단위가 기본).
- **영향**: 수집기가 계측 4원칙 ①을 스스로 위반한다. 오늘은 오차가 **392,334원(과소 59%)**
  이고 **"승 6 (86%)"라는 승률 표기도 부정확**하다 — 7건 중 6건의 `exit_reason=하드스톱`은
  실제로는 **TP1 도달 후 트레일링 보호 이익 청산**이다:
  ```
  2026-08-18 10:21:45 [TickTP1] TP1 처리 (틱) SHORT tick=1131.60 tp1=1131.66 qty=2
  2026-08-18 10:21:45 [TP1 부분청산] 1계약 @ 1131.56 PnL=+0.89pt (+42,801원) 잔여=1계약
  2026-08-18 10:21:59 [청산 완료] PnL=+1.55pt (+75,801원)
  ```
  즉 "하드스톱 7/7"이라는 다이제스트 문구를 손절 준수율 분석의 입력으로 쓰면 안 된다.
- **신규 여부**: **재발·확대.** 08-14 intra 세션이 동일 결함을 관측했다(*"수집기 §5
  '청산 1건 · 승 0(0%) · -3,135원'은 레그 단위 집계다"* — DECISION_LOG 2026-08-14).
  그때는 표본 1건이라 오차가 31,865원이었고 **fix가 등록되지 않았다.**
  오늘 7건에서 **12배로 확대**돼 재발했다. → F-1로 등록.

#### P2-2. 장전 점검 세션이 08:57이 아니라 **13:44:15**에 돌았고 리포트 산출물이 없다

- **증상**: `evidence_MW0601-20260818_pre.md`의 생성 시각이 **13:44:15 KST**다.
  대응하는 `MW0601-20260818-점검리포트-pre.md`는 **존재하지 않는다**(다이제스트만 있다).
- **근거**:
  ```
  # 미륵이 증거 다이제스트 — 2026-08-18 / PRE
  - 생성 2026-08-18 13:44:15 KST · PC MW0601 (DeskTop-MW0601)
  - 리포 /sessions/sharp-gallant-volta/mnt/futures
  ```
  본 intra 세션의 리포 경로는 `/sessions/awesome-pensive-volta/...`로 **다른 세션**이다.
- **기준 위반**: 운영 규약. `NEXT_TODO` **O-7**(*"장전 점검이 08:57±5분에 도는지"*)의
  답이 나왔다 — **아니다. 4시간 47분 늦었다.**
- **영향**: 둘이다. ① 장전 점검이 장 시작 후에 돌면 "오늘 거래할 자격이 되는가"라는
  질문 자체가 성립하지 않는다(이미 7건 진입·청산 완료). ② **G-3(점검 세션 동시 실행
  직렬화)이 그대로 재발**했다 — 08-17에 pre/intra/post 3세션이 겹쳤고 오늘 pre(13:44)와
  intra(13:49)가 5분 간격이다. `dev_memory` 동시 append 유실 위험(MW0602 `483d41a` 전례).
- **신규 여부**: **기존 TODO의 관측 결과 확정**(O-7 답 + G-3 재발). 신규 안건 아님.

#### P2-3. IntradayRegime 채터링 18회 — G-5 5거래일 관측 **2일차**

- **증상**: 09:02~13:5x 사이 레짐 전이 **18회**(08-14: 19회). `MicroRegime`은 별도로 25회.
- **근거**:
  ```
  2026-08-18 09:02:58 [IntradayRegime] NORMAL → CRASH        | day=+0.68% ATR=1.00 z=4
  2026-08-18 09:03:58 [IntradayRegime] CRASH → NORMAL        | day=+0.59% ATR=1.00 z=1
  2026-08-18 09:13:58 [IntradayRegime] NORMAL → DAY_RISK_OFF | day=+1.02% ATR=0.90 z=0
  2026-08-18 09:16:59 [IntradayRegime] DAY_RISK_OFF → NORMAL | day=+0.97% ATR=0.85 z=0
  2026-08-18 09:17:58 [IntradayRegime] NORMAL → DAY_RISK_OFF | day=+1.07% ATR=0.83 z=0
  ```
  `logs/20260818_SIGNAL.log` · 18건
- **영향**: 오늘도 실해는 확인되지 않는다 — 진입 7건 전부 `[Sizer] 레짐배수=0.8` 고정이라
  전이가 사이즈를 가르지 않았다. 위험은 여전히 잠재적(사후 분석 버킷 오염).
- **신규 여부**: **기존**(08-14 G-5 등록, "5거래일 누적 후 판단"). 오늘은 **2일차 표본**만
  더한다. **313차 원칙에 따라 확정 결론 보류.**

#### P2-4. `ConstOut` 5회 — 전부 재적합으로 자동 복구, 다만 3m에 집중

- **증상**: 3m×4(09:36·11:30·12:57·13:31) · 5m×1(12:24). 전부 1~2분 내 `해소 → 앙상블 복귀`.
- **근거**:
  ```
  2026-08-18 09:36:58 [WARNING] SIGNAL: [ConstOut] 3m 상수 출력 5분 감지 (range=0.0000 dir=+1) → 앙상블 제외
  2026-08-18 09:37:58 [INFO]    SIGNAL: [ConstOut] 3m 상수 출력 해소 → 앙상블 복귀
  2026-08-18 11:30:59 [INFO]    SIGNAL: [RouterHealth] 라우터가 ConstOut 활성 호라이즌 선택 — chosen=3m const_out=['3m'] (섀도 기록만, 정책 무변경)
  ```
- **영향**: **장중 재학습 5회가 전부 이 ConstOut이 트리거한 것**이며
  (`[GBM-64] … force=True intraday=True const_out=['3m'] scope=3m`), 그중 3회가
  P1-3의 S0 스파이크 → Degraded 선제차단 사슬로 이어졌다. 자기유발 사슬이 오늘도 3회 완주.
  `[RouterHealth]`가 "ConstOut 중인 3m을 라우터가 그대로 골랐다"를 섀도로 3회 기록했다 —
  정책 무변경이라 실해는 없으나 **관측 가치가 있다**.
- **신규 여부**: **기존**(08-14 관측 기록). 오늘은 3m 편중(4/5)이 새 정보.

---

### 확인 필요 (확정 아님)

- **`[Bias⚠] 1m FL편향 75%`가 상승 중** — 13:47 67% → 13:51 **75%**(적중 30%, 6/20).
  `record_horizon_fl_bias()`의 CRITICAL 경보 기준은 `streak ≥ 30분`인데 오늘 아직
  `[CB-FLBias]` 발동 0건이다. **15:10까지 지속되는지가 판정 조건** — 장후 확인.
- **broker(+685,000) vs engine(+661,668) 23,332원 차이** — 수수료·미결제 시차로 추정하나
  검증 안 됨. **장후 3원 대사에서 확정**(계측 4원칙 ②: 미측정 ≠ 0).
- **ProfitGuard L1-Trail 차단 82건이 실기회 손실이었는가** — 13:23:59·13:33:01·13:51:59에
  **A등급(체크리스트 11/10/9 통과)** 신호가 차단됐다. 반사실 판정은 `ensemble_decisions`가
  필요하므로 **장후**. 1거래일 표본이므로 **313차 원칙에 따라 확정 결론 금지.**
- **F-1H(15:10 하트비트 1일차) · F-1R(강제청산 리허설) · F-4M(스키마 1일차)** —
  전부 15:10 이후에만 판정 가능. **08-17에서 이월된 채 여전히 미판정.**
- **FP-CRITICAL PSI 값** — 당일 로그에 PSI 출력 **0건**. 08-14에도 같은 상태였다.
  차단은 비활성이나 **계산·file 로그는 유지돼야 한다**(CLAUDE.md 한시예외 3).
  셰도 모니터링이 살아 있는지 장후 확인 — **P1 후보**.

---

### 이미 반영된 사안 (신규 아님)

이 절을 비워두지 않는다. 오늘 로그·다이제스트가 지적하지만 **이미 결론이 난 것들**:

1. **수집기 §11 적신호 6·7**(*커버리지 290/371분 78.2%*, *13:50~15:10 81분 공백*) —
   **13:49에 수집기를 돌려 미래 구간을 분모에 넣은 계산 산물**이다. 경과분 대비 공백은 **0**.
   08-14 세션이 동일 지적을 이미 확정했다.
2. **수집기 §11 적신호 9**(*5초 초과 4건 — CB⑤ 발동 여부 확인*) — CB⑤는 `[PipePerf] total`을
   보고 `_tick_header` 간격은 별도 계측이다. **미발동이 정상.** `NEXT_TODO` G-3이 이 분리를
   이미 등록했다. (오늘 새로 문제 삼은 것은 **크기**뿐 — P1-2)
3. **수집기 §11 적신호 15**(*미커밋 변경 455건*) — **샌드박스 CRLF 아티팩트.**
   473차 후속2 §5가 *"수집기 §2·§11의 미커밋 건수는 샌드박스 실행 시 신뢰 금지"* 로 확정.
   `NEXT_TODO` F-5로 등록돼 있다.
4. **설정 불변식 `미발견` 5종** — 브랜치 격차(`v9-dev` ↔ `origin/dev` = 320/315)가 원인.
   473차 후속2가 실측 확정, 처분은 **주간회의 안건**. 점검 세션 단독 결정 사안 아님.
5. **CLAUDE.md STEP 3 "GBM 배치 재학습 (30분마다)"가 코드에 없다** — 오늘도 장중 재학습
   5회가 전부 ConstOut 트리거이고 간격이 114/54/33/33분으로 불규칙하다. 그러나 이것은
   **08-14 intra 세션이 F-8(문서 정정)로 이미 등록**했다. **신규 이상점으로 올리지 않는다.**
6. **`[Calibration]` WARNING 154건** — 기동 워밍업 스윕이며 일별 실측이 평탄하다
   (08-10 157 / 08-11 148 / 08-12 145 / 08-13 141 / 08-14 144 / 08-17 143 / **08-18 154**).
   `NEXT_TODO` G-4(로그 레벨 하향)로 등록됨.
7. **CB② 연속 손절 카운터 2회 기록, 정지 없음** — `CB_CONSEC_STOP_LIMIT=9999`라
   **카운터만 오르고 정지 안 하는 것이 정상**이다(한시예외).
8. **`WeightCollapse` 61건(약 21%)** — CLAUDE.md 확률 판단 기준 절이 기술한
   *"붕괴행 매일 약 21~22%"* 범위 안. conf 평균 집계에서 제외 대상.

---

## 2. Fix 작업 구현계획

> 🔴 **본 계획은 수립만 하고 적용하지 않는다.** 13:54 현재 라이브 프로세스(PID 21660)가
> 돌고 있다. **전 항목 15:10 강제청산 이후 적용**을 권고한다.
> 커밋도 하지 않았다 — 채팅 마지막 줄의 "커밋 대기" 목록 참조.

### F-1. 수집기 §5 손익·승패 집계를 포지션 단위로 — P1 · 대응 이상점 P2-1

- **원인 가설**: `collect_evidence.py`가 `[청산 완료]` 행만 세고 `[TP1 부분청산]` ·
  `[손절1차 조기축소]` · `[Position] 체결부분청산` 레그를 집계 밖에 둔다.
  `trades.quantity`가 청산 레그별이라는 417차 교훈과 **정확히 같은 함정**이 로그 파서 쪽에서
  반복된 것이다.
- **변경 파일**:
  - `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py`
    — 거래일 요약 생성부(§5, 약 780~880행): `[Position] 진입` 시각을 키로 **포지션 버킷**을
      만들고 그 안에 `[TP1 부분청산]`·`[손절1차 조기축소]`·`[청산 완료]` 레그를 모두 담는다.
      출력 표를 **`포지션 n건 · 승 n · 포지션손익 합계`** 로 바꾸고, 기존 레그 표는
      **`(참고) 청산 레그 n건`** 으로 단위를 머리글에 박아 남긴다(계측 4원칙 ① — 삭제 금물).
    - 승패는 `exit_reason`이 아니라 **포지션 합계 부호**로 판정하고, `하드스톱`에
      `tp1_reached`가 섞였으면 `하드스톱(TP1보호)`로 별도 표기한다.
    - 검산 앵커: 같은 날 `[ProfitGuard][DebugPnL] engine=` 값이 있으면 **대조 결과를
      한 줄 출력**한다(오늘처럼 원 단위 일치하면 `✓ engine 일치`).
  - `.claude/skills/mireuk-daily-check/references/evidence_map.md` §8-4 — 이 앵커 대조를 명기.
- **회귀 위험**: 낮음. 수집기는 **관측 전용**이고 라이브 경로에 없다. 다만 §5 표 형식이
  바뀌므로 과거 다이제스트와 표 모양이 달라진다(값 비교는 오히려 정확해진다).
- **절대원칙 저촉 여부**: 저촉 없음(6종 어디에도 해당 없음).
- **런타임 주의**: 수집기는 **표준 라이브러리 전용**이다. 외부 의존을 붙이지 않는다.
  py37_32/py310_64 어디서 돌아도 같아야 한다.
- **검증 방법**: `--date 20260818 --phase intra` 재실행 → §5 포지션 합계가 **+661,668원**,
  `✓ engine 일치`가 뜨는가. 08-14(1포지션, +31,865 누락분 포함)로도 재실행해 회귀 확인.
- **섀도 계측 필요 여부**: 불필요(집행 로직 아님).
- **적용 시점**: **장후.** 라이브 무관하나 08-18 장후 점검이 이 수집기를 쓰므로,
  **장후 점검 실행 전에 넣거나 그 다음날로 미룬다**(중간에 바꾸면 오늘 두 국면의 다이제스트
  형식이 갈린다). → 권고: **08-18 장후 점검 종료 후 적용.**
- **결정 필요 사항**: 없음.

### F-2. CB③ HALT 비활성 사실을 문서 3곳에 등재 — P1 · 대응 이상점 P1-1

- **원인 가설**: 296차가 코드에서 HALT를 껐으나 그것이 `CB3_P4_GRADE_BLOCK_ENABLED`(등급
  차단)와 **다른 층위**라는 점이 문서화 과정에서 합쳐졌다.
- **변경 파일** (⚠ **코드는 건드리지 않는다 — 문서만**):
  - `CLAUDE.md` 절대원칙 §2 — CB③ 항목에 인용 블록 추가:
    *"[2026-08-18 47X차] **CB③의 HALT 트리거는 296차(2026-07-06)부터 코드에서 비활성이다**
    (`safety/circuit_breaker.py:315~334` — 임계 미달 시 `logger.debug`만). 임계값
    `0.28`은 살아 있으나 **집행되지 않는다.** `accuracy_buf` 누적·P4 stage 추적·
    Contrarian 입력은 유지된다. 30m의 역할은 **HALT가 아니라 P4·Contrarian**이다."*
  - `CLAUDE.md` 실전 전환 기준 ② — 471차 주석의 *"CB①~⑤는 라이브 발동이 확인됐지만"* 을
    *"CB①·②·④·⑤는 라이브 발동이 확인됐으나 **CB③은 HALT가 비활성이라 발동이 구조적으로
    불가능하다**"* 로 정정. **⑥(CB③-P4 재검토)에 HALT 복원 여부를 함께 묶는다.**
  - `.claude/skills/mireuk-daily-check/references/invariants.md` §3-B 문서-코드 드리프트 표에
    한 행 추가 + §1 절대원칙 ②의 CB③ 줄에 `(HALT 비활성)` 표기.
  - `.claude/skills/mireuk-daily-check/references/phases.md` A-1의
    *"전일 EOD 재학습 실패 → 모델 미교체 → CB③ HALT 유발"* 문구를
    *"→ 모델 미교체 → **정지 없이 열화 모델로 계속 매매**(CB③ HALT는 296차부터 비활성)"* 로 정정.
- **회귀 위험**: 없음(문서). 단 **08-17 장전 리포트의 EOD 위험 서술이 낡게 된다** —
  그 리포트는 정정하지 않고 이번 항목이 상위 근거임을 DECISION_LOG에 명기한다.
- **절대원칙 저촉 여부**: 저촉 없음. **CB③을 되살리자는 제안이 아니다** — 되살리려면
  30m 처분과 한 묶음이어야 하고(CLAUDE.md 474차), 그것은 주간회의 안건이다.
- **런타임 주의**: 해당 없음.
- **검증 방법**: 다음 점검에서 `phases.md` B-6 체크가 "CB③ HALT 미발동"을 **정상**으로
  읽는가. `tests/`에 도달성 불변식 테스트를 하나 더할지는 F-3에서 판단.
- **적용 시점**: **장후.** 08-14 pre F-2(CB③ 35% 문구)·F-8(STEP 3 30분 문구)이 아직
  미적용이므로 **같은 커밋으로 묶는다** — 셋 다 "참조문서가 낡은 설계 원안을 싣고 있다"는
  한 뿌리다.
- **결정 필요 사항**: 전환기준 ⑥에 "CB③ HALT 복원 여부"를 명시적으로 넣을지.
  **권고: 넣는다** — 지금은 ⑥이 P4 등급 차단만 가리켜 HALT가 체크리스트에서 누락된다.

### F-3. `_tick_header` 정지에 **크기 기준** 적신호 추가 — P1 · 대응 이상점 P1-2

- **원인 가설**: 현행 추적(O-7·N-4)이 **건수**(15건 초과 시 상향)만 기준으로 삼아
  "4건인데 37.9초"를 잡지 못한다. 오늘 건수 기준으로는 **정상**으로 분류된다.
- **변경 파일**:
  - `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py`
    — 적신호 생성부(약 1526행): 현행 `5초 초과 n건` 외에 **`최대값 ≥ 20,000ms`** 조건을
      **독립 적신호**로 추가. 문구에 *"CB⑤는 이것을 보지 않는다(별도 계측) — 크기만 문제"* 를
      박아 오독(G-3이 이미 분리한 것)을 막는다.
    - 함께: 해당 시각의 **포지션 보유 여부**를 같은 줄에 출력한다
      (`[Position] 진입`/`[청산 완료]` 시각 사이면 `⚠ 포지션 보유 중`).
      **오늘 4건이 전부 FLAT 구간이었다는 사실이 위험 평가의 핵심**인데 지금은 사람이 손으로
      대조해야 한다.
- **회귀 위험**: 낮음(관측 전용).
- **절대원칙 저촉 여부**: 저촉 없음.
- **런타임 주의**: 표준 라이브러리 전용.
- **검증 방법**: `--date 20260818` 재실행 → `최대 37,875ms` 적신호가 뜨고
  `포지션 보유 중` 태그가 4건 모두 **미출현**인가. 08-14(최대 9,125ms)에서는 **미출현**인가.
- **섀도 계측 필요 여부**: 불필요.
- **적용 시점**: **장후.** F-1과 같은 파일이므로 **한 커밋**으로 묶는다.
- **결정 필요 사항**: 임계 20,000ms의 근거는 "종전 최대 11,625ms의 약 1.7배"라는 경험칙뿐이다.
  **사전등록 원칙(313차 ④)상 지금 정하고 이후 움직이지 않는다.**

### F-4. `PIPE_LATENCY_EXCLUDE_MODEL_SWAP` 체리픽 심사에 **S5 변종**을 근거로 첨부 — P2 · 대응 P1-3

- **원인 가설**: 08-17 pre F-1B가 이 스위치를 체리픽 후보로 올릴 때 근거가 **S0 사슬 3회**뿐이었다.
- **변경 파일**: 코드 변경 **없음.** `dev_memory/NEXT_TODO.md` F-1B 항목에
  *"⚠ 이 스위치는 08-18 12:18 `cause=S5(1630ms)` 건을 막지 못한다 — 오늘 선제차단 4건 중 1건(25%)"*
  를 추가.
- **회귀 위험**: 없음.
- **절대원칙 저촉 여부**: 저촉 없음.
- **검증 방법**: 다음 주간회의에서 브랜치 격차 처분 논의 시 이 수치가 인용되는가.
- **적용 시점**: **장후**(dev_memory 갱신은 본 세션에서 이미 수행 — §8 참조).
- **결정 필요 사항**: 없음.

### 적용 순서와 커밋 계획

| # | 커밋 | 포함 | 메시지 초안 |
|---|---|---|---|
| ① | 수집기 계측 정합 | F-1 · F-3 | `[MW0601] 47X차: 수집기 §5 포지션 단위 집계 + _tick_header 크기 적신호` |
| ② | 참조문서 정정 | **F-2** + 08-14 잔여 F-2(CB③ 35%)·F-8(STEP 3 30분) | `[MW0601] 47X차: 참조문서 정정 — CB③ HALT 비활성·임계·STEP 3 트리거 실체 반영` |

> ⚠ **①을 08-18 장후 점검 전에 넣지 말 것.** 같은 날 두 국면의 다이제스트 형식이 갈린다.
> **권고 순서: 장후 점검 완료 → ① → ②.**

---

## 3. 고도화 방안

당일 관측에서 출발한 것만 쓴다.

### G-1. ProfitGuard L1-Trail 래치의 **기회비용 섀도 계측**

- **관측 근거**: 13:19:59에 L1-Trail이 발동해 **당일 영구 래치**됐다
  (`strategy/profit_guard.py:_TrailingGuard.update()` — `is_halted=True` 후 `reset()`
  전까지 무조건 `return True`). 이후 13:54까지 **82건 차단**, 그중 최소 3건이 **A등급**이다:
  ```
  2026-08-18 13:19:59 [WARNING] TRADE: [ProfitGuard-L1] 트레일링 발동 — 피크 +927,000원 대비 20% 하락 (현재 +685,000원 < 보호선 +741,600원)
  2026-08-18 13:23:59 [INFO] SIGNAL: [차단] 게이트 강등 X — ProfitGuard 진입 차단 ([L1-Trail] …) (체크리스트 등급=A, 통과 11개)
  2026-08-18 13:51:59 [INFO] SIGNAL: [차단] 게이트 강등 X — ProfitGuard 진입 차단 ([L1-Trail] …) (체크리스트 등급=A, 통과 9개)
  ```
  구조적으로 **자기잠금**이다 — 해제 조건이 `current_pnl` 회복인데 포지션이 FLAT이고
  신규 진입이 막혀 있어 **`current_pnl`이 오를 경로가 없다.** 오늘 13:19:59~15:10
  **약 110분(세션의 30%)이 구조적 무거래**로 확정됐다.
  또한 L1-Trail 발동은 **2026-06-30 이후 처음**이다(`grep -l ProfitGuard-L1 logs/*_TRADE.log`
  → 0512·0513·0619·0625·0630·**0818**). 7주 만의 발동이라 최근 표본이 없다.
- **제안 내용**: 래치는 **그대로 두고**(수익 보존이 설계 의도다), 차단된 신호의 **반사실
  손익을 섀도로 적립**한다.
  - `strategy/profit_guard.py:_block()` — 차단 시 `ensemble_decisions`에 이미 쓰이는
    차단사유 경로에 `pg_layer` · `pg_peak_krw` · `pg_current_krw` · `grade` 를 남긴다.
  - `config/settings.py:VALIDATION_CAMPAIGN` — 신규 채널 `profit_guard_latch_watch`
    (`min_samples=20`, `min_days=10`). 차단된 A/B등급 신호에 대해 **당일 남은 구간의
    1m/3m/5m 실현 방향**으로 반사실 손익을 계산.
  - **판정문 사전등록**: *"차단 신호 반사실 합계가 유의하게 양(+)이면 `trail_ratio`
    재보정 검토, 음(−)이거나 무의미하면 현행 유지."* 합격선을 나중에 움직이지 않는다.
- **기대 효과**: "수익을 지켰다"와 "기회를 버렸다"를 **처음으로 수치로 가른다.**
  현재는 어느 쪽인지 판정할 자료 자체가 없다.
- **비용·위험**: 낮음. **집행 무변경 — 순수 섀도**(스킬 §7-7 준수).
  위험은 표본 속도다 — L1 발동이 7주에 1회면 `min_days=10`은 **수개월 스케일**이다.
  ⚠ 그러니 **표본이 안 찬다고 문턱을 낮추지 않는다**(458차 D6·473차 F-8 Phase A 전례).
- **선행 조건**: 없음. F-1(수집기 포지션 단위)과 독립.
- **우선순위 제안**: **이번 주.** 발동일이 드물어 **오늘 데이터를 놓치면 다음 표본이 7주 뒤**다.

### G-2. 메인 스레드 정지 구간과 **포지션 보유 구간의 교집합**을 상시 계측

- **관측 근거**: 오늘 5초 초과 4건(최대 37,875ms)이 **전부 FLAT 구간**에서 났다.
  이 사실이 "실해 없음"의 유일한 근거인데, 확인하려면 사람이 `[Position] 진입`/
  `[청산 완료]` 시각과 수동 대조해야 한다. **위험도가 계측되지 않는다.**
- **제안 내용**: `dashboard/main_dashboard.py:_tick_header()`의 블로킹 경고 로그에
  **포지션 상태를 인라인으로 붙인다** — `| pos=FLAT` 또는 `| pos=SHORT 2ct stop=1095.75`.
  로그 한 줄만 늘리고 판정 로직은 건드리지 않는다.
  - ⚠ `_tick_header`는 **Qt 메인 스레드**다. 포지션 조회가 DB를 타면 안 된다 —
    이미 메모리에 있는 `self.position_manager`의 스냅샷만 읽는다.
  - ⚠ COM 콜백이 아니므로 절대원칙 ④ 저촉 없음. `dynamicCall`·`emit`을 넣지 않는다.
- **기대 효과**: `블로킹 5초 초과 × 포지션 보유`를 **일별 1개 숫자**로 낸다.
  이것이 0이 아닌 날이 나오면 그때가 진짜 P0다. 지금은 그 날이 와도 알 수 없다.
- **비용·위험**: 매우 낮음(로그 문자열 추가). 회귀 위험은 메인 스레드에 무거운 조회를
  넣는 것뿐인데 스냅샷 읽기로 회피한다.
- **선행 조건**: F-3(수집기 크기 적신호)과 짝이다 — 로그가 남아야 수집기가 집계한다.
  **F-3보다 먼저 배포**해야 다음 거래일부터 값이 쌓인다.
- **우선순위 제안**: **이번 주**(장후 적용).

### G-3. `RouterHealth` 섀도를 **판정 채널로 승격 검토**

- **관측 근거**: 오늘 `[RouterHealth] 라우터가 ConstOut 활성 호라이즌 선택 — chosen=3m
  const_out=['3m'] (섀도 기록만, 정책 무변경)` 이 **3회**(11:30:59 · 12:24:59 · 12:25:59).
  즉 **앙상블에서 제외된 호라이즌을 진입 라우터는 그대로 골랐다.** 오늘 그 3분에 진입은
  없었으나(전부 차단) 구조적으로는 "상수 출력 중인 모델의 호라이즌으로 진입"이 가능하다.
- **제안 내용**: 코드 변경 없이 **채널만 등록** — `VALIDATION_CAMPAIGN`에
  `router_constout_overlap`(`min_samples=20`, `min_days=10`)을 두고
  `RouterHealth` 발생 분의 진입 여부·손익을 적립한다.
  **집행(라우터가 ConstOut 호라이즌을 피하게 하는 것)은 판정 후에.**
- **기대 효과**: "섀도로만 기록 중"이라는 상태가 **언제 끝나는지**를 정의한다.
  지금은 종료 조건이 없어 TOX-SEVERE-SPREAD가 한 달간 죽은 섀도였던 것과 같은 경로에 있다.
- **비용·위험**: 낮음. 순수 등록(462차 P4 — *"처방이 아니라 등록이다"*).
- **선행 조건**: 없음.
- **우선순위 제안**: **26주 WFA 주기** — 오늘 3회는 표본이 아니다.

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| CB③ HALT 복원 여부 | 어디에도 없음 | **전환기준 ⑥에 명시 편입** | ⑥이 지금 P4 등급차단만 가리켜 HALT가 체크리스트에서 통째로 누락된다 (P1-1) |
| 전환기준 ② 문구 | *"CB①~⑤ 라이브 발동 확인됨"* | *"CB①·②·④·⑤ 확인 / CB③은 구조적 발동 불가"* | 사실과 다르다. 471차 F-1과 같은 유형의 암묵 전제 (P1-1) |
| ProfitGuard L1 래치 | 캠페인 채널 없음 | `profit_guard_latch_watch` 신설 | 세션의 30%를 결정하는 게이트인데 기회비용이 미계측 (G-1) |

---

## 4. 진입 승패 사후검증 (장후 한정)

**해당 없음(장후 전용).** 라이브 DB 조회가 필요하고 15:10도 미도래다.

다만 **장후 세션이 이어받을 사실 3가지**를 여기 남긴다(전부 로그 기반, DB 미사용):

- 포지션 **7건 전량 SHORT**, 계약수 전량 **2계약**, 진입 등급 `A급(원시C)`×6 · `A급(원시X)`×1
- 포지션 단위 실현손익 **+661,668원**(엔진 값과 일치). 승 6 · 패 1
- 유일한 패: 13:06:59 진입 SHORT 2ct @1092.67 → 13:08:37 손절1차 조기축소 −77,139원
  → 13:19:39 하드스톱(틱) −168,139원. **손절 준수율 양호** — 의도 손절폭 `stop_dist=3.21pt`
  (`[DBG-F8] stop=1095.75`) 대비 실현 −3.33pt = **초과율 1.04배**, 틱스톱 슬리피지
  1095.88→1096.00 = **0.12pt**. 417차가 지목한 "손절 미준수" 축은 오늘 표본에서는 깨끗하다.

## 5. 수익률 향상방안 (장후 한정)

**해당 없음(장후 전용).** §4-4 요인 집계가 없으면 방안이 아니라 아이디어다.

---

## 6. 다음 거래일(또는 오늘 장후) 관측 예정

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| O-A | `[TimeExit]`·`[ExitAttempt]`·`[ForceExitPass]` 15:10:00~05 출현 / `[SchedForceExit] … 안전망 발동`(ERROR) 미출현 | 1차 경로 생존 = F-1H 1일차 판정 | **08-18 장후** |
| O-B | `[Bias⚠] 1m FL편향` 15:10까지 추이 · `[CB-FLBias]` CRITICAL 출현 여부 | streak≥30분에도 미출현이면 P5 경보 경로 점검 | 08-18 장후 |
| O-C | FP-CRITICAL PSI 값이 file 로그에 남는가 | 당일 출력 0건이면 **P1 승격**(셰도 모니터링 사망 = TOX-SEVERE-SPREAD와 같은 패턴) | 08-18 장후 |
| O-D | broker(+685,000) − engine(+661,668) = 23,332원의 정체 | 수수료·미결제 시차로 설명되는가 | 08-18 장후 3원 대사 |
| O-E | ProfitGuard L1 차단 82건 중 A/B등급 반사실 손익 | 표본 1일 — **판정 금지**, 적립만 | 채널 신설 후 10거래일 |
| O-F | `_tick_header` ≥20,000ms 재발 여부 · 포지션 보유 중 발생 여부 | 포지션 보유 중 1건이라도 나면 **P0** | 매 거래일 |
| O-G | IntradayRegime 전이 건수 (오늘 18, 08-14 19) | G-5 5거래일 누적 — **3일치 더** | 08-19~ |
| O-H | 장전 점검 실행 시각 (오늘 13:44:15) | 08:57±5분 복귀 여부 | 08-19 |

## 7. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| **CB② 복원** (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | 2026-08-29 | **11일** | 오늘 `9999` 유지 확인. **표본 진전 있음** — 연속 손절 카운터 2회 기록(13:08:37·13:19:39). 08-29 주간회의 상정 |
| FP-CRITICAL 재검토 (전환기준 ⑦) | 미정 | — | 371차 분위수 재설계 후 **라이브 관찰 중**인데 **오늘 PSI 로그 0건**. O-C에서 관찰 자체가 살아 있는지 먼저 확인 |
| TOX-SEVERE-SPREAD 처분 (전환기준 ⑨) | 미정 | — | 473차 F-8 Phase A `INSUFFICIENT`(ETA 7.1개월). **주간회의 안건** — 문턱 낮추지 말 것 |
| 브랜치 격차 처분 (`v9-dev`↔`origin/dev` 320/315) | 미정 | — | **주간회의 안건.** 설정 불변식 `미발견` 5종의 근본 원인 |

## 8. dev_memory 반영

- `DECISION_LOG.md` 추가: `## 2026-08-18 (MW0601 475차 — 장중 점검 / intra)` — **append 완료**
- `NEXT_TODO.md` 추가 체크박스: **12건** (F-1~F-4 · G-1~G-3 · O-A~O-H 요약)
- 완료 처리한 기존 항목: **O-8**(08-18 08:40 자동 기동 여부 → 08:40:01 정상 기동 확인) ·
  **O-7**(장전 점검 08:57 여부 → **13:44:15, 미준수**로 답 확정)
- **git commit·push 미수행** — 라이브 프로세스 가동 중
