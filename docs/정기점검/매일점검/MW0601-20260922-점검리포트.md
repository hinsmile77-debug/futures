# 미륵이 일일 점검 — MW0601 / 2026-09-22

> ⚠ **`.git/index.lock`이 STALE로 확정됐다(09:11:42) — 이 세션은 회수 권한이 없다.** 상세는 이상점 1-1, 조치는 "사용자 조치" 1번 참조.

# 장전(pre)

- 점검 시각: 08:59~09:12 KST · PC `MW0601` (실행 환경: 코웍 리눅스 샌드박스, 호스트명 `claude` → `--pc MW0601` 수동 지정)
- HEAD `60d598c` · 브랜치 `v9-dev`(정상 — MW0601 운영 브랜치와 일치) · 미커밋 731건(원시) / **실질 변경 3파일**(EOL 차이 제외) · 당일 커밋 0건(아직 없음 — 정상, 장전 규칙상 커밋 금지)
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260922_pre.md`

## 0. 오늘 한눈에

오늘 아침 프로그램은 정상적으로 켜졌고, 09시 정각 개장도 예정대로 됐습니다. 어제 장 마감 후 진행되는 밤사이 재학습(모델을 새 데이터로 다시 훈련시키는 작업)도 성공적으로 끝났다는 것을 두 가지 서로 다른 방법으로 확인했습니다. 오늘 시장 상황을 컴퓨터가 스스로 판단한 결과는 "위험선호(자금이 위험자산으로 몰리는 국면)"이고, 아직 개장 1분 남짓이라 실제 매매는 한 건도 없었습니다(정상 — 이 시간에 매매가 있으면 오히려 이상합니다). 오늘 거래 자격에 즉시 영향을 주는 위험 신호는 없습니다.

다만 이 점검 작업 자체가 진행되는 동안, 이 프로젝트의 코드 저장소(깃)에 "잠금 파일"이 하나 생겼고, 10분 넘게 지나도 풀리지 않아 결국 "누군가 지워야 하는 상태"로 확정됐습니다. 이것은 실제 매매 프로그램과는 무관하고, 오늘 만든 기록 파일들을 나중에 저장(커밋)할 수 있는지에만 영향을 줍니다. 같은 현상이 최근 며칠 동안도 반복됐고, 이 점검 세션이 실행되는 환경(리눅스 샌드박스)에서는 이 잠금을 직접 지울 권한이 없어서, 사용자가 컴퓨터에서 직접 지워줘야 합니다(맨 아래 "사용자 조치" 1번).

또한 시스템 내부의 몇 가지 작은 상태 기록(예: "어제 마감 작업을 다 마쳤다"는 표시)이 날짜가 바뀔 때 자동으로 지워지는 오래된 버그가 오늘도 재현됐습니다. 실제 작업은 다른 경로로 이미 성공했다는 것을 직접 로그로 확인했으므로 오늘 거래에는 영향이 없지만, 이 버그 자체는 아직 고쳐지지 않은 채 여러 날째 이어지고 있습니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | **조건부 정상** | 기동·매크로·모델·EOD재학습 전부 정상 확인. `.git` 잠금 파일 재발(STALE 확정, 사용자 회수 필요)과 상태기록 소실 버그(기존 등록)만 관측 — 둘 다 매매 자체에는 영향 없음 |
| 장중 | (장중 점검에서 채움) | |
| 장후 | (장후 점검에서 채움) | |

**절대원칙 준수 여부**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | 준수(전일) | `data/eod_retrain_done_20260921.txt`(15:53:28 완료, `daily_close_seen: true`) — 전일 정상 마감 확인. 오늘은 아직 09:00 직후라 해당 없음 |
| ② | Circuit Breaker 5종 | 준수(대기) | 오늘 발동 이력 없음(매매 자체가 아직 없음). CB② 값은 `3`(정상), CB③ 입력원(30m 채점)은 `main.py:6845` 부근에서 여전히 배선 확인 |
| ③ | CORE 피처 호라이즌 그룹별 | 준수 | 6호라이즌(1m~30m) 스케일러·모델 전부 정상 로드 (`[RF] 로드 완료: 6호라이즌 ready=True`) |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 준수 | `0xC0000409`·`STACK_BUFFER` 관련 로그 0건 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 확인 대상 아님 | 오늘 로그에 해당 호출 이상 없음 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 준수 | 관련 흔적 없음 |

**설정 불변식** — 수집기 §3 표 전 항목 `일치`. 한시예외(CB②=3, CB3_P4=False, FP_CRITICAL=False, MAX_CONTRACTS=3, SIZING_TARGET_CAPITAL_ENABLED=True, TOXICITY_SEVERE_SPREAD=False 등) 전부 `references/invariants.md` §2 기대값과 동일 — 이상 없음.

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**없음.** 오늘 09:00 개장 직후 시점 기준으로 절대원칙 위반이나 즉시 거래 위험 신호는 관측되지 않았다.

### P1 — 정확성·관측 훼손

#### 1-1. `.git/index.lock` 재발 — `--no-optional-locks`를 전부 붙였음에도 생성됨, **STALE 확정·회수 실패**

- **증상**: 이 점검 세션이 사용한 모든 git 명령(`branch --show-current`, `log --since`, `diff --numstat -w`, `status --short`)에 예외 없이 `--no-optional-locks`를 붙였음에도, 09:00경 `.git/index.lock`(0바이트)이 새로 생성된 것을 09:00 이후 확인했다. 세션 종료 직전 최종 재확인 결과 **STALE로 확정**됐고, `--reclaim` 시도도 `Operation not permitted`(EPERM)로 실패했다 — SKILL.md §0에 문서화된 "리눅스 샌드박스 마운트 경유 시 unlink 거부" 그대로 재현.
- **근거**:
  ```
  09:00(mtime) .git/index.lock — 0바이트
  09:05 git_lock_guard.py --check → HOLD 판정보류 — 나이 297초 <= 임계 600초
  09:07 재확인 → HOLD 판정보류 — 나이 441초 <= 임계 600초
  09:10 재확인 → HOLD 판정보류 — 나이 594초 <= 임계 600초
  09:11:42 재확인 → STALE 스테일 확정 — 0바이트 · 0.2시간 · git 프로세스 0개 → 커밋 불가 상태
  09:11 --reclaim 시도 → STALE 회수 실패: [Errno 1] Operation not permitted
  rm -f .git/index.lock (09:00경 시도) → rm: cannot remove: Operation not permitted
  ```
- **기준 위반**: SKILL.md §0 「git 호출 규약」 자체는 준수했으나, 그 규약이 이 환경에서 100% 방어책이 되지 못함을 재확인 — `dev_memory/DECISION_LOG.md` 2026-09-21(609차) 항목 "4. 후속 — 이 점검 세션 자신이 `.git/index.lock`을 남겼다"와 **완전히 동일한 패턴의 재발**(전날에도 옵션을 다 붙였는데도 발생, STALE 확정 후 리클레임도 실패했었음).
- **영향**: 오늘 산출물(증거 다이제스트·이 리포트·dev_memory 갱신)을 **사용자가 Windows PC에서 직접 잠금을 지우기 전까지는 아무도 커밋할 수 없다.** 다만 매매 프로그램 자체와는 무관 — 실거래·모델·데이터 파이프라인에는 어떤 영향도 주지 않는다.
- **영향(기술)**: git 인덱스 잠금이 STALE로 확정된 채 남아 `git add`/`git commit`이 전부 실패하는 상태다. 이 세션(리눅스 샌드박스)은 `unlink` 권한이 없어 회수가 원천적으로 불가능하다 — Windows PC에서 사용자가 직접 지워야 한다(사용자 조치 1번).
- **신규 여부**: **재발**(같은 패턴이 최소 2026-09-16·09-17·09-18·09-21에도 발생 — `dev_memory/DECISION_LOG.md` 다수 항목). 이번이 처음이 아니므로 "--no-optional-locks를 붙여도 이 환경에서는 100% 방지되지 않는다"는 가설이 더 굳어졌다.

#### 1-2. `[SessionStateDrop]` — 날짜 전환 시 완료 마커 소실, 오늘도 재현 (F-1/538-4, 지속)

- **증상**: `data/session_state.json`의 `p8_last_success_date`·`eod_retrain_ok_date` 두 키가 오늘 파일에서 빠져 있다. 전일(2026-09-21) 15:53:28에 EOD 재학습과 P8(재학습 직후 스케일러 최종 조정) 모두 성공했고 그 시점 로그에 "session_state p8_last_success_date + eod_retrain_ok_date 기록 완료"까지 남아 있는데도, 날짜가 바뀌면서 그 기록이 새 딕셔너리에 이어지지 않았다.
- **근거**:
  ```
  logs/20260922_WARN.log:
  08:41:13 [WARNING] SYSTEM: [출처축]... (연관 없음, 별건)
  08:41:13 [WARNING] SessionStateDrop: 완료 마커 소실 ['p8_last_success_date', 'eod_retrain_ok_date']
    — 날짜 전환(2026-09-21 → 2026-09-22)이 새 딕셔너리를 만들면서 이어받지 않았다
    (호출부=session_recovery_service.py:increment_session)

  logs/retrain_eod_20260921.log:
  15:53:28,969 [INFO] EOD_RETRAIN: [P8] session_state p8_last_success_date + eod_retrain_ok_date 기록 완료

  logs/20260922_SYSTEM.log:
  08:55:13 [INFO] [PreRetrain] EOD 마커 파일 직접 확인 (1일 전: 2026-09-21) → PreRetrain 스킵
    (session_state 미기록 보완)
  ```
- **기준 위반**: 계측 4원칙 ④(폴백 가시화 — 미설정 상태를 조용히 정상처럼 두지 않기)의 취지에 정확히 해당하는 유형. `dev_memory/NEXT_TODO.md`에 F-1(538-4)로 이미 등록돼 승인 대기 중(2026-09-04 최초 등록, 이후 매 거래일 재현 — 정확한 영업일수는 이전 리포트의 영업일 계산 정정 기록 참고).
- **영향**: 실제 작업(EOD 재학습·P8)은 정상적으로 끝났고, 개장 전 재학습 스킵 로직이 "session_state 미기록 보완" 경로로 EOD 완료 마커 파일을 직접 확인해 정확히 판단했다 — **오늘 실질적인 기능 장애는 없다.** 다만 이 마커가 사라진 채로 남아 있으면, 이 마커에 의존하는 다른 미래 코드가 "재학습이 안 됐다"고 잘못 판단할 위험이 계속 쌓인다.
- **영향(기술)**: `session_recovery_service.py:increment_session()`이 날짜 롤오버 시 새 세션 딕셔너리를 만들면서 이전 날짜의 완료 마커 키를 이어받지 않는다. 현재는 `[PreRetrain]`이 이를 우회해 `eod_retrain_done_{d}.txt` 파일을 직접 읽는 방어 로직을 갖추고 있어 실질 영향이 상쇄되고 있으나, 이 파일을 참조하지 않는 다른 소비처가 생기면 즉시 문제가 될 수 있는 구조적 결함이다.
- **신규 여부**: 기존 TODO(F-1/538-4, 2026-09-04 최초 등록) — **재발(지속)**. 새 발견 아님.

### P2 — 운영 부담·기술부채

#### 1-3. `collect_evidence.py`의 `git diff` 호출 실패 — 어제에 이어 오늘도 재현

- **증상**: 증거 수집기 실행 결과 §2에 "실질 변경 **미측정**(git diff 실패)"로 판정 보류됐다. 어제(2026-09-21) 장전 점검에서 처음 발견돼 `NEXT_TODO.md`에 P2로 등록된 것과 동일한 증상이다.
- **근거**: `evidence_MW0601-20260922_pre.md` §2 — `HEAD 60d598c · 브랜치 v9-dev · 미커밋 731건 · 실질 변경 미측정(git diff 실패)`. 세션이 직접 `git --no-optional-locks diff --numstat -w`로 재확인한 결과 실질 변경은 3파일(`features/levels/levels_store.py` +69/-1, `features/levels/premarket_levels.py` +24/-7, `scripts/cybos_autologin.py` +33/-14)뿐이었고, 이는 2026-09-18(606-3)·2026-09-21(2번 항목)에 이미 "타 세션 작업 중"으로 등록된 바로 그 3파일과 정확히 일치한다.
- **기준 위반**: 없음(절대원칙 저촉 아님) — 수집기 자체의 안정성 문제.
- **영향**: 사람이 이 리포트를 읽을 때 "731건 변경"이라는 원시 숫자만 보면 실제보다 훨씬 큰 문제로 오인할 수 있다. 수동 재확인 절차가 계속 필요하다.
- **영향(기술)**: `collect_evidence.py`의 `git diff` 호출(인자·타임아웃 등 방식 미상)이 실패해 실질 변경분 파싱을 포기하고 있다. 이 실패 자체가 §1-1의 `.git/index.lock` 재발과 시간대가 겹칠 가능성이 있어(수집기 실행 08:59:39, 잠금 발견 09:00 이후) 서로 연관됐을 가능성도 배제할 수 없다 — 장후에 시간을 더 좁혀 대조할 필요가 있다.
- **신규 여부**: 기존 TODO(2026-09-21 등록, P2) — **재발(지속, 2일 연속)**.

#### 1-4. 미커밋 3파일 지속 — 변경 없음

- **증상**: `features/levels/levels_store.py`·`features/levels/premarket_levels.py`·`scripts/cybos_autologin.py` 3개 파일이 여러 날째 미커밋 상태로 남아 있다. 오늘도 내용 변경 없이 그대로다.
- **근거**: 1-3 항목의 `git diff --numstat -w` 결과, 09-18·09-21과 동일한 파일·동일한 변경량.
- **기준 위반**: 없음 — 다른 세션(작업 중)의 소관.
- **영향**: 실전 PC에 커밋 안 된 수정이 계속 쌓이고 있다는 뜻이나, 오늘 거래에는 영향 없음.
- **영향(기술)**: 해당 없음.
- **신규 여부**: 기존 TODO(606-3, 2026-09-18 등록) — **재발(지속)**.

### 확인 필요 (확정 아님)

- **`[Canary]` z경고 폭증(15개 ≥ 임계 12개) — 08:55:13 재적합 시도 → 재적합 후 2개로 회복, 그러나 08:59:13 `[CanaryShadow]` 재측정에서 다시 15개로 상승.** 이는 스케일러(피처 값을 정규화하는 장치)가 안정 상태인지 감시하는 O-p4 관측 사다리(최근 7거래일 값: 8/12/13/8/14/7/12)의 연속 관측치다. 오늘 15개는 이 사다리에서 관측된 값 중 새로운 최댓값이다. **아직 판정 대상이 아니다** — 5거래일 관찰 주기 중이며, 09:00 개장 이후 실제 진입 판단에 영향을 줬는지는 장중 점검에서 `[ConfFloorGuard]`·`ScalerFloor` 발동 빈도와 함께 재확인해야 판정 가능하다. `dev_memory/DECISION_LOG.md` "499-C-1. Canary / CanaryShadow z경고 기준선 불일치" 항목 및 O-p4 관측 이력 참고.

### 이미 반영된 사안 (신규 아님)

- **`CB_CONSEC_STOP_LIMIT = 3`** — 2026-09-02(519차) 사용자 지시로 이미 복원 완료. 절대원칙 ② 문구("3연속")와 일치. 다만 실전 전환 기준 ⑤는 "복원 후 정상 발동 1회 관측"이 아직 남아 있음(`dev_memory/NEXT_TODO.md`에 계속 등록 중 — 새 안건 아님, 인위적으로 발동을 만들지 말 것).
- **`CB3_P4_GRADE_BLOCK_ENABLED=False`·`FP_CRITICAL_GRADE_BLOCK_ENABLED=False`·`TOXICITY_SEVERE_SPREAD_BLOCK_ENABLED=False`** — 전부 `references/invariants.md` §2에 등록된 한시예외와 일치. 재검토 기한이 별도로 도래하지 않았음.
- **`joblib=1.1.0`** (CLAUDE.md 문서상 "1.1.1") — 2026-09-21(609차)에 이미 "문서 정정만 필요, 운영 영향 없음, 우선순위 낮음"으로 등록된 사안. 오늘도 동일 버전 확인, 새 발견 아님.
- **30m 호라이즌 채점·CB③ 입력 배선** — `main.py:6845` 부근에서 `if (v["horizon"] == "30m"` 형태로 여전히 CB③에 정확도를 공급 중임을 확인. CLAUDE.md 절대원칙 §3이 적어둔 행 번호(`5539`)와는 다르지만, 이는 코드가 계속 늘어나며 행 번호가 밀린 것뿐 — 483차 정정 문구가 이미 예견한 상황이다. 기능 자체는 정상.
- **`[출처축] PHANTOM_STATE_ARTIFACT`(08:41:13, 2건)** — 554차에 "테스트가 심은 상태파일 흔적, 거래 아님"으로 이미 분류된 라벨. 새 안건 아님.
- **`[CybosProbe] dispatch/request failed`(08:58, 10건)** — 기존 등록 F-3(`NEXT_TODO.md`, P2). 여러 Cybos 내부 인터페이스를 순차 시도하다 실패하는 정상적인 프로브 동작이며, 최종적으로 성공하는 경로가 있어 기능에 지장 없음. 새 안건 아님.

**이 절이 발급한 번호**: 이상점 1-1~1-4 (관측 번호 신규 발급 없음)

---

## 2. Fix 작업 구현계획

> 장전 국면이므로 **본 계획은 수립만 하고 적용하지 않는다.** 라이브 프로세스가 돌고 있다.

### F-1. `.git/index.lock` 반복 재발 원인 규명 — P2 · 대응 이상점 1-1

- **원인 가설**: (a) 코웍 리눅스 샌드박스가 Windows 호스트 폴더를 마운트하는 방식 자체의 파일시스템 동기화 지연/경합 (b) 같은 세션 내 여러 bash 호출이 병렬은 아니지만 각기 독립 프로세스로 실행되며 git 내부 stat 캐시 갱신 타이밍이 겹칠 가능성 (c) 마운트 계층에서 다른 프로세스(예: 파일 인덱싱 서비스)가 `.git` 디렉터리를 건드릴 가능성. 현재로선 전부 가설이며 근거로 확정된 것은 없다.
- **변경 파일**: 없음(조사 항목이지 코드 수정 항목이 아님).
- **회귀 위험**: 없음(조사만).
- **절대원칙 저촉 여부**: 해당 없음.
- **런타임 주의**: 해당 없음 — 세션 도구 문제이지 py37_32/py310_64 코드 문제 아님.
- **검증 방법**: 다음 며칠간 점검 세션들이 잠금 발생 직전·직후에 실행한 정확한 명령 목록과 타임스탬프를 dev_memory에 초 단위로 남겨, 패턴(특정 명령 조합·특정 시간대)이 있는지 누적 대조한다.
- **섀도 계측 필요 여부**: 아니오.
- **적용 시점**: 조사는 즉시(다음 점검부터) 가능, 코드 변경 없음.
- **결정 필요 사항**: 이 문제가 5회 이상 반복돼도 원인이 안 잡히면, "이 환경에서는 원천 차단이 불가능하다"고 SKILL.md §0에 명시하고 "판정보류 → 세션 종료 시 재확인 → 필요 시 사용자 회수" 절차를 표준 대응으로 굳히는 편이 나을 수 있다 — 사용자 결정 필요.

### 적용 순서와 커밋 계획

이 세션은 `.git/index.lock`이 해소되기 전에는 커밋할 수 없다(1-1). 아래 "사용자 조치"에서 최종 상태를 확인한다.

> 커밋 대기: `docs/정기점검/매일점검/MW0601-20260922-점검리포트.md`(신규), `docs/정기점검/매일점검/evidence_MW0601-20260922_pre.md`(신규), `dev_memory/DECISION_LOG.md`(append), `dev_memory/NEXT_TODO.md`(append)

---

## 3. 고도화 방안

당일 관측에서 출발한 것만 쓴다.

### G-1. Canary/CanaryShadow z경고 재상승 패턴의 원인 조사 — 우선순위: 26주 WFA 주기 또는 여유 있을 때

- **관측 근거**: 오늘 08:55 재적합 직후 z경고피처가 2개(건강)로 떨어졌다가, 불과 4분 뒤인 08:59 재측정에서 다시 15개로 튀어 올랐다(O-p4 관측 사다리 사상 최고치). 재적합 직후 안정성이 몇 분을 못 간다는 뜻일 수 있다.
- **제안 내용**: `[CanaryShadow]`가 "재측정만, 재적합 없음"으로 설계돼 있어 08:59 시점에는 재보정이 일부러 실행되지 않는다. 이 설계 의도(개장 1분 전 스케일러를 바꾸면 위험하다는 판단)는 합리적이나, 재적합 직후 4분 만에 다시 임계를 넘는 피처가 급증하는 패턴이 반복되는지 최근 며칠 값(8/12/13/8/14/7/12/**15**)의 추세를 확인해볼 가치가 있다.
- **기대 효과**: 재적합 주기나 임계값(현재 12개) 자체를 재검토할 근거 자료 확보.
- **비용·위험**: 낮음 — 관측 데이터 누적만 필요, 코드 변경 없음.
- **선행 조건**: O-p4 관측 사다리가 사전등록한 5거래일 관찰 완료(313차 원칙 — 표본 부족 상태에서 결론 내지 않음).
- **우선순위 제안**: 관측 지속(다음 26주 WFA 재검증 항목 후보로 등록 검토, 지금은 급하지 않음).

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| (없음) | — | — | 오늘 관측만으로 로드맵 변경을 제안할 근거 없음 |

---

## 4. 진입 승패 사후검증 (장후 한정)

해당 없음(장후 전용) — 장전 시점이라 아직 진입 자체가 없다.

## 5. 수익률 향상방안 (장후 한정)

해당 없음(장후 전용).

---

## 6. 다음 거래일 관측 예정

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| O-p1(09-22) | `[HealthPolicy] Degraded 선제차단` 09:01:00(개장버스트, cause=S5) 발생 빈도 | 개장구간 한정·소수면 정상, 장중 지속·자동진입 실차단이면 P1 격상 | 오늘 장중·장후 |
| O-p2(09-22) | `[Canary]`/`[CanaryShadow]` z경고피처 개수 추이(오늘 15개 — 사상 최고치) | O-p4 관측 사다리 5거래일 표본 도달 시 판정 | 5거래일 후 (313차 원칙 — 표본 부족 상태 결론 금지) |
| O-p3(09-22) | `.git/index.lock` 최종 상태(HOLD→STALE 여부, 자연 해소 여부) | 이 리포트 작성 종료 시점까지 재확인, 필요 시 사용자 조치 등재 | 오늘(세션 종료 시) |
| O-p4(09-22) | `collect_evidence.py`의 `git diff` 실패와 `.git/index.lock` 생성 시각 간 상관 | 두 사건의 정확한 초 단위 순서를 다음 세션에서 재현 확인 | 다음 점검 |

## 7. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| CB② **발동 1회 관측** (값 복원은 2026-09-02 완료, `=3`) | 상시 관측 | — | 인위적으로 만들지 말 것. 오늘도 발동 없음(매매 자체가 아직 없어 관측 기회 없음) |

---

## 8. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-09-22 (MW0601 617차 — 장전 점검)` (아래 실제로 append)
- `NEXT_TODO.md` 추가 체크박스: `.git/index.lock` 재발 조사(F-1), O-p1~O-p4 관측 항목 4건
- 완료 처리한 기존 항목: 없음(전부 지속 관측 중)

---

## 세션 종료 시 `.git/index.lock` 최종 확인

09:11:42 최종 재확인 — **STALE 확정**(`git_lock_guard.py --check` → 0바이트·0.2시간·git 프로세스 0개, exit=2). `--reclaim` 시도 → `Operation not permitted`로 회수 실패. 이 세션(리눅스 샌드박스)은 `unlink` 권한이 없어 더 이상 손댈 수 없다 — 아래 사용자 조치 1번이 유일한 해소 경로다.

## 사용자 조치

| # | 할 일 | 명령·경로 | 기한 |
|---|---|---|---|
| 1 | `.git/index.lock` 회수 (STALE 확정, 이 세션은 회수 권한 없음) | Windows PC에서 `python scripts\git_lock_guard.py --reclaim` (실패 시 `.git\index.lock` 수동 삭제 — 단, 다른 git 작업이 실제로 진행 중이 아닌지 먼저 확인) | 다음 기동 전 |
| 2 | 이 리포트·증거 파일·dev_memory 변경분 커밋 (위 1번 해소 후) | 경로 명시해 `git add`: `docs/정기점검/매일점검/MW0601-20260922-*.md`, `dev_memory/DECISION_LOG.md`, `dev_memory/NEXT_TODO.md` (⚠ `git add .` 금지 — CRLF/LF로 수백 건 오탐) | 편할 때 |
| 3 | `features/levels/levels_store.py`·`premarket_levels.py`·`scripts/cybos_autologin.py` 3파일 미커밋 상태 검토 | 다른 작업 세션 소관 — 내용 검토 후 커밋 또는 폐기 결정 | 편할 때 |

**이 절이 발급한 번호**: 이상점 1-1~1-4 · 관측 O-p1~O-p4 (전부 이 절에서 처음 발급 — 오늘 첫 파일이므로 이월 대상 없음)

---

# 장중(intra)

- 점검 시각: 12:34~12:37 KST · PC `MW0601` (실행 환경: 코웍 리눅스 샌드박스, 호스트명 `claude` → `--pc MW0601` 수동 지정)
- HEAD `39d4af5` · 브랜치 `v9-dev`(정상) · 미커밋 740건(원시, git diff 실패로 실질 변경 미측정 — 1-3 지속) · **당일 커밋 5건**(장전 이후 2건 추가 — 아래 병행 세션 참조)
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260922_intra.md`

## 0i-C. 이월 처리 — 장전 항목 처분

**병행 세션 확인 결과 먼저 — 이 처분표에 직접 영향을 준다.** 이 점검 세션과 무관한 별도 작업 세션이 오늘 09:29~12:35 사이 코드를 3차례 커밋(`f536354`·`c69e061`·`bad6454`)했고, 그중 **두 건(`e3e5d91`·`39d4af5`)은 이 점검 세션이 증거를 모으고 있던 바로 그 시각(12:34:02·12:34:33)에 올라왔다** — `dev_memory/DECISION_LOG.md`의 "## 2026-09-22 (MW0601 617차 후속 — 표시 위젯 한 줄이 엔진을 죽였다)" 항목이 그 내용이다. 결론 한 줄: 오늘 오전 있었던 프로그램 재시작(아래 1-5)의 근본 원인을 그 세션이 이미 찾아 고쳤고, 회귀 테스트까지 갖춰 커밋했다 — 다만 **지금 돌고 있는 실제 매매 프로그램에는 아직 반영되지 않았다**(재기동해야 반영된다). 이 사실이 오늘 장중 절 전체의 뼈대를 바꾼다 — 1-5를 참조.

| 원 번호 | 제목(줄임) | 원 국면 | 처분 | 근거 |
|---|---|---|---|---|
| 1-1 | `.git/index.lock` STALE 확정·회수 실패 | 장전 | 🔄 지속(재발 형태 변경) | 장중 12:35경 새 잠금이 또 생겼다(0바이트, 현재 HOLD 판정보류) — 장전 것과는 별개 발생이라 아래 1-6으로 새로 등록하고 이쪽은 "패턴 지속"으로만 남긴다 |
| 1-2 | `SessionStateDrop` 완료 마커 소실 | 장전 | 🔄 지속(원인 미해결) | 장중 로그(`20260922_WARN.log`) 재확인 — 추가 발생 0건. 이 결함은 "날짜 전환 시점"에서만 발동하는 구조라 장중에는 애초에 재현 조건이 없다. 근본 원인(F-1/538-4)은 그대로 미해결 |
| 1-3 | `collect_evidence.py` git diff 실패 | 장전 | 🔄 지속(3일 연속 — 09-21·09-22 장전·09-22 장중) | 장중 수집(`evidence_..._intra.md` §2)에서도 동일하게 "실질 변경 미측정" — 재현 계속 |
| 1-4 | 미커밋 3파일 지속 | 장전 | 🔄 지속(내용 변경 없음) | 오늘 새로 커밋된 2건(617차 후속)은 이 3파일과 무관한 별개 파일(대시보드 코드) — 3파일 자체는 그대로 미커밋 상태 |
| 확인필요 | `[Canary]` z경고 15개(사상 최고치) | 장전 | 🔄 지속(313차 원칙 — 표본 부족, 판정 보류) → **O-p2로 계승** | 다음 재적합·재측정은 장 마감 후이므로 장중에는 추가 관측치 없음. 5거래일 표본 도달까지 계속 이월 |
| O-p1 | `[HealthPolicy] Degraded 선제차단` 빈도 | 장전 | ✅ **판정 완료 — 정상** | 장중 4건 관측: 09:01(개장버스트, cause=S5) · 09:17(cause=S1) · 09:45×2(cause=S0, **크래시 재기동 직후 모델 재로드 지연** — 아래 1-5와 동일 사건). 전부 개장구간 또는 재기동 직후에 국한되고 자동진입이 실제로 막힌 근거 없음 → 원 판정 기준대로 정상 |
| O-p2 | `[Canary]`/`[CanaryShadow]` z경고 5거래일 관측 사다리 | 장전 | 🔄 판정 보류(313차 원칙 — 표본 미도달) — **다음 거래일로 이월** | 오늘 15개(사상 최고치) 기록은 유지. 장중 추가 재측정 없음(재적합은 장전 1회) |
| O-p3 | `.git/index.lock` 최종 상태 | 장전 | 🔄 판정 보류(재확인 필요) — **세션 종료 시 재판정** | 장전 종료 시점엔 STALE 확정이었으나, 장중 재확인 결과 **새 잠금이 다시 생성**돼 있다(0바이트, HOLD). 이번 장중 세션 종료 직전 최종 상태를 아래 "세션 종료 시 확인"에 기록 |
| O-p4 | `git diff` 실패와 `.git/index.lock` 생성 시각 상관 | 장전 | 🔄 판정 보류 — **다음 점검으로 이월** | 오늘도 재현됐으나(1-3), 이번엔 잠금 생성 시각이 **병행 세션의 커밋 시각(12:34:33)과 더 가깝게 겹쳐** 원인이 이 점검 세션의 명령이 아니라 병행 세션의 git 동작일 가능성이 새로 떠올랐다. 초 단위 인과관계는 여전히 미확정 — 다음 점검에서 계속 좁힐 것 |

**미처분 0건.**

---

## 0i. 오늘 한눈에 (장중 갱신)

오늘 오전 큰 사건이 하나 있었습니다. 09시 42분경 화면 표시를 담당하는 프로그램 일부(작은 방향 표시 창)가 내부 그림 그리기 오류로 멈춰버렸고, 그 여파로 매매 프로그램 전체가 한 번 죽었다가 자동으로 다시 켜졌습니다. 다행히 그 순간에는 보유 중인 포지션이 없었고, 다시 켜지는 데 걸린 시간도 1분이 채 안 됐습니다 — 자동 재시작 장치가 설계대로 작동한 결과입니다. 이후 몇 분간 프로그램 상태가 "주의" 단계로 표시됐지만 4~5분 만에 정상으로 돌아왔습니다.

더 다행인 것은, 이 문제와 무관한 다른 작업(대시보드 화면 개선 작업)을 하던 팀이 마침 오늘 이 사고의 정확한 원인을 찾아내 고치는 작업을 이 점검이 진행되던 바로 그 시각에 끝냈다는 점입니다. 다만 그 수정은 지금 실제로 돌고 있는 프로그램에는 아직 반영되지 않았습니다 — 프로그램을 다시 켜야 반영됩니다. 즉 지금 이 순간에도 같은 원인으로 한 번 더 멈출 가능성 자체는 남아 있습니다(포지션을 들고 있을 때 이런 일이 생기면 더 위험할 수 있습니다). 언제 다시 켤지는 매매 담당자가 정할 일이라 이 점검에서는 그 판단 재료만 정리해 사용자 조치에 올려둡니다.

그 외에는 오늘 오전 예정대로 진입 1건·청산 1건이 있었고 결과는 이익(+23,852원)이었습니다. 저장소 파일 잠금 문제(장전부터 있던 것)는 장중에도 또 한 번 나타나 여전히 사용자가 손으로 풀어줘야 하는 상태입니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 조건부 정상 | (장전 절 참조 — 본문 변경 없음) |
| 장중 | **조건부 정상** | P0 없음·실손해 없음. 다만 09:42 프로세스 크래시+자동재시작 1회(원인 규명·수정 완료, 배포 대기 — 1-5), `.git/index.lock` 재발(1-6) 관측 |
| 장후 | (장후 점검에서 채움) | |

**절대원칙 준수 여부 (장중 갱신)**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 | 아직 해당 없음 | 15:10 전. 장후에서 최종 판정 |
| ② | Circuit Breaker 5종 | 준수 | 오늘 발동 이력 없음(①②③④⑤ 전부 미발동). `[CB⑤]` 태그 8건은 파이프라인 1,000ms 사전경고 로그일 뿐 — 실제 정지 임계(5,000ms)는 넘지 않음 |
| ③ | CORE 피처 호라이즌 그룹별 | 준수 | 오늘 유일한 진입(11:02) 체크리스트에서 VWAP는 통과(✅) — 강제 X등급 사유 없음. 미통과는 `cvd`·`fore`뿐(등급 하락 대상, 위반 아님) |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 준수 | `0xC0000409` 관련 로그 0건. 오늘 크래시(1-5)는 COM 콜백이 아니라 **matplotlib 그리기 함수(`axhline`)** 에서 난 별개 원인 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 확인 대상 아님 | 이상 없음 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 준수 | 관련 흔적 없음 |

**설정 불변식** — 수집기 §3 전 항목 `일치`(24종). 차단 게이트 35개 중 9개 의도적 비활성 — 전부 `references/invariants.md` 기대값과 일치. 이상 없음.

---

## 1i. 이상점 정리보고 (장중 신규)

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**없음.**

### P1 — 정확성·관측 훼손

#### 1-5. 화면 표시 코드의 그리기 오류로 매매 프로그램 전체가 한 번 죽었다가 자동 재시작됨 — 포지션 없어 실손해 없음, 원인은 이미 규명·수정됐으나 지금 도는 프로그램엔 미반영

- **증상**: 09:41:56경 방향 표시 보조창(`dashboard/panels/direction_indicator_dialog.py`)의 그림 그리기 함수가 "그릴 좌표축의 폭이 0"인 상태에서 기준선(`axhline`)을 그리려다 `numpy.linalg.LinAlgError: Singular matrix`를 던졌고, 이 예외가 Qt 타이머 콜백 안에서 처리되지 않아 PyQt5가 프로세스를 강제 종료(`qFatal`)시켰다. `main.py`가 62분(08:40 기동 이후) 만에 죽자 런처의 자동재시작 장치가 10초 뒤 새 프로세스를 띄웠다.
- **근거**:
  ```
  logs/Mireuk_batch/launcher_20260922_084001_14358.log:
  09:41:56 [WARNING] [ChartDBG] paintEvent slow 94.0ms ... (크래시 직전 마지막 정상 로그)
  Traceback (most recent call last):
    File ".../dashboard/panels/direction_indicator_dialog.py", line 292, in _refresh
    File ".../dashboard/panels/direction_indicator_dialog.py", line 335, in _apply
    File ".../dashboard/panels/direction_indicator_dialog.py", line 490, in _draw_chart
      linestyle=":", alpha=0.35, zorder=0,   (axhline 호출)
    ...
    numpy.linalg.LinAlgError: Singular matrix
  [AUTO-RESTART] main.py 가 62분 실행됨 -- 일시적 크래시, 카운터 초기화.
  [AUTO-RESTART] #1 시도 (시각=0942) -- 10초 후 재시작...

  logs/20260922_SYSTEM.log:
  09:43:11 [INFO] [Session] 재기동 #1 | cause=STARTUP
  09:43:15 [WARNING] [RESTART] 장중 재시작 감지 09:43 — GapOffset=복원됨  pre_market_scaler=False
  09:44:04 [WARNING] HEALTH: level=WARNING degraded=OFF latency=3948ms exc_tags=[LEVELS]×1 [RESTART]×1
  09:45:01 [WARNING] HEALTH: level=WARNING degraded=ON  latency=1827ms exc_tags=[LEVELS]×1 [RESTART]×1
  09:46:00 [INFO]    HEALTH: level=INFO    degraded=ON  latency=426ms  (회복)
  ```
- **기준 위반**: 절대원칙 ④(COM 콜백 dynamicCall·emit 금지)는 **저촉 아님** — 이번 크래시는 COM 콜백이 아니라 Qt 타이머 슬롯 안 matplotlib 호출에서 났다. 다만 계측 4원칙 ④(폴백 가시화)와 같은 계열 — 화면 표시 슬롯의 예외를 아무도 잡지 않아 "표시 문제"가 "엔진 전체 정지"로 번졌다.
- **영향**: 크래시 시점(09:41:56)에는 보유 포지션이 없었다(오늘 첫 진입은 11:02) — **직접 금전 손실은 0원**. 자동재시작이 10초~1분 내로 정상 복구했고, 매분 파이프라인도 3분 이상 끊긴 구간이 없어(§7 커버리지 확인) 기회비용도 거의 없다. 다만 **같은 결함이 포지션을 들고 있는 순간에 재발하면** 청산 판단 로직이 몇십 초~1분간 멈추는 위험으로 이어질 수 있다 — 지금 이 순간에도 그 가능성이 남아 있다(아래 참조).
- **영향(기술)**: 원인·조치·회귀 테스트까지 이 점검과 무관한 별도 세션이 **오늘 12:34경(이 점검 세션이 증거를 모으던 바로 그 시각) 이미 완료**했다 — `dev_memory/DECISION_LOG.md` "## 2026-09-22 (MW0601 617차 후속 — 표시 위젯 한 줄이 엔진을 죽였다)" 전문 참조, 커밋 `e3e5d91`(조치)·`39d4af5`(기록). 요지: 캔버스 최소폭 설정 누락으로 창을 최대화/복원하는 과정에서 그림 영역 폭이 실제로 0이 될 수 있었고(09:34~09:40 창 조작과 시간대 일치), 그 상태에서 `axhline`만 예외를 던지는 조건을 실측 확인했다. 조치는 3중 방어(치명화 차단·원인 제거·그리기 가드)로 적용됐고 회귀 테스트(`tests/test_617_dashboard_chart_zero_width.py`) 15건까지 갖췄다. 단, `dev_memory` 기록이 스스로 명시하듯 **"실행 중인 프로세스에는 재기동 전까지 반영되지 않는다"** — 09:43:11에 뜬 지금의 라이브 프로세스는 이 수정 이전 코드로 돌고 있다.
- **신규 여부**: 신규(이 점검 세션 기준 최초 보고). 단 원인 규명·수정 자체는 병행 세션이 이미 완료한 사안이므로 **이 점검은 새 Fix를 제안하지 않는다** — 아래 §2i F-2는 "언제 재기동해 반영할 것인가"라는 운영 판단만 사용자에게 넘긴다.

### P2 — 운영 부담·기술부채

#### 1-6. `.git/index.lock`이 장중에도 새로 생성됨 — 1-1과 같은 패턴이나 병행 세션의 커밋과 시간이 겹쳐 원인 후보가 하나 더 늘었다

- **증상**: 장전 종료 시점(09:11:42)에 STALE로 확정됐던 잠금과는 별개로, 이 점검 세션의 증거 수집이 끝난(12:34:21, 자가점검 "락 없음" 확인) 직후인 **12:35경 새 `.git/index.lock`(0바이트)이 다시 생겼다.**
- **근거**:
  ```
  evidence_MW0601-20260922_intra.md §2 (12:34:21 시점): "인덱스락 없음 — 이 수집 실행은 락을 만들지 않았다"
  병행 세션 커밋 39d4af5 — 2026-09-22 12:34:33 +0900
  12:36:34 재확인: .git/index.lock 존재, mtime 12:35, 0바이트
  git_lock_guard.py --check → "HOLD 판정보류 — 나이 199초 <= 임계 600초(아직 실행 중일 수 있다)" exit=3
  ```
- **기준 위반**: SKILL.md §0 「git 호출 규약」— 이 세션은 모든 git 명령에 `--no-optional-locks`를 붙였음에도(1-1과 동일한 재발 패턴) 잠금이 또 생겼다. 이번엔 **생성 시각이 이 세션의 명령보다 병행 세션의 커밋 시각(12:34:33)에 더 가깝다** — 원인이 이 점검 세션이 아니라 병행 세션(또는 두 세션의 git 접근이 겹친 것)일 가능성이 새로 생겼다(O-p4 갱신, 위 이월표 참조).
- **영향**: 현재 **판정보류(HOLD)** 상태다 — SKILL.md 규약상 "판정보류면 절대 지우지 말 것"이므로 이 세션은 아무 조치도 취하지 않는다. STALE로 확정될 경우 장전과 마찬가지로 사용자가 Windows PC에서 직접 회수해야 하며, 그때까지 오늘 산출물(이 리포트 포함) 커밋이 막힌다.
- **영향(기술)**: 원인이 이 세션 단독이 아니라 **두 세션(이 점검 세션 + 병행 대시보드 작업 세션)이 거의 동시에 같은 저장소에 git 명령을 실행**했을 가능성이 이번에 처음 제기됐다. 기존 F-1(원인 규명 조사)의 가설 목록에 "동시 다중 세션 git 접근"을 추가할 것.
- **신규 여부**: 재발(1-1과 동일 패턴, 원 사건과는 별개 발생) — 새 이상점 번호로 등록.

### 확인 필요 (확정 아님)

- 위 이월표의 O-p2(Canary z경고 5거래일 관측)가 그대로 유지된다 — 313차 원칙, 표본 미도달.

### 이미 반영된 사안 (신규 아님)

- **오늘 진입 1건(11:02 LONG 2계약)의 최종 청산이 로그상 "하드스톱"으로 찍혀 있으나 실제로는 TP1 부분청산(33%, +16,926원) 이후 남은 1계약이 이익 상태(+6,926원)로 정리된 것이다** — CLAUDE.md/`postmortem.md`가 이미 경고한 "하드스톱에 TP1 보호 이익 청산이 섞여 있다"는 바로 그 패턴. 새 결함 아님, 다만 장후 승패 집계 시 이 건을 "손절"로 세지 않도록 표시해 둔다(포지션 합계는 +0.92pt / +23,852원으로 이미 이익 확정).
- **사이저 출력(최대 3계약) vs 실제 진입(최대 2계약)** — `sizing_inversion_watch`([28] 채널, 실전 전환 기준 ⑧ 관련) 기존 등록 사안. 오늘 표본 1건 추가, 313차 원칙상 확정 결론 여전히 불가.
- **`[CB⑤]` 태그 8건(파이프라인 1,000ms 경고)** — 이것은 사전 경고 로그이지 실제 CB⑤ 정지(5,000ms 임계)가 아니다. 착시 방지 차원에서 명시.

**이 절이 발급한 번호**: 이상점 1-5~1-6 · 관측 O-i1~O-i2 (아래 §6i에서 발급)

---

## 2i. Fix 작업 구현계획 (장중 신규)

> 장중 국면이므로 **본 계획은 수립만 하고 적용하지 않는다.** 라이브 프로세스가 돌고 있다.

### F-2. 크래시 원인 수정을 언제 라이브에 반영할 것인가 — P1 · 대응 이상점 1-5

- **원인 가설**: 해당 없음 — 원인 규명·코드 수정은 병행 세션(617차 후속)이 완료했다. 이 항목은 "무엇을 고칠까"가 아니라 "언제 반영할까"라는 **운영 판단**만 다룬다.
- **변경 파일**: 없음(이미 커밋됨 — `dashboard/panels/direction_indicator_dialog.py` 외, 커밋 `e3e5d91` 참조).
- **회귀 위험**: 재기동 자체의 위험(포지션 보유 중 재기동 시 상태 복원 실패 가능성) — 재기동은 반드시 **포지션이 FLAT일 때**(현재 FLAT 확인됨) 진행해야 한다.
- **절대원칙 저촉 여부**: 없음. 오히려 저촉 위험(재발 시 청산 로직 정지)을 줄이는 방향.
- **런타임 주의**: py37_32 라이브 프로세스 재기동 — py310_64 재학습과 무관.
- **검증 방법**: 재기동 후 `tests/test_617_dashboard_chart_zero_width.py` 통과 여부는 이미 확인됐으므로, 라이브에서는 창 최대화/복원을 실제로 재현해 크래시가 재발하지 않는지 육안 확인.
- **섀도 계측 필요 여부**: 아니오(이미 라이브 검증 완료 항목이 아니라 배포 대기 항목).
- **적용 시점**: **사용자 판단 필요** — 옵션 (a) 지금(장중, 포지션 FLAT인 지금이 위험이 가장 낮은 재기동 시점) (b) 15:10 강제청산 이후 자연 재기동 시점까지 대기. (a)는 SKILL.md의 "장중 코드 배포·재기동 금지"를 이 점검 세션이 실행하는 것이 아니라 **매매 결정자인 사용자가 판단**하는 것이므로 규약 위반이 아니다(2026-09-21 1-5 선례와 동일 성격).
- **결정 필요 사항**: 위 (a)/(b) 중 선택 — 사용자 조치 절에 옵션으로 제시.

### 적용 순서와 커밋 계획

이 세션은 `.git/index.lock`이 HOLD/STALE인 동안 커밋할 수 없다(1-6). 세션 종료 시 최종 상태를 아래에 기록한다.

> 커밋 대기: `docs/정기점검/매일점검/MW0601-20260922-점검리포트.md`(장중 절 추가), `docs/정기점검/매일점검/evidence_MW0601-20260922_intra.md`(신규), `dev_memory/DECISION_LOG.md`(append), `dev_memory/NEXT_TODO.md`(append)

---

## 3i. 고도화 방안 (장중 신규)

### G-2. Qt 타이머 슬롯의 무가드 예외가 표시 문제를 엔진 정지로 번지게 하는 구조 — 우선순위: 이번 주(병행 세션이 이미 스캔 완료, 결정만 남음)

- **관측 근거**: 오늘 1-5가 실측으로 보여준 것 — 화면 표시 코드 한 줄의 예외가 매매 엔진 전체를 죽였다. 병행 세션(617차 후속)의 전수 스캔(`scripts/audit_qtimer_slot_guards.py`)에서 Qt 타이머 슬롯 36곳 중 무가드 18곳을 찾았고, 그중 **3곳은 `main.py:TradingSystem`의 엔진 슬롯**(`_on_main_heartbeat`·`_effect_report_timer_tick`·`_check_limit_entry_timeout`)이다.
- **제안 내용**: 표시 슬롯 15곳은 손대는 김에 가드를 추가하면 되지만(이미 계획됨), **엔진 슬롯 3곳은 다른 문제다** — 예외를 그냥 삼키면 "주문·청산 실패가 조용히 사라지는" 계측 4원칙 위반 사고가 재현된다. 이 셋은 "잡아서 크게 알리고 안전상태로 전이"하는 별도 설계가 필요하다는 것이 병행 세션의 판단이며, 이 점검도 동의한다.
- **기대 효과**: 엔진 핵심 타이머가 예외로 죽을 때 "조용히 사라짐"이 아니라 "즉시 알림 + 안전 정지"로 바뀐다.
- **비용·위험**: 중간 — 설계 결정이 필요(주간회의 안건으로 이미 등록됨, `dev_memory/NEXT_TODO.md` "엔진 슬롯 3건 처분 결정(주간회의)").
- **선행 조건**: 없음(독립적으로 진행 가능).
- **우선순위 제안**: 다음 주간회의 안건 — 이미 등록돼 있으므로 이 점검은 중복 등록하지 않는다.

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| (없음) | — | — | 오늘 장중 관측만으로 로드맵 변경을 제안할 근거 없음 |

---

## 4i. 진입 승패 사후검증 (장후 한정)

해당 없음(장후 전용).

## 5i. 수익률 향상방안 (장후 한정)

해당 없음(장후 전용).

---

## 6i. 다음 거래일 관측 예정 / 오늘 장후 판정 예정

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| O-i1 | 크래시 수정(617차 후속, 커밋 `e3e5d91`)의 라이브 반영 여부 | 다음 재기동(사용자 판단) 후 `[Session] 재기동` 로그의 직후 구간에서 동일 크래시 미재현 확인 | 재기동 시점(오늘 장중 또는 15:10 이후) — 장후 점검에서 반영 여부 확인 |
| O-i2 | `sizing_inversion_watch`([28]) qty≥3 표본 축적 | `references/invariants.md` §4 313차 원칙 — min_samples 도달 시 판정 | 26주 WFA 주기 (기존 등록, 표본만 추가) |
| O-p2(계승) | Canary z경고 5거래일 사다리 | 5거래일 표본 도달 시 판정 | 5거래일 후 |
| O-p3(계승) | `.git/index.lock` 최종 상태 | 이 장중 세션 종료 시점 재확인 | 아래 "세션 종료 시 확인" |
| O-p4(계승) | git diff 실패-잠금 생성 시각 상관 | 다음 점검에서 초 단위 재현 확인, 이번엔 병행 세션 가능성도 함께 검토 | 다음 점검 |

## 7i. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| CB② 발동 1회 관측 | 상시 관측 | — | 오늘도 미발동(정상 — 인위적으로 만들지 말 것) |

### 참고 — 매분 루프 커버리지 58.2%는 이상이 아니다

수집기 §11이 "매분 루프 커버리지 216/371분(58.2%)"과 "12:36~15:10 연속 155분 공백"을 자동 적신호로 띄웠다. **이것은 결함이 아니라 이 점검을 12:34~12:37(장중 진행 중)에 실행했기 때문에 생기는 당연한 결과다** — 로그가 "지금"까지밖에 없는데 판정 기준을 15:10까지로 잡았으니, 아직 오지 않은 미래 구간이 전부 "공백"으로 잡힌 것뿐이다(경과 비율 215/371≈57.9% ≈ 실제 커버리지 58.2%로 거의 일치 — 우연이 아니라 그 증거다). 09:00~12:35 구간 자체에는 3분 이상 끊긴 곳이 없다(§7 앵커 표 확인). **실제 하루 전체 커버리지 판정은 장후 점검에서 다시 본다.**

---

## 8i. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-09-22 (MW0601 617차 — 장중 점검)` (아래 실제로 append)
- `NEXT_TODO.md` 추가 체크박스: 1-5(크래시 재기동, 정보성 — 이미 병행 세션이 조치 완료), 1-6(git lock 재발, F-1 가설에 "동시 세션" 추가), O-i1·O-i2 관측 항목
- 완료 처리한 기존 항목: O-p1(HealthPolicy 선제차단 빈도 — 정상 판정 완료)

---

## 세션 종료 시 `.git/index.lock` 최종 확인 (장중)

12:36:34 1차 재확인 — HOLD 판정보류(나이 199초). 12:43:48 2차 재확인 — 여전히 HOLD(나이 512초). **12:45:34 최종 재확인 — STALE 스테일 확정**(0바이트·0.2시간·git 프로세스 0개, exit=2). `--reclaim` 시도 → `Operation not permitted`로 회수 실패(장전과 동일 패턴 — 이 세션은 `unlink` 권한이 없다). 아래 사용자 조치 5번이 유일한 해소 경로다.

## 사용자 조치 (장중 갱신 — 장전 1~3번에 이어)

| # | 할 일 | 명령·경로 | 기한 |
|---|---|---|---|
| 4 | **[운영 판단 필요]** 오늘 오전 크래시 원인 수정(커밋 `e3e5d91`)을 지금(포지션 FLAT) 재기동해 반영할지, 15:10 이후로 미룰지 결정 | 지금 반영 시 재기동 위험은 낮음(포지션 없음)이나 장중 재기동 자체가 부담이면 15:10 이후로 미뤄도 됨 — 단 그때까지는 같은 크래시가 한 번 더 날 위험이 남아 있음 | 사용자 판단, 급하지 않으면 15:10 이후 |
| 5 | `.git/index.lock` 회수 (STALE 확정, 이 세션은 회수 권한 없음) | Windows PC에서 `python scripts\git_lock_guard.py --reclaim` (실패 시 `.git\index.lock` 수동 삭제 — 단, 다른 git 작업이 실제로 진행 중이 아닌지 먼저 확인) | 다음 기동 전 |
| 6 | 이 리포트·증거 파일·dev_memory 변경분 커밋 (5번 해소 후) | 경로 명시해 `git add`: `docs/정기점검/매일점검/MW0601-20260922-*.md`, `dev_memory/DECISION_LOG.md`, `dev_memory/NEXT_TODO.md` (⚠ `git add .` 금지) | 편할 때 |

(장전 1~3번은 그대로 유효 — 위 장전 절 참조. 장전 1번과 위 5번은 서로 다른 시점에 생긴 별개의 잠금이며 둘 다 같은 명령으로 해소된다)

**이 절이 발급한 번호**: 이상점 1-5~1-6 · 관측 O-i1~O-i2

---

# 장후(post)

- 점검 시각: 16:20~16:4X KST · PC `MW0601` (실행 환경: 코웍 리눅스 샌드박스, 호스트명 `claude` → `--pc MW0601` 수동 지정)
- HEAD `39d4af5` · 브랜치 `v9-dev`(정상) · 미커밋 745건(원시) / **실질 변경 5건**(`dev_memory/DECISION_LOG.md`·`NEXT_TODO.md`·기존 미커밋 3파일) · 당일 커밋 5건(장중 이후 추가 커밋 없음)
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260922_post.md`

## 0p-C. 이월 처리 — 장전+장중 항목 처분

**병행 세션 확인 먼저.** `git log --since "2026-09-22 00:00"` 재확인 결과 장중 절 종료(12:45) 이후 새 커밋은 없다 — 오늘 커밋은 여전히 5건(장전 F536354·c69e061, 병행 세션 bad6454, 617차 후속 e3e5d91·39d4af5)뿐이다. 다만 **로그로 확인한 새 사실이 하나 있다 — 15:08:06경 프로세스가 두 번째로 죽었다가 자동 재시작했다.** 이것은 장중 점검(12:34~12:37 실행) 이후에 일어난 일이라 장중 절이 알 수 없었던 사건이며, 아래 1-7로 신규 등록한다. 이 사건이 O-i1(크래시 수정의 라이브 반영 여부) 판정에 직접 영향을 준다.

| 원 번호 | 제목(줄임) | 원 국면 | 처분 | 근거 |
|---|---|---|---|---|
| 1-1 | `.git/index.lock` STALE(09:11 확정, 09:00경 생성분) | 장전 | ✅ 해소 | 장중 12:34:21 자가점검 "인덱스락 없음" — 그 개별 잠금 인스턴스는 사라졌다(재발 패턴 자체는 1-6으로 계승) |
| 1-2 | `SessionStateDrop` 완료 마커 소실(F-1/538-4) | 장전 | 🔄 지속(원인 미해결) | 근본 원인(`session_recovery_service.py:increment_session` 날짜롤오버 시 마커 미승계)은 오늘도 안 고쳐졌다. 다만 오늘 EOD가 15:53:20에 `p8_last_success_date`·`eod_retrain_ok_date`를 정상 재기록해 `data/session_state.json` 최종 상태는 두 키 모두 2026-09-22로 일치 — 결과적 영향 없음(원인과 결과는 별개, 계측 4원칙 ④) |
| 1-3 | `collect_evidence.py` git diff 실패 | 장전·장중 | ↩️ 정정(부분) | **오늘 장후 수집(16:21)에서는 재현되지 않았다** — `실질 변경 5건`이 정상 산출됐다. 09-21·09-22 장전·장중 3연속 재현 후 처음으로 정상 작동. 간헐적 실패라는 뜻이므로 원인 조사는 계속 필요(P2 유지, 완전 해소로 보지 않음) |
| 1-4 | 미커밋 3파일 지속 | 장전 | 🔄 지속(내용 변경 없음) | `features/levels/levels_store.py`·`premarket_levels.py`·`scripts/cybos_autologin.py` — 오늘 장후에도 동일 변경량, 다른 작업 세션 소관 |
| 1-5 | 크래시 #1(09:41:56, 방향표시 다이얼로그) | 장중 | 🔄 지속 → 아래 1-7과 통합 서술 | 원인·수정은 병행 세션이 이미 완료(커밋 `e3e5d91`). 배포 여부는 아래 1-7에서 최종 판정 |
| 1-6 | `.git/index.lock` 장중 재발(12:35경 생성) | 장중 | 🔄 지속 — **여전히 STALE** | 16:21 최종 재확인: 동일 잠금(0바이트·mtime 12:35) 그대로 존재, 나이 3.8시간. 이 세션도 `--reclaim` 시도 → `Operation not permitted`(동일 패턴). 사용자 조치 필요 |
| 확인필요 | `[Canary]` z경고 15개(사상 최고치) | 장전 | 🔄 지속(313차 원칙 — 판정 보류) → O-p2로 계승 | 재적합·재측정은 장 시작 전 1회뿐이라 장후 시점 추가 관측치 없음. 다음 개장 전까지 이월 |
| O-p1 | `[HealthPolicy] Degraded 선제차단` 빈도 | 장전→장중 | ✅ (장중에 이미 판정 완료) | 재확인 불필요 |
| O-p2 | Canary z경고 5거래일 사다리 | 장전 | 🔄 판정 보류 — **다음 거래일 이월** | 오늘 15(신규 최댓값) 반영, 313차 원칙상 지금 결론 내지 않음 |
| O-p3 | `.git/index.lock` 최종 상태 | 장전→장중 | 🔄 판정 보류 → 본 절 하단 "세션 종료 시 확인"에서 **최종 판정** | 16:21 기준 STALE 지속 |
| O-p4 | `git diff` 실패-잠금 생성 시각 상관 | 장전→장중 | ↩️ 정정 — **오늘 장후 데이터로는 상관 자체가 성립 안 함** | 장후 수집은 git diff 성공 + 락은 이미 12:35생성분이 그대로 있을 뿐 새로 만들어지지 않음(자가점검 "이 수집 실행은 락을 만들지 않았다") — 원인이 이 세션의 명령 조합이 아니라는 쪽으로 더 기운다. 조사는 계속하되 이 가설(초 단위 상관)의 우선순위는 낮춘다 |
| O-i1 | 크래시 수정(`e3e5d91`)의 라이브 반영 여부 | 장중 | ✅ **판정 완료 — 반영됨(단, 의도한 경로가 아니다)** | 15:08:06 두 번째 크래시로 인한 자동 재시작(15:08:30)이 디스크의 최신 코드(수정 포함)를 다시 읽어들여 **결과적으로 반영됐다.** 사용자가 사용자 조치 4번에서 고르려던 "지금 vs 15:10 이후"는 그 전에 시스템이 스스로 재기동하면서 무의미해졌다. 상세는 1-7 |
| O-i2 | `sizing_inversion_watch`([28]) qty≥3 표본 | 장중 | 🔄 지속(표본 +2) | 오늘 진입 2건 모두 사이저 출력 3계약 → 실제 진입 2계약(메타·독성 배수에 눌림) — 아래 제5부 참조 |

**미처분 0건.**

## 0p. 오늘 한눈에 (장후 최종)

오늘 미륵이는 두 번 진입했고 둘 다 이익으로 끝났습니다(합계 약 9만 4천 원, 수수료를 뺀 실제 금액). 오전 11시 2분에 산 것(롱)과 오후 1시 16분에 판 것(숏) 모두 짧게(2분 안팎) 들고 있다가 목표가의 3분의 1을 먼저 챙기고 나머지는 보호장치로 안전하게 정리하는, 설계된 대로의 좋은 패턴이었습니다. 15시 10분 강제 청산 시각에는 이미 포지션이 없었으므로 걸릴 것도 없었고, 장 마감 후 재학습과 마무리 작업도 전부 정상적으로 끝났습니다.

다만 오늘 프로그램이 **원인 모르게 두 번 통째로 꺼졌다가 자동으로 다시 켜지는 일**이 있었습니다. 첫 번째(오전 9시 41분)는 화면에 방향을 보여주는 작은 창을 그리다가 생긴 오류였고, 원인과 고치는 코드는 이미 다른 작업에서 찾아 고쳐뒀습니다(다만 그 고친 코드는 한동안 실제로 도는 프로그램에는 반영되지 않은 채였습니다). 두 번째(오후 3시 8분)는 **아무 단서도 남기지 않고 조용히 죽었습니다** — 첫 번째와 같은 문제인지 다른 문제인지 로그만으로는 확인할 수 없습니다. 두 번 다 마침 포지션이 없는 상태였고 자동으로 1분 안에 되살아났기 때문에 실제 손해는 없었지만, 만약 포지션을 들고 있을 때 이런 일이 생기면 위험할 수 있습니다. 흥미롭게도 두 번째로 꺼졌다 켜지는 과정에서, 낮에 이미 준비돼 있던 수정 코드가 (사람이 손대지 않았는데도) 자동으로 적용됐습니다 — 프로그램을 다시 켜는 순간 최신 코드를 다시 읽어 오기 때문입니다. 그래서 지금 이 순간 돌고 있는 프로그램에는 첫 번째 문제의 수정이 반영돼 있습니다. 다만 두 번째 원인 모를 종료는 여전히 미해결 상태로 남아 있습니다.

그 외에, 어제부터 이어지던 "저장소 잠금 파일" 문제(장전 절 1번)는 낮에 한 번 저절로 풀렸다가, 낮 12시 35분쯤 새로 하나가 또 생겨서 지금까지(오후 4시 20분 기준) 풀리지 않고 있습니다. 이 역시 실제 매매와는 무관하고 오늘 만든 기록을 저장(커밋)하는 것만 막고 있습니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 조건부 정상 | (장전 절 참조 — 본문 변경 없음) |
| 장중 | 조건부 정상 | (장중 절 참조 — 본문 변경 없음) |
| 장후 | **조건부 정상** | P0 없음·실손해 없음·15:10 청산 원칙 준수·EOD 재학습/P8/마감 전부 성공. 다만 **원인 불명 2차 크래시(1-7)**와 `.git/index.lock` 재발(1-6, 여전히 STALE)이 관측됨 |
| **당일 최종** | **조건부 정상 — 자본 손실 없이 이익 마감, 단 프로세스 안정성 이상 신호 2건(크래시 2회, 그중 1건 원인 불명) 확인 필요** | 오늘 절대원칙 위반은 0건. 다만 "원인 모르게 죽는 프로세스"가 하루 2번 발생한 것은 우연으로 넘기기엔 빈도가 높다 — 내일 이후 재현 여부를 주시할 것 |

**절대원칙 준수 여부 (장후 최종 — 종가 기준)**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | **준수** | 마지막 포지션은 13:18:01에 이미 청산 완료. `[SchedForceExit] 15:11 점검 — status=FLAT … 청산 대상 없음(정상)`(15:11:15). 15:18 안전망도 잔여 0 |
| ② | Circuit Breaker 5종 | 준수 | ①②③④⑤ 전부 오늘 미발동. CB②(`=3`, 2026-09-02 복원)는 오늘 손절 자체가 없어(둘 다 TP1 보호전환 승리) 카운터 증가 없음. CB③은 30m `ready` 판정 가능 시간이 오늘 0분/0분이었음(아래 이상점 1-8 참조 — 새 결함 아님, 기존에도 간헐 재현). CB④·⑤ 임계 미도달 |
| ③ | CORE 피처 호라이즌 그룹별 | 준수 | 오늘 진입 2건 모두 단기그룹(3m) VWAP ✅(강제 X 사유 없음). `cvd` 미통과는 등급 하락 사유일 뿐(위반 아님) — 둘 다 원시 C에서 A로 정당하게 상향 |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 준수 | `0xC0000409`·`STACK_BUFFER` 로그 0건. 오늘 크래시 2건 모두 COM 콜백 경로가 아니다(1차는 matplotlib GUI 확인됨, 2차는 원인 불명이나 COM 콜백 위반의 특징적 크래시 코드도 관측되지 않음) |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 확인 대상 아님 | 이상 없음 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 준수 | 관련 흔적 없음 |

**설정 불변식** — 수집기 §3 전 24종 `일치`. 차단 게이트 35개 중 9개 의도적 비활성 — 전부 기대값과 일치. 이상 없음.

---

## 1p. 이상점 정리보고 (장후 신규)

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**없음.** 오늘 실현손익은 전량 이익이었고 절대원칙 위반도 없었다.

### P1 — 정확성·관측 훼손

#### 1-7. 프로세스가 원인 불명 상태로 두 번째 정지·자동재시작 — 15:08:06경, 흔적(트레이스백) 없음, 15:10 강제청산 시각 직전이라는 점이 우려스럽다

- **증상**: `logs/Mireuk_batch/launcher_20260922_084001_14358.log` 기준, 09:43:11에 시작된 프로세스가 **326분(약 5시간 26분) 실행된 뒤 15:08:06경 종료**됐다. 런처는 이를 `"일시적 크래시"`로 분류해 10초 후 자동 재시작했고(15:08:30 새 프로세스 기동), 15:09:10에 세션이 정상 복구됐다(`[Session] 재기동 #1`, `[BrokerSync] startup sync 무포지션 확인: FLAT → FLAT`). **오전 첫 번째 크래시(09:41:56, 이상점 1-5)와 달리 이번에는 파이썬 예외(Traceback)가 launcher 로그 어디에도 남지 않았다** — 이 로그 전체(47,531행)를 훑어도 `Traceback`은 오전 사건 1건뿐이다. `logs/crash_fault.log`(행감지 워치독)도 `[TS] 2026-09-22T15:08:05 beat_age=0s`(정상) → `[CLEAN EXIT] 2026-09-22T15:08:08 PID=27016` → `[START] 2026-09-22T15:08:30 PID=28960`로, 워치독이 "행 멈춤(hang)"으로 감지한 것이 아니라 **프로세스가 스스로 빠르게 사라진 것**임을 보여준다.
- **근거**:
  ```
  logs/Mireuk_batch/launcher_20260922_084001_14358.log:
  15:08:06 [WARNING] SYSTEM: [ChartDBG] paintEvent slow 125.0ms ... slow_cnt=28749 total_cnt=28749  (마지막 정상 로그)
  [AUTO-RESTART] main.py 가 326분 실행됨 -- 일시적 크래시, 카운터 초기화.
  Mireuk 종료. RestartCnt=0 Runtime=326min
  [AUTO-RESTART] #1 시도 (시각=1508) -- 10초 후 재시작...
  [AUTO-RESTART] main.py 재시작...
  15:08:30 [DynMC] 기동 복원: GAP_OPEN 0.670 → 0.435  (새 프로세스 시작)
  15:09:10 [Session] 재기동 #1 | cause=STARTUP
  15:09:10 [BrokerSync] startup sync 무포지션 확인(blank rows): FLAT -> FLAT (armistice cleared)

  logs/crash_fault.log:
  [TS] 2026-09-22T15:08:05 beat_age=0s watching=True strikes=0
  [CLEAN EXIT] 2026-09-22T15:08:08  PID=27016
  [START] 2026-09-22T15:08:30  PID=28960  Python 3.7.13 32bit  RSS=178MB

  (직전 09:30~15:07 사이 WARN/SYSTEM 로그를 전수 확인 — 에러·예외·access violation 등
   어떤 신호도 15:08:06 이전에 없었다. paintEvent 경고(ChartDBG)가 계속 있었지만
   이는 하루 종일 흔한 배경 소음이다)
  ```
- **기준 위반**: 절대원칙 ④(COM 콜백 dynamicCall·emit 금지)는 저촉 여부를 확인할 트레이스백 자체가 없어 판정 불가 — 다만 `0xC0000409` 특유의 로그 패턴도 없다. 계측 4원칙 ④(폴백 가시화)와 같은 계열 — "왜 죽었는지"가 **어디에도 남지 않는 죽음**이라는 점이 가장 큰 문제다.
- **영향**: 이 시각(15:08:06)에는 포지션이 없었다(마지막 청산은 13:18:01) — **직접 금전 손실 0원**. 재기동도 15:09:10에 완료돼 15:10 강제청산 시각까지 여유가 있었다(실측 약 1분 소요, 15:10까지 약 2분 여유). 매분 루프 커버리지도 09:00~15:10 371/371분(100%)으로 이 크래시로 인한 공백은 없다. **다만 15:10 마감 직전이라는 시점이 우연이라기엔 아슬아슬했다** — 재기동이 조금만 더 걸렸다면(예: Cybos 재로그인 지연) 15:10 강제청산 경로 자체가 시험대에 오를 뻔했다.
- **영향(기술)**: 원인 후보를 좁힐 단서가 로그에 없다. CLAUDE.md가 기록한 "0xC06D007F 계열 무흔적 프로세스 즉사"(537차, BLAS/LAPACK delay-load 실패 시 stderr 없이 프로세스 종료)와 증상이 일치한다(예외가 아니라 프로세스 종료라 try/except로 못 잡고 로그도 안 남는다는 서술과 정확히 부합) — 단 이것은 **유력 가설이지 확정이 아니다**. `dev_memory/NEXT_TODO.md`에 이미 등록된 "606-4 09-17 access violation 원인 조사 우선순위 결정" 항목과도 증상이 겹칠 수 있다. 두 가설 모두 이 세션이 확정할 근거는 없다 — Windows 이벤트 뷰어(응용 프로그램 로그)에 이 시각 오류 이벤트가 남아 있는지가 다음 조사의 시작점이다(리눅스 샌드박스에서는 접근 불가 — 사용자 조치 필요).
  다른 한편, **이 크래시가 결과적으로 원치 않은 경로로 O-i1을 해소했다.** 09:43:11부터 15:08:06까지 돌던 프로세스는 오전 크래시 수정(커밋 `e3e5d91`, 12:34 배포)이 작업 디렉터리에 반영된 뒤에도 **재시작 전까지는 계속 수정 전 코드로 돌고 있었다.** 15:08:30에 새로 뜬 프로세스는 디스크의 최신 `.py` 파일을 다시 읽으므로 수정된 코드로 기동됐다 — 사용자가 사용자 조치 4번에서 판단하려던 "지금 반영할지 15:10 이후로 미룰지"는 시스템이 스스로 재기동하면서 답이 정해져 버렸다. 즉 **의도한 절차가 아니라 사고로 인해 우연히 반영된 것**이며, 이 자체가 "장중 예기치 않은 재기동이 배포 시점을 사용자 통제 밖에서 결정한다"는 새로운 운영 리스크를 보여준다.
- **신규 여부**: 신규(이 점검 세션 기준 최초 보고. 오전 크래시 1-5와는 별개 사건).

### P2 — 운영 부담·기술부채

#### 1-8. CB③(30분 정확도 서킷브레이커)이 판정할 수 있는 시간이 오늘 하루 0분이었다 — 새 결함은 아니나 반복 관측되는 패턴

- **증상**: 수집기 §5 "CB③ 판정 가능 시간 — 0분 / 0분 (—)". 30분봉 예측 정확도 버퍼(`acc30m`)가 하루 종일 한 번도 "판정 가능(ready)" 상태에 도달하지 못했다 — 즉 절대원칙 ②의 다섯 번째 안전장치(30분 방향 정확도 28% 미만 시 당일 정지)가 오늘은 **발동 여부를 판단할 재료 자체가 없었다.**
- **근거**:
  ```
  evidence_MW0601-20260922_post.md §5:
  ### CB③ 판정 가능 시간 — 0분 / 0분 (—)
  acc30m 버퍼 리셋 0회 · 그때 버린 표본 0건 (스케일러 재적합이 CB③ 표본을 되감는다)

  logs/20260922_SYSTEM.log 15:40:14:
  [CB③계측] 조건성립 0분 / 판정가능 0분 / 파이프라인 0분 · 그 창 진입 0포지션 · 손익 +0원
    (임계 acc30m<0.28 · HALT 차단은 한시예외로 비활성)

  과거 7거래일 대조(evidence_*_post.md에서 직접 확인):
  09-14 47분/370분(13%) · 09-15 0분/0분 · 09-16 164분/370분(44%) · 09-17 286분/370분(77%) ·
  09-18 261분/370분(71%) · 09-21 0분/0분 · 09-22(오늘) 0분/0분
  ```
- **기준 위반**: 없음 — CB③-P4가 이미 한시예외로 차단만 비활성화돼 있어(`CB3_P4_GRADE_BLOCK_ENABLED=False`) 오늘도 실질적인 진입 차단에는 영향이 없었다. 다만 CLAUDE.md 절대원칙 §3 말미가 "30m 학습을 중단하면 안 된다 — CB③의 유일한 입력원"이라고 못박은 그 입력원이, 오늘처럼 **가용 시간 0분인 날이 최근 7거래일 중 3일(09-15·09-21·09-22)**이나 된다는 뜻이다.
- **영향**: 오늘 CB③이 판정할 기회 자체가 없었을 뿐 실제로 위험한 상황(30m 정확도 급락)이 있었는지는 알 수 없다 — "판정 안 함"과 "정상 판정 후 문제 없음"은 다르다(계측 4원칙 ②). 오늘 실현손익이 전량 이익이었으므로 실질 위험으로 이어지지는 않았다.
- **영향(기술)**: 스케일러 재적합(`ConstOut`·`WeightCollapse` 등)이 발생할 때마다 `acc30m` 버퍼가 되감기는 구조라, 30분봉 예측이 자주 상수 출력에 가까워지는 최근 장세(오늘도 `[Calibration:30m] 축퇴 감지` 148건)에서는 30m 버퍼가 "쌓이는 속도"보다 "리셋되는 속도"가 빨라 ready 상태에 도달하지 못하는 구조적 문제일 가능성이 있다. 3/7일꼴로 재현되는 패턴이라 우연으로 보기 어렵다.
- **신규 여부**: 기존 관측 패턴(09-15·09-21에도 동일)의 3회째 재현 — **새 이상점으로 처음 등록**(과거 리포트들이 이 수치를 §5에 싣기만 하고 별도 이상점으로 격상하지 않았던 것으로 확인돼, 반복성을 근거로 이번에 처음 등록한다). CB③ 기준 호라이즌 교체 논의(실전 전환 기준 ⑥)의 근거 자료로 축적할 것을 제안한다.

### 확인 필요 (확정 아님)

- 이상점 1-7(2차 크래시)의 정확한 원인 — 트레이스백이 없어 이 세션 수준에서는 "BLAS delay-load 무흔적 종료" 대 "09-17형 access violation" 두 가설 중 하나로 좁히지 못한다. Windows 이벤트 뷰어 확인이 필요(사용자 조치).
- O-p2(Canary z경고 5거래일 사다리) — 313차 원칙, 표본 판정 보류 지속.

### 이미 반영된 사안 (신규 아님)

- **EOD 재학습 로그의 `GuardGhost`(12건)·`GuardFair`(6건) 경고, 전부 1m 호라이즌** — 2026-08-25·09-04·09-09·09-11·09-21 리포트에 이미 등장한 **설계대로의 정상 동작**(457차 F7 ⑤안)이다. "배포된 pkl이 CV 미검증 intraday 모델인데 낡은 정확도 파일과 비교당하는" 상황을 가드가 스스로 감지해 그 비교를 무효 처리하고 CV 검증된 새 모델로 교체한다 — 문제 없음. 오늘은 장중재학습이 2회(09:43·13:05, 그중 1회는 1-5 크래시의 WarmupRetrain)로 평소보다 잦아 이 경고도 그만큼 더 찍혔을 뿐이다.
- **오늘 진입 2건 모두 `raw_grade='C'`가 `grade='A'`로 상향된 "A급(원시C)"** — `phases.md` B-4가 이미 "상향 경로가 정당한지 본다"고 짚은 정상 패턴. 두 건 다 `meta_action`(reduce/take)·`toxicity_action`(pass/reduce)이 정상적으로 개입해 최종 2계약으로 확정됐고, 체크리스트 미통과 항목(`cvd`·`fore`·`prev`·`chas`)도 전부 "등급 하락" 사유이지 "강제 X" 사유(VWAP)가 아니다 — 위반 없음.
- **사이저 출력(3계약) vs 실제 진입(2계약)** — `sizing_inversion_watch`([28], 실전 전환 기준 ⑧) 기존 등록 채널. 오늘 2건 추가(표본 O-i2), 313차 원칙상 확정 결론 여전히 불가.

**이 절이 발급한 번호**: 이상점 1-7~1-8 (관측 번호 신규 발급 없음 — 기존 O-i1·O-p3이 이 절에서 최종 판정됨)

---

## 2p. Fix 작업 구현계획 (장후 신규)

### F-3. 2차 무흔적 크래시(1-7) 원인 규명 — P1 · 대응 이상점 1-7

- **원인 가설**: (a) CLAUDE.md 537차가 기록한 "conda env 활성화 없이 BLAS/LAPACK 콜(`np.corrcoef`·`np.linalg.*`·`scipy.stats`) 호출 시 stderr 없이 프로세스 즉사"(`0xC06D007F`) 계열 — 단 이 프로젝트는 이미 `utils/dll_bootstrap.py:ensure_conda_dll_path()`를 진입점에서 호출하도록 가드가 있어야 하므로, 만약 이 가설이 맞다면 그 가드가 **어느 경로에서는 빠져 있다**는 뜻이 된다. (b) `dev_memory/NEXT_TODO.md` 606-4에 이미 등록된 2026-09-17 access violation과 동일 계열의 별도 원인. (c) matplotlib/Qt 렌더링 관련 미확인 네이티브 크래시(1-5와 같은 계열이지만 이번엔 Python 예외로 안 잡힌 경우 — 예: Qt 페인트 스레드 안에서 직접 난 네이티브 크래시).
- **변경 파일**: 없음(조사 항목). 조사 착수 시 `utils/dll_bootstrap.py:audit_dll_bootstrap.py --fail-on-gap` 재실행으로 (a) 가설부터 배제/확인 가능.
- **회귀 위험**: 없음(조사만).
- **절대원칙 저촉 여부**: 해당 없음.
- **런타임 주의**: py37_32 라이브 프로세스 — py310_64 재학습과 무관.
- **검증 방법**: ① Windows 이벤트 뷰어(응용 프로그램 로그)에서 PID 27016 또는 15:08:06~08 사이 종료 이벤트 확인(사용자 조치) ② `python scripts/audit_dll_bootstrap.py --fail-on-gap`으로 BLAS delay-load 가드 커버리지 재확인 ③ 다음 재현 시 `logs/crash_fault.log`의 `beat_age`가 죽기 직전까지 0에 가까웠는지 재확인(이번처럼 hang이 아니라 즉사인지 패턴 확인).
- **섀도 계측 필요 여부**: 아니오.
- **적용 시점**: 조사는 즉시 가능, 코드 변경 없음.
- **결정 필요 사항**: Windows 이벤트 뷰어 확인은 이 세션(리눅스 샌드박스)이 할 수 없다 — 사용자 조치로 이관.

### F-2 최종 상태 갱신 — 장중에서 넘어온 항목

장중 F-2("크래시 수정을 언제 반영할지" 사용자 판단 필요)는 **1-7의 자동 재시작으로 결과가 이미 정해졌다** — 15:08:30 이후 프로세스는 수정된 코드로 돈다. 사용자 조치 4번은 이 갱신을 반영해 "완료(의도치 않은 경로)"로 닫는다(아래 사용자 조치 절 참조).

### 적용 순서와 커밋 계획

이 세션도 `.git/index.lock`이 STALE인 동안 커밋할 수 없다(1-6). 세션 종료 시 최종 상태를 아래에 기록한다.

> 커밋 대기: `docs/정기점검/매일점검/MW0601-20260922-점검리포트.md`(장후 절 추가), `docs/정기점검/매일점검/evidence_MW0601-20260922_post.md`(신규), `dev_memory/DECISION_LOG.md`(append), `dev_memory/NEXT_TODO.md`(append)

---

## 3p. 고도화 방안 (장후 신규)

### G-3. "무흔적 프로세스 종료"를 구분 가능하게 만드는 계측 — 우선순위: 이번 주(1-7이 두 번째 사례를 만들었다)

- **관측 근거**: 오늘 1-7이 보여준 것 — 파이썬 예외 없이 프로세스가 사라지면 원인 조사가 사실상 불가능하다. 첫 크래시(1-5)는 트레이스백이 남아 12:34에 원인·수정까지 완료됐지만, 두 번째(1-7)는 아무 단서가 없어 F-3이 "조사는 못 하고 가설만 나열"하는 데 그쳤다.
- **제안 내용**: `logs/crash_fault.log`의 하트비트 워치독이 이미 "행 멈춤"은 잡아내므로(FreezeWatchdog), 이번처럼 **행 멈춤 없이 즉사하는 경우**를 별도로 표시하는 표를 추가한다 — 예를 들어 정상 종료 플래그(`data/_exit_normally`)도 없고 `FreezeWatchdog` 발동 로그도 없는 재시작이면 "원인 불명 즉사"로 런처가 스스로 태그를 남기게 한다(현재는 "일시적 크래시"라는 뭉뚱그린 문구뿐이라 원인 계열조차 구분이 안 된다). 이는 계측 4원칙 ④(폴백 가시화)의 연장이다.
- **기대 효과**: 다음에 같은 유형의 크래시가 나면 "행 멈춤형이었는가 즉사형이었는가"를 로그만으로 즉시 구분할 수 있다 — 현재는 이 점검 세션이 crash_fault.log를 수동으로 대조해야만 알 수 있었다.
- **비용·위험**: 낮음 — 런처 스크립트에 종료 사유 분류 로직 추가. 매매 로직과 무관.
- **선행 조건**: 없음.
- **우선순위 제안**: 이번 주 — 오늘로 무흔적 재시작이 최소 2건째(1-7, 그리고 606-4의 09-17 건)라 재현 빈도가 낮지 않다.

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| CB③ 판정가능시간 0분 재현(1-8) | 26주 WFA 재검증 후보 미등재 | `references/invariants.md` §5 "주기적 재검증 항목"에 "CB③ 입력 가용성(acc30m ready 시간)" 항목 신설 검토 | 3/7거래일꼴로 0분 재현 — CB③ 기준 호라이즌 교체(실전 전환 기준 ⑥) 논의의 정량 근거가 될 수 있다 |

---

## 4p. 진입 승패 사후검증

**4-0. 선행 확인** — EOD(`daily_close_done_20260922.txt` 15:40:15) · P8(`data/session_state.json`: `p8_last_success_date=2026-09-22`, `eod_retrain_ok_date=2026-09-22`) **둘 다 완료 확인.** DB 접근·수집기 실행 모두 실패 없이 완료.

**4-1. 오늘 할일 4분류**

오늘 `NEXT_TODO.md`에 새로 등록된 항목은 전부 오늘 자체 점검 세션들(617차·617차 후속·617차 후속2)이 만든 것이라 "완료 여부를 오늘 대조"할 만한 전일자 이월 항목이 따로 없다. 대신 어제(609차)·최근 세션이 남긴 항목 중 오늘 커밋으로 실제 반영된 것을 확인한다.

| # | NEXT_TODO 항목 | 당일 커밋 | 코드 실재 확인 | 상태 |
|---|---|---|---|---|
| 617후속-1 | 무가드 표시 슬롯 15건 가드 | `e3e5d91` | `dashboard/panels/direction_indicator_dialog.py` 외, 회귀테스트 `tests/test_617_dashboard_chart_zero_width.py` 15건 포함 확인 | ✅완료-커밋확인 |
| 617후속-2 | 엔진 슬롯 3건(`_on_main_heartbeat` 등) 처분 결정 | 없음 | 주간회의 안건으로 등록만 됨, 코드 변경 없음 | ⬜미착수(의도적 — 주간회의 대기) |
| 611후속4 | `[80]` 사전등록 | `c69e061` | 커밋 메시지 확인, 상세 코드 대조는 이 세션 범위 밖(별도 채널 — 오늘 진입 2건이 해당 채널과 직접 관련 없음) | ✅완료-커밋확인(범위 밖 상세 미검증) |

⚠ 미완료 항목(617후속-2)은 의도적으로 주간회의로 미룬 것으로 `NEXT_TODO.md`에 그렇게 명시돼 있어 P1로 올리지 않는다(완료주장-커밋없음이 아니라 처음부터 "결정 대기"로 등록됨).

**4-2. 3원 대사 (포지션 단위)**

| 원천 | 질의 | 값 |
|---|---|---|
| 로그 | `[Position] 진입` | 2 |
| `ensemble_decisions` | `entry_executed=1` | 2 |
| `trades` | `COUNT(DISTINCT entry_ts)` | 2 |
| (참고) `trades` | `COUNT(*)` — 청산 레그 | 4 |

**세 값 모두 일치(2=2=2) — 정상.** 레그 4행은 두 포지션 모두 TP1 부분청산 + 트레일 전량청산으로 나뉜 결과이며 불일치가 아니다.

**4-3. 케이스 카드**

```
[CASE-01] 진입 11:02:03 LONG · 2계약 (등급 A/원시C · horizon=3m · hurst=mean-revert)
          → 11:04:52 TP1 부분청산 33% (+0.56pt, +16,926원)
          → 11:05:00 하드스톱[exit_stage=TRAIL_AFTER_TP1, TP1 보호전환] (+0.36pt, +6,926원)
          포지션 합 +0.92pt / +23,852원(순, 수수료 11,074×2=22,148 차감 후) · 보유 2분 49초
 진입근거   [진입체크] 11:02:00 "LONG→LONG 2계약 A급(원시C) | sign✅ conf✅ vwap✅ cvd❌ ofi✅
            fore❌ prev✅ time✅ risk✅ chas✅ coun✅ | conf=37.4%"
            [Sizer] 11:02:00 "기본리스크=1,500,000 신뢰도배수=0.6 레짐배수=0.8 → 3계약" → meta_action=reduce(×0.5)·
            toxicity_action=pass(×1.0) 적용 후 최종 2계약
 예측 vs 실현  ensemble_decisions: confidence=37.4%(min_conf=37.3%, 턱걸이 통과) · quantile_expected_pt=-0.288
            (LONG 방향과 **부호가 반대** — 가격 하락 예상이었는데 실제로는 소폭 상승 후 빠르게 TP1 도달)
            실현 방향은 LONG에 유리하게 움직였다 — quantile 예측과 실현 결과가 어긋난 사례
 승패판정   ✅승 — 두 레그 모두 `하드스톱` 문자열이 찍혔으나 `exit_stage`가 각각 `TP1`·`TRAIL_AFTER_TP1`로,
            진짜 손절이 아니라 TP1 도달 후 보호 트레일로 전량 정리된 이익 청산이다
 주 요인    C(청산 규칙 — TP1 33% + 보호 트레일 전환이 빠르게(2분 49초) 작동해 작은 유리한 변동을 안전하게 확정)
 부 요인    D(사이징 — 게이트 배수(0.5×1.0)가 사이저 3계약을 2계약으로 줄임, sizing_inversion_watch 표본)
 반사실     quantile 예측(하락)이 맞았다면 이 포지션은 하드스톱(진짜 손절)로 끝났을 수 있다 — 낮은 신뢰도(37.4%,
            턱걸이)에서 승리한 사례라 다음 유사 사례 축적이 필요(n=1, 결론 보류)

[CASE-02] 진입 13:16:02 SHORT · 2계약 (등급 A/원시C · horizon=3m · hurst=mean-revert)
          → 13:17:52 TP1 부분청산 33% (+0.66pt, +21,978원)
          → 13:18:01 하드스톱[exit_stage=TRAIL_AFTER_TP1, TP1 보호전환] (+1.18pt, +47,978원)
          포지션 합 +1.84pt / +69,956원(순, 수수료 11,022×2=22,044 차감 후) · 보유 1분 59초
 진입근거   [진입체크] 13:16:01 "SHORT→SHORT 2계약 A급(원시C) | sign✅ conf✅ vwap✅ cvd❌ ofi✅
            fore✅ prev❌ time✅ risk✅ chas❌ coun✅ | conf=52.3%"
            [Sizer] 13:16:01 "→ 3계약" → meta_action=take(×0.9)·toxicity_action=reduce(×0.7) 적용 후 최종 2계약
 예측 vs 실현  confidence=52.3%(min_conf=46.0%, 여유 있게 통과) · quantile_expected_pt=-0.288
            (SHORT 방향과 **부호 일치** — 하락 예상, 실제로 하락) — CASE-01보다 신뢰도·예측 정합성 모두 높았다
 승패판정   ✅승 — CASE-01과 동일하게 `TP1`→`TRAIL_AFTER_TP1` 이익 청산
 주 요인    A(방향예측 적중 — 신뢰도·quantile 부호 모두 SHORT와 일치)
 부 요인    C(청산 규칙 — 동일한 TP1+트레일 패턴, 1분 59초 만에 정리)
 반사실     CASE-01보다 방향 확신이 높았고 실제로 더 큰 이익(+1.84pt vs +0.92pt)으로 끝났다 — 표본 2건뿐이라
            "신뢰도가 높을수록 결과가 좋다"는 결론은 내지 않는다(313차 원칙)
```

**4-4. 요인 집계**

| 요인 | 승 | 패 | 순손익 | 평균 보유(분) | 관측 거래일 수 |
|---|---|---|---|---|---|
| A(방향예측 적중) — 주요인 1건 | 1 | 0 | +69,956원 | 2.0 | 1일 |
| C(청산 규칙 — TP1+트레일) — 주요인 1건·부요인 2건(사실상 두 건 다 관여) | 2 | 0 | +93,808원 | 2.4 | 1일 |
| D(사이징 — 게이트 배수 축소) — 부요인 1건 | 1 | 0 | +23,852원 | 2.8 | 1일 |

> 브라이어 스코어·캘리브레이션은 당일 단독으로 모델 열화를 단정하지 않는다(원칙 유지). 오늘은 `[Brier] 과신 경고`가 10:36 1건 있었으나(이동평균 0.352 > 0.35) 두 진입(11:02·13:16) 모두 이 경고 이후에 일어났고 둘 다 승리 — 표본 부족으로 이 경고의 실질 영향은 판단하지 않는다.

**4-5. 313차 가드 통과 여부**

| 갈래 | 적용 결과 |
|---|---|
| ① 일자단위 판정 | **관측 1일뿐 — 판정 불가.** 오늘 2포지션은 같은 날 같은 레짐(NEUTRAL) 스냅샷을 공유하는 독립적이지 않은 관측치다 |
| ② overlap 보정 | 해당 없음(포지션 2건이 시간상 겹치지 않고 순차적이라 보정 대상 아님) |
| ③ 이상치 분해(drop-worst) | CASE-01(+23,852) 제거 시에도 CASE-02(+69,956) 단독으로 흑자 부호 유지 — 다만 n=2라 이 분해 자체가 큰 의미는 없음 |
| ④ 사전등록 — 기준 불변 | 313차 원칙 그대로 적용, 오늘 결과로 어떤 판정식도 바꾸지 않음 |
| ⑤ 부호 일관성(기간·PC 교차) | 판단 불가(표본 1일) |

**결론: 오늘의 승리 2건은 "관찰"로만 기록한다.** "TP1+트레일 청산 패턴이 잘 작동한다"거나 "낮은 신뢰도에서도 이긴다"는 식의 어떤 결론도 이 표본(n=2, 1거래일)만으로는 내리지 않는다.

---

## 5p. 수익률 향상방안

**누적 대장 갱신(§5-B, `docs/정기점검/수익률향상_누적대장.md`)** — 오늘은 신규 방안을 제안하지 않는다. 오늘 데이터가 관련된 기존 방안의 표본 칸만 갱신한다.

- **P5-06 (「분할체결 손절」 손절률 분모 오분류)** — `[ExitStageRecon]` 자기대사(15:40:14): "오늘 TRAIL_AFTER_TP1 2레그/2포지션 중 TP 이벤트 대응 2 · 미대응 0". **증분 0건**(오늘 두 TRAIL_AFTER_TP1 레그는 정상적으로 TP1과 매칭됐고 P5-06이 문제 삼는 "TP1 미대응" 오분류 사례가 아니다) — 누계 20건/6거래일 −14,707,793원 그대로 유지.
- **O-i2 / [28] `sizing_inversion_watch`** — 표본 +2건(둘 다 사이저 3계약→실제 2계약, 게이트 배수 원인: CASE-01은 meta×0.5, CASE-02는 toxicity×0.7). 313차 원칙상 확정 결론 없음, 26주 WFA 주기 표본 축적 계속.
- 그 외 P5-01~05·07~16은 오늘 진입 2건이 각 채널의 사전등록 조건(호라이즌·시간대·레짐 등)과 직접 대조할 근거가 부족해 재검증하지 못했다 — 다음 장후가 확인할 것.

오늘은 신규 6칸 방안을 등록하지 않는다 — 이유: 승패 요인 집계(§4-4)가 n=2로 313차 원칙상 어떤 정량 제안의 근거도 될 수 없고, 오늘의 실질적 이슈(1-7 크래시)는 수익률이 아니라 운영 안정성 범주라 이 절이 아니라 2p F-3·3p G-3로 별도 관리한다(09-14 563차의 동일한 처리 선례를 따름).

---

## 6p. 다음 거래일 관측 예정

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| O-t1 | 1-7과 같은 "무흔적 즉사" 재현 여부 | 재현 시 F-3 가설 (a)~(c) 중 어느 쪽에 더 부합하는지 로그로 재확인 | 다음 거래일부터 상시 |
| O-t2(계승 O-p2) | Canary z경고 5거래일 사다리 | 313차 원칙 — 표본 도달 시 판정 | 5거래일 후 |
| O-t3 | CB③ 판정가능시간 0분 재현 빈도(1-8) | 다음 5거래일 중 0분 재현이 2회 이상이면 26주 WFA 재검증 항목 신설 검토 | 5거래일 누적 후 |
| O-t4(계승 O-i2) | `sizing_inversion_watch` qty≥3 표본 | 26주 WFA 주기, min_samples 도달 시 판정 | 26주 WFA 주기 |

## 7p. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| CB② **발동 1회 관측** (값 복원은 2026-09-02 완료, `=3`) | 상시 관측 | — | 오늘도 미발동(손절 자체가 없었음 — 정상). 인위적으로 만들지 말 것 |

---

## 8p. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-09-22 (MW0601 617차 후속3 — 장후 점검)` (아래 실제로 append)
- `NEXT_TODO.md` 추가 체크박스: F-3(2차 크래시 원인규명), G-3(무흔적 종료 계측), O-t1~O-t4
- 완료 처리한 기존 항목: O-i1(크래시 수정 라이브 반영 여부 — 판정 완료), O-p3(git lock 최종상태 — 아래에서 판정)

---

## 세션 종료 시 `.git/index.lock` 최종 확인 (장후)

16:21:09 1차 확인(evidence 수집 시점) — STALE(0바이트·3.8시간·git 프로세스 0개). 이 리포트 작성 완료 시점 재확인 결과도 동일 — **STALE 지속.** `--reclaim` 시도 → `Operation not permitted`(동일 패턴, 이 세션은 `unlink` 권한 없음). **O-p3 최종 판정: 오늘 하루 동안 이 잠금(12:35 생성분)은 한 번도 자동 해소되지 않았다** — 사용자가 Windows PC에서 직접 회수해야 한다(아래 사용자 조치 참조).

## 사용자 조치 (장후 최종 갱신 — 장전 1~3, 장중 4~6번에 이어)

| # | 할 일 | 명령·경로 | 기한 |
|---|---|---|---|
| 7 | `.git/index.lock` 회수 (12:35 생성분, STALE 확정 지속, 이 세션은 회수 권한 없음) | Windows PC에서 `python scripts\git_lock_guard.py --reclaim` (실패 시 `.git\index.lock` 수동 삭제 — 단, 다른 git 작업이 실제로 진행 중이 아닌지 먼저 확인) | 다음 기동 전 |
| 8 | 오늘 세 국면 리포트·증거 파일·dev_memory 변경분 커밋 (7번 해소 후) | 경로 명시해 `git add`: `docs/정기점검/매일점검/MW0601-20260922-*.md`, `dev_memory/DECISION_LOG.md`, `dev_memory/NEXT_TODO.md` (⚠ `git add .` 금지 — CRLF/LF로 수백 건 오탐) | 편할 때 |
| 9 | **[운영 판단 권고]** 15:08 원인 불명 크래시(1-7)를 Windows 이벤트 뷰어(응용 프로그램 로그)에서 확인 — access violation·0xC06D007F류 오류 이벤트가 15:08:06~08 사이에 있는지 | 이벤트 뷰어 → Windows 로그 → 응용 프로그램에서 해당 시각 필터 | 여유 될 때(급하지 않음 — 오늘 손해 없었음) |
| 4(장중, 갱신) | ~~크래시 수정 재기동 시점 결정~~ | **완료(의도치 않은 경로)** — 15:08 두 번째 크래시로 자동 재시작되면서 수정된 코드가 이미 반영됨. 더 이상 판단할 필요 없음 | — |
| 3(장전, 지속) | `features/levels/levels_store.py`·`premarket_levels.py`·`scripts/cybos_autologin.py` 3파일 미커밋 상태 검토 | 다른 작업 세션 소관 — 내용 검토 후 커밋 또는 폐기 결정 | 편할 때 |

**이 절이 발급한 번호**: 이상점 1-7~1-8 · 관측 O-t1~O-t4 (O-i1·O-p3은 이 절에서 최종 판정, 신규 번호 아님)

---

## 9p. 종합 — 오늘 하루

### 9p-1. 종합 판정

| 국면 | 판정 | 한 줄 근거 |
|---|---|---|
| 장전 | 조건부 정상 | 기동·매크로·모델·전일 EOD 전부 정상. git lock(1건째)·SessionStateDrop(기존)만 관측 |
| 장중 | 조건부 정상 | 크래시 1건(원인·수정 완료, 미배포) + git lock 재발(2건째). 실손해 없음 |
| 장후 | 조건부 정상 | 크래시 2건째(원인불명) 추가 확인, EOD·P8·15:10 청산 전부 정상. git lock 여전히 미해소 |
| **당일 최종** | **조건부 정상 — 손익은 완전 정상(전량 이익), 프로세스는 하루 2번 원인모를 정지를 겪었다** | 자본·절대원칙 관점에서는 무결점 하루였으나(P0 0건, 실현손익 전량 흑자), 운영 안정성 관점에서는 "원인 모르게 두 번 죽는 프로그램"이라는 점이 다음 조사 우선순위다. `.git/index.lock`은 오늘도 미해소로 끝난다 |

**당일 최종 손익(종가 기준)**: 2포지션 2승(100%) · +2.76pt · net **+93,808원**(gross +138,000원 / 수수료 44,192원) — **포지션 단위**. 청산 레그는 4행(불일치 아님). MDD는 두 포지션 모두 이익 마감이라 실현 기준 하락구간 없음(0%).

**절대원칙 6종 최종 판정**: ① 준수 ② 준수(CB 5종 전부 미발동, CB③은 판정가능시간 0분으로 "판정 기회 없음") ③ 준수 ④ 준수(오늘 크래시 2건 모두 COM 콜백 경로 아님) ⑤ 확인대상 아님 ⑥ 준수. **위반 0건.**

### 9p-2. 당일 이상점 통합 대장

| 번호 | 제목 | 최초 국면 | 심각도 | 최종 상태 |
|---|---|---|---|---|
| 1-1 | `.git/index.lock` STALE(09:00경 생성분) | 장전 | P1 | ✅ 해소(그 인스턴스는 사라짐, 패턴은 1-6 계승) |
| 1-2 | `SessionStateDrop` 완료 마커 소실 | 장전 | P1 | 🔄 지속 → `NEXT_TODO` F-1(538-4), 원인 미해결이나 오늘 실질 영향 없음 |
| 1-3 | `collect_evidence.py` git diff 실패 | 장전 | P2 | ↩️ 정정(부분 해소) — 장후 수집은 정상, 간헐적 재현으로 재분류 |
| 1-4 | 미커밋 3파일 지속 | 장전 | P2 | 🔄 지속 → 다른 세션 소관 |
| 1-5 | 크래시 #1(09:41:56, 방향표시 다이얼로그) | 장중 | P1 | ✅ 원인·수정 완료, 15:08:30 재기동으로 배포까지 완료(1-7이 계기) |
| 1-6 | `.git/index.lock` 장중 재발(12:35 생성분) | 장중 | P2 | 🔄 지속 — **오늘 끝까지 STALE 미해소** → 사용자 조치 7번 |
| 1-7 | 크래시 #2(15:08:06, 원인 불명·무흔적) | 장후 | **P1** | 🆕 신규, 미해결 → `NEXT_TODO` F-3, 다음 거래일 O-t1로 관측 |
| 1-8 | CB③ 판정가능시간 0분(3/7거래일 재현) | 장후 | P2 | 🆕 신규 등록(반복 패턴 근거), O-t3로 관측 |

- P0 **0건** · P1 **4건**(1-1·1-2·1-5·1-7, 그중 미해결은 1-2·1-7) · P2 **3건**(1-3·1-4·1-6, 전부 지속) — 당일 누계 8건

### 9p-3. 사용자 조치 — 오늘 할 일 (하루치를 여기 하나로)

| # | 할 일 | 명령·경로 | 기한 |
|---|---|---|---|
| 1 | `.git/index.lock` 회수 (12:35 생성분, 오늘 하루 종일 STALE, 이 세션은 회수 권한 없음) | Windows PC에서 `python scripts\git_lock_guard.py --reclaim` (실패 시 `.git\index.lock` 수동 삭제 — 다른 git 작업이 실제로 진행 중이 아닌지 먼저 확인) | 다음 기동 전 |
| 2 | 오늘 하루치 리포트·증거 파일·dev_memory 변경분 커밋 (1번 해소 후) | 경로 명시해 `git add`: `docs/정기점검/매일점검/MW0601-20260922-*.md`, `dev_memory/DECISION_LOG.md`, `dev_memory/NEXT_TODO.md` (⚠ `git add .` 금지) | 편할 때 |
| 3 | **[권고]** 15:08 원인 불명 크래시를 Windows 이벤트 뷰어에서 확인 (access violation 등 오류 이벤트 유무) | 이벤트 뷰어 → Windows 로그 → 응용 프로그램, 15:08:06~08 필터 | 여유 될 때(오늘 손해 없어 급하지 않음) |
| 4 | `features/levels/levels_store.py`·`premarket_levels.py`·`scripts/cybos_autologin.py` 3파일 미커밋 상태 검토 | 다른 작업 세션 소관 — 내용 검토 후 커밋 또는 폐기 결정 | 편할 때 |
| 5 | 엔진 슬롯 3건(`_on_main_heartbeat` 등) 예외 처분 결정 | 주간회의 안건으로 이미 등록됨 — 별도 조치 불필요, 회의 때 검토 | 다음 주간회의 |

> 🔴 **`.git/index.lock`이 STALE로 확정됐고 이 세션은 회수하지 못했다.** 위 1번이 그 조치다.

**이 절이 발급한 번호**: 이상점 1-7~1-8 · 관측 O-t1~O-t4

---

## 제10부. 장후 자동조치 구현 결과 (2026-09-22 · 자동 실행)

> 장후 점검이 남긴 「사용자 조치」(§9p-3)와 Fix·고도화 항목을 자동 세션이 이어받아 처리한
> 결과다. **위 본문은 한 글자도 고치지 않았다**(하루 한 파일 · 국면마다 덧붙이기 규약).

### 10-1. 오늘 한눈에 — 사람 말로

오늘 가장 큰 수확은 **15시 8분에 프로그램이 왜 사라졌는지가 밝혀진 것**입니다.

장후 점검은 *"윈도우가 남긴 오류 기록은 이 세션이 볼 수 없으니 사람이 확인해 달라"* 며
이 질문을 넘겼습니다. 그런데 자동 세션은 그 기록을 직접 읽을 수 있었고, 읽어 보니
답은 **"고장 나서 죽은 게 아니다"** 였습니다.

윈도우는 프로그램이 **비정상적으로 뻗으면 반드시 오류 기록을 남깁니다.** 오늘 오전
9시 41분 사고는 그 기록이 남아 있습니다(화면 그리기 부품에서 뻗음 — 이미 오전에
원인을 찾아 고친 그 사고가 맞습니다). 그런데 **15시 8분에는 그 기록이 없습니다.**
게다가 프로그램 자신이 "정상적으로 종료함"이라고 적어두고 나갔습니다.

즉 15시 8분은 **누군가/무언가가 프로그램을 정상 절차로 끝낸 것**이고, 그것을 자동
재시작 장치가 「일시적 크래시」라고 **잘못 이름 붙인 것**입니다. 장후 점검이 걱정했던
두 가지 무서운 가설(계산 라이브러리가 조용히 프로그램을 죽이는 현상 / 9월 17일에
있었던 메모리 오류의 재발)은 **둘 다 아닙니다.**

⚠ 다만 **"누가 껐는가"는 아직 모릅니다.** 윈도우 오류 기록이 없다는 것은 "뻗은 건
아니다"까지만 말해 주지, "사람이 껐다"까지 말해 주지는 않기 때문입니다.

한 가지 더 나온 것이 있습니다. 어제(9월 21일) 프로그램이 **10번 다시 시작**했는데,
재시작 장치는 그 10번을 전부 「일시적 크래시」라고 적었습니다. 실제로 뻗은 것은
**0번**이었습니다. 즉 그 문구는 **믿을 수 없는 이름표**라는 것이 수치로 확인됐습니다.

### 10-2. 구현한 것

| 항목 | 무엇을 | 왜 | 안 하면 |
|---|---|---|---|
| **F-3 조사 완결** | 장후가 사전등록한 확인 수단 3가지를 **전부** 실행 | 3개 중 1개만 하고 결론 내면 그 판정은 근거를 잃는다(SKILL.md 함정① 정합성 게이트) | 원인 가설 3개가 그대로 남아 다음 사고 때도 똑같이 못 좁힌다 |
| **종료 원인 3축 대사 계측** | 점검 수집기가 매일 「런처 분류 × 프로그램 자체 기록 × 윈도우 오류 기록」을 맞춰 보게 함 | 오늘 조사는 손으로 했다. 손으로 한 것은 **다음에 또 손으로 해야 한다** | 다음 무흔적 종료 때 또 "확인 불가"로 넘어간다 |

**변경 파일**

- `utils/wer_crash.py` (신규) — 세 축을 읽고 PID로 맞추는 읽기 전용 모듈
- `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py` — `wer_crash_section()` 추가 + `build()` 9-d 등록
- `tests/test_620_wer_crash_recon.py` (신규) — 회귀 가드 17건

**등급 판정: A(계측·도구).** 매매 판단·주문·청산 경로에 닿지 않는다. 라이브 매매
프로세스가 이 모듈을 import 하지 않는다는 것을 테스트가 고정한다
(`test_no_live_trading_path_imports_this`).

### 10-3. F-3 조사 결과 — 사전등록 확인 수단 3종 전부 실행

장후 §2p F-3이 등록한 검증 방법 ①②③을 그대로 실행했다.

| 수단 | 결과 |
|---|---|
| ① 윈도우 이벤트 뷰어(응용 프로그램 로그) 15:08:06~08 | **오류 이벤트 0건.** 같은 날 09:41:57에는 있다(`Qt5Core.dll` / `c0000409`, PID 22668 — 1-5와 PID까지 일치) |
| ② `python scripts/audit_dll_bootstrap.py --fail-on-gap` | **exit 0 · "강제 대상 결손 없음"**(OK 21 / NEEDS_FIX 1 = `main.py` 권고 등급) |
| ③ `crash_fault.log` 즉사 패턴 | `15:08:05 beat_age=0s` → **`[CLEAN EXIT] 15:08:08 PID=27016`**. 행 멈춤이 아니라 **정상 종료 기록** |

**판정**

- 🔴 **가설 (a) BLAS/LAPACK 지연로딩 무흔적 즉사 — 배제.** 그 경우 `0xC06D007F`가
  윈도우 오류 기록에 남는다. 없다. ②도 가드 결손을 부정한다.
- 🔴 **가설 (b) 09-17형 액세스 위반 재발 — 배제.** 액세스 위반(`c0000005`)도 기록에 남는다. 없다.
- ✅ **남는 설명**: 프로세스가 **정상 종료 경로**로 나갔고, 런처가 그것을 「일시적
  크래시」로 오분류했다. 이는 618차가 로그 축에서 독립적으로 내린 진단
  (*"X → 재시작은 플래그 미기록"*)과 **일치**한다. 서로 다른 두 증거원이 같은 결론에
  도달했다(계측 4원칙 ⑤).

⚠ **확정의 경계**: 윈도우 오류 기록이 없다는 사실이 참으로 만드는 것은
**「미처리 네이티브 예외는 아니었다」까지**다. `sys.exit`·창 닫기·강제 종료(`TerminateProcess`)는
전부 기록을 남기지 않는다. **종료를 유발한 주체는 여전히 미상**이며, 618차가 넣은
`[Shutdown] intent=` 로그가 다음 재현 때 그 축을 채운다. 관측 O-t1은 **닫지 않는다.**

### 10-4. 조사 중 새로 나온 것

| 번호 | 내용 | 심각도 |
|---|---|---|
| **1-9** | 런처의 「일시적 크래시」 분류가 실제와 어긋난다 — 09-21 재기동 **10회 전부** 그 문구인데 네이티브 크래시는 **0건**, 09-22는 2회 중 **1건**만 진짜. 합산 **12회 중 1회(8.3%)만 실제 크래시** | P2 |
| **1-10** | 2026-09-17에 **대신 데이터 서버 `DIBSERVER.EXE`가 액세스 위반(`c0000005`)으로 3회 종료**(05:45:40 · 06:14:32 · 06:14:34). 미륵이 프로세스는 아니지만 **시세 공급 경로**다. 그날 미륵이도 07:01·07:25·08:55 세 번 재기동했다 — **인과는 미확인**(시각이 겹치지 않는다) | 확인 필요 |
| **참고** | `NEXT_TODO` 606-4의 "09-17 액세스 위반"은 **미륵이 `main.py` PID가 아니다**. 그날 `Qt5Core.dll`/`c0000409` 사건(05:45:18, PID 18780)은 있으나 런처가 기록한 미륵이 PID 5개 중 어느 것도 아니다 | — |

> ⚠ 1-10은 **가설이지 확정이 아니다**(313차 원칙). 시각이 직접 겹치지 않으며 표본은
> 하루뿐이다. 수집기에 이 축이 들어갔으므로 다음 재현 시 자동으로 잡힌다.

### 10-5. 하지 않은 것과 그 이유

| 항목 | 판정 | 사유 |
|---|---|---|
| **G-3** (무흔적 종료 구분 계측) | **대부분 이미 반영됨** — 중복 구현 안 함 | 618차(커밋 `49a2244`, 16:59)가 리포트 작성(16:31) **직후** 로그 축을 구현했다(`[Shutdown] intent=` · `utils/exit_flags.py`). 런처 배치 수정은 **사용자가 "로그 축만"으로 결정**했으므로 건드리지 않았다. 이번 작업은 618차가 손대지 않은 **OS 기록 축**을 점검 계측 층에만 추가한 것이다 |
| **G-1** (Canary z경고 재상승 원인) | **C등급 — 보류** | 리포트 자신이 선행조건을 "O-p4 관측 5거래일 완료(313차 원칙)"로 적었고 아직 미도달. 우선순위도 "26주 WFA 주기 또는 여유 있을 때" |
| **G-2** (Qt 타이머 슬롯 무가드 예외) | **C등급 — 보류** | 리포트가 **"주간회의 안건으로 이미 등록됨 · 이 점검은 중복 등록하지 않는다"**고 명시. 엔진 슬롯 3곳은 설계 결정이 필요한 범위 |
| **F-1** (`.git/index.lock` 재발 원인) | **해소 확인 · 코드 변경 없음** | 리포트도 "변경 파일 없음(조사 항목)". 12:35 생성분은 **이미 사라졌다**(618차가 16:59에 커밋에 성공했다는 것이 그 증거). 세션 시작·종료 시 모두 부재 확인 |
| **F-2** (크래시 수정 반영 시점) | **이미 종결** | 15:08 재기동으로 반영 완료. 장후가 이미 닫음 |
| **제5부 수익률 향상방안** | **신규 등록 없음** | 리포트가 *"오늘은 신규 6칸 방안을 등록하지 않는다"*고 스스로 판정(진입 n=2, 313차 원칙) |

### 10-6. 테스트

- **신규 `tests/test_620_wer_crash_recon.py` 17건 전부 통과.**
- 설계 의도: 파싱 정확도가 아니라 **판정의 겸손함**을 고정한다 — 「미측정」과 「0건」이
  같은 값이 되지 않는가, 윈도우 기록을 못 쟀을 때 판정이 「정상」이 아니라 「판정불가」로
  떨어지는가, 그리고 **라이브 매매 경로가 이 모듈을 import 하지 않는가**.
- 구현 중 **자기 코드에서 결함 1건을 발견해 고쳤다**: 2026-09-17로 재생해 보니
  `crash_fault.log`에 그날 행이 남아 있지 않은데(롤링 파일) 그것을 「정상종료 기록 없음」으로
  읽어 **그날 프로세스 5개 전부를 「하드킬」로 오판**했다. 파일이 짧아진 것을 사건으로
  보고하는 것이고 계측 4원칙 ②(미측정 ≠ 0) 위반이다. `covered` 플래그를 추가해
  「그날 행 없음」을 별도 표시하도록 고치고 회귀 테스트 2건을 덧붙였다.
- 영향 파일 주변(`test_618`·`test_619`·`test_513`·`test_483`) 재실행 — 이 세션 무관 실패
  1건(`test_483` fuoption 사본 줄바꿈 드리프트)은 618차 기준선에 이미 기록된 기존 실패다.

### 10-7. 사용자 조치 — §9p-3 대비 갱신

| # | 원래 할 일 | 상태 |
|---|---|---|
| 1 | `.git/index.lock` 회수 | ✅ **불필요** — 이미 해소됨. 세션 시작·종료 시 부재 확인 |
| 2 | 오늘 하루치 리포트·증거·dev_memory 커밋 | ✅ **자동 세션이 수행** (아래 커밋) |
| 3 | 15:08 크래시를 윈도우 이벤트 뷰어에서 확인 | ✅ **자동 세션이 수행** — 결과는 §10-3. 사람이 다시 볼 필요 없음 |
| 4 | 미커밋 3파일(`levels_store`·`premarket_levels`·`cybos_autologin`) 검토 | 🔶 **사용자 몫 유지** — 다른 작업 세션 소관, 자동 세션이 판단할 범위 밖 |
| 5 | 엔진 슬롯 3건 예외 처분 | 🔶 **주간회의 유지** — 변동 없음 |
| (신규) | 09-14·15·16·21 리포트·증거 미커밋분 | 🔶 **사용자 결정 — 5일째 누적**(09-18 자동조치도 남긴 항목). 오늘치(09-22)만 커밋했다 |

### 10-8. 전체 테스트 스위트

**2186 통과 / 9 실패 / 1 스킵 / 4 xfail, 684초.**

실패 9건은 618차가 어제 기록한 기준선과 **이름까지 동일**하며, 어느 것도 이 세션이
건드린 파일을 참조하지 않는다(`grep` 확인). 내역: peter 붙여넣기 계열 5 ·
라이브 DB 의존 2(`test_553`) · 형제 저장소 사본 드리프트 1(`test_483`) ·
테스트 순서 오염 1(`test_554`).

⚠ 결과 줄이 나온 **뒤** 화면 담당 코드(`dashboard/main_dashboard.py`) 정리 단계에서
메모리 오류가 나며 프로세스가 죽는다 — **이것도 기존과 같다.** 종료 코드가 아니라
결과 줄을 읽어야 한다(09-18 자동조치가 같은 것을 "완주 실패"로 오진했던 항목).

### 10-9. 커밋

| 해시 | 내용 |
|---|---|
| `e294cc3` | 코드·테스트 — `utils/wer_crash.py` 신규, 수집기 §9-d, 회귀 17건 |
| (아래) | 문서 — 이 절 + `dev_memory` + 오늘치 리포트·증거 |

브랜치 `v9-dev`. `.git/index.lock` 은 세션 시작·종료 시 모두 부재를 확인했다.
