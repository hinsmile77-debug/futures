# 미륵이 일일 점검 — MW0601 / 2026-08-20 / 장후

- 점검 시각: 16:22 KST · PC `MW0601` · 브랜치 `v9-dev` ✅(MW0601 규약 일치)
- HEAD `7a59796` · 미커밋 **2건**(CRLF 아티팩트 제외 후 실측) · 당일 커밋 2건
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260820_post.md`
- 세션 차수: **481차 후속2** (당일 장전=481차, 장중=481차 후속)
- 수집기 실행: 성공 (stderr 없음, 70.5KB)

> **미커밋 건수 정정** — 수집기 §2와 §11-14가 `461건`으로 보고했으나, 이는 코웍
> 리눅스 샌드박스가 CRLF 파일을 LF로 읽어 생긴 **전량 재작성 착시**다.
> 실측: `git diff --shortstat` = 439 files / +228,995 / −228,579(삽입≈삭제)
> → `git diff --ignore-cr-at-eol --shortstat` = **2 files / +416 / −0**
> (`dev_memory/DECISION_LOG.md` +262, `dev_memory/NEXT_TODO.md` +154 — 오늘 점검 append분).
> **작업 트리는 깨끗하다.** NEXT_TODO **F-5(수집기 CRLF 내성)** 기등록 항목의 근거다.

## 0. 한 줄 결론

**절대원칙 위반 0건 · P0 없음.** 하루 뼈대(08:55 매크로 → 09:00 개장 → 매분 9단계 371/371분
→ 15:10 청산창 FLAT → 15:18 안전망 → 15:40 마감 → 15:45 EOD 재학습 6/6 교체)는 전부 설계대로
돌았고, 손실 −348,018원은 **집행 실패가 아니라 3m 호라이즌의 손익비 비대칭**에서 나왔다.
다만 관측 계층에서 P1 3건이 남았고, 그중 둘은 어제·그제 등록분의 **재발 확인**이다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 정상 | 08:40:50 기동 · 08:45 스케일러 floor 6종 적용 · 08:55 매크로/구독 정상 (481차 리포트) |
| 장중 | 정상 | 매분 루프 09:00~15:10 **371/371분(100.0%)** · 10분 이상 공백 0건 · CB HALT 0회 |
| 장후 | **조건부 정상** | 15:10 청산창 FLAT(미발생이 정상) · EOD 6/6 교체 성공. 단 P1 3건(관측 훼손) |

**절대원칙 준수 여부**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | ✅ 준수 | 14:01:35 마지막 청산 이후 FLAT. `15:11:03 [SchedForceExit] 15:11 점검 — status=FLAT engine=0ct broker_cached=0ct bar_pass=2회 → 청산 대상 없음(정상)` · 프로세스 밖 F-2 가드 `[ForceFlatGuard] 2026-08-20 15:12:00 OK`. **안전망 ERROR 미출현**. ⚠ 실집행은 여전히 **누적 0회** (§0-2 참조) |
| ② | Circuit Breaker 5종 | ✅ 준수(예외 인지) | CB② 연속손절 4회까지 카운트·정지 없음 → `CB_CONSEC_STOP_LIMIT=9999` 모의 한정 예외(재검토 2026-08-29). CB⑤ 경고 8회(최대 3,183ms) 임계 5,000ms 미달로 미발동 — 정상. CB③ HALT는 코드상 의도적 비활성(이미 반영된 사안 §1 참조) |
| ③ | CORE 피처 호라이즌 그룹별 | ✅ 준수 | 진입 4건 전부 `entry_horizon ∈ {1m,3m,5m}` = 단기 그룹. `CORE VWAP ✗ → 강제 X등급 (group=short)` 정상 발동. 474차가 명시한 대로 mid/long 그룹은 구조적 도달 불가 — **오늘도 도달 0건, 예상대로** |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | ✅ 준수 | 0xC0000409 크래시 0건 · `crash_fault.log` 신규 기록 없음 · FaultHandler 08:40:50 활성(PID 13140) 종일 유지 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | ✅ 준수 | TR 수신 오류 0건 · `CybosInvestorRaw` 1,574건 정상 파싱 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | ✅ 준수 | 자동 통합 로그 0건 · 당일 커밋 2건 모두 `[MW0601]` 태그·수동 |

**설정 불변식** — 수집기 §3에서 `불일치` **0건**, `미발견` **5건**.
5건은 전부 **브랜치 스코프 문제**이며 이 PC의 회귀가 아니다 → §1 P1 `1-3`에서 해석한다.

**0-2. 실전 전환 기준 ② 진척 (오늘분)**

| 선행조건 | 오늘 | 누적 |
|---|---|---|
| ⓐ 15:10 강제청산 경로 **실집행** 1회 | 미발생(FLAT이라 정상) | **0회** — 2026-06-10 이래 변동 없음 |
| ⓑ 동결감시 → 하드종료 → 재기동 → 세션복원 **라이브 왕복** 1회 | 미발생(워치독 `fired=false` `strikes=0`) | **0회** |

⇒ ②는 오늘도 충족되지 않았다. **오늘 FLAT이었던 것은 설계가 막은 결과가 아니라
진입 4건이 전부 14:01 이전에 닫힌 결과**다(480차 F-2 항목과 같은 구조).

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**해당 없음.**

---

### P1 — 정확성·관측 훼손

#### 1-1. CB③ 판정 가능 여부를 로그로 재구성할 수 없다 — `acc30m=0.0%`가 "표본 없음"과 "적중 0건"을 같은 값으로 표현

- **증상**: `[DBG-CB]`가 종일 369분 기록됐는데 `acc30m`이 CB③ 임계 `0.28` 미만인 분이
  **236분(64.0%)** 이다. 그런데 `state=NORMAL`이 **369/369분**, HALT 0회다.
  두 사실을 로그만으로는 화해시킬 수 없다 — 그 분들이 "표본 부족이라 판정을 안 한 것"인지
  "판정했는데 통과한 것"인지 구분할 정보가 로그에 없다.

- **근거**:
  ```
  2026-08-20 09:01:02 [DEBUG] DEBUG: [DBG-CB] state=NORMAL consec_stops=0 acc30m=0.0% latency=0.000s
  2026-08-20 15:09:01 [DEBUG] DEBUG: [DBG-CB] state=NORMAL consec_stops=0 acc30m=10.0% latency=0.341s
  ```
  `logs/20260820_DEBUG.log` · 총 369행 · 09:01:02~15:09:01

  | acc30m 구간 | 분 수 | 비율 |
  |---|---|---|
  | `0.0%` (표본 없음 추정) | 107 | 29.0% |
  | `0 < acc < 28%` (임계 미만) | 129 | 35.0% |
  | `28% ≤ acc < 50%` | 127 | 34.4% |
  | `≥ 50%` | 6 | 1.6% |
  | **임계 0.28 미만 소계** | **236** | **64.0%** |

  버퍼 크기가 드러나는 유일한 로그는 재적합 시점뿐이다:
  ```
  09:37:00 [CB③] acc30m 버퍼 리셋 스킵 — 기존 표본 0건 < 최소 30건 (기아 방지, 표본 누적 계속)
  10:11:00 [CB③] acc30m 버퍼 리셋 스킵 — 기존 표본 14건 < 최소 30건
  11:02:00 [CB③] acc30m 버퍼 리셋 (스케일러 재적합 완료 — 이전 예측 무효화, 쿨다운=15샘플)
  11:41:03 [CB③] acc30m 버퍼 리셋 스킵 — 기존 표본 28건 < 최소 30건
  13:14:00 [CB③] acc30m 버퍼 리셋 (스케일러 재적합 완료 — 이전 예측 무효화, 쿨다운=15샘플)
  13:52:00 [CB③] acc30m 버퍼 리셋 스킵 — 기존 표본 9건 < 최소 30건
  ```
  → 버퍼는 하루 두 번(11:02·13:14) 리셋됐고 나머지 시점엔 30건에 못 미쳤다. 즉 **판정 가능
  구간이 매우 좁았을 가능성이 높은데, 그것을 확인할 계측이 없다.**

- **기전(코드 실측)**: `safety/circuit_breaker.py:618`
  ```python
  "accuracy_30m": round(sum(self._accuracy_buf) / max(len(self._accuracy_buf), 1), 3),
  ```
  분모의 `max(len, 1)` 때문에 **빈 버퍼가 조용히 `0.0`을 반환**한다.
  같은 `status_dict()`가 **`"cb3_samples": len(self._accuracy_buf)` 를 이미 내놓고 있는데**,
  소비자인 `main.py:7389`의 `[DBG-CB]` 포맷 문자열이 그 값을 출력하지 않는다.

- **기준 위반**: CLAUDE.md **계측 4원칙 ②**("미측정 ≠ 0 — `*_measured` 동반") 및
  **④**("폴백 가시화 — '표본 없음'과 '값이 0.5'를 같은 반환 타입으로 표현하지 마라").
  CLAUDE.md ④의 표에 이미 실린 `recent_accuracy()` → 조용히 0.5 사례와 **동일 패턴**이다.

- **영향**: 절대원칙 §2의 CB③이 하루 중 몇 분간 실제로 판정 가능 상태였는지 사후 재구성이
  불가능하다. FP-CRITICAL이 "저장 함수 미호출로 2개월간 PSI=0.0 고정"이었던 것과 같은
  형태의 침묵이며, 지금은 손해가 없지만 CB③ 기준 호라이즌 교체(전환기준 ⑥)를 판단할
  근거를 만들 수 없다.

- **신규 여부**: **기존 TODO** — `dev_memory/NEXT_TODO.md` 481차 후속 **G-3(다음 주, 선행 O-9)**.
  오늘 장후 관측 **O-9**가 그 선행이었고, 본 항목이 그 답이다. 신규 발견이 아니라
  **G-3의 근거를 당일 수치로 확정**한 것이며, 선행 조건이 해소됐으므로 **P2 → P1로 승격**을
  권고한다(§2 F-1).

---

#### 1-2. 수집기 §5가 부분청산 레그를 세지 않는다 — 장후 국면에서도 재발(2일차)

- **증상**: 증거 다이제스트 §5가 당일 성적을 **"청산 4건 · 승 1 (25%) · 합계 −4.47pt
  (−230,004원)"** 으로 냈다. DB 포지션 단위 실측은 **4포지션 2승 2패(50.0%) −348,018원**이다.

- **근거**: `data/db/trades.db` — 당일 레그 **7행** / 포지션 **4건**

  | 진입 | 진입수량 | 등급 | hz | 레그 | 포지션 net |
  |---|---|---|---|---|---|
  | 10:55:01 | 3 | A(원시 C) | 3m | 2 | **−269,884** |
  | 13:20:01 | 2 | C | 1m | 2 | **−147,268** |
  | 13:41:01 | 2 | A(원시 C) | 3m | 2 | **+46,752** |
  | 13:57:01 | 1 | C | 3m | 1 | **+22,382** |
  | **합계** | | | | **7** | **−348,018** |

  수집기가 센 것은 `체결청산` 4행뿐이고, **은닉된 3레그**는
  `손절1차 조기축소`(−122,256 / −45,134)와 `TP1 부분청산 33%`(+49,376) — 합 **−118,014원**이다.
  ```
  10:55:17 [손절1차 조기축소] 2계약 @ 1084.23 PnL=-1.19pt (-122,256원) 잔여=1계약
  13:20:30 [손절1차 조기축소] 1계약 @ 1088.18 PnL=-0.87pt (-45,134원) 잔여=1계약
  13:43:04 [TP1 부분청산] 1계약 @ 1083.72 PnL=+1.02pt (+49,376원) 잔여=1계약
  ```
  `logs/20260820_TRADE.log`
  → **당일 순손실의 33.9%가 다이제스트에서 사라졌고, 승률은 25%로 절반이 됐다.**
  검산: `−230,004 + (−118,014) = −348,018` = DB 포지션 합 = 전략 리포트 `net −348,017`
  (1원 반올림) — 완전 일치.

- **기준 위반**: CLAUDE.md **계측 4원칙 ①**("손익·승패 집계는 **포지션 단위가 기본**").
  417차가 이벤트 단위 사이징 통계 4종을 무효화한 것과 **같은 오류를 점검 도구가 반복**하고 있다.

- **영향**: 다이제스트만 보고 쓴 보고서는 손익을 34% 과소, 승률을 25%p 과소 보고한다.
  §11 자동 적신호 7번("하드스톱·손절 계열 4건(100%)")도 같은 이유로 틀렸다 — 실제 청산
  사유는 하드스톱 4 · 손절1차 조기축소 2 · TP1 부분청산 1이다.

- **신규 여부**: **재발(2일차)** — 481차 후속 **[1]**(장중, 2026-08-20)에서 처음 등록.
  Fix는 NEXT_TODO **F-1(P1, 장후)**, 고도화는 **G-1(포지션 조립기 1급 승격)**로 기등록.
  **오늘 장후 국면에서도 동일 결함이 재현됨을 확인**했다(장중 한정이 아니다).

---

#### 1-3. 설정 불변식 5건 `미발견`은 이 PC의 회귀가 아니라 브랜치 스코프 문제 — 8일차 미해소

- **증상**: 수집기 §3이 5개 상수를 `미발견 ⚠`로 찍고, §11 자동 적신호 **15칸 중 5칸(1~5번)을
  상시 점유**한다. 어제·그제도 같았다.

- **근거**: repo 전수 검색 결과 이 5개 이름은 **수집기 자신의 기대표에만 존재**한다.
  ```
  $ grep -rn "ZONE_ENTRY_BAN_ENFORCE" --include=*.py .
  ./.claude/skills/mireuk-daily-check/scripts/collect_evidence.py:183

  $ git grep -c "ZONE_ENTRY_BAN_ENFORCE" origin/dev     -- config/settings.py
  origin/dev:config/settings.py:2
  $ git grep -c "ZONE_ENTRY_BAN_ENFORCE" origin/v9-dev  -- config/settings.py
  (없음)
  ```
  5개 전부 동일: `MODEL_LABEL_STATE_UNLOCK_ENABLED` · `PRE_RETRAIN_DONE_BY_EOD_ENABLED` ·
  `ZONE_ENTRY_BAN_ENFORCE` · `ZONE_ENTRY_BAN_SHADOW_ENABLED` · `PIPE_LATENCY_EXCLUDE_MODEL_SWAP`.
  → **`origin/dev`(MW0602 운영 브랜치)에만 실재하고 `v9-dev`에는 없다.**

- **기준 위반**: CLAUDE.md 절대원칙 계열 위반은 아니다. 스킬 **함정 ③**(멀티PC·브랜치 상이)에
  해당하며, 수집기 기대표가 브랜치를 구분하지 않는다.

- **영향**: 두 가지다.
  1. **적신호 희석** — §11의 1/3이 매일 같은 5칸으로 채워져 진짜 적신호를 가린다.
     오늘도 실제로 봐야 할 것(6~8번)이 5칸 뒤에 밀렸다.
  2. **확인 필요(확정 아님)** — `ZONE_ENTRY_BAN_SHADOW_ENABLED`가 v9-dev에 없다면
     "집행과 무관하게 위반 계측은 항상 켜져 있어야 한다"는 462차 P1-a 취지가 **MW0601에서는
     성립하지 않는다**. 캠페인 채널 `[53]`이 MW0602 단독으로만 표본을 적립하고 있을 수 있다.
     → 표본 적립 주체 확인이 필요하다(§6 O-4).

- **신규 여부**: **기존 TODO · 8일차** — NEXT_TODO **F-4 (P2, 0819 F-1 이월 · 7일차)
  "수집기 브랜치 스코프 분리"**. 오늘로 **8일차**이며, 2-①의 희석 효과가 매일 누적되므로
  **P2 → P1 승격**을 권고한다(§2 F-2).

---

### P2 — 운영 부담·기술부채

#### 2-1. 메인 스레드 블로킹 5초 초과 4건(최대 8,375ms)이 어떤 안전장치의 사정권에도 없다

- **증상**: `_tick_header` 간격이 5,000ms를 넘은 사례가 4건, 최대 8,375ms인데
  CB⑤도 FZ-1 워치독도 발동하지 않았다. **둘 다 정상 동작이다** — 문제는 그 사이가 비었다는 것이다.

- **근거**:
  ```
  12:38:05 [LiveDBG] _tick_header 간격 5297ms — 메인 스레드 블로킹 발생 | pipe_elapsed=0 watchdog_alerted=[]
  12:40:08 [LiveDBG] _tick_header 간격 8375ms — 메인 스레드 블로킹 발생 | pipe_elapsed=0 watchdog_alerted=[]
  13:16:04 [LiveDBG] _tick_header 간격 5141ms — 메인 스레드 블로킹 발생 | pipe_elapsed=0 watchdog_alerted=[]
  13:54:04 [LiveDBG] _tick_header 간격 5047ms — 메인 스레드 블로킹 발생 | pipe_elapsed=0 watchdog_alerted=[]
  ```
  `logs/20260820_WARN.log` · 블로킹 총 25건 중 5초 초과 4건

  같은 시각 CB⑤가 재는 값은 전혀 다르다 — 당일 `[PipePerf]` 경고 8회 **최대 3,183ms**:
  ```
  12:40:03 [CB⑤] 파이프라인 2838ms 경고 (기준 1000ms)
  12:40:03 [PipePerf] total=2838ms | S0=2ms S1=562ms S2=16ms S3=0ms S4=808ms S5=1102ms S6=286ms S7=60ms S8=2ms
  13:15:03 [PipePerf] total=3183ms | S0=2835ms S1=25ms ...   ← 당일 최대
  ```

- **기준 위반**: CLAUDE.md **계측 4원칙 ①**(단위 명시). `CB_PIPE_PAUSE_MS = 5_000`은
  **파이프라인 경과시간**(`PipePerf.total`)에 걸리고, `_tick_header 간격`은 **메인 스레드
  전체 정지시간**이다. 4건 모두 `pipe_elapsed=0` — **파이프라인이 돌지 않는 동안** 메인
  스레드가 멈췄다는 뜻이므로 CB⑤는 **구조적으로 이 구간을 볼 수 없다.**
  FZ-1 워치독은 `FREEZE_WATCHDOG_STALL_SEC=180`이라 8.4초는 당연히 못 잡는다.
  ⇒ **5초~180초 구간은 주인이 없다.**

- **영향**: 오늘은 FLAT 구간(12:38~13:16)과 포지션 없는 구간에 걸려 실손해 0이다.
  그러나 2026-08-19 동결(480차)이 정확히 "프로세스는 살아 있는데 메인 스레드가 멈춤"이었고,
  그때 실손해가 0이었던 것도 **우연히 FLAT이었기 때문**이다. 같은 무관측 구간이 남아 있다.

- **NEXT_TODO 전제 정정**: **O-10의 전제가 틀렸다.**
  > O-10 (장후) `_tick_header` 블로킹 종일 분포 — … **5,000ms 초과 1건이라도 나오면
  > CB⑤ 실발동 — 사유 추적 필수.**

  오늘 4건 나왔지만 CB⑤는 발동하지 않았고, **발동하지 않는 것이 설계상 옳다**.
  `_tick_header 간격 ≥ 5,000ms`는 CB⑤ 발동의 필요조건도 충분조건도 아니다.
  → O-10 문언을 폐기하고 §2 F-3으로 대체한다.

- **신규 여부**: **기전은 신규**(계측 단위 불일치의 확인은 오늘이 처음).
  현상 자체는 O-10으로 기등록, 480차 동결 사고와 같은 계열.

#### 2-2. `ConstOut ['3m']` 6회 — O-11 임계 도달했으나 상향 근거로는 약하다

- **증상**: `[ConstOut] ['3m'] 상수 출력 확정` **6회**(09:36 / 10:10 / 11:01 / 11:40 / 13:13 / 13:51),
  종일 9분. **전량 3m 단일 호라이즌.**
- **근거**: `logs/20260820_WARN.log` · `[ModelHealth] ConstOut 6회/9분 {"3m": {"events": 6, "minutes": 9}}`

  | 일자 | 유효가동률 | ConstOut | 분포 | WeightCollapse |
  |---|---|---|---|---|
  | 08-12 | 75.1% | 8회/14분 | 3m 3 · 5m 5 | 78분 |
  | 08-13 | 78.4% | 2회/3분 | 3m 1 · 5m 1 | 77분 |
  | 08-14 | 76.7% | 5회/7분 | 3m 5 | 79분 |
  | 08-18 | 76.5% | 6회/8분 | 3m 5 · 5m 1 | 79분 |
  | **08-20** | **76.2%** | **6회/9분** | **3m 6** | **79분** |

- **판정**: O-11이 정한 "6회 이상이면 08-14 F-8 재발 강도 상향" 임계를 수치상 충족하나,
  **직전 밴드(2~8회) 안이고 유효가동률도 밴드 안(75.1~78.4%)** 이다.
  **강도 상향 근거로는 약하다 — 관찰 유지.** 다만 **3m 단독 집중**은 08-14 이후 두 번째이며,
  아래 §5 R-1(3m 손익비)과 같은 방향을 가리킨다.
- **신규 여부**: 기존 관측 항목 O-11.

---

### 확인 필요 (확정 아님)

- **`[GuardGhost] 3m` — EOD 비교 기준이 유령**
  ```
  15:46:10 [GuardGhost] 3m 비교 기준이 유령이다 — 배포된 pkl은 CV 미검증 intraday 모델
  (학습 2026-08-20 13:21:00까지)인데 acc.txt=0.4217는 다른 모델의 성적이다.
  15:46:10 [GuardGhost] 3m 가드 미적용 — … CV 검증된 신규 모델(cv=0.4048 / 최신OOS=0.3508)로
  교체한다 [457차 F7 ⑤안]
  ```
  457차 F7 ⑤안대로 **설계된 경로로 처리**됐다(교체 강행). 다만 3m은 당일 장중 재학습 6회가
  전부 3m이라 이 상황이 **구조적으로 반복**될 수 있다. 며칠 연속인지 확인 필요.
- **3m 라이브 적중률이 전 호라이즌 중 최저** — `[ModelLive]` 당일:
  1m 0.3533(n=368) · **3m 0.2828(n=244)** · 5m 0.3103(n=145) · 10m 0.3099(n=71) · 15m 0.2128(n=47).
  하루치이므로 **313차 원칙에 따라 확정 결론 보류.** 단 §5 R-1의 30거래일 손익과 부호가 같다.
- **`ZONE_ENTRY_BAN_SHADOW_ENABLED` 부재의 실효** — 1-3 참조. MW0601이 zone-ban 위반 표본을
  적립하지 않고 있다면 캠페인 채널 `[53]`은 MW0602 단독 적립이다. 확인 전까지 판정하지 않는다.
- **개장 첫봉 z 폭발** — `institution_futures_net` σ_floor 0.15는 08:45:03에 6개 호라이즌
  전부 **실적용 확인**(O-3 충족). 그럼에도 09:00 봉에서 `max_z=-15.07`가 났고,
  **09:01:00 한 시점에만 출현하고 재발하지 않았다**(O-2 충족: 개장 첫봉 한정).
  → floor가 z 폭발을 막지 못한다는 뜻이므로 NEXT_TODO **G-2**(개장 첫봉 z 프로파일 상설 계측)의
  근거가 보강됐다. 단일 관측이라 임계 재설계는 보류.

---

### 이미 반영된 사안 (신규 아님 — 함정 ① 방지)

- **CB③ HALT 트리거 자체가 코드에서 비활성** — `safety/circuit_breaker.py:315~334`.
  임계 `0.28`은 살아 있으나 미달 시 `logger.debug("[CB③ 비활성] …")`만 찍고 HALT/경고를
  내지 않는다(250차 → **296차 30m 전면 퇴역**으로 확정). `_accuracy_buf` 누적·P4 stage 추적은
  모니터링용으로 유지. **`dev_memory/DECISION_LOG.md` 25183·25490행에 이미 기록**돼 있고
  CLAUDE.md 등재는 **F-2로 기등록**. → **재상정 금지.** 오늘 `acc30m < 0.28` 236분에도
  HALT가 없는 1차 원인이 이것이다(1-1은 그와 별개로 "판정 가능 여부를 못 본다"는 문제).
- **CB② 연속 손절 4회 · 정지 없음** — `CB_CONSEC_STOP_LIMIT = 9999`(모의투자 한정 예외,
  CLAUDE.md 절대원칙 §2). 카운터 로그가 보이는 것이 정상. 복원 재검토 **2026-08-29**.
- **`MAX_CONTRACTS = 3`** — 431차 배포분. **재상정 금지.**
- **전략 상태 경보 `UNDERPERFORM` / "🔄 교체 후보 탐색"** — 판정은 조치 여부와 무관하게
  매일 재계산된다(**함정 ①: 판정 ≠ 결정**). MDD는 **자본 대비 3.0%** 로 기준 15% 안이며,
  `MDD(peak대비)=274.2%`는 461차가 판정에 쓰지 말라고 못박은 하위호환 값이다 — **인용 금지.**
  Sh=0.32 · 롤링20일 누적 +198,565원.
- **`TOXICITY_SEVERE_SPREAD_BLOCK_ENABLED = False`** — 473차 F-8 Phase A가 표본 ETA
  7.1개월로 `INSUFFICIENT` 판정. 오늘 진입 4건의 `spread_ticks`는 5.9998 / 3.0029 / 3.9978 /
  5.9998로 **전부 8틱 미만**, `spread_extreme_shadow` 발화 0건. 표본 증가 없음.

---

### 정상 확인 (이상점 아님 — 재상정 방지용 기록)

1. **EOD 재학습 성공** — `data/eod_retrain_done_20260820.txt`:
   `horizons_replaced: 6/6` · `rows: 40395` · `cols: 97` · `t_total_s: 237.0` ·
   `daily_close_seen: true` · `wait_dc_timeout: false`.
   `Python : 3.10.20 64-bit` = **py310_64 정상**(191차 결정, 재거론 금지).
   P8 스케일러 재적합 6호라이즌 완료(15:49:02), `p8_last_success_date` 기록 확인.
2. **매분 루프 커버리지 09:00~15:10 371/371분 (100.0%)**, 08:55~15:12 10분 이상 공백 0건.
   로그 종료 15:40 = 직전 5거래일 중앙값과 **델타 +0분**.
3. **FZ-1 워치독 오탐 0건** (O-5 충족) — `data/heartbeat_MW0601_20260820.json`:
   `"strikes": 0` · `"fired": false` · `"watching": true` · `beat_age_sec 3.8`.
4. **F-2 프로세스 밖 FLAT 가드 라이브 정상** — `logs/force_flat_guard_20260820.log`
   `[ForceFlatGuard] 2026-08-20 15:12:00 OK … 하트비트: 파일 14s 전 기록 · 이벤트루프 나이 3s`.
   480차 후속4(`7a59796`)의 "감시 개시를 파일에 남긴다"가 **라이브에서 첫 확인**됐다.
5. **1계약 TP1은 부분청산이 아니라 보호전환이 설계다 — 결함 아님.**
   CASE-04에서 `[TickTP1] qty=1` 뒤에 부분청산 체결이 없어 유실로 보이지만, 실제로는
   ```
   14:00:52 [SingleContractTP1] 1계약 TP1 도달 -> 보호전환 LONG mode=atr_profit price=1079.36 stop=1075.86->1078.87
   ```
   손절선을 −2.58pt에서 **+0.43pt로 끌어올려** 최종 +0.48pt를 확보했다. 정상 동작.
6. **`손절1차 조기축소`는 손실을 줄이는 방향으로 작동했다.** CASE-01에서 2계약을 1084.23에
   정리(−1.19pt)한 뒤 잔여 1계약이 1082.70 하드스톱(−2.92pt). 전량을 하드스톱까지 끌었다면
   gross −438,000원, 실제 gross **−265,000원** — 약 **173,000원 절감**.
7. **F-8 Phase B 배선 3거래일차 무결** — `ensemble_decisions.spread_ticks` /
   `.spread_extreme_shadow` 당일 진입 4건 전량 적재, NULL 0건.
8. **3원 대사 일치**(§4-2) · **`sizing_trace` 정상 적재**(471차 후속6 G-1).
9. **CB⑤ 미발동이 정상** — 당일 최대 파이프라인 3,183ms < 임계 5,000ms.
   `[PipePerf]` 스파이크 8건 중 6건이 `S0`(모델 리로드) 단독 — CLAUDE.md가 기술한 정례 프로파일과 일치.

---

## 2. Fix 작업 구현계획

> **본 세션은 예약작업이며 코드를 변경하지 않았다.** 아래는 계획이다.
> 사용자가 승인하면 별도 세션에서 구현한다.

### F-1. `[DBG-CB]`에 표본 수·판정가능 여부 병기 — P1 · 대응 이상점 1-1

- **원인 가설**: `status_dict()`가 이미 `cb3_samples`를 내놓는데 소비자가 출력하지 않는다.
  `accuracy_30m`의 분모 `max(len,1)`이 빈 버퍼를 `0.0`으로 위장한다.
- **변경 파일**:
  - `main.py:7388~7393` — `[DBG-CB]` 포맷에 `n=%d ready=%s` 추가.
    `n = _cb["cb3_samples"]`, `ready = (n >= CB_ACC30M_MIN_SAMPLES)`.
    `n == 0`이면 `acc30m=—`(하이픈)로 출력해 **"표본 없음"과 "적중 0%"를 문자로 구분**한다.
  - `safety/circuit_breaker.py:618` — `accuracy_30m`을 버퍼가 비었을 때 **`None` 반환**으로
    바꾸고, 기존 float 소비자를 위해 `accuracy_30m_or_zero`를 **하위호환 별칭**으로 남긴다.
    (461차 `mdd_pct` → `mdd_pct_of_capital` 전환과 동일 패턴)
  - `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py` §5 —
    `CB③ ready 분 / 장중 분` 한 줄 추가.
- **회귀 위험**: `accuracy_30m`을 float로 가정하는 소비자(대시보드 패널·`daily_stats` 저장·
  `to_state_dict`)가 `None`에서 깨질 수 있다. → **별칭을 먼저 배선하고 소비자를 하나씩 이관**한다.
  1단계(로그 병기)만 먼저 넣으면 회귀 위험 **0**이다.
- **절대원칙 저촉 여부**: 없음. **판정 로직을 바꾸지 않는다**(계측만). 임계 `0.28` 무변경.
- **런타임 주의**: `main.py`·`safety/`는 **py37_32** 런타임. COM 콜백 밖이므로 절대원칙 ④ 무관.
- **검증**: `tests/test_457_fallback_visibility.py` 확장 — `accuracy_30m`이 빈 버퍼에서
  `0.0`을 반환하지 않는지 단위 테스트. 다음 거래일 `[DBG-CB]`에 `n=`·`ready=`가 369행 전부
  찍히고, `n=0` 구간이 `acc30m=—`로 나오는지 확인.
- **섀도 계측 필요 여부**: 불필요(게이트 신설 아님).
- **적용 시점**: **다음 기동**(장 마감 후 배포 → 익일 09:00부터 관측).
- **결정 필요 사항**: 없음. 단 **G-3(NEXT_TODO)의 우선순위를 "다음 주" → "이번 주"로 승격**할지
  결정 필요 — 선행 O-9가 오늘 해소됐으므로 승격 권고.

### F-2. 수집기 설정 불변식 표를 브랜치별로 분기 — P1 · 대응 이상점 1-3

- **원인 가설**: 기대표가 단일 리스트라 `dev`/`v9-dev` 공통분과 브랜치 전용분이 섞여 있다.
- **변경 파일**:
  - `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py:170~190` —
    각 항목에 `"branches": ["dev"]` 또는 `["dev","v9-dev"]` 키를 추가하고,
    `git branch --show-current` 결과와 교집합이 없으면 **표에서 제외**한다.
    제외분은 별도 소절 `_이 브랜치 범위 밖 N건: …_`로 **개수와 이름을 남긴다**
    (CLAUDE.md 계측 4원칙 ③ 탈락 가시화 — `… 외 N개` 규약).
  - 위 5개 상수는 `["dev"]`로 지정.
  - §11 자동 적신호 생성부 — 브랜치 범위 밖 항목은 적신호로 승격하지 않는다.
- **회귀 위험**: MW0602(`dev`)에서는 표가 그대로여야 한다 → 기본값을 `["dev","v9-dev"]`로 두어
  **미지정 항목은 종전과 동일** 동작.
- **절대원칙 저촉 여부**: 없음(점검 도구, 라이브 경로 아님).
- **런타임 주의**: 수집기는 표준 라이브러리만 쓰므로 py37/py310 무관.
- **검증**: 양 브랜치에서 `--phase post` 실행 → v9-dev는 `미발견` **0건**,
  dev는 종전과 동일 5행 유지. `docs/정기점검/매일점검/evidence_MW0601-*_post.md` §11이
  15칸 → **10칸**으로 줄어드는지 확인.
- **적용 시점**: 즉시 가능(라이브 프로세스와 무관).
- **결정 필요 사항**: 5개 상수를 **v9-dev에도 이식할 것인지**는 별개 안건이다.
  특히 `ZONE_ENTRY_BAN_SHADOW_ENABLED`(섀도 계측)는 462차 취지상 양 PC에 있어야 하므로
  **주간회의 상정 권고**(§3 로드맵 표 참조).

### F-3. 메인 스레드 정지 전용 임계 신설 — P2 · 대응 이상점 2-1

- **원인 가설**: CB⑤(파이프라인 5초)와 FZ-1(하트비트 180초) 사이 구간에 감시자가 없다.
- **변경 파일**:
  - `config/settings.py` — 신설 `MAIN_THREAD_STALL_WARN_MS = 5_000`,
    `MAIN_THREAD_STALL_ALERT_MS = 15_000`, `MAIN_THREAD_STALL_BLOCK_ENABLED = False`
    (**섀도로 시작 — 차단하지 않는다**).
  - `main.py` `_tick_header` 블로킹 계측부 — 임계 초과 시 `[MainStall]` 태그로
    `gap_ms` · `pipe_elapsed` · `in_pipeline`(bool) · `position_status`를 함께 남긴다.
    `pipe_elapsed=0`인 건은 **"파이프라인 밖 정지"** 로 별도 카운트.
  - 수집기 §5 — `메인 스레드 정지: 파이프라인 내 N건 / 밖 M건 (최대 X ms)` 로 **단위 분리 표기**.
- **회귀 위험**: 계측만 추가하므로 낮음. 로그량 증가는 오늘 기준 25행/일로 무시 가능.
- **절대원칙 저촉 여부**: 없음. **CB⑤의 임계·판정은 건드리지 않는다**(절대원칙 §2 불변).
- **런타임 주의**: py37_32. **COM 콜백 안에 넣지 말 것**(절대원칙 ④) —
  `_tick_header`는 Qt 타이머 슬롯이므로 콜백 밖임을 확인했다.
- **검증**: 2주 섀도 후 "파이프라인 밖 정지" 분포를 보고 `MAIN_THREAD_STALL_ALERT_MS`를
  실측 기반으로 재설정. 차단 승격은 **그 다음 판단**이며 주간회의 안건.
- **섀도 계측 필요 여부**: **필요**(게이트 성격 → 스킬 규약 7번).
- **적용 시점**: 다음 기동.
- **결정 필요 사항**: **NEXT_TODO O-10 문언 폐기 승인**(전제가 틀렸다 — §1 2-1).

### F-4. (기등록) 수집기 §5 포지션 단위 집계 — P1 · 대응 이상점 1-2

NEXT_TODO **F-1(P1, 장후)** + **G-1(포지션 조립기 1급 승격)** 그대로다. 오늘 새로 더할 것은
**장후 국면에서도 재현됨**과 검산식뿐이다:
> `§5 합계 + 은닉 레그 합 = DB 포지션 합 = 전략 리포트 net` (오늘: −230,004 + −118,014 = −348,018 ✓)
이 등식을 **수집기 자체 정합성 체크**로 넣으면 회귀를 자동 검출할 수 있다
(§5에 이미 있는 `정합성: OK (칸합계·진입·JointGateBlock 3종 일치)`와 같은 자리).

### 적용 순서와 커밋 계획

1. **커밋 ①** — F-2(브랜치 스코프) + F-4(포지션 조립기·정합성 등식). 둘 다 수집기 단독,
   라이브 무영향 → 먼저 넣어 **내일 점검부터 올바른 다이제스트**를 받는다.
   `[MW0601] 482차: 점검 수집기 — 브랜치 스코프 분리 + 포지션 단위 집계 + 정합성 등식`
2. **커밋 ②** — F-1 1단계(로그 병기만). 라이브 코드지만 계측 전용.
   `[MW0601] 482차 후속: CB③ 판정가능 여부 가시화 — [DBG-CB] n·ready 병기`
3. **커밋 ③** — F-3 섀도 계측. `[MW0601] 482차 후속2: 메인 스레드 정지 섀도 계측 (차단 없음)`
4. **커밋 ④**(선택) — F-1 2단계(`accuracy_30m` → `None` + 별칭). 소비자 이관 완료 후.

---

## 3. 고도화 방안

### G-1. CB③ 판정 가능 시간을 일일 지표로 승격

- **관측 근거**: 오늘 `acc30m < 0.28`이 **236분(64.0%)** 인데 HALT 0회, 그리고 그 이유를
  로그로 재구성할 수 없었다(§1 1-1). 버퍼 리셋 로그로 역산하면 판정 가능(n≥30) 구간이
  하루 중 **짧았을 가능성이 높지만 확인이 불가능**하다.
- **제안 내용**: `[ModelHealth]` EOD 한 줄에 `CB③ ready=N분/369분` 을 추가하고,
  `daily_stats`에 `cb3_ready_minutes` 컬럼을 둔다. **3거래일 연속 ready < 50%면 §11 적신호.**
- **기대 효과**: 정량. "CB③이 실제로 살아 있는 시간"이 처음으로 시계열이 된다 —
  전환기준 ⑥(CB③ 기준 호라이즌 교체)을 논의할 근거가 생긴다.
- **비용·위험**: 낮음. 계측 전용, 판정 무관여. 섀도 불필요.
- **선행 조건**: **F-1**(로그 병기).
- **우선순위 제안**: **이번 주** (NEXT_TODO G-3을 이 형태로 확정 — 선행 O-9 해소됨).

### G-2. 스케일러 재적합 ↔ CB③ 버퍼의 상호작용 계측

- **관측 근거**: 오늘 `ConstOut` 6회가 전부 3m에 몰렸고, 그 재적합이 CB③ 버퍼 리셋 판단을
  **6회 유발**했다(실제 리셋 2회 / 기아방지 스킵 4회). 재적합이 잦을수록 CB③은 표본을
  못 채우고, 표본을 못 채우면 판정이 안 된다 — **두 계측이 서로를 침식**하는데 그 관계가
  어디에도 기록되지 않는다.
- **제안 내용**: `[ConstOut] … → acc30m 버퍼 리셋` 로그에 **리셋 직전 버퍼 크기와 직전
  ready 지속 분**을 병기하고, EOD에 `재적합 N회 → CB③ 표본 손실 M건`을 집계한다.
- **기대 효과**: 정량. "3m 재적합 빈도가 CB③ 가용성을 얼마나 깎는가"를 처음 수치화한다.
- **비용·위험**: 낮음(로그 필드 추가). 섀도 불필요.
- **선행 조건**: G-1.
- **우선순위 제안**: 다음 주.

### G-3. 진입 후보 시간 하한 미달 경보의 판정 근거 보강

- **관측 근거**: `⚠ mc-conf 괴리: 최근 5거래일 평균 진입후보 53분/일 < 하한 60분 — 금일 62분`
  (15:40:06 WARN). 오늘은 62분으로 하한을 넘겼는데도 5일 평균이 미달이라 경보가 떴다.
  같은 날 `[ConfFloorGuard] 자동진입 하한 도달 불가 — 보정기 출력상한 0.3528 < 필요 0.3790`
  (09:06:00)가 **1회** 발생해, 그 시점엔 어떤 신호도 진입 하한을 넘을 수 없었다.
- **제안 내용**: 진입 후보 분 집계에서 **`ConfFloorGuard` 발동 구간을 분리 카운트**한다
  (`후보 62분 (그중 도달불가 N분)`). 지금은 "기회가 없었다"와 "구조적으로 불가능했다"가
  같은 숫자에 섞여 있다 — 계측 4원칙 ②의 확장.
- **기대 효과**: 5일 평균 하한 경보의 오탐/정탐을 구분할 수 있다.
- **비용·위험**: 낮음.
- **선행 조건**: 없음.
- **우선순위 제안**: 다음 주.

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| 전환기준 ② ⓐ (15:10 실집행) | 미충족 · 누적 **0회** | 변경 없음 — **F-1R 리허설 실행 촉구** | 오늘도 FLAT이라 미발생. 표본을 기다려서는 영원히 안 온다(진입이 전부 14:01 이전에 닫힘) |
| 전환기준 ② ⓑ (동결 왕복) | 미충족 · 누적 **0회** | 변경 없음 | FZ-1 `fired=false` 유지 중 |
| 전환기준 ⑥ (CB③-P4) | 문언 확장 기등록 | **"CB③ ready 시간이 장중 50% 이상일 것"을 판정 전제로 추가** 검토 | 판정 입력이 존재하지 않는 시간에 임계만 논하는 것은 무의미(§1 1-1) |
| 계측 4원칙 ① 적용범위 | 주간회의 기등록 | **유지 — 근거 1건 추가** | 오늘 §5 재발(1-2)과 O-10 전제 오류(2-1)가 둘 다 "단위 불일치"다 |
| `ZONE_ENTRY_BAN_SHADOW_ENABLED` 양 PC 배선 | 미등록 | **주간회의 신규 상정** | 462차 "집행과 무관하게 항상 켜져 있어야 한다"가 MW0601에서 성립하지 않을 수 있다(1-3) |

---

## 4. 진입 승패 사후검증 (장후 한정)

**4-0. 선행 확인** — EOD `daily_close` **완료**(`data/daily_close_done_20260820.txt`
`2026-08-20T15:40:07`) · **P8 완료**(`[P8] session_state p8_last_success_date +
eod_retrain_ok_date 기록 완료`, 15:49:02). DB 접근·수집기 실행 실패 없음.
현재 16:22 KST = **15:35 이후이므로 라이브 DB 분석 차단 구간 아님**(CLAUDE.md 456차).

**4-1. 오늘 할일 4분류**

| # | NEXT_TODO 항목 | 당일 커밋 | 코드 실재 확인 | 상태 |
|---|---|---|---|---|
| 1 | 480차 후속4 — F-2 가드 감시 개시 파일 기록 | `7a59796` | `scripts/force_flat_guard.py` · 로그 실증 | ✅완료-커밋확인 |
| 2 | 473차 F1~F3 검증 — F-8 Phase B 라이브 확인 | `f94536f` | `ensemble_decisions.spread_ticks` 적재 확인 | ✅완료-커밋확인 |
| 3 | 481차 F-1 수집기 §5 부분청산 레그 합산 | 없음 | 미구현(§1 1-2 재현) | ⬜미착수 — 장후 적용 예정분, **익일 이월** |
| 4 | 481차 F-2 스킬 참조문서 461·474차 정합 | 없음 | 미구현 | ⬜미착수 — 익일 이월 |
| 5 | 481차 F-3 CB③-P4 판정 입력 결정(조사만) | 없음 | — | 🔄범위변경 → §1 1-1로 흡수, F-1으로 재정의 |
| 6 | 0819 F-1 / 481차 F-4 수집기 브랜치 스코프 | 없음 | 미구현(**8일차**) | ⬜미착수 → §1 1-3에서 **P1 승격 권고** |

> ⚠ 3·4·6은 "장후 적용" 예정분이었으나 **본 세션은 예약작업이라 코드를 변경하지 않는다**
> (스킬 코드 변경 정책). 따라서 미착수는 정책상 정상이며, 익일 사용자 지시 세션으로 이월한다.
> 다만 **6번은 8일차**이므로 §1 P1로 올렸다.

**4-2. 3원 대사 (포지션 단위)**

| 원천 | 질의 | 값 |
|---|---|---|
| 로그 | `[Position] 진입` | **4** |
| `ensemble_decisions` | `entry_executed=1` | **4** |
| `trades` | `COUNT(DISTINCT entry_ts)` | **4** |
| (참고) `trades` | `COUNT(*)` — 청산 레그 | 7 |

→ **세 값 일치. 레그 7 > 포지션 4는 불일치가 아니다.**
부수 확인: `ensemble_decisions` 당일 369행 · `weight_collapsed=1` **79행(21.4%)** —
CLAUDE.md가 기술한 "매일 약 21~22%" 범위 내. `sizing_trace` 4건 전량 적재.

**4-3. 케이스 카드**

```
[CASE-01] 진입 10:55:00 LONG · 3계약 (등급 A / 원시 C · horizon=3m · hurst=neutral)
          → 손절1차 조기축소 2ct @1084.23 (-1.19pt) → 하드스톱(틱) 1ct @1082.50 (-2.92pt)
          → 포지션 합 -5.30pt / -269,884원 · 보유 65초
 진입근거   [진입체크] sign✅ conf✅ vwap✅ cvd✅ ofi✅ fore❌ prev✅ time✅ risk✅ chas❌ coun✅ | conf=39.7%
            [Position] 손절=1082.83 1차=1086.44(×0.50) 2차=1088.25
            [Sizer] 실효잔고=50,000,000 기본리스크=1,500,000 신뢰도=0.6 레짐=1.0 안전=1.00 → 3계약
            sizing_trace: qty_sizer_raw=3 → qty_auto=3 · binding_gate=null · meta_size_sizing=1.0
                          (431차 무정보 폴백 중립화가 작동 — meta_size_raw 0.5가 사이징에 미반영)
 예측 vs 실현  conf=0.397 vs min_conf=0.370 (여유 +0.027) · spread_ticks=5.9998 (<8, 정상)
 승패판정   ❌패 — tp1_reached=**False** (1차 1086.44 미도달, 고가가 진입가 부근)
 주 요인    D. 진입 직후 역행 — 진입 1085.54 → 65초 만에 손절선 1082.83 관통
 부 요인    ① `fore`(외국인) 미통과 상태로 진입 ② 3계약(당일 최대)이 최악 케이스에 배정
 반사실     조기축소가 없었다면 gross -438,000원. **조기축소가 173,000원을 지켰다** — 집행은 잘했다.
            방향 판단이 틀린 것이지 집행이 틀린 것이 아니다.
```

```
[CASE-02] 진입 13:20:00 LONG · 2계약 (등급 C / 원시 C · horizon=1m · hurst=neutral)
          → 손절1차 조기축소 1ct @1088.18 (-0.87pt) → 하드스톱(틱) 1ct @1087.04 (-2.01pt)
          → 포지션 합 -2.88pt / -147,268원 · 보유 170초
 진입근거   [진입체크] sign✅ conf✅ vwap✅ cvd❌ ofi❌ fore❌ prev❌ time✅ risk✅ chas✅ coun✅ | conf=39.5%
            [Position] 손절=1087.18 1차=1089.34(×0.30) 2차=1090.78
            sizing_trace: qty_sizer_raw=3 → qty_auto=2 · binding_gate="meta"(0.631)
 예측 vs 실현  conf=0.3947 vs min_conf=0.366 (여유 +0.029) · spread_ticks=3.0029
 승패판정   ❌패 — tp1_reached=**False** (1차 1089.34까지 0.36pt인데 미도달)
 주 요인    D. 진입 직후 역행
 부 요인    체크리스트 **4개 미통과**(cvd·ofi·fore·prev)로 pass_count=7 — 당일 진입 중 최저 근접
 반사실     체크리스트 미통과 4개면 진입을 보류했을 경우 -147,268원 회피.
            ⚠ 다만 CASE-04도 미통과 4개인데 승리했다 — **표본 2건으로 규칙을 만들지 말 것(313차).**
```

```
[CASE-03] 진입 13:41:01 LONG · 2계약 (등급 A / 원시 C · horizon=3m · hurst=trend)
          → TP1 부분청산 33% 1ct @1083.72 (+1.02pt) → 하드스톱(틱) 1ct @1082.68 (-0.02pt)
          → 포지션 합 +1.00pt / +46,752원 · 보유 177초
 진입근거   [진입체크] sign✅ conf✅ vwap✅ cvd✅ ofi✅ fore❌ prev✅ time✅ risk✅ chas✅ coun✅ | conf=36.8%
            [Position] 손절=1080.16 1차=1083.47(×0.60) 2차=1085.12  (pass_count=10 — 당일 최고)
            sizing_trace: qty_sizer_raw=3 → qty_auto=2 · binding_gate="meta"(0.562)
 예측 vs 실현  conf=0.368 vs min_conf=0.366 (여유 +0.002 — 당일 최소 마진)
 승패판정   ✅승 — tp1_reached=**True**. TP1 후 잔여 1계약은 본전 부근 트레일 스톱에서 정리
 주 요인    A. TP1 도달 후 트레일이 이익을 지켰다 (설계대로)
 부 요인    2차(1085.12) 미도달 — 3m TP2까지 가는 추세는 아니었다
 반사실     TP1이 없었다면 잔여 전량이 -0.02pt 부근 → 포지션 합 약 -2,000원. **TP1이 승패를 갈랐다.**
```

```
[CASE-04] 진입 13:57:01 LONG · 1계약 (등급 C / 원시 C · horizon=3m · hurst=trend)
          → 하드스톱(틱) 1ct @1078.92 (+0.48pt)  ※ 이익 상태의 트레일 스톱 청산
          → 포지션 합 +0.48pt / +22,382원 · 보유 274초
 진입근거   [진입체크] sign✅ conf✅ vwap✅ cvd❌ ofi❌ fore❌ prev❌ time✅ risk✅ chas✅ coun❌ | conf=38.9%
            [Position] 손절=1075.94 1차=1079.38(×0.60) 2차=1081.10  (pass_count=6 — 당일 최저)
            sizing_trace: qty_sizer_raw=1 · kelly_mult=0.1 · **kelly_advised_skip=1**
 예측 vs 실현  conf=0.389 vs min_conf=0.366 · spread_ticks=5.9998
 승패판정   ✅승 — tp1_reached=**True**(14:00:52 tick=1079.36 ≥ tp1=1079.30).
            1계약이라 부분청산이 아니라 **SingleContractTP1 보호전환**: stop 1075.86 → 1078.87
 주 요인    A. TP1 도달 → 보호전환이 이익을 확정
 부 요인    KellySkip 권고(=1)를 무시하고 1계약으로 진입 — **결과는 승**
 반사실     보호전환이 없었다면 손절선 1075.94까지 열려 있었다. 설계가 +22,382원을 지켰다.
            ⚠ 캠페인 채널 [8] KellySkip 표본 **+1건(승)**. 여전히 min_samples 미달 — **판정 금지.**
```

**4-4. 요인 집계**

| 요인 | 승 | 패 | 순손익 | 평균 보유(분) | 관측 거래일 수 |
|---|---|---|---|---|---|
| A. TP1 도달 → 이익 확정(부분청산 또는 보호전환) | 2 | 0 | **+69,134** | 3.8 | 1 |
| D. 진입 직후 역행 → 손절 계단 | 0 | 2 | **−417,152** | 2.0 | 1 |
| **합계** | **2** | **2** | **−348,018** | 2.9 | 1 |

보조 축(당일 4건 전량 LONG · 전량 `SYSTEM_AUTO` · 전량 `meta_action='reduce'`):

| 축 | 승 | 패 | 순손익 |
|---|---|---|---|
| `tp1_reached=True` | 2 | 0 | +69,134 |
| `tp1_reached=False` | 0 | 2 | −417,152 |
| horizon 3m | 2 | 1 | −200,750 |
| horizon 1m | 0 | 1 | −147,268 |
| 등급 A(원시 C) | 1 | 1 | −223,132 |
| 등급 C(원시 C) | 1 | 1 | −124,886 |

**4-5. 313차 가드 통과 여부**

| 갈래 | 적용 결과 |
|---|---|
| ① 일자단위 판정 | **관측일 1일 · n=4포지션 → 당일 단독 판정 불가.** 아래 R-1만 30거래일 표본으로 승격 |
| ② overlap 보정 | 4포지션이 서로 겹치지 않음(10:55 / 13:20 / 13:41 / 13:57, 최장 보유 274초) — 보정 불요 |
| ③ 이상치 분해(drop-worst) | CASE-01(−269,884) 제거 시 3포지션 2승 **−78,134원** — **부호 유지**(여전히 적자) |
| ④ 사전등록 — 기준 불변 | 캠페인 합격선·`min_samples` **무변경**. [8] KellySkip 표본 +1만 갱신, 문턱 인하 없음(458차 D6 준수) |
| ⑤ 부호 일관성(기간·PC 교차) | R-1의 3m 적자는 07-21~ 80포지션과 08-05~(431차 이후) 51포지션에서 **모두 음수** — 통과. PC 교차는 MW0602 리포트 대조 필요(금요일점검) |

---

## 5. 수익률 향상방안 (장후 한정)

### R-1. 3m 호라이즌은 승률이 아니라 **손익비**가 문제다

| 칸 | 내용 |
|---|---|
| **근거** | CASE-01(3m 패 −269,884) · CASE-03(3m 승 +46,752) · CASE-04(3m 승 +22,382) · 요인 D.<br>당일 3m 3포지션 2승인데 합계 **−200,750원**. 30거래일로 넓히면 같은 형태가 반복된다(아래 표) |
| **변경 대상** | `config/settings.py:ATR_HORIZON_TP1_MULT['3m']`(현행 0.5) 및 3m 하드스톱 폭 산출부<br>`strategy/exit/` 손절폭 계산 — **먼저 섀도로 재측정만** |
| **기대효과** | 3m 손익비 0.46 → 0.8 개선 시, 07-21~ 3m 80포지션 기준 EV −8,779 → 약 **+3,000원/포지션**(추정). 당일 표본 적용 시 CASE-01의 −269,884가 −169,877(평균 손실) 수준으로 제한 |
| **회귀 위험** | TP1을 늦추면 CASE-03·04의 승리가 사라질 수 있다 — **3m 승률 65%는 TP1이 만든 것**일 가능성. 손절폭을 좁히면 CASE-01 같은 즉시 역행에서 더 자주 털린다 |
| **검증** | 섀도 2주(`ATR_HORIZON_TP1_MULT['3m']` 후보 0.6/0.7 병행 기록) → 포지션 단위 EV 비교. **라이브 변경 금지**, 판정 기준 사전등록 필수 |
| **표본 상태** | n=**80포지션**(07-21~) / 51포지션(08-05~) · 관측일 약 22거래일 · **313차 가드 ①②③⑤ 통과, ④ 준수** |

**뒷받침 실측 — 포지션 단위, 계약당 정규화 (2026-07-21~08-20, n=145포지션)**

| hz | n | 승률 | 평균이익 | 평균손실 | 손익비 | 합계 net | 합계/계약 | 평균/계약 |
|---|---|---|---|---|---|---|---|---|
| **3m** | **80** | **65.0%** | 77,965 | **−169,877** | **0.46** | **−702,359** | **−119,330** | **−1,492** |
| 5m | 47 | 51.1% | 181,745 | −161,212 | 1.13 | +654,009 | +489,250 | +10,410 |
| 1m | 15 | 93.3% | 57,276 | −147,268 | 0.39 | +654,591 | +428,911 | +28,594 |

- **3m은 승률 65%인데 EV가 음수다** — 평균 손실이 평균 이익의 **2.2배**.
  `0.65 × 77,965 − 0.35 × 169,877 = −8,780원/포지션`.
- **431차 `MAX_CONTRACTS=3` 이후(08-05~)로 좁혀도 부호 동일**: 3m 51포지션 70.6% **−363,655원**.
  → 계약수 상한 문제가 아니다(⑤ 부호 일관성 통과).
- 전략 리포트 30일 순EV와도 부호 일치: `3m=−6,753원(104건)`이 **유일한 음수**
  (1m +32,730 / 5m +11,276).
- **3m은 진입의 과반을 차지한다**(80/145, 08-05~는 51/72) — 가장 많이 쓰는 호라이즌이
  가장 나쁘다.
- 오늘 `[ModelLive] 3m 당일 라이브 적중률=0.2828 (n=244)`로 전 호라이즌 중 최저,
  `ConstOut` 6회 전량 3m, 장중 재학습 6회 전량 3m, `GuardGhost` 발생도 3m —
  **모델 건강 축에서도 3m이 단독으로 나쁘다**(§1 확인 필요 참조).

> ⚠ **확정 결론 금지 사항**: 위는 "3m을 끄자"는 근거가 **아니다**. 3m 퇴역은 앙상블 가중과
> 라우터 경계(`ENTRY_HORIZON_B1/B2`)를 동시에 바꾸는 **매매 정책 변경**이며, 474차가 경고한
> 대로 라우터 경계 변경은 실측 전제가 약하다. R-1은 **청산 파라미터 축**에 한정한다.

### R-2. (반증 보고) 등급 인플레(원시 C → 최종 A)는 손익 축에서 **R-후보가 아니다**

당일 장중 점검이 O-12로 "등급 인플레 R-후보 3일차"를 걸어뒀다. **전체 표본으로 보면 반대다.**

| 전이 | n(포지션) | 승 | 승률 | 합계 net | 평균 net |
|---|---|---|---|---|---|
| **C → A** (인플레) | **62** | 43 | **69.4%** | **+801,999** | **+12,935** |
| C → C (대조군) | 28 | 20 | 71.4% | **−154,625** | −5,522 |
| X → A | 7 | 6 | 85.7% | +410,106 | +58,587 |
| X → C | 3 | 3 | 100.0% | +78,382 | +26,127 |
| B → A | 1 | 0 | 0.0% | −534,458 | −534,458 |

`data/db/trades.db` · `raw_grade` 기록 시작 **2026-07-30** 이후 전량 · 포지션 단위

- 08-19(−79,158) · 08-20(−223,132) 연속 적자는 **62포지션 표본에서 보면 소표본 변동**이다.
  일자별로도 흑자일(08-04 +387,122 / 08-07 +625,744 / 08-18 +538,058)이 더 크다.
- **313차 원칙에 따라 "등급 인플레가 손실 원인"이라는 방향의 확정 결론을 금지**하고,
  **O-12를 R-후보에서 강등**해 단순 관찰 항목으로 되돌릴 것을 권고한다.
- ⚠ **단, 481차 후속 [2]의 기전 주장은 그대로 유효하다** — "등급 인플레가 CB③-P4의
  유일한 사정권(C급 자동진입 차단)을 구조적으로 비운다"는 손익과 **논리적으로 독립**이며,
  오늘도 A급 2건이 전부 원시 C였다(4건 중 2건, 50%). NEXT_TODO **F-3** 및 전환기준 ⑥
  문언 확장 안건은 유지한다.

---

## 6. 다음 거래일 관측 예정

- [ ] **O-1 (08-21 장후) 수집기 §5 포지션 단위 재현** — F-4 미적용 상태면 §5 합계와
      `[청산 완료]`·DB 포지션 합의 3자 차이를 다시 기록한다. **3일 연속이면 P1 유지 근거 확정.**
- [ ] **O-2 (08-21 장중) `_tick_header` 5초 초과 건수와 `pipe_elapsed` 값** —
      `pipe_elapsed=0`이 아닌(=파이프라인 내) 5초 초과가 **1건이라도** 나오면 CB⑤ 실발동
      가능성이므로 즉시 추적. 오늘은 4/4가 `pipe_elapsed=0`이었다.
- [ ] **O-3 (08-21 장후) 3m 라이브 적중률·ConstOut 집중도** — 오늘 3m 0.2828 / ConstOut 6/6이 3m.
      **2일 연속 3m 단독 집중이면 R-1 우선순위를 "이번 주"로 올린다.**
- [ ] **O-4 (08-21) `ZONE_ENTRY_BAN_SHADOW_ENABLED` 실효 확인** — v9-dev에 부재.
      캠페인 채널 `[53]` 표본이 MW0601에서 적립되고 있는지 금요일점검 대조로 확인.
- [ ] **O-5 (08-21 장후) `GuardGhost` 3m 재발 여부** — 오늘 1회. 3일 연속이면
      "장중 재학습 → EOD 비교기준 소실"이 구조적 문제이므로 별도 등록.
- [ ] **O-6 (08-21) 등급 인플레 4일차** — R-2 반증에 따라 **R-후보 아님**. 관찰만 계속하고
      **일자단위 누적으로만 기록**한다(신호단위 판정 금지).
- [ ] **O-7 (08-21 장후) 15:10 강제청산 실집행** — 누적 0회. 진입이 15:05 이후까지 열려 있는
      날이 오면 반드시 `[ForceExitPass] → [TimeExit] → [ExitAttempt]` 순서를 확인한다.
      **`[SchedForceExit] … 안전망 발동`(ERROR)이 뜨면 P0.**
- [ ] **O-8 (08-21) 로컬 커밋 push** — `origin/v9-dev` 대비 **ahead 8**. 어제 7 → 오늘 8로 증가.
      MW0602가 480차 후속·473차 검증분을 못 본다.
- [ ] **O-9 (08-21 장전) 미커밋 건수 표기** — 수집기가 CRLF 착시로 `461건`을 보고한다.
      F-5 적용 전까지 **매 리포트에서 `--ignore-cr-at-eol` 실측치로 정정**할 것.

---

### 부록 A. 오늘 하루 뼈대 대조표

| 앵커 | 설계 | 실측 | 판정 |
|---|---|---|---|
| 08:40 | 런처 기동 | `08:40:50 [FaultHandler] 활성화 PID=13140` | ✅ |
| 08:45 | 스케일러 floor | `08:45:03 [ScalerFloor]` 6호라이즌 적용 | ✅ |
| 08:55 | 매크로 → 레짐 + 실시간 구독 사전 시작 | 레짐 `RISK_ON` 확정, 구독 정상 | ✅ |
| 09:00 | 정규장 개장 · 매분 루프 | 09:00~15:10 **371/371분(100.0%)** | ✅ |
| 매분 | STEP 1~9 | `[PipePerf]` 369회 · S0~S8 전 단계 기록 | ✅ |
| 09:37~13:52 | 장중 재학습 | 6회(전량 3m, `Python 3.10.20 64-bit`) | ✅ (⚠ 3m 편중) |
| 15:10 | **강제청산(절대원칙 §1)** | FLAT(14:01:35 이후) → `[SchedForceExit] 15:11 청산 대상 없음(정상)` | ✅ 미발생이 정상 |
| 15:12 | F-2 프로세스 밖 FLAT 가드 | `[ForceFlatGuard] 15:12:00 OK` | ✅ |
| 15:18 | 안전망 청산 | 대상 없음 · ERROR 미출현 | ✅ |
| 15:40 | 자가학습 마감 + SHAP 심사 | `daily_close_done` 15:40:07 · `[SHAP] 주간 심사` 종일 13회 · `[ModelHealth]` 기록 | ✅ |
| 15:40 | 정상 종료 | `[Shutdown] … (daily_close)` → 15:40:22 `(auto_shutdown)` | ✅ |
| 15:45 | **EOD 재학습(py310_64)** | 15:45:05 시작 → 15:49:02 완료 · **6/6 교체** · P8 스케일러 재적합 | ✅ |

### 부록 B. 장중 레짐·차단 요약

- `[IntradayRegime]` 전이 **11회**(09:01:59 `NORMAL → CRASH` 시작, 이후 `DAY_RISK_OFF ↔ NORMAL` 왕복).
- 진입 퍼널(총 369분): `FLAT 209 → conf미달 90 → CoherenceGate 9 → 게이트차단 55 → 후보 6 → 진입 4`.
  정합성 `OK`.
- 차단 101건 / 28종. 최다 `등급X — 2_confidence` **43건**, 이어 `3_vwap`·`6_foreign` 각 **40건**.
  `6_foreign`(외국인)은 **진입한 4건 전부에서도 미통과**였다 — 진입·차단 양쪽에서 상시
  실패하는 항목이므로 판별력 재검토 대상(단, 오늘 1일 관측이라 등록만 하고 판정하지 않는다).
- `JointGateBlock` 2건(11:37 C급 joint=0.350 / 11:42 A급 joint=0.415) — 무정보 폴백 1건.
  **표본 18건 부족 — 판정 보류**(캠페인 [7]).
