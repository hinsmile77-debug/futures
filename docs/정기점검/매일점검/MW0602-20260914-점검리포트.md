# 미륵이 일일 점검 — MW0602 / 2026-09-14

# 장전(pre)

- 점검 시각: 09:00:01 KST · PC `MW0602` · 브랜치 `dev`(정상 — MW0602 운영 브랜치와 일치)
- HEAD `0ba6acf` · 미커밋 0건 · 당일 커밋 0건
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260914_pre.md`
- 스킬 지침서 정본·사본 세대 대조: **일치**(`rev: 2026-08-26e`) — 사본 드리프트 없음
- 인덱스락(`.git/index.lock`): **없음**(실행 시작·렌더 시점 2점 확인) · 병행 세션(당일 커밋·산출물): 없음(9/12·9/13은 휴장이라 직전 산출물은 9/11)

## 0. 한 줄 결론

오늘 아침 프로그램은 정상적으로 켜졌습니다. 프로그램과 증권사(Cybos) 연결, 모델 불러오기, 시장 상황(레짐) 판단, 실시간 시세 구독이 모두 개장 전에 정상적으로 끝났고, 오늘 거래에 직접 위험한 문제는 없습니다. 다만 지난 금요일(9/11)에도 있었던 두 가지 사소한 운영 이슈(프로그램 종료 확인 절차, 점검 실행 시각)가 주말을 건너 오늘도 그대로 남아 있고, 3분물 모델 교체가 보류된 지 여러 거래일째 이어지고 있어 이번 주 회의에서 결정이 필요합니다. **오늘 거래를 시작해도 좋습니다.**

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | **정상** | 브랜치·설정·모델·매크로 수집·실시간 구독 전부 정상. 신규 결함 없음 |
| 장중 | (미실시) | |
| 장후 | (미실시) | |

**절대원칙 준수 여부**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① 15:10 강제청산 (오버나이트 금지) | 해당 없음(장전) | 전일(9/11) 포지션 정상 종료. `session_state.json`에 오늘 새 거래일(`count=1`)로 초기화됨 |
| ② Circuit Breaker 5종 | 정상 | 발동 이력 없음(장전 구간). CB②는 설계상 모의투자 한정 비활성(9999) — 정상 상태 |
| ③ CORE 피처 호라이즌 그룹별 | 조건부 정상 | 워밍업 구간(08:45~08:50)에 CORE 피처 일부(above_vwap·cvd_divergence·ofi_norm·opt_chain_pcr)가 일시 축퇴했다가 08:50 전 해소(§1-1). 체크리스트 차단에는 영향 없음(섀도) |
| ④ COM 콜백 내 dynamicCall·emit 금지 | 해당 없음(장전) | 코드 변경 없음 |
| ⑤ GetRepeatCnt/GetCommData 파라미터 | 해당 없음(장전) | 코드 변경 없음 |
| ⑥ 알파 리서치 봇 자동통합 금지 | 정상 | 자동 통합 흔적 없음 |

**설정 불변식** — 수집기 §3 표 전 24행 `일치`. 차단 게이트 32개 중 8개 꺼짐 — 전부 CLAUDE.md에 근거가 있는 기존 한시예외(CB③-P4·FP-CRITICAL·TOX-SEVERE-SPREAD 등)이며 오늘 새로 바뀐 것은 없다.

---

## 1. 이상점 정리보고

### P0 — 없음

오늘 장전 점검에서 오늘 거래에 직접 위험한 항목은 발견되지 않았다.

### P1 — 정확성·관측 훼손

#### 1-1. CORE 피처 4종이 워밍업 구간에 일시 축퇴했다가 개장 전 해소됨 — 기존 추적 패턴

- **증상**: 프로그램이 매매 판단에 쓰는 핵심 피처 중 "VWAP 위치"·"CVD 다이버전스"·"호가 불균형(ofi_norm)"·"옵션 풋콜비율" 네 가지가 08:45:21에 6개 시간대(1분~30분) 전부에서 일시적으로 "쓸모없다"고 판정돼 임시 무력화(항등 변환)됐다. 08:48:00에는 ofi_norm 하나만 남았고, 08:50:00에는 완전히 해소돼 개장(09:00) 전까지 정상 상태를 유지했다.
- **근거**:
  ```
  2026-09-14 08:45:21 [WARNING] SIGNAL: [CORE준비도] trigger=A_WARMUP 축퇴 4/6 — above_vwap×6hz cvd_divergence×6hz ofi_norm×6hz opt_chain_pcr×6hz | 정상 진입 전 해소 여부를 확인하라
  2026-09-14 08:48:00 [WARNING] SIGNAL: [CORE준비도] trigger=A_WARMUP 축퇴 1/6 — ofi_norm×6hz | 정상 진입 전 해소 여부를 확인하라
  2026-09-14 08:50:00 [INFO] SIGNAL: [CORE준비도] trigger=A_WARMUP 축퇴 0/6 — CORE 스케일 정상
  2026-09-14 08:55:00 [INFO] SIGNAL: [CORE준비도] trigger=A_WARMUP 축퇴 0/6 — CORE 스케일 정상
  ```
  `logs/20260914_SIGNAL.log`
- **기준 위반**: CLAUDE.md 절대원칙 ③(CORE 피처 호라이즌 그룹별) — 단, 체크리스트 자체를 차단하는 것이 아니라 스케일러 계산 단계의 섀도 계측이다(470차 B4 설계). 실제 진입 등급을 낮추거나 막지는 않는다.
- **영향**: 개장 10분 전 잠깐 신호 품질이 흔들렸을 수 있지만, 개장 전에 스스로 정상화됐고 오늘 첫 거래에는 영향이 없습니다.
- **영향(기술)**: `ScalerFloor`·`ScalerRefresh`가 raw_std≈0인 피처를 identity(0,1)로 강제하는 워밍업 단계 현상. §12 고착 지표 `CORE축퇴_개장해소`(파생, 494차 G-1)에서 추적하는 "해소" 패턴에 해당(과거 8관측일 중 무축퇴 4·해소 2·미해소 2). 오늘은 "해소" 케이스.
- **신규 여부**: 기존 TODO(470차 B4·476차 G-2가 이미 등록·추적 중인 채널, `CORE축퇴_피처`·`CORE준비도` 고착 지표). 신규 코드 조치 불필요 — 관찰만 계속한다.

### P2 — 운영 부담·기술부채

#### 1-2. `launcher_guard` 고착 — `발화` 지속 (F-1, 이미 P1 승격·기록됨 — 재론 안 함)

- **증상**: 전일 프로세스 정상 종료 확인 플래그(`session_state.json`의 `auto_shutdown_done_date`)가 오늘도 빈 문자열이다. 이미 여러 거래일째 계속되고 있으며 사용자 결정 대기 항목(F-1)으로 이미 P1 승격·등록돼 있다.
- **근거**: `data/session_state.json` → `"auto_shutdown_done_date": ""`, `"p8_last_success_date": "2026-09-11"`, `"eod_retrain_ok_date": "2026-09-11"` (전일 EOD·P8은 정상 완료 — 9/12·13 휴장이라 전일 거래일은 9/11)
- **기준 위반**: 없음(운영 관측 지표) — 안전장치 오작동이 아니라 "종료 확인 플래그가 비어 있다"는 관측 사실
- **영향**: 매일 반복 보고되는 항목이지만 실질적인 매매 위험은 없습니다. 사용자가 런처 종료 로직을 검토해 결정을 내리면 해소됩니다.
- **영향(기술)**: `launcher_guard` 고착 지표(§12) — 사전등록 규칙(313차 ④)에 따라 이미 지난 세션에서 P1로 승격 완료됨(O-17). **오늘은 지속 확인일 뿐 재승격이 아니다** — 함정①에 따라 재론하지 않는다.
- **신규 여부**: 기존 TODO(`dev_memory/NEXT_TODO.md` F-1). 9/11 기준 14일째였고, 9/12·13 휴장을 건너 오늘이 재개 후 다음 관측일이다.

#### 1-3. 장전 점검 실행 시각이 개장 1초 후 — F-2 지속 (마진은 최근 5거래일 중 가장 양호)

- **증상**: 오늘 장전 증거 수집이 09:00:01에 실행됐다(개장 0분 1초 후). 최근 기록 중 가장 개장에 가깝다(9/11 −36초, 9/10 −39초, 9/9 +5초, 9/8 −24초, 9/7 −58초).
- **근거**: `evidence_MW0602-20260914_pre.md` §「장전 발화 마진」
- **기준 위반**: 없음 — 예약 시각 자체가 08:58:40으로 설정돼 있으나 부팅·초기화 단계 소요로 개장을 살짝 넘긴다
- **영향**: 장전 점검 표본에 개장 후 첫 몇 초 데이터가 섞일 수 있으나, 이 시점까지 진입·체결·차단이 전부 0건이라(§5 거래일 요약) 실질적 혼입은 없었습니다.
- **영향(기술)**: §5 거래일 요약 — 진입체크·진입등록·체결·차단·사이저 호출 전부 0건(09:00:01 시점). 혼입 위험 없음.
- **신규 여부**: 기존 TODO(F-2, 장전 예약 시각 08:58:40 조정 승인 — 누적 지속, 사용자 결정 대기)

### 확인 필요 (확정 아님)

- 없음. 표본 부족으로 판정을 보류할 신규 사안이 오늘 발견되지 않았다.

### 이미 반영된 사안 (신규 아님)

- **§12 고착 지표 6종** — `CORE안전`(✅ 100%, benign — F-3 개선 후 정상 상태)·`GuardFair_유효`(무효 100%, 457차 fair_valid 설계상 판정 무영향 섀도)·`전략판정`(INSUFFICIENT 100%, 표본 게이트 특성상 예상됨)·`sgd_alpha`(0.01000 고착, 이미 "ALPHA_MAX 포화·양방향 손잡이 상실"로 원인 규명 완료, 26주 WFA 등재 제안 중)·`drift_action`(DRIFT_UP_SATURATED 고착, 492차 F-7이 이미 다룬 사안)·`launcher_guard`(위 1-2) — 전부 기존에 이미 등록·설명된 추적 대상이며 오늘 새로 조치할 것이 없다.
- **CB②(`CB_CONSEC_STOP_LIMIT=9999`) 비활성** — 실전 전환 기준 ⑤·489차 D2에 따라 기한이 날짜가 아니라 "⑧ 실전 자본 재설정 해제와 동일 커밋"으로 이미 확정돼 있다. 오늘 재론하지 않는다.
- **CB③-P4·FP-CRITICAL·TOXICITY_SEVERE_SPREAD 등 차단 비활성 8종** — 전부 CLAUDE.md 절대원칙 §2에 이미 결정·기록된 한시예외. 오늘 설정값 확인 결과 의도대로 유지 중.
- **`[CybosInvestor] futures supported=False` (수급 3주체 +0)** — 기존에 이미 인지된 구조적 한계(Cybos 선물 투자자 TR 미제공). 신규 사안 아님.
- **`3m EOD 모델 교체 보류`(1-2, 9/11 기준 9거래일 연속)** — 이미 사용자 결정 대기(주간회의 상정 여부)로 등록돼 있다. 장전 시점에는 판정 대상이 아니다(EOD 시점에 재확인).

---

## 2. Fix 작업 구현계획

오늘 장전 점검에서 코드 변경이 필요한 신규 결함을 발견하지 못했다. 위 1-1~1-3 모두 기존 `NEXT_TODO.md` 항목(F-1·F-2·470차/476차 CORE축퇴 관측 채널)으로 이미 추적 중이라 신규 Fix를 만들지 않는다.

---

## 3. 고도화 방안

당일 관측에서 새로 제안할 만한 고도화 항목이 없다. 발견된 운영 이슈(예약 시각 지연, 런처 종료 확인, CORE 워밍업 축퇴)는 모두 기존 채널(F-1·F-2·CORE축퇴_피처)로 이미 다뤄지고 있으므로 중복 제안하지 않는다.

---

## 4. 진입 승패 사후검증 — 해당 없음(장후 전용)

## 5. 수익률 향상방안 — 해당 없음(장후 전용)

---

## 6. 다음 거래일 관측 예정

오늘 새로 등록할 관측(O-*)이 없다. 다음 관측 ID는 `O-78`(수집기 §11 채번 기준)이며, 필요 시 장중·장후에서 이 번호부터 발급한다.

## 7. 기한 도래 항목

| 항목 | 기한 | 상태 | 조치 |
|---|---|---|---|
| CB② 복원 (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | **사건 기한**(⑧ 실전 자본 재설정 해제와 동일 커밋) — 날짜 아님 | — | 489차 D2, 오늘 조치 없음 |
| `launcher_guard` 고착 (F-1) | 사전등록: 10거래일 연속 → P1 (이미 승격됨) | 9/11 기준 14일째, 재론 안 함 | 사용자 결정 대기 |
| 장전 예약 시각 조정 (F-2) | 없음(사용자 승인 대기) | 누적 지속 | 사용자 결정 대기 |
| 3m EOD 모델 교체 보류 (1-2) | 없음(사용자 결정 대기) | 9/11 기준 9거래일 연속 — 오늘 EOD에서 갱신 예정 | 주간회의 상정 여부 결정 대기 |

---

## 8. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-09-14 (MW0602 565차 — 일일 점검: 장전)` — 신규 append (본 세션에서 처리)
- `NEXT_TODO.md` 추가 체크박스: 없음(기존 F-1·F-2·CORE축퇴 채널 지속 확인만)
- 완료 처리한 기존 항목: 없음

---

## 사용자 조치 (장전 시점 기준)

| # | 할 일 | 명령·경로 | 기한 |
|---|---|---|---|
| 1 | 런처 GUARD `!=` 수정 여부 결정 (누적 지속, 재개 후 다음 관측일) | `dev_memory/NEXT_TODO.md` F-1 | 없음(누적 지속) — 결정만 하면 반영은 수분 내 |
| 2 | 장전 예약 시각 08:58:40 → 더 이른 시각 조정 승인 여부 | `dev_memory/NEXT_TODO.md` F-2 | 없음(누적 지속) |
| 3 | 3m EOD 모델 교체 보류 — 주간회의 상정 여부 결정 (9/11 기준 9거래일 연속) | `dev_memory/NEXT_TODO.md` "1-2" | 없음(사용자 결정 대기) |
| 4 | 커밋 대기 — 아래 "커밋 대기" 참조 | `git status` | 편한 때 |

**커밋 대기**: `docs/정기점검/매일점검/MW0602-20260914-점검리포트.md`(신규) · `dev_memory/DECISION_LOG.md`(append) · `dev_memory/NEXT_TODO.md`(append) — 예약 실행 규약상 이 세션은 커밋하지 않는다. `git --no-optional-locks status --short`로 직접 확인 후 경로를 명시해 add할 것(`git add .` 비권장 — CRLF/LF 차이로 대량 오탐 발생).

세션 종료 시 `.git/index.lock`·`.git/HEAD.lock` 잔존 확인: **없음**. ⚠ 작성 중 검증 명령(`--no-optional-locks` 누락)이 0바이트 `.git/index.lock`을 1건 생성했으나, `unlink`는 이 샌드박스에서 `EPERM`으로 막혀도 `rename`은 허용된다는 점(`invariants.md` §0-B)을 이용해 `mv .git/index.lock .git/index.lock.stale.$$`로 세션 안에서 즉시 비켜냈다. 이후 `git --no-optional-locks status/branch/log` 전부 정상 동작 확인 — **사용자 조치가 필요했던 사안 아님**(잔존 스테일 파일 `.git/index.lock.stale.5`는 위생 정리용으로만 참고, 커밋에 영향 없음).

---

# 장중(intra)

- 점검 시각: 12:40:30 KST · PC `MW0602` · 브랜치 `dev` (정상)
- HEAD `ff95905` · 미커밋 3건(`DECISION_LOG.md`·`NEXT_TODO.md`·본 리포트 신규) · 당일 커밋 1건
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260914_intra.md`
- 커버리지: 09:00~12:40 (매분 루프 진행 중 — 15:10까지의 최종 판정은 장후에서 한다)
- 스킬 지침서 정본·사본 세대 대조: **일치**(`rev: 2026-08-26e`)
- 인덱스락: **없음**(실행 시작·렌더 시점 2점 확인) · 병행 세션: 없음(당일 커밋 1건은 §1-4 참조)

## 0i-C. 이월 처리 — 장전 항목 처분

| 원 번호 | 제목(줄임) | 원 국면 | 처분 | 근거 |
|---|---|---|---|---|
| 1-1 | CORE 피처 4종 워밍업 축퇴 | 장전 | ✅ 해소(장전 내 자체 종결) | 08:50:00 `[CORE준비도] 축퇴 0/6` — 09:00 개장 전 이미 해소돼 장전 절에서 스스로 닫힌 항목. 오늘 진입 4건 전부 `vwap✅`(§1-4a) — 재발 없음 |
| 1-2 | `launcher_guard` 고착(F-1) | 장전 | 🔄 지속 | 오늘 관측 갱신 없음(장중엔 EOD 전이라 재확인 대상 아님). 원 번호 계승, 재론 안 함 |
| 1-3 | 장전 점검 실행 시각 지연(F-2) | 장전 | 🔄 지속 | 오늘 갱신 없음(장전 1회성 관측). 원 번호 계승 |
| — | 신규 관측(O-*) | 장전 | 해당 없음 | 장전이 오늘 새로 등록한 O-*는 없음(§6 "다음 관측 ID = O-78"만 예고). 미처분 없음 |

**미처분 0건.**

## 0-i. 한 줄 결론

지금까지(09:00~12:40) 미륵이는 정상적으로 매매했습니다. 오늘 자동으로 들어간 거래 4건이 전부 이익으로 끝났고(합계 약 38만 4천원), 청산·안전장치·차단 로직 모두 설계대로 작동했습니다. 오늘 거래에 직접 위험한 문제는 없습니다. 다만 정오 무렵(11시 4분) 다른 PC에서 만든 코드 수정 하나가 이 컴퓨터의 저장소에 반영됐는데, 지금 돌고 있는 프로그램을 재시작한 것은 아니라서 오늘 거래에는 영향이 없었습니다. **지금까지는 정상이며, 특별히 지금 조치할 일은 없습니다.**

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 정상 | (이월 처리표 참조) |
| 장중 | **정상(진행 중)** | 진입 4/4 승, 청산·CB·차단 로직 전부 설계대로. P0 없음 |
| 장후 | (미실시) | |

**절대원칙 준수 여부 (09:00~12:40 구간)**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① 15:10 강제청산(오버나이트 금지) | 해당 없음(아직 시각 전) | 15:10 이후는 장후에서 판정 |
| ② Circuit Breaker 5종 | 정상 | `연속 손절 1회` 카운터 2건 발생했으나 정지 임계 미도달(CB②는 설계상 9999로 비활성). CB③-P4는 6건 발동했으나 차단은 비활성 상태로 계측만(30m 퇴역에 따른 기존 한시예외) |
| ③ CORE 피처 호라이즌 그룹별(특히 `vwap` 강제 X) | **정상** | 오늘 진입 4건 전부 `[진입체크]` 로그에서 `vwap✅` 확인(§1-4a) — P0 트리거 없음 |
| ④ COM 콜백 내 dynamicCall·emit 금지 | 해당 없음(코드 변경 없음, 이 세션) | — |
| ⑤ GetRepeatCnt/GetCommData 파라미터 | 해당 없음(코드 변경 없음, 이 세션) | — |
| ⑥ 알파 리서치 봇 자동통합 금지 | 정상 | 자동 통합 흔적 없음 |

**설정 불변식** — 수집기 §3 표 전 24행 `일치`. 차단 게이트 32개 중 8개 꺼짐 — 장전과 동일, 오늘 새로 바뀐 것 없음.

---

## 1i. 이상점 정리보고

### P0 — 없음

오늘 09:00~12:40 구간에서 오늘 거래에 직접 위험한 항목은 발견되지 않았다. `vwap` 미통과 상태로 진입한 건도 없다(§1-4a).

### P1 — 정확성·관측 훼손

없음.

### P2 — 운영 부담·기술부채

#### 1-4. 장중(11:04)에 다른 PC(MW0601) 코드 체리픽 커밋이 `dev`에 반영됨 — 라이브 재기동은 없었음

- **증상**: 오늘 11:04:28에 `ff95905 [MW0601] 559차 후속3 체리픽: 로더 최소표본에 intraday 전달 (원 커밋 d9bdea6, v9-dev)` 커밋이 이 PC의 운영 브랜치(`dev`)에 반영됐다. 라이브 엔진 프로세스는 08:41:21 `[Session] 재기동 #1 | cause=STARTUP` 이후 오늘 재기동 로그가 없어, 이 커밋이 지금 돌고 있는 프로세스를 재시작시키지는 않았다.
- **근거**:
  ```
  ff95905 2026-09-14 11:04:28 +0900 [MW0601] 559차 후속3 체리픽: 로더 최소표본에 intraday 전달 (원 커밋 d9bdea6, v9-dev)
  2026-09-14 08:41:21 [INFO] SYSTEM: [Session] 재기동 #1 | cause=STARTUP
  ```
  `git log`, `logs/20260914_SYSTEM.log` — 11:04 전후 "재기동"/"AUTO-RESTART" 로그 0건
- **기준 위반**: 없음(절대원칙 위반 아님). 멀티PC 커밋 규약(CLAUDE.md "다른 PC 브랜치 커밋 체리픽 시 원 커밋 sha·원 PC·이유를 DECISION_LOG에 남긴다")은 **준수됨** — `dev_memory/DECISION_LOG.md`에 원 커밋 `d9bdea6`·원 PC MW0601(`v9-dev`)·이유(사용자 지시)가 명시돼 있다.
- **영향**: 장중에 코드 저장소가 한 번 바뀌었지만, 지금 실행 중인 프로그램은 그대로이고 오늘 거래에는 아무 영향이 없었습니다. 이 수정은 장중 자동 재학습이 실행될 때만 관여하는데, 오늘은 재학습 조건 자체가 충족되지 않아(§1-4b) 이 수정이 실제로 쓰인 적도 없습니다.
- **영향(기술)**: 변경 대상은 GBM 재학습 로더의 `_load_from_db`(장중 최소표본 임계를 `MIN_TRAIN_BARS`(15,000)에서 `MAX_TRAIN_BARS_INTRADAY`(4,800)로 조건부 전환)로, `retrain_intraday.py`는 `py310_64` 서브프로세스로 매번 새로 기동돼 디스크상의 최신 코드를 읽으므로 메인 프로세스 재기동 없이도 다음 장중 재학습부터 반영된다. DECISION_LOG 문서 자체가 "MW0602는 418차 백필 오염 0일이라 이 버그가 아직 발현하지 않는다"고 명시한 선제 조치다. 오늘 `[DriftRetrain] state=SKIP`(조건 미충족)만 반복돼 이 코드 경로 자체가 아직 실행되지 않았다.
- **신규 여부**: 신규 관측(오늘 처음 발생한 병행 세션 이벤트). 코드 자체는 기존 TODO 없이 사용자 지시로 당일 즉시 반영된 것이며, 이 점검 세션이 만든 변경이 아니다.

#### 1-4b. STEP 3(GBM 배치 재학습) 오늘 0회 — 정상(조건 미충족)

- **증상**: `[DriftRetrain] state=SKIP` 이 09:00부터 12:40까지 매분 반복. 조건A(`acc5m<0.25 & n≥20 & 60분경과`)·조건B(`acc5m<0.15 & n≥15 & 30분경과`) 모두 미충족(`cond_a=False cond_b=False`).
- **근거**: `logs/20260914_SYSTEM.log` 09:00:00~09:03:00 `[DriftRetrain] state=SKIP acc5m=0.0% n=0 …`
- **기준 위반**: 없음. CLAUDE.md 470차 각주 — "STEP 3은 30분마다가 아니라 조건부 트리거이며, 재학습 0회가 정상인 날이 있다."
- **영향**: 오늘 낮까지 자동 재학습이 한 번도 돌지 않았는데, 이것은 문제가 아니라 재학습이 필요할 만큼 모델 정확도가 나빠지지 않았다는 뜻입니다.
- **영향(기술)**: 표본 n=0(5m 예측·채점 표본 자체가 조건 창 안에서 아직 안 쌓임) — 재학습 미발동은 정상 분기.
- **신규 여부**: 기존 패턴(함정①·SKILL.md B-2). 신규 항목 아님 — 이상점 번호를 붙이지 않고 확인 사실만 §1i에 기록한다.

### 확인 필요 (확정 아님)

없음. 표본 부족으로 판정을 보류할 신규 사안이 오늘 장중 새로 발견되지 않았다.

### 이미 반영된 사안 (신규 아님)

- **사이저 출력(3계약) vs 실제 진입(2계약)** — 오늘 12건 모두 게이트 배수(hurst 0.50·tox 0.70·meta 무정보폴백 등)에 눌려 2계약으로 축소됐다. 기존 채널 `sizing_inversion_watch`([28], `dev_memory/NEXT_TODO.md` "2-1")가 이미 추적 중이며 신규 등록하지 않는다. 증거금(`MarginCap`)은 오늘 12회 조회 전부 `state=OK`로 축소 0건 — 사이저 산출을 실제로 누른 것은 증거금이 아니라 품질게이트였다.
- **CB③-P4 상태 전환(`NORMAL→RESTRICTED`) 6건(10:47~10:54)** — `CB3_P4_GRADE_BLOCK_ENABLED=False`(296·297차 한시예외)로 계측만 되고 차단에는 관여하지 않는다. CLAUDE.md 절대원칙 §2에 이미 기록된 예외.
- **`CORE준비도` 워밍업 축퇴(08:45:21~08:48:00)** — 0i-C 이월 처리표에서 ✅ 해소로 이미 닫음. 470차 B4·476차 G-2 기존 채널.
- **`Calibration` 축퇴 감지 168건** — CleanShadow 설계상 라이브·클린 분포 모두 저분산일 때 발생하는 정상 보정 폴백(raw 통과)이며 이미 알려진 동작. 신규 등록 대상 아님.

---

## 2i. Fix 작업 구현계획

오늘 장중 점검에서 코드 변경이 필요한 신규 결함을 발견하지 못했다. §1i-4의 병행 커밋은 이미 완료·문서화된 사안이라 이 세션에서 추가 조치가 필요 없다.

---

## 3i. 고도화 방안

당일 관측에서 새로 제안할 만한 고도화 항목이 없다.

---

## 4i. 진입 승패 사후검증 — 해당 없음(장후 전용)

## 5i. 수익률 향상방안 — 해당 없음(장후 전용)

---

## 6i. 오늘 거래 요약 (09:00~12:40, 잠정 — 15:10 최종 청산 전)

- **포지션 4건 · 승 4 / 종료 4 (100%) · 합계 +8.00pt (+383,988원 net)**
- 비용 축: gross +400,000원 · 수수료 16,012원 · net +383,988원 (수수료 비중 4%)
- 진입 4건 전부 호라이즌 `3m` — 등급 `A급(원시C)`×2, `C급`×2, 전부 `vwap✅`
- 최종청산 사유: `하드스톱(틱)[TP1보호]`×2(둘 다 TP1 도달 후 보호스톱 = 이익청산, 진짜 손절 아님) · `TP2(전량)`×2
- 진입 가능 시간: 허용 170분 / 금지(`OTHER`, 11:50~13:00 존) 50분 — 금지구간 진입 0건은 구조적으로 정상(462차 P1-a 기존 등록 사안)
- 메인 스레드 블로킹 1건(08:41:24, 3063ms) — 장전 워밍업 구간 발생, CB⑤ 임계(5000ms) 미도달, 5초 초과 0건
- 매분 루프 커버리지 09:00~12:40 221/221분(100%), 10분 이상 공백 0건

## 7i. 기한 도래 항목 (장전과 동일 — 오늘 상태 변화 없음)

| 항목 | 기한 | 상태 | 조치 |
|---|---|---|---|
| CB② 복원 (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | 사건 기한(⑧ 해제와 동일 커밋) | — | 489차 D2, 오늘 조치 없음 |
| `launcher_guard` 고착 (F-1) | 사전등록: 10거래일 연속 → P1(이미 승격됨) | 지속 | 사용자 결정 대기 |
| 장전 예약 시각 조정 (F-2) | 없음(사용자 승인 대기) | 지속 | 사용자 결정 대기 |
| 3m EOD 모델 교체 보류 (1-2, NEXT_TODO) | 없음(사용자 결정 대기) | 9/11 기준 9거래일 연속 — 오늘 EOD 확인 예정 | 주간회의 상정 여부 결정 대기 |

---

## 8i. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-09-14 (MW0602 566차 — 일일 점검: 장중)` — 신규 append (본 세션에서 처리)
- `NEXT_TODO.md` 추가 체크박스: 없음(기존 채널 지속 확인만)
- 완료 처리한 기존 항목: 없음

---

## 사용자 조치 (장중 시점 기준, 누적)

| # | 할 일 | 명령·경로 | 기한 |
|---|---|---|---|
| 1 | 런처 GUARD `!=` 수정 여부 결정 (누적 지속) | `dev_memory/NEXT_TODO.md` F-1 | 없음(누적 지속) |
| 2 | 장전 예약 시각 08:58:40 → 더 이른 시각 조정 승인 여부 | `dev_memory/NEXT_TODO.md` F-2 | 없음(누적 지속) |
| 3 | 3m EOD 모델 교체 보류 — 주간회의 상정 여부 결정 | `dev_memory/NEXT_TODO.md` "1-2" | 없음(사용자 결정 대기) |
| 4 | 커밋 대기 — 아래 "커밋 대기" 참조 | `git status` | 편한 때 |

**커밋 대기**: `docs/정기점검/매일점검/MW0602-20260914-점검리포트.md`(장중 절 append) · `dev_memory/DECISION_LOG.md`(append) · `dev_memory/NEXT_TODO.md`(append) — 예약 실행 규약상 이 세션은 커밋하지 않는다.

세션 종료 시 `.git/index.lock`·`.git/HEAD.lock` 잔존 확인: **없음**(세션 종료 직전 재확인). ⚠ 검증 중 이 세션 스스로 `--no-optional-locks` 를 빠뜨린 `git -c core.autocrlf=true status` 호출이 0바이트 `.git/index.lock` 을 1건 남겼으나, `mv .git/index.lock .git/index.lock.stale.$$` 로 즉시 비켜내 회수했다(장전과 같은 패턴, invariants.md §0-B). 실질 변경 3건(`DECISION_LOG.md`·`NEXT_TODO.md`·본 리포트)만 확인 — 나머지 `.claude/skills/` 대량 M 표시는 `--no-optional-locks` 없이 돌린 일반 `git status`의 CRLF/LF 오탐이었다(`-c core.autocrlf=true` 로 재확인 시 사라짐).

---

# 장후(post) — 종합 완성본

- 점검 시각: 16:42 KST · PC `MW0602` · 브랜치 `dev`(정상)
- HEAD `6e20c37` · 미커밋 3건(`DECISION_LOG.md`·`NEXT_TODO.md`·본 리포트) · 당일 커밋 4건
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260914_post.md`
- 범위: EOD(15:40 `daily_close()`) + P8(15:52 `retrain_eod.py` 스케일러 재적합)까지 전부 완료 확인
- 스킬 지침서 정본·사본 세대 대조: **일치**(`rev: 2026-08-26e`, 664줄, 최종커밋 2026-08-26과 정합) — `--skill-rev` 인자 미제출로 기계 대조는 "미제출"(정상, 예약 프롬프트 미배선)
- 인덱스락: **없음**(실행 시작·렌더 시점 2점 확인) · 병행 세션: **있음 — 아래 0-C 이월표 참조**(장중 절이 놓친 커밋 2건을 이 절에서 보완)

## 0-C. 이월 처리 — 장전·장중 항목 최종 처분

| 원 번호 | 제목(줄임) | 원 국면 | 처분 | 근거 |
|---|---|---|---|---|
| 1-1 | CORE 피처 4종 워밍업 축퇴 | 장전 | ✅ 해소(이미 장전 내 자체 종결, 재확인) | 오늘 진입 4건 전부 `vwap✅`, 재발 없음. EOD 재학습 직후(`trigger=E_EOD`)도 `축퇴 0/6` 정상 |
| 1-2 | `launcher_guard` 고착(F-1) | 장전 | 🔄 지속 | 오늘 갱신 없음(EOD 종료 후에도 `session_state.json` 확인 예정 — 아래 참조). 사용자 결정 대기 지속 |
| 1-3 | 장전 점검 실행 시각 지연(F-2) | 장전 | 🔄 지속 | 오늘 갱신 없음(장전 1회성 관측) |
| 1-4 | 장중(11:04) MW0601 코드 체리픽 반영 | 장중 | ↩️ **정정 — 장중이 놓친 커밋 2건 추가 확인** | 아래 1-4' 참조. 장중 절은 `ff95905`(11:04) 1건만 인지했으나, 실제로는 12:40 보고 시점까지 `a35b91f`(12:24)·`9f33221`(12:39) 2건이 이미 더 반영돼 있었다 |
| 1-4b | STEP3 재학습 0회(장중 관측) | 장중 | ✅ 해소(관측 유지, 결론 불변) | EOD 시점까지 종합해도 장중 조건부 트리거 판단(정상)에 변화 없음 |
| `1-2`(NEXT_TODO) | 3m EOD 모델 교체 보류(9거래일 연속) | 장전 인계 | ✅ **해소 — 오늘 EOD에서 교체됨** | 아래 2-2 참조 |
| `2-1`(NEXT_TODO) | 사이저 출력(3계약) vs 실체결(2계약) | 장중 | 🔄 지속 | 오늘 12건 전부 품질게이트(hurst 0.50·tox 0.70 등)에 눌림, 증거금(MarginCap) 축소 0건 — 장중과 동일 관측 유지 |
| — | 신규 관측(O-*) | 장전·장중 | 해당 없음 | 등록된 O-* 없음. 이 절에서 O-78 신규 등록(아래 6항) |

**미처분 0건.**

### 1-4′. 병행 세션 보완 — 장중이 놓친 커밋 2건 (P2, 정정)

- **증상**: 장중 절(12:40:30 작성)은 "당일 커밋 1건(`ff95905`, 11:04:28)"이라고만 적었다. 그런데 `git log`로 다시 확인하면 장중 리포트가 쓰인 12:40:30 **이전에** 이미 커밋이 2건 더 있었다 — `a35b91f`(12:24:16)·`9f33221`(12:39:40). 즉 장중 절의 "병행 세션: 없음(당일 커밋 1건은 §1-4 참조)"이라는 문장은 그 시점 기준으로도 부정확했다(실제 3건).
- **근거**:
  ```
  ff95905 2026-09-14 11:04:28 +0900 [MW0601] 559차 후속3 체리픽: 로더 최소표본에 intraday 전달 (원 커밋 d9bdea6, v9-dev)
  a35b91f 2026-09-14 12:24:16 +0900 [MW0601] 561차 체리픽: 호가깊이 판정기가 점표본을 읽고 있었다 (원 커밋 fdbbf2a, v9-dev)
  9f33221 2026-09-14 12:39:40 +0900 [MW0601] 티켓 T-BOOK-1 등록 — 호가깊이 추정량 정리 (v9-dev 67aeb88, 장 마감 후 착수)
  6e20c37 2026-09-14 16:21:59 +0900 [MW0601] 565차 체리픽: 풀타임 수집 Phase 3 — 차트 TR 보충 + 로그 소급 (원 커밋 8fe2316, v9-dev)
  ```
- **기준 위반**: 없음(절대원칙 위반 아님). 다만 §0 "병행 세션 확인" 절차를 장중 절이 완전히 이행하지 못한 사례로 기록한다.
- **영향**: 오늘 실제 거래에는 영향이 없었습니다. 4건 모두 저장소에 기록만 남기는 작업(주석 보강·문서·티켓 등록)이었고, 지금 돌고 있는 프로그램을 재시작시키는 코드는 아니었습니다. 다만 장중 보고서가 "커밋 1건"이라고 적어 실제(3건)보다 적게 보고했다는 점은 다음에 참고할 사항입니다.
- **영향(기술)**: `a35b91f`는 `collection/cybos/realtime_data.py`·`config/constants.py`·`utils/db_utils.py`에 **주석/독스트링만** 변경(호가 깊이 추정량 `_tot`→`_avg` 오프라인 판정기 입력 교정, DDL 실체 무변경 — 커밋 메시지 자체가 "조치(라이브 런타임 무변경)"라고 명시). `9f33221`은 `dev_memory/NEXT_TODO.md` 43줄 추가뿐(티켓 등록, 코드 변경 0). `6e20c37`(16:21, 장 마감 후)은 `main.py`에 08:41~08:44 1회성 "전 거래일 차트 TR 보충" 신규 코드 30줄을 추가했으나 **오늘 EOD(15:40)·P8(15:52) 이후**에 반영돼 오늘 재학습에는 관여하지 않았다 — 내일 08:41 첫 발동이므로 아래 6항(O-78)에서 관측을 예약한다. 4건 전부 멀티PC 체리픽 규약(원 커밋 sha·원 PC·이유)을 `dev_memory/DECISION_LOG.md`에 기록해 두었다.
- **신규 여부**: 이 절 자체(1-4′)는 신규 등록(장중 보고 정확도에 대한 정정). 커밋 내용 자체는 기존 사용자 지시에 따른 정상 작업.

---

## 1P. 이상점 정리보고 (장후 신규)

### ⚠ P0 — 1-5. `log_manager.signal()` TypeError로 14:25 파이프라인 1분 크래시 + 자동진입 15분 정지 (재발 버그, 3회차)

**오늘 무슨 일이 있었나 (평문)**

오늘 오후 2시 25분, 프로그램 내부의 로그 기록 함수 하나가 "인자를 너무 많이 받았다"는 이유로 그 순간 실행되던 코드를 통째로 멈춰 세웠습니다. 이 사고로 그 1분(14시 24분 봉)의 판단 결과는 기록되지 못하고 건너뛰어졌고, 안전장치가 자동으로 "혹시 몰라 15분간 자동 진입을 꺼두자"고 판단해 14시 25분부터 14시 40분까지 자동 매매가 정지됐습니다. 다행히 그 15분 동안 실제로 진입할 만한 좋은 신호는 없었던 것으로 확인되어 **오늘은 놓친 거래가 없었습니다.** 하지만 이 결함은 우연이 아니라 **코드 자체에 박혀 있는 확정적인 버그**이고, 앞으로 특정 조건(신뢰도가 막판에 기준선 아래로 떨어지는 상황)이 다시 오면 **똑같이 재발합니다.** 더 심각한 것은, 같은 성격의 문제가 **2026년 5월에 이미 한 번 발견돼 "고쳤다"고 기록됐던 버그의 재발**이라는 점이고, 그때 고친 방법 자체가 이번 경우는 막지 못하는 방법이었습니다. 게다가 아직 발동한 적은 없지만 **같은 결함을 가진 코드가 한 군데 더** 있고, 그쪽은 하필 매일 아침 9시 5분~9시 15분(개장 직후 가장 예민한 시간대)에 발동할 수 있는 위치에 있습니다.

**근거**

```
2026-09-14 14:25:01 [ERROR] SYSTEM: [ERR-FATAL] minute_pipeline: signal() takes from 2 to 3 positional arguments but 5 were given
2026-09-14 14:25:01 [CRITICAL] SYSTEM: [ERR-FATAL] minute_pipeline: signal() takes from 2 to 3 positional arguments but 5 were given | 자동진입 OFF + 15분 쿨다운
2026-09-14 14:25:30 [WARNING] SYSTEM: ⚠ 파이프라인 1분 30초 미실행 — 분봉 수신 지연 의심
2026-09-14 14:25:31 [WARNING] SYSTEM: [유지보수 패스] 원 실행 미완주 분 감지(scenario B): bar=2026-09-14 14:24:00 > last_done=2026-09-14 14:23:00 — 그 분의 예측·호라이즌 저장은 생략된다(의도됨)
2026-09-14 14:29:02 [INFO] SIGNAL: [차단] ENTRY 타임아웃 쿨다운 — 659초 후 재진입 가능   … (14:40:01까지 카운트다운 지속)
```
`logs/20260914_WARN.log:474,482-484`, `logs/20260914_SIGNAL.log:4136` 외 4건(14:32·14:33·14:36·14:38)

- **근본 원인(코드 레벨)**: `main.py:7928-7933` — checklist가 `auto_entry=True`를 준 뒤 "[268차-P2] 동적 conf_floor" 로직이 신뢰도 부족을 이유로 자동진입을 다시 끄면서 그 사실을 로그로 남기려는 호출:
  ```python
  log_manager.signal(
      "[P2] conf_floor dynamic=%.1f%% (static=%.1f%%) → auto_entry=OFF "
      "(conf=%.1f%%)",
      _ens_conf_floor_dyn * 100, ENS_CONF_FLOOR_FOR_AUTO * 100,
      confidence * 100,
  )
  ```
  그런데 `logging_system/log_manager.py:175`의 실제 시그니처는 `def signal(self, msg: str, level: str = "INFO", **_kwargs)`다 — **printf 스타일(`%d`, `%s`)의 추가 위치 인자를 지원하지 않는다.** `**_kwargs`는 초과 **키워드** 인자만 흡수하고 초과 **위치** 인자는 흡수하지 못한다. 위 호출은 `msg` 뒤에 위치 인자 3개를 더 주므로 `self` 포함 실인자 5개가 최대 3개(`self, msg, level`)짜리 함수에 들어가 `TypeError`가 난다.
  전수 AST 스캔(수동 grep이 아니라 `ast.parse` + `Call` 노드 순회, 오탐 없음) 결과 **같은 결함을 가진 호출이 코드베이스 전체에 정확히 2곳** 있다:
  | 위치 | 초과 위치 인자 | 오늘 발동 여부 |
  |---|---|---|
  | `main.py:7928` `[P2] conf_floor dynamic` | 3개(총 5인자, "5 were given"과 정확히 일치) | **오늘 14:25:01 실제 발동** |
  | `main.py:7909` `[ColdStart] pass=%d < required=%d` | 4개(총 6인자) | 미발동(도달하려면 09:05~09:15 콜드스타트 최소 pass 미달 조건이 필요) |
- **이미 아는 문제인가(함정① 확인)**: **아니다 — 정확히는, "같은 부류의 오래된 문제"이지 "이미 고쳐진 문제의 재보고"가 아니다.** `dev_memory/DECISION_LOG.md` 2026-05-20(68·69차)·2026-05-22(86차) 항목에 이미 기록이 있다: 86차가 "3회차 재발 방지 설계"라며 `signal()`/`system()`/`trade()` 시그니처에 `**_kwargs`를 추가했고 "인자 개수에 무관하게 crash 불가"라고 결론지었다. **이 결론 자체가 틀렸다** — `**kwargs`는 초과 *키워드* 인자만 막고 초과 *위치* 인자는 막지 못한다는 Python 기본 동작을 그때 오판했다. 오늘 결함은 86차 이후(즉 이 "방지 설계"가 이미 있는 상태에서) 새로 추가된 호출부(`[P2] conf_floor dynamic`)가 같은 함정에 다시 걸린 **4번째 발생**이다.
- **영향**: 오늘은 다행히 이 15분 동안 진입할 만큼 좋은 신호가 없어서 실제로 놓친 거래는 없습니다. 하지만 이 버그는 조건만 맞으면 언제든 다시 일어나고, 특히 똑같은 문제를 가진 코드가 한 곳 더 있는데 그곳은 매일 장 시작 직후(9시 5분~15분)에 걸릴 수 있는 자리라서, 다음번엔 개장 직후 자동매매가 통째로 몇 분 멈출 수도 있습니다.
- **영향(기술)**: (1) 14:24:00분 봉의 예측·체크리스트 결과는 DB에 저장되지 않고 건너뛰었다(유지보수 패스가 "재실행 없음"으로 의도적으로 처리 — 이중 처리 방지 설계는 정상 동작). (2) `_auto_entry_disabled_until` 15분 쿨다운이 걸려 14:25:01~14:40:01 자동진입 차단(설계된 안전장치 발동 — 원인은 버그지만 쿨다운 메커니즘 자체는 정상). (3) 같은 결함 클래스가 코드베이스에 정확히 1건 더(`main.py:7909`) 잠재해 있다.
- **Fix 계획**: 아래 2절 참조.

---

## 2. Fix 작업 구현계획

### F-2026-09-14-1 (P0 대응, 오늘 안 배포 권고 — 장 시작 전 반드시 확정)

1. **`main.py:7928-7933`** — 위치 인자 대신 문자열을 미리 `%` 포매팅해서 단일 `msg` 인자로 전달:
   ```python
   log_manager.signal(
       "[P2] conf_floor dynamic=%.1f%% (static=%.1f%%) → auto_entry=OFF "
       "(conf=%.1f%%)" % (
           _ens_conf_floor_dyn * 100, ENS_CONF_FLOOR_FOR_AUTO * 100,
           confidence * 100,
       )
   )
   ```
2. **`main.py:7909-7912`** — 동일 패턴(현재 미발동이지만 도달 가능하므로 함께 수정):
   ```python
   log_manager.signal(
       "[ColdStart] pass=%d < required=%d (%d~%d) → X등급 강등" % (
           _cr["pass_count"], _cs_min_pass, _cs_start, _cs_end,
       )
   )
   ```
3. **회귀 가드 신설** — `tests/test_566_log_manager_call_arity.py`: `ast.parse`로 `main.py`(및 필요 시 전 코드베이스)를 순회해 `log_manager.{signal,system,trade,health}` 호출 중 위치 인자가 2개(`msg` 포함)를 초과하는 호출을 전수 검출해 실패시킨다. 이번처럼 "3회차 방지 설계"가 실제로는 위치 인자를 막지 못했던 것과 같은 실패가 재발하지 않도록, **어떤 값이 오든 크래시 여부를 실제로 실행해서 검증**한다(mock 함수 시그니처로 동일 인자 수 호출을 실행해 `TypeError` 발생 여부 확인) — 정적 인자 개수 검사만으로는 `**_kwargs`가 "받아줄 것처럼" 보여 다시 방심할 수 있다(457차 계측 4원칙과 동일 취지).
4. **참고 — 근본적으로는 `log_manager.signal/system/trade/health`가 Python `logging` 모듈처럼 `*args`를 받아 `msg % args`를 직접 수행하도록 인터페이스를 바꾸는 것이 이런 호출부 전수 검사 필요성 자체를 없앤다.** 다만 이는 기존 호출부 중 "위치 인자 1개(현재는 `level` 자리로 조용히 잘못 들어가는)"를 쓰는 다른 호출들의 동작을 바꾸므로(예: `main.py:7825` `[StartupWarmup] ... (약 %d분 남음)"`, `_warmup_remain)` — 현재는 `_warmup_remain`이 `level` 인자로 잘못 들어가 메시지의 `%d`가 치환되지 않은 채 그대로 로그에 찍힌다, 크래시는 없지만 메시지가 항상 틀린 값을 보여주는 별도의 조용한 버그다) 별도 검토가 필요하다. **이번 배포 범위에서는 제외**하고 `NEXT_TODO.md`에 별도 항목(P2)으로만 등록한다.
5. 실행 위치: **장후이므로 코드 수정을 이 세션이 직접 적용할 수는 있으나, 예약 실행 규약상 커밋은 하지 않는다.** 위 진단·수정안은 다음에 이 파일을 커밋할 세션(사용자 또는 다음 세션)이 그대로 반영하도록 이 절에 diff 형태로 남긴다. **다음 장 시작(내일 08:55 이전)까지 1·2·3을 반영·커밋할 것을 사용자 조치 1번으로 올린다.**

### 기존에 이미 반영된 사안 (재제안 아님, 함정① 확인)

- §1i(장중)의 "MarginCap 0건", "P4 CVD+OFI 강등" 등은 이미 기존 채널이 추적 중 — 재제안하지 않음.

---

## 3. 고도화 방안

### G-2026-09-14-1. 오늘 관측 기반 — P4(CVD+OFI 역방향 강등) C등급이 오늘 A등급보다 성과가 좋았다 (표본 극소, 참고용)

- **근거**: 아래 제4부 표. A등급(원시 체크리스트 그대로) 2건은 TP1 도달 후 보호스톱으로 마감(+0.48pt, +0.94pt). P4 강등으로 C등급이 된 2건은 TP2까지 완주(+2.44pt, +4.14pt) — 오늘 4건 중 수익 상위 2건이 전부 "강등된" 쪽이었다.
- **왜 26주 재검증이 아니라 관찰만 하는가**: 표본 n=4(방향별 n=2)로 313차 원칙("표본이 부족한 채널에 확정 결론을 내리지 않는다")에 정면으로 걸린다. 우연일 가능성이 압도적으로 높다. 다만 P4 강등 자체가 이미 "자동진입 A/B 차단"만 하고 진입 자체를 막지는 않으므로(체크리스트 통과 + 사이저 산출 시 C등급 자동진입은 이미 허용), 이 관찰은 **정책 제안이 아니라 검증 캠페인에 넣을 만한 가설의 씨앗**이다.
- **변경 대상**: 없음(오늘은 관찰만). 검증 캠페인에 "P4 강등 여부별 TP2 도달률" 채널 신설을 제안할지는 다음 주간회의 안건으로 이관.
- **표본 상태**: n=4, 확정 결론 금지.

---

## 4. 진입 승패 사후검증 (제4부, 장후 전용)

> 3원 대사 확인 — 로그 §5(포지션 4건) · 수집기 §5(포지션 4건) · `trades.db`(8레그, `net_pnl_krw` 합계 383,988원) **전부 일치**. `strategy_report_20260914_154024.txt`의 "당일손익 net +383,989원"과는 배너 반올림 오차 1원(기존에 이미 알려진 반올림 관례, 465차·470차).

### 포지션 1 — 10:44:01 SHORT 2계약 (3m, Hurst=mean-revert, 최종등급 A/원시C)

- **판단 근거**: 앙상블 원시 conf 38.8%·grade C(방향은 맞았으나 신뢰도 낮음) → 체크리스트 9/11 통과(미통과: 아래 집계 중 2항목) → 자동 A등급 승격(배수×1.5, TREND_FOLLOW 모드)
- **체크리스트 미통과 항목**: 집계상 이 4건 전체에서 `cvd`×3·`ofi`×2·`prev`×2·`fore`×1·`chase`×1(합 9 = 9+8+10+8 각 건 미통과 수 합)이며, 개별 매칭은 로그상 명확히 분리되지 않아(수집기가 건별로 태그를 남기지 않음) 이 절에서는 집계치로만 인용한다 — **개별 귀인은 확정하지 않는다**(313차).
- **경과**: 10:45:12 TP1 부분청산 33%(+0.58pt, +27,009원) → 10:45:33 하드스톱(틱)[TP1보호](−0.10pt, −6,991원, `exit_outcome=손실`이나 TP1 이후 보호트레일이라 포지션 전체는 이익)
- **결과**: **+0.48pt / +20,018원(net)** — 승
- **요인 태그**: `단기_모멘텀_방향적중`(SHORT 진입 후 하락 지속) + `보호트레일_조기청산`(TP2까지 못 가고 되돌림에 걸림)

### 포지션 2 — 10:51:01 LONG 2계약 (3m, Hurst=mean-revert, 최종등급 C·원시C — P4 강등)

- **판단 근거**: 앙상블 원시 conf 39.7%·grade C → 체크리스트 8/11 통과 → 자동 A등급 산출 직후 **`[P4] CVD+OFI 동시 역방향 → 등급 A→C 강등`** 발동(두 CORE 지표가 진입방향과 반대) — 최종 실행등급 C
- **경과**: 10:51:17 TP1 부분청산 33%(+0.48pt, +22,009원) → 10:54:01 **TP2(전량)**(+1.96pt, +96,009원)
- **결과**: **+2.44pt / +118,018원(net)** — 승, 오늘 4건 중 2번째 큰 수익
- **요인 태그**: `P4강등에도_방향적중` + `TP2완주`(P4가 A/B 자동진입을 막았을 뿐 C등급 자동진입 자체는 허용돼 정상 진입, 결과적으로 손실 방지 목적의 강등이 이번엔 수익 기회를 줄이지 않았다)

### 포지션 3 — 11:08:01 LONG 2계약 (3m, Hurst=trend, 최종등급 A/원시C)

- **판단 근거**: 앙상블 원시 conf 41.1%·grade C → 체크리스트 **10/11**(오늘 가장 높은 통과 수) → 자동 A등급(추세추종 모드)
- **경과**: 11:08:34 TP1 부분청산 33%(+0.99pt, +47,487원) → 11:09:17 하드스톱(틱)[TP1보호](−0.05pt, −4,513원)
- **결과**: **+0.94pt / +42,974원(net)** — 승
- **요인 태그**: `단기_모멘텀_방향적중` + `보호트레일_조기청산`(포지션1과 동일 패턴 — A등급 2건 모두 TP2 미도달)

### 포지션 4 — 11:17:02 LONG 2계약 (3m, Hurst=trend, 최종등급 C·원시C — P4 강등)

- **판단 근거**: 앙상블 원시 conf 40.0%·grade C[30m역방향차단 표시] → 체크리스트 8/11 → 자동 A등급 산출 직후 **P4 CVD+OFI 역방향 강등 → C**
- **경과**: 11:19:03 TP1 부분청산 33%(+0.93pt, +44,489원) → 11:24:01 **TP2(전량)**(+3.21pt, +158,489원)
- **결과**: **+4.14pt / +202,978원(net)** — 승, **오늘 최대 수익 포지션**
- **요인 태그**: `P4강등에도_방향적중` + `TP2완주`(포지션2와 동일 패턴)

### 요인 집계 (n=4, 확정 결론 금지)

| 요인 | 해당 건수 | 순손익 합계 | 비고 |
|---|---|---|---|
| A등급(원시 그대로, P4 미발동) | 2건(P1·P3) | +62,992원 | 둘 다 TP1 도달 후 보호트레일로 조기청산 |
| C등급(P4 CVD+OFI 역방향 강등) | 2건(P2·P4) | +320,996원 | 둘 다 TP2 전량청산까지 완주 |
| Hurst=mean-revert | 2건(P1·P2) | +138,036원 | |
| Hurst=trend | 2건(P3·P4) | +245,952원 | |
| 방향 적중 여부 | 4/4 (100%) | +383,988원 | 오늘은 방향 예측이 전부 맞았다 — 손실 요인 자체가 없었음 |

> ⚠ 표본 n=4는 어떤 요인 조합에 대해서도 통계적 결론을 뒷받침하지 못한다. 위 표는 "오늘 하루의 사실 기록"이지 "P4 강등이 낫다"는 주장이 아니다.

---

## 5. 수익률 향상방안 (제5부, 제4부 요인 집계에서만 도출)

### R-1. P4(CVD+OFI 역방향) 강등 여부별 TP2 도달률 — 검증 캠페인 채널 신설 검토 (설계 안건 — 결정 아님)

| 항목 | 내용 |
|---|---|
| 근거 | 제4부 요인 집계 — 오늘 P4 강등 2건 전부 TP2 완주(+2.44pt·+4.14pt), 비강등 A등급 2건 전부 TP1 보호청산 그침(+0.48pt·+0.94pt). n=4로 확정 불가하나 방향은 일관됐다 |
| 변경 대상 | 신설: `config/settings.py:VALIDATION_CAMPAIGN`에 `p4_demote_tp2_rate_watch` 채널 추가(구현은 하지 않음 — 설계만) — 진입 시 `_p4_cvd_ofi_demoted` 플래그(이미 존재, `main.py:7941`)를 `trades` 또는 `ensemble_decisions`에 저장해 P4 강등 여부별 TP2 도달률·순EV를 집계 |
| 기대효과 | 만약 패턴이 재현되면(표본 축적 후) "P4 강등을 A/B 자동진입 차단이 아니라 C등급 유지"라는 현행 설계가 실제로도 손실 방지뿐 아니라 수익 기회 보존에도 맞는 설계였음을 확인하거나, 반대로 우연이었음을 조기에 기각할 수 있다 |
| 회귀위험 | 없음(관측 채널 신설만, 진입 로직 무변경) |
| 검증 | `min_samples=20`(기존 캠페인 관례) 도달 시 사전등록 기준으로 판정. 그 전에는 판정하지 않는다 |
| 표본상태 | **n=4, 신규 채널 미착수** — 오늘은 아이디어 제안 단계 |

### R-2. `log_manager` 편의 메서드 호출부 전수 정합성 검사를 회귀 스위트에 상시 편입

| 항목 | 내용 |
|---|---|
| 근거 | 제1부 1-5 — 2026-05 "3회차 재발 방지 설계"가 실제로는 위치 인자 초과를 막지 못했고, 오늘 4번째로 재발했다(main.py:7928). 같은 결함이 main.py:7909에 1건 더 잠재 |
| 변경 대상 | `tests/test_566_log_manager_call_arity.py` 신설(위 Fix 3번과 동일 항목 — 여기서는 "26주 주기가 아니라 매 커밋마다 도는 상시 회귀"로 등록하는 것이 핵심 차이) |
| 기대효과 | 같은 부류의 버그가 5번째로 재발하는 것을 커밋 시점에 차단 |
| 회귀위험 | 낮음 — 신규 테스트 추가만, 기존 코드 변경 없음(단, 위 Fix 1·2가 먼저 반영돼야 이 테스트가 현재 코드베이스에서 통과한다) |
| 검증 | 신설 테스트가 현재 main.py:7909·7928 두 건을 잡아내는지 먼저 확인(수정 전) → Fix 적용 후 통과하는지 재확인 |
| 표본상태 | 해당 없음(코드 정합성 테스트, 표본 개념 아님) |

---

## 6. 다음 거래일 관측 예정 (O-*)

- **O-78**: 오늘 16:21 반영된 `main.py` 신규 코드(`[MW0601] 565차 체리픽` — 08:41~08:44 전 거래일 차트 TR 보충, `SESSION_BARS_ENABLED` 플래그 조건부)가 **내일 08:41~08:44 구간에 처음으로 라이브 발동**한다. 확인할 것: `[SessionBackfill] ... 차트 보충 — chart=... existing=... inserted=... mismatch=...` 로그가 정상 출력되는지(또는 `SESSION_BARS_ENABLED=False`면 스킵이 정상), 예외 발생 시 `[SessionBackfill] 실패 (무해): ...`로 조용히 넘어가는지(코드상 `except Exception` 가드가 있어 설계상 크래시 불가 — 단, 이 가드 자체가 today의 signal() 사고와 다른 층위임을 유의: 이번 것은 broker COM 호출 예외를 잡는 try/except고, signal() 버그는 그 try/except 블록 진입 이전 로그 호출 자체가 깨진 경우라 가드 종류가 다르다). 내일 장전 점검(pre)이 판정할 것.

## 7. 기한 도래 항목 (당일 갱신)

| 항목 | 기한 | 상태 | 조치 |
|---|---|---|---|
| **F-2026-09-14-1**(`signal()` TypeError 2건 수정 + 회귀테스트) | **내일 장 시작 전(08:55)** | 🆕 신규, 미적용 | **사용자 조치 1번 — 최우선** |
| CB② 복원 (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | 사건 기한(⑧ 해제와 동일 커밋) | — | 489차 D2, 오늘 조치 없음 |
| `launcher_guard` 고착 (F-1) | 사전등록: 10거래일 연속 → P1(이미 승격됨) | 9/11 기준 14일째 지속, 재론 안 함 | 사용자 결정 대기 |
| 장전 예약 시각 조정 (F-2) | 없음(사용자 승인 대기) | 누적 지속 | 사용자 결정 대기 |
| ~~3m EOD 모델 교체 보류~~ | — | ✅ **오늘 해소**(acc 0.4037→0.4404로 교체) | 조치 없음(자연 해소) |
| `2-1` 사이저(3계약) vs 실체결(2계약) | `sizing_inversion_watch`([28]) min_samples=20 | 지속(품질게이트에 매일 눌림, 증거금 무관) | 표본 축적 대기 |

---

## 8. 종합 판정표

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 정상 | 브랜치·설정·모델·매크로 수집·실시간 구독 전부 정상 |
| 장중 | 정상(병행세션 보고 누락 1건 있었으나 실질 영향 없음) | 진입 4/4 승, 청산·CB·차단 로직 설계대로. 병행 커밋 집계만 사후 보완 |
| 장후 | **조건부 정상 — P0 1건(재발 버그) 확인, 오늘 실피해는 없음** | 거래 결과 양호(+383,988원), EOD/P8 정상, 15:10 강제청산 원칙 준수(포지션 없어 트리거 불요). 단 `signal()` TypeError가 14:25 파이프라인 1분 크래시 + 자동진입 15분 정지를 일으켰고 재발 확정적이라 내일 장 시작 전 조치 필요 |
| **최종** | **조건부 정상 — 내일 개장 전 코드 수정 1건 필요** | 오늘 거래 자체는 완결·이상 없음. 내일을 위한 조치가 남아 있음 |

## 9. 당일 이상점 통합 대장

| 번호 | 제목 | 발견 국면 | 최초 심각도 | 최종 심각도 | 최종 처분 |
|---|---|---|---|---|---|
| 1-1 | CORE 피처 4종 워밍업 축퇴 | 장전 | P1 | P1 | ✅ 해소(개장 전 자연 해소) |
| 1-2 | `launcher_guard` 고착(F-1) | 장전 | P2 | P2 | 🔄 지속(사용자 결정 대기) |
| 1-3 | 장전 예약 시각 지연(F-2) | 장전 | P2 | P2 | 🔄 지속(사용자 결정 대기) |
| 1-4 | 장중 MW0601 코드 체리픽 1건 | 장중 | P2 | P2 | ↩️ 정정(실제 3건, 1-4′ 참조) |
| 1-4′ | 병행 세션 보완(놓친 커밋 2건) | 장후 | P2 | P2 | ✅ 확인·기록 완료(실질 영향 없음) |
| 1-5 | `signal()` TypeError 재발(4회차) — 자동진입 15분 정지 | 장후 | **P0** | **P0** | 🆕 신규 — Fix 계획 수립, **미적용**(내일 개장 전 필요) |
| `1-2`(NEXT_TODO) | 3m EOD 모델 교체 9거래일 보류 | 장전 인계 | P2 | — | ✅ 해소(오늘 교체 완료) |
| `2-1`(NEXT_TODO) | 사이저 3계약 vs 실체결 2계약 | 장중 | P2 | P2 | 🔄 지속(표본 축적 대기) |

**미처분 0건.**

## 10. 당일 최종 손익 · 절대원칙 6종 최종 판정 (종가 기준)

**손익** — 원천: `data/db/trades.db`(브로커 net 대사 대상, `fetch_daily_net_for_verdict()` 미탑재 브랜치라 엔진 net을 그대로 사용 — `strategy_report` 배너와 1원 오차로 대사 일치 확인)

| 항목 | 값 |
|---|---|
| 포지션 단위 | 4건, 승 4 / 패 0 (100%) |
| 청산 레그 단위 | 8건(부분 4 + 최종 4) — **승패 집계에는 쓰지 않음**(417차 원칙) |
| 합계 pt | +8.00pt(gross 환산) |
| gross | +400,000원 |
| 수수료 | 16,012원(CREON 채널 요율 0.0019%, `commission_rate_used` 확인) |
| **net** | **+383,988원** |
| MDD | 자본대비 2.13% / peak대비 167.9%(둘 다 병기 — 461차 규약) |
| 종가 포지션 | 0계약(FLAT) |
| 차단 발동 | 61건(35종, 대부분 등급X/신뢰도미달·ATR부족) |
| CB 발동 | 연속손절 카운터 2회(정지 없음, CB②=9999 설계상 비활성) · CB③-P4 상태전환 10건(차단은 한시예외로 비활성) |
| 한시예외 적용 | CB③-P4·FP-CRITICAL·TOXICITY_SEVERE_SPREAD 등 8종 — 전부 CLAUDE.md 기록과 일치 |
| STEP 중 걸린 것 | STEP6(파이프라인) 14:25 1회 크래시(위 1-5) — 그 외 8단계 정상 |
| 15:10 강제청산 이행 여부 | **해당 없음(정상)** — 15:11:21 점검 시점 이미 FLAT, 청산 대상 없음 |
| EOD 재학습 | **성공**(6/6 호라이즌 교체, GBM 21,812행×105피처, 소요 114.0초) |
| P8 스케일러 재적합 | **성공**(15:52:19, 500봉, 6호라이즌) |

**절대원칙 6종 최종 판정**

| # | 원칙 | 최종 판정 | 근거 |
|---|---|---|---|
| ① 15:10 강제청산 | **준수(트리거 불요)** | 15:10 이전 전량 청산 완료, 15:11:21 안전망 점검 시 FLAT 확인 |
| ② Circuit Breaker 5종 | **준수** | CB①(1분 신호반전)·④(변동성 3배)·⑤(API지연 5초) 발동 없음. CB②는 설계상 모의투자 한정 비활성(9999, 489차 D2 기한 유효). CB③(30분정확도) 계측만(P4 차단 비활성은 296차 한시예외) |
| ③ CORE 피처 그룹별(vwap 강제 X) | **준수** | 진입 4건 전부 `vwap✅`. `3_vwap` 미통과로 강제 X 처리된 항목은 진입 후보에 없었음 |
| ④ COM 콜백 내 dynamicCall·emit 금지 | **준수** | 오늘 코드 변경(체리픽 4건) 전부 COM 콜백 밖 경로. `6e20c37`의 신규 차트보충 코드도 커밋 메시지가 "BlockRequest, COM 콜백 밖"임을 명시 |
| ⑤ GetRepeatCnt/GetCommData 파라미터 | **준수** | 해당 패턴 변경 없음 |
| ⑥ 알파 리서치 봇 자동통합 금지 | **준수** | 자동 통합 흔적 없음 |

> ⚠ 절대원칙 자체는 6종 모두 준수했다. 1-5(`signal()` TypeError)는 절대원칙 위반이 아니라 **계측/로깅 코드의 소프트웨어 결함**이며, 우연히 오늘은 실거래 손실로 이어지지 않았을 뿐 재발 시 자동매매 가용성에 실질적 위험이 있다.

---

## 사용자 조치 (하루 전체 종합)

| # | 할 일 | 명령·경로 | 걸리는 시간 | 안 하면 생기는 일 | 기한 |
|---|---|---|---|---|---|
| 1 | **`signal()` TypeError 2건 수정** — `main.py:7928`·`main.py:7909`를 위 2절 diff대로 수정하고 회귀테스트(`tests/test_566_log_manager_call_arity.py`) 추가 후 커밋 | `main.py`, 신규 테스트 파일 | 15~20분(수정+검증) | 다음에 같은 조건(체크리스트 auto_entry=True인데 신뢰도가 막판에 동적 하한 아래로 떨어짐, 또는 개장 직후 콜드스타트 최소 pass 미달)이 오면 **또 파이프라인이 1분 멈추고 자동진입이 15분 정지**한다. 이번엔 운 좋게 그 15분에 좋은 신호가 없었지만 다음엔 그렇지 않을 수 있다 | **내일 장 시작(08:55) 전** |
| 2 | 런처 GUARD `!=` 수정 여부 결정 (F-1, 9/11 기준 14일째 지속) | `dev_memory/NEXT_TODO.md` F-1 | 결정 후 반영 수분 | 매일 반복 보고(실질 매매 위험은 없음) | 없음(누적 지속) |
| 3 | 장전 예약 시각 08:58:40 → 더 이른 시각 조정 승인 여부 | `dev_memory/NEXT_TODO.md` F-2 | 결정 후 반영 수분 | 매일 반복 보고(실질 위험 없음) | 없음(누적 지속) |
| 4 | `p4_demote_tp2_rate_watch` 채널 신설 여부 — 주간회의 상정 (제5부 R-1, 설계 안건) | 주간회의 | 논의 5분 | 표본이 계속 안 쌓여 오늘 관찰(n=4)이 우연인지 패턴인지 영원히 모름 | 다음 주간회의 |
| 5 | 커밋 대기 — 아래 참조 | `git status` | 편한 때 | — | 편한 때 |

**커밋 대기**: `docs/정기점검/매일점검/MW0602-20260914-점검리포트.md`(장후 절 append) · `dev_memory/DECISION_LOG.md`(append) · `dev_memory/NEXT_TODO.md`(append) — 예약 실행 규약상 이 세션은 커밋하지 않는다. `git --no-optional-locks status --short`로 직접 확인 후 경로를 명시해 add할 것(`git add .` 비권장).

세션 종료 시 `.git/index.lock`·`.git/HEAD.lock` 잔존 확인: **없음**(종료 직전 재확인, `git --no-optional-locks status`·`ls -la .git/*.lock` 모두 정상).
