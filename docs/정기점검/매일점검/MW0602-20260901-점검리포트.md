# 미륵이 일일 점검 — MW0602 / 2026-09-01

# 장전(pre)

- 점검 시각: 09:03 KST · PC `MW0602` · 브랜치 `dev`(정상 — MW0601은 `v9-dev`, 서로 다름)
- HEAD `64b4b92` · 미커밋 0건 · 당일 커밋 0건
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260901_pre.md`
  (최초 실행 `evidence_MW0602-20260901_pre_0900.md` — `--skill-rev` 오기재로 재실행, 아래 1-2 참조)

## 0. 한 줄 결론

오늘 미륵이는 정상적으로 켜졌고 거래할 준비가 됐습니다. 위험한 문제(P0)는 없습니다.
다만 점검 프로그램 자체가 예정보다 늦게, 정확히는 장이 열리고 22초 뒤에 실행돼서 오늘 기록
중 개장 직후 몇 초가 장전 자료와 장중 자료에 겹쳐 들어갈 수 있습니다 — 이건 몇 주째 반복되는
사소한 예약 시간 문제이고 이미 알려져 있으며, 오늘 새로 생긴 문제가 아닙니다. 그 외에는 모델이
정상적으로 켜졌고, 어제 마감 작업(재학습)도 문제없이 끝났고, 오늘 아침 시장 분위기(레짐) 판단도
제때 끝났습니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | **정상(조건부)** | 기동·모델로드·전일 EOD+P8·레짐 확정 전부 정상. 조건: 점검 실행 지연(1-1), 이미 알려진 사본 드리프트(1-2)·PC태그 누락(1-3)이 미해소 상태로 이월 |
| 장중 | (장중 절에서 판정) | |
| 장후 | (장후 절에서 판정) | |

**절대원칙 준수 여부**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | 해당 없음(장전) | 아직 거래 시작 전 — 09:00:22 기준 진입 0건 |
| ② | Circuit Breaker 5종 | 정상 | `CB_state=NORMAL`, `CB②`는 한시예외(9999) 그대로. 아직 신호 없어 발동 여지 없음 |
| ③ | CORE 피처 호라이즌 그룹별 | 조건부 | `ofi_norm`이 08:59:01·09:00:58 기준 1m~30m 전 호라이즌에서 identity 강제(축퇴) — 08:55:20 재적합으로 z경고 12→4개까지는 줄었으나 ofi_norm 자체는 해소 안 됨. 기존에 계측 중인 패턴(1-4) |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 위반 로그 없음 | 정적 코드 검토는 장전 범위 밖. 크래시 서명 0건(§11 참고) |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 위반 로그 없음 | 해당 오류 패턴 관측 안 됨 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 준수 | 자동통합 흔적 없음 |

**설정 불변식** — 수집기 §3 표 전 항목 `일치`. 이상 없음(29개 차단 게이트 중 7개 꺼짐 — 전부 문서화된 한시예외/기능토글, 신규 불일치 없음).

---

## 1. 이상점 정리보고

### P0 — 없음

### P1 — 없음

### P2 — 운영 부담·기술부채

#### 1-1. 장전 점검 실행이 개장 22초 뒤에 시작됨

- **증상**: 오늘 다이제스트 생성 시각 09:00:22, 장전 발화 마진 **−22초**(개장 후). 최근 5거래일도
  전부 음수: 08-31 −7초 · 08-27 −30초 · 08-26 −44초 · 08-25 −34초 · 08-24 −36초.
- **근거**: `evidence_MW0602-20260901_pre.md` §「⏱ 장전 발화 마진」.
- **기준 위반**: 절대원칙 위반은 아님. 다만 phases.md A-2의 "실시간 구독 09:00 이전 시작"
  취지와 어긋나는 방향(점검이 개장 후 실행)이며, 장전/장중 표본이 한 증거 파일에 섞일 수 있다.
- **영향**: 지금은 실질적인 문제로 이어지지 않았습니다 — 레짐 판단(08:58:25)과 실시간 데이터
  구독(08:45:19)은 모두 개장 전에 정상적으로 끝났습니다. 다만 점검 프로그램 자체가 늦게 도는
  버릇이 계속되면, 나중에 "장 열리기 전 상태"를 봐야 할 때 이미 몇 초~몇 분치 장중 데이터가
  섞인 자료를 보게 될 위험이 있습니다.
- **영향(기술)**: 예약 cron 지터로 다이제스트 §5(거래일 요약)가 개장 후 초 단위 진입/차단
  이벤트를 포함할 수 있다. `phases.md`가 명시한 "cron을 08:58:30 이전으로 앞당기지 말 것"
  금지선(레짐 확정 08:58:19~25 증거 상실 위험) 때문에 단순 조기화로는 해결이 안 된다.
- **신규 여부**: **재발/지속** — 2026-08-19부터 `dev_memory/DECISION_LOG.md`·`NEXT_TODO.md`에
  G-2(발화 마진 상시 기록)·O-10(3거래일 연속 09:00 이전 판정) 등으로 계속 추적 중인 만성
  패턴이다. 08-25 O-10 판정은 "실패"(0/3)로 이미 기록돼 있다. **오늘은 새 발견이 아니라
  기존 관측치에 값 하나(−22초)를 추가하는 것.** 신규 Fix 제안 없음 — 이미 "cron 08:58:30
  이전 금지" 처방이 등록돼 있어 재제안하지 않는다(함정 ①).

#### 1-2. 점검 지침서 사본 드리프트 — 앱 저장 스킬이 정본보다 낡음 (기존 사안, 오늘 재확인)

- **증상**: 이 세션이 `Skill` 도구로 불러온 사본은 `rev: 2026-08-26`(494차)이었으나, 저장소
  정본은 `rev: 2026-08-26e`(494차+498차 후속+499차)로 더 최신이었다. 사본에는 §0-B(락 rename
  우회 절차)·§7 체크리스트 신규 항목(사용자조치 닫기전 재확인)·`--skill-rev` 핸드셰이크 등이
  빠져 있었다.
- **근거**: Skill 도구 로드 결과 첫 줄 `<!-- rev: 2026-08-26 (494차 — 사본 드리프트 방어 ·
  git 호출 규약 · 병행 세션 확인) -->` vs 저장소 `.claude/skills/mireuk-daily-check/SKILL.md`
  첫 줄 `<!-- rev: 2026-08-26e (494차 + 498차 후속 + 499차 …) -->`.
  `evidence_MW0602-20260901_pre.md` §2: `🔴 사본 드리프트 — 사본 rev 2026-08-26 ≠ 정본 2026-08-26e`.
- **기준 위반**: SKILL.md §0 "이 문서는 사본일 수 있다" 원칙.
- **영향**: 이번 점검 자체는 저장소 안의 최신 지침(정본)을 직접 읽어서 그대로 따랐기 때문에
  실질적인 문제는 없었습니다. 다만 이 세션이 껐다 켜질 때마다 자동으로 불러오는 사본이
  낡은 채로 남아 있으면, 나중에 정본을 다시 확인하는 걸 깜빡하는 세션이 예전 규칙으로
  돌 위험이 있습니다.
- **영향(기술)**: 앱 저장 스킬(`save_skill`)과 예약작업 3종(장전·장중·장후) 프롬프트는
  git으로 전파되지 않는다(SKILL.md §1-B) — 이 세션이 파일을 고쳐도 반영되지 않는다.
  `--skill-rev` 인자가 예약 프롬프트 3종에 아직 배선되지 않아 대조가 "미제출"이 되는 것도
  같은 원인.
- **정정 사항**: 처음에 증거 수집기를 `--skill-rev 2026-08-26e`(정본 값을 그대로 복사)로
  실행해 대조가 거짓으로 "일치"를 찍었다(스크립트 자체가 "정본에서 복사하면 검사가
  무의미해진다"고 경고하는 바로 그 실수). 컨텍스트에 실제 로드된 사본 rev(`2026-08-26`)로
  다시 실행해 정정했다 — 이 정정된 값이 위 증거다.
- **신규 여부**: **기존 TODO** — `dev_memory/NEXT_TODO.md`에 "사용자 몫 · 7회째 이월"로
  이미 등록돼 있다(`F-10`: 정본에서 값을 복사하지 말 것, 예약 프롬프트 3종에 `--skill-rev`
  배선). 저장소 안에서 세션이 대신할 방법이 없어 사용자 조치로 유지한다(§9-3 참조).

### 확인 필요 (확정 아님)

- **CORE 축퇴(ofi_norm, 1-4)**: 08:55:20 재적합으로 z경고 피처가 12개→4개로 줄었지만
  ofi_norm 자체는 09:00:58(장 시작 후 n=500봉 기준)까지도 raw_std≈0으로 전 호라이즌 identity
  강제 상태다. 이것이 "진짜 저변동"인지 "입력 부재"인지는 §12 `CORE축퇴_원인축` 채널이 이미
  섀도 계측 중이며(494차 F-1), 20거래일 축적 후 판정 예정이다. **313차 원칙에 따라 오늘
  단독 관측만으로 확정 결론 내리지 않는다.**

#### 1-4. `ofi_norm` CORE 피처, 개장 후에도 6개 호라이즌 전부 축퇴 상태 (지속 관측)

- **증상**: 09:00:58(트리거 `B_OPEN`, n=500봉 — 순수 장중 데이터 기준)에도
  `1m/3m/5m/10m/15m/30m` 전 호라이즌에서 `ofi_norm raw_std≈0(0.0334) → identity(0,1) 강제`.
- **근거**: `logs/20260901_SIGNAL.log` 08:59:01, 09:00:58 각 6줄(`[ScalerRefresh]`).
- **기준 위반**: 절대원칙 ③(CORE 피처 그룹별 분리). 단기 그룹 CORE 실체크리스트 소비 키는
  `ofi_pressure = sign(ofi_norm)`이므로, ofi_norm이 identity 강제 상태여도 부호(sign) 자체는
  살아 있어 체크리스트 통과 여부에는 직접 영향이 없을 가능성이 높다 — 단, 이는 오늘 로그만으로
  확정할 수 없다(확인 필요 항목 참고).
- **영향**: 프로그램이 시장의 매수·매도 힘 불균형을 재는 지표 하나가 오늘도 "특징이 없다"고
  판단해 원래 계산 대신 단순한 기본값으로 대체하고 있습니다. 지금까지는 이게 진입 여부를
  직접 막지는 않는 것으로 보이지만, 이 지표에 의존하는 판단이 있다면 그 판단이 평소보다
  덜 정교해질 수 있습니다.
- **영향(기술)**: `ScalerRefresh` 워밍업 로직이 raw_std가 임계 미만이면 스케일러를 identity(0,1)로
  강제해 FLAT 100% 출력을 방지하는 보호 장치인데, 이 보호가 매일 발동한다는 것은 원본
  `ofi_norm` 계산 자체의 변동성이 구조적으로 낮다는 뜻일 수 있다.
- **신규 여부**: **기존 관측 채널** — 다이제스트 §12 `CORE축퇴_피처`가 8거래일 292표본 중
  `ofi_norm×6hz`를 최빈값(163건)으로 이미 추적 중이다. 판정은 20거래일 축적 후 313차 원칙에
  따라 별도 세션에서 이뤄진다. 오늘은 이 관측을 재확인하는 데이터 포인트 하나를 더한 것뿐이다.

### 이미 반영된 사안 (신규 아님)

- **PC명 태그 누락 커밋 `f38a3e3`**: 오늘 브랜치 로그에 잡혔지만 08-31 07:32 커밋이며,
  08-31 장후 세션이 이미 "1-9(P2)"로 등록해 `DECISION_LOG.md`에 기록돼 있다
  (9개 문서 파일·8,202줄, 전부 문서 산출물). 오늘 새로 생긴 커밋이 아니므로 신규 이상점으로
  올리지 않는다.
- **CB②(`CB_CONSEC_STOP_LIMIT=9999`)**: 정상 한시예외. 복원 시점은 날짜가 아니라 전환기준
  ⑧ 해제와 동일 커밋(489차 D2) — 오늘 재검토 기한 도래 아님.
- **CB③-P4·FP-CRITICAL 비활성**: 둘 다 값 `일치`(정상), 재검토 기한은 별도 라이브 검증
  완료 시점이며 오늘 도래하는 기한 없음.

---

## 2. Fix 작업 구현계획

오늘 장전 관측 범위에서 **신규 코드 Fix가 필요한 P0/P1 이상점은 없다.** 1-1·1-2·1-3은 전부
기존에 등록된 사용자 조치/운영 조치 사항이며, 저장소 코드 변경으로 풀리는 문제가 아니다
(1-1은 cron 스케줄 조정 금지선이 걸려 있고, 1-2·1-3은 앱 저장 스킬·git 태그 규율처럼 세션
바깥의 사용자 조치 영역).

> 장전 국면이므로 어차피 코드 변경은 실행하지 않는다(라이브 프로세스 구동 중).

---

## 3. 고도화 방안

오늘 관측에서 새로 제안할 만한 고도화는 없다. 1-1(발화 마진)·1-4(ofi_norm 축퇴)는 이미
섀도 계측·추적 채널이 가동 중이며 판정 시점(각각 O-10 재시도, 20거래일 축적)이 정해져 있어
추가 제안은 함정 ①(이미 결정된 사안 재제안)에 해당할 위험이 있다.

---

## 4. 진입 승패 사후검증 (장후 한정)

해당 없음(장전) — 09:00:22 기준 진입 0건.

## 5. 수익률 향상방안 (장후 한정)

해당 없음(장전).

---

## 6. 다음 거래일 관측 예정

오늘은 신규 O-* 등록 없음(다음 발급 번호는 `O-61`, 다이제스트 §11 기준 — 기존 O-10 등
진행 중인 항목만 재확인).

## 7. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| CB② 복원 (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | 사건 기한(⑧ 해제와 동일 커밋) — 날짜 아님 | — | 489차 D2, 오늘 해당 없음 |
| CB③-P4 / FP-CRITICAL 재검토 | 라이브 검증 완료 시점(날짜 미지정) | — | 오늘 도래 아님 |

---

## 8. dev_memory 반영

이 세션은 예약 실행(장전)이라 **커밋하지 않는다.** dev_memory 갱신은 아래 "장후" 완성 시
한 번에 반영하는 것이 원칙이나, SKILL.md §6에 따라 장전 세션도 append 가능하므로 아래를
`DECISION_LOG.md`·`NEXT_TODO.md`에 추가한다.

- `DECISION_LOG.md` 추가 항목: `## 2026-09-01 (MW0602 508차 — 일일 점검·장전)`
- `NEXT_TODO.md` 신규 체크박스: 없음(전부 기존 이월 항목 재확인)
- 완료 처리한 기존 항목: 없음

**커밋 대기**: `docs/정기점검/매일점검/MW0602-20260901-점검리포트.md`,
`docs/정기점검/매일점검/evidence_MW0602-20260901_pre.md`,
`docs/정기점검/매일점검/evidence_MW0602-20260901_pre_0900.md`,
`dev_memory/DECISION_LOG.md`, `dev_memory/NEXT_TODO.md`

---

# 장중(intra)

- 점검 시각: 12:44 KST · PC `MW0602` · 브랜치 `dev`(정상)
- HEAD `64b4b92` · 미커밋 3건(`dev_memory/DECISION_LOG.md`, `dev_memory/NEXT_TODO.md`,
  이 보고서 신규 — 전부 이 세션 자신의 산출물, 새로 생긴 코드 변경 아님) · 당일 커밋 0건
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260901_intra.md`
  (커버리지 09:00~12:41, 진행 중인 장중이라 미래 시각은 공백으로 세지 않음)
- 병행 세션 확인: 오늘 커밋 0건, `docs/정기점검/매일점검/` 최근 파일 전부 이 세션이 만든 것 —
  다른 세션과 겹친 흔적 없음. `.git/index.lock` 없음(실행 시작·렌더 2점 확인).

## 0. 한 줄 결론

지금까지(정오까지) 미륵이는 매분 빠짐없이 정상적으로 돌았습니다. 위험한 문제(P0)는 없습니다.
오늘 아침부터 지금까지 9번 진입해서 5번은 이기고 4번은 졌고, 합쳐서 약 11만 원 정도 손실
상태입니다(수수료 포함). 이 손실은 큰 손실 한두 번이 낸 게 아니라 이기고 지는 폭이 비슷한
채로 승부가 갈린 것에 가깝습니다. 점심시간(11시 50분~1시)에는 원래 설계대로 진입을 쉬고
있고, 이건 정상입니다. 딱 하나, 점검이 끝나갈 무렵(12시 41분) 시세 정보를 받아오는 통신이
한 번 5초 넘게 멈췄다가 돌아온 게 있었는데, 지금까지는 1번뿐이고 실제 매매 판단에는 영향을
준 흔적이 없어 보입니다 — 다만 다음 점검 때 다시 그러는지는 지켜볼 필요가 있습니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 정상(조건부) | (위 장전 절 참조, 변경 없음) |
| 장중 | **정상(조건부)** | 매분 루프 100%(222/222분), ERROR 0건, 오버나이트·CORE 강제X 위반 없음. 조건: 1-2(사본 드리프트)·1-4(ofi_norm 축퇴) 지속, 신규 1-5(지수 피드 5초 지연 1회) 관찰 등록 |
| 장후 | (장후 절에서 판정) | |

**절대원칙 준수 여부**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | 해당 없음(장중, 아직 15:10 전) | 12:41 기준 미도래. 종가 판정은 장후에서 |
| ② | Circuit Breaker 5종 | 정상 | CB② 이벤트 6건 전부 카운터 증가만(9999 한시예외로 정지 없음, 정상). CB⑤(파이프라인) 경고 1건(12:01, 1556ms — 기준 1000ms 초과했으나 정지 임계 5000ms 미도달) |
| ③ | CORE 피처 호라이즌 그룹별 | 조건부(지속) | `ofi_norm` 12:30까지도 1m~30m identity 강제 지속(1-4). 진입 9건 전부 `vwap✅` 확인(원본 로그 `[진입체크]` 9건 직접 대조 — vwap 관련 P0 없음) |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 위반 로그 없음 | 크래시 서명 신규 0건(전 로그 대조) |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 위반 로그 없음 | 해당 오류 패턴 관측 안 됨 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 준수 | 자동통합 흔적 없음 |

**설정 불변식** — 수집기 §3 표 전 항목 `일치`(장전과 동일, 변경 없음).

---

## 1. 이상점 정리보고

### 이월 처리표 (장전 → 장중)

| 번호 | 장전 심각도 | 처분 | 근거 | 장중 심각도 |
|---|---|---|---|---|
| 1-1 (점검 발화 지연 −22초) | P2 | 🔄 지속(해당 사건은 장전 1회성 — 재관측 대상 아님) | 장중 다이제스트 자체는 12:41:21에 정상 생성됨. 만성 패턴(O-10/G-2)은 계속 추적 | P2 |
| 1-2 (점검 지침서 사본 드리프트) | P2 | 🔄 지속 | 장중 다이제스트 §2도 동일하게 `🔴 사본 드리프트 — 사본 2026-08-26 ≠ 정본 2026-08-26e` 재확인 | P2 |
| 1-3 (PC태그 누락 커밋 `f38a3e3`) | P2 | 🔄 지속(조치 불요 — 08-31 세션이 이미 "1-9"로 등록 완료, 재등록 안 함) | 오늘 신규 커밋 0건이라 변화 없음 | P2 |
| 1-4 (`ofi_norm` CORE 축퇴) | 확인 필요 | 🔄 지속 | 12:30:59까지 `ofi_norm` 1m~30m raw_std≈0 → identity 강제 계속 관측(원본 로그 직접 대조). §12 `CORE축퇴_피처` 채널이 계속 축적 중, 20거래일 판정 전까지 확정 결론 없음(313차 원칙) | 확인 필요(지속) |

**미처분 0건** — 장전이 등록한 이상점 전부 위 표로 처분됨.

### P0 — 없음

진입 9건 전부 `[진입체크]` 원문에서 `vwap✅` 확인, 크래시 서명 0건, ERROR 로그 0건, 15:10 강제청산은 아직 시간이 안 됨.

### P1 — 없음

### P2 — 운영 부담·기술부채

#### 1-5. 지수 시세 조회(MarketEye)가 12:41:24에 5초 넘게 멈췄다가 돌아옴 (신규 관찰)

- **증상**: `[CybosIndex] BlockRequest 타임아웃: Cybos BlockRequest timeout (5s) progid=CpSysDib.MarketEye code=K2G01P`
  1건, 같은 시각 `[IndexPoll] state=SLOW ms=5058`(직전 폴은 78ms로 정상). `[BlockReq]` 창 집계에서도
  같은 시각 `max_ms=5010`. 그 앞뒤로는 전부 40~300ms대 정상 범위(하루 종일 유사 타임아웃 없음 —
  `BlockRequest 타임아웃` 문자열 이 1건이 유일).
- **근거**: `logs/20260901_WARN.log` 12:41:24, `logs/20260901_SYSTEM.log` 12:41:24
  (`[IndexPoll]`·`[BlockReq]` 양쪽 교차 확인).
- **기준 위반**: 절대원칙 직접 위반 아님. 다만 CB⑤의 실질 구현 임계(`CB_PIPE_PAUSE_MS=5,000ms`,
  초과 시 5분 진입정지)와 같은 자릿수라 눈여겨볼 값이다 — 단, 이번에 지연된 것은 매매 판단에
  쓰는 틱 파이프라인이 아니라 코스피200 지수·VKOSPI를 보여주는 **참고용 지수 피드**(MarketEye)다.
  같은 시각 CB⑤ 파이프라인 경고 로그(`[CB⑤] 파이프라인 …ms 경고`)는 발생하지 않았다 — 즉
  진입 판단 파이프라인 자체는 지연되지 않은 것으로 보인다.
- **영향**: 화면에 보여주는 코스피200 지수 값이 5초 정도 잠깐 안 들어온 것으로 보이고, 실제
  매매 판단이나 진입/청산에는 영향을 준 흔적이 없습니다. 다만 이번이 마지막이 아닐 수 있어서
  다음 점검 때 다시 확인이 필요합니다.
- **영향(기술)**: `CpSysDib.MarketEye`(코드 `K2G01P`) 단발성 COM BlockRequest 타임아웃. 발생
  시각이 이 점검의 증거 수집 시각(12:41:21)과 초 단위로 겹치지만, 이 스킬의 수집기는 로그·설정·
  git만 읽고 Cybos COM API를 호출하지 않으므로(SKILL.md §2 "수집기는 DB를 읽지 않는다") 이
  세션이 원인일 가능성은 낮다 — 우연한 시각 근접으로 본다. 표본 1건이라 313차 원칙에 따라
  확정 결론(간헐적 vs 반복적)을 내리지 않는다.
- **신규 여부**: **신규 관찰** — 오늘 첫 발생. 다음 국면(장후) 점검에서 반복 여부를 확인한다
  (아래 "6. 다음 거래일 관측 예정"에 `O-62`로 등록).

### 확인 필요 (확정 아님)

- 1-4 (지속) — 위 이월 처리표 참조.

---

## 2. Fix 작업 구현계획

오늘 장중 관측 범위에서 신규 코드 Fix가 필요한 P0/P1 이상점은 없다. 1-5는 표본 1건짜리 단발성
COM 타임아웃이라 지금 코드를 고칠 근거가 아니다(313차 원칙 — 표본 부족).

> 장중 국면이므로 어차피 코드 변경은 실행하지 않는다(라이브 프로세스 구동 중).

---

## 3. 고도화 방안

오늘 장중 관측에서 새로 제안할 고도화는 없다. 1-2·1-4는 이미 등록된 채널·사용자 조치로
추적 중이며, 1-5는 아직 재발 여부조차 모르는 표본 1건이라 조치를 제안하기엔 이르다.

---

## 4. 진입 승패 사후검증 (장후 한정)

해당 없음(장중) — 이 절은 장후 점검에서 EOD 확정 데이터로 수행한다.

## 5. 수익률 향상방안 (장후 한정)

해당 없음(장중).

---

## 6. 다음 국면(장후) 관측 예정

- **`O-62`**: 1-5(지수 피드 5초 타임아웃)가 15:10까지 재발했는지 `logs/20260901_WARN.log`의
  `BlockRequest 타임아웃` 건수로 확인. 1건에 그쳤으면 단발성으로 종결, 2건 이상이면 P1 승격
  검토(반복되는 COM 레벨 지연은 CB⑤ 실질 파이프라인에도 번질 위험이 있다).
- 기존 `O-10`(발화 마진 3거래일 연속 09:00 이전), 20거래일 CORE 축퇴 판정(1-4)은 그대로 진행 중.

## 7. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| CB② 복원 (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | 사건 기한(⑧ 해제와 동일 커밋) — 날짜 아님 | — | 오늘 해당 없음(장전과 동일) |
| CB③-P4 / FP-CRITICAL 재검토 | 라이브 검증 완료 시점(날짜 미지정) | — | 오늘 도래 아님 |

---

## 8. dev_memory 반영

이 세션도 예약 실행(장중)이라 **커밋하지 않는다.**

- `DECISION_LOG.md` 추가 항목: `## 2026-09-01 (MW0602 509차 — 일일 점검·장중)`
- `NEXT_TODO.md` 신규 체크박스: `O-62`(지수 피드 타임아웃 재발 확인) 1건 등록
- 완료 처리한 기존 항목: 없음

**커밋 대기**: `docs/정기점검/매일점검/MW0602-20260901-점검리포트.md`,
`docs/정기점검/매일점검/evidence_MW0602-20260901_intra.md`,
`dev_memory/DECISION_LOG.md`, `dev_memory/NEXT_TODO.md`

---

# 장후(post)

- 점검 시각: 16:34 KST · PC `MW0602` · 브랜치 `dev`(정상) · HEAD `0127f78` · 미커밋 3건
  (`dev_memory/DECISION_LOG.md`, `dev_memory/NEXT_TODO.md`, 이 보고서 — 전부 이 점검 계열
  자신의 산출물)
- 당일 커밋 **2건**(아래 1-6 참조) — 장전·장중 절 작성 이후 새로 생겼다
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260901_post.md`
- 정본 SKILL.md `rev: 2026-08-26e`(494차+498차 후속+499차)를 이 세션이 직접 `Read` 로
  확인해 그대로 따랐다. 이 세션이 `Skill` 도구로 불러온 사본은 `rev: 2026-08-26`(494차)로
  더 낡았다(1-2 지속, 아래 이월 처리표 참조) — `--skill-rev 2026-08-26`으로 실행해
  다이제스트가 이 드리프트를 정확히 재확인했다(§2 "🔴 사본 드리프트").
- 병행 세션 확인: 오늘 커밋 2건 모두 **이 점검 계열이 아닌 별도 세션**(511차 체리픽·512차
  UI 조정)의 산출물이었다 — 아래 1-6에서 다룬다. `docs/정기점검/매일점검/` 최근 파일은
  전부 이 점검 계열이 만든 것. `.git/index.lock` 없음(실행 시작·렌더 2점 확인, §2).

## 0. 한 줄 결론 — 종합 판정: **정상(조건부)**

오늘 미륵이는 하루 종일 문제없이 돌았고, 위험한 문제(P0)는 없습니다. 오전부터 마감까지
총 11번 진입해서 7번 이기고 4번 졌고(64%), 수수료를 포함해서 하루 합계 약 8만 원 손실로
마감했습니다. 3시 10분에 자리를 정리해야 하는 원칙은 문제없이 지켜졌습니다 — 사실 그
시각에는 이미 포지션이 하나도 없는 상태였습니다(마지막 거래가 2시 43분에 끝났습니다).
장 마감 후 처리(재학습 등)도 전부 정상적으로 끝났습니다.

다만 오늘 낮 동안 두 가지 코드 변경이 커밋됐습니다. 하나는 안전과 관련된 것으로, 어제
자매 컴퓨터(MW0601)에서 실제로 "손절 주문이 163번 연속 거부되어 144초 동안 손절이
작동하지 못한" 사고가 있었는데, 이 컴퓨터에는 같은 사고가 아직 일어난 적이 없지만
같은 구조적 결함이 있다는 것을 확인하고 미리 막는 코드를 가져온 것입니다(오후 2시 36분
커밋). 다른 하나는 화면 UI에서 숫자를 조정하는 간단한 설정 변경입니다(오후 3시 27분
커밋). 두 변경 모두 프로그램을 껐다 켜야 실제로 적용되는데, 오늘은 프로그램을 다시
시작하지 않았으므로 **오늘 거래에는 반영되지 않았습니다** — 내일 아침 재시작 후에
실제로 잘 작동하는지 확인이 필요합니다. 또한 이 두 변경이 저장소 회의록(dev_memory)에
아직 기록되지 않았다는 절차상의 빈틈이 있어 아래에서 다룹니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 정상(조건부) | (장전 절 참조, 변경 없음) |
| 장중 | 정상(조건부) | (장중 절 참조, 변경 없음) |
| 장후 | **정상(조건부)** | 종가까지 절대원칙 6종 위반 없음. 11포지션 7승4패, net −80,528원. EOD·P8 정상 완료. 조건: 1-2·1-4 지속, 신규 1-6(장중 코드 커밋 2건의 회의록 미기록·라이브 미검증) P1 |
| **당일 최종** | **정상(조건부)** | 위와 동일 — 하루 전체 기준으로도 P0 없음 |

**절대원칙 준수 여부 — 종가 기준**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | **준수** | 마지막 포지션 청산 14:43:12. 15:11:20 `[SchedForceExit] status=FLAT … bar_pass=1회 → 청산 대상 없음(정상)` — "청산 로그 없음"이 아니라 "청산할 것이 없었음"(phases.md C-1). 오버나이트 포지션 0건 |
| ② | Circuit Breaker 5종 | 준수 | CB② 이벤트 8건(연속손절 카운터 증가만, `CB_CONSEC_STOP_LIMIT=9999` 한시예외 정상), CB⑤ 파이프라인 정지 임계(5,000ms) 도달 0건(최대 3,265ms), CB①③④ 발동 로그 없음(발동할 상황 자체가 없었음) |
| ③ | CORE 피처 호라이즌 그룹별 | 조건부(지속) | 진입 11건 전부 `[진입체크]` 원문에서 `vwap✅` 확인(P0 없음). 다만 `ofi_norm`이 08:45~13:02까지 identity 강제 지속(1-4, 20거래일 판정 전) |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 위반 로그 없음 | `[FaultHandler] 크래시 서명 보존 — 신규 0건`(08:40:45), 하루 중 신규 크래시 서명 0건 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 위반 로그 없음 | 해당 오류 패턴 관측 안 됨 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 준수 | 자동통합 흔적 없음 |

**설정 불변식** — 수집기 §3 표 전 항목 `일치`(장전·장중과 동일, 변경 없음). 차단 게이트
29개 중 7개 꺼짐 — 전부 문서화된 한시예외.

---

## 1. 이상점 정리보고

### 이월 처리표 (장전+장중 → 장후)

| 번호 | 직전 심각도 | 처분 | 근거 | 최종 심각도 |
|---|---|---|---|---|
| 1-1 (점검 발화 지연 −22초) | P2 | 🔄 지속(장전 1회성 사건, 재관측 대상 아님) | 만성 채널(O-10/G-2)만 계속 추적 | P2 |
| 1-2 (점검 지침서 사본 드리프트) | P2 | 🔄 지속 | 장후 다이제스트도 동일하게 `사본 2026-08-26 ≠ 정본 2026-08-26e` 재확인. 이 세션은 정본을 직접 읽어 그대로 따름(위 "점검 시각" 절 참조) | P2 |
| 1-3 (PC태그 누락 커밋 `f38a3e3`) | P2 | 🔄 지속(조치 불요 — 08-31 세션이 이미 "1-9"로 등록 완료, 재등록 안 함) | 변화 없음 | P2 |
| 1-4 (`ofi_norm` CORE 축퇴) | 확인 필요 | 🔄 지속 | 08:45:19~13:02:59까지 identity 강제 지속 관측(§12 `CORE축퇴_피처` 채널, 원인축 채널도 `<0.50(진짜 저변동)`이 882/894 = 98.7%로 "진짜 저변동" 쪽이 우세). 20거래일 축적 후 판정, 오늘은 데이터 포인트 추가 | 확인 필요(지속) |
| 1-5 (지수 피드 MarketEye 5초 타임아웃) | P2(신규 관찰, `O-62`) | ✅ **해소(단발성 확정)** | `logs/20260901_WARN.log` 전수 재검색 — `BlockRequest 타임아웃` 하루 총 **1건**(12:41:24, 장중 관찰분과 동일 건). 15:10까지 재발 0건. 사전등록(장중 절)대로 "1건 유지 → 종결" 판정 | 종결 |

**미처분 0건** — 장전·장중이 등록한 이상점 전부 위 표로 처분됨.

### P0 — 없음

진입 11건 전부 `[진입체크]` 원문에서 `vwap✅` 확인, 크래시 서명 신규 0건, 15:10 기준
오버나이트 포지션 0건, ERROR 로그 0건(전 채널).

### P1 — 오늘 두 건의 코드 커밋이 회의록(dev_memory)에 기록되지 않음

#### 1-6. 장중에 코드 커밋 2건이 발생했고, 그중 안전 관련 커밋이 회의록 규약을 지키지 않음

- **증상**: 오늘 14:35:58에 `a73b5bc`([MW0602] 511차 체리픽: 청산 주문 브로커 거부
  대응), 15:27:20에 `0127f78`([MW0602] 512차: 수익 보존 가드 파라미터 입력 격자
  재설정)이 커밋됐다. 둘 다 이 점검 세션이 아닌 별도 세션의 산출물이다(§0 병행 세션
  확인). `dev_memory/DECISION_LOG.md`를 `511차`·`512차`로 전수 검색했으나 **일치 0건**
  — 커밋 메시지 자체는 매우 상세하지만(원 커밋 sha·원 PC·이유가 커밋 메시지 안에는
  있다), CLAUDE.md 멀티PC 컨벤션이 요구하는 "DECISION_LOG.md에 별도로 남긴다"가
  이행되지 않았다.
- **근거**: `git --no-optional-locks log --format="%h %s" --since="2026-09-01 00:00:00"`
  결과 2건. `grep -n "511차\|512차" dev_memory/DECISION_LOG.md` 결과 0건(§2 대조).
- **기준 위반**: CLAUDE.md "멀티PC 작업 컨벤션" — "다른 PC 브랜치의 커밋을 cherry-pick
  하면 DECISION_LOG.md에 남긴다 — 원 커밋 sha·원 PC·가져온 이유 세 가지. 코드 변경이
  없는 문서 체리픽도 예외가 아니다"(2026-08-14 470차 등록 규약). `a73b5bc`는 `v9-dev`의
  `a06cd05`를 가져온 명백한 체리픽이라 이 규약이 정면으로 적용된다.
- **영향**: 오늘 이 프로그램이 실제로 위험했던 일은 없습니다. 다만 이 두 변경이 왜,
  어떤 근거로 이뤄졌는지가 커밋 메시지 안에만 있고 저장소의 공식 회의록에는 없어서,
  나중에 다른 컴퓨터(MW0601)나 다른 사람이 "이 결정이 왜 내려졌는지"를 다시 찾으려면
  git 로그를 직접 뒤져야 합니다. 작은 절차 누락이지만, 반복되면 과거 결정을 몰라서
  같은 실수를 반복하는 일이 실제로 여러 번 있었습니다(오늘 CLAUDE.md 본문에도 그
  사례들이 여러 건 적혀 있습니다).
- **영향(기술)**: `511차` 체리픽은 `_send_broker_exit_order` 청산 경로 8곳에 거부 대응
  훅(재시도 백오프 `EXIT_REJECT_BACKOFF_SEC=[1,2,4,8]`초, 연속 거부 3회 시 1회 경보)을
  붙였고, `EmergencyExit`(`main.py:17520`, `_BrokerOrderAdapter.send_market_order`)
  경로는 여전히 실패 코드를 삼키는 **9번째 미훅 경로**로 남아 있다고 커밋 메시지가
  스스로 밝혔다("이번 범위 밖, 등록만") — 그러나 `dev_memory/NEXT_TODO.md`를
  `EmergencyExit`·`17520`으로 검색해도 **등록된 항목이 없다**(아래 재확인). 이 항목도
  함께 비어 있다.
- **라이브 반영 여부 — 중요**: 두 커밋 모두 오늘 **재기동 없이** 커밋만 됐다.
  `logs/20260901_SYSTEM.log`에서 `[System] DB 초기화 완료`가 하루 중 **1회**(08:41:00)뿐이고
  이후 재기동 로그가 없다 — 즉 오늘 장중 내내 돈 프로세스는 이 두 커밋 **이전** 코드로
  계속 실행됐다. `511차` 커밋 메시지 자신도 "① 라이브 미검증 — 재기동 전까지 구코드가
  돈다"고 명시했다. 오늘 청산 11건은 전부 `ExitSendOrderResult ret=0`(성공)이라
  이 фикс가 필요한 상황 자체가 없었다(§2 재확인, `주문 실패`·`거부` 문자열 0건).
- **신규 여부**: **신규(장후 발견)** — 장전·장중 절 작성 시점(각 09:03, 12:44)에는
  아직 커밋되지 않았다(14:35·15:27).

### P2 — 운영 부담·기술부채

없음(1-1~1-4는 이월 지속 항목, 신규 P2 없음).

### 확인 필요 (확정 아님)

- 1-4 (지속) — 위 이월 처리표 참조.

---

## 2. Fix 작업 구현계획

오늘 관측 범위에서 즉시 코드 수정이 필요한 P0/P1 이상점은 없다. 1-6은 **코드가 아니라
기록(dev_memory) 문제**이므로 Fix가 아니라 "6. dev_memory 반영"에서 이 세션이 직접
채운다(511차/512차 요약 항목 추가 + `EmergencyExit` 9번째 경로 미훅을 `NEXT_TODO.md`에
정식 등록). 코드 변경 실행은 장후 국면이라도 이 세션의 관측 범위 밖(다른 세션이 이미
검증·커밋 완료)이므로 재실행하지 않는다 — 대신 **내일 장전 점검이 재기동 후 511차 fix가
실제로 로드됐는지 확인**하도록 `O-63`으로 등록한다(아래 6절).

---

## 3. 고도화 방안

오늘 관측에서 새로 제안할 고도화는 없다. 1-4는 이미 20거래일 축적 판정이 예정돼 있고,
1-6은 절차 보완(dev_memory 기록)이지 코드 고도화가 아니다.

---

## 4. 진입 승패 사후검증 (EOD+P8 — 장후 한정)

### 4-0. 선행조건 확인

- EOD `daily_close()`: `data/daily_close_done_20260901.txt` 존재, 15:40:21 기록.
- P8 스케일러 재적합: `data/eod_retrain_done_20260901.txt`에
  `selfcheck_label_state: ok=True detail=6/6 호라이즌 현행 레이블(fixed) 학습본` +
  `logs/retrain_eod_20260901.log` 15:47:43 `[P8] 스케일러 재적합 완료 n=500봉` +
  `[P8] session_state p8_last_success_date + eod_retrain_ok_date 기록 완료`.
  **둘 다 오늘 날짜로 완료 확인** — 선행조건 충족.
- EOD 재학습: `horizons_replaced: 5/6`. 나머지 1개(`3m`)는 `[Retrain] 3m 교체 보류(EOD
  모델가드) — acc 하락 0.0282 > 허용 0.0250(new=0.3756 old=0.4037) — 참고용 저장,
  구모델 유지`(15:45:38) — 이것은 결함이 아니라 **모델가드가 정상 작동해 더 나쁜
  모델로의 교체를 막은 것**이다.

### 4-1. 오늘 할일 대조 (요구1 보강)

| # | NEXT_TODO 항목 | 당일 커밋 | 코드 실재 확인 | 상태 |
|---|---|---|---|---|
| 1 | (511차 체리픽 — MW0601 사고 대응, NEXT_TODO에 사전 등록 없이 진행됨) | `a73b5bc` | `config/settings.py` `EXIT_REJECT_*` 4개 상수 + `main.py` 청산 경로 8곳 훅 확인(커밋 diff 직접 열람) | ✅완료-커밋확인(단, 라이브 미검증 — 위 1-6) |
| 2 | (512차 — ProfitGuard UI 파라미터 격자, 사용자 지시) | `0127f78` | `dashboard/panels/profit_guard_panel.py` 스핀박스·슬라이더 range/step 변경 확인 | ✅완료-커밋확인(UI 전용, `strategy/profit_guard.py` 라이브 로직 무변경 — 커밋 메시지 자체 명시) |
| 3 | EmergencyExit 9번째 청산 경로 미훅 "등록"(511차 커밋 메시지 자체 주장) | — | `dev_memory/NEXT_TODO.md`를 `EmergencyExit`·`17520`으로 검색 — **등록된 항목 없음** | ⚠**완료주장-등록없음** → P1(위 1-6에 포함, 이 세션이 6절에서 직접 등록) |

함정① 게이트: 위 세 항목 모두 `config/settings.py:VALIDATION_CAMPAIGN_DECISIONS`
대상이 아니다(검증 캠페인 채널이 아니라 안전장치·UI 코드 변경이므로).

### 4-2. 3원 대사

| 원천 | 질의 | 값 |
|---|---|---|
| 로그 | `[Position] 진입` | 11 |
| DB(판정) | `ensemble_decisions` `entry_executed=1` (`ts like '2026-09-01%'`) | 11 |
| DB(체결) | `trades` `COUNT(DISTINCT entry_ts)` (`entry_ts like '2026-09-01%'`) | 11 |
| DB(체결, 참고) | `trades` `COUNT(*)` (청산 레그) | 16 |

**11 = 11 = 11 — 일치.** 레그 16건은 불일치가 아니다(TP1 부분청산 5건이 레그를 늘림,
evidence_map.md §8-5 규칙대로). §5 배너 `-80,528원`과도 검산 일치(수집기 §5, 차 0원).

### 4-3. 승패 판정 — `tp1_reached` 기준

`exit_reason`만으로 세지 않았다. `하드스톱`/`하드스톱(틱)` 9건 중 **5건은 `tp1_reached=1`
(TP1 도달 후 보호 트레일 = 이익 청산)**이고 **4건은 `tp1_reached=0`(진짜 손절)**이다.

**포지션 11건 · 승 7 / 패 4 (64%) · 합계 −0.96pt / net −80,528원**

| 시각 | 방향 | 계약 | 등급(원시) | 신뢰도 | MetaGate | 결과 | 최종 사유 |
|---|---|---|---|---|---|---|---|
| 09:35:58 | LONG | 2 | A(C) | 43.9% | take×0.9 | ✅승 +149,950 | 하드스톱[TP1보호] |
| 09:41:59 | LONG | 2 | A(C) | 42.3% | take×0.9 | ✅승 +179,938 | 하드스톱[TP1보호] |
| 09:51:58 | LONG | 1 | C | 39.1% | take×0.9 | ✅승 +17,972 | 하드스톱(틱)[TP1보호] |
| 10:42:59 | LONG | 2 | A(C) | 41.8% | take×0.9 | ✅승 +132,924 | 하드스톱[TP1보호] |
| 10:47:59 | LONG | 2 | C | 39.8% | **reduce×0.5** | ❌패 −205,080 | 손절1차→하드스톱(틱)[TP1미도달] |
| 11:02:58 | SHORT | 2 | A(C) | 36.6% | **reduce×0.5** | ❌패 −194,056 | 손절1차→하드스톱(틱)[TP1미도달] |
| 11:20:59 | LONG | 1 | A(C) | 40.4% | **reduce×0.5** | ❌패 −91,037 | 하드스톱(틱)[TP1미도달] |
| 11:26:59 | SHORT | 1 | A(C) | 37.9% | **reduce×0.5** | ✅승 +14,972 | 하드스톱(틱)[TP1보호] |
| 11:41:59 | SHORT | 1 | A(C) | 37.0% | **reduce×0.5** | ❌패 −115,019 | 하드스톱(틱)[TP1미도달] |
| 14:38:59 | LONG | 1 | A(C) | 42.6% | take×0.958 | ✅승 +14,956 | 하드스톱(틱)[TP1보호] |
| 14:41:59 | LONG | 1 | A(C) | 39.5% | take×0.9 | ✅승 +13,952 | 하드스톱(틱)[TP1보호] |

### 4-4. 케이스 딥다이브 — 진짜 손절 4건 (E: 손절 준수율)

417차가 유일하게 유의했던 축(손절폭 초과비율)을 오늘 4건에 그대로 적용했다.
`손절 준수율 = 실현손실pt ÷ 의도손절폭pt`(100%=정확히 의도한 선에서 멈춤,
100% 초과=밀림/슬리피지, 100% 미만=의도보다 일찍 끊김).

```
[CASE-A] 진입 10:47:59 LONG 2계약 @1073.70(체결평균 1073.62) · 3m · hurst=neutral
         손절 지정 1071.05 (의도폭 2.65pt) · 등급 C(원시C) · conf=39.8% · MetaGate reduce×0.5
         → 10:50:25 손절1차 조기축소 1계약(-1.24pt) → 10:52:39 하드스톱(틱) 1계약(-2.78pt)
         포지션 합 -4.02pt / -205,080원 · 보유 4분26초
 진입근거   [진입체크] LONG→LONG 2계약 C급 | sign✅ conf✅ vwap✅ cvd❌ ofi❌ fore❌ prev✅ … | conf=39.8%
 승패판정   ❌패 — tp1_reached=0,0 (둘 다 진짜 손절)
 손절준수율 최종레그 2.78/2.65 = 105%(경미한 초과, ~0.13pt) — 417차가 우려한 대폭 초과(68.8%)와는 다른 규모
 주요인    A(방향예측 실패 — cvd·ofi·fore 3항목 미통과인 채로 진입, conf 39.8%는 임계 36.4%에 근접)
 부요인    E(경미한 손절 초과, 소폭)

[CASE-B] 진입 11:02:58 SHORT 2계약 @1067.48 · 3m · hurst=neutral
         손절 지정 1070.08 (의도폭 2.60pt) · 등급 A(원시C) · conf=36.6% · MetaGate reduce×0.5
         → 11:03:51 손절1차 조기축소 1계약(-1.36pt) → 11:04:18 하드스톱(틱) 1계약(-2.44pt)
         포지션 합 -3.80pt / -194,056원 · 보유 1분20초
 진입근거   [진입체크] SHORT→SHORT 2계약 A급(원시C) | cvd✅ ofi✅ fore❌ prev❌ … | conf=36.6%(임계 36.4%에 가장 근접)
 승패판정   ❌패
 손절준수율 최종레그 2.44/2.60 = 94% — 의도보다 오히려 일찍 끊김(양호)
 주요인    A(방향예측 실패, 오늘 11건 중 최저 신뢰도 진입)
 부요인    — (손절 집행 자체는 양호)

[CASE-C] 진입 11:20:59 LONG 1계약 @1072.34(체결 1072.26) · 1m(오늘 유일한 1m 진입) · hurst=neutral
         손절 지정 1070.50 (의도폭 1.84pt) · 등급 A(원시C) · conf=40.4% · MetaGate reduce×0.5
         → 11:22:10 하드스톱(틱) 1계약(-1.78pt), 부분청산 없음(1계약이라 분할 불가)
 진입근거   [진입체크] LONG→LONG 1계약 A급(원시C) | cvd❌ ofi✅ fore❌ prev✅ chas❌ … | conf=40.4%
 승패판정   ❌패
 손절준수율 1.78/1.84 = 97% — 의도한 선 안쪽에서 정확히 정지
 주요인    A(방향예측 실패)
 부요인    — (손절 집행 정상)

[CASE-D] 진입 11:41:59 SHORT 1계약 @1062.70(체결 1062.60) · 3m · hurst=neutral
         손절 지정 1064.92 (의도폭 2.22pt) · 등급 A(원시C) · conf=37.0% · MetaGate reduce×0.5
         → 11:47:29 하드스톱(틱) 1계약(-2.26pt)
 진입근거   [진입체크] SHORT→SHORT 1계약 A급(원시C) | cvd✅ ofi❌ fore❌ prev✅ chas❌ … | conf=37.0%
 승패판정   ❌패
 손절준수율 2.26/2.22 = 102% — 경미한 초과(~0.04pt)
 주요인    A(방향예측 실패)
 부요인    E(미세한 손절 초과, 무시할 수준)
```

### 4-5. 집계표

| 요인 | 승 | 패 | 순손익 | 평균 보유(분) | 관측 거래일 수 |
|---|---|---|---|---|---|
| A(방향예측) — 주요인 | 7 | 4 | −80,528원 | 승 2.1 / 패 3.3 | 1 |
| E(손절 집행 부요인, 4건 중) | — | 2(경미 초과) | 포함됨(별도 분리 불가) | — | 1 |

**313차 다섯 갈래 적용**

1. **판정 단위(일자)**: 오늘은 관측 거래일 **1일**뿐이다. 승패·요인 비율 모두 결론이 아니라
   관찰이다.
2. **overlap 보정**: 11건 중 동시 보유 최대 1포지션(다음 진입은 이전 청산 이후) — 표본 간
   중첩 없음. 이 축은 문제 없음.
3. **이상치 분해**: 최대 승 +179,938원(09:41:59), 최대 패 −205,080원(10:47:59) 하나씩
   제외해도 나머지 9건 합계는 +25,079 − 194,056 − 91,037 − 115,019 + 나머지승리분
   ≈ 여전히 소수 큰 거래(09:41·10:47) 두 건이 하루 손익 변동의 상당 부분을 설명한다 —
   **1일 표본이라 우연/필연을 가릴 수 없다.**
4. **사전등록**: 아래 §5에서 "MetaGate reduce=손실 편중" 관찰에 대한 판정 조건을
   미리 등록한다(사후에 기준을 낮추지 않는다).
5. **부호 일관성**: 비교할 다른 PC·다른 기간 표본이 이 리포트 범위엔 없음 — 판단 보류.

**⚠ 눈에 띄지만 결론으로 쓰지 않는 관찰**: 오늘 `MetaGate reduce×0.5`로 사이즈가
줄어든 진입 5건 중 4건이 패, 1건이 승(80% 패)이었고, `take` 진입 6건은 전부 승(100%
승)이었다. **이것을 새 게이트 제안의 근거로 쓰지 않는다** — `config/settings.py:
VALIDATION_CAMPAIGN_DECISIONS["meta_gate"]`가 2026-08-23(489차 D1)에 표본 1,233건
(분위당 411건)으로 이미 "최종 탈락 확정 — 분리도 −0.0136(부호까지 반대)"로 판정을
끝낸 채널과 이름·성격이 겹친다(다만 그 채널의 입력 `entry_quality_prob`과 오늘 관찰한
`meta_action`/`meta_size_mult`가 완전히 같은 산출물인지는 이 세션에서 확인하지 않았다
— 그것부터 확인이 함정① 재확인의 다음 단계다). 오늘 n=5는 그 대규모 검증을 뒤집을 근거가
못 된다. **관찰로만 등록**하고(§5 R-4), 판정은 새 사전등록 없이는 다시 열지 않는다
(위 결정 항목의 "재개 조건은 소진됐다"는 문구 그대로).

---

## 5. 수익률 향상방안 (제5부 — §4 요인 집계에서만 도출)

오늘 표본(1거래일, 11건)은 313차 기준으로 **파라미터 변경을 정당화하지 못한다.** 아래는
"방안"이 아니라 **사전등록된 관찰 조건**이다 — 표본이 차면 그때 판정한다.

| 칸 | R-4: MetaGate reduce/take 분리 관찰 |
|---|---|
| 근거 | CASE-A·B·C·D(전부 reduce) + 승리 케이스 중 11:26:59(reduce, 유일한 reduce 승) — reduce 5건 중 4패1승, take 6건 중 6승0패 |
| 변경 대상 | (없음 — 관찰 등록 단계, 코드 변경 제안 아님) |
| 기대효과 | (판정 보류 — 아래 조건 충족 전까지 추정하지 않는다) |
| 회귀 위험 | (해당 없음 — 코드 변경 없음) |
| 검증 | 이미 확정 종결된 `meta_gate` 채널(489차 D1)과 오늘 관찰의 입력이 동일한지부터
  다음 세션이 확인. 동일하면 **재개 조건이 소진된 질문이므로 재검증하지 않는다.**
  다르면(예: 오늘 관찰이 `entry_quality_prob`이 아니라 `meta_action`/`meta_size_mult`
  자체의 별개 게이트라면) 신규 사전등록 후 **min_samples=20 · min_days=5 이상** 축적 뒤
  313차 다섯 갈래 재적용 |
| 표본 상태 | n=11(reduce 5 / take 6), 관측일 1일 — **313차 미달, 판정 보류** |

---

## 6. dev_memory 반영

이 세션은 예약 실행(장후)이라 **커밋하지 않는다.**

- `DECISION_LOG.md` 추가 항목: `## 2026-09-01 (MW0602 510차 — 일일 점검: 장후 종합)`
  — 종합 판정, 1-6(511차/512차 dev_memory 미기록 발견 및 이 세션이 대신 기록),
  제4부 딥다이브 요약, 제5부 R-4 사전등록.
- `NEXT_TODO.md` 신규 체크박스:
  - `O-63` 511차 fix 라이브 반영 확인 — **내일 장전** 재기동 로그에서 새 `[System] DB
    초기화 완료` 확인 후, 그날 만약 청산 거부(`주문 실패`/`거부`)가 있었다면
    `EXIT_REJECT_BACKOFF_SEC` 백오프·경보 로그가 실제로 뜨는지 확인. 없으면(거부
    자체가 없으면) 미검증 상태 유지 기록.
  - `EmergencyExit`(`main.py:17520`, `_BrokerOrderAdapter.send_market_order`) 9번째
    청산 경로 미훅 — 511차 커밋 메시지가 "등록만" 한다고 밝혔으나 실제로는
    `NEXT_TODO.md`에 없었다(4-1 항목3). 이 세션이 신규 등록.
  - `R-4` MetaGate reduce/take 분리 관찰 — 위 §5 조건 그대로 등록.
  - 사용자 몫 · 지침서 사본 갱신 — 계속 이월(§0에 이번에도 재확인).
- 완료 처리한 기존 항목: `O-62`(지수 피드 타임아웃 재발 확인) — 1건 유지, 단발성으로 종결.

**커밋 대기**: `docs/정기점검/매일점검/MW0602-20260901-점검리포트.md`(장후 절 append),
`docs/정기점검/매일점검/evidence_MW0602-20260901_post.md`,
`dev_memory/DECISION_LOG.md`, `dev_memory/NEXT_TODO.md`

---

## 사용자 조치

1. **511차 안전 패치(청산 주문 거부 대응)가 아직 실제로 켜지지 않았습니다.** 오늘
   오후 2시 36분에 코드는 저장됐지만 프로그램을 재시작해야 실제로 적용됩니다.
   내일 아침 프로그램을 켜면 자동으로 적용되니 별도 조치는 필요 없지만, 재시작됐는지
   한 번 확인해 주시면 좋습니다. — **걸리는 시간: 확인만 1분.** 안 하셔도 다음
   재시작 때 자동으로 적용되니 급하지 않습니다.
2. **오늘 두 코드 변경(511차·512차)이 저장소 회의록에 기록되지 않아, 이번 점검
   세션이 대신 채워 넣었습니다.** 별도로 하실 일은 없지만, 두 커밋과 이 점검
   리포트·회의록 변경분을 함께 검토 후 커밋해 주세요. — **걸리는 시간: 검토 후
   커밋 5분 내외.** 안 하시면 다음에도 이런 기록 누락이 반복될 수 있습니다(과거에도
   비슷한 사례가 여러 번 있었습니다).
3. **점검 프로그램이 매번 살짝 낡은 지침서 사본을 불러옵니다(9회째 반복 중인 사안).**
   저장소 안의 최신 버전을 앱에 다시 저장해 주셔야 합니다(`save_skill` 기능,
   `overwrite: true`로). 이번 점검은 이 문제를 직접 알아채고 저장소 원본을 대신
   읽어서 정확하게 진행했으니 오늘 결과에는 영향이 없습니다. — **걸리는 시간: 1분
   내외.** 안 하셔도 당장 위험하진 않지만, 언젠가 이 문제를 놓치는 세션이 나오면
   낡은 규칙으로 점검할 위험이 있습니다.
4. **파일 하나가 아직 git에 등록되지 않았습니다**(`docs/프롬프트/MW0602_장후자동조치_설치지침_20260826.md`,
   여러 날째 이월). 경로를 정확히 지정해서 add 해 주세요(전체 `git add .`는 권장하지
   않습니다 — 줄바꿈 문자 차이로 수백 개 파일이 딸려 들어옵니다). — **걸리는 시간:
   1분.** 안 하셔도 당장 문제는 없지만 계속 이월됩니다.

이 외에 오늘 당장 조치가 필요한 위험한 문제(P0)는 없습니다.

---

## 오늘 요약

- 리포트: `docs/정기점검/매일점검/MW0602-20260901-점검리포트.md`
- P0 0건 · P1 1건(1-6, dev_memory 기록 누락 — 코드 위험 아님) · P2 0건(신규, 이월 3건 지속)
- 손익(브로커 net 기준·포지션 단위): **11포지션 7승4패(64%) · gross −48,000원 ·
  수수료 32,528원 · net −80,528원**
- 절대원칙 6종 전부 준수(15:10 강제청산 포함, 그 시각 이미 포지션 0)
- 가장 시급한 것: 없음(P0 없음). 다음으로 중요한 것은 사용자 조치 1·2번(511차 재기동
  확인, dev_memory 커밋 검토).

---

## 장후 자동조치 구현 결과 (2026-09-01 17:40 · 자동)

**결론 먼저 — 오늘은 고칠 코드가 없었습니다.** 리포트가 장전·장중·장후 세 국면 모두에서
"신규 코드 Fix 없음 · 신규 고도화 없음"이라고 스스로 판정했기 때문입니다. 그래서 이
자동조치는 **코드를 한 줄도 바꾸지 않았고**, 대신 「사용자 조치」 4개 항목을 하나씩
실물로 확인한 뒤 커밋만 정리했습니다.

### 1. 「사용자 조치」 4개 항목 처리 결과

| # | 항목 | 처분 | 근거 |
|---|---|---|---|
| 1 | 511차 안전 패치 재기동 확인 | **사용자·내일 장전 몫으로 이월** | 재기동은 코드 작업이 아니다. 확인 절차는 이미 `O-63`으로 `NEXT_TODO.md`에 등록돼 있다 |
| 2 | 511차·512차 dev_memory 기록 + 커밋 | ✅ **이 자동조치가 커밋 수행** | 기록 자체는 510차 점검 세션이 이미 작성해 뒀고(미커밋), 커밋이 남아 있었다. 아래 3절 |
| 3 | 점검 지침서 앱 사본 갱신(`save_skill`) | **사용자 몫으로 이월 (11회째)** | 앱 저장소는 git 바깥이라 세션이 대신 할 수 없다 |
| 4 | `docs/프롬프트/MW0602_장후자동조치_설치지침_20260826.md` git add | 🔴 **이미 이행돼 있었음 — 조치 불요** | 아래 2절 |

### 2. 🔴 사용자 조치 4번은 이미 끝난 일이었습니다 (낡은 항목이 계속 재출력됨)

리포트는 이 파일이 "아직 git에 등록되지 않았다, 여러 날째 이월"이라고 적었지만,
**실제로는 어제(2026-08-31) 07:32:27 커밋 `f38a3e3`에 이미 추가돼 있었습니다.**

```
$ git --no-optional-locks log --diff-filter=A --format="%ci %h %s" -- docs/프롬프트/MW0602_장후자동조치_설치지침_20260826.md
2026-08-31 07:32:27 +0900 f38a3e3 docs: 익절6차 진입포인트 생성방안 종합 + MW0602 검증 리포트

$ git --no-optional-locks status --porcelain -- docs/프롬프트/
(출력 없음 = 작업트리 깨끗)
```

즉 **이행 여부를 다시 확인하지 않은 채 문구만 그대로 옮겨 적힌 항목**입니다. 이것은
이 저장소가 CLAUDE.md 여러 곳에서 반복 경고하는 **함정 ①(이미 반영된 것을 또 제안)**
과 같은 계열이며, 하필 이번엔 지적 대상이 `f38a3e3` — 리포트가 같은 날 "1-3 PC태그
누락 커밋"으로 따로 추적하고 있던 바로 그 커밋이었습니다. 한 커밋을 한쪽에서는
"태그가 없다"고 추적하면서, 다른 쪽에서는 "그 커밋이 담은 파일이 아직 안 올라갔다"고
적고 있었던 셈입니다.

**왜 중요한가 · 안 고치면**: 사용자 입장에서는 매일 "1분이면 되는 일"이 목록에 남아
있으니 계속 신경이 쓰이는데 실제로는 할 일이 없습니다. 이런 항목이 쌓이면 「사용자 조치」
절 전체의 신뢰도가 떨어지고, 진짜 해야 할 항목(3번 같은)이 묻힙니다.
**걸리는 시간**: 다음 점검 세션이 이 항목을 목록에서 빼기만 하면 됩니다(0분).
→ `NEXT_TODO.md`에 `O-64`로 등록했습니다.

### 3. 커밋

코드 변경 0건이므로 커밋 대상은 **문서 4건**입니다.

- `docs/정기점검/매일점검/MW0602-20260901-점검리포트.md` (신규 — 장전·장중·장후 + 이 절)
- `dev_memory/DECISION_LOG.md` (append — 508·509·510차 + 512차 후속)
- `dev_memory/NEXT_TODO.md` (append — 508·509·510차 + 512차 후속)

⚠ **`evidence_MW0602-20260901_*.md` 3건은 커밋하지 않았습니다.** 리포트 각 절의
"커밋 대기" 목록에 올라 있지만 `.gitignore:117`(`docs/정기점검/매일점검/evidence_*.md`)이
의도적으로 제외하는 파일입니다 — 규칙을 우회하지 않았습니다. 이 역시 매 국면 "커밋 대기"에
반복 등재되고 있어 `O-64`에 함께 적었습니다.

### 4. 구현하지 않은 것과 그 이유

| 항목 | 등급 | 이유 |
|---|---|---|
| `R-4` MetaGate reduce/take 분리 | **C (승인 대기)** | 리포트 자신이 "313차 미달, 판정 보류 · n=11 · 관측 1거래일"로 적었다. 표본 미달 항목은 구현하지 않는다. 이미 `NEXT_TODO.md`에 사전등록 조건까지 기록돼 있다 |
| `EmergencyExit` 9번째 청산 경로 미훅 | **C (승인 대기)** | 주문·청산 실행 경로(`_BrokerOrderAdapter.send_market_order`, `main.py:17520`)는 자동조치 범위에서 명시적으로 제외된 영역이다. 510차가 `NEXT_TODO.md`에 등록만 해 뒀고 그대로 둔다 |
| 511차 라이브 검증 | — | 재기동이 필요한 관측 작업이지 코드 작업이 아니다(`O-63`) |

### 5. 테스트

**추가·실행한 테스트 없음** — 코드 변경이 0건이라 회귀 대상이 없습니다. 문서 3건만
바뀌었고 그중 둘은 append 전용 기록 파일입니다.

### 6. 사용자가 직접 하실 일

1. **점검 지침서 앱 사본 갱신** (`save_skill`, `overwrite: true`, 정본 `rev: 2026-08-26e`)
   — 11회째 이월입니다. 앱 안쪽 저장소라 세션이 대신 못 합니다. **1분.**
2. **내일 아침 프로그램 재시작 확인** — 511차 안전 패치가 그때 켜집니다. 자동으로
   적용되니 별도 조작은 필요 없고, 켜졌는지만 보시면 됩니다. **1분.**
3. ~~프롬프트 파일 git add~~ — **이미 되어 있습니다. 하실 일 없습니다**(위 2절).

### 7. 커밋 해시

- `16302b2` [MW0602] 512차 후속: 장후 자동조치 — 코드변경 0건 · 커밋 정리 · 낡은
  사용자조치 항목 적발 (문서 3건, 코드·테스트 변경 없음) · `origin dev` 푸시 완료
  (`0127f78..16302b2`).
- 이 절(§7)만 해시 기입을 위해 뒤이어 한 번 더 커밋됐다.
