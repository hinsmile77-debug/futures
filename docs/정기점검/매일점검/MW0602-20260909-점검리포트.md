# 미륵이 일일 점검 — MW0602 / 2026-09-09

# 장전(pre)

- 점검 시각: 08:59~09:03 KST · PC `MW0602`
- HEAD `25396fa` · 미커밋 6건 · 당일 커밋 0건
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260909_pre.md`
- 스킬 지침서: 정본 rev `2026-08-26e` = 앱 사본 rev `2026-08-26e` — **일치**(사본 드리프트 없음)

## 0. 한 줄 결론

**오늘 거래할 자격이 됩니다.** 모델·스케일러·매크로 판단(레짐)이 모두 정상적으로 준비를 마쳤고, 안전장치를 강제로 꺼둔 상태나 어제부터 넘어온 고장은 없습니다. 다만 두 가지는 개장 직후 상황을 계속 지켜봐야 합니다 — ①일부 보조 계산값 하나가 아직 "임시 대체값"으로 계산되고 있고(거래 판단에 직접 관여하는 핵심 값은 아닙니다), ②개장 첫 순간 일부 입력값이 평소 범위를 크게 벗어난 것으로 감지됐습니다(과거에도 있었던, 개장 때 흔한 현상으로 보입니다). 위험을 뜻하는 신호는 아니지만 장중 점검에서 확인이 필요합니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 정상 (관찰 항목 2건) | 모델 6개 호라이즌 전부 로드 성공 · 레짐 NEUTRAL 확정 · 어제 마감·재학습 성공 · CB 강제정지 이력 없음 |
| 장중 | (미실행) | |
| 장후 | (미실행) | |

**절대원칙 준수 여부** (장전 시점 — 아직 거래가 시작되지 않아 대부분 "해당없음")

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | 해당없음 | 아직 장 시작 전. 어제(09-08) 포지션은 정상 마감(`[Shutdown] 정상 종료 플래그 기록`, 15:40:19) |
| ② | Circuit Breaker 5종 | 정상(대기) | 오늘 아침 HALT/PAUSED 로그 없음. CB②는 모의투자 한정으로 여전히 사실상 꺼둔 상태(설계된 예외, CLAUDE.md 기재) |
| ③ | CORE 피처 호라이즌 그룹별 | 조건부 | `ofi_norm` 1개가 개장 후에도 "평평한 값" 방지용 임시 대체 상태 — 아래 1-1 참조. 강제 차단 대상 아님 |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 해당없음(장전 관측 불가) | 코드 변경 없음(장전 규칙 준수) |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 해당없음(장전 관측 불가) | |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 정상 | 자동 통합 흔적 없음 |

**설정 불변식** — 수집기 §3 표 전부 `일치`. 이상 없음.

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

해당 없음.

### P1 — 정확성·관측 훼손

해당 없음.

### P2 — 운영 부담·기술부채

#### 1-1. CORE 피처 `ofi_norm`이 개장 이후에도 "임시 대체값" 상태로 남아 있음

- **증상**: 매수·매도 힘의 불균형을 재는 `ofi_norm`이라는 값이, 최근 30개 분봉 동안 거의 변화가 없어(표준편차 ≈0) 프로그램이 이 값을 있는 그대로 쓰지 못하고 "평평한 값(0)으로 몰리는 것을 막기 위한 표준 대체값"으로 바꿔 쓰고 있습니다. 장 시작 직전(08:58:57)에도, 장이 시작한 직후(09:00:57)에도 이 상태가 이어졌습니다.
- **근거**:
  ```
  2026-09-09 08:58:57 [WARNING] SIGNAL: [ScalerRefresh] 1m CORE 'ofi_norm' raw_std≈0(0.0264) → identity(0,1) 강제 (FLAT 100% 방지) n_nonzero=30/30 frac_zero=0.000 raw_range=0.1403
  2026-09-09 08:58:57 [WARNING] SIGNAL: [CORE준비도] trigger=A_WARMUP 축퇴 1/6 — ofi_norm×6hz | 정상 진입 전 해소 여부를 확인하라
  2026-09-09 09:00:57 [WARNING] SIGNAL: [CORE준비도] trigger=B_OPEN 축퇴 1/6 — ofi_norm×6hz | 정상 진입 전 해소 여부를 확인하라
  ```
  `logs/20260909_SIGNAL.log` · 총 65건(장전 구간) · 08:45:17~08:58:57(장전) / 09:00:57(개장 직후 재확인)
- **기준 위반**: CLAUDE.md 절대원칙 §3(CORE 피처, 단기 그룹) — 단 `ofi_norm`은 미통과 시 "등급 하락"만 규정돼 있고 "강제 차단(X)" 대상이 아니므로 즉시 거래 중단 사유는 아님.
- **영향**: 진입 여부를 정하는 여러 판단 근거 중 하나가 오늘도 "표준 기본값"으로 대체돼 있습니다. 이것만으로 거래가 막히지는 않지만, 이 값이 실제로 필요한 순간에는 판단 정확도가 조금 떨어질 수 있습니다.
- **영향(기술)**: `ScalerRefresh`가 `ofi_norm`의 raw_std≈0을 감지해 identity(0,1) 스케일러를 강제 적용. `CORE준비도` 축퇴 개수 1/6(단기 6호라이즌 중 1개 피처)이 A_WARMUP(장전)과 B_OPEN(개장) 두 트리거에서 모두 관측됨 — `references/postmortem.md` 관측 축 `CORE축퇴_개장해소` 기준으로는 "미해소" 사례.
- **신규 여부**: **신규 아님 — 기존 다발 패턴**. `dev_memory/DECISION_LOG.md` 항목 `[1-4] 단기 CORE 'ofi_norm' identity 강제`(0824~ 연속 관측)와 동일 계열. 이미 처분 경로가 등록돼 있음(A-18/A-19, 주간회의 전용 — CLAUDE.md 절대원칙 §3이 CORE 교체 자체를 금지하므로 일일 점검에서 새 안건을 내지 않음). **여기서는 "오늘도 재현됨"만 기록**.

#### 1-2. 개장 직후(09:00:57) 6개 시간대(호라이즌) 전부에서 동일한 3개 입력값이 평소 범위를 크게 벗어남 — 확인 필요

- **증상**: 정규장이 시작된 첫 분(09:00~09:01봉)에서, `기관 선물 순매수`·`전일 같은 시각 대비 수익률`·`거래량 가속도` 세 값이 1m·3m·5m·10m·15m·30m **여섯 시간대 모두**에서 동시에 "평소보다 4배 표준편차 이상 벗어난 값"으로 감지됐습니다.
- **근거**:
  ```
  2026-09-09 09:00:57 [WARNING] SIGNAL: [Model] 1m 극단 z-score 3개 피처 감지 (|z|>4) — 스케일러 노후화 또는 이상 데이터 의심
  2026-09-09 09:00:57 [WARNING] SIGNAL: [Model] 1m extreme z-score top=institution_futures_net=+11.77, prev_day_same_hour_ret=-11.66, volume_acceleration=+7.25
  2026-09-09 09:00:57 [WARNING] SIGNAL: [Model] 3m/5m/10m/15m/30m 극단 z-score 3개 피처 감지 (동일 패턴)
  ```
  `logs/20260909_SIGNAL.log` · 6개 호라이즌 × 2줄(요약+대표) · 09:00:57 단일 시각
- **기준 위반**: 없음(안전장치 위반 아님) — 계측·경보 목적의 WARNING.
- **영향**: 장이 열리는 바로 그 순간에는 원래도 물량이 몰리고 가격이 크게 튀기 때문에, 이런 값들이 순간적으로 튀는 것 자체는 드문 일이 아닙니다. 다만 6개 시간대 전부에서 같은 3개 값이 동시에 튄 것은 오늘 처음 정확히 이 조합으로 확인된 것이라, 예측 정확도에 실제 영향이 있었는지는 장중에 더 봐야 압니다.
- **영향(기술)**: `institution_futures_net`·`prev_day_same_hour_ret`·`volume_acceleration`은 CORE 피처(`cvd_delta_norm`/`ofi_pressure`/`vwap_position`)가 아니므로 2026-06-09 결정(`_CORE_MASK_EXEMPT`)의 AutoMask 면제 대상이 아닐 가능성이 있음 — AutoMask가 실제로 이 3개를 격리했는지, 격리됐다면 그 분봉의 예측·진입체크에 어떤 영향을 줬는지 로그(`[AutoMasked]`)로 확인 필요. 유사 사례(`dev_memory/DECISION_LOG.md` [1-3], F-Q 섀도 계획)는 다른 4개 피처(`ofi_imbalance`·`ofi_reversal_speed`·`microprice_bias`·`cancel_add_ratio`) 조합이었어서 **피처 구성이 다름** — 완전히 같은 재현은 아님.
- **신규 여부**: 확인 필요(313차 원칙 — 1회 관측, 확정 결론 금지). 개장 시 z-score 급등 자체는 알려진 현상(F-Q 섀도 계획 존재)이나 오늘의 구체적 피처 조합은 처음 확인됨. → 아래 §6 O-75로 등록.

### 확인 필요 (확정 아님)

- 1-2(위)와 동일 — 장중 점검에서 09:00~09:05 구간 `[AutoMasked]` 로그와 실제 예측·진입 영향을 대조해야 판정 가능.

### 이미 반영된 사안 (신규 아님)

- **`ofi_norm` CORE 축퇴(1-1)** — `dev_memory/DECISION_LOG.md` [1-4], 0824차부터 연속 관측. 처분 경로는 A-18/A-19(주간회의 전용, 절대원칙 §3이 CORE 교체 금지). 오늘 새 조치 불필요.
- **개장 시 z-score 급등 현상 자체** — `dev_memory/DECISION_LOG.md` [1-3], F-Q(섀도 계측 신설, 08:59 재측정) 계획 존재. 다만 오늘의 구체 피처 조합은 다름(위 1-2 참조) — 그래서 "완전히 같은 사안"으로 종결하지 않고 §6에 별도 관측으로 남김.
- **미커밋 6건** — `docs/정기점검/금요일점검/MW0602/` 아래 2026-09-04(금요일) 캠페인 산출물 6종(코드 변경 아님, 정기 생성물). 코드 변경이 아니므로 실전 PC 운영 규칙 위반 아님. 커밋 여부는 사용자 판단(§9 사용자 조치 참고).
- **PC명 태그 누락 커밋 1건**(`8a01299 Merge branch 'dev'...`) — 이전 세션에서 이미 존재하던 병합 커밋으로 오늘 새로 생긴 것이 아님. 신규 조치 불필요.
- **NEXT_TODO 이월 항목들**(`1-2` 3m 호라이즌 EOD 교체 보류 7거래일째 예상·`1-3` raw_data.db pruning lock·`1-5` 2026-09-07 브로커 net 오염 정정 승인 대기·`1-6` 왕복비용 라이브 요율 사용 문제·`F-1`/`F-2`/`C-5`) — 전부 어제(09-08) 장후 세션에서 이미 등록·추적 중인 항목. 오늘 장전 점검에서 상태 변화 없음. 상세는 §9 사용자 조치에 다시 옮기지 않고 `dev_memory/NEXT_TODO.md` 원본을 참조하도록 안내만 함.

---

## 2. Fix 작업 구현계획

오늘 장전 관측에서 새로 필요한 코드 수정 없음. 1-1·1-2 모두 기존 설계(CORE 교체 금지, AutoMask 면제 정책)의 정상 범위 안에 있고, 처분 경로도 이미 등록돼 있음(A-18/A-19 주간회의, F-Q 섀도).

> 장전 국면이므로 어차피 코드 변경·배포·재기동은 하지 않음(라이브 프로세스 구동 중).

---

## 3. 고도화 방안

### G-1. 1-2 z-score 급등의 재현성 확인을 위한 개장 첫 5분 피처 스냅샷 비교

- **관측 근거**: 오늘 개장 첫 분(09:00:57)에 6개 호라이즌 전부에서 `institution_futures_net`·`prev_day_same_hour_ret`·`volume_acceleration` 3개 피처가 동시에 |z|>4를 기록. 과거 알려진 사례([1-3])와 값이 튄 피처 구성이 다름.
- **제안 내용**: `[Model] N개 피처 감지` WARNING이 뜬 날짜·시각·피처명·z값을 별도로 누적 집계하는 표(§12 고착 지표류 형태)를 만들어, "개장 시 항상 튀는 피처"와 "오늘만 튄 피처"를 구분한다.
- **기대 효과**: 매일 다른 피처가 튀는지, 특정 피처(예: `institution_futures_net`)가 반복적으로 튀는지 알 수 있으면 F-Q 섀도 계획의 범위를 그 피처들까지 넓힐지 판단할 수 있다.
- **비용·위험**: 낮음 — 기존 로그를 파싱해 집계만 추가. 판정·차단 로직에는 손대지 않음.
- **선행 조건**: 없음.
- **우선순위 제안**: 26주 WFA 주기 또는 다음 §12 지표 확장 시점에 편입 검토(신규 P0/P1 아님).

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| (없음) | — | — | 오늘 관측만으로는 실전 전환 기준에 반영할 만한 새 근거 없음 |

---

## 4. 진입 승패 사후검증 (장후 한정)

해당 없음(장후 전용) — 장전 점검이므로 오늘 진입 자체가 아직 없음(§0 표 참조, 매분 루프 09:00 시작 직후).

## 5. 수익률 향상방안 (장후 한정)

해당 없음(장후 전용).

---

## 6. 다음 거래일 관측 예정

> 관측 ID 채번: 다음 관측 ID = **O-75** (다이제스트 §11 산출값 그대로 사용)

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| O-75 | `[Model] N개 피처 감지 (|z|>4)`(09:00~09:05 구간) + `[AutoMasked]` 동시 출현 여부, `institution_futures_net`/`prev_day_same_hour_ret`/`volume_acceleration` 재현 여부 | 같은 3개 피처가 다시 튀면 "구조적"(패턴), 매일 다른 피처면 "장 초반 일반 현상". AutoMask 격리가 실제로 걸렸는지도 확인 | 2026-09-09 장중/장후 점검 |

## 7. 기한 도래 항목

`references/invariants.md` §2 대조 — 오늘 신규 기한 도래 없음.

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| CB② 복원 (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | 사건 기한(⑧ 해제와 동일 커밋) — 날짜 아님 | — | 489차 D2, 오늘 변화 없음 |
| `1-3`(0907 P1) `raw_data.db` pruning lock 정정 승인 | 2026-09-14(월) EOD 전 | 5거래일 | 사용자 승인 대기 (기존 NEXT_TODO 항목) |

---

## 8. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-09-09 (MW0602 548차 — 일일 점검: 장전)` (아래 실행)
- `NEXT_TODO.md` 추가 체크박스: O-75 1건
- 완료 처리한 기존 항목: 없음(장전 국면 — 판정은 장중/장후 몫)

---

**커밋 대기**: 없음(예약 실행이라 커밋하지 않음). 이 세션이 새로 만든/수정한 파일 —
`docs/정기점검/매일점검/evidence_MW0602-20260909_pre.md`(신규),
`docs/정기점검/매일점검/MW0602-20260909-점검리포트.md`(신규),
`dev_memory/DECISION_LOG.md`(append 예정),
`dev_memory/NEXT_TODO.md`(append 예정).

---

# 장중(intra)

- 점검 시각: 12:41~13:05 KST · PC `MW0602` · 관측 창 09:00~12:41(진행 중 — 15:10까지 판정은 장후 몫)
- HEAD `25396fa` · 미커밋 9건(dev_memory 2 + 금요일 캠페인 산출물 6 + 이 리포트 1, 코드 변경 없음) · 당일 커밋 0건
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260909_intra.md`
- 인덱스락: 없음 · 병행 세션: 없음(오늘 신규 산출물은 이 세션 것뿐)

## 0. 한 줄 결론

**지금까지는 정상적으로 돌아가고 있고, 오늘 거래 중 위험 신호는 없습니다.** 아침에 확인이 필요하다고 남겨뒀던 두 가지 모두 확인됐습니다 — ①값이 튀었던 세 가지 입력은 프로그램이 설계대로 즉시 걸러내고 넘어갔습니다 ②그 세 값의 조합은 오늘 한 번만 나타났고 다시 반복되지 않았습니다. 지금까지 4번 진입해서 3번 이익, 1번 손실로 마감했고 손실 금액보다 이익 금액이 커서 합계는 이익입니다(+83,938원). 매분 해야 할 일이 빠짐없이 돌았고(222분 중 222분, 공백 0건), 안전장치를 강제로 꺼둔 상태에서 문제가 생긴 것도 없습니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 정상 (관찰 항목 2건 → 아래에서 모두 처분) | §0 표 참조 |
| 장중 | **정상 (신규 P0/P1 없음)** | 포지션 4건 3승1패 +83,938원 · 매분 루프 222/222(100%) 공백 0 · vwap 미통과 진입 0건 · CB 발동 없음(카운터 증가만) |
| 장후 | (미실행) | |

**절대원칙 준수 여부** (09:00~12:41 구간)

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① 15:10 강제청산 | 해당없음(아직 도래 전) | 장후 점검 대상 |
| ② Circuit Breaker | 정상 | CB① 미발동, CB②는 카운터만 증가(정지 없음 — 설계된 예외), CB③-P4는 acc30m NORMAL→RESTRICTED 전환 6건이지만 차단 자체가 비활성이라 무영향, CB④·⑤ 미발동(메인스레드 블로킹 최대 2,578ms로 5초 미만) |
| ③ CORE 피처 (강제 X는 vwap 위반) | **정상 — vwap 위반 진입 0건** | 오늘 진입 4건의 `[진입체크]` 원문 전부 `vwap✅` 확인(`logs/20260909_TRADE.log` 10:30:57·11:06:58·11:33:58·11:44:57). `ofi_norm` identity 강제는 지속(1-1, 등급하락 대상일 뿐 강제X 아님) |
| ④ COM 콜백 dynamicCall·emit 금지 | 해당없음 | 장중 코드 변경 없음(원칙 준수) |
| ⑤ GetRepeatCnt/GetCommData | 해당없음 | 장중 코드 변경 없음 |
| ⑥ 알파봇 자동통합 금지 | 정상 | 흔적 없음 |

---

## 1. 이월 처리표 (장전 → 장중)

| 번호 | 항목 | 처분 | 근거 |
|---|---|---|---|
| 1-1 | `ofi_norm` CORE 축퇴(identity 강제) | 🔄 지속 | 09:00:57 이후에도 `[CORE준비도]` 축퇴 관측 지속(08:45:17~10:34:57 구간, 최종 1/6). 기존 처분 경로(A-18/A-19, 주간회의 전용) 그대로 — 절대원칙 §3이 CORE 교체 자체를 금지하므로 신규 조치 없음 |
| 1-2 | 개장 첫 분 6개 호라이즌 동시 z-score 급등(`institution_futures_net`·`prev_day_same_hour_ret`·`volume_acceleration`) → `O-75`로 등록 | ✅ 해소(정상 작동 확인) | 09:00:57 `[Model] 극단 z-score 3개 피처 감지` 직후 같은 분(09:00:57) `[AutoMasked] 이상값 3개 즉시 격리 예측 (CORE 제외): ['institution_futures_net', 'volume_acceleration', 'prev_day_same_hour_ret']`로 **즉시 격리 확인**. 이후 09:01:57부터는 다른 피처 조합(`ofi_imbalance`·`ofi_reversal_speed`·`cancel_add_ratio`·`queue_directional_depletion` 등, 기존 `[1-3]` F-Q 계열과 동일)으로 전환돼 **오늘의 특정 3피처 조합은 재발하지 않음**. 판정: 개장 첫 분의 일반적 스케일러 불안정 현상이었고, AutoMask가 설계대로 CORE 제외 피처를 격리해 실제 예측에 영향을 주지 않았다 |
| — | 미커밋 9건(dev_memory 2·금요일 캠페인 산출물 6·리포트 1) | 지속(장후 일괄 커밋 예정) | 코드 변경 없음 — 실전 PC 운영 규칙 위반 아님 |
| — | `F-1`·`F-2`·`C-5`·`1-2`(3m EOD 교체 보류, 오늘로 7거래일 연속)·`1-3`(pruning lock, 2026-09-14 EOD 전 승인 대기)·`1-5`(브로커net 오염 정정 승인 대기)·`1-6`(왕복비용 라이브요율, C등급)·`launcher_guard`(발화 지속, 이미 P1 기록됨 — 재론 안 함)·`O-55`·`O-59`·`O-70`·`O-73` | 🔄 전부 지속 — 상태 변화 없음(아래 §6 표본 추가만 반영) | `dev_memory/NEXT_TODO.md` 09-08 장후~09-09 장전 항목 그대로. 오늘 장중 새 판정 근거 없음 |

**미처분 0건.**

---

## 2. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**해당 없음.** vwap 미통과 진입 0건(4건 전부 `vwap✅`), 브랜치 `dev` 일치, 인덱스락 없음, 오버나이트 해당없음(장중).

### P1 — 정확성·관측 훼손

해당 없음(신규).

### P2 — 운영 부담·기술부채 (신규 없음 — 기존 계열 재확인만)

#### 2-1. 사이저 산출(최대 3계약)이 실제 진입(최대 2계약)에서 매번 눌림 — 기존 계열, 오늘도 재현

- **증상**: 오늘 사이저는 4번 모두 3계약을 산출했지만 실제 체결은 4번 모두 2계약이었습니다. 이유는 셋 중 하나였습니다 — 손절 안정성 판단(Hurst)이 배수를 절반으로 깎은 경우가 3번, 증거금(계좌에 걸 수 있는 최대 담보) 한도가 먼저 깎은 경우가 1번(10:30:57)입니다.
- **근거**: `logs/20260909_SIGNAL.log` `[SizerMatch]` 4건 — 10:30:57 `binding=없음(안전군·상한·증거금 경로)`, 11:06:58/11:33:58/11:44:57 `binding=hurst(0.50)`.
- **기준 위반**: 없음 — 설계대로 동작. `MAX_CONTRACTS=3` 상한 자체는 오늘도 구속하지 않음(431차 이후 지속되는 상태).
- **영향**: 실전 전환 기준 ⑧의 `[28] sizing_inversion_watch` 채널이 필요로 하는 "3계약 이상 실제 체결" 표본이 오늘도 쌓이지 않았습니다.
- **영향(기술)**: `binding_gate` 분포에 `margin` 1건이 추가됐다는 점은 470차가 지적한 "⑧ 해제 전까지 증거금이 먼저 자른다"는 구조를 오늘도 재확인한 것. 신규 조치 없음(이미 등록된 구조적 상태).
- **신규 여부**: 신규 아님. `O-55`(SizerMatch tox 바인딩 관찰)에 오늘 표본 추가(hurst×3, 없음×1, tox 0건) — 판정 보류 유지.

### 확인 필요 (확정 아님)

해당 없음 — O-75는 위 이월표에서 해소 처리.

### 이미 반영된 사안 (신규 아님)

- **`A급(원시C)` 승격 4건(오늘 진입 전부)** — 체크리스트 상향 경로는 462차가 이미 "승격이 성과를 바꾸지 않는다(승격효과 -3,058원 vs 원시등급효과 -106,022원)"로 분해·확정한 채널 `[36] grade_x_promotion`의 정상 동작. 신규 조치 불필요.
- **CB③-P4 acc30m NORMAL→RESTRICTED 전환 6건** — 30m 퇴역(296차) 이후 상시 관찰되는 패턴(CLAUDE.md 절대원칙 §2 각주). 차단 자체가 비활성이라 실거래 영향 없음.
- **`WeightCollapse` 안전망 44건(09:07:57~12:38:03)** — 앙상블 호출 221건 대비 약 19.9%로 기존 관측 밴드(20.3~20.5%±5%p, `O-1`/`O-8`/`O-43` 계열) 안. 신규 조치 불필요.
- **`Engine run_shadow` 목표초과(5ms) 221건** — §12 고착지표 `challenger_shadow`(SLOW 100%)로 이미 P2 등록·추적 중인 사안. 오늘 최대 46.9ms(0825 최댓값 412ms보다 낮음). 재론 안 함.

---

## 3. Fix 작업 구현계획

오늘 장중 관측에서 새로 필요한 코드 수정 없음. 사이저-증거금 상한 구조(2-1)는 실전 전환 기준 ⑧ 해제 시 함께 재설계될 사안이며 일일 점검 범위 밖.

> 장중 국면이므로 코드 변경·배포·재기동은 하지 않음(라이브 프로세스 구동 중).

---

## 4. 고도화 방안

### G-2. O-75류 관측을 "AutoMask 격리 성공/실패" 축으로 상시 집계

- **관측 근거**: 오늘 O-75가 AutoMask 로그 대조만으로 15분 내 판정 가능했다(마스킹 여부가 이미 로그에 있었음).
- **제안 내용**: `[Model] 극단 z-score N개 피처 감지` 발생 시 같은 분(또는 다음 1분 내) `[AutoMasked]`가 해당 피처를 포함해 출현했는지 자동 대조하는 표를 §12류 고착지표에 추가.
- **기대 효과**: "튀었는데 격리 안 됨" 케이스만 사람이 보면 되므로 장중 점검 시간이 줄어든다.
- **비용·위험**: 낮음 — 기존 로그 파싱만 추가, 차단·임계 로직 무변경.
- **우선순위 제안**: 26주 WFA 주기 또는 §12 지표 확장 시점.

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| (없음) | — | — | 오늘 장중 관측만으로는 실전 전환 기준에 반영할 근거 없음 |

---

## 5. 진입 승패 사후검증 (장후 한정)

해당 없음(장후 전용) — 진행 중인 장중이므로 15:10 이후 최종 결과가 아직 없음. 현재까지 잠정: 포지션 4건(레그 8) · 승 3 / 4 (75%) · 합계 +2.02pt · gross +101,000원 · 수수료 17,062원 · **net +83,938원**.

## 6. 수익률 향상방안 (장후 한정)

해당 없음(장후 전용).

---

## 7. 다음 거래일/장후 관측 예정

> 관측 ID 채번: 다음 관측 ID = **O-76** (다이제스트 §11 산출값)

| ID | 관측 대상 | 판정 기준 | 판정 예정 |
|---|---|---|---|
| O-73 | `[ProfitGuard]` Tier 발동 시각·기준액(계좌전체 vs 시스템 실현손익) | 오늘 net(+83,938원 잠정)이 Tier 발동 임계에 못 미쳐 아직 미발동. 15:10까지 임계 도달 시 그 시각·기준액을 기록해 판정 | 오늘 장후 |
| O-55 | `[SizerMatch]` tox 바인딩 비율 | 오늘 표본(hurst×3, 없음×1) 추가 — tox 바인딩 0건 지속. 표본 축적 대기 | 다음 거래일 이후 |

## 8. 기한 도래 항목

`references/invariants.md` §2 대조 — 오늘 신규 기한 도래 없음(장전과 동일).

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| CB② 복원 | 사건 기한(⑧ 해제와 동일 커밋) | — | 변화 없음 |
| `1-3` `raw_data.db` pruning lock 정정 승인 | 2026-09-14(월) EOD 전 | 5거래일 | 사용자 승인 대기 |

---

## 9. dev_memory 반영

장중에는 커밋하지 않음 — 장후 점검에서 장전+장중 항목을 한 번에 `DECISION_LOG.md`·`NEXT_TODO.md`에 반영 예정. 이번 장중에서 새로 처분된 항목: `O-75`(✅ 해소).

---

**커밋 대기**: 없음(예약 실행, 장중은 커밋하지 않음). 이 세션이 새로 만든/수정한 파일 —
`docs/정기점검/매일점검/evidence_MW0602-20260909_intra.md`(신규),
`docs/정기점검/매일점검/MW0602-20260909-점검리포트.md`(이 세션에서 장중 절 append),
`dev_memory/DECISION_LOG.md`(장후에 일괄 append 예정),
`dev_memory/NEXT_TODO.md`(장후에 일괄 append 예정).

---

# 장후(post)

- 점검 시각: 16:32~16:50 KST · PC `MW0602`(오버라이드 — 실행 호스트 `host=claude`, 지정 PC MW0602)
- HEAD `25396fa` · 미커밋 9건(코드 변경 0건 — `dev_memory` 2 + 금요일 캠페인 산출물 6 + 이 리포트 1) · 당일 커밋 0건
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260909_post.md`
- 스킬 지침서: **저장소 정본**(`.claude/skills/mireuk-daily-check/SKILL.md`) rev `2026-08-26e`(664줄)를 먼저 읽고 진행. 앱 사본과 일치(사본 드리프트 없음). 예약 프롬프트가 로드한 사본이 지시한 절차보다 정본의 절차(0-B rename 우회·병행세션확인·2점 인덱스락 샘플 등)가 더 상세해 정본을 따랐다.
- 범위: **EOD+P8** — EOD(`daily_close`) 15:40:19 완료 · P8(스케일러 재적합) 15:52:48 완료. 둘 다 `data/session_state.json`에 `p8_last_success_date=2026-09-09`, `eod_retrain_ok_date=2026-09-09`로 확인됨.
- 인덱스락: 없음(실행 시작·렌더 2점 확인) · 병행 세션: 없음(오늘 신규 산출물은 이 리포트를 쓴 장전·장중·장후 세션뿐)

## 0. 한 줄 결론

**오늘 하루 절대원칙을 지키며 정상적으로 마감했습니다.** 4번 진입해서 3번 이익, 1번 손실로 끝났고 수수료를 뺀 최종 손익은 **+83,938원**입니다(브로커 실측 기준으로도 오차 2원 이내로 같음). 밤새 포지션을 들고 가는 것을 막는 규칙(오버나이트 금지)은 15:10 이전에 이미 모든 포지션이 청산되어 있어 자연히 지켜졌습니다. 하루를 마감하는 학습 작업(EOD)과 그 다음 날 예측 기준값을 다시 맞추는 작업(P8)도 둘 다 정상적으로 끝났습니다. 아침에 확인이 필요하다고 남겨둔 항목 두 가지는 모두 장중에 해소됐습니다. 새로 발견된 위험 신호(P0)는 없고, 운영상 계속 지켜봐야 할 기존 사안 두 가지(사소한 계산값 하나가 개장 초반에만 임시값으로 대체되는 것, 계산된 계약 수보다 실제로 더 적게 체결되는 구조적 상태)는 오늘도 재현됐지만 전부 이미 파악돼 있던 것이고 거래 자체를 막지 않았습니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 정상 (관찰 항목 2건 → 장중에 모두 처분) | §0 표 참조 |
| 장중 | 정상 (신규 P0/P1 없음) | 포지션 4건 3승1패 +83,938원 · 매분 루프 222/222(100%) · vwap 미통과 진입 0건 |
| 장후 | **정상 (신규 P0/P1 없음, 기존 P2 계열 2건 재확인)** | EOD·P8 정상 완료 · 절대원칙 6종 전부 준수 · 15:10 강제청산 대상 없음(이미 전량 청산) |
| **당일 최종** | **정상** | 4포지션 3승1패(75%) · +2.02pt · net **+83,938원**(포지션 단위) · 안전장치 강제비활성 상태에서 이상 없음 |

**절대원칙 준수 여부** (종가 기준 — 장중 중간판정 아님)

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | **준수** | 11:45:27 이후 포지션 없음(FLAT). 15:11:18 `[SchedForceExit] status=FLAT … bar_pass=1회 → 청산 대상 없음(정상)`. 15:18 안전망 단계까지 갈 필요 자체가 없었음 |
| ② | Circuit Breaker 5종 | 준수(①·③·④·⑤ 미발동, ②는 설계된 예외로 카운터만 동작) | CB②는 `CB_CONSEC_STOP_LIMIT=9999`로 모의투자 한정 사실상 비활성(카운터만 증가, 11:45:27 "연속 손절 1회"). CB③-P4 acc30m NORMAL→RESTRICTED 전환 10건이나 30m 퇴역으로 차단 자체 비활성(무영향). CB④·⑤ 미발동(메인스레드 블로킹 최대 2,578ms, 5초 미만) |
| ③ | CORE 피처 호라이즌 그룹별 | **정상 — 강제X(vwap) 위반 0건** | 진입 4건 전부 `[진입체크]` 원문에 `vwap✅` 확인. `ofi_norm` identity 강제는 08:45~10:34에만 재현되고 10:35 이후 EOD까지 재발 0건(§1 3-1 참조 — 오늘은 개장 후 약 1시간45분 만에 해소) |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 해당없음(오늘 코드 변경 0건) | `git status` 상 변경분은 문서·`dev_memory`뿐, 코드 파일 변경 없음 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 해당없음(오늘 코드 변경 0건) | 위와 동일 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 정상 | 자동 통합 흔적 없음 |

**설정 불변식** — 수집기 §3 표(21개 항목) 전부 `일치`. 차단 게이트 인벤토리 32개 중 8개 꺼짐 — 전부 이미 기록된 한시예외(CB③-P4·FP-CRITICAL·TOX-SEVERE-SPREAD 등). 이상 없음.

---

## 0p-C. 이월 처리 — 장전+장중 항목 전부 처분

| 원 번호 | 제목(줄임) | 원 국면 | 처분 | 근거 |
|---|---|---|---|---|
| 1-1 | `ofi_norm` CORE 축퇴(identity 강제) | 장전(→장중 지속) | ✅ **해소**(오늘분 재발 종료) | `[CORE준비도]` 08:45:17~10:34:57 구간 17건 관측 후, 10:35 이후 EOD(15:52:47)까지 **"축퇴 0/6 — CORE 스케일 정상"** 20회 연속(`trigger=D_FORCE`/`C_PERIODIC`/`E_EOD`). 오늘은 개장 후 1시간45분 만에 해소됨. ⚠ 근본 원인(구조적 저변동 CORE 피처)은 미해결 — 처분 경로는 여전히 A-18/A-19(주간회의 전용, 절대원칙 §3 CORE 교체 금지). "오늘분 재현·해소 시각"만 새로 기록 |
| 1-2 | 개장 첫 분 6개 호라이즌 동시 z-score 급등 (`O-75`) | 장전 | ✅ 해소(장중에 이미 처분) | 장중 절 이월표에서 처분 완료 — 09:00:57 즉시 AutoMask 격리 확인, 이후 재발 0건 |
| 2-1 | 사이저 산출(3계약)이 실제 진입(2계약)에서 매번 눌림 | 장중 | 🔄 **지속(하루 종일)** | 오늘 4건 전부 사이저 3계약 산출 → 실제 2계약 체결. `binding` 분포: `hurst(0.50)`×3, `없음(증거금 경로)`×1(10:30:57, `[MarginCap] 산출=3→상한=2`). 실전 전환 기준 ⑧의 `[28] sizing_inversion_watch` 채널은 오늘도 3계약 이상 실체결 표본을 못 쌓음(431차 이후 74+4=78포지션 연속 qty≥3 0건) |
| — | 미커밋 9건(dev_memory 2·금요일 캠페인 산출물 6·이 리포트 1) | 장전~장후 | 🔄 지속 → 아래 "커밋 대기"로 일괄 이관 | 코드 변경 없음 — 실전 PC 운영 규칙 위반 아님 |
| — | `F-1`·`F-2`·`C-5`(장전부터의 사용자 결정/승인 대기)·NEXT_TODO `1-2`(0907, 3m EOD 교체 보류 7거래일째)·`1-3`(0907, pruning lock, 2026-09-14 EOD 전 승인)·`1-5`(0907, 브로커net 오염 정정 승인)·`1-6`(0907, 왕복비용 라이브요율 C등급)·`launcher_guard`(발화 지속, 이미 P1 기록 — 재론 안 함)·`O-55`(SizerMatch tox 바인딩)·`O-59`·`O-70` | 장전~장중 | 🔄 전부 지속 — 오늘 판정 근거 추가 없음(§4-1·§6 참조) | `dev_memory/NEXT_TODO.md` 그대로. 오늘 새 판정 근거 없음 |
| `O-73` | `[ProfitGuard]` Tier 발동 시각·기준액 관찰 | 장중 | **판정 보류 → 다음 거래일 이월** | 오늘 순손익 +83,938원이 Tier 발동 임계에 못 미쳐 로그상 `[ProfitGuard] 설정 업데이트 완료`(08:41:14)·`[ProfitGuard] 일간 리셋 완료`(15:40:18) 두 줄뿐, Tier 발동 로그 0건. 손익 규모가 더 커지는 날까지 판정 불가 |
| `O-55` | `[SizerMatch]` tox 바인딩 비율 | 장중 | **판정 보류 → 다음 거래일 이월** | 오늘 표본 4건 추가(`hurst`×3, `없음`×1) — tox 바인딩은 오늘도 0건. 누적 표본 축적 계속 필요 |

**미처분 0건.**

---

## 1. 이상점 정리보고 (하루 통합)

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**해당 없음.**

### P1 — 정확성·관측 훼손

**해당 없음.** 3원 대사(§4-2) 전부 일치, 오늘 할일 4분류(§4-1)에 `⚠완료주장-커밋없음` 항목 없음.

### P2 — 운영 부담·기술부채 (오늘 신규 없음 — 기존 계열만 재확인)

이월표(0p-C)의 `2-1`(사이저-증거금 상한 구조) 외 신규 없음. 아래는 오늘 처음 확인된 **관찰**(P-급 아님, 313차 원칙에 따라 확정 결론 아님)만 별도 기록.

#### 3-1. (관찰) 손실 포지션에서 앙상블 진입방향과 해당 호라이즌 단일모델 예측방향이 어긋남

- **증상**: 오늘 유일한 손실 포지션(11:44:58 LONG 진입, 5m 호라이즌)에서, 실제 매매를 결정한 앙상블 신호는 LONG(방향 +1)이었지만, 그 5분짜리 예측만 따로 보면 모델은 오히려 하락 쪽에 살짝 더 무게를 두고 있었습니다(하락확률이 상승확률보다 높음). 그리고 실제로 가격은 내려갔습니다.
- **근거**:
  ```
  predictions.db: ts='2026-09-09 11:44:00' horizon='5m' direction=0(FLAT) confidence=0.4385
                  up_prob=0.2678 down_prob=0.2937 flat_prob=0.4385 actual=-1(DOWN) correct=0
  ensemble_decisions: ts='2026-09-09 11:44:00' direction=1(LONG) confidence=0.4359 grade='C'→'A'
  trades: entry_ts='2026-09-09 11:44:58' … pnl_pts=-1.90 (합계, 부분+최종)
  ```
  `data/db/predictions.db` · `data/db/trades.db` 조회(읽기 전용) — 2026-09-09 11:44 단일 시점
- **기준 위반**: 없음(안전장치 위반 아님) — 앙상블은 여러 호라이즌·레짐 신호를 종합해 판단하므로 특정 한 호라이즌 예측과 다를 수 있음. 설계상 정상 동작 범위.
- **영향**: 오늘 하루 손실 −99,286원이 발생한 유일한 거래에서, 그 거래에 쓰인 5분짜리 개별 예측은 사실 반대 방향에 더 가까운 확률을 보이고 있었다는 뜻입니다. 이번 한 건만으로는 우연인지 반복되는 패턴인지 알 수 없습니다.
- **영향(기술)**: 5m 단일호라이즌 `predictions` 확률(up=0.2678/down=0.2937, 사실상 flat 우세·down>up)과 종합 앙상블 신호(direction=1, conf=0.4359)의 방향 불일치 사례. `meta_gate_horizon='5m'`으로 이 호라이즌이 게이트 판단에 실제 관여했음이 확인됨. n=1(오늘 1건)이라 313차 원칙상 파라미터 변경 근거로 쓸 수 없음 — 아래 §6 `O-76`으로 등록해 표본을 쌓는다.
- **신규 여부**: 신규 관찰. `dev_memory/NEXT_TODO.md`·`DECISION_LOG.md` 검색상 "앙상블-단일호라이즌 방향 불일치" 계열의 기존 등록 없음(함정① 확인 완료).

### 확인 필요 (확정 아님)

- 3-1(위) — n=1, 313차 원칙에 따라 확정 결론 보류. `O-76`으로 등록, 유사 사례 누적 시 판정.

### 이미 반영된 사안 (신규 아님)

- **`ofi_norm` CORE 축퇴(1-1)** — 오늘분은 10:35에 해소. 근본 처분 경로는 A-18/A-19(주간회의 전용, CORE 교체 금지). 신규 조치 불필요.
- **사이저-증거금 상한 구조(2-1)** — 431차·470차가 이미 규명한 구조(실전 전환 기준 ⑧ 해제 전까지 증거금이 먼저 자름). 신규 조치 불필요.
- **CB③-P4 acc30m 전환 10건** — 30m 퇴역(296차) 이후 상시 관찰 패턴, 차단 비활성이라 무영향.
- **GuardFair 무효 100%(48건/8일 누적, 오늘도 6건 추가)** — 457차 fair_valid 설계상 판정 무영향(섀도), 주간회의 2026-08-28 안건 ⑦ 입력으로 이미 등록됨.
- **3m EOD 모델 교체 보류(오늘로 7거래일 연속)** — `retrain_eod_20260909.log`: `[Retrain] 3m 교체 보류(EOD 모델가드) — acc 하락 0.0545 > 허용 0.0250 (new=0.3492 old=0.4037)`. `dev_memory/NEXT_TODO.md` `1-2`(0907)로 이미 등록·주간회의 상정 대기 중. 오늘은 "7거래일 연속"이라는 카운트만 갱신.
- **`mc-conf 괴리` 경보(15:40:18, 금일 19분 < 하한 25분)** — 369차부터 상시 운영되는 경보(변동성 자동조정 아님, 사용자 판단용). 오늘 값만 새로 관측된 것이지 경보 체계 자체는 기존 사안.
- **미커밋 9건 · PC명 태그 누락 커밋 1건** — 전부 기존에 이미 확인된 것(코드 변경 없음/과거 병합 커밋). 신규 조치 불필요.

---

## 2. Fix 작업 구현계획

오늘 하루 관측에서 새로 필요한 코드 수정 **없음**. 3-1(관찰)은 표본 1건이라 Fix 대상이 아니라 관찰 등록(O-76) 대상이다. 2-1(사이저-증거금 구조)은 이미 알려진 구조이며 실전 전환 기준 ⑧ 해제와 함께 재설계될 사안(일일 점검 범위 밖).

> 오늘은 코드 변경·배포·재기동을 하지 않았다(장전·장중은 라이브 프로세스 구동 중이라 원천 금지, 장후도 새로 필요한 변경이 없어 실행하지 않음).

---

## 3. 고도화 방안

### G-3. `O-76`(앙상블-단일호라이즌 방향 불일치) 상시 집계로 승격 검토

- **관측 근거**: 3-1 — 오늘 유일한 손실 거래에서 진입에 쓰인 호라이즌의 단일모델 확률이 실제 매매방향과 반대 쪽에 더 가까웠다.
- **제안 내용**: `entry_executed=1`인 모든 분에 대해 `ensemble_decisions.direction`과 `predictions`(해당 `meta_gate_horizon`)의 `up_prob`/`down_prob` 비교값(부호 일치 여부)을 매일 집계하는 표를 §12류 고착지표 형태로 추가한다.
- **기대 효과**: 방향 불일치가 손실과 상관이 있는지(반대로 일치해도 손실이 나는 경우와 비교) 표본이 쌓이면 판정할 수 있다.
- **비용·위험**: 낮음 — 기존 두 테이블 조인만 추가, 차단·임계 로직 무변경.
- **선행 조건**: 없음(읽기 전용 집계).
- **우선순위 제안**: 26주 WFA 주기 또는 §12 지표 확장 시점. 신규 P0/P1 아님.

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| (없음) | — | — | 오늘 관측(n=4, 1거래일)만으로는 실전 전환 기준 변경 근거 없음 |

---

## 4. 진입 승패 사후검증 (EOD+P8)

**4-0. 선행 확인** — EOD(15:40:19 완료, `daily_close`) · P8(15:52:48 완료, `p8_last_success_date=2026-09-09`) 둘 다 확인됨. 수집기 실행·DB 접근 모두 실패 없음(읽기 전용 조회, `predictions.db`·`trades.db` 정상 응답).

### 4-1. 오늘 할일 4분류

| # | NEXT_TODO 항목 | 당일 커밋 | 코드 실재 확인 | 상태 |
|---|---|---|---|---|
| `1-2`(0907) | 3m EOD 모델 교체 보류 — 7거래일 연속 | 없음 | `retrain_eod_20260909.log`: `[Retrain] 3m 교체 보류(EOD 모델가드)` | ⬜ 미착수(주간회의 상정 대기 — 사유: 사용자/주간회의 결정 필요, 일일 점검 범위 밖) |
| `1-3`(0907) | `raw_data.db` pruning lock 정정 승인 | 없음 | 코드 변경 없음, 승인 대기 상태 그대로 | ⬜ 미착수(사유: 사용자 승인 대기, 기한 2026-09-14 EOD 전) |
| `1-5`(0907) | 브로커net 오염 정정 승인 | 없음 | 코드 변경 없음 | ⬜ 미착수(사유: 사용자 승인 대기) |
| `1-6`(0907) | 왕복비용 라이브요율 사용 수정 | 없음 | `scripts/generate_validation_campaign_report.py:8905` 미수정 확인(grep) | ⬜ 미착수(사유: C등급·브랜치 이식 산물, 주간회의/사용자 지시 필요) |
| `O-75` | 개장 z-score 급등 재현성 | 없음(관찰 항목, 코드변경 대상 아님) | 해당없음 | ✅ 완료(판정) — 장중에 판정 완료, 위 0p-C 참조 |

> "완료"로 주장됐는데 커밋이 없는 항목(`⚠완료주장-커밋없음`)은 **오늘 0건**이다. 오늘 `DECISION_LOG.md`에 "완료"로 적힌 항목 자체가 없었다(장전 세션의 1-1·1-2는 "신규 조치 없음"으로 명시적으로 판정만 했을 뿐 "완료"를 주장하지 않음).

### 4-2. 3원 대사 (포지션 단위)

| 원천 | 질의 | 값 |
|---|---|---|
| 로그 | `[Position] 진입` (`TRADE.log`) | 4 |
| `ensemble_decisions` | `entry_executed=1` (2026-09-09) | 4 |
| `trades` | `COUNT(DISTINCT entry_ts)` | 4 |
| (참고) `trades` | `COUNT(*)` — 청산 레그 | 8 |

**포지션 4 = 4 = 4 — 일치.** 레그 8건은 불일치가 아니다(이익 포지션 3건이 TP1 부분청산+최종청산으로 각 2레그씩 쪼개짐).

### 4-3. 케이스 카드

```
[CASE-01] 진입 10:30:58 LONG · 2계약 (등급 A/원시 C · horizon=3m · hurst=neutral)
          → 10:31:24 TP1 부분청산 33% (+0.88pt/+41,876원) → 10:31:58 하드스톱[TP1보호] (+0.82pt/+38,876원)
          포지션 합 +1.70pt / +80,752원 · 보유 약 1분
 진입근거   [진입체크] LONG→LONG 2계약 A급(원시C) | sign✅ conf✅ vwap✅ cvd✅ ofi❌ fore✅ prev❌ time✅ risk✅ chas✅ coun✅ | conf=41.9%
            [Sizer] 신뢰도배수=0.6 레짐배수=0.8 안전배수=1.00 → 3계약, [MarginCap] 산출=3→상한=2로 축소(binding=증거금)
 예측 vs 실현  ensemble: direction=1(LONG) conf=0.4194 min_conf_effective=0.394
             predictions(3m): direction=0(FLAT) conf=0.4026, up=0.2994/down=0.2980(거의 동률) actual=1(UP)
 승패판정   ✅승 — 최종청산 `하드스톱`이지만 trades.tp1_reached=1·exit_outcome='이익'(보호 트레일)
 주 요인    A (방향예측 적중 — 앙상블 LONG, 실현 UP)
 부 요인    C (TP1 보호트레일로 이익 확정, 조기 확정이라 이후 추가 상승분은 못 얻음)
            D (사이징 — 증거금 상한이 3→2계약으로 실사이즈를 구속)
 반사실     보호트레일 없이 끝까지 홀드했다면 다음 진입시점(11:06, 1120.03)까지 가격이
            더 올랐으므로 추가 이득 가능성이 있었으나, 반대로 그 사이 반락 리스크도 있었다 —
            사후관찰일 뿐 결론 아님(313차 ④)

[CASE-02] 진입 11:06:58 LONG · 2계약 (등급 A/원시 C · horizon=1m · hurst=mean-revert)
          → 11:08:36 TP1 부분청산 33% (+0.23pt/+9,372원) → 11:08:58 하드스톱[TP1보호] (+1.09pt/+52,372원)
          포지션 합 +1.32pt / +61,744원 · 보유 약 2분
 진입근거   [진입체크] sign✅ conf✅ vwap✅ cvd✅ ofi✅ fore❌ prev✅ time✅ risk✅ chas❌ coun✅ | conf=41.6%
            [SizerMatch] sizer=3계약→actual=2계약, binding=hurst(0.50)
 예측 vs 실현  ensemble: direction=1(LONG) conf=0.4157
             predictions(1m): direction=1(UP) conf=0.3334, up=down=flat≈0.333(사실상 축퇴 — LEARNING.log
             동시간대 [Calibration] 축퇴 감지 패턴과 같은 계열) actual=0(FLAT), correct=0
 승패판정   ✅승 — `하드스톱` tp1_reached=1·exit_outcome='이익'
 주 요인    A (방향예측 적중 — 다만 해당 1m 단일예측 자체는 축퇴 상태로 정보가치가 낮았음, 아래 참고)
 부 요인    C (TP1 보호트레일) · D (Hurst 게이트가 사이즈를 절반으로 축소)
 반사실     해당하지 않음(단일 호라이즌 확률이 이미 무정보에 가까워 반사실 추정이 무의미)
 참고       predictions 1m 확률이 3분류 거의 동률(축퇴)이라는 점은 §12 고착지표 `Calib_폴백사유`
            계열의 기존 관찰과 일치 — 신규 발견 아님, 313차 원칙상 이 1건으로 결론 내지 않음

[CASE-03] 진입 11:33:59 LONG · 2계약 (등급 A/원시 C · horizon=3m · hurst=mean-revert)
          → 11:34:15 TP1 부분청산 33% (+0.38pt/+16,864원) → 11:34:59 하드스톱[TP1보호] (+0.52pt/+23,864원)
          포지션 합 +0.90pt / +40,728원 · 보유 약 1분
 진입근거   [진입체크] sign✅ conf✅ vwap✅ cvd✅ ofi✅ fore✅ prev✅ time✅ risk✅ chas❌ coun✅ | conf=41.2%(오늘 미통과 항목 최소, 1개뿐)
            [SizerMatch] sizer=3계약→actual=2계약, binding=hurst(0.50)
 예측 vs 실현  ensemble: direction=1(LONG) conf=0.4122
             predictions(3m): direction=1(UP) conf=0.3334, up=down=flat≈0.333(축퇴, CASE-02와 동일 계열) actual=0, correct=0
 승패판정   ✅승 — `하드스톱` tp1_reached=1·exit_outcome='이익'
 주 요인    A (방향예측 적중)
 부 요인    C (TP1 보호트레일) · D (Hurst 게이트 사이즈 축소)
 반사실     해당하지 않음(위와 동일 사유)

[CASE-04] 진입 11:44:58 LONG · 2계약 (등급 A/원시 C · horizon=5m · hurst=mean-revert)
          → 11:45:18 손절1차 조기축소 (-0.65pt/-34,643원) → 11:45:27 하드스톱(틱)[TP1미도달] (-1.25pt/-64,643원)
          포지션 합 -1.90pt / -99,286원 · 보유 약 29초
 진입근거   [진입체크] sign✅ conf✅ vwap✅ cvd❌ ofi✅ fore✅ prev✅ time✅ risk✅ chas❌ coun✅ | conf=43.6%
            [Sizer] atr=1.0171 → 3계약 산출, [MarginCap] state=OK 산출=2 상한=2(증거금 여유 있었음 — 오늘 유일하게 hurst/증거금이 아닌 다른 배수(kelly=1.00·meta=0.58)로 2계약 확정)
 예측 vs 실현  ensemble: direction=1(LONG) conf=0.4359
             predictions(5m): direction=0(FLAT) conf=0.4385, up=0.2678/down=0.2937(하락쪽이 더 높음) actual=-1(DOWN), correct=0
             → §1 3-1 관찰: 앙상블은 LONG을 택했지만 해당 호라이즌 단일예측은 하락에 더 가까웠고, 실제로 하락
 승패판정   ❌패 — `하드스톱(틱)` tp1_reached=0·exit_outcome='손실'(진짜 손절, TP1 미도달)
 주 요인    A (방향예측 실패 — 실현 DOWN, 진입은 LONG)
 부 요인    C (손절1차 조기축소가 손실을 완화 — 아래 반사실) · E (손절 준수율 양호, 슬리피지 없음)
 반사실     손절1차 조기축소가 없었다면 2계약을 그대로 최종 하드스톱까지 들고 갔을 것이고, 그 경우
            손실은 -1.25pt×2계약 = -2.50pt였을 것 — 실제로는 조기축소 덕에 -1.90pt로 **0.60pt 완화**됨.
            손절 준수율(실현손실 1.25pt ÷ 의도손절폭 ATR1.0171×1.5=1.526pt = 0.819)도 1.0 미만이라
            의도한 손절폭을 넘지 않았음(슬리피지로 인한 초과손실 없음, 417차가 우려한 패턴 아님)
```

### 4-4. 요인 집계

| 요인(주) | 승 | 패 | 순손익 | 평균 보유(분) | 관측 거래일 수 |
|---|---|---|---|---|---|
| A (방향예측) | 3 | 1 | +83,938원 | 약 1.1분 | **1일** |

부요인 빈도(참고, 확정 아님) — C(청산규칙/보호트레일·조기축소) 4건 전부, D(사이징 게이트 구속) 3건(hurst 2·증거금 1), E(체결·손절준수) 1건(case-04, 양호).

> **표본이 극히 작다(4건, 1거래일).** 위 표는 "오늘 무슨 일이 있었나"를 요약한 것이지 파라미터 변경 근거가 아니다 — 아래 4-5 참조.

### 4-5. 313차 가드 통과 여부

| 갈래 | 적용 결과 |
|---|---|
| ① 일자단위 판정 | **관측 1거래일 — 판정 보류.** 4건 모두 같은 날 같은 레짐(NEUTRAL) 아래 발생해 독립 관측치가 아니다. 파라미터 변경 근거로 쓰지 않는다 |
| ② overlap 보정 | 4건은 시간상 순차적(한 포지션 청산 후 다음 진입)이라 시간 중첩 없음 — 표본 수 자체를 부풀리는 문제는 없으나, 표본 수(4)가 원래 작다 |
| ③ 이상치 분해(drop-worst) | 최대 이익(case-01, +80,752원) 제외 시 나머지 합 +3,186원(여전히 흑자, 부호 안 바뀜). 최대 손실(case-04, -99,286원) 제외 시 나머지 3건 합 +183,224원. 어느 쪽을 빼도 부호는 유지되나 표본이 너무 작아 이 자체가 의미를 갖지 않는다 |
| ④ 사전등록 — 기준 불변 | 오늘 분석은 사전등록된 채널 판정이 아니라 관찰(O-76 신규 등록)이다. 기존 사전등록 채널(§13 확정 결정 레지스트리)의 기준은 건드리지 않았다 |
| ⑤ 부호 일관성(기간·PC 교차) | 관측 거래일이 1일뿐이라 기간 교차·PC 교차 검증 불가 — 보류 |

**결론**: 오늘 집계로는 어떤 파라미터도 바꿀 근거가 없다. 3-1(O-76) 관찰만 등록하고 표본을 쌓는다.

---

## 5. 수익률 향상방안

§4-4 요인 집계에서 출발한 항목만 쓴다. 다이제스트 §13 확정 결정 레지스트리 대조 결과 "앙상블-단일호라이즌 방향 불일치" 계열의 기존 채널·결정 없음(신규 제안 가능, 함정① 통과).

### R-1. 앙상블-단일호라이즌 방향 불일치 관찰 채널 신설 (관찰 등록 — 코드 변경 아님)

| 칸 | 내용 |
|---|---|
| 근거 | CASE-04, 요인 태그 A(방향예측 실패) — 앙상블 LONG 진입 vs 해당 호라이즌(5m) 단일예측이 하락 우세(down_prob 0.2937 > up_prob 0.2678)였고 실현도 하락 |
| 변경 대상 | (코드 변경 없음 — 관찰 채널 신설안) `scripts/generate_validation_campaign_report.py` 또는 신규 스크립트에 `ensemble_decisions.direction` vs `predictions(meta_gate_horizon)` 부호 대조 집계 추가 (§3 G-3과 동일 제안) |
| 기대효과 | 미정 — 표본 부족(n=1)이라 정량 추정 불가. 방향 불일치 시 승률이 유의하게 낮다면 진입 전 이 대조를 등급 하락 요인으로 추가하는 안을 검토할 근거가 됨 |
| 회귀 위험 | 없음(관찰 단계, 차단·등급 로직 미변경) |
| 검증 | 다음 26주 WFA 또는 캠페인 채널 신설 시 `min_samples`(가칭 10~20건) 사전등록 후 판정 |
| 표본 상태 | n=1, 관측 1거래일 — **313차 가드 통과 못함(판정 보류)**. `O-76`으로 등록해 축적 |

---

## 6. 다음 거래일 관측 예정

> 관측 ID 채번: 다음 관측 ID = **O-76**(다이제스트 §11 산출값 — 현행 최댓값 O-75 다음)

| ID | 관측 대상(로그/DB 패턴) | 판정 기준 | 판정 예정 |
|---|---|---|---|
| O-76(신규) | `ensemble_decisions.direction` vs `predictions`(해당 `meta_gate_horizon`)의 `up_prob`/`down_prob` 부호 일치 여부, 실현 승패와의 상관 | 불일치 사례 10건 누적(또는 관측 10거래일) 시 승률 차이 검정. 그 전에는 케이스 관찰만 | 표본 축적 후(다음 26주 WFA 또는 누적 시점) |
| O-73(지속) | `[ProfitGuard]` Tier 발동 시각·기준액 | 순손익이 Tier 임계를 넘는 날 발동 로그로 판정 | 다음 거래일 이후 |
| O-55(지속) | `[SizerMatch]` tox 바인딩 비율 | tox 바인딩 발생 시 판정, 현재 누적 0건 | 다음 거래일 이후 |

---

## 7. 기한 도래 항목

`references/invariants.md` §2 대조 — 오늘 신규 기한 도래 없음.

| 항목 | 기한 | 남은 거래일 | 조치 |
|---|---|---|---|
| CB② 복원(`CB_CONSEC_STOP_LIMIT` 9999→2~3) | 사건 기한(⑧ 해제와 동일 커밋) — 날짜 아님 | — | 489차 D2, 오늘 변화 없음 |
| `1-3`(0907) `raw_data.db` pruning lock 정정 승인 | 2026-09-14(월) EOD 전 | 3거래일(09-10·11·14) | 사용자 승인 대기 |

---

## 8. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-09-09 (MW0602 548차 — 일일 점검: 장후·종합)` (아래 실행)
- `NEXT_TODO.md` 추가 체크박스: `O-76` 1건. 판정 완료 처리: `O-75`(이미 장중에 처리됨, 중복 처리 안 함)
- 완료 처리한 기존 항목: 없음(오늘 코드 fix 없음 — 전부 관찰/이월)

---

## 9p. 종합 — 오늘 하루

### 9p-1. 종합 판정

| 국면 | 판정 | 한 줄 근거 |
|---|---|---|
| 장전 | 정상 | 모델·스케일러·레짐 정상 준비, 관찰 2건 등록 |
| 장중 | 정상 | 관찰 2건 모두 처분(1건 해소·1건 O-75 판정), 신규 P0/P1 없음 |
| 장후 | 정상 | EOD+P8 정상, 절대원칙 6종 준수, 신규 관찰 1건(O-76) 등록 |
| **당일 최종** | **정상** | 하루 종일 신규 P0/P1 없음. 4포지션 3승1패로 마감, 안전장치 강제비활성 상태(8종)에서 사고 없음 |

**당일 최종 손익(종가 기준)**: 4포지션 3승 1패(75%) · +2.02pt · net **+83,938원**(포지션 단위, `trades.db` 합산)
— 참고 대사: 일일 전략 배너 net +83,937원 / 브로커 실측 함수 `fetch_daily_net_for_verdict()` net +83,936원. 세 값 오차 ≤2원(반올림) — 계측 4원칙 ⑤ 관점에서 세 원천(로그 배너·DB 집계·브로커 함수)이 사실상 일치, 실질 불일치 아님. gross +101,000원, 수수료 약 17,062~17,064원(수수료/gross ≈ 17%). ※ `fetch_daily_net_for_verdict()`를 이 세션(리눅스 샌드박스)에서 재호출하면 `[BrokerChannel] 감지 실패 — CREON 폴백 적용` 경고가 뜨는데, 이는 이 분석 세션이 실제 Windows·CREON 환경이 아니라서 발생하는 **호출 환경 문제**다. 이미 저장된 `trades.commission_rate_used=0.000019`(0.0019%, MW0602 CREON 정상 요율)는 라이브 시스템이 기록 당시 올바르게 계산한 값이라 오늘자 실손익 수치 자체는 영향 없음.

**MDD**: 롤링20일 2.13%(자본대비) / 106.8%(peak대비) — 461차 규약대로 자본대비 기준 사용.

**절대원칙 6종 최종 판정**: ① 준수(강제청산 대상 없음, 11:45 이후 FLAT) · ② 준수(CB① 미발동, CB②는 설계된 예외, CB③④⑤ 무영향/미발동) · ③ 준수(강제X 위반 0건, CORE 축퇴는 10:35 해소) · ④ 해당없음(코드 변경 없음) · ⑤ 해당없음(코드 변경 없음) · ⑥ 준수(자동통합 흔적 없음).

**진입/청산 건수**: 진입 4건(포지션 4) · 청산 레그 8건(부분 4 + 최종 4). **종가 포지션**: 0계약(FLAT). **차단**: 68건(32종, 최다 사유 `2_confidence` 미달 35건). **사이저 호출**: 9회, 실제 진입은 4회 전부 산출(3계약)보다 적은 2계약 체결(binding: hurst 3·증거금 1). **CB 이벤트**: 연속손절카운터 1회 증가, 일간리셋 2회. **한시예외**: CB②·CB③-P4·FP-CRITICAL·TOX-SEVERE-SPREAD 등 8개 게이트 계속 비활성(전부 기록된 상태, 오늘 변화 없음). **매분 9단계**: STEP 3(GBM 배치 재학습)은 오늘 장중 발동 0회 — `[DriftRetrain] state=SKIP`이 반복 관측되며 이는 조건(5m acc<25%·n≥20 등) 미충족에 따른 **정상**(SKILL.md 함정 경고 그대로 적용, 이상 아님). **15:10 강제청산**: 이행 여부 판정 = 대상 없음(이미 11:45부터 FLAT이라 발동할 필요 자체가 없었음, 로그 `[SchedForceExit] 청산 대상 없음(정상)` 확인). **EOD 재학습**: 성공(`horizons_replaced: 5/6` — 3m은 EOD 모델가드로 교체 보류, 기존 NEXT_TODO `1-2`(0907) 계열로 이미 추적 중, 오늘로 7거래일 연속).

### 9p-2. 당일 이상점 통합 대장

| 번호 | 제목 | 최초 국면 | 심각도 | 최종 상태 |
|---|---|---|---|---|
| 1-1 | `ofi_norm` CORE 축퇴(identity 강제) | 장전 | P2 | ✅ 해소(오늘분, 10:35) — 근본 원인은 A-18/A-19로 별도 관리 중 |
| 1-2 | 개장 첫 분 6개 호라이즌 z-score 급등 (`O-75`) | 장전 | 관찰 | ✅ 해소·판정 완료(장중) |
| 2-1 | 사이저 산출(3계약)이 실제(2계약)로 눌림 | 장중 | P2 | 🔄 지속(하루 종일) → NEXT_TODO 기존 계열(`[28] sizing_inversion_watch`) |
| 3-1 | 앙상블-단일호라이즌 방향 불일치 (`O-76` 신규 등록) | 장후 | 관찰 | 🆕 신규 등록, 판정 보류(n=1) → 다음 거래일 이월 |

- P0 **0건** · P1 **0건** · P2 **1건**(당일 누계, 2-1) · 관찰 2건(1-2 해소·3-1 신규)

### 9p-3. 사용자 조치 — 오늘 할 일

| # | 할 일 | 명령·경로 | 기한 |
|---|---|---|---|
| 1 | 오늘 생성/수정된 파일 커밋(코드 변경 없음, 문서·기록물만) — 아래 "커밋 대기" 목록의 경로를 지정해 `git add` 후 커밋. `git add .`는 쓰지 말 것(CRLF로 수백 건이 오탐 표시됨) | `git --no-optional-locks add <경로 지정>` → `git commit` | 편할 때 |
| 2 | `1-3`(0907) `raw_data.db` pruning lock 정정 승인 여부 결정 | `dev_memory/NEXT_TODO.md` `1-3` 항목 확인 | 2026-09-14(월) EOD 전 |
| 3 | `1-5`(0907) 브로커net 오염 정정 승인 여부 결정 | `dev_memory/NEXT_TODO.md` `1-5` 항목 확인 | 정해진 기한 없음(누적 지속) |
| 4 | `1-2`(0907) 3m EOD 모델 교체 보류(오늘로 7거래일 연속) — 주간회의 상정 여부 결정 | `dev_memory/NEXT_TODO.md` `1-2` 항목 확인 | 다음 주간회의 |

> 안 하면 생기는 일 — ①은 안 해도 시스템 동작에 영향 없음(순수 기록물). ②는 2026-09-14 EOD 전까지 미결이면 다음 세션이 같은 질문을 반복해서 묻게 된다. ③·④는 방치될수록 "재검토하기로 했는데 안 함"(CB②·CB③-P4류) 패턴이 반복될 위험이 커진다(다만 아직 그 임계에 도달한 것은 아님).

---

**커밋 대기**: 예약 실행이라 커밋하지 않음. 오늘 하루 새로 만든/수정한 파일 전체 —
`docs/정기점검/매일점검/evidence_MW0602-20260909_pre.md`(신규),
`docs/정기점검/매일점검/evidence_MW0602-20260909_intra.md`(신규),
`docs/정기점검/매일점검/evidence_MW0602-20260909_post.md`(신규),
`docs/정기점검/매일점검/MW0602-20260909-점검리포트.md`(신규 — 이 파일),
`dev_memory/DECISION_LOG.md`(append 예정),
`dev_memory/NEXT_TODO.md`(append 예정).
세션 종료 시 `.git/index.lock` 없음(2점 확인) — 회수할 것 없음.

---

## 장후 자동조치 구현 결과 (17:25~18:10 · 자동)

> 예약작업 `mireuk-postmarket-autofix` 가 이 절을 덧붙였다. **위 본문은 고치지 않았다**
> (하루 한 파일·국면마다 append 규약). 이번 실행은 **매매 관련 코드를 한 줄도 바꾸지
> 않았다** — 바뀐 것은 일일 점검 증거 수집기(관측 도구)와 그 테스트뿐이다.

### A. 구현 완료 — 고도화 3건을 §12 관측 채널로 배선

오늘 리포트의 Fix 절은 3국면 전부 「신규 없음」이었다. 그래서 고도화 방안 3건과
수익률 향상방안 R-1 을 대상으로 삼았는데, 넷 다 **"표를 하나 만들어 표본을 쌓자"**
는 같은 모양이었고 우선순위도 「§12 지표 확장 시점」으로 지정돼 있었다. 그 자리에 붙였다.

| # | 무엇을 | 왜 지금 | 안 하면 |
|---|---|---|---|
| **G-1** | 개장 첫 5분(09:00~09:04)에 |z|>4 로 튄 **대표 피처**를 매일 집계 | 오늘 §1-2 가 물은 「이게 매일 그런가」를 사람이 답하려면 로그를 다시 파싱해야 했다 | 매일 아침 같은 질문을 다시 하게 된다 |
| **G-2** | 극단 z-score 가 뜬 날 **AutoMask 격리가 실제로 돌았는지** 대조 | 오늘 `O-75` 를 15분 만에 판정할 수 있었던 이유가 이 대조였다 | 「튀었는데 격리 안 된 날」을 사람이 눈으로 찾아야 한다 |
| **G-3**(=5p R-1 관측 절반) | 실체결 진입에서 **앙상블 방향 vs 그 호라이즌 단일예측 방향** 대조 | 오늘 §3-1 이 등록한 `O-76` 의 표본이 쌓일 곳이 없었다 | `O-76` 이 「표본을 쌓는다」고 적고 실제로는 아무 데도 안 쌓인다 |

**걸린 시간** 약 45분. **바뀐 파일** 2개 —
`.claude/skills/mireuk-daily-check/scripts/collect_evidence.py`(채널 3개 신설),
`tests/test_548_daily_check_indicator_channels.py`(신규 18건).

**세 채널이 지금 말하는 것** (수집기 실제 실행, 최근 11거래일)

| 채널 | 판정 | 값 분포 |
|---|---|---|
| `개장첫5분_극단피처` | ✅ 변동 (51표본) | `volume_acceleration`×12 · `va_bandwidth`×9 · `quality_investor_stale`×7 · 외 12종 |
| `극단z_격리대조` | ⚪ 정상고착 (8일) | `격리`×8 — **미격리 0건** |
| `앙상블_단일호라이즌_방향대조` | ✅ 변동 (84진입) | `일치`×41 · `미측정(예측없음)`×18 · `불일치`×13 · `동률`×12 |

→ **G-1 의 답은 「아니오」다.** 개장마다 같은 피처가 튀는 것이 아니라 매일 다르다
(최빈 `volume_acceleration` 도 23.5%). 스케일러 기준통계가 개장 구간에서 구조적으로
깨져 있다는 신호는 **아직 없다**.
→ **G-2 의 답은 「지금까지는 잘 돌았다」다.** 8거래일 전부 격리가 붙었다.

### B. 이번 배선이 특별히 피한 것 세 가지

계측을 새로 만들 때마다 이 프로젝트가 당해온 형태들이라, 각각 회귀 테스트로 못박았다.

1. **스로틀된 로그를 분모로 쓰지 않았다.** `[Model] … 극단 z-score N개 피처 감지`
   는 호라이즌별 **600초 스로틀**이다. 오늘 실측으로 DB 는 호라이즌당 **37분**을
   기록했는데 로그에 남은 건 **3분**뿐이다. 로그로 셌다면 **10배 이상 과소**이고,
   그 과소가 늘 "덜 튀었다" 쪽이라 진짜 미격리가 조용히 정상으로 분류됐을 것이다.
   → 분모는 `scaler_monitor.db` 로 잡았다.
2. **폴백을 정상값으로 채우지 않았다.** `meta_gate_horizon` 은 457차가 잡아낸
   영구 폴백('1m')이었고 DB 실측상 정상값이 처음 나온 날이 **2026-08-12** 다.
   그 이전을 표본에 넣거나 NULL 을 '1m' 으로 채우면 **엉뚱한 호라이즌과 대조**한다.
3. **미측정을 0으로 세지 않았다.** DB 에 못 붙은 날은 `무발생` 이 아니라 표본에서
   뺐고, 「가동했는데 극단 0건」과 「가동 기록 자체가 없음」도 갈랐다.

### C. 하지 않은 것과 이유

| 항목 | 왜 안 했나 |
|---|---|
| **5p R-1 의 나머지 절반** — 방향 불일치를 **진입 전 등급 하락 요인**으로 추가 | **매매 정책 변경(C등급)**. 예약작업이 자율로 넘볼 범위가 아니다. R-1 자신도 표본 상태를 「313차 가드 통과 못함(판정 보류)」로 적었다 |
| 사용자 조치 `2`·`3`·`4` (NEXT_TODO `1-3`·`1-5`·`1-2`) | 전부 **사용자 결정**이 필요한 항목이다. `NEXT_TODO.md` 에 그대로 이월했다 |
| `O-77`(아래) 28파일 수정 | 테스트 파일 28개를 한꺼번에 고치는 일이라 **별도 결정 사항**이다 |

🔴 **`O-76` 을 지금 숫자로 판정하지 말 것.** 새 채널이 소급 창에서 **불일치 13건**을
보여주지만, 오늘 §6 이 건 사전등록(「불일치 10건 누적 또는 관측 10거래일」)은
**오늘부터 앞으로** 세라는 뜻이다. 소급 누적으로 문턱을 채우면 사후탐색이고,
그것이 §9-4 검증 시계 리셋 대상이다. **판정 개시 시점은 사용자·주간회의 몫으로 남긴다.**

### D. 🔴 부수 발견 — 전체 테스트가 애초에 돌아가지 않고 있었다 (`O-77` 신규)

스킬 절차대로 「기존 스위트」를 돌리려다 발견했다. `python -m pytest tests/` 는
**단 한 건도 실행하지 못하고** `ValueError: I/O operation on closed file` 로 죽는다.
원인은 **28개 테스트 파일이 import 될 때 `sys.stdout` 을 다시 묶는 것**이다
(`sys.stdout = io.TextIOWrapper(sys.stdout.buffer, …)` — 454차부터 538차까지 관행처럼
누적). pytest 의 캡처 스트림이 그렇게 교체되면 세션 정리 단계에서 깨진다.

**파일 하나씩 돌리면 멀쩡하다 — 그래서 아무도 몰랐다.** 이번에 123개를 개별 실행해
확인한 결과 **107개 클린**, 나머지 16개의 실패는 **전부 이번 변경 이전부터의 것**이다
(내 변경을 되돌리고 재실행해 동일 결과 확인: `test_479`·`487`·`493`·`527`·`528`
각 1건 실패, `test_500_*` 4건 수집 오류, 7건 `no tests ran`).
※ `test_493` 1건은 어제 이식이 들여온 것으로 이미 `1-6` 에 등록돼 있다.

**왜 중요한가** — 이 스킬의 §5 는 「관련 테스트 + 기존 스위트를 돌려라」인데,
그 절반이 **구조적으로 이행 불가**였다. 로그는 정상인데 지키려던 것이 죽어 있던
FP-CRITICAL·TOX 죽은 섀도와 같은 계열이다. `O-77` 로 등록했다.
⚠ **고치지 않았다** — 28파일 동시 수정은 이번 범위 밖이다.

### E. 테스트

`tests/test_548_daily_check_indicator_channels.py` **18/18 통과**.
통과만으로는 부족해서 **일부러 8가지로 코드를 망가뜨려 테스트가 잡는지 확인**했다 —
+1분 허용 제거 / 날짜 대조 제거 / 미측정→무발생 강등 / 미가동일→무발생 /
`coalesce` 재도입 / `group by ts` 제거 / N<3→확인대상 / 최악분 판정 무력화.
**8종 전부 테스트가 잡았다.** py37_32(3.7.13) 컴파일 확인, 수집기 실제 실행으로
§12 렌더까지 확인했다.

인접 스위트: `test_547` 5/5 · `test_500_broker_net_state_sample` 12/12 ·
`test_501` 8/8 · `test_493` 19/20(1건은 위 `1-6`, 본 변경과 무관).

### F. 사용자가 직접 해야 할 일

| # | 할 일 | 시간 | 안 하면 |
|---|---|---|---|
| 1 | `1-3`(0907) `raw_data.db` pruning lock 정정 승인 | 1분 | **2026-09-14(월) EOD 전** — 미결이면 다음 세션이 같은 질문을 반복 |
| 2 | `1-5`(0907) 브로커net 롤오버 오염 정정 승인 | 1분 결정 + 5분 반영 | 전환기준 ① 판정이 09-07분 136,000원을 과소 반영한 채로 계속 간다 |
| 3 | `1-2`(0907) 3m EOD 모델 교체 보류(7거래일 연속) 주간회의 상정 여부 | 1분 | 「재검토하기로 했는데 안 함」 패턴으로 굳는다 |
| 4 | `1-6`(0907) GP 교차 채널 왕복비용 상수 교체 여부 | 2분 | 그 채널 판정이 PC별로 달라진다. `test_493` 실패도 이것이다 |
| 5 | 🆕 `O-77` 테스트 스위트 복구 착수 여부 결정 | 2~3분 결정 | 앞으로도 스위트 전체 검증 없이 개별 실행에만 의존하게 된다 |
| 6 | 🆕 `O-76` 판정 개시 시점 결정(소급분 사용 금지) | 2분 | 다음 세션이 소급 13건으로 문턱을 채워 사후탐색을 저지를 수 있다 |
| 7 | `F-1` 런처 GUARD `!=` 수정 결정(9일째) · `F-2` 예약 시각 승인 · `C-5` VKOSPI 대조(23회째) | 각 1~3분 | 계속 이월 |

**커밋**: `0c4d3ee` (5파일 · +1,604/-3). **푸시**: `origin dev` 완료 (`25396fa..0c4d3ee`).
