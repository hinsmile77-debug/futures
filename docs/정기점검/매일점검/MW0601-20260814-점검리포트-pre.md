# 미륵이 일일 점검 — MW0601 / 2026-08-14 / 장전(pre)

- 점검 시각: 09:00~09:20 KST · PC `MW0601` (예약작업 `mireuk-premarket-check`, 08:57 트리거)
- HEAD `e8a56ea` · 브랜치 `v9-dev` (**MW0601 규약 일치 ✓**, `origin/v9-dev` 대비 behind 0)
- 미커밋 433건 — **전부 개행문자(CRLF) 차이. 실코드 변경 0건** (§1 P2-3 참조)
- 당일 커밋 1건 (`a581231` [MW0602] 469차 — 점검 스킬만, 라이브 코드 무변경)
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260814_pre.md`
- **본 점검은 개장 직전/직후에 수행됐다 — 코드 변경·커밋·라이브 DB 조회를 일절 하지 않았다**
  (CLAUDE.md 「🔴 장중 라이브 DB 분석 금지」). 아래 Fix는 전부 **장후 적용** 계획이다.

## 0. 한 줄 결론

**⚠ P0 없음 — 오늘 거래 자격은 충족한다.** 브랜치·런타임·브로커 접속·전일 EOD 재학습(6/6)·
레짐 확정·실시간 사전구독이 전부 설계대로 완료됐고, 설정 불변식 21행은 어제와 동일하게 전부
`일치`다. 다만 **점검 계측 계층에 P1 2건**(브랜치 간 감시표 불일치 · 참조문서의 재인용 금지 수치)이
있어 오늘부터 매 점검이 상시 오탐 5건을 달고 나온다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | **정상** | A-0~A-4 전 항목 확인됨. 기동 08:40:45 → EarlyWarmup 08:45 → Phase1~4 refit → Canary 재적합 성공 → 레짐 RISK_ON 확정 08:58:19 |
| 장중 | 해당 없음 (장후 점검 대상) | 09:00:10 `[TimeRouter] 시간대 전환 → GAP_OPEN` 까지만 관측 |
| 장후 | 해당 없음 | — |

**절대원칙 준수 여부** (장전 국면에서 판정 가능한 것만)

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | 판정 불가(장전) | 기동 시 `[Position] 저장 상태가 어제 데이터 — 무시` (08:41:08) → 이월 포지션 없음. 포지션=FLAT |
| ② | Circuit Breaker 5종 | **확인됨** | 전 임계 상수 `일치`(CB③ 0.28 / STRICT 0.42 / RESTRICTED 0.30 / `CB_PIPE_PAUSE_MS`=5,000 / `CB_DAILY_HALT_FULL_BLOCK`=3). 블로킹 최대 2,625ms로 CB⑤ 임계 미달 |
| ③ | CORE 피처 호라이즌 그룹별 | **조건부 확인** | 08:45·08:48 워밍업 구간에서 `above_vwap` raw_std=0.0000 → identity 강제(6호라이즌 전부). 08:49:59 Phase2 refit부터 소멸. §1 P2-5 |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | **확인됨** | `crash_fault.log` 당일 신규 기록 없음(08:40:45 로테이션 이후 무증). 0xC0000409 없음 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 해당 없음 | `BROKER=cybos` 경로 — Kiwoom TR 호출 없음 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | **확인됨** | 당일 알파봇 기동 로그 0건 |

**설정 불변식** — 26행 중 `일치` 21행 · **`미발견` 5행**.
5행은 전부 462·468차 스위치이며 **MW0601 브랜치(`v9-dev`)에 코드 자체가 없다.** → §1 P1-1.
어제(0813) 21행 전부 일치였고 감시표만 26행으로 늘었다 — **MW0601 쪽 회귀가 아니다.**

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**없음.**

### P1 — 정확성·관측 훼손

#### 1-1. 불변식 감시표 5행이 `미발견` — 감시표는 `dev` 세대인데 코드는 `v9-dev` 세대다

- **증상**: 수집기 §3 설정 불변식에서 아래 5개 상수가 `미발견 ⚠`로 뜨고, §11 자동 적신호
  1~5번을 차지한다. `config/settings.py`(5,600행) 전문 grep에서 **5개 모두 0건**이다.

  | 상수 | 기대값 | 출처 |
  |---|---|---|
  | `MODEL_LABEL_STATE_UNLOCK_ENABLED` | `True` | 468차 G-1 |
  | `PRE_RETRAIN_DONE_BY_EOD_ENABLED` | `True` | 468차 F-1 |
  | `ZONE_ENTRY_BAN_ENFORCE` | `False` | 462차 P1-a |
  | `ZONE_ENTRY_BAN_SHADOW_ENABLED` | `True` | 462차 P1-a 섀도 |
  | `PIPE_LATENCY_EXCLUDE_MODEL_SWAP` | `True` | 462차 P2 |

- **근거**:
  ```
  $ grep -n "ZONE_ENTRY_BAN_ENFORCE" config/settings.py      → 0건
  $ git log --all --oneline -S"ZONE_ENTRY_BAN_ENFORCE" -- config/settings.py
    c63b4ee [MW0602] 462차: 0811 점검 — 자기유발 차단 2건 + min_conf 계측 결함
  $ git rev-list --left-right --count v9-dev...origin/dev  → 310  300
  $ git merge-base v9-dev origin/dev
    5e1426c 2026-07-05 291차: 검증 캠페인 계측·판정 자동화
  ```
  `c63b4ee`(462차)·`f2332be`/`ce3d9d9`(468차)는 **`origin/dev`에만** 있다.
  반면 감시표를 5행 늘린 커밋 `a581231`(469차, 2026-08-14 07:13)은 **`v9-dev`에 체리픽돼 있다.**
  즉 **감시 대상만 넘어오고 감시할 코드는 안 넘어왔다.**

- **기준 위반**: CLAUDE.md 계측 4원칙 ③(탈락 가시화) 및 스킬 함정 ③(멀티PC·브랜치 상이).
  감시표는 `references/invariants.md` §0이 *"§2 한시예외 값도 `v9-dev` 기준"* 이라고 선언해 두었는데,
  §2-B 신설 5행은 그 선언과 달리 `dev` 기준으로 작성됐다.

- **영향**: 두 방향이다.
  1. **관측 훼손** — 오늘부터 MW0601의 매 점검이 적신호 5건을 상시로 달고 나온다. 진짜
     불일치 1건이 생겨도 6건 중 하나로 묻힌다(계측 4원칙 ③이 경계하는 바로 그 상태).
  2. **기능 격차** — MW0601에는 `PIPE_LATENCY_EXCLUDE_MODEL_SWAP`(모델 교체 구간을 CB⑤
     판정 지연에서 차감)이 **없다.** CLAUDE.md가 기록한 정례 스파이크는 `S0`(모델 리로드)
     단독이므로, MW0601은 모델 교체 때 CB⑤ 자기유발 위험을 MW0602보다 더 안고 돈다.
     오늘은 최대 블로킹 2,625ms로 임계(5,000ms) 미달이라 실현되지 않았다.

- **신규 여부**: **신규**. 어제(0813) 리포트는 *"설정 불변식 21행 전부 일치"* 였고
  (`DECISION_LOG.md` 22760행), 5행은 오늘 07:13 체리픽으로 추가됐다.

- ⚠ **오해 금지**: 이것은 2026-08-12 사고(MW0601이 289커밋 낡은 `dev`로 전환)의 재발이 **아니다.**
  브랜치는 `v9-dev`로 정상이고 `origin/v9-dev` 대비 behind 0이다.

#### 1-2. 점검 참조문서가 CLAUDE.md의 「재인용 금지」 수치를 그대로 싣고 있다 (CB③ 35%)

- **증상**: `references/invariants.md` 두 곳이 CB③ 임계를 **35%** 로 적는다.
- **근거**:
  ```
  invariants.md:33   - ③ 30분 정확도 < 35% → 당일 정지
  invariants.md:153  | CB③ "30분 정확도 **< 35%** → 당일 정지" | CB_ACCURACY_MIN_30M = 0.28
                     | 임계가 완화됐는데 절대원칙 문구는 그대로 |
  ```
  이 파일의 최종 갱신은 `a581231`(469차, 2026-08-14 07:13)로 **어제자 정정보다 뒤**다.
- **기준 위반**: CLAUDE.md 절대원칙 §2 —
  *「[2026-08-13 461차 문서정정] ③의 "35%"는 2026-06-02부터 틀린 문구였다 — 재인용 금지.」*
  461차(`0424f64`, MW0601)가 이미 CLAUDE.md를 정정했다. 게다가 153행의 해설
  *"임계가 완화됐는데 문구는 그대로"* 는 461차가 **명시적으로 부정한 해석**이다 — 완화가 아니라
  98차(`f96d341`)가 **집계 대상(분모)** 을 바꿔(FLAT 예측 제외 → 2클래스) 무정보 기준선이
  33%에서 50%로 옮겨간 데 따른 동반 조정이었다.
- **영향**: 함정 ②가 정확히 겨냥하는 상태다. 점검자가 참조하도록 설계된 문서 자체가 금지수치를
  담고 있어, 다음 세션이 "CB③ 문구 미정정"을 신규 안건으로 다시 올릴 위험이 있다
  (2026-07-15 Hurst 완화가 3주째 재상정된 것과 같은 사고 형태).
- **신규 여부**: **신규**(461차 정정의 미전파). `NEXT_TODO.md`에 해당 항목 없음.

### P2 — 운영 부담·기술부채

#### 1-3. `미커밋 433건`은 전부 개행문자 차이 — 코웍 샌드박스 실행 시의 계측 오탐

- **증상**: 수집기 §2가 `미커밋 433건`, §11 적신호 7번이 `미커밋 변경 433건`을 올린다.
- **근거**:
  ```
  $ git diff --ignore-all-space --numstat | wc -l   → 0
  $ git diff --stat config/settings.py CLAUDE.md
    6241 insertions(+), 6241 deletions(-)          ← 삽입=삭제, 전형적 개행 차이
  $ file config/constants.py → UTF-8 text, with CRLF line terminators
  $ git config --get core.autocrlf → (미설정)
  ```
  워킹트리 파일은 CRLF인데 인덱스 블롭은 LF다. 윈도우 쪽 전역 `core.autocrlf=true`가
  적용되던 것이 **리눅스 샌드박스에서는 전역 설정이 보이지 않아** 전 파일이 수정으로 보인다.
- **기준 위반**: `references/phases.md` A-4 *「미커밋 변경이 있는가 — 실전 PC에 커밋 안 된
  수정을 남기지 않는다」* 를 판정 불가로 만든다. 계측 4원칙 ③(탈락·노이즈 가시화)과 같은 계열.
- **영향**: A-4가 무력화된다. 진짜 미커밋 수정이 433행 안에 숨어도 못 잡는다.
  **라이브 동작에는 영향 없다** — 윈도우 본체 git 상태는 정상일 가능성이 높다.
- **신규 여부**: 신규(예약작업으로 코웍 샌드박스에서 수집기를 처음 돌린 날). 어제까지의
  MW0601 점검은 윈도우 `claude` CLI에서 돌아 이 현상이 없었다.

#### 1-4. `--pc` 인자 부재 — 수집기가 샌드박스 호스트명으로 `UNKNOWN`을 낼 뻔했다

- **증상**: 스킬 문서는 `--pc MW0602`를 필수로 안내하지만, `v9-dev`의
  `collect_evidence.py`는 **`--pc` 옵션을 갖고 있지 않다**(`unrecognized arguments: --pc`).
  `MIREUK_PC_ID` 환경변수 경로도 없다.
- **근거**:
  ```
  $ python3 .claude/skills/.../collect_evidence.py --phase pre --pc MW0601 --out-auto
    error: unrecognized arguments: --pc MW0601
  $ git log --all --oneline | grep 467차
    0f44477 [MW0602] 467차: 일일 점검 수집기 --pc override   ← origin/dev 전용
  ```
  샌드박스 호스트명은 `claude`이므로 그대로 돌리면 `evidence_UNKNOWN-20260814_pre.md`가 된다.
  이번 회차는 `platform.node()`를 런타워에서 대체하는 래퍼로 우회해 정상 파일명을 얻었다.
- **기준 위반**: 스킬 함정 ③ — *"어느 PC의 관찰인지 영영 모르게 된다"*. 461차가 같은 이유로
  `evidence_UNKNOWN-20260813_post.md` 발생을 점검했던 계열(당시엔 미발생으로 종결).
- **영향**: 예약작업이 매일 자동으로 도는 한 재발한다. 1-1과 **같은 뿌리**(467차도 `dev` 전용).

#### 1-5. 워밍업 구간에서 CORE `above_vwap` raw_std=0.0000 → 6호라이즌 identity 강제

- **증상**:
  ```
  08:45:16 [WARNING] SIGNAL: [ScalerRefresh] 1m CORE 'above_vwap' raw_std≈0(0.0000)
                              → identity(0,1) 강제 (FLAT 100% 방지)
  ```
  1m·3m·5m·10m·15m·30m **전 호라이즌**에서 동일. 08:45:16(12건) → 08:48:01(6건) 총 18건.
  `cvd_divergence`도 08:45:16에 raw_std=0.0304로 함께 걸렸다.
- **기준 위반 여부**: CLAUDE.md 절대원칙 §3 — VWAP 위치는 단기(1m·3m·5m)·중기(10m·15m)
  그룹의 CORE이며 **미통과 시 강제 X**다. 다만 오늘은 **가드가 설계대로 작동해 FLAT 고착을
  막았고**, 08:49:59 Phase2 refit부터 경고가 소멸했다 → **위반 아님, 관측 항목**.
- **영향**: 프리장 30봉이 VWAP 한쪽에만 있으면 구조적으로 발생한다. 오늘은 개장 11분 전에
  해소돼 09:00 첫 봉에 영향이 없었다. 해소 전에 개장하는 날에는 1m CORE가 사실상 상수로
  들어간다 — 그 경우 절대원칙 §3의 "강제 X"가 무의미해진다.
- **신규 여부**: 신규 관측(단, 로그 문구 `(FLAT 100% 방지)`가 있으므로 가드 자체는 기존 설계).

#### 1-6. 투자자 수급 3종이 `+0` — 형식상 미측정 표기는 있으나 하류 소비가 미확인

- **증상**:
  ```
  08:58:19 [INFO] DATA: [CybosInvestor] futures supported=False source=FutureMst_oi
    foreign=+0 individual=+0 institution=+0 oi=157334 ... option_supported=False
    reason=Cybos 선물 투자자 TR 미발견; 미결제약정만 제공
  08:41:16 [INFO] SYSTEM: [Capability] ... investor=Y/N(fetch=0,runtime=N)
  ```
- **기준**: 계측 4원칙 ② *「미측정 ≠ 0」*. 같은 줄에 `supported=False`·`reason=`이 있어
  **로그 계층은 원칙을 지킨다.** 문제는 그 `+0`이 피처로 흘러가는지다.
- **정황**: `institution_futures_net`이 Phase1·2·3 refit의 **잔존 z경고**에 반복 등장한다
  (08:48:01 / 08:49:59 / 08:54:58). 상수 0이면 스케일러에서 std≈0으로 나와야 정상이다.
- **영향**: 미확정. 451차 `program_*` 3종(상수 0이 "정상 수집"으로 위장 → 폐기)과 **같은 패턴일
  가능성**이 있으나, 오늘 증거만으로는 단정할 수 없다.
- **신규 여부**: 부분 기지 — `DECISION_LOG.md` 2391행이 같은 로그를 다룬 이력이 있다.

### 확인 필요 (확정 아님)

- **1-6의 하류 소비 경로**: `institution_futures_net`이 (a) 상수 0인데 피처로 들어가는지,
  (b) 학습 X의 97개 동결 슈퍼셋에 포함돼 있는지. **`raw_features` 조회가 필요하므로 장후로
  미룬다**(CLAUDE.md 장중 라이브 DB 금지). 판정 근거: `raw_features_horizon` 최신 30행의
  `institution_futures_net` 분산과 `feature_names.pkl` 포함 여부.
- **1-5의 만성도**: 오늘 1일차다. `above_vwap raw_std≈0`이 08:49 이후까지 남는 날이
  있는지 5거래일 누적해야 판정한다 — 313차 원칙에 따라 **확정 결론 보류**.
- **기동 시 `[Calibration] 축퇴 감지` 133건**(08:40:59~08:41:08, 6호라이즌 × 다수 창):
  CLAUDE.md가 기록한 403차 축퇴 가드 배포 이후의 **기지 상태**로 보이나, 133건이 어제
  대비 늘었는지는 비교하지 않았다(장후 §5 대사에서 확인).

### 이미 반영된 사안 (신규 아님)

- **CB② `CB_CONSEC_STOP_LIMIT = 9999`** — 미조치가 아니다. 2026-08-01 W4 결산이
  복원을 4주 연기했고 **재검토 기한은 2026-08-29**로 아직 도래하지 않았다(CLAUDE.md 절대원칙 §2).
- **CB③-P4 / FP-CRITICAL / TOX-SEVERE-SPREAD 차단 비활성** — 셋 다 문서화된 한시예외이며
  수집기 §3에서 전부 `일치`. TOX-SEVERE-SPREAD는 461차에 실전 전환 기준 ⑨로 승격 완료
  (`NEXT_TODO.md` 문서·운영 절 `[x]`), 복원 선행조건 F-8(섀도 배선)은 **미착수 상태로 등록됨**.
- **실전 전환 기준 ③ MDD 분모 명시 / ⑧ 조건 2(`MAX_CONTRACTS` 10→3)** — 각각 461차·431차에
  반영 완료. 재상정하지 말 것.
- **N-6 `eod_retrain_done` horizons_replaced 6/6** — 어제 관측 예정 항목이었고 **충족**
  (아래 §4 참조). 익일 CB③ HALT 위험 없음.

---

## 2. Fix 작업 구현계획

> **본 계획은 수립만 한다. 오늘 09:00 개장 이후이므로 적용은 전부 15:10 강제청산 이후(장후)다.**
> 라이브 프로세스가 돌고 있고, 예약작업 지시가 코드 변경을 명시적으로 금지한다.

### F-1. 불변식 감시표를 브랜치 인식형으로 — P1 · 대응 이상점 1-1

- **원인 가설**: 469차가 감시 대상을 늘리면서 `dev`에만 존재하는 상수를 넣었고, 그 커밋이
  `v9-dev`로 체리픽되면서 "코드 없는 감시"가 됐다. 감시표에 **어느 브랜치 기준인지** 필드가 없다.
- **변경 파일**:
  - `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py` — 불변식 스펙 dict에
    `branches: ("dev",)` / `("v9-dev",)` / `None`(공통) 키 추가. 판정 함수에서 현재
    `git branch --show-current`가 `branches`에 없으면 **`미발견`이 아니라 `해당없음(타브랜치)`**
    으로 렌더링하고 §11 적신호에서 제외.
  - 같은 파일 §11 생성부 — `해당없음` 행은 적신호 카운트에 넣지 않는다.
  - `.claude/skills/mireuk-daily-check/references/invariants.md` §2-B — 각 행에 `브랜치` 열 추가.
- **회귀 위험**: 낮음. 판정 분기만 추가되며 기존 `일치`/`불일치` 경로는 무변경.
  ⚠ **`해당없음`이 새로운 은폐 수단이 되면 안 된다** — 표 하단에 `타브랜치 미적용 N종`을
  반드시 한 줄로 남긴다(계측 4원칙 ③).
- **절대원칙 저촉 여부**: 없음(점검 도구 계층, 라이브 코드 무변경).
- **런타임 주의**: 표준 라이브러리만 사용. py37_32/py310_64 양쪽 호환 유지(`ast.Str` 계열 주의는
  이 파일에 해당 없음).
- **검증 방법**: `v9-dev`에서 실행 시 §11 적신호가 7건 → 2건(축퇴·미커밋)으로 줄고,
  5행이 `해당없음(dev 전용)`으로 표시되는지. `dev` 체크아웃 후 동일 스크립트로 5행이
  다시 `일치`로 뜨는지 교차 확인.
- **섀도 계측 필요 여부**: 불필요(게이트 아님).
- **적용 시점**: **장후**.
- **결정 필요 사항**: **사용자 결정 필요** — 근본 처방은 둘 중 하나다.
  - (A) 감시표를 브랜치 인식형으로(위 계획). 두 브랜치 분기를 **인정**하는 방향.
  - (B) 462·468차 5종을 `v9-dev`로 체리픽. 기능 격차를 **없애는** 방향. 특히
    `PIPE_LATENCY_EXCLUDE_MODEL_SWAP`은 CB⑤ 자기유발 방지라 MW0601에도 값어치가 있다.
  - 권고: **A를 먼저(오탐 제거), B는 별건으로 심사.** B는 라이브 동작을 바꾸므로 섀도 관찰
    없이 체리픽하면 안 된다. 참고로 merge-base가 2026-07-05(291차)로 **두 브랜치는 310/300
    커밋 분기 상태**이며, 이 격차 자체를 어떻게 할지는 주간회의 안건이다.

### F-2. `invariants.md`의 CB③ 35% 문구 정정 — P1 · 대응 이상점 1-2

- **변경 파일**:
  - `.claude/skills/mireuk-daily-check/references/invariants.md`
    - 33행 `③ 30분 정확도 < 35%` → `③ 30분 **방향성** 정확도 < **28%** (`CB_ACCURACY_MIN_30M`)`
      + 461차 정정 요지 각주(FLAT 예측 제외 → 2클래스 50% 기준선, 0.28 = 랜덤의 56%).
    - 153행 행을 **삭제하지 말고** "해소됨(461차 `0424f64`)"으로 판정 변경 —
      *"임계가 완화됐는데 문구는 그대로"* 라는 오해석을 그대로 두면 재발한다.
    - §3(재인용 금지)에 **3-3항 신설**: "2026-06-02 이전 `acc30m` 수치와 직접 비교 금지".
- **회귀 위험**: 없음(문서).
- **절대원칙 저촉 여부**: 없음. CLAUDE.md 문구를 그대로 따른다.
- **검증 방법**: `grep -n "35%" references/invariants.md` → CB③ 관련 0건.
  다음 점검 세션이 CB③ 문구 정정을 신규 안건으로 올리지 않는지(다음 거래일 관측).
- **적용 시점**: **장후**. 커밋 태그 `[MW0601]`.
- **결정 필요 사항**: 없음 — CLAUDE.md가 이미 정본이다.

### F-3. 수집기 `--pc` / `MIREUK_PC_ID` 지원 — P2 · 대응 이상점 1-4

- **변경 파일**: `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py`
  - `argparse`에 `--pc` 추가, `pc_id()`를 `--pc > MIREUK_PC_ID > platform.node()` 우선순위로 변경.
  - override 시 헤더에 `MW0601 (override · host=claude)` 표기(폴백 가시화 — 계측 4원칙 ④).
  - `UNKNOWN`으로 떨어지면 **stderr 경고**를 낸다(§금요일점검 PC 폴더 규약과 동일 취지).
- **원인 가설**: 467차(`0f44477`)가 `dev`에만 있다. F-1과 같은 뿌리이므로 **같은 커밋에 묶는다.**
- **회귀 위험**: 낮음. 인자 미지정 시 기존 동작 그대로.
- **검증 방법**: 샌드박스에서 `--pc MW0601` 실행 → `evidence_MW0601-*.md` 생성 + 헤더에
  `override · host=claude` 표기 확인.
- **적용 시점**: **장후**. F-1과 동일 커밋.

### F-4. 수집기 미커밋 집계에서 공백/개행 차이 분리 — P2 · 대응 이상점 1-3

- **변경 파일**: `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py` §2 생성부
  - `git status --short` 총건수와 별도로 `git diff --ignore-all-space --numstat` 건수를 구해
    **`미커밋 433건 (실변경 0건 · 개행차이 433건)`** 형태로 렌더링.
  - §11 적신호는 **실변경 건수**로만 판정한다.
- **회귀 위험**: 낮음. git 호출 1회 추가(로컬, 라이브 무관).
- **적용 시점**: **장후**. F-1·F-3과 동일 커밋 가능.

### 적용 순서와 커밋 계획

1. **커밋 ①** — F-2(문서 정정 단독) — `[MW0601] 462차: invariants.md CB③ 문구 정정 — 재인용 금지 수치 제거`
2. **커밋 ②** — F-1 + F-3 + F-4(수집기 3종) — `[MW0601] 462차 후속: 불변식 감시 브랜치 인식 + --pc override + 개행차이 분리`

> ⚠ **차수 번호는 사용자 확인 후 확정한다.** `origin/dev`가 이미 469차까지 썼고 `v9-dev`는
> 461차가 마지막이라 번호가 겹친다(스킬 §6: 세션 차수는 원격 기준). 470차 부여가 안전할 수 있다.
> **본 점검은 커밋하지 않았다.**

---

## 3. 고도화 방안

### G-1. 브랜치 격차 자체를 매일 계측한다 — "몇 커밋 갈라져 있는가"를 수집기 §2에 넣는다

- **관측 근거**: 오늘 `v9-dev`와 `origin/dev`가 **310 / 300 커밋** 갈라져 있고 merge-base가
  **2026-07-05(291차)** 임을 확인했다. 이 사실은 지금 어느 리포트에도 자동으로 안 남는다.
  1-1(감시표 불일치)과 1-4(`--pc` 부재)가 **둘 다 이 격차의 증상**이었는데, 각각을 개별
  버그로 발견하는 데 시간을 썼다.
- **제안 내용**: 수집기 §2에 3줄 추가 —
  `상대 브랜치(dev/v9-dev) 대비 ahead/behind` · `merge-base 날짜와 차수` ·
  `상대 브랜치에만 있는 `config/settings.py` 상수 개수`. 마지막 항목이 기능 격차의 대리지표다.
- **기대 효과**: "저쪽에만 있는 스위치 N개"가 매일 한 줄로 보인다. 오늘 같은 5행 오탐이
  발생하기 전에 **원인이 먼저 눈에 들어온다.**
- **비용·위험**: 낮음. `git rev-list --left-right --count`와 정규식 상수 추출 재사용.
  ⚠ 상대 브랜치를 `origin`에서 fetch해야 최신이다 — fetch 실패 시 `(오래된 기준)` 표기 필요.
- **선행 조건**: F-1.
- **우선순위 제안**: **이번 주**.

### G-2. 프리장 CORE 상수화 조기경보 — `above_vwap` std=0이 개장까지 남으면 알린다

- **관측 근거**: 오늘 08:45·08:48 두 차례 6호라이즌 전부에서 `above_vwap` raw_std=0.0000이
  관측됐고 08:49:59 Phase2 refit에서 해소됐다. **해소된 것은 결과지 보장이 아니다** —
  Phase 스케줄(08:48/08:50/08:55/08:59)의 마지막 refit 이후에도 남으면 09:00 첫 봉이
  상수 CORE로 들어간다.
- **제안 내용**: `[PreMarket] Phase4 refit 완료` 직후 CORE 피처(그룹별)의 `raw_std`를 재확인해
  0인 것이 있으면 **개장 전 1회 경고**를 낸다(`[CoreConst] 09:00 개장 시점 1m CORE
  'above_vwap' std=0 — 이 봉의 CORE 판정은 무의미`). 차단이 아니라 **알림만**.
- **기대 효과**: 절대원칙 §3의 "미통과 → 강제 X"가 실제로 의미를 갖는 날과 아닌 날을
  구분할 수 있게 된다. 지금은 구분이 없다.
- **비용·위험**: 낮음. 기존 `ScalerRefresh` 경고를 Phase4 시점에서 한 번 더 평가할 뿐.
  ⚠ **차단 로직으로 승격하지 않는다** — 게이트 신설은 섀도 계측을 먼저 거친다(스킬 §7-7).
- **선행 조건**: 5거래일 누적 관측(만성도 판정). 오늘이 1일차.
- **우선순위 제안**: 이번 주 관측 → 다음 주 판단.

### G-3. `institution_futures_net` 등 수급 3종의 "미측정 0" 여부를 EOD에서 자동 판정

- **관측 근거**: `supported=False`인데 `foreign=+0 individual=+0 institution=+0`이 매 fetch마다
  나오고, 동시에 `institution_futures_net`이 Phase1~3 refit **잔존 z경고**에 반복 등장한다.
  451차 `program_*` 3종(상수 0이 정상 수집으로 위장 → 폐기)과 형태가 같다.
- **제안 내용**: EOD 체인에 `raw_features` 당일 열별 `nunique==1` 검사를 추가하고, 해당 열이
  **학습 X의 97개 동결 슈퍼셋에 포함돼 있으면** 리포트에 `[ConstFeature] N종` 으로 낸다.
  468차 G-2 "고착 지표" 검출과 같은 계열이므로 **중복 구현 전 `dev` 쪽 구현을 먼저 확인할 것.**
- **기대 효과**: 계측 4원칙 ②의 자동 검출을 DB 계층까지 넓힌다(461차 G-5가 INSERT 계층에서
  한 일의 대응물).
- **비용·위험**: 중간. **라이브 DB 전수 스캔이므로 반드시 EOD(15:45 이후)에만 돈다** —
  2026-08-10 CB⑤ 자기유발 전례.
- **선행 조건**: 위 "확인 필요" 1항의 장후 조사 결과.
- **우선순위 제안**: 이번 주(조사) → 판정 후 구현.

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| 브랜치 격차 관리 | 어디에도 없음 | 주간회의(금요일점검) 상설 안건으로 등록 | 310/300 커밋 분기가 오늘 오탐 5건 + 도구 결손 1건을 만들었다. 개별 버그로 처리하면 계속 재발한다 |
| 실전 전환 기준 ⑨(TOX-SEVERE-SPREAD) | 선행조건 F-8 미착수 | 변경 제안 없음 | 461차에 이미 승격·등록 완료. 재상정 금지(함정 ①) |
| 실전 전환 기준 ⑧ | 조건 1(실전 자본)만 남음 | 변경 제안 없음 | 단, `dev` 쪽 §2-B가 468차 G-1 라이브 확인을 ⑧의 선행조건으로 걸었다. **그 G-1이 `v9-dev`엔 없다** — F-1(B안) 판단 시 함께 볼 것 |

---

## 4. 진입 승패 사후검증 (장후 한정)

**해당 없음(장후 전용).** 단, 장전 국면에서 판정 가능한 선행 항목만 아래에 기록한다.

**전일(2026-08-13) EOD 재학습 — 성공 ✓**

```
data/eod_retrain_done_20260813.txt
  completed: 2026-08-13 15:49:06 · rows 39809 · cols 97
  horizons_replaced: 6/6 · t_total_s 241.6
logs/retrain_eod_20260813.log
  15:45:04 [EOD_RETRAIN] Python : 3.10.20 64-bit            ← py310_64 정상(191차 결정)
  15:49:06 [EOD_RETRAIN] 재학습 완료: 호라이즌 교체=6/6  가드보류=0
  15:49:06 [EOD_RETRAIN] [P8] 스케일러 재적합 완료 n=500봉
  15:49:06 [EOD_RETRAIN] [P8] session_state p8_last_success_date + eod_retrain_ok_date 기록 완료
```

- `NEXT_TODO.md` **N-6 충족** — `horizons_replaced 6/6` 유지. **익일 CB③ HALT 위험 없음.**
- 6호라이즌 전부 `[GuardGhost] 가드 미적용 … CV 검증된 신규 모델로 교체 [457차 F7 ⑤안]`
  경로로 교체됐다. **설계된 경로이며 이상 아님**(현행 배포본이 CV 미검증 intraday 모델이라
  `acc.txt`가 "존재하지 않는 모델의 성적"인 상황을 457차가 다룬 그 처리).
- 참고: 30m만 acc가 내려갔다(0.4502 → 0.4442). 30m은 296차에 앙상블에서 퇴역한 호라이즌이라
  **판정 영향 없음.**

## 5. 수익률 향상방안 (장후 한정)

**해당 없음(장후 전용).**

---

## 6. 장전 체크리스트 판정표 (`references/phases.md` A절)

| 항목 | 판정 | 근거 |
|---|---|---|
| A-0 브랜치 = `v9-dev` | **확인됨** | `git branch --show-current` → `v9-dev`. behind 0 |
| A-1 중복 기동 없음 | **확인됨** | `[FaultHandler] 활성화 … PID=4232` 단일. 런처 08:40:01 1회 |
| A-1 런타임 py37_32 | **확인됨** | `launcher_20260814_084001_20710.log:12` `32-bit Python 확인: …\envs\py37_32\python.exe` |
| A-1 Cybos 접속·COM | **확인됨** | 08:41:16 `[Capability] broker=cybos connect=Y/Y balance=Y/Y … server=Cybos 모의투자` |
| A-1 모델 번들 로드 | **확인됨** | 08:40:59 `[RF] 로드 완료: 6호라이즌 ready=True` · 08:55:16 `[MetaConf] 상태 복원 완료 (fitted=4종, total=6241, ver=5)` |
| A-1 전일 EOD 재학습 성공 | **확인됨** | §4 참조. 6/6 교체 |
| A-2 매크로 → 레짐 판정 | **확인됨** | 08:55:16 seed fetch → 08:58:19 `[Regime] RISK_ON (점수=3) \| VIX=14.6 \| SP500=+0.65% \| USD/KRW -0.04%` |
| A-2 실시간 구독 **사전** 시작 | **확인됨** | 08:45:16 `[EarlyWarmup] Cybos RT 08:45 선행 구독 시작 (프리장 봉 15봉 확보)` → `FutureCurOnly`/hoga 구독 완료. 09:00 첫 봉 유실 없음 |
| A-2 옵션 체인 로드 | **확인됨** | 08:41:16 `[OptionChain] 초기화 완료: 체인 캐시 5242 종목` · 09:01:17 `target=24 valid=24 PCR=0.682 ATM_PCR=0.868 GEX=59.68B` |
| A-2 선물 분봉 TR = `OPT50029` | **해당 없음** | `BROKER=cybos` — OPT계열 TR 미사용(로그 0건). 분봉은 `[BAR-CLOSE]` 틱 집계 경로. `OPT10080` 사용 0건 확인 |
| A-3 불변식 전 행 `일치` | **부분** | 21행 `일치` · 5행 `미발견`(→ P1 1-1) |
| A-3 한시예외 재검토 기한 경과 | **미도래** | CB② 2026-08-29(15일 남음) · CB③-P4 / FP-CRITICAL / TOX-SEVERE-SPREAD는 기한형 아님 |
| A-4 미커밋 변경 | **판정 불가** | 433건 전부 개행 차이(→ P2 1-3) |
| A-4 PC명 태그 | **확인됨** | 최근 12건 모두 `[MW####]` 접두 |
| A-4 타 PC 커밋 pull | **확인됨** | `a581231`(MW0602 469차) 반영됨. 단 코드는 미반영(→ 1-1) |

**부가 관측 (장중 감시용)**

- `[Canary] z경고피처=19개 ≥ 12개` → 장전 재적합 → `z경고 →3개 ✓ 임계 이하`(08:55:16). **정상 복구.**
- Phase1~4 refit 잔존 z경고 추이: 14→5 / 7→4 / 8→4 / 7→3. `atr`·`avg_volume`이 전 단계 잔존.
- 메인 스레드 블로킹 **1건 · 최대 2,625ms**(08:41:18, 기동 중). **5초 초과 0건 → CB⑤ 미발동.**
  `NEXT_TODO` **N-4**(≥5,000ms 건수, 15건 초과면 G-3 상향) 장전 시점 **0건**.
- `[CybosProbe]` 10건 WARNING — 투자자 TR 탐색 실패(`CoInitialize가 호출되지 않았습니다` /
  `잘못된 클래스 문자열입니다`). `investor=Y/N(fetch=0,runtime=N)`과 정합. 기지 상태.
- 09:00:10 `[TimeRouter] 시간대 전환 → GAP_OPEN: 시초가 급변 — 고신뢰·소규모 진입만 허용`.
  DynMC 기동 복원값 `GAP_OPEN 0.670 → 0.441`.

---

## 7. 오늘 관측 예정 (장중·장후로 이월)

`NEXT_TODO.md` 2026-08-14 N-1~N-8 중 장전에 판정된 것과 이월된 것:

| # | 항목 | 장전 판정 |
|---|---|---|
| N-1 | `[JointGateBlock 차단]` 중 `meta=0.50` 비율 (3일 연속 80%+면 확정) | 이월 — 0813이 1일차(85.7%) |
| N-2 | 진입 건수 — 0건이면 2거래일 연속 → 진입0 딥다이브 | 이월 |
| N-3 | 퍼널 `JointGateBlock=N` vs `TRADE.log` grep 일치 | 이월 |
| N-4 | `_tick_header ≥5000ms` 건수 (15건 초과면 상향) | 장전 **0건** |
| N-5 | `[FeatureReg] 5m … 제외: ['opt_chain_pcr']` 만성도 | 이월 — 장전 0건 |
| N-6 | `eod_retrain_done` `horizons_replaced` | **충족 6/6 ✓** |
| N-7 | `[ConfFloorGuard]` 경보 vs `conf > out_max` 분봉 수 | 이월 |
| N-8 | 예약작업 `mireuk-postmarket-check` 트리거 15:50 변경 여부 | 이월 |

**신규 이월 (본 점검 발)**

- O-1 `above_vwap raw_std≈0` 이 08:49 이후에도 남는 날이 있는가 (5거래일 누적, 판정 08-20)
- O-2 `institution_futures_net` 의 당일 분산 — 상수 0인가 (장후 `raw_features` 조회)
- O-3 `[Calibration] 축퇴 감지` 일일 건수 추이 (오늘 기동 시 133건)

---

## 8. 자체 검증

- [x] 브랜치를 가장 먼저 확인했다 (`v9-dev`, MW0601 규약 일치)
- [x] 모든 이상점에 로그 시각과 인용이 붙었다
- [x] 각 이상점을 CLAUDE.md 조항 / 계측 4원칙 / `phases.md` A절에 대응시켰다
- [x] dev_memory 기존 항목을 중복 보고하지 않았다 (§1 "이미 반영된 사안" 절 참조)
- [x] 재인용 금지 수치를 근거로 쓰지 않았다 — 오히려 참조문서의 잔존을 P1로 보고했다(1-2)
- [x] 캠페인 권고를 "이미 코드에 있는가" 확인 후 보고했다 (CB②·⑧·⑨ 전부 기반영으로 분류)
- [x] Fix 계획이 파일·함수 수준까지 구체적이다
- [x] **코드를 변경하지 않았다 / 라이브 DB를 스캔하지 않았다** (수집기는 로그 전용)
- [x] dev_memory 갱신 완료, **커밋하지 않았다**
