# 미륵이 일일 점검 — MW0601 / 2026-08-20 / 장전(pre)

- 점검 시각: **09:01~09:20 KST** · PC `MW0601` · 브랜치 `v9-dev`
  ⚠ 예약 실행이 08:57에 걸려 있으나 실제 착수는 **09:01**(개장 직후)이었다. 따라서 본 리포트는
  순수 장전이 아니라 **"장전 + 개장 첫 2분"** 을 본다. 09:00 이후 관측은 그 사실을 표기했다.
- HEAD `f94536f` · 미커밋 **457건(전량 CRLF 오탐 — 실질 소스 변경 0건, 2-2 참조)** · 당일 커밋 1건
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260820_pre.md`
- 수집기 실행: **성공** (49.0KB, `--pc MW0601 --phase pre --out-auto`, stderr 없음)
- **코드 변경 0건 / 라이브 DB 질의 0건** (CLAUDE.md 장중 DB 분석 금지 준수 — 로그·파일 mtime·git만 사용)

## 0. 한 줄 결론

**⚠ P0 없음 — 오늘 거래 자격 있음.** 브랜치·런타임·전일 EOD 재학습·매크로/레짐·실시간 사전구독이
모두 정상이며 절대원칙 위반 관측 0건이다. 다만 09:00 첫 봉에서
`institution_futures_net` **z=-15.07** (최근 4거래일 첫봉 최대 +6.52 대비 이례)이 관측됐고,
단기 CORE 피처 `ofi_norm`의 스케일러 **identity 강제율이 2거래일 연속 100%** 라 —
둘 다 "예측이 조용히 나빠지는" 축이므로 장중·장후에 반드시 되짚어야 한다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | **정상(조건부)** | A-0~A-4 전 항목 통과. 조건 = P1 2건(z 폭발·ofi_norm)을 장중 관측 항목으로 승계 |
| 장중 | 해당 없음(별도 국면) | 09:00~09:02만 참고 관측 |
| 장후 | 해당 없음(별도 국면) | — |

**절대원칙 준수 여부**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | **해당 없음(장전)** | 기동 시 `[BrokerSync] startup sync 무포지션 확인 … FLAT -> FLAT (armistice cleared)` 08:41:33. 전일 이월 포지션 0. 집행 자체는 장후 판정 대상 |
| ② | Circuit Breaker 5종 | **정상** | 상수 5행 전부 `일치`(CB③ 0.28 / CB_ACC_RESTRICTED 0.30 / STRICT 0.42 / `CB_PIPE_PAUSE_MS`=5,000 / `CB_DAILY_HALT_FULL_BLOCK`=3). 09:01:02 CB⑤ 경고 2건은 **장시작 버스트 예외(임계 9s)** 로 정상 처리 |
| ③ | CORE 피처 호라이즌 그룹별 | **관측 훼손 의심** | `ofi_norm`(단기 CORE)이 전 refresh에서 `identity(0,1) 강제` — 1-2 참조. 진입 0건이라 체크리스트 발동은 아직 없음 |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | **정상** | `crash_fault.log` 신규 기록 없음, `0xC0000409` 흔적 0건, 프로세스 단일 PID 13140 생존 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | **해당 없음** | Cybos 경로(`broker=cybos`). 해당 규약은 Kiwoom TR 경로 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | **정상** | 당일 로그에 알파봇/백테스트 자동 큐 발화 0건 |

**설정 불변식** — 수집기 §3: `일치` 22행 · `값 확인` 1행 · **`미발견 ⚠` 5행**.
5행은 전부 **수집기 기대표의 브랜치 스코프 오탐**이며 코드 무결성 문제가 아니다(2-3 참조).
**한시 예외 재검토 기한 경과 0건** — CB② 복원 재검토 `2026-08-29` 미도래(9일 남음).

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**없음.**

---

### P1 — 정확성·관측 훼손

#### 1-1. 09:00 첫 봉 `institution_futures_net` z = **-15.07** — 프리장 "미측정을 0으로" 충전이 만든 z 폭발

- **증상**: 개장 첫 봉(ts=09:00) 스케일링에서 6개 호라이즌 **전부** 동일하게
  `institution_futures_net` 이 최대 |z| 피처로 잡혔고, 값이 **-15.07** 이다.
  같은 시각 `[Model] … 극단 z-score 3개 피처 감지 (|z|>4) — 스케일러 노후화 또는 이상 데이터 의심`
  이 6회 동반됐다.
- **근거**:
  ```
  2026-08-20 09:01:00 [WARNING] SIGNAL: [Model] 1m 극단 z-score 3개 피처 감지 (|z|>4) — 스케일러 노후화 또는 이상 데이터 의심
  2026-08-20 09:01:00 [WARNING] SIGNAL: [ScalerMonitor] ts=09:00 horizon=1m age=2m max_z=-15.07(institution_futures_net) extreme=3
  ... (3m·5m·10m·15m·30m 동일, 총 6건)
  ```
  `logs/20260820_SIGNAL.log` L87~104 · 6건 · 09:01:00 단일 시각

  원천이 되는 프리장 수급 응답:
  ```
  2026-08-20 08:58:06 [INFO] DATA: [CybosInvestor] futures supported=False source=FutureMst_oi
      foreign=+0 individual=+0 institution=+0 oi=150799 … reason=Cybos 선물 투자자 TR 미발견
  2026-08-20 08:58:36 [INFO] DATA: [CybosInvestor] … 동일 (fetch#2)
  ```
  `logs/20260820_DATA.log` L378~381

  **최근 5거래일 첫봉(1m) 최대 |z| 대조 — 오늘만 자릿수가 다르다**

  | 일자 | 첫봉 max_z | 피처 |
  |---|---|---|
  | 2026-08-13 | +6.52 | `ret_15m` |
  | 2026-08-14 | +4.58 | `volume_acceleration` |
  | 2026-08-18 | +6.42 | `ret_15m` |
  | 2026-08-19 | +4.99 | `cancel_add_ratio` |
  | **2026-08-20** | **-15.07** | **`institution_futures_net`** |

  `institution_futures_net`가 max_z 로 잡힌 건수: **오늘 6건 / 2026-08-19 0건.**

- **기준 위반**:
  - **계측 4원칙 ②(미측정 ≠ 0)** — `supported=False`(TR 응답 없음)를 `institution=+0` 으로
    표현해, 09:02부터 들어오는 진짜 값(어제 실측 `institution=+979`)과 **같은 타입·같은 축**에
    실린다. `*_measured` 동반이 없다.
  - **코드가 약속한 보장을 초과한다.** `model/multi_horizon_model.py:236~240`이
    이 피처에 `σ_floor = 0.15`를 걸면서 주석으로
    *"장 시작 첫봉에서 z=-4.22 실측(07/01 08:59) … σ_floor=0.15 → z≈-2.0 → AutoMask 임계 이하"*
    라고 명시한다. 오늘 실측 -15.07은 그 설계 보장의 **약 7.5배**다.
- **영향**: 09:00 봉의 6개 호라이즌 예측 입력이 전부 극단값 3개를 포함한 채 만들어졌다.
  진입은 0건이라 **오늘 실손해는 없다**. 다만 이 봉은 STEP 9로 DB에 저장되고 SGD 온라인
  학습·스케일러 재적합의 입력이 되므로, 매일 개장 첫 봉이 오염 표본을 1건씩 적립할 수 있다.
- **신규 여부**: **신규**(값·피처 조합 기준). 상위 개념인 "프리장 수급 0-충전"은
  2026-08-19 pre 리포트 2-1이 다뤘으나 **z 폭발과 연결한 정량 관측은 오늘이 처음**이다.
  ⚠ **`quality_investor_stale` z=+22.34는 여기에 섞지 말 것** — 08-14·08-18·08-19·08-20에
  **소수점까지 동일한 값**으로 나오는 포화(클립) 상수이며 오늘의 변화가 아니다.

---

#### 1-2. 단기 CORE `ofi_norm` 스케일러 identity 강제율 — **2거래일 연속 100%**

- **증상**: 스케일러 재적합 때마다 `ofi_norm` 의 raw σ가 0에 가까워
  표준화를 포기하고 `identity(0,1)`(= 원값 통과)로 강제된다. 오늘 장전 구간
  refresh 7회 × 6호라이즌 = **42회 전부** 발동했다.
- **근거**:
  ```
  2026-08-20 08:45:03 [WARNING] SIGNAL: [ScalerRefresh] 1m CORE 'ofi_norm' raw_std≈0(0.0379) → identity(0,1) 강제 (FLAT 100% 방지)
  2026-08-20 09:01:02 [WARNING] SIGNAL: [ScalerRefresh] … raw_std≈0(0.0352) → identity(0,1) 강제
  ```
  `logs/20260820_SIGNAL.log` · 42건 · 08:45:03~09:01:02

  **거래일별 발동률** (분모 = `ScalerRefresh] ts=` 이벤트 × 6호라이즌)

  | 일자 | refresh 이벤트 | 호라이즌 검사 | identity 강제 | 발동률 |
  |---|---|---|---|---|
  | 2026-08-18 | 48 | 288 | 204 | 70.8% |
  | 2026-08-19 | 39 | 234 | 234 | **100.0%** |
  | 2026-08-20 (장전) | 7 | 42 | 42 | **100.0%** |

  전일 EOD 재학습(15:45~16:08)에서도 6호라이즌 전부 동일 발동:
  ```
  2026-08-19 16:08:45,426 [WARNING] SIGNAL: [ScalerRefresh] 1m CORE 'ofi_norm' raw_std≈0(0.0346) → identity(0,1) 강제
  ```
  `logs/retrain_eod_20260819.log`

- **기준 위반**: **CLAUDE.md 절대원칙 §3** — `ofi_norm`(OFI 불균형)은 단기 그룹
  (1m·3m·5m)의 **CORE 피처**이며 미통과 시 등급 하락 대상이다. 표준화가 상시 무력화되면
  그 CORE가 모델·체크리스트에 기여하는 정도를 **관측할 수 없게 된다**(정확히 계측 4원칙
  ④가 경계하는 "폴백이 정상값처럼 보인다").
  또한 CLAUDE.md §3이 이미 *"`cvd_divergence`·`ofi_norm` 학습표본의 91%가 정확히 0.0인
  점질량은 분위수 비닝으로도 해소되지 않는 잔여 이슈"* 라고 등록해 둔 축과 같은 뿌리다.
- **영향**: 오늘 진입 0건이라 즉시 손해는 없다. 다만 발동률이 70.8% → 100%로 **올라가는
  추세**이며, 100%면 이 CORE는 사실상 상수 취급으로 예측에 기여하지 못한다.
- **신규 여부**: **기존 관측의 임계 접근.** 2026-08-19 pre 리포트가 "확인 필요"로
  *"`ofi_norm` raw_std 급락의 원인 … 최소 3거래일 관측 후 결론(313차)"* 을 걸어뒀고,
  `dev_memory/NEXT_TODO.md` **P-4**가 *"3거래일 연속 90%+면 CORE 재심사 안건 상정"* 을
  사전등록했다. **현재 2거래일 연속 100% — 오늘 종일 집계가 90%를 넘으면 P-4 성립,
  그 다음 거래일이 3일차다.** 사전등록 문턱을 낮춰 앞당겨 판정하지 말 것(458차 D6).
- **판정 보류 사유(313차)**: 원인 후보 ①틱 수집 경로 변화 ②`features/technical/ofi.py`
  산출 로직 ③실제 미시구조(호가 1틱 깊이 축소) 중 무엇인지 **로그만으로는 갈리지 않는다.**
  판정 수단은 이미 있다 — `scripts/ofi_norm_distribution_probe.py`(2026-08-19 21:24 신설,
  읽기 전용). **장중 DB 금지 구간이므로 장후 실행**(F-2).

---

### P2 — 운영 부담·기술부채

#### 2-1. `CybosProbe` 프리장 실패는 **"TR이 없다"가 아니라 "프리장엔 안 준다"** 였다 — 오늘 확정

- **증상**: 08:58 프리장 프로브에서 `CpSysDib.CpSvrNew7221` 이
  `-2147221008 CoInitialize가 호출되지 않았습니다` 로 실패하고, 그 결과가
  `reason=Cybos 선물 투자자 TR 미발견` 으로 합산된다. **동일 TR이 09:02:33부터는 성공한다.**
- **근거**:
  ```
  2026-08-20 08:58:06 [WARNING] PROBE: [CybosProbe] CpSysDib.CpSvrNew7221 dispatch/request failed: (-2147221008, 'CoInitialize가 호출되지 않았습니다.', None, None)
  2026-08-20 08:58:06 [WARNING] PROBE: [CybosProbe] Dscbo1.FutureTrader  … (-2147221005, '잘못된 클래스 문자열입니다.')  ← 후보가 실제로 없음(정당)
  ---
  2026-08-20 09:02:33 [INFO]    PROBE: [CybosProbe] CpSysDib.CpSvrNew7221 ok status=0 nonempty_headers=3 rows=30
  2026-08-20 09:02:33 [INFO]    PROBE: [CybosProbe] Dscbo1.CpSvr8111      ok status=0 nonempty_headers=56 rows=1
  ```
  `logs/20260820_PROBE.log` (장전 실패 10건 / 09:02~09:04 성공 6건)

  소비자 쪽 전환도 확인된다(2026-08-19 종일 기록, 오늘도 09:02부터 동일):
  ```
  2026-08-19 08:58:25 [INFO] DATA: [CybosInvestor] futures supported=False source=FutureMst_oi foreign=+0 …
  2026-08-19 09:02:21 [INFO] DATA: [CybosInvestor] futures supported=True  source=CpSysDib.CpSvrNew7221 foreign=-1023 individual=+81 institution=+979 …
  ```
  일자별 `supported=True` 건수: 08-10 393 · 08-11 393 · 08-12 393 · 08-13 378 · 08-14 393 ·
  08-18 393 · 08-19 280(동결로 조기 종료) · **08-20 3(09:04 현재)**.
- **기준 위반**: 계측 4원칙 **②(미측정 ≠ 0)** + **③(탈락 가시화)** — 실패 사유가
  "클래스 없음(-2147221005)"과 "COM 미초기화(-2147221008)"로 성격이 전혀 다른데
  한 문장(`TR 미발견`)으로 합쳐진다.
- **영향**: 실손해 0. 다만 이 오표기가 **1-1의 z 폭발을 "원인 불명"으로 보이게 만드는
  1차 원인**이다. 두 이상점은 같은 뿌리이며 F-3에서 함께 처리한다.
- **신규 여부**: **재발(2일차)** — 2026-08-19 pre 리포트 P2-1이 동일 증상을 보고했고
  같은 리포트가 커밋 초안까지 적어뒀다(`[MW0601] 478차 후속2: CybosProbe CoInitialize 배선
  + 미시험/미발견 분리 표기`). **해당 커밋은 이력에 없다**(최근 12커밋 확인). 미착수.
  단, 오늘 새로 확정된 부분이 있다 — **"프리장 미제공 → 09:02 정상"이라는 일중 전환 자체가
  정상 동작이라는 것**이며, 이는 어제 리포트가 도달하지 못한 결론이다.

#### 2-2. 수집기 §2 "미커밋 457건" — CRLF 오탐 **재발(2일차)**, 실질 소스 변경 **0건**

- **근거**: `git diff --ignore-cr-at-eol --stat` → **출력 없음**.
  `git diff --stat` 은 `439 files changed, 228,618 insertions(+), 228,618 deletions(-)`
  (모든 파일이 삽입=삭제 → 전파일 재작성 = 줄바꿈 아티팩트).
  실제 남는 것은 **미추적 18건**(`docs/정기점검/매일점검/` 리포트·다이제스트)뿐이다.
  원인: 코웍의 bash는 리눅스 샌드박스라 `core.autocrlf`가 false다. **윈도우 git은 clean을 본다.**

  > ⚠ **판정 명령을 잘못 고르면 반대 결론이 나온다 — 다음 세션 주의.**
  > `git diff --ignore-cr-at-eol --name-only` 는 **여전히 200여 파일을 나열한다**
  > (blob이 다르면 이름은 나온다). 실질 변경 판정에는 반드시
  > **`--stat` 또는 `--numstat`** 을 쓸 것. 오늘 `--numstat` 결과는
  > `dev_memory/DECISION_LOG.md 142/0` · `dev_memory/NEXT_TODO.md 75/0` **두 줄뿐**이며,
  > 이 둘은 본 점검이 방금 append 한 것이다. 교차검증으로
  > `git show HEAD:<f> | tr -d '\r' | md5sum` vs `tr -d '\r' < <f> | md5sum` 도
  > `model/multi_horizon_model.py`·`strategy/entry/checklist.py` 에서 **일치**를 확인했다.
- **영향**: §11 자동 적신호 7번 오탐. 진짜 미커밋이 457 속에 묻힌다(계측 4원칙 ③).
  ⚠ 어제(2026-08-19)는 실질 4건이 묻혀 있었다 — **오늘은 0건이라 우연히 무해했을 뿐**이다.
- **신규 여부**: **재발(2일차)** — 0819 pre 리포트 2-2. 미착수.

#### 2-3. 수집기 §3 `미발견 ⚠` 5행 — **7일째 동일 오탐**, 코드 무결성 문제 아님

- **근거**: 5개 상수를 repo 전체에서 실측 검색한 결과
  `MODEL_LABEL_STATE_UNLOCK_ENABLED` · `PRE_RETRAIN_DONE_BY_EOD_ENABLED` ·
  `ZONE_ENTRY_BAN_ENFORCE` · `ZONE_ENTRY_BAN_SHADOW_ENABLED` ·
  `PIPE_LATENCY_EXCLUDE_MODEL_SWAP` — **전부 `.claude/skills/…/collect_evidence.py`
  (수집기 기대표) 안에만 존재**하고 `config/settings.py`를 포함한 어떤 소스에도 없다.
  관련 키워드(`zone_entry_ban`, `MODEL_LABEL_STATE`, `EXCLUDE_MODEL_SWAP`) 검색도 0건.
  다만 `_pre_retrain_done` **런타임 플래그 자체는 살아 있고 정상 동작**한다
  (`main.py:791/4730/7299/11523`, 오늘 08:55:03 `[PreRetrain] … 스킵` 로그로 실증).
- **영향**: §11 자동 적신호 5칸을 상시 점유해 진짜 신호를 밀어낸다. 특히
  `MODEL_LABEL_STATE_UNLOCK_ENABLED` 는 **실전전환 기준 ⑧의 선행조건**으로 못박힌 항목이라,
  MW0601 관측만으로 ⑧을 판정하면 틀린다.
- **신규 여부**: **재발(0819 리포트 1-3 기준 7일째)**. 이월 Fix(수집기 브랜치 스코프 분리)
  미착수.

#### 2-4. 수집기 §7 "로그 종료시각 델타 **-399분**" — 장전 국면에서 구조적 무의미

- **근거**: 다이제스트 §7 하단이 *직전 5거래일 중앙값 15:40 vs 오늘 09:01 → 델타 -399분*
  으로 출력한다. 오늘 로그는 **아직 끝나지 않았다** — 09:01은 종료시각이 아니라 현재시각이다.
- **원인**: 이 대조는 480차 G-2가 **장후 조기종료(2026-08-19 동결)를 잡으려고** 넣은 지표인데,
  국면 분기 없이 `pre`에서도 출력된다.
- **영향**: 매일 장전 리포트마다 "-399분"류의 거짓 적색이 생겨, 진짜 조기종료가 났을 때
  둔감해진다(늑대소년). 오늘 §11 자동 적신호에는 안 올라왔으나 §7 본문에는 남는다.
- **신규 여부**: **신규**(G-2는 2026-08-19 배포, 오늘이 첫 장전 노출).

---

### 확인 필요 (확정 아님)

- **`[IntradayRegime] NORMAL → CRASH` 09:01:59** (`day=-0.61% atr=1.00 z=3`).
  개장 2분 만의 전이다. `dev_memory/NEXT_TODO.md` **P-5**(*"IntradayRegime 종일 전이 40회
  이상 2일 연속이면 G-7 즉시 착수"*)의 관측 대상이며, 08-19가 반일 51회였다.
  **장중 국면에서 종일 카운트로 판정할 것** — 장전 표본으로 결론 금지.
- **`ofi_norm` raw_std 급락의 원인** — 1-2 참조. 판정에 `predictions.db`/`raw_data.db`
  접근이 필요하므로 **장후로 미룬다**(CLAUDE.md 장중 라이브 DB 분석 금지, 2026-08-10
  CB⑤ 자가유발 전례).
- **`institution_futures_net` σ_floor 0.15가 오늘 실제로 적용됐는지.**
  오늘 `[ScalerFloor]` 발동 목록은 호라이즌당 6종
  (`macro_vix`·`macro_sp500_chg`·`macro_us10y_chg`·`quality_investor_age_sec`·
  `toxicity_atr_stress`·`macro_risk_on`)이며 `institution_futures_net`는 **없다**.
  즉 학습 σ가 0.15 이상이었다는 뜻인데, 그렇다면 -15.07을 만들려면 원값 편차가
  2.26 이상이어야 한다. **σ_floor 미발동이 정상인지 자체가 확인 대상**이며,
  스케일러 상태 조회가 필요하므로 장후 판정.

### 이미 반영된 사안 (신규 아님)

- **동결 워치독(FZ-1) 라이브 배선 확인됨.** 오늘 08:41:33
  `[FreezeWatchdog] 기동 — 하트비트 180s 정체 ×2회 연속 시 os._exit(43) → 런처 재기동
  … 하트비트파일=data/heartbeat_MW0601_20260820.json`.
  480차 G-1(하트비트 별도 파일 분리)도 실물 확인:
  `{"beat_age_sec": 0.3, "watching": true, "strikes": 0, "fired": false, "window": ["09:00","15:45"]}`.
  → **NEXT_TODO `FZ-11`(오늘자 오탐 0건 확인) 관측 1일차 진행 중** — 장후에 종일 `beat_age` 확인.
  근거: 커밋 `22cbb67`·`552d982`·`ea60409`.
- **`MAX_CONTRACTS = 3`** — 431차 배포분이며 오늘 `[Capital]` 로그로 재확인
  (`base_risk=1,500,000원(3.0%) … MAX_CONTRACTS=3 최대노출=152,144,999원`).
  실전전환 ⑧의 **조건 2는 이미 충족**이다. 남은 해제 조건은 실전 자본 재설정뿐 — 재상정 금지.
- **CB② `CB_CONSEC_STOP_LIMIT = 9999`** — 모의투자 한정 예외이며 **재검토 기한 2026-08-29
  미도래**. "복원 안 했다"를 이상점으로 올리지 않는다(2026-08-01 결산 결정).
- **CB③-P4 / FP-CRITICAL / TOX-SEVERE-SPREAD 비활성** — 전부 CLAUDE.md 등재된 한시 예외이며
  값이 기대값과 `일치`. 재상정 금지.
- **`OPT50029` 미출현** — 오늘 `broker=cybos`이고 `OPT50029`는 Kiwoom 경로
  (`collection/kiwoom/api_connector.py`) 전용이다. 로그에 `OPT#####` 토큰 0건은 **정상**.
  0819 리포트 2-3이 이미 체크리스트 문구 결함으로 등록했다.

---

## 2. Fix 작업 구현계획

> **장전/개장 직후 국면이므로 본 계획은 수립만 하고 적용하지 않는다.**
> 라이브 프로세스(PID 13140)가 돌고 있고 라이브 DB 분석 금지 구간이다.
> **전 항목 적용 시점 = 15:10 강제청산 이후.**

### F-1. 프리장 수급 0-충전 가시화 + 첫봉 z 폭발 차단 — P1 · 대응 1-1

- **원인 가설**: `[CybosInvestor]`가 `supported=False` 구간에서
  `foreign/individual/institution` 을 **0으로 채워 반환**하고, EarlyWarmup(08:45)~
  Phase4 refit(08:59)의 스케일러가 그 0 상수 30봉으로 μ·σ를 학습한다.
  09:00 첫 봉에서 진짜 값이 들어오는 순간 z가 폭발한다.
  ※ **확정이 아니다** — σ_floor 미발동(위 "확인 필요" 3번)과 정합하는지 먼저 봐야 한다.
- **변경 파일**:
  - `collection/cybos/investor_data.py` — 반환 dict에
    `institution_futures_net_measured` / `foreign_futures_net_measured` /
    `retail_futures_net_measured` (bool) **동반 반환**. 값 자체는 0 유지(하위호환).
    계측 4원칙 ② 규약 그대로.
  - `model/multi_horizon_model.py` — 스케일러 적합 시
    `*_measured == False` 인 행을 **해당 피처에 한해 제외**(행 전체 드롭 아님).
    제외 개수를 `[ScalerRefresh] … 제외 n행(미측정)` 으로 남긴다(계측 4원칙 ③).
  - `config/settings.py` — `INVESTOR_UNMEASURED_SCALER_EXCLUDE_ENABLED = False`
    (**섀도 우선**. 계산·로그만 하고 실제 제외는 하지 않는다).
- **회귀 위험**: 스케일러 적합 표본이 줄어 σ가 커지고 다른 피처 z가 눌릴 수 있다.
  그래서 **1단계는 섀도**다 — 며칠 `제외 n행` 로그만 보고 승격 판단.
- **절대원칙 저촉 여부**: 없음. §3 CORE 피처 목록에 `institution_futures_net`은 없고,
  CORE 마스크·체크리스트를 건드리지 않는다.
- **런타임 주의**: `investor_data.py`는 py37_32 본체. **COM 콜백 내부 아님**
  (`_on_receive_tr_data` 밖에서 호출) — 절대원칙 §4 저촉 없음.
  `multi_horizon_model.py`는 py37_32(추론)·py310_64(재학습) 양쪽에서 로드되므로
  **pickle 포맷을 바꾸지 말 것**(딕셔너리 키 추가만, protocol=4 유지).
- **검증 방법**:
  ① `tests/test_481_unmeasured_investor.py` 신설 — `supported=False` 응답에서
    `*_measured=False`가 나오는지, 스케일러 제외 카운트가 맞는지.
  ② 다음 거래일 09:01 `[ScalerMonitor] ts=09:00 … max_z=` 가
    **|z| < 8** 로 내려오는지(현행 -15.07 → 설계 목표 |2.0|의 중간 관문).
  ③ `[ScalerRefresh] … 제외 n행(미측정)` 이 08:45~08:59 구간에만 나오는지.
- **섀도 계측 필요 여부**: **필요** — 위 플래그가 그 장치다.
- **적용 시점**: **장후**. 즉시 적용 금지(스케일러 경로는 매분 파이프라인 STEP 4~5 한복판).
- **결정 필요 사항**: 없음(섀도라 되돌리기 쉽다).

### F-2. `ofi_norm` 분포 프로브 실행 — P1 · 대응 1-2 · **코드 변경 없음**

- **원인 가설**: 미정. 후보 3종(틱 수집 경로 / `features/technical/ofi.py` 산출 / 실제 미시구조).
- **실행 대상**: `scripts/ofi_norm_distribution_probe.py`(2026-08-19 21:24 신설, 읽기 전용).
  **아직 한 번도 실행된 적이 없다** — 산출물이 `docs/정기점검/` 어디에도 없다.
  ⚠ FP-CRITICAL·TOX-SEVERE-SPREAD와 같은 "만들어 놓고 안 돌린 계측" 패턴이 되기 직전이다.
- **절차**:
  ```
  (15:10 이후) python scripts/ofi_norm_distribution_probe.py --days 20 --out docs/정기점검/
  ```
  `utils.analysis_db.guard_intraday()` 가 장중이면 rc=2로 스스로 막는다 — 우회하지 말 것.
- **판정 기준(사전등록)**: 08-18 이전 대비 raw σ가 **구간 경계에서 계단식으로 꺾이면**
  코드/수집 경로 변화(①②), **완만히 감소하면** 미시구조(③).
- **회귀 위험**: 없음(읽기 전용).
- **적용 시점**: **장후 즉시.** 이것이 1-2의 3일차 판정을 위한 유일한 근거원이다.
- **결정 필요 사항**: 3일차(2026-08-21)에 P-4가 성립하면 **CORE 재심사는 주간회의 안건**이다.
  절대원칙 §3은 "교체 불가"이므로 세션이 단독으로 CORE를 내리지 않는다.

### F-3. `CybosProbe` COM 초기화 + 실패 사유 분리 표기 — P2 · 대응 2-1 (0819 이월)

- **원인 가설**: 프로브 워커 스레드가 `pythoncom.CoInitialize()` 없이 Dispatch를 시도한다.
  `collection/cybos/api_connector.py:176`·`:386`에는 이미 배선이 있으나
  **프로브 경로가 그 함수를 타지 않는다.**
- **변경 파일**:
  - `collection/cybos/api_connector.py` — 프로브 워커 진입부에
    `pythoncom.CoInitialize()` / 종료부에 `CoUninitialize()`. **기존 `_blockreq` 패턴 그대로 복사.**
  - 같은 파일 — 프로브 결과 표기를 3분류로:
    `미시험(COM 초기화 실패)` / `미발견(클래스 없음, -2147221005)` / `ok`.
  - `collection/cybos/investor_data.py` — `reason=` 문자열이
    **"프리장 미제공(09:00 이후 재시도)"** 과 "TR 미발견"을 구분하도록 수정.
    프리장 실패는 **정상 동작**임이 오늘 확정됐다(2-1).
- **회귀 위험**: `CoUninitialize` 누락 시 스레드 종료에서 COM 참조 누수.
  반드시 `try/finally`. **`dynamicCall`·`emit`을 콜백 안에 넣지 말 것**(절대원칙 §4).
- **런타임 주의**: py37_32. COM 스레드 경계 — 가장 사고가 잦은 지점이므로
  0xC0000409 재발 감시(`logs/crash_fault.log`) 1거래일 필수.
- **검증 방법**: 다음 거래일 08:58 `logs/*_PROBE.log`에서
  `-2147221008` **0건**, `CpSvrNew7221` 이 `미발견`이 아니라 `ok` 또는
  `프리장 미제공`으로 찍히는지.
- **적용 시점**: 장후. **F-1과 별도 커밋**(COM 경로는 롤백 단위를 좁게 유지).

### F-4. 수집기 브랜치 스코프 분리 — P2 · 대응 2-3 (0819 F-1 이월, 7일째)

- **변경 파일**: `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py`
  — 불변식 기대표에 `branches: ["v9-dev"]` / `["dev"]` / `["*"]` 필드 추가.
  현재 브랜치에 해당하지 않는 행은 `미발견`이 아니라 **`해당없음(브랜치 dev 전용)`** 으로 표기하고
  **§11 자동 적신호에서 제외**한다.
- **회귀 위험**: 진짜 상수 소멸을 놓칠 수 있다 → `["*"]`(양 브랜치 공통)이 기본값이어야 한다.
- **검증 방법**: 다음 실행에서 §11 적신호가 7건 → **2건**(축퇴 + 미커밋)으로 줄어드는지.
- **적용 시점**: 장후. 수집기는 라이브 프로세스와 무관하므로 위험 낮음.

### F-5. 수집기 CRLF 내성 + §7 국면 스코프 — P2 · 대응 2-2·2-4

- **변경 파일**: 같은 파일
  - 미커밋 집계를 `git diff --ignore-cr-at-eol --numstat`(⚠ `--name-only` 아님, 2-2 주의 참조) 기준으로 바꾸고,
    원 카운트는 `(원시 457건 — 줄바꿈 차이 제외 시 0건)` 으로 **둘 다 표기**(계측 4원칙 ④).
  - §7 "로그 종료시각 5거래일 대조" 블록을 **`phase == "post"` 에서만** 출력.
    `pre`/`intra`에서는 `_장후 전용 지표 — 현 국면 미출력_` 한 줄.
- **검증 방법**: `--phase pre` 로 재실행 시 §2 미커밋이 0(+미추적 18)로, §7 종료시각 표가
  미출력으로 바뀌는지.
- **적용 시점**: 장후.

### 적용 순서와 커밋 계획

1. **커밋 ①** — F-4 + F-5 (수집기만, 라이브 무관·위험 최저)
   `[MW0601] 481차 (1/3): 수집기 오탐 3종 제거 — 브랜치 스코프 · CRLF 내성 · §7 국면 스코프`
2. **커밋 ②** — F-3 (COM 경로 단독)
   `[MW0601] 481차 (2/3): CybosProbe CoInitialize 배선 + 프리장 미제공/미발견 분리 표기`
3. **커밋 ③** — F-1 (섀도 플래그 포함)
   `[MW0601] 481차 (3/3): 프리장 수급 미측정 플래그(*_measured) + 스케일러 제외 섀도`
4. F-2는 코드 변경 없음 — 실행 후 산출물만 `docs/정기점검/`에 추가.

---

## 3. 고도화 방안

### G-1. EOD 재학습 체인의 프로세스 독립성을 **설계 자산으로 명문화**하고 역방향을 계측한다

- **관측 근거**: 2026-08-19 메인 프로세스는 13:41부터 동결이었다(480차 사고). 그런데
  `logs/retrain_eod_20260819.log`는 **15:45:03 시작 → 16:08:45 정상 완료**
  (`Python : 3.10.20 64-bit`, `[P8] session_state p8_last_success_date + eod_retrain_ok_date
  기록 완료`)이고, 모델 파일 12종이 16:05~16:08에 전부 교체됐다.
  그 덕에 오늘 08:55 `[PreRetrain] … 1영업일 전(2026-08-19) EOD 재학습 성공 → 스킵`이 성립했고,
  **CLAUDE.md가 경고한 "재학습 실패 → 모델 미교체 → CB③ HALT" 연쇄가 발생하지 않았다.**
  즉 **메인이 죽어도 EOD가 사는 구조**가 어제 실전에서 한 번 증명됐다 — 이건 우연히 얻은
  복원력이지 문서화된 설계가 아니다.
- **제안 내용**:
  ① `CLAUDE.md` 운영 환경 절에 *"EOD 재학습 체인(`EOD_RETRAIN.bat`/`retrain_eod.py`)은
     장중 본체(py37_32)와 **별도 프로세스**이며, 본체 이상 시에도 독립 완주한다"* 를 명기.
     지금은 py310_64 분리 이유가 OOM 회피로만 적혀 있어, 이 복원력이 **읽는 사람에게 안 보인다.**
  ② **역방향을 계측한다** — 지금은 반대 경우(EOD가 실패했는데 본체는 멀쩡)를 감지하는
     장치가 08:55 `[PreRetrain]` 한 곳뿐이고, 그때는 이미 개장 5분 전이다.
     `scripts/campaign_steps.py` EOD 체인 마지막에
     `data/eod_retrain_ok_YYYYMMDD.txt` 마커를 쓰고, **런처가 08:41 기동 시** 그 마커의
     날짜가 직전 거래일인지 확인해 아니면 `[EODGuard] 전일 EOD 미완료 — 모델 노후 N일` 을
     **WARN 채널에 남긴다.**
- **기대 효과**: 모델 노후 상태로 개장하는 사건을 **08:41에** 알 수 있다(현행 08:55 대비 +14분).
  측정 지표 = 다음 26주 동안 `[EODGuard]` 발화 건수와, 그중 09:00 전 조치로 이어진 비율.
- **비용·위험**: 마커 파일 1개 + 런처 분기 1개. 낮음. **섀도 불필요**(경보만, 차단 없음).
- **선행 조건**: 없음.
- **우선순위 제안**: **이번 주**. F-2/F-3보다 뒤, F-4/F-5와 같은 주.

### G-2. 개장 첫 봉 `max_z` 를 일별 기준선으로 상설 계측한다

- **관측 근거**: 1-1의 -15.07은 **5거래일 첫봉 max_z를 손으로 뽑아 비교했기 때문에**
  이례임을 알 수 있었다. 그 대조가 없었으면 `[ScalerMonitor]` WARNING 6줄은
  매일 나오는 잡음 속에 묻혔을 것이다 — 실제로 `quality_investor_stale z=+22.34` 는
  4거래일 연속 소수점까지 같은 포화 상수인데 매일 최댓값으로 올라와 **진짜 변화를 가린다.**
- **제안 내용**: `scripts/collect_evidence.py` §5 거래일 요약에
  **"개장 첫봉 z 프로파일"** 블록 신설 —
  ① 오늘 `ts=09:00` 의 `max_z` 값·피처·`extreme` 개수
  ② 직전 5거래일 같은 값의 중앙값과 델타
  ③ **포화 상수 필터** — 3거래일 이상 소수점까지 동일한 값은 `(포화)` 표기하고 2순위로 내린다.
- **기대 효과**: 1-1 같은 건을 **손 대조 없이** 다이제스트 §11에 자동으로 띄운다.
  측정 지표 = 다음 20거래일 중 이 블록이 유의 델타(|Δz| ≥ 5)를 띄운 횟수와, 그중 실제 원인이
  규명된 비율.
- **비용·위험**: 수집기 파싱 로직 1개(정규식 `max_z=([-+][\d.]+)\(([a-z_]+)\)`).
  라이브 무관. 낮음.
- **선행 조건**: 없음. F-5와 같은 커밋에 넣을 수 있다.
- **우선순위 제안**: **이번 주**(F-5와 병합).

### G-3. `identity(0,1) 강제` 를 CORE 피처 한정 **일일 집계**로 승격

- **관측 근거**: 1-2의 발동률(70.8% → 100% → 100%)은 `grep -c` 두 번으로 나왔지만,
  **`dev_memory/NEXT_TODO.md` P-4가 "3거래일 연속 90%+" 라는 사전등록 문턱을 세워 놓고도
  그 비율을 자동으로 세는 곳이 없다.** 문턱만 있고 계기가 없는 상태 —
  FP-CRITICAL이 "학습분포 저장 함수가 호출된 적 없어 2개월간 PSI=0.0"이었던 것과 같은 형태다.
- **제안 내용**: 수집기 §5에 **"CORE 스케일러 폴백률"** 행 —
  CORE 피처(`cvd_divergence`·`ofi_norm`·VWAP 위치)별로
  `identity 강제 / 전체 호라이즌 검사` 를 일별 %로 내고, **직전 2거래일과 나란히** 찍는다.
  90% 3연속이면 §11 자동 적신호로 올린다.
- **기대 효과**: P-4 판정이 사람 손 집계에서 기계 집계로 옮겨간다. 문턱을 사후에 낮추는
  사전등록 위반(458차 D6)도 구조적으로 어려워진다.
- **비용·위험**: 낮음. **다만 이것은 계측일 뿐 CORE 처분이 아니다** —
  절대원칙 §3 CORE 교체는 주간회의 안건이며 세션이 단독 결정하지 않는다.
- **선행 조건**: **F-2 프로브 결과가 먼저다.** 원인을 모르는 채 비율만 자동화하면
  "숫자는 있는데 아무도 뭘 해야 할지 모르는" 채널이 하나 더 는다.
- **우선순위 제안**: **다음 주**(F-2 결과 확인 후).

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| EOD 체인 프로세스 독립성 | 문서화 안 됨 | `CLAUDE.md` 운영 환경 절에 명기 + `[EODGuard]` 계측 | 2026-08-19 동결에도 EOD가 완주해 CB③ HALT를 막았다 — 우연이 아니라 구조인데 문서에 없다 (G-1) |
| 개장 첫봉 z 프로파일 | 없음 | 수집기 §5 상설 블록 | 1-1은 손 대조로만 발견됐다 (G-2) |
| CORE 스케일러 폴백률 | NEXT_TODO P-4에 문턱만 존재 | 수집기 §5 자동 집계 | 문턱은 있는데 계기가 없다 (G-3) |
| 실전전환 ② 선행 ⓑ (동결→재기동 왕복) | CLAUDE.md 등재됨, 라이브 미확인 | **변경 제안 없음** — `FZ-1L` 리허설 대기 유지 | 오늘 `strikes=0 fired=false`로 배선만 확인. 왕복은 미관측 |

---

## 4. 진입 승패 사후검증 (장후 한정)

**해당 없음(장후 전용).** 참고로 오늘 09:20 현재 진입 0건 · 청산 0건 · 차단 0건 · `[Sizer]` 0건.

## 5. (장후 전용 절)

**해당 없음(장후 전용).**

---

## 부록 A. 장전 체크리스트 판정 (`references/phases.md` A절)

| 항목 | 판정 | 근거 |
|---|---|---|
| A-0 브랜치 = `v9-dev` | ✅ | `git branch --show-current` → `v9-dev`. `origin/v9-dev` 대비 **ahead 7 · behind 0** |
| A-1 중복 기동 없음 | ✅ | PID 13140 단일. 런처 `[GUARD] 기존 프로세스 종료 완료 -- 새 인스턴스 시작` 08:40 |
| A-1 런타임 py37_32 32-bit | ✅ | 런처 L10~13 `환경 활성화: py37_32` / `32-bit Python 확인` |
| A-1 Cybos 접속·COM 정상 | ✅ | `[Capability] broker=cybos connect=Y/Y balance=Y/Y order=Y/N … server=Cybos 모의투자` 08:41:33 |
| A-1 모델 번들 로드 | ✅ | `[RF] 로드 완료: 6호라이즌 ready=True` 08:41:12 · `[Calib] 기동 사전 학습 완료: 18000건` |
| A-1 **전일 EOD 재학습 성공** | ✅ | `retrain_eod_20260819.log` 15:45:03→16:08:45 완주, `Python 3.10.20 64-bit`, `[P8] … 기록 완료`. 모델 12종 mtime 16:05~16:08 |
| A-2 매크로 → 레짐 판정 | ✅ | 08:58:06 `[Regime] RISK_ON (점수=2) \| VIX=15.8 \| SP500=+0.21% \| USD/KRW -1.46%` |
| A-2 **실시간 사전 구독** | ✅ | 08:45:03 `[CybosRT-START] tick/hoga subscribe end` + `[EarlyWarmup] Cybos RT 08:45 선행 구독 시작 (프리장 봉 15봉 확보)` — 09:00보다 15분 앞섬 |
| A-2 옵션 체인 로드 | ✅ | 08:41:33 `[OptionChain] 초기화 완료: 체인 캐시 5242 종목` · 09:01:51 `target=24 valid=24 PCR=0.921 GEX=12.20B` |
| A-2 선물 분봉 TR `OPT50029` | **해당 없음** | Cybos 경로. `OPT50029`는 Kiwoom 전용(0819 2-3 등록) |
| A-3 불변식 전행 일치 | ⚠ | 22 일치 / 1 값확인 / **5 미발견(오탐 — 2-3)** |
| A-3 한시예외 기한 경과 | ✅ | CB② 재검토 `2026-08-29` **미도래**. 나머지 3종은 기한 없는 조건부 |
| A-4 미커밋 변경 | ✅ | 실질 **0건**(CRLF 오탐 457 — 2-2). 미추적 18건은 본 점검 산출물 |
| A-4 PC명 태그 | ✅ | 최근 12커밋 전부 `[MW0601]` |
| A-4 타 PC 커밋 pull | ⚠ | `behind 0`이나 **push 안 된 로컬 7커밋 존재** — MW0602가 480차 후속을 아직 못 본다. NEXT_TODO `[MW0601 479차] MW0602 배포 확인` 미완 |

## 부록 B. 프리장 파이프라인 타임라인 (실측)

```
08:40:50  [FaultHandler] 활성화 PID=13140
08:41:10  [System] DB 초기화 완료 → 미륵이 초기화
08:41:12  [RF] 로드 완료: 6호라이즌 ready=True
08:41:33  [BrokerSync] FLAT -> FLAT (armistice cleared) · [Capital] MAX_CONTRACTS=3
08:41:33  [FreezeWatchdog] 기동 (180s ×2 → os._exit(43)) · [System] Qt 이벤트 루프 진입
08:45:03  [EarlyWarmup] scaler 노후=17h → 선행 warmup (30봉) · [CybosRT-START] tick/hoga 구독 ✅
08:45:59  [PreMarket] GapOffset 사전 설정 close=1050.50
08:48:00  [PreMarket] Phase1 refit  z경고 18 → 7
08:50:00  [PreMarket] Phase2 refit  z경고  7 → 4
08:55:03  매크로 seed fetch · [Canary] z경고 11개(임계 12) · [PreRetrain] 정당 스킵
08:55:04  [PreMarket] Phase3 refit  z경고 11 → 6
08:58:06  [Regime] RISK_ON 확정 · [CybosInvestor] supported=False (프리장 정상)
08:59:01  [PreMarket] Phase4 refit  z경고  7 → 7  ← 개선 정체
09:00:00  정규장 개장 · [ShadowSession] LIVE 전환 · [RegimeFingerprint] PSI=0.008 level=0 ✅
09:01:00  ⚠ [ScalerMonitor] max_z=-15.07(institution_futures_net) ×6        ← 1-1
09:01:02  [PipePerf] total=2353ms (S5=1200 S4=541 S6=498) · CB⑤ 경고 [장시작 버스트→임계9s]
09:01:59  [IntradayRegime] NORMAL → CRASH (day=-0.61% atr=1.00 z=3)          ← 확인 필요
09:02:33  [CybosProbe] CpSvrNew7221 ok rows=30 · CpSvr8111 ok               ← 2-1
```

> `08:59:01 Phase4 refit z경고 7 → 7`(개선 0)은 **오늘 진짜로 눈여겨볼 지점**이다.
> Phase1~3은 매번 줄었는데 마지막만 정체했고, 잔존 목록에 `institution_futures_net`이
> 계속 들어 있다(`atr, avg_volume, cancel_add_ratio, institution_futures_net,
> microprice_bias, toxicity_queue_stress, va_bandwidth`). 1-1과 같은 뿌리로 보이나
> **인과 확정은 F-1 섀도 로그를 봐야 한다.**

---

*본 리포트는 로그·파일 mtime·git 이력만으로 작성됐다. 라이브 DB 질의 0건 · 코드 변경 0건.*
