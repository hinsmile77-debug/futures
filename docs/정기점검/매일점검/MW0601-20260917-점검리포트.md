# 미륵이 일일 점검 — MW0601 / 2026-09-17

- 점검 시각: 09:10 KST · PC `MW0601` · 브랜치 `v9-dev`
- HEAD `3507853` · 미커밋 657건(대부분 줄바꿈 문자 차이로 추정 — 아래 §1 확인 필요 참조) · 당일 커밋 6건
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260917_pre.md`

## 0. 오늘 한눈에

**조건부 정상 — 오늘 새벽부터 개장 직전까지 프로그램이 다섯 번 자동으로 꺼졌다 켜졌지만, 개장(오전 9시) 전에 모두 정상 복구되어 지금은 문제없이 돌아가고 있습니다.**

가장 큰 사건은 오전 5시 45분에 증권사 프로그램(브로커) 접속이 끊긴 것입니다. 자동으로 다시 접속을 시도했는데 첫 번째 시도가 실패했고, 원인은 같은 이름의 증권사 프로그램이 이미 하나 더 켜져 있어서 자동 로그인 프로그램이 화면을 잘못 인식했기 때문입니다. 결국 오전 6시 15분에 재시도가 성공해서 약 30분 만에 복구됐습니다. 다행히 개장(오전 9시)까지는 3시간 넘게 남아 있어서 실제 거래에는 지장이 없었습니다.

그 외에도 오전 6시 16분, 7시 3분, 7시 26분, 8시 56분, 이렇게 총 네 번을 더 껐다 켰습니다. 이 중 세 번은 원인을 확인했는데, 프로그램이 시작할 때 확률 계산(캘리브레이션)을 다시 맞추는 과정에서 컴퓨터가 튕겨나가는 오류였습니다. 특히 마지막 재시작(오전 8시 55분)은 하필 시장 상황(레짐)을 판단하는 매우 중요한 시간대 한가운데서 일어났습니다. 다행히 28초 만에 다시 켜져서 오전 8시 58분에 시장 상황 판단(레짐: 중립)을 제시간에 마쳤지만, 아슬아슬했습니다. 조금만 더 늦게 다시 켜졌다면 개장 준비가 늦어졌을 수 있습니다.

오늘 아침 현재까지 실제로 주문을 넣거나 매매를 시작한 내역은 없고(포지션 없음), 어제 밤 모델 재학습도 정상적으로 끝났습니다. 지금 이 시각(오전 9시 10분) 프로그램은 정상적으로 시세를 받아오고 있고, 자동 매매를 막을 만한 심각한 문제는 없습니다. 다만 자동 진입 확률 계산기가 오늘 아침 일시적으로 "어떤 신호가 와도 자동 진입 기준을 넘지 못하는" 상태였는데, 이는 최근 며칠간 반복돼 온 익숙한 패턴이라 새로운 문제는 아닙니다.

**사용자가 지금 당장 할 일은 없습니다.** 다만 오늘 새벽의 재시작 반복 현상은 원인 조사가 더 필요해서, 장 마감 후 코드 수정 계획을 세워뒀습니다(§2 참고).

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 조건부 정상 | 재시작 5회(그중 1회는 브로커 접속 끊김, 3회는 확률계산 중 프로그램 오류) — 전부 개장 전 복구 완료 |
| 장중 | (진행 중 — 다음 절 참조) | |
| 장후 | (미실행) | |

**절대원칙 준수 여부** (장전 시간대라 실제 거래는 아직 없어 대부분 "해당없음")

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | 해당없음 | 아직 장전(09:10) — 청산 대상 포지션 자체가 없음 |
| ② | Circuit Breaker 5종 | 해당없음(미발동, 정상) | 오늘 진입·청산 0건이라 발동 조건 자체가 없었음 |
| ③ | CORE 피처 호라이즌 그룹별 | 해당없음 | 오늘 진입체크 통과 0건 |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 위반 흔적 없음 | 오늘 로그에 `0xC0000409`/`STACK_BUFFER` 없음. 단, 아래 1-2의 access violation은 콜백이 아니라 sklearn 적합 스레드에서 발생 — 이 원칙과는 무관 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 확인 안 함 | Cybos 브로커는 이 TR 체계를 쓰지 않음(Kiwoom 전용 원칙) |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 위반 없음 | 오늘 관련 활동 없음 |

**설정 불변식** — 수집기 §3 표 전 항목 `일치`. 이상 없음(CB②=3, CB3_P4=False, FP_CRITICAL=False, MAX_CONTRACTS=3, TOXICITY_SEVERE_SPREAD=False 등 전부 의도한 값 그대로).

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

없음. (1-2는 "간발의 차로 P0를 피한 사례"로 P1에 등재 — 아래 참조)

### P1 — 정확성·관측 훼손

#### 1-1. 오전 5시 45분 CYBOS Plus(증권사 프로그램) 접속 끊김 — 자동 재로그인 1차 시도 실패, 복구까지 약 29분

- **증상**: 새벽에 정상 기동해 있던 프로그램이 05:45:18에 브로커(Cybos Plus) 접속이 끊긴 것을 감지했다. 자체 재접속 시도(`connect_broker()`)가 "U-CYBOS/CYBOS Plus is not connected" 예외로 실패해 프로세스가 종료됐고, 자동재시작 장치의 첫 재로그인 시도(05:45:39)도 실패했다. 이후 06:04~06:05 사이 3차례 런처 재시도는 모두 "cybos API 세션이 300초 안에 준비되지 않았습니다" 오류로 끝났고, 06:14:50 재시도에서 비로소 로그인에 성공, 06:15:35 런처가 정상 기동해 06:16:14 프로세스가 재기동했다.
- **근거**:
  ```
  2026-09-17 05:44:48 [INFO] SYSTEM: [System] 대기 중 | 장 전 — 매크로 수집 대기 ...
  2026-09-17 05:45:18 [ERROR] SYSTEM: [System] 키움 연결 끊김 — 재연결 시도
  2026-09-17 05:45:18 [INFO] SYSTEM: [Notify] 🚨 [05:45:18] [미륵이] 🚨 cybos 연결 끊김 — 재연결 시도 중
  ```
  `logs/20260917_SYSTEM.log` 및 `logs/20260917_WARN.log`

  ```
  Traceback (most recent call last):
    File "main.py", line 15386, in _scheduler_tick
      self.connect_broker()
    File "collection/cybos/api_connector.py", line 428, in connect
      raise RuntimeError("U-CYBOS/CYBOS Plus is not connected. ...")
  [AUTO-RESTART] main.py 가 6분 실행됨 -- 일시적 크래시, 카운터 초기화.
  [AUTO-RESTART] #1 시도 (시각=0545) -- 10초 후 재시작...
  [AUTO-RESTART] cybos 연결 끊김 -- 재로그인 시도...
  [AUTO-RESTART] 재로그인 실패 -- 재시작 중단
  ```
  `logs/Mireuk_batch/launcher_20260917_053851_9923.log`

  ```
  === autologin start: 2026-09-17 05:45:39 ===
  [로그인 창 'CYBOS Starter'] 자식 컨트롤 4개:
    '예(&Y)' '아니요(&N)' ... '프로그램(CPSTART.EXE)이 이미 실행중입니다. ...'
  [WARN] Product menu control was not found and login form not detected.
  [ERROR] CYBOS Plus menu is not selected in the login window.
  [WARN] 로그인 자동화 실패 -- 재시도...
  [ERROR] Auto-login failed.

  === autologin start: 2026-09-17 06:14:50 ===
  ... (정상 로그인 폼 인식, 입력, 로그인 성공)
  [OK] CybosPlus 연결 성공 (ServerType=1)
  ```
  `logs/cybos_autologin_diag.log`

  ```
  [ERROR] cybos API 세션이 300초 안에 준비되지 않았습니다.
  [HINT]  LAUNCH_API.bat 를 먼저 실행한 뒤 이 런처를 다시 실행하십시오.
  ```
  `logs/Mireuk_batch/launcher_20260917_060518_15106.log` (06:04~06:05 사이 3회 재시도 중 마지막)

- **기준 위반**: 명시적 절대원칙 위반은 아니다(장외 시간대라 매매 자격에 직접 영향은 없었음). 다만 "오늘 거래할 자격이 되는가"를 묻는 장전 점검의 목적상, 자동 복구 체계의 신뢰성 결함으로 등재한다.
- **원인**: 자동 재로그인 스크립트가 "CYBOS Starter" 로그인 창을 열려 했으나, 이미 실행 중이던 CPSTART.EXE(구 증권사 프로그램 프로세스)의 "이미 실행중입니다 — 강제 종료하시겠습니까?" 확인 대화상자가 먼저 뜨면서 로그인 폼 인식에 실패했다. 왜 CPSTART.EXE가 이미 떠 있었는지(정상 종료 안 됨 vs 이전 프로세스 잔존)는 미확인.
- **영향**: 새벽에 증권사 프로그램 접속이 끊겨 프로그램이 약 29분간 정상 작동하지 못했다. 장 시작 3시간 15분 전이라 실제 거래·데이터 수집에는 지장이 없었다.
- **영향(기술)**: `connect_broker()` 경로가 예외를 던지며 프로세스를 종료시키는 설계 자체는 의도된 장애조치(fail-fast)로 보이나, 자동재시작(`AUTO-RESTART`)의 첫 재로그인 실패 후 "재시작 중단"으로 완전히 멈춘 뒤 어떤 외부 트리거(스케줄 재시도로 추정 — 미확인)가 06:04부터 런처를 재호출했는지 확인되지 않았다. 근본 원인(CPSTART.EXE 잔존 원인)도 미확인.
- **신규 여부**: 신규(같은 시그니처의 과거 사례를 `dev_memory/DECISION_LOG.md`·`NEXT_TODO.md`에서 찾지 못함 — `CPSTART`, `이미 실행중`, `재로그인 실패` 키워드 모두 0건).

#### 1-2. 오전 6시~9시 사이 프로세스 5회 재기동, 그중 3회는 확률보정(calibration) 적합 중 access violation 크래시 — 마지막 1건은 08:55 매크로/레짐 판정 창 한복판에서 발생

- **증상**: 오늘 05:39~08:55 사이 프로세스가 총 5번(05:39, 06:16, 07:02, 07:25, 08:55) 새로 시작됐다. 이 중 3번의 전환(06:16→07:02, 07:02→07:25, 07:25→08:55)은 자동재시작 로그에 "일시적 크래시"로만 기록되고 별도 예외 로그가 없었으나, `logs/crash_fault.log`(Python `faulthandler`가 남긴 전체 스레드 스냅샷)를 대조한 결과 매번 같은 자리에서 `Windows fatal exception: access violation`이 발생했다. 발생 지점은 전부 프로그램 시작(또는 장전 재보정) 시 여러 호라이즌(1m~30m)의 확률 보정기를 한꺼번에 준비하는 `_preload_horizon_calibration()` 함수 안, sklearn 로지스틱회귀 적합(`calibration.py:fit()`)이 병렬 처리(joblib)로 넘어가는 지점이었다.
  마지막 사례(08:55:11)는 하필 그날의 매크로 수집·시장상황(레짐) 판정이 진행되던 시각과 정확히 겹쳤다. 이 판정은 09:00 개장 전 반드시 끝나야 하는 하루 한 번뿐인 절차다. 크래시 직전 마지막 로그가 "매크로 seed fetch 완료 — 레짐 확정은 08:58 2단계로 연기"였고, 크래시 28초 뒤(08:55:41) 새 프로세스가 시작해 이 절차를 처음부터 다시 밟아 08:58:14에 "레짐 확정: NEUTRAL"로 간신히 제시간에 마쳤다.
- **근거**:
  ```
  [AUTO-RESTART] main.py 가 46분 실행됨 -- 일시적 크래시, 카운터 초기화.  (06:16→07:02 전환)
  [AUTO-RESTART] main.py 가 23분 실행됨 -- 일시적 크래시, 카운터 초기화.  (07:02→07:25 전환)
  [AUTO-RESTART] main.py 가 90분 실행됨 -- 일시적 크래시, 카운터 초기화.  (07:25→08:55 전환)
  ```
  `logs/Mireuk_batch/launcher_20260917_061535_17120.log` (라인 415, 858, 1466)

  ```
  Thread 0x000065b4 (most recent call first):
    File "...\sklearn\utils\extmath.py", line 162 in safe_sparse_dot
    File "...\sklearn\linear_model\_base.py", line 409 in decision_function
    File "...\sklearn\linear_model\_base.py", line 442 in _predict_proba_lr
    File "...\sklearn\linear_model\_logistic.py", line 1670 in predict_proba
    File "learning/calibration.py", line 177 in _measure_span
    File "learning/calibration.py", line 219 in _evaluate_degeneracy
    File "learning/calibration.py", line 267 in fit
    File "learning/calibration.py", line 158 in record
    File "learning/calibration.py", line 520 in record
    File "main.py", line 4267 in _preload_horizon_calibration
    File "main.py", line 771 in __init__
    File "main.py", line 20296 in main
  Windows fatal exception: access violation
  ```
  `logs/crash_fault.log` 라인 18705 부근(타임스탬프 08:55:11 — AUTO-RESTART가 "90분 실행됨"으로 기록한 크래시와 시각 일치)

  같은 함수 경로(`_preload_horizon_calibration` → `calibration.py:fit()` → sklearn `_logistic_regression_path`/joblib)의 access violation이 `logs/crash_fault.log` 라인 6442(06:16 무렵), 9984(07:01~07:02 무렵)에도 동일하게 반복됨.

  ```
  2026-09-17 08:55:08 [INFO] SYSTEM: [System] 매크로 seed fetch 완료 — 레짐 확정은 08:58 2단계로 연기
  2026-09-17 08:55:13 [INFO] SYSTEM: [TickUI] alive ticks=2510 code=A056A close=1066.14
  [크래시 — 90분 실행됨]
  2026-09-17 08:56:40 [INFO] SYSTEM: [FaultHandler] 활성화 | ... PID=20064  (새 프로세스 시작, 08:55:41)
  2026-09-17 08:58:14 [INFO] SYSTEM: [Regime] NEUTRAL (점수=0) | ... 레짐 확정
  ```
  `logs/20260917_SYSTEM.log`

- **기준 위반**: 명시적 절대원칙 조항은 없으나, CLAUDE.md 매분 실행 파이프라인의 "08:55 매크로 수집 → 레짐 판정" 앵커 절차의 안정성이 위협받았다는 점에서 계측·운영 신뢰성 문제로 등재.
- **영향**: 오늘 새벽부터 개장 직전까지 프로그램이 여러 번 저절로 꺼졌다 켜졌다. 자동으로 복구됐기 때문에 실제 피해는 없었지만, 하필 시장 상황을 판단하는 오전 8시 55분 그 순간에 한 번 더 꺼지면서 아슬아슬하게(28초 뒤 재시작, 3분 뒤 판정 완료) 개장 전 판단을 마쳤다. 조금만 더 지연됐다면 개장 준비에 실제 차질이 생겼을 수 있다.
- **영향(기술)**: sklearn `LogisticRegression.fit()`이 joblib 병렬 백엔드로 넘어가는 지점에서 반복적으로 `access violation`(Windows 예외 코드 0xC0000005)이 발생한다. CLAUDE.md 537차가 문서화한 "numpy/BLAS 호출에서 프로세스가 즉사"(예외 코드 `0xC06D007F`, DLL delay-load 실패) 현상과 증상이 표면적으로 비슷해 보이나 **예외 코드가 다르므로 같은 원인으로 단정하지 말 것**. 537차 가드(`ensure_conda_dll_path()`)가 이 경로에도 적용돼 있는지, 아니면 joblib이 py37_32(32bit) 환경에서 여는 병렬 워커 프로세스/스레드와 sklearn·BLAS 스레드가 충돌하는 별개의 문제인지는 장후 조사가 필요하다.
- **신규 여부**: 신규. `dev_memory/DECISION_LOG.md`·`NEXT_TODO.md`에서 `access violation` 키워드는 여러 건 있으나, 전부 다른 코드 경로다 — B90(33차, Cybos COM/TR timeout 경로)과 304차 후속(daily_close()의 Qt 위젯 직접조작)뿐이며, `_preload_horizon_calibration`·`_evaluate_degeneracy`·`_measure_span` 조합의 크래시는 이력에 없다.
- **참고**: 05:39→05:45 전환과 06:16 기동 자체는 1-1(브로커 접속 끊김)이 원인이라 이 항목과 별개다. 이 항목은 **1-1과 무관하게 그 자체로 3회 반복된 crash 패턴**이다.

### P2 — 운영 부담·기술부채

#### 1-3. 브로커 접속 끊김 로그 문구가 실제 브로커명과 다르게 "키움"으로 표시됨

- **증상**: 05:45:18 접속 끊김 시 `[ERROR] SYSTEM: [System] 키움 연결 끊김 — 재연결 시도`로 기록됐으나, 이 PC는 Cybos Plus(대신증권)를 쓰고 있고 같은 순간 알림 로그는 정확히 `[Notify] cybos 연결 끊김`으로 표시했다.
- **근거**: `main.py:15383` — `logger.error("[System] 키움 연결 끊김 — 재연결 시도")`가 브로커 종류와 무관하게 하드코딩돼 있다. 반면 바로 다음 줄의 `notify_connection_lost(getattr(self.broker, "name", "브로커"))`는 실제 브로커명을 정확히 가져온다.
- **기준 위반**: 없음(기능 오류 아님). 계측 정확성 문제.
- **영향**: 실제 매매나 접속 자체에는 영향이 없다. 다만 이 로그 한 줄만 보고 조사하면 "왜 Cybos 시스템에 키움 관련 오류가 뜨나"로 혼동할 수 있다.
- **영향(기술)**: Kiwoom 단일 브로커 시절 하드코딩된 문자열이 멀티브로커 지원 이후에도 갱신되지 않은 것으로 추정.
- **신규 여부**: 신규.

### 확인 필요 (확정 아님)

- **미커밋 변경 657건의 "실질 변경" 계산 실패**: 수집기가 "실질 변경 미측정(git diff 실패)"로 표시했다. `CLAUDE.md` 1개 파일만 직접 대조한 결과 1,301줄 삭제 + 1,301줄 추가로 줄 수가 완전히 같아, 내용이 아니라 줄바꿈 문자(CRLF/LF) 차이로 보인다. 이는 SKILL.md가 이미 경고한 "리눅스 샌드박스에서의 git status가 CRLF 차이로 수백 건을 변경으로 표시한다" 현상과 일치한다. **다만 657건 전체가 전부 그런지는 확인하지 못했다** — `git diff --numstat` 자체가 실패해 자동 판별이 안 됐다. 실제 코드 변경이 섞여 있는지는 Windows 환경에서 직접 `git status`로 재확인이 필요하다(313차 원칙 — 표본·판정 도구 부족 시 확정 결론 보류).

### 이미 반영된 사안 (신규 아님)

- **`[출처축] 미분류 entry_source ['PHANTOM_STATE_ARTIFACT']`** (오늘 10건, 05:39:48~08:56:40): 554차가 이미 이 라벨을 "자동"이 아니라 "미측정"으로 집계하도록 처리했다(`dev_memory/DECISION_LOG.md` 1293행 부근). 오늘 로그는 그 설계가 정상 작동 중임을 보여주는 WARNING이지 새 결함이 아니다.
- **`[ConfFloorGuard] 자동진입 하한 도달 불가`** (09:00:00, 1건 — 보정기 출력상한 0.3479 < 필요 0.4210): 2026-09-03부터 반복 관측된 패턴으로 `dev_memory/NEXT_TODO.md`에 O-p1(장전 등록, 이월)로 이미 등록돼 있다. 오늘 아침 09:00 시점에는 이 상태였으나, 09:00 이후 해소 여부는 장중 절이 이어서 관측한다. 새 이상점으로 올리지 않는다.
  > ↩️ 12:40 판정 완료 — 장중 절 「이월 처리표」 O-p1 참조(하루 종일 고정이 아니라 경계선 왕복이었고, 그 왕복이 이상점 1-4·1-6과 직결된다).
- **`[SessionStateDrop] 완료 마커 소실 ['p8_last_success_date', 'eod_retrain_ok_date']`** (오늘 2건, 05:39:48): F-1(538-4)로 이미 등록된 사안이며 오늘로 8거래일 연속 재현이다. `dev_memory/NEXT_TODO.md`에 "F-1 (538-4) 승인 재요청 (P1, 7거래일 연속 재현)"으로 이미 올라가 있고 사용자 결정 대기 중이다. 새 항목으로 만들지 않고 지속 관찰만 이어간다. (참고: `data/session_state.json`에서 오늘 두 마커가 실제로 빠져 있음을 직접 확인함 — 재현 사실 자체는 확정.)

---

## 2. Fix 작업 구현계획

> 장전 점검이므로 **아래는 계획만 세우고 적용하지 않는다.** 라이브 프로세스가 개장 후 돌고 있다. 적용은 15:10 강제청산 이후(장후)로 권고한다.

### F-1. CYBOS 자동 재로그인 — 중복 CPSTART.EXE 프로세스 선제 종료 — P1 · 대응 이상점 1-1

- **원인 가설**: 이전 세션(전날 야간 종료 또는 이전 재시작)이 남긴 CPSTART.EXE 프로세스가 완전히 종료되지 않은 채 남아 있다가, 자동 재로그인 스크립트가 새 로그인 창을 열려 할 때 "이미 실행 중" 확인 대화상자로 앞을 가로막았다.
- **변경 파일**:
  - CYBOS 자동로그인 스크립트(`cybos_autologin_diag.log`를 생성하는 스크립트 — 파일 경로는 장후 세션에서 `grep -rn "ncStarter.exe" .`로 특정 필요) — 로그인 시도 전에 기존 CPSTART.EXE 프로세스를 먼저 확인하고, 남아있으면 "예(&Y)" 버튼으로 강제 종료 후 재시도하는 분기 추가.
- **회귀 위험**: 정상적으로 이미 로그인된 CPSTART.EXE까지 잘못 종료시키면 오히려 새 장애를 만든다 — 반드시 "로그인 창의 확인 대화상자로 뜬 경우"에만 종료하도록 조건을 좁혀야 한다.
- **절대원칙 저촉 여부**: 없음.
- **런타임 주의**: 이 스크립트는 Windows 네이티브 자동화(창 핸들 조작)이며 py37_32/py310_64 구분과 무관.
- **검증 방법**: 의도적으로 CPSTART.EXE를 하나 더 띄워둔 상태에서 재로그인 스크립트를 수동 실행해, 새 분기가 정상 종료 후 로그인에 성공하는지 확인.
- **섀도 계측 필요 여부**: 불필요(UI 자동화 수정, 게이트 아님).
- **적용 시점**: 장후.
- **결정 필요 사항**: 없음.

### F-2. `_preload_horizon_calibration()` access violation 원인 조사 — P1 · 대응 이상점 1-2

- **원인 가설**: sklearn `LogisticRegression.fit()`이 joblib 병렬 백엔드로 넘어갈 때 py37_32(32bit) 환경에서 스레드/BLAS 충돌이 발생하는 것으로 추정되나 미확정. 537차의 DLL delay-load 문제(`0xC06D007F`)와는 다른 예외 코드(`access violation`)이므로 같은 가드로 해결되지 않을 가능성이 높다.
- **변경 파일**: (조사 우선, 코드 변경은 원인 확정 후)
  - `learning/calibration.py:327 fit()` — joblib 병렬 백엔드(`n_jobs`) 설정 확인. `n_jobs=1`로 강제해 병렬 처리를 끄고 재현되는지 시험.
  - `main.py:4267 _preload_horizon_calibration()` — 여러 호라이즌을 순차 대신 동시에 준비하는지 확인, 동시성이 원인이면 순차화.
- **회귀 위험**: `n_jobs=1`로 바꾸면 기동 시 보정기 적재 속도가 느려질 수 있음(체감 지연 측정 필요).
- **절대원칙 저촉 여부**: 없음.
- **런타임 주의**: py37_32 32bit 환경 — CLAUDE.md 537차 경고("conda env를 활성화하지 않고 python.exe를 직접 부르지 말 것")와 `utils/dll_bootstrap.py:ensure_conda_dll_path()`가 이 진입점(`main.py` 최상단)에도 적용돼 있는지 우선 확인.
- **검증 방법**: `python scripts/audit_dll_bootstrap.py --fail-on-gap`로 이 진입점이 가드 대상에 포함되는지 확인. 포함돼 있는데도 재현되면 537차와 무관한 별개 원인이므로 joblib/n_jobs 실험으로 좁힌다.
- **섀도 계측 필요 여부**: 불필요(버그 수정).
- **적용 시점**: 장후 — 조사 우선, 코드 변경은 원인 확정 후 별도 세션.
- **결정 필요 사항**: 오늘 안에 재현 안 되면 원인 확정까지 시간이 걸릴 수 있음 — 우선순위를 P1으로 유지할지 사용자 확인 필요(3회 반복 + 08:55 창 근접 사례 때문에 P1 유지 권고).

### F-3. `main.py:15383` 로그 문구 브로커명 정정 — P2 · 대응 이상점 1-3

- **원인 가설**: 멀티브로커 지원 이전 하드코딩 잔재.
- **변경 파일**:
  - `main.py:15383` — `logger.error("[System] 키움 연결 끊김 — 재연결 시도")` → `logger.error("[System] %s 연결 끊김 — 재연결 시도", getattr(self.broker, "name", "브로커"))`
- **회귀 위험**: 없음(로그 문자열만 변경).
- **절대원칙 저촉 여부**: 없음.
- **런타임 주의**: 없음.
- **검증 방법**: Cybos·(가능하면) Kiwoom 양쪽에서 접속 끊김을 재현해 로그가 실제 브로커명을 따라가는지 확인.
- **섀도 계측 필요 여부**: 불필요.
- **적용 시점**: 장후.
- **결정 필요 사항**: 없음.

### 적용 순서와 커밋 계획

1. F-3 (문구 정정, 위험 최소) — 메시지 초안 `[MW0601] {차수}차: 접속끊김 로그 문구를 실제 브로커명으로 정정` (차수는 적용 시점 최신 번호 — 오늘 기준 597차가 이미 다른 건에 쓰였으므로 598차 이상)
2. F-1 (CPSTART 중복 프로세스 처리) — 사용자 확인 후 적용
3. F-2 (access violation 조사) — 조사 결과에 따라 별도 세션에서 후속 커밋

> 본 계획은 수립만 하고 적용하지 않는다. 15:10 강제청산 이후 적용을 권고한다.

---

## 3. 고도화 방안

### G-1. `access violation` 발생 시 프로세스 종료 대신 해당 호라이즌만 건너뛰는 방어 로직

- **관측 근거**: 오늘 `_preload_horizon_calibration()` 안의 access violation이 3회 반복돼 전체 프로세스를 매번 통째로 재시작시켰다. 그중 1회는 08:55 매크로/레짐 판정 창과 겹쳐 개장 준비에 여유 시간을 크게 깎아먹었다.
- **제안 내용**: `_preload_horizon_calibration()`을 호라이즌별로 개별 `try/except`로 감싸, 특정 호라이즌의 보정기 적재가 실패해도 나머지 호라이즌은 정상 진행하고 프로세스 전체는 살아남게 한다. 단, `access violation`은 Python 예외가 아니라 OS 신호이므로 일반 `try/except`로 못 잡는다는 점(CLAUDE.md 537차와 동일한 제약)에 유의 — 근본 해결(F-2)이 우선이며 이 항목은 F-2로 원인을 못 잡았을 때의 완화책이다.
- **기대 효과**: 이 크래시 패턴이 재발해도 전체 재시작 대신 국지적 실패로 그쳐, 08:55 같은 임계 시간대에 걸리더라도 나머지 파이프라인은 계속 돈다.
- **비용·위험**: OS 신호 자체는 프로세스를 통째로 죽이므로 이 완화책의 실효성이 제한적일 수 있다 — F-2 원인 규명이 실패할 경우의 차선책으로만 검토.
- **선행 조건**: F-2 조사 결과.
- **우선순위 제안**: F-2 결과가 "빠른 해결 불가"로 나올 경우에만 이번 주 내.

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| 08:55 매크로/레짐 판정 절차의 크래시 내성 | 문서화 없음 | "주기적 재검증 항목"에 "08:55 앵커 안정성" 관측 항목 신설 검토 | 오늘 이 창에서 크래시가 실제로 발생했고, 회복이 28초 만에 됐기에 망정이지 더 늦었으면 개장 준비에 실질적 지장이 있었을 사례 |

---

## 4. 진입 승패 사후검증 (장후 한정)

해당 없음(장전 전용 — 오늘 진입 0건).

## 5. 수익률 향상방안 (장후 한정)

해당 없음(장전 전용).

---

## 6. 다음 거래일 관측 예정

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| F-2 후속 | `access violation` + `_preload_horizon_calibration` 재현 여부 | 재현 시 F-2 조사 착수 우선순위 상향, 미재현 3거래일 연속 시 관찰 격하 | 다음 장전 |
| F-1 후속 | `CPSTART.EXE 이미 실행중` 자동로그인 실패 재현 여부 | 재현 시 F-1 적용 우선순위 상향 | 다음 장전 |

## 7. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| CB② **발동 1회 관측** (값 복원은 2026-09-02 완료, `=3`) | 상시 관측 | — | 오늘 진입 0건으로 관측 기회 없음. 인위적 발동 시도 금지(CLAUDE.md 명시) |
| TOX-SEVERE-SPREAD 26주 재검증 | 26주 WFA 주기(상시) | — | 표본 적립 속도 느림(약 7.1개월 스케일) — 오늘 별도 조치 없음 |

---

## 7-B. 사용자 조치 필요 — `.git/index.lock` 재발

이 점검 세션 도중(정확한 생성 시각 미상, 09:02경으로 추정 — `collect_evidence.py` 실행
전후) `.git/index.lock`(0바이트)이 다시 생성됐다. `python scripts/git_lock_guard.py --check`
결과 **STALE 확정**(0바이트·0.2시간·git 프로세스 0개, exit=2). `--reclaim` 시도 →
`Operation not permitted`로 회수 실패 — 09-16 장중·장후 절과 정확히 같은 유형(리눅스
샌드박스 마운트 경유 환경의 알려진 제약, SKILL.md 명시).

**사용자가 Windows에서 직접 `C:\Users\82108\PycharmProjects\futures\.git\index.lock`을
지워야 한다.** 지우기 전까지 오늘 이 세션이 만든 문서(이 리포트·증거·DECISION_LOG·
NEXT_TODO 갱신분)가 전부 미커밋 상태로 남는다. 걸리는 시간은 파일 하나 삭제(수 초),
안 하면 다음 세션(장중)도 커밋을 못 하고 같은 문제를 다시 보고하게 된다.

## 8. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-09-17 (MW0601 장전 점검 — 예약작업)` — 파일 맨 끝에 append(기존 09-16 장전/장중/장후 점검 항목과 같은 위치·형식)
- `NEXT_TODO.md` 추가 체크박스: F-1, F-2, F-3, G-1 (총 4건)
- 완료 처리한 기존 항목: 없음(전부 신규 또는 지속 관찰)

---

**이 절이 발급한 번호**: 이상점 1-1~1-3 · Fix F-1~F-3 · 고도화 G-1

---

# 미륵이 일일 점검 — MW0601 / 2026-09-17 / 장중

- 점검 시각: 12:33 KST · PC `MW0601` · 브랜치 `v9-dev`
- HEAD `2cd4acb` · 미커밋 659건(대부분 CRLF/LF 차이로 추정 — 장전 절 "확인 필요" 참조, 미확정) · 당일 커밋 9건(전부 어제 밤~오늘 새벽 작업분, 이 점검과 무관)
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260917_intra_1228.md`

> ⚠ **병행 세션 알림** — 12:27:40경 이 세션과 거의 같은 시각에 **다른 세션(윈도우 환경, 실제 호스트명 `DeskTop-MW0601`)이 같은 장중 점검용 증거 수집을 별도로 실행**했다(`evidence_MW0601-20260917_intra.md`, 접미사 없음, 12:28:38 생성 — 이 세션이 만든 `evidence_MW0601-20260917_intra_1228.md`와는 별개 파일). 그 세션이 만든 것으로 추정되는 `.git/index.lock`(0바이트, 12:28 생성)이 이 절 작성 시각(12:33)까지 계속 남아 있고, `git_lock_guard.py --check`가 반복적으로 **판정보류(나이 600초 미만 — 실행 중인 git일 수 있음)**를 반환해 손대지 않았다. 이 리포트 파일 본문은 이 세션이 작성을 시작하는 시점(12:33)까지 다른 세션이 건드린 흔적이 없었다(마지막 수정 09:15 그대로) — 다만 **이 절을 저장한 직후 그 세션이 별도로 같은 파일에 장중 절을 또 붙일 가능성**이 있다. 만약 그런 흔적(같은 날짜에 「장중」 절이 두 번, 또는 이상점 번호 중복)이 발견되면 다음 세션(장후)이 SKILL.md 규약대로 **번호 축이 아니라 캠페인 키/내용으로 대조**해 처리할 것.

## 0i-C. 이월 처리 — 장전 항목 처분

| 원 번호 | 제목(줄임) | 원 국면 | 처분 | 근거 |
|---|---|---|---|---|
| 1-1 | CYBOS 재로그인 1차 실패(29분 복구) | 장전 | ✅ 해소 | 05:45:18 감지 → 06:14:50 로그인 성공으로 이미 장전에 복구 완료. 장중(09:00~12:33) 재발 없음 — `grep "cybos_autologin_diag" logs/` 09:00 이후 신규 항목 없음. F-1(재로그인 스크립트 방어 로직)은 미적용 상태로 지속(적용은 장후) |
| 1-2 | `_preload_horizon_calibration` access violation 3회 반복(08:55 창 근접) | 장전 | ✅ 해소(오늘 몫 관측 완료) | `logs/Mireuk_batch/launcher_20260917_061535_17120.log`의 AUTO-RESTART 전환 3건이 전부 08:56:40 이전이며, 그 이후(09:00~12:33) 추가 재기동·`access violation` 신규 발생 없음(`logs/crash_fault.log`에 08:55:11 시점 묶음 이후 새 크래시 없음, 프로세스는 08:56:41부터 12:33까지 3시간 37분 연속 생존). F-2(원인 조사)·G-1(완화책)은 미착수 상태로 지속(적용은 장후) |
| 1-3 | 접속 끊김 로그 문구 "키움"으로 오기(실제 Cybos) | 장전 | 🔄 지속 | 오늘 추가 접속 끊김이 없어 새 발생 사례는 없음. F-3(문구 정정) 적용 대기 그대로 |
| 확인 필요: 미커밋 657건 CRLF 추정 | — | 장전 | 🔄 지속 | 이 세션에서 재확인하지 않음(다음 세션은 Windows 환경 직접 확인 권고, 장전 절 기록 유지) |
| 이미 반영: `[출처축] PHANTOM_STATE_ARTIFACT` 10건 | — | 장전 | ✅ 해소(원인 소멸) | 전부 08:56:40 이전 재시작 사이클에서만 발생 — 재시작이 멈춘 09:00 이후 신규 발생 0건(`grep "PHANTOM_STATE_ARTIFACT" logs/20260917_WARN.log` 전량이 05:39~08:56 구간) |
| 이미 반영: `[SessionStateDrop]` 완료 마커 소실(F-1=538-4, 8거래일째) | — | 장전 | 🔄 지속 | 기동 시 1회성 마커라 장중에 상태 변화 없음. 사용자 승인 대기 그대로 |
| O-p1 | `[ConfFloorGuard]` 자동진입 하한 도달 불가(09:00 시점) | 장전 | 🔄 지속 → 판정: 장중 내내 간헐 반복 | 09:00 미달 → 10:49 일시 복구 → 10:55 재미달 → 11:07 재복구 → 11:26 재미달(최종 관측). 다만 09:05 이후로는 **EKS(아래 1-4)가 더 상위에서 이미 자동진입 자체를 막고 있어** ConfFloorGuard 상태와 무관하게 진입 0건 — 실질적으로 이중 차단 상태 |

**미처분 0건.**

## 0i. 지금 상황 한눈에

**여전히 조건부 정상 — 다만 오늘 오전 9시 5분에 켜진 안전장치 하나가 지금 이 시각(낮 12시 33분)까지도 자동매매를 완전히 멈춰 놓고 있고, 이 상태는 오늘 장이 끝날 때까지(오후 3시 10분) 계속될 가능성이 높습니다.**

이 안전장치는 "확신이 부족하면 아예 거래하지 않는다"는 취지로 미리 설계된 것이라 고장이 아닙니다. 오늘 오전 9시 5분, 프로그램이 판단하는 신뢰도가 계속 기준(41%)에 못 미치자 스스로 매매를 멈췄습니다. 이후 30분 간격으로 다섯 차례(9시 21분, 9시 53분, 10시 25분, 10시 57분, 11시 29분) 자체적으로 "이제 다시 켜도 될까"를 확인했지만 매번 기준을 충족하지 못했고, 오전 11시 30분부터는 설계상 더 이상 스스로 확인하지 않도록 되어 있어서 그대로 멈춰 있는 상태입니다. 오늘까지 **사흘 연속**(9월 15일·16일·17일) 같은 패턴이 반복되고 있습니다.

다행히 이 때문에 손실이 난 것은 아닙니다 — 애초에 거래 자체가 없었기 때문에 오늘 손익은 0원이고 포지션도 없습니다. 다만 사흘째 반복되는 패턴이라 "왜 신뢰도가 계속 기준을 못 넘기는가"에 대한 조사와, 최소한 "오늘은 자동으로 재개되지 않는다"는 사실을 화면에 표시해주는 기능(이미 제안되어 사용자 승인을 기다리고 있음)의 처리가 필요해 보입니다.

이것 말고는 오전 프로그램의 새벽 재시작 문제(장전 절 참고)가 개장 전에 이미 다 정리됐고, 개장 이후 지금까지는 시세 수신·예측 계산·안전장치 계산 등 나머지 부분은 전부 정상적으로 돌아가고 있습니다. 진입이 아예 없다 보니 청산·손절·강제청산 같은 절차도 오늘은 아직 해당되는 것이 없습니다.

**사용자가 지금 당장 할 일은 없습니다.** 다만 사흘 연속 패턴에 대해 아래 "사용자 조치"를 참고해 주세요.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 조건부 정상 | (장전 절 참조) |
| 장중 | 조건부 정상 | 안전장치(EKS) 09:05 발동 후 미해제로 진입 0건 지속. 설계대로 동작, 위반 없음. 나머지 파이프라인(시세·예측·재학습 판단·CB③-P4 섀도) 전부 정상 |
| 장후 | (미실행) | |

**절대원칙 준수 여부** (진입 0건 — 대부분 "해당없음")

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 | 해당없음(진행 전) | 12:33 현재 포지션 없음 |
| ② | Circuit Breaker 5종 | 준수(미발동, 정상) | ①②④ 미발동. ③은 차단 비활성 상태에서 섀도 감시만(아래 참조). ⑤는 경고 1건(09:40, 파이프라인 2996ms — 5000ms 하드 임계 미달로 미발동) |
| ③ | CORE 피처 호라이즌 그룹별 | 해당없음 | 진입체크 통과 0건이라 실집행 검증 대상 없음 |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 위반 흔적 없음 | `0xC0000409`/`STACK_BUFFER` 0건 |
| ⑤ | GetRepeatCnt/GetCommData | 확인 안 함 | Cybos 브로커는 해당 없음(Kiwoom 전용) |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 위반 없음 | 관련 활동 없음 |

**설정 불변식** — 수집기 §3 표 전 항목 `일치`. 이상 없음.

---

## 1i. 이상점 정리보고

### P0 — 없음

### P1 — 없음(신규)

### P2 — 운영 참고

#### 1-4. Early Kill Switch(신호 신뢰도 저하 시 자동매매를 일시 정지하는 안전장치) 09:05 발동 후 11:30 마감까지 회복 실패 — **사흘 연속**(09-15·09-16·09-17) 종일 자동매매 정지

- **증상**: 09:05:00 conf_max(신뢰도 상한)=34.4%가 발동선(당시 유효 최소신뢰도 mc=42.1%에서 여유폭 2.0%p를 뺀 40.1%)에 못 미쳐 EKS 발동. 이후 09:20부터 30분 간격으로 5회 자동 회복 시도(09:21·09:53·10:25·10:57·11:29)를 했으나 매번 "최근 10봉 중 신뢰도가 기준을 넘는 봉이 3개 이상"이라는 회복 조건을 채우지 못해 실패(conf_hits 0~2/10). 설계상(`safety/system_health.py:39 EKS_RECOVERY_DEADLINE=11:30`) 재시도는 11:30 마감까지만 이루어지며 이후 새 시도 로그가 없다. 12:23:00(이 세션이 확인한 최종 시점)까지도 `[차단] SHS-EKS 당일 관망 활성` 지속 중이며, 15:40 일일 마감 전까지 자동 재개 가능성은 사실상 없다.
- **근거**:
  ```
  2026-09-17 09:05:00 [WARNING] SYSTEM: [SHS-EKS] Early Kill Switch 발동 conf_max=34.4% < 발동선=40.1%(mc=42.1%-margin2.0%p) core_pass=0/5봉(측정 0봉) → 일시 관망 (09:20부터 30분 간격 자동 회복 평가, 마감 11:30)
  2026-09-17 09:05:00 [CRITICAL] SYSTEM: [SHS-EKS] Early Kill Switch 발동 — 일시 관망 conf_max=34.4% bars=5 → 스케일러·conf 회복 시 자동 재개 (09:20부터 30분 간격 평가)
  2026-09-17 09:21:00 [WARNING] SYSTEM: [SHS-EKS] EKS 미해제 (시도 #1) — 미충족: conf_hits=0/10(필요3,임계38%)
  2026-09-17 09:53:01 [WARNING] SYSTEM: [SHS-EKS] EKS 미해제 (시도 #2) — 미충족: conf_hits=0/10(필요3,임계38%)
  2026-09-17 10:25:01 [WARNING] SYSTEM: [SHS-EKS] EKS 미해제 (시도 #3) — 미충족: conf_hits=0/10(필요3,임계38%)
  2026-09-17 10:57:00 [WARNING] SYSTEM: [SHS-EKS] EKS 미해제 (시도 #4) — 미충족: conf_hits=1/10(필요3,임계37%)
  2026-09-17 11:29:00 [WARNING] SYSTEM: [SHS-EKS] EKS 미해제 (시도 #5) — 미충족: conf_hits=2/10(필요3,임계37%)
  2026-09-17 12:23:00 [INFO] SIGNAL: [차단] SHS-EKS 당일 관망 활성 — conf34%미달
  ```
  `logs/20260917_WARN.log`(발동·5회 시도) · `logs/20260917_SYSTEM.log`(회복 평가 시작 로그) · `logs/20260917_SIGNAL.log`(EKS 활성 매분 로그, 09:20~12:23 사이 반복)
- **기준 위반**: 위반 아님 — `safety/system_health.py`에 명시된 설계된 안전장치의 정상 동작(주석: "마감: 11:30 이후 재평가 없음 — 거래 시간 부족"). 다만 `dev_memory/NEXT_TODO.md`에 이미 등록된 **G-2(2026-09-15 등록, 승인 대기)** "EKS가 11:30 마감을 넘겨 미해제 확정되면 대시보드에 '오늘 자동 재개 없음 — 수동 진입만 가능' 배지를 상시 표시"가 오늘로 **사흘째** 그대로 적용 가능한 상황이다.
- **영향(평문)**: 위 §0i에 기술한 그대로 — 손실은 없으나 사흘 연속 하루 종일 자동매매가 정지되는 패턴이 이어지고 있어, 왜 신뢰도가 계속 기준을 못 넘기는지에 대한 조사 필요성이 커지고 있다.
- **영향(기술)**: 하위 파이프라인(체크리스트·사이저·MetaGate·HurstGate·CB③-P4 섀도)은 전부 정상 동작 중이며 병목은 EKS 단일 지점이다. EKS의 conf_max가 계속 낮게 나오는 배경으로 `logs/20260917_LEARNING.log`에 `[Calibration] 축퇴 감지`(보정기 출력이 거의 상수에 가까움, span<0.02·auc<0.53) 경고가 오늘 하루 동안 매우 높은 빈도로 반복 관측됐다(05:39부터 09:20까지 표본 다수, 이후에도 09:00~12:24 사이 `ScalerFloor`·`Model` 태그로 z-score 이상·스케일 하한 강제 경고가 계속됨) — EKS conf_max 미달과 보정기 축퇴 경고가 시간대상 겹치는 것으로 관측되나, **인과관계는 이 세션에서 확정하지 않는다**(313차 원칙 — 상관 관측일 뿐 회귀분석 미실시).
- **신규 여부**: 오늘 발생 자체는 이번 세션에서 처음 기록(신규 번호 부여, 09-16 리포트의 동일 유형 항목 1-4를 계승하는 성격). 근본 패턴은 09-15 최초 발견 이후 **사흘째 동일**이며, 이미 G-2로 등록·승인 대기 중인 사안의 반복이다(함정① 준수 — 새 Fix를 제안하지 않고 기존 G-2 승인 필요성만 재차 강조).

#### 1-5. 09:40:07 메인 스레드 7,734ms 정지 — CB⑤(파이프라인 지연 감시) 사각 구간 4,738ms(61%)

- **증상**: 오늘 관측된 메인 스레드 정지 3건(5초 초과) 중 유일하게 개장 이후(장중)에 발생한 사례. 같은 분의 파이프라인 자체 경과시간(`PipePerf total`)은 2,996ms로 CB⑤ 하드 임계(5,000ms) 미만이었지만, 실제 메인 스레드 전체 정지시간은 7,734ms였다 — 차이 4,738ms(61%)는 CB⑤도 FZ-1 워치독(180초 임계)도 보지 않는 사각지대다.
- **근거**:
  ```
  2026-09-17 09:40:07 [WARNING] SYSTEM: [MainStallTrace] 스택 스냅샷 기록 (2/20) → logs/mainstall_traceback_20260917.log
  2026-09-17 09:40:03 [WARNING] SYSTEM: [CB⑤] 파이프라인 2996ms 경고 (기준 1000ms)
  2026-09-17 09:40:03 [WARNING] HEALTH: [Health] level=WARNING degraded=OFF | latency=2996ms | quality=0.94 | cache_age=63s | exceptions_10m=1 | exc_tags=[LEVELS 09:30]×1
  ```
  `logs/20260917_WARN.log` · `logs/20260917_HEALTH.log`
- **기준 위반**: 위반 아님. CB⑤ 미발동은 결함이 아니다(파이프라인 경과시간과 메인 스레드 전체 정지시간은 단위가 다르다 — 482차 F-3 정정 사항). 다만 이 사각지대 자체는 이미 482차 F-3 섀도 계측(`MAIN_THREAD_STALL_*`)으로 관찰 중인 대상이며 F-5(09-16 등록, "EKS 발동 시각 메인 스레드 23,578ms 정지" 조사)와 같은 계열의 반복 관측이다.
- **영향(평문)**: 순간적으로 화면 갱신이 0.15초 이상 밀렸을 수 있으나 매매·안전장치 판단에 실질적 영향은 없었다. 다만 이런 정지가 CB⑤ 계측이 못 보는 구간에서 반복된다는 사실 자체가 계속 쌓이고 있다.
- **영향(기술)**: `exc_tags=[LEVELS 09:30]×1` — 09:30 무렵 "LEVELS"(옵션/가격 레벨 관련으로 추정) 컴포넌트에서 예외가 1건 있었고, 이 09:40 정지와 시간대가 근접하나 **직접적 인과는 이 세션에서 확인하지 않았다**(추가 조사는 장후 또는 F-5로 이관).
- **신규 여부**: 오늘 시각(09:40)은 신규이나, 패턴 자체(CB⑤ 사각 구간 반복 관측)는 09-16 1-5(F-5)와 동일 계열 — 새 Fix를 제안하지 않고 F-5 조사에 참고자료로 추가한다.

### 확인 필요 (확정 아님)

- 1-5의 "LEVELS 09:30" 예외 태그와 09:40 메인 스레드 정지의 인과관계 — 표본 1건뿐이라 313차 원칙에 따라 확정 결론 보류. 다음 유사 사례 발생 시 함께 대조할 것.

### 이미 반영된 사안 (신규 아님)

- **CB③-P4 acc30m 단계 전환**(NORMAL↔RESTRICTED↔WATCH 오늘 6회, 10:24~12:20): 차단은 `CB3_P4_GRADE_BLOCK_ENABLED=False`로 비활성 상태이며 섀도 감시만 정상 동작 중(절대원칙 §2 한시예외와 일치). 새 이상점 아님.
- **GBM 배치 재학습 0회**(장중 실행 기준): `[PreRetrain]`이 08:55·08:57 두 차례 모두 "1영업일 전 EOD 재학습 성공"을 근거로 정상 스킵했고, 이후 `[DriftRetrain]`·`[ConstOut]` 후속 트리거도 발동하지 않았다. CLAUDE.md 483차 정정에 따라 **0회가 정상**이며 이상점 아님.

---

## 2i. Fix 작업 구현계획

새 Fix 없음. 장전 F-1~F-3, G-1은 미적용 상태로 지속(적용 시점은 장후 그대로). 위 1-4·1-5는 기존 G-2·F-5(09-16 등록)의 반복 관측이라 새 Fix를 내지 않는다(함정① 준수).

## 3i. 고도화 방안

새 항목 없음. 기존 **G-2(2026-09-15 등록, 승인 대기)**의 승인 필요성이 사흘 연속 반복으로 더 뚜렷해졌다는 점만 아래 "사용자 조치"에 재기재한다.

## 4i. 진입 승패 사후검증 (장후 한정)

해당 없음(장중 전용 — 오늘 진입 0건, EKS로 전면 차단).

## 5i. 수익률 향상방안 (장후 한정)

해당 없음(장중 전용).

## 6i. 다음 거래일 관측 예정 (장전 절 표에 아래 추가)

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| O-i1 | `[SHS-EKS]` 내일도 09:05 전후 재발동 여부, 나흘 연속이면 G-2 승인 긴급도 재상향 | 나흘 연속 시 P1 격상 검토 | 다음 장전~장중 |
| O-i2 | `[Calibration] 축퇴 감지` 빈도 추이 — 오늘 수준(고빈도) 지속 여부 | 3거래일 이상 지속 시 별도 조사 항목 등록 검토 | 다음 장중 |

## 7i. 기한 도래 항목 (장전 절 표와 동일, 변경 없음)

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| CB② 발동 1회 관측 | 상시 관측 | — | 오늘도 진입 0건(EKS 차단)으로 관측 기회 없음 |
| TOX-SEVERE-SPREAD 26주 재검증 | 26주 WFA 주기(상시) | — | 변경 없음 |

---

## 사용자 조치 (장중 시점 갱신)

1. **`.git/index.lock` — 아직 지우지 마세요.** 12:28경 다시 생성된 0바이트 락이 이 절 작성 시각(12:33)까지 남아 있으나, `git_lock_guard.py`가 반복적으로 "판정보류(실행 중인 git일 수 있음)"를 반환하고 있습니다. 병행 세션(다른 Claude 세션 또는 Windows 자동 점검)이 지금 이 순간에도 같은 저장소에서 git 작업을 하고 있을 가능성이 있습니다. **몇 분 더 기다렸다가** 여전히 남아 있으면 그때 지워주세요 — 지금 지우면 실행 중인 작업이 깨질 수 있습니다. 걸리는 시간: 몇 분 대기 후 확인.
2. **(정보 공유, 조치 시급성 낮음)** EKS 안전장치가 사흘째 같은 패턴(09:05 발동 → 11:30까지 5회 회복 시도 전부 실패 → 종일 미해제)을 반복하고 있습니다. 이미 제안되어 있는 "오늘은 자동 재개 안 됨" 화면 표시 기능(G-2)의 승인 여부를 검토해 주시면 좋겠습니다. 급한 일은 아니며, 승인하시면 다음 장후 세션에서 구현 계획을 구체화하겠습니다.

---

**이 절이 발급한 번호**: 이상점 1-4~1-5 · 관측 O-i1~O-i2

---

# 장중(intra) — 관측 구간 09:00~12:31 (작성 2026-09-17 12:40)

## 0. 이 시간까지 한눈에

**이상 — 오늘 오전 내내 단 한 건도 사고팔지 않았습니다. 그리고 오늘 남은 시간에도 자동으로는 거래가 재개되지 않습니다.**

프로그램이 멈춘 것은 아닙니다. 시세도 정상적으로 받고 있고, 예측도 매분 돌고 있으며, 오전 11시 8분~14분 사이에는 "지금 들어가도 좋다"는 최고 등급(A) 판단까지 세 번 나왔고 몇 계약을 살지도 계산됐습니다. 그런데도 주문이 나가지 않았습니다.

이유는 **아침 9시 5분에 "오늘은 쉬자"는 안전장치 하나가 켜졌기 때문**입니다. 이 장치는 개장 직후 프로그램의 판단이 너무 흐릿하면 그날 자동매매를 멈추는 기능입니다. 켜진 뒤에는 30분마다 다섯 번 "이제 괜찮아졌나" 다시 확인하는데, 오늘은 다섯 번 다 실패했습니다. 그리고 이 재확인은 **오전 11시 30분까지만** 하도록 정해져 있어서, 지금은 다시 확인할 기회 자체가 없습니다. 오늘 자동 거래는 끝났습니다.

**여기까지는 어제·그저께와 같습니다(사흘 연속). 오늘 새로 밝혀낸 것은 "왜 하필 사흘 전부터 이러느냐"입니다.**

조사해 보니 이 안전장치가 참고하는 "개장 직후 판단의 또렷함" 점수는 사흘 내내 **34.4%로 소수점까지 똑같았습니다.** 시장이 사흘 연속 똑같을 리는 없습니다. 실제로는 **점수를 만드는 계산기 자체가 아침에는 34.8%보다 높은 값을 낼 수 없는 상태**였고, 안전장치가 요구하는 합격선은 40% 근처였습니다. 즉 **아침에는 무슨 일이 있어도 합격이 불가능**했습니다. 이것은 9월 1일부터 하루도 빠짐없이 똑같았습니다.

그러면 왜 9월 14일까지는 멈추지 않았을까요. **그 시각 프로그램이 느렸기 때문입니다.** 개장 직후 첫 계산이 1초를 넘기면 "이 점수는 못 믿겠다"며 아예 빼고 세는 규칙이 있는데, 9월 8일~14일에는 이 계산이 매번 1.1~1.8초가 걸려서 점수가 통째로 제외됐고, 그래서 안전장치가 켜질 일이 없었습니다. 그런데 9월 15일부터 이 계산이 0.85~0.95초로 **빨라지면서** 점수가 셈에 들어갔고, 그 점수는 어차피 합격이 불가능한 값이었으므로 안전장치가 곧바로 켜졌습니다.

**정리하면, 프로그램이 빨라져서 거래가 멈췄습니다.** 1초라는 경계선 하나가 우연히 방패 노릇을 하고 있었던 것이고, 오늘 기록은 경계에서 불과 0.07초 차이였습니다. 시장을 보고 내린 판단이 아니라 **초시계 눈금 하나가 그날 거래 여부를 정하고 있었습니다.**

한 가지 더 아쉬운 점이 있습니다. 오전 10시 49분과 11시 7분에 계산기가 잠깐 정상으로 돌아왔고, 마지막 재확인(11시 28분)에서는 합격 조건 3개 중 **2개까지 채웠습니다.** 1개만 더 채웠으면 오늘 거래가 재개됐을 것입니다. 그런데 2분 뒤 11시 30분에 재확인 기한이 끝나버렸습니다.

**사용자가 지금 당장 할 일**: 없습니다. 오늘 손실은 0원이고(거래가 없으니), 잘못된 주문이 나갈 위험도 없습니다. 다만 **장 마감 후 판단이 필요한 사안 2건**을 §7 사용자 조치에 적어 뒀습니다. 코드는 장중이라 손대지 않았습니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 조건부 정상 | 재시작 5회 — 전부 개장 전 복구 완료 |
| **장중** | **이상** | **진입 0건 · 3거래일 연속. 원인은 시장 판단이 아니라 계측 결함(1-4). 자동 재개 기회는 11:30에 소멸** |
| 장후 | (미실행) | |

**절대원칙 준수 여부** (12:31 기준)

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | 해당없음 | 포지션 0 — 청산 대상 없음 |
| ② | Circuit Breaker 5종 | 미발동(정상) | CB①~⑤ 발동 0건. 진입 0건이라 CB②는 관측 기회 없음 |
| ③ | CORE 피처 호라이즌 그룹별 | 해당없음 | 진입체크 통과 0건. 단 GAP_OPEN 구간 CORE **측정 0봉**이 3일 연속(→ 1-5) |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 위반 흔적 없음 | 장중 `0xC0000409`/`STACK_BUFFER` 0건 |
| ⑤ | GetRepeatCnt/GetCommData | 해당없음 | Cybos 브로커는 해당 TR 체계 미사용 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 위반 없음 | 관련 활동 없음 |

**용어 한 번 풀기** — 본문에서 다음 약어를 씁니다.
`EKS`(Early Kill Switch, 개장 직후 판단이 흐리면 그날 자동매매를 멈추는 안전장치) ·
`conf`(모델이 낸 방향 예측의 확신도) · `min_conf`/`mc`(그날 자동진입에 필요한 확신도 하한, DynMC가 매일 산출) ·
`ConfFloorGuard`(확신도 보정기의 출력상한이 min_conf에 못 미치는지 감시하는 계측) ·
`GAP_OPEN`(09:00~09:05 개장 직후 구간) ·
`계측 4원칙`(CLAUDE.md 코드 규약 — ② 미측정≠0, ③ 탈락 가시화, ④ 폴백 가시화, ⑤ 대사는 모든 축).

---

## 이월 처리표 — 장전 항목 처분

| 원 번호 | 항목 | 처분 | 근거 |
|---|---|---|---|
| 1-1 | CYBOS 접속 끊김 · 자동 재로그인 1차 실패 | ✅ **해소** | 09:00 이후 `키움 연결 끊김`·`CPSTART.EXE 이미 실행중`·재로그인 로그 **0건**. 장중 재현 없음 |
| 1-2 | 재기동 5회 · calibration 적합 중 access violation | ✅ **해소** | 09:00 이후 `access violation`·프로세스 재기동 **0건**. 장중은 단일 프로세스 연속 생존 |
| 1-3 | 브로커 접속 끊김 로그 문구가 "키움"으로 오표기 | 🔄 **지속** | 문구 사안. 05:45 로그 그대로이며 장중 추가 발생 없음. F-3 계획 유효 |
| O-p1 | ConfFloorGuard 도달불가 상태의 장중 해소 여부 | ✅ **판정 완료** | 09:00 도달불가 → **10:49 복구** → 10:55 재이탈 → **11:07 복구** → 11:26 재이탈. 하루 종일 고정이 아니라 **경계선 부근 왕복**. 단 이 왕복이 EKS 해제 실패와 직결됨(→ 1-4·1-6) |
| F-2 후속 | `access violation` + `_preload_horizon_calibration` 재현 여부 | 🔄 **지속(판정 보류)** | 장중 미재현. 판정 예정일이 원래 "다음 장전"이므로 그때 닫는다 |
| F-1 후속 | `CPSTART.EXE 이미 실행중` 재현 여부 | 🔄 **지속(판정 보류)** | 장중 미재현. 판정 예정일 "다음 장전" 유지 |

**미처분 0건.**

---

## 1. 이상점 정리보고 (장중)

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

없음.

> 1-4는 **영업 영향이 가장 크지만(3거래일 연속 거래 전면 정지) P1로 둔다.** 실패
> 방향이 보수적(관망)이라 손실·오주문 위험이 없기 때문이다. 심각도를 부풀리지 않되
> §7 사용자 조치에서는 최우선으로 올린다.

### P1 — 정확성·관측 훼손

#### 1-4. EKS 발동 여부가 시장이 아니라 **개장 첫 봉의 1초 경주**로 정해지고 있다 — 3거래일 연속 거래 전면 정지의 진짜 원인 🆕

**증상**

2026-09-15·16·17 사흘 연속 09:05에 EKS가 발동해 당일 자동진입이 전면 차단됐다.
세 날의 발동 로그가 `conf_max=34.4%`로 **소수점까지 동일**하다.

```
2026-09-15 09:05:00 [SHS-EKS] Early Kill Switch 발동 conf_max=34.4% < 발동선=39.9%(mc=41.9%-margin2.0%p) core_pass=0/5봉(측정 0봉)
2026-09-16 09:05:02 [SHS-EKS] Early Kill Switch 발동 conf_max=34.4% < 발동선=41.1%(mc=43.1%-margin2.0%p) core_pass=0/5봉(측정 0봉)
2026-09-17 09:05:00 [SHS-EKS] Early Kill Switch 발동 conf_max=34.4% < 발동선=40.1%(mc=42.1%-margin2.0%p) core_pass=0/5봉(측정 0봉)
```

**근거 — 3단 인과, 전부 로그·코드로 확인**

**① `conf_max`는 시장 관측이 아니라 보정기 출력상한이다(상시, 최소 2026-09-01부터).**

```
2026-09-17 09:00:00 [ConfFloorGuard] 자동진입 하한 도달 불가 — 보정기 출력상한 0.3479 < 필요 0.4210
                                     (conf_floor=0.330, min_conf=0.421, span=0.0063, auc=0.550).
                                     이 상태에서는 어떤 신호도 자동진입 하한을 넘을 수 없다.
```

09:00 시점 `출력상한 0.3479 · span 0.0063 · auc 0.550`이 **검사한 모든 거래일에 동일**하다
(09-01·02·03·04·07·09·10·14·15·16·17 실측 — `min_conf`만 0.418~0.431로 날마다 다르다).
즉 EKS가 비교하는 `conf_max`(≈0.344)는 **보정기가 기동 직후 낼 수 있는 최댓값**이지
그날 시장의 또렷함이 아니다. 발동선은 `mc − 0.02 ≈ 0.40`이므로
**09:00 봉이 셈에 들어가는 한 EKS 발동은 항등적으로 참**이다.

⚠ **이 축퇴 자체는 신규 발견이 아니며 갈림점도 아니다** — 진입 47건(09-01)·65건(09-04)
났던 날에도 09:00 값은 똑같이 `0.3479/0.0063/0.550`이었다. **축퇴는 상수다.**

**② 갈림점은 오직 `_gap_pipe_delayed`의 1000ms 경계다.**

`main.py:9046` — GAP_OPEN 봉 기록 직전:

```python
_gap_pipe_delayed = (time.perf_counter() - _pipe_t0) * 1000 >= 1000
```

`safety/system_health.py:record_gap_open_bar()`는 `pipeline_delayed=True`이면
`conf_max` 산입에서 **그 봉을 통째로 제외**한다
(`if pipeline_delayed: … elif horizon_policy_blocked: …` — 지연이 우선순위를 가진다).
그리고 `HORIZON_TIME_POLICY[(900,905)] = []`이므로 **09:01~09:04 네 봉은 매일 정책차단**이다
(`config/settings.py:1179`). 남는 것은 **09:00 한 봉뿐**이고, 그 봉마저 제외되면
`conf_max=0.0` → `_is_cold_start=True` → **EKS 미발동**이 된다.

일자별 09:00 봉 실측(`[PipePerf] total`. `_gap_pipe_delayed` 측정점은 S7·S8 이전이므로
실제 비교값은 `total − (S7+S8)` ≈ 괄호값):

| 날짜 | 09:00 `PipePerf total` | 비교값 ≈ | 1000ms | EKS 판정 로그 | 당일 진입 |
|---|---|---|---|---|---|
| 09-08 | 1,094ms | ~1,075 | 초과 | `판정 유예 — GAP_OPEN 봉 부족(0봉)` | 7 |
| 09-09 | 1,397ms | ~1,330 | 초과 | `미발동 — cold-start (conf_max=0% delayed=1 policy_blocked=4/5봉)` | 9 |
| 09-10 | 1,753ms | ~1,703 | 초과 | `미발동 — cold-start (delayed=1 policy_blocked=4)` | 0 |
| 09-11 | 1,810ms | ~1,783 | 초과 | `미발동 — cold-start (delayed=2 policy_blocked=3)` | 0 |
| 09-14 | 1,164ms | ~1,144 | 초과 | `미발동 — cold-start (delayed=1 policy_blocked=4)` | 4 |
| **09-15** | **847ms** | **~772** | **미만** | **발동** | **0** |
| **09-16** | **898ms** | **~831** | **미만** | **발동** | **0** |
| **09-17** | **951ms** | **~928** | **미만** | **발동** | **0** |

**완전 분리다.** 미발동 5일은 전부 1,075ms 이상, 발동 3일은 전부 928ms 이하.
`HORIZON_TIME_POLICY`는 2026-07-29 이후 무변경이고
(`git --no-optional-locks log -S HORIZON_TIME_POLICY` 실측 커밋 3건, 최신 `82f78cf`),
09-14~09-15 사이 커밋은 **전부 대시보드 표시 전용**이다. 즉 정책·임계가 바뀐 것이
아니라 **파이프라인이 빨라진 것**이 유일한 변화다.

⚠ 09-10·09-11은 미발동인데도 진입 0건이다 — **EKS 미발동이 진입을 보장하지는 않는다.**
"EKS만 풀면 거래가 돌아온다"로 읽지 말 것.

**③ 그 결과 안전장치가 "시장이 흐리다"가 아니라 "오늘 09:00 계산이 빨랐다"를 재고 있다.**

오늘 비교값 ~928ms는 경계에서 **72ms** 떨어져 있다. 개장 버스트 지연의 자연 산포
안쪽이며, 앞으로도 날마다 동전던지기처럼 갈린다.

**기준 위반**

- **계측 4원칙 ②(미측정 ≠ 0)** — 제외된 봉을 `conf_max=0.0`으로 환원하고, 그 0.0을
  `_is_cold_start` 판정의 **양성 근거**로 쓴다. "재지 않았다"가 "정상(cold-start)"이라는
  결론을 만든다. 반대로 재기만 하면 발동이 확정된다.
- **계측 4원칙 ④(폴백 가시화)** — 발동 로그가 `conf_max=34.4%`만 찍고 그 값이
  **보정기 상한에 갇힌 값**임을 남기지 않는다. 같은 분에 `ConfFloorGuard`가
  "어떤 신호도 넘을 수 없다"를 이미 찍고 있는데 EKS는 그것을 모른 채 판정한다 —
  **계측 4원칙 ⑤(대사는 모든 축을 걸어라)** 의 변종이기도 하다.
- 매분 9단계 **STEP 6·7**(앙상블 진입 판단 → 진입 실행) 경로가 STEP 5 이전의 계측
  결함으로 봉쇄됨.

**영향**

오늘·어제·그저께 사흘 동안 자동매매가 한 건도 일어나지 않았습니다. 손해는 나지 않았지만
(거래가 없으니 0원), 프로그램이 "시장이 나빠서 쉰다"가 아니라 **"아침 계산이 0.07초
빨랐기 때문에 쉰다"** 는 상태이며, 앞으로도 날마다 우연히 갈립니다. 실전 전환에 필요한
거래 표본도 그만큼 쌓이지 않습니다.

**영향(기술)**

`safety/system_health.py:evaluate_early_kill_switch()`의 발동 3조건
(`conf_max < mc − margin` ∧ `core_pass_count == 0` ∧ `not _is_cold_start`) 중 앞의 둘은
**기동 직후 상시 참**이고(①·1-5), 세 번째만 변동한다. 그 세 번째의 유일한 입력이
`_gap_pipe_delayed`이며, 이는 실행 환경 성능에 종속된 비결정 값이다. 따라서 EKS의
일일 판정은 **관측 대상(시장)과 독립**이다. 273차 히스테리시스(`EKS_TRIGGER_MARGIN=0.02`)는
`mc` 근소 미달을 막으려던 장치인데, 실제 격차는 0.344 vs 0.40으로 **5.6%p**라 마진이
무력하다. 하위 파이프라인(체크리스트·사이저·MetaGate·HurstGate)은 정상이다 —
오늘 11:08~11:14 `[Checklist] 통과 7/11 → 등급 A`가 3회, `[Sizer]` 2~3계약 산출이 5회 나왔다
(그 3건은 EKS와 별개로 `[P4] CVD+OFI 동시 역방향 → 등급 A→C 강등`에도 걸렸다 —
오늘은 두 게이트가 **독립적으로** 같은 분을 막았다).

> ⚠ **함정 ① 확인**: EKS 발동 사실 자체와 대시보드 배지 제안(G-2)은 09-15·09-16 리포트와
> `dev_memory/NEXT_TODO.md`에 **이미 등록돼 있다.** 이 항목이 신규로 주장하는 것은
> **원인(1000ms 경주)과 항등성(보정기 상한 < 발동선)** 두 가지다. 두 선행 리포트는 이를
> *"설계된 안전장치의 정상 동작, 위반 아님"* 으로 판단했다. 이 항목은 그 판단을
> **뒤집지 않고 좁힌다** — 코드는 설계대로 동작한다. 설계가 재고 있다고 믿는 대상이
> 실제 재는 대상과 다르다.

---

#### 1-5. GAP_OPEN CORE **측정 0봉**이 3거래일 연속 — 459차가 사전등록한 재설계 트리거가 도래했다 🆕

**증상**

발동 로그 `core_pass=0/5봉(측정 0봉)`이 09-15·16·17 **3일 연속 동일**하다.
분자(통과 0)와 분모(측정 0)가 같이 0이다.

**근거**

`safety/system_health.py:record_gap_open_bar()` 도크스트링(459차 F2)이 이 상황을 미리
적어두고 **판정 기준까지 사전등록**했다:

> `core_pass=x/y봉(측정 z봉)`에서 z가 지속적으로 0에 가까우면 "CORE 조건이 사실상 상시
> 참"이라는 뜻이므로, 그때 표본을 근거로 발동 조건 재설계를 결정할 것(NEXT_TODO 459차).

`z = 0`이 3일 연속이므로 "0에 가깝다"가 아니라 **정확히 0**이다. 발동 3조건 중
`core_pass_count == 0`은 **측정하지 않았기 때문에** 항상 참이며, 안전장치를 조이는
방향으로만 작동한다.

**기준 위반** — 계측 4원칙 ②(미측정 ≠ 0). 459차가 분모(`core_measured`)를 계측하도록
고쳤으나 **발동 조건은 의도적으로 그대로 뒀고**(라이브 발동 특성을 근거 없이 바꾸지
않는다는 보수적 선택), 그 유예의 해제 조건이 지금 충족됐다.

**영향**

안전장치가 "핵심 지표 3개가 다 떨어졌다"고 판단해 멈추는 것처럼 보이지만, 실제로는
**그 3개를 한 번도 재지 않았습니다.** 재지 않은 것이 자동으로 "다 떨어졌다"로 처리되고
있습니다.

**영향(기술)**

`main.py:9036` 부근 `_gap_core_checks = _cr.get("checks")`가 GAP_OPEN 구간에서 `None`이라
`_core_votes = 0` → `_core_all_ok = False` → `core_pass_count` 미증가. 동시에
`core_measured=False`로 분모도 안 늘어 `측정 0봉`이 된다. 즉 발동 조건
`core_pass_count == 0`은 **정보를 담고 있지 않은 상수**다. 1-4의 ①과 합치면 발동
3조건 중 **둘이 상수**이고 나머지 하나가 1000ms 난수다.

⚠ **표본 3일이다. 재설계 방향까지 지금 확정하지 말 것**(313차 원칙). 지금 확정된 것은
"사전등록 트리거가 도래했다"는 사실뿐이며, 무엇으로 바꿀지는 표본을 더 봐야 한다.

---

### P2 — 운영 부담·기술부채

#### 1-6. 오늘 EKS 해제가 **조건 1개 차이**로 무산됐고, 2분 뒤 마감이 닫혔다 🆕

**증상 · 근거**

```
09:21 EKS 미해제 (#1) — conf_hits=0/10(필요3,임계38%)
09:53 EKS 미해제 (#2) — conf_hits=0/10(필요3,임계38%)
10:25 EKS 미해제 (#3) — conf_hits=0/10(필요3,임계38%)
10:49 [ConfFloorGuard] 하한 도달 가능 복구 — 출력상한 0.3750 ≥ 필요 0.3710
10:57 EKS 미해제 (#4) — conf_hits=1/10(필요3,임계37%)
11:07 [ConfFloorGuard] 하한 도달 가능 복구 — 출력상한 0.3853 ≥ 필요 0.3710
11:29 EKS 미해제 (#5) — conf_hits=2/10(필요3,임계37%)
        ← EKS_RECOVERY_DEADLINE = 11:30 (safety/system_health.py:57). 이후 재평가 없음
```

`conf_hits`가 **0 → 0 → 0 → 1 → 2로 단조 증가**했고, 보정기가 10:49·11:07에 실제로
회복했다. 필요치는 3이었다. 12:31 현재까지 추가 시도 로그 0건(설계대로).

⚠ **09-16과 구분할 것** — 어제는 `0,0,1,1,0`으로 **비단조**였고 마지막 시도 임계가
46%로 튀었다. 오늘은 **회복 추세가 분명한 상태에서 시계가 먼저 끝났다.** 결과는 같지만
원인이 다르므로 한 사례로 묶지 말 것.

**기준 위반** — 위반 아님(설계된 마감). 재설계 검토 대상.

**영향** 오늘은 회복이 눈앞이었는데 시계가 먼저 끝났습니다. 조금만 더 기다렸으면
오후 거래는 가능했을 수 있습니다.

**영향(기술)** `EKS_RECOVERY_DEADLINE=11:30` · `EKS_RECOVERY_INTERVAL_MIN=30` 조합에서
09:20 첫 시도 기준 **최대 5회**가 상한이다. 마감의 도입 취지(오후 늦은 재개는 청산 창을
압박)는 유효하나, `conf_hits`가 단조 증가 중인 경우를 구분하지 않는다.
⚠ 마감·간격·필요 히트수는 **매매 정책 파라미터**이므로 본 절에서 변경을 제안하지 않는다
(→ §3 G-4, 주간회의 안건).

#### 1-7. 점검 수집기의 「Traceback 출현 — 크래시/메모리 계열」 적신호가 **오탐**이다 🆕

**증상 · 근거**

증거 다이제스트 §11-3이 `logs/20260917_WARN.log: Traceback 출현 3건 — 크래시/메모리 계열`
을 올렸다. 원본 확인 결과 실제 파이썬 트레이스백은 **0건**이고, 매칭된 것은 전부
로그 **파일명 문자열**이다(4건 중 3건이 수집 시점까지 발생):

```
07:30:51 [MainStallTrace] 스택 스냅샷 기록 (1/20) → logs/mainstall_traceback_20260917.log
08:56:40 [MainStallTrace] 스택 스냅샷 기록 (1/20) → logs/mainstall_traceback_20260917.log
09:40:07 [MainStallTrace] 스택 스냅샷 기록 (2/20) → logs/mainstall_traceback_20260917.log
12:30:05 [MainStallTrace] 스택 스냅샷 기록 (3/20) → logs/mainstall_traceback_20260917.log
```

`MainStallTrace`(482차 F-3 섀도 계측)가 산출 파일명에 `traceback`을 포함하면서,
수집기의 크래시 패턴이 자기 계측의 정상 동작을 크래시로 읽는다. **계측이 늘어날수록
오탐이 늘어나는 구조**이며 스냅샷 상한이 20이므로 최대 20건까지 부풀 수 있다.

**기준 위반** — SKILL.md §0 *"§11 자동 적신호는 출발점이지 결론이 아니다"* 가 상정한
그대로의 사례. 계측 4원칙 ③(탈락 가시화)의 역방향 — 잡히지 말아야 할 것이 잡힌다.

**영향** 점검 보고서가 "크래시가 3건 있었다"고 잘못 말할 수 있습니다. 오늘 장중에
실제 크래시는 없었습니다.

**영향(기술)** `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py:131`
크래시 패턴 목록에 `Traceback`이 단순 부분문자열로 들어 있다. `MainStallTrace`는
2026-08-19 480차 이후 상시 동작하므로 **모든 미륵이 점검 리포트가 이 오탐에 노출**된다.

---

### 이미 반영된 사안 (신규 아님)

- **`[ConstOut]` 3m·5m 상수 출력 10건** — 2026-09-14 딥다이브
  (`docs/정기점검/매일점검/MW0601-20260914-3m호라이즌_ConstOut루프-딥다이브.md`)가
  `BiasReset uniform fallback`의 확정적 부산물로 규명 완료. 오늘 10건은 09-14(30건)·
  09-11(30건) 대비 **감소**했고, `[ConstOut] … 재학습 트리거 억제`가 정상 작동해
  루프도 끊겼다. 새 이상점으로 올리지 않는다.
- **`[WeightCollapse]`** — 401차가 상시 43~47% 발생을 계측 완료,
  `BAR_ONLY_RELAX_ENABLED=True`로 완화 배포됨. 오늘 관측도 그 범위 안.
- **`[ConfFloorGuard]` 도달불가** — 장전 O-p1으로 등록됨(이월표에서 판정 완료).
- **EKS 미해제 시 대시보드 배지(G-2)** — 2026-09-15 등록, **사용자 승인 대기 중**.
  오늘로 3일째 적용 가능 상황이 반복됐다(→ §7-2).
- **메인 스레드 5초 초과 정지** — 482차 F-3 `MAIN_THREAD_STALL_*` 섀도 관찰 구간.
  오늘 09:40:07(7,734ms)·12:30:05(5,360ms) 2건이 장중 발생. CB⑤와 단위가 달라
  미발동이 정상이다.
- **ATR 1.0pt 미만 차단 4건**(0.94·0.97·0.97·0.99pt) — CLAUDE.md 26주 WFA 항목
  `ATR_MIN_ENTRY`(539차 등록, 초회 실행 **FAIL**)가 이미 다루는 사안. 9월 ATR 중앙값
  1.03pt·`<1.0pt` 45.6%라는 실측과 정합적이다. 재보정은 26주 주기 절차를 따를 것.
- **`[GradeEVGuard]` (섀도) A등급 최근 30일 n=70 평균순EV=−9,259원** — 섀도 게이트가
  설계대로 관측 중(미적용). 표본·판정 기준이 그 채널에 사전등록돼 있으므로 여기서
  결론내지 않는다.

---

## 2. Fix 작업 구현계획 (장중 — **제안만. 코드 미변경**)

> 🔴 장중이라 코드·커밋·재기동을 하지 않았다. 아래는 장후 적용 후보다.
> **F-4·F-5는 계측 수정이라 매매 정책을 바꾸지 않는다. F-6은 점검 도구 수정이다.**
> **EKS 임계·마감·필요 히트수 변경은 매매 정책이므로 여기 넣지 않는다**(→ §3 G-4).

### F-4. EKS 발동 로그에 「보정기 상한」과 「지연 판정」을 동반 기록 — P1 · 대응 1-4 (계측 전용)

- **변경 대상**: `safety/system_health.py:evaluate_early_kill_switch()`의 발동/미발동
  4개 분기, `main.py:9046` 부근 호출부.
- **변경 내용**:
  ① 호출부에서 그 분의 `ConfFloorGuard` 상태(`출력상한`·`span`·`auc`·`도달가능 여부`)를
  인자로 받아 로그에 병기한다 —
  `→ 발동 (conf_max=34.4% < 발동선=40.1% | 보정기 상한=34.8% ⇒ 구조적 도달불가)`.
  ② `_gap_pipe_delayed` 판정 결과와 그때의 경과 ms를 **발동 분기에서도 항상** 남긴다.
  현재는 발동 분기에서 `delayed`/`policy_blocked` 카운트가 인쇄되지 않아, 발동일에
  왜 제외되지 않았는지를 로그만으로 알 수 없다 — 이번 조사에서 `PipePerf`를
  교차대조해야 했던 이유가 이것이다.
- **회귀 위험**: 낮음. **판정 로직 무변경**, 로그 인자 추가만.
- **검증**: 다음 거래일 09:05 로그에 두 값이 함께 찍히는지(발동·미발동 어느 쪽이든).
- **표본 상태**: 즉시 확인 가능(매 거래일 1회).

### F-5. `_gap_pipe_delayed` 제외 봉을 「미측정」으로 분리 계상 — P1 · 대응 1-4·1-5 (계측 전용)

- **변경 대상**: `safety/system_health.py:record_gap_open_bar()` · `evaluate_early_kill_switch()`.
- **변경 내용**: `conf_max`가 **한 번도 갱신되지 않은 경우**(`_gap_open_conf_max` 초기값
  유지)를 `None`으로 구분해, `_is_cold_start` 판정 근거에 "conf 측정 0건"을 명시한다.
  현재는 `conf_max == 0.0`이라는 **폴백값**이 cold-start의 양성 근거로 쓰인다
  (계측 4원칙 ②·④).
  ⚠ **이번 단계는 계상·로그만 바꾸고 발동 조건식은 건드리지 않는다** — 459차가 같은
  이유로 조건을 그대로 둔 선례를 따른다(안전장치의 라이브 발동 특성을 근거 없이
  바꾸지 않는다).
- **회귀 위험**: 낮음~중간. `_is_cold_start` 계산 경로를 만지므로 **현재 라이브 동작과
  동일함**을 회귀 테스트로 먼저 고정할 것.
- **검증**: `tests/`에 09-08~09-17 8일치 실측 입력(delayed/policy/conf 조합)을 넣어
  판정 결과가 기존과 **100% 일치**하는지 고정. 그 위에 새 계상값만 추가 검증.
- **표본 상태**: 과거 8거래일 로그로 즉시 재구성 가능.

### F-6. 점검 수집기 크래시 패턴 오탐 제거 — P2 · 대응 1-7

- **변경 대상**: `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py:131`.
- **변경 내용**: `Traceback` 매칭에서 파일명 언급을 제외한다. 패턴 자체는 유지하고
  `mainstall_traceback_`을 포함한 줄만 제외 목록에 둔다.
- **회귀 위험**: 낮음. 단 **진짜 트레이스백을 놓치지 않도록** 제외 조건을 파일명
  접두사로 좁게 잡을 것.
- **검증**: 오늘 WARN.log로 재실행해 §11에서 해당 적신호가 사라지고, 인위적으로
  `Traceback (most recent call last):`를 넣은 픽스처에서는 여전히 잡히는지.
- **표본 상태**: 즉시.
- ⚠ **스킬 수정이므로 SKILL.md §실행-9의 3곳 갱신 규약을 따를 것** — 저장소 정본 ·
  이 PC 앱 저장 스킬 · 예약작업 3종. MW0602 몫은 **파일로 건네야 한다**(브랜치 분기).

### 적용 순서와 커밋 계획

1. 장 마감(15:10) 후 **F-4 → F-5 → F-6** 순.
   F-4가 먼저인 이유: 1-4의 진단을 **라이브 로그로 재확인할 수단**을 먼저 만든 뒤
   계상 구조를 만져야, 바꾼 뒤 판정이 달라졌는지 볼 기준선이 생긴다(계측 4원칙 ④의 취지).
2. 커밋 제목 `[MW0601] 599차: …` 형식.
3. F-5는 **회귀 테스트를 먼저 작성**해 현행 동작을 고정한 뒤에 손댈 것.
4. **이 장중 세션은 커밋하지 않는다**(SKILL.md §6 — 점검 예약 실행은 커밋·푸시 금지).

---

## 3. 고도화 방안

### G-2 (기존, 승인 대기 — 3일째 반복). EKS 미해제 확정 시 대시보드 상시 배지

2026-09-15 등록. 오늘로 **3거래일 연속** 적용 가능 상황이 반복됐다.
11:30 마감을 넘기면 "오늘 자동 재개 없음 — 수동 진입만 가능"을 상시 표시한다.
12:31 현재 화면만 봐서는 "왜 아무 일도 없는지" 알 수 없다.

### G-3 (신규). EKS 판정 전에 **판정 가능성 자체를 먼저 묻는다** — 섀도 선행

- **당일 관측 근거**: 1-4 ① — 09:00 보정기 출력상한(0.3479)이 발동선(≈0.40)보다 낮아
  EKS의 비교가 **정보를 담지 못한 채** 참이 된다. 그 사실은 같은 분 `ConfFloorGuard`
  로그에 이미 있는데 EKS가 읽지 않는다.
- **제안**: `evaluate_early_kill_switch()` 진입부에서 "그 분의 보정기 상한 < 발동선"이면
  **판정 불가(UNMEASURABLE)** 로 분류해 섀도 로그만 남긴다. 실제 발동 여부는 **무변경**.
- **기대효과**: 며칠이면 "EKS 발동일 중 몇 %가 판정 불가 상태였는가"가 실측된다.
  현재 추정은 **100%**이며, 그 경우 EKS는 시장 신호를 한 번도 본 적이 없다는 뜻이 된다.
- **회귀 위험**: 없음(섀도). 승격 판단은 표본 확보 후.
- **검증**: `[SHS-EKS-Shadow] verdict=UNMEASURABLE|MEASURABLE` 일별 집계.
- **표본 상태**: **미착수.** 승격 전 최소 10거래일 관측 권장(사전등록 필요).
- ⚠ 게이트 신설은 **섀도를 거친 뒤 승격**한다(SKILL.md §실행-7). 이 항목은 섀도만 제안한다.

### G-4 (신규, **주간회의 안건 — 결정 아님**). EKS 회복 마감·조건 재설계

- **당일 관측 근거**: 1-6 — `conf_hits` 단조 증가(0→0→0→1→2) 중 11:30 마감으로 종료.
- **논점**: ⓐ 마감을 늦출 것인가(청산 창 압박과 상충) ⓑ `conf_hits` 추세가 상승이면
  1회 연장할 것인가 ⓒ 간격 30분을 좁힐 것인가 ⓓ **애초에 1-4가 고쳐지면 발동 자체가
  줄어 이 논점이 사라지는가**.
- ⚠ **매매 정책 변경이므로 본 세션은 제안하지 않는다.** ⓓ가 참이면 ⓐ~ⓒ는 불필요하므로
  **F-4·F-5 적용 후 재관측이 선행**되어야 한다.
- **표본 상태**: 미달(EKS 발동일 3건). 확정 결론 금지(313차 원칙).

### 로드맵·실전전환 기준 반영 제안

- **실전 전환 기준 ⑤(CB② 발동 1회 관측)** — 진입 0건이 3일 연속이라 **관측 기회 자체가
  생기지 않는다.** 1-4가 해소되지 않으면 ⑤는 무기한 열린 채로 남는다. 두 항목이
  독립이 아님을 전환기준 판정 시 명시할 것.
- **실전 전환 기준 ①(4주 통산 수익률 양수)** — 거래 없는 날이 4주 창에 누적되면
  분자·분모가 모두 얇아진다. 판정 시 **거래일 수와 진입 0일 수를 병기**할 것.

---

## 6. 관측 예정 (다음 국면이 판정)

| ID | 관측 대상 | 판정 기준 | 판정 예정 |
|---|---|---|---|
| **O-i1** | 15:10까지 `[SHS-EKS]` 해제 로그 출현 여부 | 설계상 11:30 이후 재평가 없음 → **0건이 정상**. 1건이라도 있으면 설계 이해가 틀린 것 | 오늘 장후 |
| **O-i2** | 15:10까지 진입 0건 유지 여부 | 자동 진입 0건 예상. 수동 진입이 있으면 `[체결동기화] 외부진입`으로 구분 집계 | 오늘 장후 |
| **O-i3** | `[ConfFloorGuard]` 오후 도달가능/불가 왕복 횟수 | 오전 로그 5줄 · 상태 전환 **4회**(불가→가능→불가→가능→불가). 오후 패턴이 오전과 다른지 | 오늘 장후 |
| **O-i4** | 다음 거래일 09:00 `PipePerf total`과 EKS 판정의 대응 | `total − (S7+S8) ≥ 1000ms` → **미발동(cold-start)** / `< 1000ms` → **발동**. 어긋나면 1-4의 인과가 불완전 | 다음 장중 |
| **O-i5** | `MainStallTrace` 스냅샷 누적(12:31 현재 3/20) | 20 도달 시 상한 소진 — 그 뒤 정지가 계측되지 않는다. 도달 여부 | 오늘 장후 |

> ⚠ **O-i4는 1-4의 반증 가능한 예측이다.** 어긋나면 이 절의 진단을 **정정**할 것.
> 두 조건 모두 로그 한 줄로 확인된다.

---

## 7. 사용자 조치

> 지금 당장 거래에 위험한 것은 없습니다. 아래 1·2는 **장 마감 후** 판단이 필요합니다.

### 1) EKS 계측 수정(F-4·F-5) 적용 여부 결정 — 최우선

- **지금 할 일**: 없음(장중). 장 마감 후 "F-4·F-5 구현해"라고 지시.
- **걸리는 시간**: 구현·테스트 약 1~2시간. **매매 정책·임계는 바뀌지 않습니다**
  (로그와 계상만 바뀝니다).
- **안 하면**: 내일도 09:00 계산이 1초보다 빠르면 또 하루 종일 거래가 멈춥니다.
  최근 3일 실측은 **빨라지는 추세**라 재발 가능성이 높습니다. 그리고 왜 멈췄는지
  로그만으로는 계속 알 수 없습니다.

### 2) G-2(대시보드 "오늘 자동 재개 없음" 배지) 승인 여부 결정

- **지금 할 일**: 없음. 승인/보류만 알려 주시면 됩니다.
- **걸리는 시간**: 승인 시 구현 약 30분.
- **안 하면**: 화면만 봐서는 "왜 오늘 아무 거래도 없는지" 알 수 없는 상태가 계속됩니다
  (오늘로 3일째). 2026-09-15 등록 후 3일째 대기 중입니다.

### 3) `.git/index.lock` — 이번에는 **지우지 마십시오**

- **상태**: 12:28에 생성(0바이트). `python scripts/git_lock_guard.py --check` 결과
  **HOLD(판정보류 — 나이 219초 ≤ 임계 600초)**, 종료코드 0.
- **지금 할 일**: 없음. 판정보류는 실행 중인 git일 수 있어 **지우면 인덱스가 깨집니다**
  (SKILL.md 명시). 장후 세션이 재판정합니다.
- ⚠ 장전 절 §7-B는 STALE 확정이라 삭제를 요청했고, 그 뒤 09:51 커밋(`2cd4acb`)이
  성공했으므로 **그 건은 해소됐다.** 지금 것은 이 세션 수집기가 만든 **새 락**이다.

---

## 8. dev_memory 반영

- `DECISION_LOG.md`: `## 2026-09-17 (MW0601 장중 점검 — 세션)` 항목 append
  (증상→원인→결정→Why→How to apply→검증).
- `NEXT_TODO.md`: F-4·F-5·F-6·G-3·G-4 체크박스 추가, O-i1~O-i5 관측 등록.
  459차 F2 항목에 **"트리거 도래(2026-09-17, 측정 0봉 3일 연속)"** 를 덧붙일 것.
- **커밋하지 않는다**(SKILL.md §6 — 점검 예약 실행은 커밋·푸시 금지).

---

**이 절이 발급한 번호**: 이상점 1-4~1-7 · Fix F-4~F-6 · 고도화 G-3~G-4 · 관측 O-i1~O-i5

---

## 0i-D. 병행 세션 충돌 확인 — 12:47 후속 (같은 국면이 이 파일에 두 번 append됨)

이 문단은 위 두 개의 "장중" 절이 **서로 다른 세션이 거의 동시에 작성해 충돌한 결과**임을
기록하기 위해 12:47경 덧붙인다(SKILL.md 「병행 세션 확인」·「같은 국면을 하루에 두 번 돌면
번호가 아니라 내용으로 대조」 규약에 따름). **본문을 고치거나 지우지 않았다.**

- **무엇이 있었나**: 이 세션(리눅스 샌드박스 경유, 12:33 작성, 이상점 1-4~1-5 발급)이 먼저
  저장했고, 거의 같은 시각 별도 세션(윈도우 네이티브 환경, 실제 호스트명 `DeskTop-MW0601`,
  12:40~12:47경 작성, 이상점 1-4~1-7 발급)이 뒤이어 같은 파일에 자신의 "장중" 절을 붙였다.
  두 절이 **이상점 번호 1-4·1-5, 관측 O-i1·O-i2를 서로 다른 내용에 중복 발급**했다 —
  2026-08-26에 기록된 것과 같은 유형의 번호 축 충돌이다.
- **두 절의 관계**: 뒤에 붙은 절(윈도우 세션, "관측 구간 09:00~12:31 (작성 12:40)")이
  **이 세션의 1-4보다 훨씬 더 깊은 원인 분석**을 담고 있다 — EKS(자동매매 안전정지 장치)
  발동의 근본 원인을 "시장 판단"이 아니라 "개장 직후 파이프라인 처리시간이 1초 경계를
  넘는지에 따라 conf_max(신뢰도 상한) 값이 계상에 포함되는지 여부가 갈리고, 포함되면
  그 값이 3거래일간 소수점까지 34.4%로 고정되어 있어 애초에 합격이 불가능했다"는 계측
  설계 결함으로 규명했다(그 절 1-4·1-5·1-6 참조). 이 세션의 1-4는 "설계된 안전장치의
  정상 동작(09-15·09-16과 동일 패턴)"이라는 표면적 판정에 머물렀다 — **틀린 판정은
  아니지만, 뒤 절이 그 이면의 진짜 원인을 밝혀 판정을 사실상 대체한다.**
- **권고**: 장후 세션은 이 날의 EKS 관련 최종 판정·Fix 채택 시 **이 세션의 1-4/1-5가 아니라
  윈도우 세션의 1-4~1-7(F-4~F-6, G-3~G-4)을 근거로 삼을 것.** 이 세션의 1-4/1-5는 표면
  관측으로서는 사실이나 원인 설명이 불완전하므로, 당일 이상점 통합 대장에는 **윈도우
  세션 쪽 번호·내용을 채택**하고 이 세션의 것은 "같은 사안의 선행 관측(원인 미규명 버전)"
  으로만 병기할 것을 제안한다.
- **git 상태**: `.git/index.lock`은 이 문단 작성 시각에도 여전히 존재 — 두 세션이 순차적으로
  파일을 저장한 것은 파일시스템 쓰기이지 git 커밋이 아니므로 이 파일 자체의 append 충돌과
  index.lock의 존재는 별개 현상이다(둘 다 병행 세션 활동의 증거이긴 하다). 손대지 않았다.
- **커밋 대기**: 이 문단을 포함한 리포트 전체 — 코드 변경 없음.

---

## 제2부-B. 구현 결과 — F-4·F-5·G-2 (사용자 승인, 13:0x~13:5x)

> **사용자 승인**: "9/17 장중 F-4·F-5 구현해" · "G-2 승인" (2026-09-17 장중).
> **장중 실행이지만 라이브에 영향 없음** — 파이썬은 이미 메모리에 올린 모듈을 쓰므로
> 디스크의 `.py` 수정은 **재기동 전까지 동작을 바꾸지 않는다.** 대상 3파일
> (`main.py`·`safety/system_health.py`·`dashboard/main_dashboard.py`)은 모두 기동 시
> import 완료 상태임을 확인했다. **재기동·커밋은 하지 않았다.** 실반영은 장 마감 후
> 정규 재기동 시점이다.

### 바뀐 것 / 안 바뀐 것

| | 내용 |
|---|---|
| **바뀐 것** | 로그 내용 · `to_dict()` 키 3개 추가 · 대시보드 배지 문구 |
| **안 바뀐 것** | **EKS 발동/해제 판정** · 발동 임계 · 회복 마감 11:30 · 필요 히트수 3 · `_is_cold_start` 계산식 · 진입/차단 경로 |

변경 규모: `safety/system_health.py` +136 / `main.py` +48 / `dashboard/main_dashboard.py` +25
(총 199줄 추가 · 10줄 삭제). 신규 테스트 `tests/test_599_eks_gap_open_instrumentation.py` **38건 전부 통과**.

### F-4 — 판정 로그에 「무엇을 보고 그렇게 판정했는지」를 남긴다

1. **`main.py` GAP_OPEN 블록** — 봉별 `[SHS-EKS-Bar]` 1줄 신설.
   종전에는 `_gap_pipe_delayed`를 계산해 넘기고 **버려서**, 어느 봉이 왜 `conf_max`에서
   빠졌는지가 어디에도 안 남았다(계측 4원칙 ③ 탈락 가시화).
   `try/except`로 감싸 계측이 파이프라인을 깨뜨리지 못하게 했다.
2. **`main.py` 판정 호출부** — `self.ensemble.conf_floor_state` ·
   `ensemble_calibrator.output_max` · `.rank_auc`를 넘긴다(`getattr` 방어 + `try/except`).
   새로 배선할 것은 없었다 — `conf_floor_state`는 `main.py:8156`이 이미 쓰던 속성이다.
3. **`safety/system_health.py` 판정 4분기 전부** — 종전에는 **발동 분기가
   `delayed`/`policy_blocked` 카운트를 안 찍었다.** 그래서 이날 조사가 `[PipePerf]`
   8일치를 교차대조해야 1000ms 경계를 찾을 수 있었다. 4분기에 공통 꼬리를 붙였다.
4. **`to_dict()`에 `gap_open_delayed` 추가** — 노출된 적이 없어 대시보드·알림이 제외
   사유 두 갈래 중 한쪽(`policy_blocked`)만 볼 수 있었다.
5. **발동 CRITICAL 알림**에도 `(delayed=· policy=· conf측정=)` 병기.

**실제 렌더링 결과**(테스트로 실측):

```
[2026-09-17 재현 — 발동]
[SHS-EKS] Early Kill Switch 발동 conf_max=34.4% < 발동선=40.1%(mc=42.1%-margin2.0%p)
  core_pass=0/5봉(측정 0봉) | bars=5 (delayed=0 policy=4 conf측정=1) core측정=0봉
  | 보정기 상한=34.8% auc=0.550 state=unreachable
    ⇒ 구조적 도달불가(상한<발동선 — 이 판정은 시장이 아니라 보정기를 잰 것)
  → 일시 관망 (09:20부터 30분 간격 자동 회복 평가, 마감 11:30)

[2026-09-14 재현 — cold-start 미발동]
[SHS-EKS] EKS 미발동 — cold-start (conf_max=0% delayed=1 policy_blocked=4 / 5봉) …
  | bars=5 (delayed=1 policy=4 conf측정=0) core측정=0봉 | 보정기 상한=34.8% … ⇒ 구조적 도달불가

[보정기가 정상인 상태에서 발동했다면 — 판별력 확인]
[SHS-EKS] Early Kill Switch 발동 … | 보정기 상한=38.5% auc=0.617 state=reachable ⇒ 도달가능
```

🔵 **세 번째가 이 Fix의 요점이다.** 보정기가 정상일 때 발동하면 `⇒ 도달가능`으로 찍혀
**진짜 시장 신호로 인한 발동과 구분된다.** 지금까지는 둘이 같은 로그였다.
이 한 줄이 이날 수행한 8일치 교차대조를 대체한다.

### F-5 — "재지 않았다"와 "재보니 0이었다"를 분리 계상

- `_gap_open_conf_measured_count` 신설 — `conf_max`에 **실제로 반영된** 봉만 센다.
  `__init__` · `record_gap_open_bar()` · `reset_daily()` · `to_dict()` 4곳.
- 배경: `_gap_open_conf_max`의 **초기값이 `0.0`** 이라(`system_health.py:75`)
  "한 봉도 안 셌다"와 "셌는데 conf가 0.0"이 같은 값이 된다. 그리고 그 `0.0`이
  `_is_cold_start`의 **양성 근거**로 쓰인다(계측 4원칙 ②).

> 🔴 **`_is_cold_start` 계산식은 손대지 않았다.** `conf_max == 0.0`을
> `not conf_measured`로 바꾸는 것이 "옳은" 수정으로 보이지만 **두 식은 동치가 아니다** —
> "산입됐는데 conf가 정확히 0.0"인 봉에서 갈린다. 그 경우의 라이브 빈도를 모른다.
> **먼저 세고, 표본이 쌓인 뒤에 판정식을 논한다**(459차가 발동 조건을 그대로 둔 것과
> 같은 보수적 순서). `test_cold_start_formula_untouched`가 이 약속을 고정한다 —
> 나중에 바꾸면 그 테스트가 깨지면서 "의도한 변경인가"를 되묻는다.

### G-2 — 대시보드 배지 (승인됨)

- `safety/system_health.py:is_eks_recovery_closed(now=None)` 신설 —
  `_eks_active and now >= EKS_RECOVERY_DEADLINE`. `to_dict()`에 `eks_recovery_closed` 노출.
- `main.py:11154` 호출부가 `recovery_closed=`로 전달.
- `dashboard/main_dashboard.py:update_shs_badge()`에 4번째 상태 추가.

| 시각 | 종전 | 적용 후 |
|---|---|---|
| 11:30 이전 | `⛔ 관망일` / "자동 진입 차단 중" | `⛔ 관망일` / "**11:30까지 30분 간격 자동 회복 평가 중**" |
| **11:30 이후** | `⛔ 관망일` / "자동 재개 대기" ← **거짓** | `⛔ 오늘 종료` / "**오늘 자동 재개 없음 (수동 진입만 가능)**" |

툴팁 범례(`main_dashboard.py:14136`)의 `"EKS : 일시 관망 (자동 재개 대기)"`도 2줄로 분리했다.

> ⚠ **임계를 대시보드에 복제하지 않았다.** 11:30 판정은 `system_health`가 단일 출처이며,
> `test_dashboard_badge_does_not_duplicate_deadline_constant`가 배지 함수 안에
> `EKS_RECOVERY_DEADLINE`·`time(11, 30)`·`datetime.now()`가 **코드로** 나타나지 않는지
> 검사한다(주석은 허용). 461차 `mdd_pct`가 같은 이름으로 두 분모를 가졌던 사고의 예방.
> `test_recovery_closed_agrees_with_can_attempt_recovery`가 배지와 실제 스케줄러가
> 같은 상수를 본다는 불변식을 11:29·11:30·11:31·13:00 네 시점에서 고정한다.

### 검증

**신규 `tests/test_599_eks_gap_open_instrumentation.py` — 38건 통과.** 구성:

| 묶음 | 무엇을 고정하나 |
|---|---|
| 판정 불변 (7+1건) | **실측 7거래일**(09-09~09-17) 입력으로 발동/미발동이 라이브 관측과 100% 일치. 발동 3일·미발동 4일로 양쪽 분기를 덮는다 |
| 픽스처 충실성 (7건) | 픽스처가 cold-start 로그의 `delayed`/`policy` 카운트를 그대로 재현하는지 — 없으면 "내가 지어낸 입력으로 내 코드를 통과시킨 것"이 된다 |
| F-4 인자 무영향 | 보정기 인자를 넘겨도 판정이 같다(로그 전용) |
| F-5 분모 | 제외 봉은 분모에 안 들어감 · **미측정(0) ≠ 측정된 0.0(5)** 구분 · `reset_daily` 누락 방지 |
| **로그 렌더링 (5건)** | 4분기 로그가 실제로 치환되는가. `%` 인자 불일치를 logging이 **조용히 삼키므로** 직접 확인한다 |
| G-2 (6건) | 마감 전/후/EKS 미발동일 · 배지↔스케줄러 상수 일치 · 임계 미복제 |
| 정책 상수 불변 (2건) | `EKS_RECOVERY_DEADLINE`·`INTERVAL`·`MIN_HITS`·`TRIGGER_MARGIN`·`MIN_BARS` 무변경 = G-4가 주간회의 안건임을 코드로 고정 |

🔴 **테스트 작성 중 두 번 잡혔다 — 둘 다 기록해 둔다.**
① 첫 픽스처가 09:00 봉을 `policy_blocked=True`로 놨는데, 그러면 발동 3일이 재현되지
않았다. 실제로는 그 봉만 `policy_blocked=False`다(앙상블이 실제 결정을 내므로) —
**테스트가 제 진단의 오류를 잡아낸 것**이며, 수정 후 8일 전부 일치했다.
② `caplog`가 빈 목록을 돌려줬다 — 이 프로젝트 로거는 루트로 **전파되지 않는다.**
그대로 뒀으면 **항상 통과하는 가짜 테스트**가 될 뻔했다. 직접 핸들러로 바꾸고,
`test_capture_helper_actually_captures`로 "헬퍼가 정말 잡는지"를 먼저 검사한다.

**문법 검사**: `py_compile` 3파일 통과.
**인접 회귀**: `test_482_conf_floor_state` · `test_523_conf_floor_auc_and_state_snapshot` ·
`test_459_shs_core_measured` · `test_537_dll_bootstrap_coverage` **전부 통과**.

---

#### 1-8. 오늘 커밋된 피터 사료 작업이 기존 회귀 가드 2건을 깨뜨려 놨다 🆕 (P1)

**이 절의 구현과 무관한 선행 결함이다 — 검증 중에 발견했다.**

| 테스트 | 실패 내용 | 내 변경과의 관계 |
|---|---|---|
| `test_457_fallback_visibility.py::test_db_fallback_columns_have_measured_flag` | `dashboard/main_dashboard.py -> peter_paste : or '', or '', or 0.0` — DB 컬럼에 폴백값을 쓰면서 폴백 플래그가 없다 | **무관.** 내 diff에 `peter_paste` **0건**, HEAD 판에 이미 **3건** 존재 |
| `test_456_wave1_stats_and_shs.py::test_trend_sql_counts_positions_not_legs` | `KeyError: '2026-08-10'` — `fetch_trend_daily(30)`에 해당 날짜 없음 | **무관.** 30일 창이 밀려 2026-08-10이 빠진 데이터 의존 테스트로 보인다(장중이라 DB 확인 보류) |

**기준 위반** — 앞 건은 **계측 4원칙 ④(폴백 가시화)** 정면 위반이며, `test_457`은 바로
그것을 막으려고 만든 전수 가드다. 즉 **가드는 설계대로 울렸는데 아무도 안 들었다.**
2026-09-13 CLAUDE.md가 기록한 `O-77`(스위트가 죽어 6일간 못 들음)과 같은 계열이다.

**영향** 피터 사료 붙여넣기 기능이 값이 없을 때 빈 문자열·0을 DB에 쓰는데, 그것이
"진짜 값 0"인지 "없어서 0"인지 구분할 수 없게 저장되고 있습니다. 나중에 그 데이터를
집계하면 조용히 틀린 수치가 나옵니다.

**영향(기술)** `dashboard/main_dashboard.py`의 `peter_paste` INSERT 3곳.
`test_457`은 `*_measured`/`*_fallback` 동반 컬럼 추가 또는 `_DB_FALLBACK_PINNED`에
**근거와 함께** 등록할 것을 요구한다(근거 없이 등록하는 것은 규약 위회).

**처분** — 이번 세션 범위 밖이라 **고치지 않았다**(피터 사료 도메인 미확인 · 다른
세션의 작업물). `NEXT_TODO`에 **F-7**로 등록. `test_456` 쪽은 장후 DB 확인 후 판정
(**O-i6**).

---

**이 절이 발급한 번호**: 이상점 1-8 · Fix F-7 · 관측 O-i6

---

## 제2부-C. 병행 세션 번호 대조 (15:45 작성)

> 이 절은 **새 판단을 하지 않는다.** 같은 날 장중 절이 **두 번** 작성되면서 이상점·관측
> 번호가 겹쳤으므로, 어느 번호가 무엇을 가리키는지만 고정한다.
> 앞선 두 절의 본문은 **고치지 않았다**(SKILL.md §5-2 — append 는 append 다).

### 무슨 일이 있었나

| 세션 | 실행 환경 | 증거 파일 | 리포트 절 |
|---|---|---|---|
| **[예약]** 12:27:40 수집 · 12:33 작성 | 리눅스 샌드박스(`claude (override)`) | `evidence_MW0601-20260917_intra_1228.md` | 294행 `# … / 장중` |
| **[대화]** 12:28:38 수집 · 12:40 작성 | 윈도우 실호스트(`DeskTop-MW0601`) | `evidence_MW0601-20260917_intra.md` | 446행 `# 장중(intra)` |

[예약] 절이 **이 충돌을 정확히 예고했다** — *"이 절을 저장한 직후 그 세션이 별도로 같은
파일에 장중 절을 또 붙일 가능성이 있다. … 번호 축이 아니라 캠페인 키/내용으로 대조해
처리할 것."* 그 지시를 여기서 이행한다.

⚠ 부수 사항: [예약] 절이 `# 미륵이 일일 점검 — MW0601 / 2026-09-17 / 장중` 이라는
**최상위 문서 헤더**를 다시 썼다(국면 절 헤더가 아니라). 하루 한 파일 규약상 `#` 헤더는
파일 맨 위 하나여야 한다. **고치지 않고 남긴다** — 의미가 바뀌는 수정이라 append 규약에
걸린다. 다음 세션이 판단할 것.

### 번호 대조표 — **같은 번호가 다른 것을 가리킨다**

| 번호 | [예약] 12:33 | [대화] 12:40 | 관계 |
|---|---|---|---|
| **1-4** | EKS 09:05 발동~11:30 회복 실패, **사흘 연속** — **P2 · 운영 참고** | EKS 발동이 **1초 경주로 정해진다**(원인 규명) — **P1** | **같은 현상 · 다른 깊이.** [예약]은 "설계된 안전장치의 정상 동작"까지, [대화]는 그 판정이 시장과 독립임을 실측. 상충 아님 — **[대화]가 [예약]을 포함한다** |
| **1-5** | 09:40:07 메인 스레드 **7,734ms 정지**(CB⑤ 사각 4,738ms) | GAP_OPEN CORE **측정 0봉** 3일 연속(459차 트리거 도래) | 🔴 **완전히 다른 사안이 같은 번호.** [대화]는 메인 스레드 정지를 「이미 반영된 사안」(482차 F-3 섀도)으로 처리했다 — 판정은 상충하지 않는다 |
| **1-6 · 1-7 · 1-8** | (발급 없음) | EKS 해제 1개 차이 무산 / 수집기 Traceback 오탐 / `peter_paste` 폴백 가드 FAIL | [대화] 단독 |
| **O-i1** | 내일도 재발동하는지 — 나흘 연속이면 P1 격상 | 15:10까지 EKS 해제 로그 0건인지 | 다름 |
| **O-i2** | `[Calibration] 축퇴` 빈도 추이 | 15:10까지 진입 0건 유지 | 다름 |
| **O-i3~O-i6** | (발급 없음) | ConfFloorGuard 오후 전환 / **09:00 PipePerf 예측 검증** / MainStallTrace 20 상한 / test_456 판정 | [대화] 단독 |
| **F-4~F-7 · G-3·G-4** | (발급 없음) | Fix·고도화 | [대화] 단독 |

### 인용 규약 (다음 세션·주간회의용)

🔴 **이 날짜의 `1-4`·`1-5`·`O-i1`·`O-i2`를 번호만으로 인용하지 말 것.**
`1-4[예약]` / `1-4[대화]` 처럼 출처를 붙이거나, 아예 **내용으로** 부를 것
(예: "1초 경주" · "메인 스레드 7,734ms"). 2026-08-26 `O-8` 중복 발급 사고
(498차 F-11)의 **이상점 번호 축 변종**이며, 그때와 달리 이번에는 앞 세션이
경고를 남겨 둬 즉시 대조가 가능했다.

### 판정에 미치는 영향

**없다 — 두 절의 결론은 상충하지 않는다.** 다만 심각도는 다르다:
[예약]이 `P2 · 운영 참고 · P1 없음`으로 닫은 것을 [대화]가 **P1**로 올렸다.
근거는 [예약]이 갖지 못한 실측(보정기 상한 0.3479 불변 · 09:00 `PipePerf` 8일치 분리)이며,
[예약] 절의 판단을 뒤집는 것이 아니라 **그때 없던 근거를 더한 것**이다.
⇒ **오늘의 최종 심각도는 P1**로 읽을 것.

### 이 시점(15:45) 상태

- 장 마감(15:35) 경과 — **이 세션의 장중 관측은 여기서 닫는다.**
- **O-i1·O-i2(대화) 는 아직 미판정**이다. 장후 절이 15:10까지의 `[SHS-EKS]` 해제 로그와
  진입 건수로 닫을 것. 12:31 시점 예측은 "둘 다 0건"이었다.
- `.git/index.lock`(12:28 [대화] 세션 수집기 생성분)이 **STALE 로 확정**됐다
  (0바이트 · 3.3시간 · git 프로세스 0개, exit=2). `--reclaim` 은 이 환경에서
  **권한 거부**로 실행하지 못했다 → **사용자 수동 삭제 필요**(§7-4).

---

**이 절이 발급한 번호**: 없음 (대조 전용)

---

## 제2부-D. 채점기(앙상블 보정기) 딥다이브 — 사용자 지시 (16:00~16:20, 장 마감 후)

> **사용자 질문**: *"아침 점수는 40점을 못 넘더라도 일정시간 이후(예: 09:20 이후) 점수는
> 정상 채점되어 40점 이상이 되는지 점검해."*
> 장 마감(15:35) 이후이므로 **라이브 DB 조회를 수행했다**(CLAUDE.md 장중 금지 조항 비해당).
> 원천: `data/db/predictions.db:ensemble_decisions` · 로그 · 코드.
> ⚠ **코드 변경 없음.** 이 절은 진단이다.

### 0. 한 줄 답

**40점을 넘는 분은 있다. 그러나 그것은 채점기가 정상으로 돌아와서가 아니라, 채점기가
꺼졌기 때문이다.** `conf ≥ 0.40` 인 분의 **91.2%가 「보정 미적용(raw 통과)」** 상태였다.
채점기가 켜져 있는 동안 그 출력은 **하루 종일 0.40을 넘지 못한다.**

### 1. 질문에 대한 직접 답 — 시간대별 실측

2026-09-17, 붕괴행(`weight_collapsed=1`, CLAUDE.md 제외 지시) 제외:

| 시 | n | conf 평균 | conf 최대 | min_conf 평균 | 합격 |
|---|---|---|---|---|---|
| 08 | 1 | 0.3439 | 0.3439 | 0.4210 | 0 |
| 09 | 47 | 0.3103 | 0.3580 | 0.3813 | **0** |
| 10 | 45 | 0.3502 | 0.3630 | 0.3751 | **0** |
| 11 | 46 | 0.3566 | 0.3749 | 0.4197 | 7 |
| 12 | 46 | 0.3558 | 0.3725 | 0.6145 | 0 |
| 13 | 45 | 0.3476 | 0.3627 | 0.3672 | 0 |
| 14 | 44 | 0.3898 | **0.5458** | 0.3885 | **17** |
| 15 | 6 | 0.3429 | 0.3943 | 0.9700 | 0 |

⚠ **11시의 합격 7건은 점수가 오른 것이 아니라 합격선이 내려온 것**이다 —
그 시간대 `conf` 최대는 0.3749인데 `min_conf`가 0.371까지 내려왔다.
점수가 실제로 튄 것은 **14시 한 번(0.5458)** 뿐이며, 그 구간이 곧 보정 미적용 구간이다(§3).

**45거래일 누적 프로파일** (붕괴행 제외, n=9,462):

| 시 | conf 평균 | min_conf 평균 | 합격률 |
|---|---|---|---|
| 09 | 0.3398 | 0.3852 | **27.1%** |
| 10 | 0.3783 | 0.3796 | 37.0% |
| 11 | 0.3837 | 0.4219 | 31.7% |
| 12 | 0.3985 | 0.6169 | 1.5% *(점심 진입금지존 — 설계대로)* |
| 13 | 0.3863 | 0.3795 | **46.9%** |
| 14 | 0.3957 | 0.4080 | **44.2%** |
| 15 | 0.3861 | 0.9761 | 0.0% *(마감 컷오프 — 설계대로)* |

🔴 **EKS 회복 창(09:20~11:30)이 하루 중 가장 나쁜 구간과 정확히 겹친다.**
합격률이 09시 27.1%로 최저이고, 13~14시(46.9%·44.2%)에 정점을 찍는데
**EKS는 11:30에 문을 닫는다.** 일자별 11:30 이전 합격 건수: 09-15 **0건** ·
09-16 17건 · 09-17 7건. 09-15는 **수학적으로 해제가 불가능**했다.

### 2. 🔴 채점기가 **35일째 얼어붙어 있다** (신규·이 딥다이브의 핵심)

기동 시 복원 로그가 **모든 날·모든 재기동에서 완전히 동일**하다:

```
[Calibration] 보정기 복원 완료 (n=888 method=platt fitted=True degenerate=False
              unreachable=False span=0.00630 auc=0.550 out_max=0.3479)
```

- `n=888` 이 **2026-08-13 이후 단 한 번도 변하지 않았다.**
- 아티팩트 `data/ensemble_calibrator.pkl` **mtime = 2026-09-15 15:40** (오늘은 09-17).
- 저장 성공 로그 `[Calibration] 앙상블 보정기 저장 완료` 는 **35일간 6회뿐**
  (08-11 n=592 / 08-13 n=888 / 08-19 / 08-28 / 09-10 / 09-15 — **뒤 4회 전부 n=888**).
- 반면 `daily_close()`(15:40) 는 **매일 정상 실행**됐다(09-14~09-17 전부 확인).

**왜 저장이 안 되는가 — 코드로 확정**

```python
# learning/calibration.py:277, 303
self._fitted = False        # 축퇴 감지 시 / 도달불가 감지 시

# learning/calibration.py:save()
if not _SKLEARN_OK or not self._fitted:
    return False            # ← 조용히 False. 로그 없음
```
```python
# main.py:13062
if self.ensemble.ensemble_calibrator.save(ENSEMBLE_CALIBRATOR_PATH):
    log_manager.system("... 저장 완료 ...")      # 실패 시 아무 로그도 없다
```

⇒ **장 마감 시점에 보정기가 축퇴/도달불가 상태면 그날 학습이 통째로 버려지고,
그 사실이 로그에 남지 않는다.** 성공했을 때만 한 줄 찍히므로 **부재를 읽어야
알 수 있다** — 계측 4원칙 ④(폴백 가시화) 위반이며, FP-CRITICAL 죽은 게이트·
TOX 죽은 섀도와 **같은 계열**이다.

**얼어붙은 상태가 하필 진입 불가 상태다**: `out_max=0.3479` < 진입 하한 `min_conf≈0.42`.
그래서 매일 아침은 **구조적으로 진입 불가에서 시작**한다(장중 절 이상점 1-4 ①의 원인).

### 3. 🔴 점수가 40을 넘는 것은 채점기가 **꺼졌을 때**다

`confidence` 와 `confidence_raw` 가 같으면 보정이 적용되지 않은 것이다.
45거래일, 붕괴행 제외, n=9,462:

| 구간 | n | 보정 미적용(raw 통과) | 비율 |
|---|---|---|---|
| `conf < 0.40` | 6,783 | 3,449 | 50.8% |
| **`conf ≥ 0.40`** | 2,679 | **2,442** | **91.2%** |

⇒ **40점 돌파의 9할이 「채점 포기」 구간에서 나온다.** 채점기가 살아 있으면 상한
0.348에 눌려 넘을 수 없고, 축퇴 가드가 그것을 꺼야 원점수가 그대로 통과한다.
이는 CLAUDE.md 「확률 판단 기준」 절의 403차 기록(*축퇴 가드 배포로 보정기가 사실상
꺼져 raw 값이 나온다 — 평균 conf 0.316 → 0.491*)과 **독립 경로로 재확인된 것**이다.

**오늘 축퇴/도달불가 상태 전이 1,591회** (`[Calibration]` 상태변경 로그 집계):
축퇴 감지 615 · 축퇴 해소 594 · 하한 도달불가 196 · 도달불가 해소 186.
**분당 평균 4회 이상 켜졌다 꺼진다.** 같은 신호가 몇 초 차이로 보정되기도, 안 되기도 한다.

### 4. 이상점 등록

#### 1-9. 앙상블 보정기가 35일째 동결 — 매일 아침 「진입 불가」 상태로 기동한다 🆕 (P1)

**증상** 기동 복원값이 35일째 `n=888 / span=0.0063 / auc=0.550 / out_max=0.3479` 로 동일.
그 `out_max` 가 진입 하한(≈0.42)보다 낮아 **아침에는 어떤 신호도 자동진입이 불가능**하다.

**근거** §2 (복원 로그 전수 · 아티팩트 mtime · 저장 성공 로그 6회 · 코드 2곳).

**기준 위반** 계측 4원칙 ④ — `save()` 가 `_fitted=False` 에서 **조용히 False** 를 돌려주고
호출부가 `if save():` 로 성공만 로깅한다. 실패가 관측되지 않는다.

**영향** 미륵이가 매일 아침을 "확신도 채점기가 고장난 상태"로 시작합니다. 그날 배운 것은
장 마감 때 대부분 버려지고, 다음 날 또 같은 자리에서 시작합니다. 35일째입니다.

**영향(기술)** 동결 상태가 EKS 발동 3조건 중 첫째(`conf_max < mc − margin`)를 상시 참으로
만든다(장중 절 1-4 ①). 즉 **1-4 와 1-9 는 같은 뿌리**이며, 1-4 의 1000ms 경주는
이 동결이 만든 「항상 불합격」을 **가리고 있던 우연한 차폐막**이었다.

**처분** 이번 세션은 **진단만 한다.** 수정 방향이 최소 3갈래이고(§5) 어느 쪽도
3일 표본으로 정할 일이 아니다. → `NEXT_TODO` **F-9** 등록.

#### 1-10. EKS 회복 창(09:20~11:30)이 합격률 최저 구간과 겹친다 🆕 (P2)

**증상·근거** §1 45거래일 프로파일 — 합격률 09시 27.1% / 13시 46.9% / 14시 44.2%.
11:30 이전 합격 건수는 09-15 **0건** · 09-16 17건 · 09-17 7건.

**기준 위반** 위반 아님(설계된 마감). 단 장중 절 G-4(회복 마감 재설계)의 **정량 근거**가
이제 생겼다 — 종전에는 "오늘 1건 차이로 아쉬웠다"는 일화뿐이었다.

**영향** 회복 판정을 하필 가장 불리한 시간대에만 합니다. 오후였다면 통과했을 날이 있습니다.

**영향(기술)** EKS 회복은 `conf_hits ≥ 3 / 최근 10봉`인데 09~11시 기저 합격률이 27~37%다.
기대 히트수가 2.7~3.7로 **임계 3과 거의 같아** 판정이 사실상 동전 던지기다.
→ G-4 안건에 이 수치를 첨부한다. ⚠ **마감 연장은 매매 정책 변경**이므로 여전히 주간회의 몫.

### 5. 수정 방향 (제안만 — 결정 아님)

| 갈래 | 내용 | 위험 |
|---|---|---|
| **A. 저장 실패를 보이게** | `save()` 실패 시 사유와 함께 WARNING. **가장 먼저 할 일이며 위험이 없다** | 없음(로그 전용) |
| **B. 축퇴 상태에서도 버퍼는 저장** | 모델이 축퇴여도 `probs/labels` 는 살려 다음날 이어 학습 | 중 — 축퇴 모델을 되살릴 위험, 분리 저장 필요 |
| **C. 축퇴 가드·상한 재설계** | `DEGENERATE_SPAN_MIN=0.02` 대비 실측 span 0.0009~0.021 — 임계가 실측 분포 한복판이라 하루 1,591회 진동한다 | **높음 — 매매 정책에 직결. 주간회의 안건** |

⚠ **A → (관측) → B/C 순서를 지킬 것.** C 를 먼저 건드리면 무엇 때문에 좋아졌는지
알 수 없다(장중 절 F-4 를 F-5 보다 먼저 한 것과 같은 이유).
⚠ 표본 주의: §2 의 동결은 35일 실측이라 확정이나, §5 의 **처방 효과는 미검증**이다.

---

**이 절이 발급한 번호**: 이상점 1-9~1-10 · Fix F-9

---

## 제2부-E. 왜 v9-dev 에서만 발생하는가 — dev 브랜치 대조 (16:20~16:40)

> **사용자 지시**: *"dev 와 비교하고 왜 v9-dev에만 이 문제가 발생하는지 딥다이브해."*
> ⚠ **전제를 먼저 검증했다** — "dev 에는 없다"는 것이 사실인지부터 확인했고, 사실이었다.
> 원천: `git show dev:…` 코드·`dev` 브랜치 `dev_memory/DECISION_LOG.md`(MW0602 관측물).
> ⚠ MW0602 의 **로그·DB 는 이 PC 에 없다**(런타임 산출물 미커밋). 따라서 아래는
> **코드 대조 + MW0602 자신의 기록**에 근거하며, MW0602 라이브 재현은 하지 않았다.

### 0. 한 줄 답

**MW0602 가 2026-08-23 에 이미 고쳤고, 그 커밋이 v9-dev 로 오지 않았다.**
원 커밋 **`2356820` [MW0602] 485차: 앙상블 보정기 영속화 복구 (F-1) + 불변식 4종**.
v9-dev 는 **버그가 있는 원본 코드를 그대로 들고 있다** — 다른 원인이 아니라 **동일 버그 · 미전파**다.

### 1. MW0602 는 26일 먼저 같은 것을 발견했다

`dev` 의 `DECISION_LOG.md`(2026-08-21, MW0602 484차 계열) 원문:

> **[발견] 앙상블 보정기 영속화가 6거래일 동안 조용히 실패했다 (이상점 1-1, P1)**
> **증상**: `data/ensemble_calibrator.pkl` mtime **2026-08-11 15:40** 이후 무갱신.
> 기동 복원 로그가 8거래일 **6필드 전부 동일** — `n=1183 … out_max=0.3897`.
> **원인(코드 실재 확인)**: `save()` — `if not _SKLEARN_OK or not self._fitted: return False`
> (**로그 없음**) … `main.py` — `if save(…): log(…)` 형태로 **`else` 분기가 없다.**
> → 15:40 마감 시각에 축퇴/도달불가 상태면 저장이 통째로 건너뛰어지고 **양쪽 모두 무로그**.
> **Why 이 순서인가**: 3번(부정 분기 로그)이 핵심이다. 코드를 안 고쳤더라도 이 로그 하나만
> 있었으면 6거래일이 아니라 하루 만에 발견됐다. FP-CRITICAL·TOX-SEVERE-SPREAD 와 **같은 실패 계열**이다.

🔴 **오늘(2026-09-17) 이 세션이 v9-dev 에서 독립적으로 도달한 진단과 문장 단위로 일치한다.**
같은 버그이고, 같은 실패 계열이며, MW0602 가 **26일 먼저** 닫았다.

### 2. 코드 대조 — 네 조각 전부 v9-dev 에 없다

| 조각 | `dev` (MW0602) | `v9-dev` (MW0601) |
|---|---|---|
| `calibration.py:save()` 게이트 | `if not _SKLEARN_OK:` — **`_fitted` 제거됨** | `if not _SKLEARN_OK or not self._fitted:` 🔴 |
| `calibration.py:load()` fitted | `state.get("fitted", True)` (축퇴 저장본 부활 방지) | `self._fitted = True` **무조건** 🔴 |
| `main.py` 저장 호출부 | `if _cal.save(...): … else: WARNING 저장 건너뜀` | `if …save(...): …` — **`else` 없음** 🔴 |
| 회귀 테스트 | `tests/test_484_calibrator_persistence.py` **있음** | **없음** 🔴 |

원 커밋: `2356820` (2026-08-23 14:13:42, `[MW0602] 485차`).
`git log dev -S "if not _SKLEARN_OK:" -- learning/calibration.py` 로 특정했다.

### 3. 결과 — 두 PC 의 현재 상태

| | `dev` (MW0602) | `v9-dev` (MW0601) |
|---|---|---|
| 동결 발견 | 2026-08-21 | **2026-09-17 (오늘)** |
| 수정 | **2026-08-23 `2356820`** | **없음** |
| 동결 시작 | 2026-08-11 (n=1183) | 2026-08-13 (n=888) |
| 동결 스냅샷 | span 0.02133 · auc 0.574 · **out_max 0.3897** | span 0.00630 · auc 0.550 · **out_max 0.3479** |
| 아티팩트 | 3,008B → **5,299B** (08-24 갱신 재개) | **3,008B, 2026-09-15 mtime, 그대로** |
| 동결 지속 | 10거래일에서 끊김 | **35일째 진행 중** |

🔴 **두 PC 의 동결 아티팩트가 똑같이 3,008바이트에서 출발했다.** MW0602 는 수정 다음 날
5,299B 로 자라며 갱신이 재개됐고, **MW0601 은 아직 3,008B 다.** 같은 병에 한쪽만 약을 먹었다.

⚠ **동결 스냅샷의 품질도 다르다.** MW0601 이 붙들고 있는 `out_max=0.3479` 는 MW0602 가
붙들고 있던 `0.3897` 보다 **낮다.** 진입 하한이 ≈0.42 이므로 둘 다 미달이지만,
**MW0601 쪽이 더 깊이 미달**이다 — 같은 버그라도 **하필 더 나쁜 순간에 얼어붙었다.**

### 4. 왜 전파되지 않았나 — 날짜가 말해준다

수정 커밋 `2356820` 은 **2026-08-23**이다. 그리고 CLAUDE.md 「멀티PC 작업 컨벤션」:

> 🔴 **[2026-08-23 사용자 결정 / MW0602 487차] 이 절의 멀티PC 정책은 2026-08-23부로
> 폐기됐다.** MW0601·MW0602 는 더 이상 컨벤션·안건·채널 번호를 상호 조율하지 않으며,
> `dev` 브랜치는 **MW0602 단독 운영**이다.

**수정(485차)과 조율 폐기(487차)가 같은 날이다.** 485차가 먼저지만 몇 시간 차이이며,
그날 이후 "한쪽 PC 의 픽스를 다른 쪽에 반영한다"는 **의무가 사라졌다.**
브랜치는 이후 `dev` 단독 536커밋 / `v9-dev` 단독 524커밋으로 갈라졌다.

⚠ **이것은 그 결정을 비판하는 것이 아니다.** 조율 폐기는 비용을 줄이려는 타당한 결정이었고
CLAUDE.md 가 그 근거를 적고 있다. 다만 **그 결정에 "안전·계측 결함 픽스는 예외"라는
단서가 없었고**, 그 공백이 35일간 방치로 나타났다. 이것이 이 절의 실질 발견이다.

### 5. 이상점 등록

#### 1-11. 보정기 영속화 픽스가 26일째 v9-dev 에 전파되지 않았다 🆕 (P1)

**증상** §2 네 조각 전부 미적용. `dev` 는 2026-08-23 에 닫은 결함이 v9-dev 에서 진행 중.

**근거** §1~§4 (코드 4곳 대조 · 원 커밋 `2356820` 특정 · 아티팩트 크기/mtime · MW0602 원문).

**기준 위반** CLAUDE.md 「멀티PC 작업 컨벤션」의 **공백**. 조율 폐기 결정에 안전·계측
결함 픽스의 예외 조항이 없다. 계측 4원칙 ④(폴백 가시화)는 v9-dev 에서 여전히 위반 중.

**영향** 다른 PC 가 한 달 전에 고친 문제를 이쪽은 계속 겪고 있었습니다. 두 PC 가 같은
프로그램을 돌리지만 고친 내용이 자동으로 건너오지 않습니다.

**영향(기술)** 1-9(동결)의 **직접 원인**이며, 1-9 는 다시 1-4(EKS 항상 불합격)의 뿌리다.
즉 오늘 조사한 세 이상점이 **하나의 미전파 커밋으로 수렴**한다.

**처분** 체리픽 후보로 등록(F-10). ⚠ **자동 적용하지 않는다** — §6 참조.

### 6. 조치 제안 (결정 아님)

**F-10. `2356820` 을 v9-dev 로 체리픽**한다. 오늘 599차를 dev 로 보낸 것의 **역방향**이며,
CLAUDE.md 가 요구하는 체리픽 기록(원 sha · 원 PC · 이유)을 남긴다.

⚠ **그대로 붙지 않을 수 있다.** `learning/calibration.py` 는 두 브랜치에서 줄 위치가
크게 다르다(`save()` 가 dev 626행 / v9-dev 396행). 599차처럼 **격리 worktree 에서
시험 적용 → 테스트 → 확정** 순서를 지킬 것.

⚠ 🔴 **이 픽스가 「아침 상한이 0.42 를 넘게 된다」는 뜻은 아니다 — 그것은 미검증이다.**
`dev` 의 기록은 수정 **이틀 뒤(2026-08-25)에도** `09:00:01 [ConfFloorGuard] … out_max=0.3479
< 필요 0.4420` 을 관측하고 있다(그쪽 O-4 로 관측만 등록). 즉 **영속화가 복구돼도 아침
출력상한이 진입 하한을 넘는다는 보장은 없다.** F-10 이 닫는 것은
**「그날 배운 것을 버리지 않는다」 + 「버려졌을 때 로그에 남는다」** 두 가지다.
1-4(EKS)가 자동으로 해소된다고 쓰지 말 것 — 별도 관측이 필요하다(O-t2).

**우선순위**: F-9-A(저장 실패 가시화)는 F-10 에 **포함**되므로 중복이다.
→ **F-9-A 를 F-10 으로 흡수**하고, 자체 구현 대신 검증된 원 커밋을 가져온다.

### 7. ↩️ 정정 — 제2부-D §3 의 「1,591회」

제2부-D §3 이 *"오늘 축퇴/도달불가 상태 전이 1,591회"* 를 **앙상블 보정기 하나의 것처럼**
서술했다. **부정확하다.** `learning/calibration.py` 의 `logger` 는 모듈 단위라
`ensemble_calibrator` · `horizon_calibrator` · `extremity_corrector` 등 **여러 보정기
인스턴스가 같은 태그를 공유**한다. 1,591 은 **그 합계**이며 앙상블 보정기 단독 수치가 아니다.

**바뀌지 않는 것**: §2 의 동결(복원값 35일 동일 · 아티팩트 mtime · 저장 성공 로그 6회)과
§3 의 핵심 실측(**conf≥0.40 인 분의 91.2% 가 보정 미적용**)은 `ensemble_decisions` DB 와
아티팩트에서 직접 잰 값이라 **영향 없다.**
→ 보정기별 분리 집계는 로그에 인스턴스 식별자가 없어 **현재 불가**하다. 그 자체가
계측 결함이므로 **F-11**(로그에 보정기 이름 접두 추가)로 등록한다.

---

**이 절이 발급한 번호**: 이상점 1-11 · Fix F-10~F-11 · 관측 O-t2
