# 미륵이 일일 점검 — MW0601 / 2026-08-19 / 장중(intra)

- 점검 시각: 12:27~12:40 KST · PC `MW0601` · 브랜치 `v9-dev` ✅
- HEAD `624a275` · 미커밋 461건(**CRLF 아티팩트 — 실질 4건**, 478차 장전 확인분) · 당일 커밋 0건
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260819_intra.md`
- 전일 대조본: `MW0601-20260818-점검리포트-intra.md` · 당일 장전본: `MW0601-20260819-점검리포트-pre.md`

> 🔴 **본 세션은 코드를 변경하지 않았고 라이브 DB에 분석 쿼리를 돌리지 않았다.**
> 근거는 전부 로그·설정·git 메타다. §2 Fix 계획은 **수립만** 하며 적용은 15:10 강제청산 이후다.

---

## 0. 한 줄 결론

**P0 없음** — 절대원칙 6종 위반 0건, CB⑤ 미발동, 매분 루프 실질 공백 0.
다만 09:49:01 진입 1건이 **Chejan 선행 레이스로 위험 파라미터 5종이 조용히 기본값으로 떨어져** TP1이 설계의 2배 폭으로 설정된 채 운용됐고(P1), STEP 1 채점 커버리지가 **10m 20% / 15m 13%** 로 구조적으로 결손돼 있다(P1).

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 정상(장전본 참조) | 브랜치 `v9-dev` · EOD 6/6 교체 · 08:58:25 `[Regime] NEUTRAL` · PSI 라이브 첫 확인 |
| 장중 | **조건부 정상** | 파이프라인·CB·청산은 설계대로. 진입 경로 1건 레이스 + STEP 1 채점 결손 2건 |
| 장후 | 해당 없음(장후 전용) | |

**절대원칙 준수 여부**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | 판정 유보(장중) | 12:40 현재 FLAT. 15:10 경로는 장후 점검 대상. `F-1R` 리허설 미실행 상태 유지 |
| ② | Circuit Breaker 5종 | **정상** | CB⑤ 최대 파이프라인 2,217ms < 5,000ms → 미발동. CB② 카운터 2회(9999라 정지 없음 = 정상). CB③ acc30m 버퍼 누적 유지(`11:49:01 [CB③] 리셋 스킵 — 표본 14건 < 30건`) |
| ③ | CORE 피처 호라이즌 그룹별 | **정상** | 진입 3건 전부 `vwap✅` — 강제X 위반 **0건**. 09:48:01 `[Checklist] CORE VWAP ✗ → 강제 X등급 (group=short)` 로 차단 경로도 라이브 동작 확인 |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | **정상** | `0xC0000409`·`STACK_BUFFER`·무응답 0건. `[CybosEvent] recv begin/end` 8건 전부 정상 종료 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 해당 없음 | 브로커 `cybos` — `OPT#####` 계열 미사용(478차 F-4 문서화 대상) |
| ⑥ | 알파 리서치 봇 자동통합 금지 | **정상** | 알파봇 통합·백테스트 큐 로그 0건 |

**설정 불변식** — 수집기 §3에서 `미발견` 5행(`MODEL_LABEL_STATE_UNLOCK_ENABLED` / `PRE_RETRAIN_DONE_BY_EOD_ENABLED` / `ZONE_ENTRY_BAN_ENFORCE` / `ZONE_ENTRY_BAN_SHADOW_ENABLED` / `PIPE_LATENCY_EXCLUDE_MODEL_SWAP`).
**전부 `dev` 브랜치 전용 상수이며 `v9-dev`에는 존재하지 않는다** — 브랜치 스코프 미분리로 인한 오탐이다(478차 장전에서 6일째 재발로 등록, `F-2`). 나머지 21행은 전부 `일치`.

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**해당 없음.** 확인 항목: 강제X 위반(0) · CB 오발동(0) · 크래시(0) · 장중 코드배포/재기동(0) · 알파봇 자동통합(0).

---

### P1 — 정확성·관측 훼손

#### 1-1. Chejan 체결이 `open_position()`을 앞질러 **위험 파라미터 5종이 조용히 기본값으로 진입** — TP1이 설계의 2배 폭

- **증상**: 09:49:01 LONG 3계약 진입에서 `open_position()`이 예외로 실패했고, 포지션은 대신 체결 콜백 경로(`apply_entry_fill()` FLAT 분기)로 열렸다. 그 결과 `[Position] 진입` 행이 **아예 남지 않았고**(11:11·11:26 진입에는 있다), 손절/TP 폭이 호라이즌·레짐 배수 없이 계산됐다.

- **근거**:
  ```
  2026-08-19 09:49:01 [ERROR] SYSTEM: [FixB] open_position 실패 direction=LONG
                                       status_before=LONG err=이미 포지션 보유 중
  ```
  `logs/20260819_WARN.log` · 1건 · 당일 유일 ERROR

  TRADE 로그의 순서가 레이스를 그대로 보여준다 — **체결 3건이 `[주문요청]` 로그보다 먼저 찍혔다**:
  ```
  09:49:01 [Sizer] … → 3계약 (최소=1)
  09:49:01 [진입체크] LONG→LONG 3계약 A급(원시C) | … | conf=40.7%
  09:49:01 [Chejan] 상태=접수 주문번호=814 … 체결=3 미체결=0
  09:49:01 [Position] 체결진입 LONG 1계약 @ 1018.32 | 보유=1계약     ← 이미 열림
  09:49:01 [Position] 체결진입 LONG 1계약 @ 1018.42 | 보유=2계약
  09:49:01 [주문요청] LONG->LONG 3계약 @ 1018.74 등급=A 체결대기     ← 그 다음에 주문요청 로그
  09:49:01 [Position] 체결진입 LONG 1계약 @ 1018.36 | 보유=3계약
  ```
  `logs/20260819_TRADE.log`

  **대조군**(정상 경로, 같은 날):
  ```
  11:11:01 [Position] 진입 SHORT 3계약 @ 1016.68 | 손절=1019.66 1차=1015.69(×0.50) 2차=1013.70 horizon=3m hurst=neutral
  11:11:01 [Position] 체결보정 SHORT 1계약 @ 1016.72 …
  ```

- **수치 검증 — TP1이 정확히 2배로 설정됐다**:
  · 09:49:01 `[ATR-Horizon] 진입 호라이즌=3m tf=4.34 → TP1×0.5` (`logs/20260819_SIGNAL.log`) — 시스템은 3m을 선택했고 ×0.5를 의도했다.
  · `[PendingOrder]` 스냅샷 `'atr': 2.2271`, 3차 체결 후 평균 진입가 `1018.3667`.
  · 실제 TP1: `09:53:13 [TickTP1] TP1 처리 (틱) LONG tick=1020.60 tp1=1020.59` → `1020.59 − 1018.3667 = 2.223 ≈ ATR × 1.00`.
  · 설계값(3m, ×0.50): `1018.3667 + 1.114 = 1019.48`.
  → **TP1이 1.11pt(약 2배) 멀리 잡혔다.**

- **코드 경로**(읽기만 함, 변경 없음):
  · `main.py:16332-16365` — `open_position()`을 `try/except`로 감싸고 실패 시 **ERROR 한 줄만 남기고 그대로 진행**한다. `_optimistic`·`open_position_done` 플래그도 세워지지 않는다.
  · `main.py:16516-16527` — 그 실패 뒤 체결이 타는 `_ts_handle_entry_fill_cybos_safe()`의 `apply_entry_fill()` 호출부는 **`entry_horizon` 인자를 넘기지 않는다**(같은 파일 `main.py:14171`의 범용 경로는 넘긴다).
  · `strategy/position/position_tracker.py:506-541` — `apply_entry_fill()` FLAT 분기는 `entry_horizon`만 받고 `hurst_bucket`·`extra_stop_mult`·`quantile_expected_pt`·`quantile_uncertainty_pt`·`checklist_pass_count`는 **파라미터 자체가 없다**(`open_position()` 서명 `position_tracker.py:268-283`에는 있다).
  · `position_tracker.py:1213-1219` — `ATR_HORIZON_TP1_MULT.get(self.entry_horizon, ATR_TP1_MULT) if self.entry_horizon else ATR_TP1_MULT` / `HURST_REGIME_ATR_MULT.get(...) if ... and self.entry_hurst_bucket else None` → 둘 다 폴백.

- **기준 위반**: 매분 9단계 **STEP 7(진입 실행) → STEP 8(청산 트리거)** · **계측 4원칙 ④(폴백 가시화)** — 기본값이 쓰였는데 그 사실이 어디에도 남지 않았다. FP-CRITICAL(2개월 PSI=0.0)·`_entry_horizon_pre`(370/370건 `'1m'` 고착)와 **같은 결함 패턴**이다.

- **영향**:
  1. **집행**: 그 포지션은 `hurst_bucket=None`(레짐 조건부 손절/TP 배수 미적용) · `extra_stop_mult=1.0`(349차 VolatilityBurstGate 급변장 가드 미적용) · `entry_horizon=None`(TP1 ×1.00) 로 운용됐다. 오늘은 결과적으로 유리했으나(TP1이 멀어 +2.13pt/1계약) **운은 근거가 아니다.** 반대 방향이었으면 TP1 미도달로 하드스톱까지 밀렸을 경로다.
  2. **관측**: `[Position] 진입` 행이 없어 그 포지션의 **의도 손절폭이 사후 계산 불가**다 → 417차 재분해에서 유일하게 유의했던 축인 **손절 준수율(실현손실 ÷ ATR×1.5)의 영구 결손 1건**. `checklist_pass_count`(456차 F10-B)·`quantile_*`도 동일.
  3. **귀속**: `trades.entry_horizon`이 그 포지션에 대해 NULL 계열로 남는다(장중이라 DB 미확인 — 장후 확인 항목 `O-1`).

- **신규 여부**: **신규.** `[FixB] open_position 실패` 는 2026-08-10~08-19 10거래일 로그 전수에서 **오늘 09:49:01 1건이 유일**하다(나머지 29건은 전부 정상 `낙관적 오픈 완료`). 다만 레이스 자체는 코드가 알고 있었다 — `main.py:16489` 주석이 *"False(레이스 컨디션: BlockRequest 내 Chejan 선행)이면 else→apply_entry_fill로 qty 누적"* 이라고 **수량 누적만** 다루고 있고, **위험 파라미터 유실은 다루지 않는다.**

---

#### 1-2. STEP 1 채점 커버리지가 호라이즌별로 구조적 결손 — 10m 20% / 15m 13%

- **증상**: 매분 실행돼야 할 과거 예측 검증(STEP 1)이 호라이즌마다 전혀 다른 빈도로 돈다. 10m은 **분(minute)이 10의 배수 또는 그 직전**일 때만, 15m은 **15의 배수 또는 그 직전**일 때만 채점된다.

- **근거**: `logs/20260819_LEARNING.log` — 15m 채점 시각 **전량 27개**:
  ```
  09:15 09:29 09:30 09:44 09:45 09:59 10:00 10:14 10:15 10:29 10:30 10:44 10:45
  10:59 11:00 11:14 11:15 11:29 11:30 11:44 11:45 11:59 12:00 12:14 12:15 12:29 12:30
  ```
  3분 연속 구간을 보면 10m·15m만 빠진 것이 분명하다:
  ```
  10:03:01 ✗ 1m / ✗ 3m / ✗ 30m
  10:04:00 ✓ 1m / ✓ 5m / ✗ 30m
  10:05:00 ✗ 1m / ✗ 3m / ✗ 5m / ✗ 30m
  ```
  30m은 09:30부터 **매분 연속** 채점된다(09:30·09:31·09:32·…).

  **날짜 무관 구조적 고정** — 3거래일 전량 집계:

  | 호라이즌 | 08-12 | 08-14 | 08-18 | 08-19(12:35 현재) | 1m 대비 |
  |---|---|---|---|---|---|
  | 1m | 369 | 366 | 368 | 135 | 100% |
  | 30m | 340 | 339 | 340 | 89 | 92% |
  | 3m | 245 | 242 | 244 | 86 | 67% |
  | 5m | 146 | 143 | 145 | 48 | 39% |
  | **10m** | **72** | **70** | **72** | **41** | **20%** |
  | **15m** | **47** | **46** | **47** | **27** | **13%** |

- **기준 위반**: 매분 9단계 **STEP 1** ("T-1·T-3·T-5·T-10·T-15·T-30분 채점"). 스킬 참조문서 `references/phases.md:57` B-1 체크항목 *"T-1·T-3·T-5·T-10·T-15 채점이 매분 돌았는가"* → **답은 "아니다"** 이며, 그 항목이 실제로 커버리지로 확인된 기록이 dev_memory에 없다.

- **영향**:
  1. 10m·15m는 CLAUDE.md 474차 실측 기준 **앙상블 가중 0.29·0.28(합 57%)** 을 지는 축인데, 그 둘의 온라인 피드백(`horizon_calibrator.record()` · `extremity_corrector.record()` · `pred_buffer.recent_accuracy()`)이 1m 대비 **5~8배 희소**하다. 보정기가 가장 늦게 배우는 호라이즌이 가중을 가장 많이 갖고 있다.
  2. `dev_memory/NEXT_TODO.md` 의 `[ModelLive] 10m·15m` 관측 채널이 *"n=72/47 · 3거래일 — 313차 확정 결론 보류"* 로 표본 부족을 caveat 처리하고 있는데, **이 n은 표본 부족이 아니라 구조적 상한**이다. 거래일을 더 쌓아도 하루 72/47을 넘지 못한다 — 기다리는 전략이 성립하지 않는다.

- **⚠ 원인 미확정 (313차 원칙)**: 채점기 `learning/prediction_buffer.py:323 verify_and_update()`는 `HORIZONS` 전체를 균등 처리하고 저장기 `save_step9_batch()`(같은 파일 144행)도 `horizon_proba.items()`를 그대로 순회한다 — **둘 다 호라이즌 필터가 없다.** 따라서 원인은 그 위(STEP 5)에서 `horizon_proba`에 10m·15m가 그 분에만 실리는 것으로 좁혀지나, **확정하지 않았다.** 후보 ① STEP 5 예측 발행 단계의 호라이즌별 주기 ② `horizon_proba` 구성 시점의 조건부 제외. 장후 `F-6`으로 확인.

- **신규 여부**: **신규**(현상 자체). 단 하위 증상(n=72/47)은 기존 `[ModelLive] 10m·15m` 관측 항목에 이미 숫자로만 기록돼 있었다.

---

### P2 — 운영 부담·기술부채

#### 1-3. 스킬 참조문서 `phases.md` B-1의 "T-30 퇴역" 문구가 CLAUDE.md 474차와 정면 충돌

- **증상**: `references/phases.md:58` 이 *"**T-30은 퇴역 대상**이다 — 30m이 앙상블에서 빠졌으므로 여기 남아 있다면 그 자체가 점검거리"* 라고 지시한다. 그대로 따르면 오늘 30m 채점 **89건**을 이상점으로 보고하게 된다.
- **근거**: CLAUDE.md 절대원칙 §3 말미(474차) — *"🔴 **30m 학습을 중단하면 안 된다 — CB③의 유일한 입력원이다.** `main.py:5539`가 `if v["horizon"] == "30m"` 으로 걸러 `circuit_breaker.record_accuracy()`에 먹인다."* 라이브 실측도 일치한다: `main.py:5546-5553` 이 30m·conf>0.38·FLAT제외 조건으로만 CB③에 적재하고, 오늘 `11:49:01 [CB③] acc30m 버퍼 리셋 스킵 — 기존 표본 14건 < 최소 30건` 이 그 누적을 보여준다.
- **기준 위반**: 문서-문서 드리프트. **CLAUDE.md가 정본**이므로 `phases.md`가 낡았다.
- **영향**: 스킬을 쓰는 다음 세션이 **정상 동작을 이상점으로 보고**할 유인이 구조적으로 남아 있다. 함정 ①(판정≠결정)의 문서판이다.
- **신규 여부**: **신규.**

---

### 확인 필요 (확정 아님)

| ID | 내용 | 무엇을 더 보면 판정되는가 |
|---|---|---|
| C-1 | 1-1 레이스가 **왜 오늘만** 났는가 — 09:49:01 파이프라인 `S7=385ms`(당일 최대급)로 주문 전송 구간이 늦었다(`[PipePerf][DBG] total=658ms … S7=385ms`). 지연이 유인인지 불명 | 장후에 `ensemble_decisions`의 09:49 행 + 최근 10거래일 `[PipePerf] S7` 분포 대조. **표본 1건이므로 확정 결론 금지(313차)** |
| C-2 | 1-1 포지션의 `trades.entry_horizon` 실제 저장값 | 장후 DB 조회(`guard_intraday()` 해제 후) |
| C-3 | IntradayRegime 전이 급증(아래 참조)의 실해 유무 | 오늘 종일 전이 횟수 확정 + `레짐배수` 변동 여부. 현재까지 `[Sizer]` 6회 전부 `레짐배수=0.8` 고정이라 **실해 미확인** |

---

### 이미 반영된 사안 (신규 아님) — 함정 ① 방지

이 절을 비워두지 않았다. 아래는 **오늘 로그가 지적하지만 이미 코드·결정에 반영돼 있는 것**이다.

| 관측 | 이미 반영된 근거 | 오늘 수치 |
|---|---|---|
| `[JointGateBlock 차단] LONG 2계약 A급 (meta=0.50 tox=0.70 joint=0.350)` ×3 (10:05:01 · 10:44:03 · 11:02:01) | CLAUDE.md 실전전환 ⑧ 431차 — *"MetaGate 무정보 폴백(reduce 밴드 47.6%가 하드코딩 0.50)의 **사이징 경로만** 중립화(JointGateBlock 차단 기준 `size_multiplier`는 **무변경** — [7] PASS 게이트 보존)"*. **의도된 설계다** | 오늘 MetaGate 61회 중 `action=reduce size_mult=0.50` **31회(51%)** |
| `[CB] 연속 손절 1회`(11:12:08) · `2회`(11:21:33) — **한 포지션(11:11 SHORT 3계약)이 2 카운트** 생성 | `DECISION_LOG.md` 360차 — *"`_post_loss_tier1_exit`도 TP 부분청산과 동일하게 매 단계 CB/Kelly에 기록하므로, 포지션 하나가 손실 이벤트를 최대 2회(1차+최종) 만들 수 있게 됨 — CB②(현재 9999로 비활성) 복원 시 감안 필요"*. NEXT_TODO에 08-29 안건으로 계류 중 | `CB_CONSEC_STOP_LIMIT=9999` 유지 확인 → 정지 없음(정상) |
| `min_conf 0.65→0.62` 가 11:50:05부터 46분 연속 | **점심구간 zone_mc 상한**(`config/settings.py:505 MC_ZONE_MAX = 0.65`). 08-14·08-18 모두 **11:50~12:59:59 정확히 같은 창**에서 70회씩 나타난다 → 정상 동작 | 오늘 11:50:05 진입, 08-14/08-18과 동일 |
| `[RouterHealth] 라우터가 ConstOut 활성 호라이즌 선택 — chosen=3m` ×4 | NEXT_TODO `G-3b RouterHealth 섀도 → 판정 채널 승격 검토 (26주 WFA 주기)` 로 이미 등록 | 08-18 3회 → 08-19 **4회**(12:35 현재) |
| `[ScalerRefresh] … CORE 'ofi_norm' raw_std≈0 → identity(0,1) 강제` 다발 | 478차 장전에서 **[신규 P1] 2거래일째**로 이미 등록. `G-1`(감시 스크립트) · `G-2`(원인 3분기 진단) 계획 수립 완료 | 아래 §1-4 갱신 |
| 불변식 감시표 `미발견` 5행 | 478차 장전 **[기존 P1 재발 · 6일째]**, `F-2` 등록 완료 | 변동 없음 |
| 미커밋 461건 | 478차 장전 **[신규 P2] CRLF 아티팩트 — 실질 4건** | 변동 없음 |

---

### 1-4. 기존 등록 항목 — 오늘 장중 갱신치 (신규 보고 아님)

| ID | 항목 | 어제까지 | 오늘 12:35 현재 |
|---|---|---|---|
| G-1/G-2 | `ofi_norm` identity 강제 (**3거래일째**) | 08-18 1m CORE 36회 중 **34회(94%)** · 08-17 이전 0~6회 | **32/36 (89%)** · 6개 호라이즌(1m·3m·5m·10m·15m·30m) **동시** 216행 |
| O-G | IntradayRegime 전이 (**4일차**) | 08-14 19회 · 08-18 22회 (**종일**) | **42회 (반일)** — 종일 환산 ~80회. `day`가 **-1.00% 경계에서 진동**(11:13→11:14→11:15→11:16 **4분 연속 4회**, day −0.72/−0.85/−0.82/−0.77%). ⚠ `min_conf`·`레짐배수`에는 전파되지 않음(C-3) |
| G-A/G-B | RegimeFingerprint PSI 라이브 | 09:00:00 하트비트 1건(477차 F-5 첫 확인) | **40건 · PSI 0.009~0.012 · level=0 전량** — 라이브 경로 정상 |
| G-C | DriftAdjuster | 기동 `alpha=0.01000, 마지막 액션=DRIFT_UP` | 장중 변동 없음 — 15:40 EOD 확인 대상 |
| GR-3V | ProfitGuard `src=` 토큰 | 장전 차단 0건 → 미확인 | **차단 여전히 0건** → 미확인 유지 |
| [9] | 실전전환 ⑨ TOX-SEVERE-SPREAD | 08-18 `spread_ticks≥20` 1분 / 진입 0건 | **장중 DB 금지로 미측정**(0이 아니다 — 계측 4원칙 ②). 장후 확인 |
| — | ConstOut 재적합 / 장중 재학습 | 08-18 6회 · 08-14 4회 (평시 2~6) | **4회** (09:35 · 10:09 · 10:42 · 11:48) — 정상 범위. ⚠ 최근 5거래일 ConstOut 14건 중 **13건이 `['3m']`** — 3m 편중 |

---

## 2. Fix 작업 구현계획

> 🔴 **본 계획은 수립만 하고 적용하지 않는다. 라이브 프로세스가 돌고 있다.**
> 전 항목 **15:10 강제청산 이후** 적용. 런타임은 별도 표기가 없으면 `py37_32`.

### F-5. 진입 레이스 시 위험 파라미터 유실 차단 + 폴백 가시화 — **P1** · 대응 이상점 1-1

- **원인 가설**: Cybos 실시간 체결(`Dscbo1.CpFConclusion`)이 `SendOrder` 리턴 직후 `open_position()` 호출 **전에** 도착하면, `apply_entry_fill()` FLAT 분기가 먼저 포지션을 열어 `open_position()`이 "이미 포지션 보유 중"으로 예외를 던진다. 그 경로에는 `entry_horizon`·`hurst_bucket`·`extra_stop_mult`·`quantile_*`·`checklist_pass_count`를 실을 자리가 없다.
- **변경 파일** (3단계, 위험도 오름차순 — **1단계만 오늘 적용 권고**):

  **① 가시화만 (무해, 즉시 적용 가능)**
  - `main.py:16361-16365` — `except` 분기의 ERROR 로그에 **유실된 파라미터를 명시**한다:
    `[FixB] open_position 실패 … | 유실=entry_horizon={} hurst={} extra_stop_mult={} pass_count={} → TP1/손절 폴백 적용`.
  - `strategy/position/position_tracker.py` `_recalculate_levels()` — `entry_horizon is None` 또는 `entry_hurst_bucket is None` 인데 포지션이 열려 있으면 **WARNING 1회** (`[Levels] 폴백 적용 hz=None hurst=None → TP1×{ATR_TP1_MULT}`). 계측 4원칙 ④의 직접 이행.
  - `main.py:16517-16527` — `apply_entry_fill()` 호출부에 **`entry_horizon=pending.get("entry_horizon")` 인자 추가**. `main.py:14171` 범용 경로와 시그니처를 맞추는 것뿐이며 기본값이 `None`이라 회귀 위험이 사실상 없다.

  **② 파라미터 전달 확장 (중간 위험, 다음 세션)**
  - `strategy/position/position_tracker.py:458` `apply_entry_fill()` 서명에 `hurst_bucket` · `extra_stop_mult` · `checklist_pass_count` **선택 인자 추가**(전부 기본값 유지 → 기존 호출부 무영향), FLAT 분기(506-520)에서만 대입.
  - `main.py:16517` · `main.py:14171` 두 호출부에서 `pending`의 대응 키를 전달.

  **③ 레이스 자체 제거 (고위험, 별도 세션)**
  - 체결 콜백을 `open_position()` 완료까지 큐잉하는 방안 — ⚠ **절대원칙 ④에 인접한다.** `_on_receive_tr_data` 계열 COM 콜백 안에서 `dynamicCall`·`emit`이 발생하지 않음을 **적용 전 호출 스택으로 반드시 확인**할 것. 확인 미완이면 손대지 않는다.
- **회귀 위험**: ①은 로그 추가 + 기본값 인자 1개 → 사실상 없음. ②는 `apply_entry_fill()` 호출부 3곳(`main.py:14171`·`16517`, 그리고 `_ts_manual_position_restore` 경유 경로) 전수 확인 필요. ③은 크래시 위험이 있어 이번 계획에서 **실행하지 않는다**.
- **절대원칙 저촉 여부**: ①②는 저촉 없음. ③은 ④에 인접 — 위와 같이 가드 명시.
- **런타임 주의**: 전부 `py37_32` 본체. 재학습 코드 무관.
- **검증**: `tests/` 에 `test_478_entry_fill_fallback_visibility.py` 신설 — `apply_entry_fill()` FLAT 분기로만 연 포지션에서 `entry_horizon`이 보존되고, 미보존 시 WARNING이 나오는지. 라이브는 다음 `[FixB] open_position 실패` 발생 시 로그에 `유실=` 토큰이 붙는지로 판정(발생이 드물어 **판정 기한을 걸지 않는다**).
- **섀도 계측 필요 여부**: 불필요 — 게이트·차단 로직 신설이 아니라 기존 값 전달·기록이다.
- **적용 시점**: ① **장후 즉시**, ② 다음 세션, ③ 보류.
- **결정 필요 사항**: 없음(①②는 관측 복원이므로 매매 정책 무변경).

### F-6. STEP 1 채점 커버리지 계측 + 원인 확정 — **P1** · 대응 이상점 1-2

- **원인 가설**: STEP 5에서 `horizon_proba`에 10m·15m가 그 분에만 실린다. **미확정.**
- **변경 파일**:
  - **1단계(조사만, 코드 변경 0)**: `scripts/step1_scoring_coverage.py` 신설 — 읽기 전용. `logs/*_LEARNING.log`의 `✓/✗ {h} 예측` 을 **호라이즌별·분(minute) mod h 별**로 집계해 일자별 커버리지 표를 낸다. ⚠ `guard_intraday()` 불필요(로그만 읽음).
  - **2단계(원인 확정 후)**: `main.py` STEP 5 구간에서 `horizon_proba` 키 집합을 매분 1줄로 남긴다 — `[HorizonEmit] hz={sorted(horizon_proba)} n={len}`. 이것만으로 ①/② 후보가 갈린다.
  - **3단계는 원인 확정 전에는 계획하지 않는다.** 발행 주기를 바꾸는 것은 **모델 부하·CB⑤ 지연에 직결**되므로 별도 안건이다.
- **사전등록 판정문** (⚠ 임계는 지금 등록하고 이후 움직이지 않는다 — 313차 ④, 458차 D6):
  > *"`[HorizonEmit]`이 10m·15m를 매분 싣고 있는데도 채점이 2/h로 남으면 원인은 채점기 쪽이다(후보 ②). 발행 자체가 2/h이면 원인은 STEP 5다(후보 ①). 5거래일 관측으로 확정하며, 그 전에는 발행 주기를 바꾸지 않는다."*
- **회귀 위험**: 1단계 0. 2단계는 로그 1줄(파이프라인 S5 구간, `PipePerf` 영향 <1ms 예상).
- **절대원칙 저촉 여부**: 없음. ⚠ **30m은 건드리지 않는다** — CB③의 유일한 입력원(CLAUDE.md §3 474차).
- **런타임 주의**: 스크립트는 표준 라이브러리만 — 어느 환경이든 무관. `main.py` 변경은 `py37_32`.
- **검증**: 스크립트 출력이 본 리포트 §1-2 표(08-12/14/18/19)를 재현하는가.
- **적용 시점**: 1단계 장후 즉시, 2단계 장후.
- **결정 필요 사항**: 없음(조사 단계).

### F-7. `phases.md` B-1 T-30 문구 정정 — **P2** · 대응 이상점 1-3

- **변경 파일**: `.claude/skills/mireuk-daily-check/references/phases.md` B-1 —
  *"**T-30은 퇴역 대상**이다 …"* → *"**T-30은 채점이 유지돼야 한다** — 30m은 앙상블 가중 0.0(296차 퇴역)이지만 **CB③의 유일한 입력원**이므로(CLAUDE.md 절대원칙 §3 474차, `main.py:5546`) 매분 채점이 정상이다. **채점이 끊기면 그것이 이상점이다.**"*
- **회귀 위험**: 문서만. **F-2·F-4와 같은 커밋**(스킬 참조문서 정정 묶음).
- **적용 시점**: 장후.

### 적용 순서와 커밋 계획

1. `[MW0601] 478차 후속1: F-5① 진입 레이스 폴백 가시화 — apply_entry_fill entry_horizon 전달 + 유실 파라미터 로그`
2. `[MW0601] 478차 후속2: F-6 1단계 step1_scoring_coverage 스크립트 신설 (읽기 전용)`
3. `[MW0601] 478차 후속3: 스킬 참조문서 정정 — F-2(브랜치 스코프) + F-4(브로커 분기) + F-7(T-30 문구)`

> **본 세션은 커밋하지 않는다.** 위 3건은 장후 세션의 작업 목록이다.

---

## 3. 고도화 방안

당일 관측에서 출발한 것만 쓴다.

### G-5. 진입 파라미터 완전성 불변식 테스트

- **관측 근거**: 1-1 — 진입 3건 중 1건이 **위험 파라미터 5종을 빠뜨린 채** 열렸는데, 그 사실을 알려준 것은 ERROR 한 줄뿐이었고 TP1이 2배가 된 것은 **아무 계측에도 걸리지 않았다**. 471차 F-1(15:10 경로 6개월 도달불가) · 474차(mid/long CORE 그룹 도달불가) · 473차(spread 섀도 한 달 미배선)와 **같은 유형**이다.
- **제안 내용**: `tests/test_478_entry_param_completeness.py` — 포지션을 여는 **모든 경로**(`open_position()` · `apply_entry_fill()` FLAT 분기 · `_ts_manual_position_restore()`)를 AST로 열거하고, 각 경로가 `entry_horizon`·`entry_hurst_bucket`을 설정할 수단을 갖는지 정적 검사. 못 갖는 경로가 생기면 테스트가 깨진다. 474차 `tests/test_473_core_group_reachability.py`가 라우터 도달성을 고정한 것과 같은 방식이다.
- **기대 효과**: "포지션을 여는 새 경로가 생겼는데 파라미터를 안 실었다"를 코드 리뷰가 아니라 CI에서 잡는다. 측정 가능형: 경로 3종 → 테스트가 3종 전부 열거하고, 하나라도 결손이면 FAIL.
- **비용·위험**: 소(테스트 1개, 라이브 무영향). 섀도 계측 불필요.
- **선행 조건**: F-5① 적용.
- **우선순위 제안**: **이번 주.**

### G-6. STEP 1 채점 커버리지를 일일 수집기 §5에 상설 편입

- **관측 근거**: 1-2 — 10m·15m 채점 결손이 **최소 3거래일(08-12·14·18) 동일 수치로 안정**돼 있었는데 오늘 처음 드러났다. 매일 도는 수집기가 보지 않는 축이었기 때문이다.
- **제안 내용**: `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py` §5(거래일 요약)에 **"STEP 1 채점 커버리지"** 표 1개 추가 — 호라이즌별 채점 건수 / 경과분 대비 비율 / 전일 대비 델타. 계산은 `_LEARNING.log`의 `✓|✗ {h} 예측` 카운트뿐이라 **DB 접근 없음**(장중 안전).
- **기대 효과**: 채점 파이프의 호라이즌 편중을 매일 자동 노출. 임계는 **걸지 않는다**(섀도 관찰). 26주 WFA 시 `invariants.md` §5 "고착 지표 감시목록"과 함께 재검토.
- **비용·위험**: 소. 수집기는 표준 라이브러리만 쓰므로 환경 무관.
- **선행 조건**: 없음(F-6과 독립 — F-6은 원인, 이것은 상시 계측).
- **우선순위 제안**: **이번 주.**

### G-7. IntradayRegime 전이 히스테리시스 — **섀도 계측 먼저, 집행 변경 금지**

- **관측 근거**: 1-4 O-G — 오늘 반일 **42회** 전이(08-14 19 / 08-18 22 종일 대비 2배). `day`가 임계 −1.00% 근방에서 진동하며 11:13→11:14→11:15→11:16 **4분 연속 4회** 뒤집혔다(`logs/20260819_SIGNAL.log`).
- **제안 내용**: **1단계 — 계측만.** `scripts/intraday_regime_flap_watch.py` 신설(읽기 전용, `_SIGNAL.log` 파싱). 일자별 ① 전이 횟수 ② 체류시간 중앙값 ③ 전이 시점 `day` 값의 임계 대비 거리 분포를 낸다.
  **사전등록 판정문**: *"전이 시점 `day`의 임계 대비 |거리| 중앙값이 0.10%p 미만인 상태가 5거래일 연속이면 히스테리시스(또는 최소 체류시간) 도입을 주간회의 안건으로 올린다. 그렇지 않으면 현행 유지."* ⚠ 임계는 지금 등록하고 이후 움직이지 않는다(313차 ④).
- **기대 효과**: "레짐이 자주 바뀐다"는 인상을 판정 가능한 수치로 바꾼다. **지금은 실해가 확인되지 않았다** — `[Sizer]` 6회 전부 `레짐배수=0.8` 고정이고 `min_conf`에도 전파되지 않았다(C-3).
- **비용·위험**: 소. ⚠ **집행(차단·등급·배수)에 연결 금지** — 섀도 관찰 후 승격(스킬 §7-7).
- **선행 조건**: 없음.
- **우선순위 제안**: 이번 주(계측만).

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| STEP 1 채점 커버리지 | 미등록 | **26주 WFA 재검증 항목에 편입 제안** (455차 "호라이즌 방향예측 피처셋 재검증"과 같은 절) | 호라이즌별 학습 피드백 밀도는 IC 드리프트의 **선행 조건**이다. 가중 57%를 지는 축이 5~8배 희소하게 배우는 상태는 피처셋 재검증의 전제를 흔든다 |
| 실전전환 ② (CB 정상작동) | `F-1R` 리허설 미실행 | **변동 없음** — 오늘도 진입 3건 전부 15:10 이전 종료 예정 | 자연 발생을 기다리는 경로가 아님이 재확인됨 |
| 실전전환 ⑤ (CB② 복원) | 재검토 **2026-08-29** | **잔여 10일 → 오늘 8일**(영업일 기준 6일). 상정 시 **360차의 "포지션 하나가 손실 이벤트 2회" 사실을 반드시 함께 올릴 것** — 오늘 11:11 포지션 1건이 카운터를 2까지 올린 것이 그 실례다 | 한도를 2~3으로 되돌리면 **단일 포지션의 계단식 손절만으로 당일 정지**가 성립한다. CLAUDE.md 절대원칙 §2의 문구("5분 내 손절 3연속")와 코드(`safety/circuit_breaker.py:206-210` — **시간창 없음**, 승리 레그로만 리셋)의 차이도 같은 자리에서 정리 필요 |

---

## 4. 진입 승패 사후검증

**해당 없음(장후 전용).** — 다만 장후 세션이 이어받도록 장중 관측치를 남긴다.

- 진입 2건 등록(`[Position] 진입`) / 진입체크 통과 3건 / **레이스로 `[Position] 진입` 미기록 1건(09:49)** → 3원 대사 시 **로그 기준 2 vs `ensemble_decisions` 기준 3**으로 어긋날 것이 예상된다. **이 불일치는 P1 1-1의 결과이며 새로운 이상점이 아니다.**
- 청산 3건 · 승 2 (67%) · 합계 **+1.38pt (+64,417원)**
  | 시각 | 방향 | PnL(pt) | PnL(원) | 사유 |
  |---|---|---|---|---|
  | 09:57:22 | LONG | +1.03 | +50,139 | 하드스톱(틱) — ⚠ **TP1 보호 이익 청산**(09:53:13 TP1 도달 후 스톱 상향). `exit_reason`만으로 패로 세지 말 것 |
  | 11:21:33 | SHORT | −2.89 | −146,192 | 하드스톱(틱) — **진짜 손실** |
  | 11:42:02 | SHORT | +3.24 | +160,470 | TP2(전량) |
- **손절 준수율**: 11:11 SHORT 의도 손절폭 = `1019.66 − 1016.68 = 2.98pt`, 실현 `−2.89pt` → **0.97배(초과 없음)**. 09:49 LONG은 `[Position] 진입` 부재로 **계산 불가**(1-1 영향 ②).

## 5. 수익률 향상방안

**해당 없음(장후 전용).**

---

## 6. 다음 관측 예정 (오늘 장후 / 내일)

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| O-1 | 09:49 포지션의 `trades.entry_horizon` · `ensemble_decisions` 09:49 행 | NULL/미기록이면 1-1 영향 ③ 확정 | 2026-08-19 장후 |
| O-2 | 오늘 종일 `[FixB] open_position 실패` 추가 발생 | 1건 유지면 단발, 2건 이상이면 F-5 우선순위 상향 | 2026-08-19 장후 |
| O-3 | `ofi_norm` identity 강제 **종일** 비율 (3거래일째) | 08-18의 94%를 넘는가 / 15:50 E_EOD에서도 걸리는가 | 2026-08-19 장후 |
| O-4 | IntradayRegime 종일 전이 횟수 | 08-18의 22회 대비 2배 이상이면 G-7 계측 착수 | 2026-08-19 장후 |
| O-5 | 15:10 강제청산 경로 — `[ForceExitPass]`→`[TimeExit]`→`[ExitAttempt]` | 오늘도 15:10 이전 전량 청산이면 **⑤ 미해소 유지**. `[SchedForceExit] 안전망 발동`(ERROR) 출현 시 P0 | 2026-08-19 장후 |
| O-6 | 15:40 `[BrokerPnl] EOD 확정 — gross … − 수수료 … = net …` (G-D) | 토큰 출현 여부 | 2026-08-19 장후 |
| O-7 | `ensemble_decisions.fp_psi` 비-NULL 행수 = 당일 행수 (G-A/G-B) | 일치하면 477차 F-5 라이브 확정 | 2026-08-19 장후 |
| O-8 | STEP 1 채점 커버리지 (F-6 1단계 스크립트) | 08-12/14/18/19 표 재현 여부 | 2026-08-20 |
| O-9 | `[ModelLive]` 10m·15m **5일차** | 어제 20.8%/21.3%. ⚠ n 상한이 구조적(72/47)임을 전제로 읽을 것 | 2026-08-19 장후 |

## 7. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| CB② 복원 (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | 2026-08-29 | **10일**(영업일 8일) | 오늘 `9999` 유지 확인. 08-29 상정 시 **360차 "포지션당 손실 이벤트 최대 2회"** 와 **코드에 시간창이 없다**(`circuit_breaker.py:206-210`)는 사실을 함께 올릴 것 — §3 표 참조 |
| DD-3 ghost_bypass 범위 | 2026-08-26 | 7일 | 변동 없음 |
| `v9-dev` ← `dev` 315커밋 정렬 | 주간회의 | — | 불변식 `미발견` 5행의 근원. 오늘도 재현 |

---

## 8. dev_memory 반영

- `DECISION_LOG.md` 추가: `## 2026-08-19 (MW0601 478차 후속 — 장중 점검)`
- `NEXT_TODO.md` 추가 체크박스: 9건 (F-5·F-6·F-7 / G-5·G-6·G-7 / O-1·O-2·O-8)
- 완료 처리한 기존 항목: 없음
- **커밋 0건** — 장중이므로 `git commit`·`git push` 하지 않았다.

---

## 자체 검증

- [x] 라이브 DB에 분석 쿼리를 돌리지 않았다 — 로그·설정·`git` 메타·소스 읽기만. `sqlite3` 호출 0건
- [x] 코드를 변경·재기동하지 않았다 — `git status` 변동 없음(수집기 산출물 2개 신규 파일만)
- [x] 모든 이상점에 로그 시각·파일명·인용이 붙었다
- [x] `conf` 절대값을 CLAUDE.md 구 "확률 판단 기준" 표에 대보지 않았다 — 오늘 `min_conf`(DynMC `base=0.401`, 점심구간 zone 0.65) 대비 상대 위치로만 서술. 붕괴행(`weight_collapsed`, 45/207 = 21.7% — 문서 기재 21~22%와 일치)은 평균 집계에서 제외
- [x] dev_memory 기존 항목을 중복 보고하지 않았다 — §1-4·"이미 반영된 사안"으로 분리
- [x] 재인용 금지 수치 미사용: 2026-06-25 SHAP=0 / 0801 §9-3 이벤트 단위 사이징 4종 / CB③ "35%" / 417차 "379건 중 86건" / 476차 §3 "133건·29.6%"
- [x] 표본 부족 항목에 확정 결론 없음(313차): 1-1 레이스 원인(n=1) · 1-2 원인 후보(미확정) · C-1·C-3
