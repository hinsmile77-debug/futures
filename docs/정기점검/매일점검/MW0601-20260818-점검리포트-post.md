# 미륵이 일일 점검 — MW0601 / 2026-08-18 / 장후(post)

- 점검 시각: 16:22 KST · PC `MW0601` · 브랜치 `v9-dev` (규약 일치)
- HEAD `7dc14bc` (474차) · 당일 커밋 **0건** · 미커밋 458건(⚠ CRLF 아티팩트 — §1 오탐 정정 참조)
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260818_post.md`
- 선행 세션: 475차(intra, 13:49) · pre(13:44, 리포트 부재)
- 수집기 실행: **성공**. 단 리포 판 `collect_evidence.py`에 `--pc` 인자가 없어(스킬 캐시본과 세대 차이)
  `platform.node()`를 `DeskTop-MW0601`로 주입해 실행했다. 산출물 PC명은 정상(`MW0601`).

---

## 0. 한 줄 결론

**P0 없음.** 오늘 미륵이는 절대원칙 6종을 모두 지켰고 포지션 단위 **+661,668원 / 7전 6승(85.7%)**
으로 마감했으며 EOD 재학습도 6/6 호라이즌 교체에 성공했다 — 다만 **손익을 낸 것은 방향예측이 아니라
청산 규칙이었고**(당일 라이브 방향적중률 1m 34.8% / 3m 29.5% / 5m 37.2%), 그 청산 규칙은 강한 추세일에
가용 이익의 일부만 실현했다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | **조건부** | 08:40 자동 기동·매크로·구독 정상. 단 장전 점검 세션 자체가 13:44에 돌아 기능하지 않았다(O-7/O-H, 기등록) |
| 장중 | 정상 | 매분 루프 커버리지 09:00~15:10 **371/371분 100%**, 10분 이상 공백 0건. 크래시 0 |
| 장후 | 정상 | 15:10 FLAT·안전망 미발동 / 15:40 daily_close / 15:45~15:50 EOD 재학습 6/6 교체 |

**절대원칙 준수 여부**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | ✅ **지켜짐(무포지션)** | 최종 청산 13:19:39. `15:11:21 [SchedForceExit] 15:11 점검 — status=FLAT engine=0ct broker_cached=0ct bar_pass=1회 → 청산 대상 없음(정상)`. 안전망 ERROR **미출현** |
| ② | Circuit Breaker 5종 | ✅ 설계대로 | CB② 카운터 2회(정지 없음=한시예외) · CB③-P4 단계전환 6회 · CB⑤ 경고 5분(전부 <5,000ms) · CB④/① 무발동 |
| ③ | CORE 피처 호라이즌 그룹별 | ✅ 일관 | 진입 7건 `entry_horizon` = 1m×1 / 3m×2 / 5m×4 — **전부 단기군**. 474차가 확정한 mid/long 도달 불가와 일치 |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | ✅ | `crash_fault.log` 신규 기록 없음, 0xC0000409 0건, 정상 종료 플래그 2회 기록 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | ✅ | TR 수신 오류 0건. `CybosInvestorRaw` 1,574건 정상 파싱 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | ✅ | 당일 자동 통합·자동 백테스트 큐 로그 0건 |

**설정 불변식** — 수집기 §3에서 `미발견` 5종(`MODEL_LABEL_STATE_UNLOCK_ENABLED` ·
`PRE_RETRAIN_DONE_BY_EOD_ENABLED` · `ZONE_ENTRY_BAN_ENFORCE` · `ZONE_ENTRY_BAN_SHADOW_ENABLED` ·
`PIPE_LATENCY_EXCLUDE_MODEL_SWAP`). **전부 브랜치 격차**(`v9-dev` ↔ `origin/dev` = 320/315)가 원인이며
473차 후속2가 이미 확정했다 — **신규 아님**, 주간회의 안건으로 계류 중. 나머지 21종 전부 `일치`.

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**해당 없음.**

---

### P1 — 정확성·관측 훼손

#### 1-1. DriftAdjuster가 6거래일 연속 `alpha 0.01000→0.01000` — SGD 자기적응 루프가 상한에서 정지

- **증상**: 매 EOD `daily_close()`에서 "3일 연속 정확도 50% 미만 → alpha 상향"이 발동하는데
  **alpha가 한 번도 바뀌지 않는다.** 조정기가 매일 WARNING을 남기지만 실제 조정량은 0이다.
- **근거**:
  ```
  2026-08-10 15:40:17 [WARNING] LEARNING: [DriftAdjuster] 3일 연속 정확도 50% 미만 → alpha 0.01000→0.01000
  2026-08-11 15:40:23 [WARNING] LEARNING: [DriftAdjuster] 3일 연속 정확도 50% 미만 → alpha 0.01000→0.01000
  2026-08-12 15:40:19 [WARNING] LEARNING: [DriftAdjuster] 3일 연속 정확도 50% 미만 → alpha 0.01000→0.01000
  2026-08-13 15:40:18 [WARNING] LEARNING: [DriftAdjuster] 3일 연속 정확도 50% 미만 → alpha 0.01000→0.01000
  2026-08-14 15:40:16 [WARNING] LEARNING: [DriftAdjuster] 3일 연속 정확도 50% 미만 → alpha 0.01000→0.01000
  2026-08-18 15:40:22 [WARNING] LEARNING: [DriftAdjuster] 3일 연속 정확도 50% 미만 → alpha 0.01000→0.01000
  2026-08-18 15:40:22 [INFO]    LEARNING: [DriftAdjuster] acc=15.7% → alpha=0.01000 (DRIFT_UP) | 이력=['35%', '31%', '29%', '10%', '16%']
  ```
  `logs/2026081*_LEARNING.log` · **6/6 거래일 전부 동일**. 기동 로그도 10일 내내
  `로드: alpha=0.01000, 이력 10일, 마지막 액션=DRIFT_UP`.
- **원인(코드 확인)**: `learning/self_learning/drift_adjuster.py:28` `ALPHA_MAX = 0.01`.
  `_adjust_alpha()`가 `min(self._alpha * 1.5, ALPHA_MAX)`로 클램프하므로 **이미 상한**이다.
  반대 방향도 막혀 있다 — `RECOVERY_THRESHOLD = 0.58`인데 실측 SGD 일일 정확도는
  10일 최대 **35%**(daily_stats.sgd_accuracy: 08-12 30.9% / 08-13 28.7% / 08-14 10.0% / 08-18 15.7%)라
  회복 경로도 도달 불가다. → **양방향 모두 도달 불가 = 조정기가 상수 반환기가 됐다.**
- **기준 위반**: 계측 4원칙 **④ 폴백 가시화** — "기본값(포화값)이 쓰였으면 그 사실을 남겨라".
  현재 로그는 `0.01000→0.01000`이라는 **형식적 조정문**을 남겨 조정이 일어난 것처럼 읽힌다.
  또한 매분 9단계 **STEP 2(SGD 온라인 자가학습)** 의 적응 축이 실질 정지 상태다.
- **영향**: 학습률이 고정되므로 SGD가 시장 변화에 더 빠르게 적응할 여지가 없다.
  손익에 즉각적 위험은 없으나(오늘 흑자는 청산 규칙이 만든 것) **자가학습이 작동한다는 전제가
  6거래일째 사실이 아니다.** FP-CRITICAL·TOX-SEVERE-SPREAD와 동일한
  "형식은 살아 있고 실질은 죽은" 패턴이다.
- **신규 여부**: **신규.** `dev_memory/DECISION_LOG.md`·`NEXT_TODO.md`의 DriftAdjuster 항목 4건은
  전부 "표본 부족 스킵"(D68 등) 계열이며 **포화(saturation)는 어디에도 없다.**

#### 1-2. TP1 보호전환 섀도 채널 2종의 모집단이 소멸 — 08-13 이후 0행

- **증상**: `tp1_trail_shadow`·`synthetic_partial_exits` 두 테이블이 **2026-08-12를 마지막으로
  신규 행이 없다.** 그 사이 08-14 1포지션 · 08-18 7포지션이 TP1에 도달했는데도 0행이다.
- **근거**:
  ```
  tp1_trail_shadow          : 총 68행, 기간 2026-07-22 ~ 2026-08-12 (08-12: 3행) · 2026-08-18: 0행
  synthetic_partial_exits   : 총 77행, 기간 2026-07-20 ~ 2026-08-12 (08-12: 3행) · 2026-08-18: 0행
  tp2_hold_shadow           : 총 0행 (사상 0)
  ```
  `data/db/trades.db` (읽기전용 조회, 15:35 이후 — 장중 DB 분석 금지 구간 아님)
- **원인(코드 확인)**: 두 훅은 `main.py:13067 self._record_tp1_trail_shadow(...)` 위치상
  **qty=1 전용 경로**에 있다(주석: *"qty=1은 TP1 이후 4단계 트레일링 대신 static ATR-lock
  1회 보호전환만 받는데 …"*). qty≥2는 실제 부분청산 경로로 빠져 훅을 통과하지 않는다.
  그런데 **진입 수량 분포가 바뀌었다** — `trades.entry_qty` 일자별:
  ```
  08-10  qty1×7  qty2×1  qty3×2
  08-11          qty2×3
  08-12  qty1×1  qty2×4  qty3×1     ← qty=1 마지막 등장
  08-14          qty2×1
  08-18          qty2×7             ← 100% qty=2
  ```
  즉 **버그가 아니라 모집단 소멸**이다. 431차(`MAX_CONTRACTS` 10→3 + 품질군 min() 합성)
  이후 사이징이 2계약으로 수렴하면서 qty=1이 사라졌다.
- **기준 위반**: 계측 4원칙 **② 미측정 ≠ 0** / **③ 탈락 가시화**.
  `VALIDATION_CAMPAIGN["tp1_trail_shadow"]["min_samples"] = 15`는 표본이 **더 이상 오지 않는데도**
  "적립 중"으로 보이며, 리포트가 이를 `INSUFFICIENT`로만 표시해 **정지 사실이 드러나지 않는다.**
  473차 F-8이 TOX-SEVERE-SPREAD에서 확인한 "기다리면 닫히는 게이트가 아니다"와 같은 구조다.
- **영향**: TP1 직후 보호전환이 너무 타이트한지를 재는 유일한 계측 경로가 죽었다.
  하필 오늘이 그 질문이 가장 크게 걸린 날이다(§5 R-1 참조 — 실현 6.85pt/ct vs 10분 MFE 43.2pt/ct).
- **신규 여부**: **신규.** `NEXT_TODO.md:4681` [363차 후속]은 "**첫** 기록 확인"이고,
  `:3630` [402차 후속5]는 [22]×[12] 교차해석이다 — **모집단 소멸은 미등록.**

---

### P2 — 운영 부담·기술부채

#### 1-3. `daily_broker_pnl`이 **총손익(gross)** 인데 컬럼·표기에 단위가 없다 — O-D 확정

- **증상**: 브로커 기준 +685,000원과 엔진 기준 +661,668원의 23,332원 차이(475차 O-D, "추정하나 미검증").
- **근거(원 단위 일치 확인)**: `trades` 14레그 `gross_pnl_krw` 합 = **685,000원**,
  `commission_krw` 합 = 2×(1,699+1,695+1,675+1,670+1,641+1,647+1,639) = **23,332원**,
  `net_pnl_krw` 합 = **661,668원**. 685,000 − 23,332 = 661,668 — **정확히 일치**.
  → `daily_broker_pnl.pnl`은 **수수료 차감 전 gross**다. 미결제 시차가 아니다.
- **기준 위반**: 계측 4원칙 **① 단위 명시** — `_per_leg`/`_per_position`과 같은 취지로
  `_gross`/`_net`이 이름에 없다.
- **영향**: 대시보드·리포트에서 두 값이 나란히 보이면 매일 23,332원(오늘 기준 net의 3.5%)의
  설명 불가 격차가 생긴다. 실전 전환 기준 ①(4주 통산 수익률 양수) 판정 시 **어느 쪽을 쓰는지가
  결과를 바꾼다.**
- **신규 여부**: 격차 자체는 O-D로 기등록(미검증) — **본 세션에서 원인 확정.**

#### 1-4. RegimeFingerprint PSI의 정상 구간(CLEAR/WATCH)이 어디에도 시계열로 남지 않는다

- **증상**: 당일 로그 전체에서 `PSI` 문자열 **0건**. 그러나 죽은 것이 아니다 —
  `data/daily_reports/strategy_report_20260818_154023.txt`에
  `PSI : 0.008 (CLEAR)` / `PSI/feat: cvd=0.142 vwap_position=0.008 ofi=0.005`가 정상 출력된다.
- **원인(코드 확인)**: `main.py:6071~6086`이 **`PSI > 0.30`(CRITICAL) 또는 `> 0.20`(ALARM)일 때만**
  로그를 남긴다. 예외는 `logger.debug`로 삼켜지고(`main.py:6098`), DEBUG 채널에는
  `[DBG-F*]` 전용 로거만 실리므로 **DEBUG 로그에도 안 남는다**(당일 `RegimeFingerprint` 0건 확인).
  EOD 스냅샷 1개(`fp.get_psi()`)만 하루에 한 번 남는다.
- **기준 위반**: 계측 4원칙 **② 미측정 ≠ 0** — "PSI가 하루 종일 조용했다"와 "update_live가 매분
  예외로 죽었다"를 로그만으로 구분할 수 없다(오늘은 EOD 값이 비영이라 살아 있음이 확인됐을 뿐).
- **영향**: **실전 전환 기준 ⑦의 해제 요건이 "정상 구간에서 PSI가 오르내리는 것을 실측 확인"인데,
  그 실측에 쓸 시계열이 하루 1점씩만 쌓인다.** 371차 분위수 재설계 후 오늘 PSI=0.008(CLEAR)은
  ⑦ 방향으로 좋은 신호이므로, 지금이 계측을 붙일 시점이다.
- **신규 여부**: O-C가 "0건이면 P1 승격"으로 등록돼 있었으나 **전제가 달랐다** — 죽은 게 아니라
  임계 미만이라 안 찍힌 것이다. **O-C는 P1이 아니라 본 항목(P2)으로 대체한다.**

#### 1-5. 대시보드 진입모드가 최소 08-03 이래 `manual`(A/B/C 전면 허용) 고정

- **근거**: `ensemble_decisions.entry_mode` — 진입 성사행 기준 08-03(24) · 08-05(19) · 08-07(11) ·
  08-10(10) · 08-12(6) · 08-14(1) · **08-18(7) 전부 `manual`**. `entry_mode_fallback` 370/370행 `0`
  (471차 후속4 F-9의 폴백이 아니라 **실제 설정값**이다).
  `main.py:8371` `allowed_grades = {"auto":["A"], "hybrid":["A","B"], "manual":["A","B","C"]}`.
- **영향**: 오늘은 최종등급이 7건 모두 A라 **실해 없음**. 다만 등급 필터가 사실상 무효화된 상태이며,
  실전 전환 시 이 값이 무엇이어야 하는지가 어느 체크리스트에도 없다.
- **신규 여부**: 신규(관측). 정책 결정 사항이므로 코드 수정 제안이 아니라 **주간회의 안건**으로 올린다.

---

### 확인 필요 (확정 아님)

- **10m·15m 당일 라이브 적중률 급락** — `[ModelLive]` 08-18: 10m **20.8%(n=72)** · 15m **21.3%(n=47)**.
  직전 2거래일은 10m 35.2%/37.1%, 15m 29.8%/32.6%였다. CLAUDE.md 474차 항목 기준 이 둘의
  앙상블 가중 합은 **약 57%**다. 다만 **n=72/47 · 관측 3거래일**뿐이므로
  **313차 원칙에 따라 확정 결론 보류.** → 무엇을 더 보면 판정되는가: `[ModelLive]` 5거래일 누적과
  `[51] ConstOut 호라이즌 건강도` 채널(471차 후속7)의 동일 기간 교차.
- **`quantile_expected_pt`의 부호가 승패와 역방향으로 보인다** — 7건 중 진 1건(13:06)이
  **가장 큰 음수(-0.672)** 였고, 가장 크게 이긴 11:39건은 -0.002였다. 부호 규약(가격 기준인지
  포지션 기준인지)을 코드에서 확인하기 전에는 해석 불가. **n=7, 판정 금지.**
- **`GuardGhost` 4건(3m·5m)** — "배포 pkl이 CV 미검증 intraday 모델이라 acc.txt는 존재하지 않는
  모델의 성적"이라 가드를 건너뛰고 교체. 457차 F7 ⑤안의 **설계된 동작**이지만, 장중 재학습이
  잦은 날일수록 EOD 가드가 무력화되는 구조다. 08-11~08-14에도 반복(6/12/12/2건).
  → 무엇을 더 보면 판정되는가: `GuardGhost` 발생일 vs 익일 라이브 적중률의 5거래일 대조.

### 이미 반영된 사안 (신규 아님) — 함정 ① 방지

| 오늘 눈에 띈 것 | 이미 어디에 있는가 |
|---|---|
| CB③ acc30m이 12:39:59 **3.3%** 까지 내려갔는데 HALT 없음 | **의도된 비활성.** `safety/circuit_breaker.py:315~331` — `if acc < effective_min:` 분기가 `logger.debug("[CB③ 비활성] …")`만 남긴다(296차 30m 전면 퇴역). 문서 등재는 `NEXT_TODO` **F-2**로 기등록 |
| CB② 연속 손절 카운터 1·2회(13:08:37 · 13:19:39), 정지 없음 | `CB_CONSEC_STOP_LIMIT = 9999` 한시예외(CLAUDE.md 절대원칙 §2). 재검토 기한 **2026-08-29(11일 뒤)** |
| ProfitGuard L1-Trail 13:19:59 발동 → 15:09:59까지 진입 차단 | 475차 intra **[5]** 및 `NEXT_TODO` **G-1**(기회비용 섀도 채널)로 기등록. 오늘 실측 확정치는 아래 §3 G-2에 승계 |
| 수집기 §5 손익 **+269,334원** | **레그 단위 과소 표기.** `NEXT_TODO` **F-1**(수집기 포지션 단위 집계, 장후 적용 예정)로 기등록 |
| 설정 불변식 `미발견` 5종 | 브랜치 격차 320/315 — 473차 후속2 확정, 주간회의 계류 |
| 미커밋 458건 | 리눅스 샌드박스 **CRLF 아티팩트**. `NEXT_TODO` **F-5** 기등록. 실제는 `--ignore-cr-at-eol` 기준 10파일 |
| 장전 점검 13:44 실행·리포트 부재 | O-7 답 확정(475차) → **O-H**로 승계, G-3(세션 직렬화) 기등록 |

### 오탐 정정 — 수집기 §11 자동 적신호

| # | 적신호 | 판정 |
|---|---|---|
| 6 | "청산 7건 중 하드스톱·손절 계열 **100%** — 손절 준수율 확인 필요" | **오탐.** `하드스톱` 7레그 중 **6레그가 이익 청산**이다(+1.55/+2.13/+1.30/+1.42/+0.17/+2.38pt). TP1 33% 부분청산 후 보호전환된 스톱이 `하드스톱`으로 기록되는 알려진 라벨 혼입(스킬 §0 2-6). **진짜 손절은 13:07 포지션 1건뿐.** |
| 8 | "블로킹 5초 초과 5건(최대 37,875ms) — CB⑤ 발동 여부 확인" | **CB⑤ 미발동이 정상.** ⓐ 5건 전부(12:17:12 · 12:22:03 · 12:30:04 · 13:28:36 · 14:21:03) **포지션 FLAT 구간**이다(보유 구간은 10:20~11:42, 13:07~13:19). ⓑ CB⑤가 재는 것은 `_tick_header` 간격이 아니라 파이프라인 총시간이며 당일 최대 **2,826ms** < `CB_PIPE_PAUSE_MS=5,000`. → **O-F 답: 포지션 보유 중 ≥5초 블로킹 0건 (P0 아님).** |
| 14 | "미커밋 변경 458건" | CRLF 아티팩트 — 위 표 참조 |
| 15 | `data/_exit_normally` 없음 | 정상. `15:40:23`·`15:40:38` 두 번 기록됨(로그 교차확인 완료) |

---

## 2. Fix 작업 구현계획

> **본 세션은 코드를 변경하지 않았다.** 아래는 계획이며, 사용자 지시 후 별도 세션에서 구현한다.

### F-1. DriftAdjuster 포화 가시화 + 회복 임계 재검토 — **P1** · 대응 이상점 1-1

- **원인 가설**: `ALPHA_MAX=0.01`이 이미 도달됐고 `RECOVERY_THRESHOLD=0.58`이 실측 정확도 분포
  (10일 10~35%) 밖이라 양방향 조정이 모두 불가능하다. 로그가 그 사실을 표현하지 않는다.
- **변경 파일**
  - `learning/self_learning/drift_adjuster.py: _adjust_alpha()` —
    `new == old`이면 액션을 `DRIFT_UP`이 아니라 **`SATURATED_MAX`**(회복측은 `SATURATED_MIN`)로
    반환하고, 로그 문구를 `alpha 0.01000 유지 — ALPHA_MAX 포화 (연속 N일)`로 바꾼다.
    포화 연속일수 `_saturated_days`를 상태에 추가해 `data/drift_adjuster_state.json`에 영속한다.
  - `learning/self_learning/drift_adjuster.py: get_state()` — 대시보드 조회용 dict에
    `saturated: bool`, `saturated_days: int` 추가.
  - **⚠ 이번 커밋에서 `ALPHA_MAX`·`RECOVERY_THRESHOLD` 값 자체는 바꾸지 않는다.**
    값 변경은 학습률 정책 변경이며 사전등록 절차(§9)를 거쳐야 한다 — F-2로 분리.
- **회귀 위험**: 낮음. 반환 액션 문자열을 소비하는 곳(`main.py:11245` 대시보드 표시)이
  `DRIFT_UP` 문자열을 하드코딩 비교하지 않는지 확인 필요.
- **절대원칙 저촉**: 없음(6종 무관).
- **런타임**: `py37_32` 라이브 경로(`daily_close()`). COM 콜백 외부.
- **검증**: `pytest tests/` 신규 `test_476_drift_saturation.py` — alpha=ALPHA_MAX 상태에서
  `record_accuracy(0.2, n=100)` 호출 시 액션이 `SATURATED_MAX`이고 로그에 `포화`가 포함되는지.
  라이브는 **다음 거래일 15:40 로그에 `포화 (연속 7일)`** 출현 여부.
- **섀도 계측 필요**: 불필요(로그·상태 표기만, 집행 무변경).
- **적용 시점**: 즉시(장후, 라이브 프로세스 15:40:38 종료 확인됨).
- **결정 필요 사항**: 없음.

### F-2. SGD 학습률 정책 재검토 안건화 — **P1(결정)** · 대응 이상점 1-1

- **내용**: `ALPHA_MAX`(0.01) 상향 또는 `RECOVERY_THRESHOLD`(0.58) 현실화를 **주간회의 안건**으로
  올린다. 근거는 F-1이 만든 `saturated_days` 시계열이 2주 쌓인 뒤에 붙인다.
- **변경 파일**: `config/settings.py` 아님 — 우선 `dev_memory/NEXT_TODO.md` 안건 등록만.
- **왜 지금 값을 안 바꾸는가**: 학습률 상향은 SGD 예측 분포를 직접 흔들고, 그 예측이
  앙상블·DynMC 임계·CB③ 입력으로 전파된다. **사전등록 없는 임계 변경은 458차 D6과 같은 위반**이다.
- **적용 시점**: 2026-08-29 주간회의(CB② 복원 안건과 동일 회차).

### F-3. TP1 보호전환 섀도를 qty≥2로 확장 — **P1** · 대응 이상점 1-2

- **원인 가설**: 훅이 qty=1 전용 분기에만 있고, 사이징이 qty=2로 수렴해 모집단이 사라졌다.
- **변경 파일**
  - `main.py: _ts_record_tp1_trail_shadow()` — 호출 지점을 qty=1 분기(현 `:13067`) 외에
    **부분청산 실행 직후(`ratio = PARTIAL_EXIT_RATIOS[stage-1]` 경로의 stage==1 완료 지점)** 에도
    추가하고, 테이블에 `entry_qty`·`hook_path`(`qty1_static` / `qty2plus_partial`) 컬럼을 신설한다.
  - `utils/db_utils.py` — `tp1_trail_shadow` 스키마 마이그레이션(ALTER TABLE ADD COLUMN, NULL 허용).
  - `config/settings.py: VALIDATION_CAMPAIGN["tp1_trail_shadow"]` — 판정을 `hook_path`별로 분리
    (기존 qty=1 표본과 신규 qty≥2 표본을 **섞지 않는다** — 청산 메커니즘이 다르다).
- **회귀 위험**: 중간. qty≥2 경로는 실제 부분청산이 일어나므로 `protect`/`atr` 인자의 의미가
  qty=1과 다르다 — **인자 의미가 다른 채로 같은 테이블에 넣으면 계측 4원칙 ① 위반**이라
  `hook_path` 컬럼이 필수다. 기존 68행은 전부 `qty1_static`으로 백필한다.
- **절대원칙 저촉**: 없음. **집행 무변경**(기록만).
- **런타임**: `py37_32` 라이브. COM 콜백 외부(청산 처리 경로).
- **검증**: 다음 거래일 TP1 도달 시 `tp1_trail_shadow`에 `hook_path='qty2plus_partial'` 행 생성 확인.
  주간 리포트 `[12]` 채널이 두 경로를 분리 출력하는지.
- **섀도 계측 필요**: 본 항목 자체가 섀도 계측 복구다.
- **적용 시점**: 장후(다음 기동 전).
- **결정 필요 사항**: qty=1 기존 68행 표본을 `min_samples=15` 판정에 계속 쓸지
  (권고: **쓴다. 단 qty1 경로 한정 판정으로 문구를 좁힌다.**)

### F-4. `daily_broker_pnl` 단위 명시 — **P2** · 대응 이상점 1-3

- **변경 파일**
  - `utils/db_utils.py` — `daily_broker_pnl`에 `pnl_gross_krw`(기존 `pnl` 별칭) ·
    `commission_krw` · `pnl_net_krw` 3컬럼으로 확장. 기존 `pnl`은 하위호환 별칭으로 남기되
    **판정·리포트에서는 쓰지 않는다**(461차 `mdd_pct` 처리와 동일 관례).
  - `strategy/ops/daily_exporter.py` — 리포트에 `당일 broker(gross) / engine(net) / 수수료` 3행 표기.
- **회귀 위험**: 낮음(추가 컬럼). 대시보드가 `pnl`을 읽는 지점 확인 필요.
- **검증**: 다음 거래일 EOD에 `gross − commission = net`이 원 단위로 맞는지 자동 assert 로그.
- **적용 시점**: 장후.

### F-5. RegimeFingerprint PSI 매분 값 기록 — **P2** · 대응 이상점 1-4

- **변경 파일**
  - `main.py:6059~6098` — 레벨 무관 5분 간격 하트비트 1줄
    (`[RegimeFingerprint] PSI=%.3f %s (heartbeat)`) 추가 + `except` 를 `logger.debug` →
    `logger.warning`(5분 스로틀)로 승격. **미측정과 0을 구분한다**(계측 4원칙 ②).
  - `data/db/predictions.db: ensemble_decisions` — `fp_psi REAL` · `fp_level INTEGER` 컬럼 추가
    (473차 F-8 Phase B가 `spread_ticks`를 붙인 것과 같은 방식·같은 이유).
- **회귀 위험**: 낮음. WARN 로그 볼륨은 5분 스로틀로 하루 최대 ~74줄.
- **절대원칙 저촉**: 없음. **`FP_CRITICAL_GRADE_BLOCK_ENABLED`는 `False` 그대로 둔다**(집행 무변경).
- **검증**: 다음 거래일 `logs/*_SYSTEM.log`에 heartbeat 74±5줄, `ensemble_decisions.fp_psi` 비-NULL 370행.
- **적용 시점**: 장후. **실전 전환 기준 ⑦의 선행 계측**이므로 F-3과 같은 우선순위.

### 적용 순서와 커밋 계획

1. `[MW0601] 476차: DriftAdjuster 포화 가시화 + PSI 매분 계측` — F-1, F-5
2. `[MW0601] 476차 후속: TP1 보호전환 섀도 qty≥2 확장 (스키마)` — F-3
3. `[MW0601] 476차 후속2: daily_broker_pnl gross/net 단위 명시` — F-4
4. 문서: F-2 안건 등록 + 1-5 주간회의 안건

> ⚠ 475차의 미커밋 산출물(pre/intra 리포트·다이제스트·dev_memory)과 08-17 세션의 미커밋 10파일이
> **아직 대기 중**이다. 위 커밋보다 **먼저** 정리해야 이력이 섞이지 않는다.

---

## 3. 고도화 방안

### G-1. 추세일 조기 절단 감시 채널 `trend_day_early_exit_watch` (섀도)

- **관측 근거**: 오늘은 진입 7건 **전부 SHORT · 전부 방향 적중**, 장중 레인지 73.1pt
  (5일평균 32.3pt), 15:05 종가 1073.18로 첫 진입가 대비 **-59.3pt**. 그런데 포지션당 평균 보유
  **1.4분**, 실현 **6.85pt/계약**(포지션 합 13.70pt ÷ 2계약). 같은 진입 시점의 사후 최대유리폭(MFE):
  | CASE | 진입 | 실현/ct | MFE+10분 | MFE+20분 | MFE+30분 |
  |---|---|---|---|---|---|
  | 01 | 10:20:59 | 1.22 | 1.77 | 4.75 | 17.87 |
  | 02 | 10:39:59 | 1.27 | **13.47** | 18.91 | 18.91 |
  | 03 | 10:49:59 | 1.57 | 5.98 | 5.98 | 10.44 |
  | 04 | 10:56:00 | 1.67 | 2.24 | 5.36 | 10.80 |
  | 05 | 11:34:59 | 1.24 | 6.29 | 8.57 | 8.57 |
  | 06 | 11:39:59 | 2.30 | **12.72** | 12.72 | 12.72 |
  | 07 | 13:07:01 | -2.42 | 0.75 | 1.93 | 1.93 |
  | **합** | | **6.85** | **43.22** | 58.22 | 81.24 |
  (계산: `data/db/raw_data.db: raw_candles` 1분봉 low/high, SHORT 기준 `entry − low`)
- **제안 내용**: 집행은 건드리지 않고 **포지션별 MFE/MAE를 +5/+10/+20분 시점으로 기록**하는
  섀도 테이블 `position_mfe_shadow`(entry_ts, horizon_min, mfe_pt, mae_pt, realized_pt_per_ct,
  regime, intraday_range_pt)를 신설하고 `VALIDATION_CAMPAIGN`에
  `min_samples=30` · `min_days=15` 로 **사전등록**한다. 판정문은 사전에 고정한다:
  *"추세일(당일 레인지 ≥ 5일평균×1.5) 부분집합에서 realized/MFE10 중앙값 < 0.35 이면
  TP1 배수·보호전환 재설계를 주간회의 안건으로 올린다."*
- **기대 효과**: 오늘 같은 날 realized/MFE10 = **0.158**. 이 비율이 추세일에 반복 관측되면
  `ATR_HORIZON_TP1_MULT`(1m 0.3 / 3m 0.5 / 5m 0.7) 재보정의 **사전등록된** 근거가 된다.
- **비용·위험**: 기록 전용이라 집행 위험 0. MFE는 **사후 최적값이라 도달 불가능한 상한**이므로
  비율의 절대수준이 아니라 **레짐별 상대 비교**로만 해석해야 한다 — 판정문에 명시할 것.
- **선행 조건**: F-3(섀도 배선 복구)과 같은 커밋이 자연스럽다.
- **우선순위 제안**: **이번 주.** ⚠ **n=7 · 1거래일 — 313차 원칙에 따라 오늘 수치로는 어떤 임계도
  바꾸지 않는다.** 이 제안은 계측 신설까지다.

### G-2. ProfitGuard L1-Trail 자기잠금 구조 — 기회비용 섀도(G-1 기등록 항목의 실측 확정)

- **관측 근거**: `13:19:59 [ProfitGuard-L1] 트레일링 발동 — 피크 +927,000원 대비 20% 하락
  (현재 +685,000원 < 보호선 +741,600원)`. 이후 **15:09:59까지 L1 차단 133건 · L2-Tier2 27건**
  (`logs/20260818_SIGNAL.log`, `[ProfitGuard] 진입 차단` 총 160건).
  차단 구간 110분 = 세션 371분의 **29.6%**.
  구조적 문제: 보호선 해제는 **새 피크 갱신**으로만 가능한데, 차단 중에는 신규 진입이 없어
  피크가 갱신될 수 없다 → **당일 재개 경로가 존재하지 않는다(자기잠금).**
- **제안 내용**: 기등록 G-1(기회비용 섀도)에 **해제 조건 축**을 추가한다 —
  `strategy/risk/profit_guard.py`에 시간 감쇠(예: 발동 후 N분 경과 시 보호선을 피크×0.8 → 피크×0.7로
  완화) 또는 등급 예외(A등급 1계약 한정 허용)를 **섀도로만** 평가.
- **기대 효과**: 오늘 차단된 133분 중 A등급 후보의 반사실 손익이 산출된다(O-E 적립 중).
- **비용·위험**: 완화는 이익 보호 취지를 직접 훼손한다 — **섀도 판정 전 집행 변경 금지.**
- **선행 조건**: O-E(1거래일 표본) 5거래일 누적.
- **우선순위 제안**: 이번 주(설계만), 26주 WFA 주기(판정).

### G-3. `[ModelLive]` 호라이즌별 라이브 적중률의 EOD 로그 → DB 승격

- **관측 근거**: `retrain_eod_*.log`의 `[ModelLive]`는 **08-13부터만** 존재하고(08-10~08-12 로그에 0건)
  텍스트로만 남아 추세 대조에 매번 grep이 필요하다. 오늘 10m 20.8% / 15m 21.3%(§1 확인 필요)를
  판정하려면 5거래일 시계열이 필요한데 지금은 로그 파일 보존기간에 종속된다.
- **제안 내용**: `data/db/predictions.db`에 `model_live_daily(date, horizon, live_acc, n, cv_acc, oos_acc)`
  테이블 신설, `scripts/retrain_eod.py`에서 로그와 동시에 INSERT.
- **기대 효과**: 471차 후속7 `[51] ConstOut 호라이즌 건강도` 채널과 조인 가능해져,
  "ConstOut이 잦은 호라이즌이 라이브 적중률도 낮은가"를 질의 한 번으로 답한다.
- **비용·위험**: 낮음. `py310_64` 경로이므로 `pickle`/스키마 호환 주의 불필요(순수 sqlite3).
- **선행 조건**: 없음.
- **우선순위 제안**: 이번 주.

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| 전환기준 ② (CB 정상작동) | 15:10 실집행 **여전히 0회** | **미충족 유지** — 오늘도 15:10 시점 FLAT이라 경로가 시험되지 않았다. `F-1R` 리허설(사용자 실행)이 유일한 해소 경로 | 하트비트(`bar_pass=1회`)는 **경로 생존**만 증명한다. 주문 발주는 증명하지 않았다(471차 G-3) |
| 전환기준 ⑦ (FP-CRITICAL) | 재검토 대기 | **F-5를 선행 계측으로 등재** | 오늘 PSI=0.008 CLEAR — 371차 분위수 재설계 후 정상 구간이 확인되나 **하루 1점**이라 "오르내림" 요건 판정 불가 |
| 전환기준 ⑨ (TOX-SEVERE-SPREAD) | `INSUFFICIENT`(ETA 7.1개월) | **진전 없음 기록** — 오늘 `spread_ticks ≥ 20` **1분 / 진입 0건**(최대 28.0틱) | 473차 F-8 Phase A의 적립속도 0.094건/거래일과 정합 |
| 전환기준 ① (4주 통산 수익률) | — | **gross/net 중 net 기준임을 명문화**(F-4) | 오늘 하루만 23,332원 차이 |
| 진입모드 정책 | 미등재 | **`entry_mode` 실전 목표값을 전환 체크리스트에 신설 제안** | §1-5 — `manual`(A/B/C 전면허용)이 무의식적 기본값으로 굳어 있다 |

---

## 4. 진입 승패 사후검증

### 4-0. 선행 확인

| 항목 | 결과 |
|---|---|
| `daily_close` (15:40) | ✅ `data/daily_close_done_20260818.txt` = `2026-08-18T15:40:23.271522` |
| EOD 재학습 (15:45~) | ✅ `data/eod_retrain_done_20260818.txt` — `rows: 40140 / cols: 97 / **horizons_replaced: 6/6** / t_total_s: 340.2`. `Python : 3.10.20 64-bit`(**정상** — py310_64 전용) |
| **P8** 스케일러 재적합 | ✅ `15:50:50 [P8] 스케일러 재적합 완료 n=500봉 elapsed=0.05s horizons=['1m','3m','5m','10m','15m','30m']` + `p8_last_success_date` · `eod_retrain_ok_date` 기록 완료 |
| DB 접근 | ✅ 전부 `mode=ro`. 15:35 이후이므로 장중 분석 금지 구간 아님. 라이브 프로세스는 15:40:38 종료 확인 |
| 수집기 | ✅ (§ 머리말의 `--pc` 주입 건 외 오류 없음) |

### 4-1. 오늘 할일 4분류 (475차 NEXT_TODO 대조)

| # | NEXT_TODO 항목 | 당일 커밋 | 코드 실재 확인 | 상태 |
|---|---|---|---|---|
| O-1 | EOD 재학습 완주 | 없음(관측 항목) | `logs/retrain_eod_20260818.log` 15:45:08~15:50:50 | ✅**완료 — 6/6 교체** |
| O-2 | `session_state.json` P8 키 재생성 | 없음 | `[P8] session_state p8_last_success_date … 기록 완료` | ✅완료 |
| O-3 | P8 키 잔존(08-19 기동 후) | — | — | ⬜내일 |
| O-A/F-1H | 하트비트 1일차 | 없음 | `15:11:21 [SchedForceExit] … bar_pass=1회` | ✅**완료 — 안전망 ERROR 미출현** |
| F-1R | 15:10 리허설 | — | 사용자 실행 필요 | ⬜미착수 |
| F-4M | 스키마 마이그레이션 1일차 | 없음 | `entry_block_axes`·`health_preblock`·`sizing_trace` 370행 비-NULL | ✅완료 |
| O-B | `[Bias⚠] 1m FL편향` 추이 | 없음 | 13:55:59 79% 도달 → `[BiasReset] … uniform fallback 적용` | ✅**완료 — P5 경보 불필요**(streak 10분에서 리셋돼 30분 미도달, `[CB-FLBias]` 0건이 정상) |
| O-C | FP PSI file 로그 | 없음 | `main.py:6071` 임계 미만이라 미출력 — §1-4로 대체 | 🔄**범위변경** |
| O-D | broker−engine 23,332원 | 없음 | 수수료 합계와 원 단위 일치 | ✅**완료 — 원인 확정**(§1-3) |
| O-E | L1 차단 반사실 | 없음 | 133건 적립 | ⬜적립 중(1일차) |
| O-F | ≥20,000ms 블로킹 · 포지션 보유 여부 | 없음 | 13:28:36 37,875ms — **FLAT 구간** | ✅**완료 — P0 아님** |
| O-G | IntradayRegime 전이 | 없음 | 08-14 19회 / 08-18 18회 | ⬜적립 중(2/5일) |
| O-H | 장전 점검 시각 | 없음 | 오늘 13:44:15 | ⬜내일 판정 |
| F-1 | 수집기 포지션 단위 집계 | **없음** | 미적용(§5 여전히 레그) | ⬜**미착수 — 장후인 지금이 적용 시점** |
| F-2 | CB③ HALT 비활성 문서 등재 | 없음 | 미적용 | ⬜미착수 |
| F-3 | `_tick_header` 크기 기준 적신호 | 없음 | 미적용 | ⬜미착수 |

> ⚠ **"완료주장-커밋없음"(⚠) 항목은 없다.** 미착수 3건(F-1/F-2/F-3)은 475차가 "장 마감 후 적용"으로
> 명시한 것이며 본 세션도 코드 변경을 하지 않으므로 **다음 구현 세션으로 이월**한다.

### 4-2. 3원 대사 (포지션 단위)

| 원천 | 질의 | 값 |
|---|---|---|
| 로그 | `[Position] 진입` / `[체결진입]` | **7** |
| `ensemble_decisions` | `entry_executed=1` (date=2026-08-18) | **7** |
| `trades` | `COUNT(DISTINCT entry_ts)` | **7** |
| (참고) `trades` | `COUNT(*)` — 청산 레그 | 14 |

**세 값 일치 — 관측 훼손 없음.** 레그 14 > 포지션 7은 불일치가 아니다(각 포지션이 TP1 + 최종 2레그).

**손익(포지션 단위, 계측 4원칙 ①)**

| 지표 | 값 |
|---|---|
| 포지션 | **7** (승 6 / 패 1 · 승률 **85.7%**) |
| 실현 pt 합 | **+13.70pt** |
| 실현 net | **+661,668원** (`daily_stats` 08-18 값과 원 단위 일치) |
| gross / 수수료 | +685,000원 / 23,332원 |
| PF (당일 리포트) | 3.70 |
| 롤링 20일 | 누적 +1,308,736원 · Sharpe 1.95 · **MDD(자본대비) 3.3%** · 판정 `OUTPERFORM` |

> ⚠ 수집기 §5의 **+269,334원(+5.62pt)** 은 레그 단위 집계라 59% 과소다 — **인용 금지**(F-1 미적용 상태).
> ⚠ MDD는 **자본 대비 3.3%** 만 쓴다. 같은 리포트의 `MDD(peak대비)=127.0%`는 461차가 판정에서
> 배제한 `mdd_pct_of_peak`이다 — **15% 기준에 대보지 말 것.**

### 4-3. 케이스 카드

> conf는 그날의 `min_conf`(DynMC p65 산출) 대비 상대 위치로만 읽는다.
> **CLAUDE.md 구 "확률 판단 기준" 표에 대보지 않았다**(2026-07-31 스케일 전환).
> 붕괴행(`weight_collapsed=1`) 79행은 conf 집계에서 제외했다(당일 비붕괴 291행 평균 conf 0.382).

```
[CASE-01] 진입 10:20:59 SHORT · 2계약 (등급 A/원시 C · horizon=3m · hurst=mean-revert)
          → TP1 33% 10:21:45 +0.89pt / 하드스톱(보호) 10:21:59 +1.55pt
          → 포지션 합 +2.44pt / +118,602원 · 보유 1.0분
 진입근거  10:20:58 [진입체크] SHORT→SHORT 2계약 A급(원시C) | sign✅ conf✅ vwap✅ cvd✅ ofi✅ fore❌ prev✅ time✅ risk✅ chas✅ coun✅ | conf=37.9%
           [Sizer] 3계약 → sizing_trace{qty_sizer_raw:3, binding_gate:'hurst', quality_mults{meta:0.575, hurst:0.5}} → 2계약
 예측 vs 실현  conf=0.379 / min_conf=0.373 (여유 +0.006) · q_exp=+0.228 · spread=5.0틱
 승패판정  ✅승 — `하드스톱`이나 **tp1_reached=True**(보호전환 후 이익 청산)
 주 요인   C (청산 규칙)   부 요인 A (방향 적중)
 반사실    +30분 MFE 17.87pt/ct — 보유했다면 크게 달랐다(사후 상한, 도달 보장 아님)

[CASE-02] 진입 10:39:59 SHORT · 2계약 (등급 A/원시 X · horizon=1m · hurst=neutral)
          → TP1 10:40:41 +0.41 / 하드스톱(보호) 10:40:59 +2.13 → 합 +2.54pt / +123,610원 · 1.0분
 진입근거  10:39:59 [진입체크] … fore❌ chas❌ | conf=37.4%   ※ 앙상블 X → 체크리스트 통과 상향(285차-P5)
           sizing_trace{binding_gate:'tox', tox:0.7, meta:0.75} 3→2계약
 예측 vs 실현  conf=0.374(raw 0.355) / min_conf=0.365 (여유 +0.009) · q_exp=+0.051
 승패판정  ✅승 (tp1_reached=True)
 주 요인   C   부 요인 A
 반사실    +10분 MFE **13.47pt/ct** — 당일 최대 절단폭

[CASE-03] 진입 10:49:59 SHORT · 2계약 (A/원시 C · 5m · trend)
          → TP1 10:50:31 +1.84 / 하드스톱(보호) 10:50:59 +1.30 → 합 +3.14pt / +153,650원 · 1.0분
 진입근거  10:49:58 [진입체크] … ofi❌ fore❌ chas❌ | conf=40.0%
           sizing_trace{binding_gate:'meta', meta:0.655, tox:1.0} 3→2계약
 예측 vs 실현  conf=0.400(raw 0.490) / min_conf=0.365 · q_exp=-0.109
 승패판정  ✅승 (tp1_reached=True)      주 요인 C   부 요인 A
 반사실    +30분 MFE 10.44pt/ct

[CASE-04] 진입 10:56:00 SHORT · 2계약 (A/원시 C · 5m · trend)
          → TP1 10:57:30 +1.92 / 하드스톱(보호) 10:57:59 +1.42 → 합 +3.34pt / +163,660원 · 2.0분
 진입근거  10:55:59 [진입체크] … cvd❌ fore❌ prev❌ chas❌ | conf=43.9%
           sizing_trace{binding_gate:'tox', tox:0.7, meta:0.9} 3→2계약
 예측 vs 실현  conf=0.439 / min_conf=0.365 (당일 최대 여유 +0.074) · q_exp=+0.046
 승패판정  ✅승 (tp1_reached=True)      주 요인 C   부 요인 A
 반사실    MAE10=6.72pt — 7건 중 역행폭 최대였으나 스톱에 닿지 않았다

[CASE-05] 진입 11:34:59 SHORT · 2계약 (A/원시 C · 5m · trend)
          → TP1 11:35:21 +2.31 / 하드스톱(틱) 11:35:47 +0.17 → 합 +2.48pt / +120,718원 · 0.8분
 진입근거  11:34:59 [진입체크] … ofi❌ prev❌ chas❌ | conf=44.4%
           sizing_trace{binding_gate:'tox', tox:0.7} · **spread=8.00틱**(당일 진입 중 최대)
 예측 vs 실현  conf=0.444(당일 진입 최고) / min_conf=0.365 · q_exp=-0.248
 승패판정  ✅승 — 다만 2번째 레그 +0.17pt는 사실상 본전. **틱스톱이 22초 만에 발동**
 주 요인   C   부 요인 E (틱스톱 민감도)
 반사실    +10분 MFE 6.29pt/ct

[CASE-06] 진입 11:39:59 SHORT · 2계약 (A/원시 C · 5m · trend)
          → TP1 11:41:25 +2.22 / 하드스톱(보호) 11:42:00 +2.38 → 합 +4.60pt / +226,706원 · 2.0분
 진입근거  11:39:59 [진입체크] … ofi❌ (나머지 전항 ✅ — 당일 유일한 pass_count=10 조합) | conf=38.6%
           sizing_trace{binding_gate:'tox', tox:0.7, meta:0.75} 3→2계약
 예측 vs 실현  conf=0.386 / min_conf=0.365 · q_exp=-0.002
 승패판정  ✅승 (tp1_reached=True) — **당일 최대 수익 포지션**
 주 요인   A (방향 적중)   부 요인 C
 반사실    +10분 MFE 12.72pt/ct

[CASE-07] 진입 13:07:01 SHORT · 2계약 (A/원시 C · 3m · mean-revert)   ← 유일한 패
          → 손절1차 조기축소 13:08:37 -1.51 / 하드스톱(틱) 13:19:39 -3.33 → 합 -4.84pt / -245,278원 · 12.6분
 진입근거  13:06:59 [진입체크] … cvd❌ prev❌ | conf=42.7%
           sizing_trace{binding_gate:'hurst', hurst:0.5, tox:0.7, meta:0.9} 3→2계약 · spread=6.0틱
 예측 vs 실현  conf=0.427 / min_conf=0.361 · **q_exp=-0.672(당일 최저)**
 승패판정  ❌패 — tp1_reached=**False**(TP1 미도달). MFE10 = 0.75pt/ct로 유리 구간이 거의 없었다
 주 요인   A (방향 실패)
 부 요인   E (손절 준수) — 의도 손절폭 3.21pt(`[DBG-F8] stop=1095.75`) 대비 실현 -3.33pt =
           **초과율 1.04배**, 틱스톱 슬리피지 1095.88→1096.00 = **0.12pt**. 준수율 정상
 반사실    13:08:37 손절1차 조기축소가 이미 -1.51pt를 확정했다. 이 계단이 없었다면
           전량이 -3.33pt 근처에서 청산돼 손실이 더 컸을 가능성 — **미검증**
```

### 4-4. 요인 집계

| 주 요인 | 승 | 패 | 순손익(원) | 평균 보유(분) | 관측 거래일 수 |
|---|---|---|---|---|---|
| **C** 청산 규칙 | 5 | 0 | +680,240 | 1.2 | 1 |
| **A** 방향예측 | 1 | 1 | -18,572 | 7.3 | 1 |
| B 타이밍 | 0 | 0 | — | — | 0 |
| D 사이징 | 0 | 0 | — | — | 0 |
| E 체결·슬리피지 | 0 | 0 | (부요인 2건) | — | 1 |
| F 외생 | 0 | 0 | — | — | 0 |

**읽는 법**: 오늘 흑자는 **C(청산 규칙)가 만든 것**이고, A(방향예측)는 손익 기여가 사실상 0이다
(1승 1패, -18,572원). 같은 날 `[ModelLive]` 방향 적중률이 1m 34.8% / 3m 29.5% / 5m 37.2%인 것과
정합적이다. **동시에 §3 G-1의 MFE 대조는 그 C가 상승분의 16%만 취했음을 보인다** — 즉
"청산이 잘했다"와 "청산이 절단했다"가 같은 규칙의 양면이다.

### 4-5. 313차 가드 통과 여부

| 갈래 | 적용 결과 |
|---|---|
| ① 일자단위 판정 | **관측 1거래일 — 모든 채널 판정 보류.** 요인 집계·MFE 비율·사이저 압력 전부 결론 금지 |
| ② overlap 보정 | 포지션 7건이 10:20~13:19에 몰려 있고 5건이 서로 30분 이내다 — **독립 관측 아님.** 사실상 "같은 하락 파동 1개"에 대한 7번의 표본 |
| ③ 이상치 분해(drop-worst) | 최악 1건(CASE-07 -245,278원) 제거 시 6전 6승 +906,946원. 최고 1건(CASE-06) 제거 시 6전 5승 +434,962원 — **어느 쪽을 빼도 부호 불변**(흑자) |
| ④ 사전등록 — 기준 불변 | 본 세션은 어떤 임계·합격선도 바꾸지 않았다. G-1은 **판정문을 먼저 고정한 뒤** 계측을 시작하는 형태로 제안했다 |
| ⑤ 부호 일관성(기간·PC 교차) | 8월 거래일 net: 08-10 **-244,067** / 08-11 -3,760 / 08-12 +170,443 / 08-13 0 / 08-14 +28,730 / 08-18 **+661,668** — **부호 불일관.** MW0602 교차는 본 세션 범위 밖(브랜치 격차 계류) |

---

## 5. 수익률 향상방안

### R-1. TP1 이후 보호전환 폭의 레짐 조건화 (섀도 선행)

| 칸 | 내용 |
|---|---|
| 근거 | CASE-01~06 (주 요인 **C**, 전부 tp1_reached=True · 평균 보유 1.2분). 실현 6.85pt/ct vs MFE+10분 43.22pt/ct — **비율 0.158** |
| 변경 대상 | (1단계, 이번 제안) `main.py: _ts_record_tp1_trail_shadow()` 배선 확장 = **F-3** + `position_mfe_shadow` 신설 = **G-1**. (2단계, 판정 후) `config/settings.py: ATR_HORIZON_TP1_MULT` 및 `strategy/exit/*: compute_trailing_stop_tier()`의 레짐 조건화 |
| 기대효과 | **정량 추정 금지 구간이다.** 당일 표본 적용 시 상한은 MFE 기반 +36.4pt/ct이나 이는 사후 최적이며 도달 보장이 없다. 실제 기대효과는 섀도 시뮬레이션(`compute_trailing_stop_tier` 재사용)이 산출한다 |
| 회귀 위험 | 보호전환 완화는 **TP1 이익을 되돌려줄 수 있다.** 오늘 CASE-05는 TP1 후 22초 만에 틱스톱이 걸렸다 — 완화했다면 +0.17pt가 손실로 뒤집혔을 수 있다. **양방향 위험** |
| 검증 | `position_mfe_shadow` + 확장된 `tp1_trail_shadow`로 **min_days=15 · min_samples=30**. 판정문 사전등록: *"추세일 부분집합에서 realized/MFE10 중앙값 < 0.35 이면 재설계 안건화"* |
| 표본 상태 | **n=7 · 관측 1거래일 · overlap 심각(②)** — 313차 가드 **미통과**. 따라서 본 항목은 **계측 신설까지만** 제안하며 임계 변경은 제안하지 않는다 |

### R-2. ProfitGuard L1-Trail 자기잠금 해제 조건 (섀도 선행)

| 칸 | 내용 |
|---|---|
| 근거 | 13:19:59 L1 발동 → 15:09:59까지 **차단 133건**(+L2 27건), 세션의 **29.6%**. 발동 근거는 gross 피크 +927,000 → +685,000(-26.1%) |
| 변경 대상 | `strategy/risk/profit_guard.py` — L1 보호선 계산에 **경과시간 감쇠** 파라미터 추가(섀도 전용 필드로 먼저). 기존 O-E 반사실 집계와 같은 테이블 |
| 기대효과 | 차단된 133분 중 자동진입 자격(등급 A·conf ≥ min_conf) 구간의 반사실 손익. 오늘은 13:20~15:09에 시장이 추가로 **-19.5pt** 하락했으므로 SHORT 기회가 있었을 가능성 — **미검증** |
| 회귀 위험 | 이익 보호 취지 직접 훼손. 08-10(-244,067원) 같은 날 L1이 손실을 막았는지 반대로 확인 필요 |
| 검증 | 기등록 G-1 채널에 `unlock_policy` 축 추가, **min_days=10**. 판정 전 집행 변경 금지 |
| 표본 상태 | n=133 차단분 · **관측 1거래일** — 313차 미통과. 적립만 |

### R-3. 사이징 — 오늘은 변경 제안 없음

| 칸 | 내용 |
|---|---|
| 근거 | 7건 전부 `qty_sizer_raw=3 → qty_auto=2`. binding_gate: `tox` 4건 · `hurst` 2건 · `meta` 1건. `MAX_CONTRACTS=3` 상한에 걸린 건 **0건** |
| 판단 | 요인 집계에 **D(사이징)가 0건**이다. 진입 수량이 승패를 가른 증거가 오늘 표본에 없다 — 417차가 확정한 "진입수량 vs 계약당손익 무관(rho=+0.004)"과 정합 |
| 결론 | **제안 없음.** [28] `sizing_inversion_watch`에 표본만 적립. ⚠ 1거래일 판정 금지 |

---

## 6. 다음 거래일(08-19) 관측 예정

- [ ] **O-3 P8 키 잔존** — 08-19 08:41 기동 후 `session_state.json`에 `p8_last_success_date` 유지되는가
- [ ] **O-H 장전 점검 08:57±5분 복귀** — 오늘 13:44:15(4시간 47분 지연). 미복귀면 G-3 승격
- [ ] **F-1R 15:10 강제청산 리허설 — ⚠ 사용자 실행 필요.** 전환기준 ②의 유일한 해소 경로
- [ ] **DriftAdjuster 7일차** — F-1 미적용 상태면 `alpha 0.01000→0.01000` 7번째 출현 확인
- [ ] **`tp1_trail_shadow` 0행 3일차** — qty=1 진입이 다시 나오는지(사이징 분포)
- [ ] **`[ModelLive]` 10m·15m 4일차** — 20.8%/21.3%이 반등하는지(§1 확인 필요)
- [ ] **O-G IntradayRegime 전이 3일차** — 08-14 19회 / 08-18 18회
- [ ] **O-E ProfitGuard L1 차단 반사실 2일차**
- [ ] **CB② 복원 안건** — 2026-08-29까지 **11일**. `CB_CONSEC_STOP_LIMIT=9999` 유지 확인(오늘 카운터 2회 기록)

---

## 부록. 본 세션 자체 검증

- 코드 변경 **0건** · 커밋 **0건**
- DB 접근은 전부 `mode=ro`, 시각 16:2x(장중 분석 금지 구간 밖), 라이브 프로세스 종료 확인(15:40:38)
- 손익은 **포지션 단위**로 집계(계측 4원칙 ①) — `daily_stats` 및 gross/수수료/net 3원 교차검증 완료
- conf 절대값을 CLAUDE.md 구 "확률 판단 기준" 표에 대보지 않음(2026-07-31 스케일 전환)
- 붕괴행 79행(21.4%)을 conf 평균 집계에서 제외
- MDD는 `mdd_pct_of_capital`(3.3%)만 인용, `mdd_pct_of_peak`(127.0%)는 판정 미사용
- 재인용 금지 수치(2026-06-25 SHAP=0, 0801 §9-3 이벤트 단위 사이징 통계 4종) **미사용**
- 캠페인 권고는 코드(`safety/circuit_breaker.py`, `config/settings.py`, `main.py`) 실물 확인 후 보고
- 표본 1거래일 채널(요인 집계·MFE·[28]·G-1)에 **확정 결론 없음**(313차)
