# 미륵이 일일 점검 — MW0602 / 2026-08-18 / 장후(post)

- 점검 시각: 16:31 KST · PC `MW0602` · 브랜치 `dev` ✅ (운영 브랜치 일치)
- HEAD `90c66cb` · 미커밋 3건 · 당일 커밋 0건
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260818_post.md`
- 수집기 실행: **성공** (`--pc MW0602` override 적용, 헤더 `MW0602 (override · host=claude)` 확인)

> 이 리포트는 `-post` 접미사 파일이다. 같은 날 `-pre`·`-intra` 리포트가 **동시 실행 세션**에서
> 별도로 작성됐다(다이제스트 3종이 16:31:54/16:31:59/16:32:16에 생성). 기존 날짜본을 덮지 않았다.
> ⚠ 세 세션이 원격 git 기준으로 독립 산출해 **차수가 `475차`로 충돌**했다 — §1 1-9 참조.

---

## 0. 한 줄 결론

**오늘 미륵이는 절대원칙 6종을 전부 지켰고 P0는 0건이다.** 다만 어제까지 "라이브 미검증"이던
471·473차 계측 4종이 오늘 처음 실동작했고, 그 첫 데이터가 **새 계측 하나(ConfFloorGuard 상태
샘플)가 구조적으로 무의미하다**는 것과 **절대원칙 ③의 단기 CORE 중 `ofi_norm`이 10:15~14:57
연속 축퇴 상태였다**는 것을 드러냈다 — 둘 다 P1.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 정상 | 08:40:41 기동(PID 2836) · 08:45 매크로/스케일러 워밍업 · 설정 불변식 26행 전부 `일치`·`미발견` 0 |
| 장중 | 조건부 정상 | 매분 루프 **371/371분(100%)** · 10분 이상 공백 0건. 단 `ofi_norm` 축퇴 4시간 42분 지속(P1-2), degraded=ON 2분(P2-1) |
| 장후 | 정상 | 15:11 `[SchedForceExit]` 하트비트 정상 · `daily_close` 15:40:16 · EOD 재학습 **6/6 교체 완주** 15:47:44 · P8 스탬프 2키 기록 완료 |

**절대원칙 준수 여부**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | ✅ **준수 (청산 대상 없음)** | 마지막 청산 14:45:51, 이후 `14:50 이후 — 신규 진입 금지 구간 (345차)` 6건. 15:10 시점 포지션 FLAT. `15:11:15 [SchedForceExit] 15:11 점검 — status=FLAT engine=0ct broker_cached=0ct bar_pass=2회 → 청산 대상 없음(정상)`. **⚠ "청산 로그 없음"이 아니라 "청산할 것이 없었음"이다.** |
| ② | Circuit Breaker 5종 | ✅ 정상 | `[CB] 연속 손절 1→2→3회`(10:10:58·13:05:30·13:36:20) 카운터 정상 증가, `CB_CONSEC_STOP_LIMIT=9999`라 정지 없음(설계대로). CB③~⑤ 발동 0건. 부분청산 손실 2건은 `연속 손절 카운터 미증가 (포지션 단위 집계, 470차 C1)`로 정확히 처리됨 |
| ③ | CORE 피처 호라이즌 그룹별 | ⚠️ **조건부** | `vwap` 미통과 진입 **0건** ✅(강제 X 규칙 준수). 그러나 `ofi_norm`이 10:15:58~14:57:00 연속 축퇴(identity 강제) → 진입 18건 중 `ofi❌` 14건. **P1-2** |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | ✅ 준수 | `0xC0000409`·`STACK_BUFFER` 전 로그 **0건**. `[FaultHandler] 활성화` 08:40:41, 크래시 흔적 없음. 15:40:31 정상 종료 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | ✅ 준수 | TR 수신 오류·`record_name` 관련 예외 0건. `OPT50029` 경로 정상(분봉 409봉 수신) |
| ⑥ | 알파 리서치 봇 자동통합 금지 | ✅ 준수 | 알파봇 자동통합·백테스트 자동큐 흔적 **0건** |

**설정 불변식** — 수집기 §3 표 **26행 전부 `일치` 또는 의도된 `값 확인`**. `불일치` 0건 ·
`미발견` **0건**.

> 🔎 **어제(08-17 MW0601 `v9-dev`)의 `미발견` 5종이 오늘 MW0602 `dev`에서는 0건**이다.
> 473차 후속2 [5]가 "브랜치 분기가 원인"이라고 확정한 진단과 정확히 일치한다 —
> 그 5종은 `[MW0602]` 커밋으로만 `dev`에 들어간 상수였다. **오늘 리포트는 그 진단의
> 반대편 증거**이며, `v9-dev` ↔ `origin/dev` = 320/315 격차 처분은 여전히 주간회의 안건이다.

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**해당 없음.** 절대원칙 6종 전부 준수, 오버나이트 잔여 포지션 0, 크래시 0, 미커밋 라이브 코드 0.

---

### P1 — 정확성·관측 훼손

#### 1-1. `[ConfFloorGuard] state=` 상태 샘플이 **진입 금지 존에서만** 기록된다 — 470차 L2 계측이 구조적으로 무의미

- **증상**: 470차 L2가 "무조건 상태 샘플"로 신설한 `[ConfFloorGuard] state=` 로그가 오늘
  **80건 전부 `ZONE_BLACKOUT`** 한 값이다. 진입 허용 290분 동안 **0건**이다.
- **근거**:
  ```
  2026-08-18 11:50:58 [INFO] SIGNAL: [ConfFloorGuard] state=ZONE_BLACKOUT 출력상한=N/A 필요=0.6200 (conf_floor=0.330, min_conf=0.620, zone_entry=False)
  ```
  `logs/20260818_SIGNAL.log` · 총 80건 · 11:50:58~15:09:00
  **시간대 분포: 11시 10건 / 12시 60건 / 15시 10건 = 정확히 80분.**
  같은 날 존 체류 실측(§5 진입 가능 시간 예산): `OTHER`(11:50~13:00) **70분** +
  `EXIT_ONLY`(15:00~15:10) **10분** = **80분**. 1분당 1샘플이 진입 금지 존에서만 찍혔다.

  정작 봐야 할 사건은 **진입 허용 구간에서 실제로 일어났고, 별도 엣지 로그로만 남았다**:
  ```
  09:22:57 [WARNING] SIGNAL: [ConfFloorGuard] 자동진입 하한 도달 불가 — 보정기 출력상한 0.3835 < 필요 0.3840 (conf_floor=0.330, min_conf=0.384, span=0.0183). 이 상태에서는 어떤 신호도 자동진입 하한을 넘을 수 없다.
  10:24:57 [WARNING] SIGNAL: [ConfFloorGuard] 자동진입 하한 도달 불가 — 보정기 출력상한 0.3767 < 필요 0.3840 (conf_floor=0.330, min_conf=0.384, span=0.0141).
  ```
- **기준 위반**: `references/invariants.md` §5 26주 재검증 항목 —
  *"⚠ 조건부 로그(임계 초과 시에만 출력)는 넣지 말 것 — **100% 고착이 구조적으로 보장된다**"*.
  470차가 이 규약을 지키려고 만든 로그가 실제로는 조건부였다.
- **영향**: 수집기 §12 `ConfFloor` 행은 오늘 `⚪ 표본부족 / ZONE_BLACKOUT×80`으로 나왔고,
  관측일이 쌓이면 **`🔴 고착 ZONE_BLACKOUT 100%`로 굳는다**. §12가 잡으려던 것
  (*"BLOCKED 고착이면 어떤 신호도 자동진입 하한을 못 넘는 상태가 종일 지속된 것"*)은
  이 축에 영원히 안 나타난다. 468차 G-2·470차 C1'이 경계한 **"새로 만든 계측이 조용히
  죽는다"** 의 세 번째 사례다(292차 CB③-P4 · 303차 FP-CRITICAL · 311차 spread 섀도에 이어).
- **신규 여부**: **신규.** 470차 L2 배포(2026-08-14) 이후 **첫 거래일 관측**이다
  (08-15 광복절 · 08-17 KRX 휴장 → 오늘이 1일차).

#### 1-2. 절대원칙 ③의 단기 CORE `ofi_norm`이 10:15~14:57 **연속 축퇴** — 진입 18건 중 `ofi❌` 14건

- **증상**: `[CORE준비도]` 42샘플 중 **29건이 `축퇴 1/6 — ofi_norm×6hz`**이며, 10:15:58부터
  14:57:00까지 **끊김 없이 연속**이다(09:00~09:57 구간은 `0/6 정상`이었다).
  그 사이 `[ScalerRefresh] 1m CORE 'ofi_norm' raw_std≈0(…) → identity(0,1) 강제` 180건.
- **근거**:
  ```
  09:00:57 [SIGNAL] [CORE준비도] trigger=B_OPEN 축퇴 0/6 — CORE 스케일 정상
  …(09:02~09:57 D_FORCE 0/6 ×10)…
  10:15:58 [SIGNAL] [CORE준비도] trigger=C_PERIODIC 축퇴 1/6 — ofi_norm×6hz | 정상 진입 전 해소 여부를 확인하라
  …(10:31~14:57 연속 1/6 ×28)…
  15:47:44 [SIGNAL] [CORE준비도] trigger=E_EOD 축퇴 1/6 — ofi_norm×6hz     ← EOD 이후에도 미해소
  ```
  `logs/20260818_SIGNAL.log` · `logs/retrain_eod_20260818.log`
  raw_std 실측 궤적: 08:45 `0.0294` → 10:15 `0.0417` → 11:32~14:16 `0.0307~0.0317` 고착.

  진입 18건 체크리스트 미통과 누적(수집기 §5): **`ofi`×14** · `chas`×12 · `cvd`×7 ·
  `prev`×6 · `fore`×5 — **`ofi`가 최다**다.
- **기준 위반**: CLAUDE.md 절대원칙 **③** (단기 그룹 1m·3m·5m의 CORE `OFI 불균형`,
  미통과 → 등급 하락) / 9단계 **STEP 4**(피처 생성).
- **영향**: 단기 CORE 3종 중 1종이 하루의 **4시간 42분** 동안 판별력 없는 상태로 체크리스트에
  들어갔다. 오늘 진입 18건이 전부 `raw_grade='C'`에서 출발한 것과 무관하지 않을 수 있다.
- **신규 여부**: **기존 등록 사안의 새로운 발현** — 신규 결함이 아니다.
  CLAUDE.md 절대원칙 §2 FP-CRITICAL 항목의 371차 문단이 이미 이렇게 적어 두었다:
  > *"cvd_divergence·ofi_norm 학습표본의 91%가 정확히 0.0인 점질량은 분위수 비닝으로도 완전히
  > 해소되지 않는 잔여 이슈로 별도 등록(CORE 피처 자체 조사 필요, 이번 수정 범위 밖)."*

  오늘 처음으로 **470차 B4 계측(`[CORE준비도]`)이 그 잔여 이슈를 "일중 지속시간"으로
  가시화**했다. 계측이 제 몫을 한 사례다.
- **⚠ 313차 원칙**: 관측일 **1일**(이 계측의 라이브 1일차)이다. **"identity 강제 → `ofi❌`"의
  인과는 미검증**이며 확정 결론을 내지 않는다. 판정 조건은 §6 O-2에 등록했다.

#### 1-3. 일일 전략 리포트 배너에 **자본 대비 MDD가 없다** — 461차 규약의 미적용 잔여 경로

- **증상**: `strategy_report_20260818_154016.txt`가 MDD를 **peak 대비로만** 출력한다.
  ```
  Live    : Sh=-0.20  MDD=143.3%(누적손익peak대비)  WR=66.7%  PF=0.97
  롤링20일: 누적 -98575원  Sh=-0.20  MDD=143.3%(누적손익peak대비, 1,445,833원)
  ```
  자본 대비 환산: **1,445,833원 ÷ 50,000,000원 = 2.89%**.
- **근거**: `strategy/ops/daily_exporter.py:70` · `:79` — 두 줄 모두 `(누적손익peak대비)` 고정.
  같은 리포에서 `config/settings.py:4613`은 이렇게 못박고 있다:
  ```
  "⚠ MDD는 `mdd_pct_of_capital`로 볼 것 — `mdd_pct`(peak 대비)는 판정 금지(461차)."
  ```
- **기준 위반**: CLAUDE.md 실전 전환 기준 ② 하위 461차 결정 —
  *"461차부터 판정(`_compute_verdict`)·**리포트 배너**·대시보드는 `mdd_pct_of_capital`만 쓴다."*
- **영향**: 143.3%라는 수치가 전환기준 ③(`MDD ≤ 15%`)과 나란히 읽히면 461차가 실제로 겪은
  사고(`UNDERPERFORM` + 전략 교체 권고)가 그대로 재발한다. 실제 차이는 143.3% vs **2.89%** 다.
- **신규 여부**: **부분 신규** — 461차 조치는 **판정 경로에는 이미 적용돼 있다**(오늘 판정은
  `INSUFFICIENT — 데이터 부족(60일)`이라 MDD를 쓰지 않았고, `test_465_…py:306`이 분모 라벨
  표기를 강제하고 있어 즉시 오독은 막혀 있다). **남은 것은 표시 경로 하나**다.
  → 함정 ① 확인 완료: 신규 안건으로 올릴 것은 "배너에 자본 대비 병기" **하나뿐**이다.

#### 1-4. 수집기 §12 `전략판정` 🔴 고착(`UNDERPERFORM` 7/7)이 **오늘 실제 판정과 다르다**

- **증상**: 수집기 §12가 `전략판정 | 🔴 고착 | 7일 | UNDERPERFORM×7`로 적신호 13번을 올렸는데,
  오늘 `strategy_report_20260818_154016.txt`의 실제 판정은 **`INSUFFICIENT`** 다.
- **근거**:
  - 다이제스트 §12 `전략판정` 행 = `UNDERPERFORM`×7 (관측일 7)
  - 당일 리포트 = `판정 : INSUFFICIENT` / `사유 : 데이터 부족 (60일) — 5일 이후 재판정`
  - `grep "UNDERPERFORM\|INSUFFICIENT" logs/20260818_*.log` → **0건** (로그 원천 아님)
  - 패턴 정의: `collect_evidence.py:265` `stuck_indicators["전략판정"]`
- **기준 위반**: 계측 정확성 — §12는 "고착이면 판정식이 무의미해진 것"을 잡는 장치인데,
  **당일 값을 반영하지 못한 상태로 🔴을 띄웠다**.
- **영향**: 적신호 13번이 오탐일 가능성이 크다. 468차 G-2가 등록한 *"감시 대상 목록 자체가
  낡는 것은 일일 점검이 못 잡는다"* 계열이며, 오탐에 익숙해지면 진짜 고착이 같은 자리에
  섞여 걸러진다(473차 후속2 [3]이 지적한 바로 그 위험).
- **⚠ 확정 아님**: §12가 읽는 원천(`strategy_registry.db:strategy_live_snapshots` 추정)과
  `daily_exporter` 출력이 **서로 다른 시점의 값**일 가능성이 남아 있다. 조사 항목으로 등록(§2 F-3).

---

### P2 — 운영 부담·기술부채

#### 1-5. HealthPolicy `degraded=ON` 2분 (14:31~14:32) — 2026-08-12 대비 신규

- **근거**:
  ```
  14:30:01 [WARNING] HEALTH: [Health] level=WARNING degraded=OFF | latency=745ms | quality=1.00 | cache_age=138s | exceptions_10m=8
  14:31:00 [WARNING] SYSTEM: [HealthPolicy] Degraded 선제차단: streak=1.75+0.35 ≥ 2 (latency=745ms quality=1.00 cache=0s exc10m=8)
  14:31:00 [WARNING] SYSTEM: [HealthPolicy] 자동 Degraded Mode 진입 (warn_streak=2.10, warn_ratio=35%, window=5분)
  14:33:00 [INFO] HEALTH: [Health] level=INFO degraded=OFF | latency=591ms | …
  ```
- **영향**: `HEALTH_DEGRADED_BLOCK_AUTO_ENTRY=True`이므로 실제 차단이 발생했다 —
  전략 리포트 2차 게이트차단 25건 중 **`Degraded신뢰도=1`** 이 그것이다.
  `ensemble_decisions.health_preblock` 비영 **2행**(471차 후속2 스키마)과 정확히 대응.
- **주의**: `quality=1.00`(데이터 품질 정상)인데 **지연 축만으로** 발동했다.
  `config/settings.py:5367~5388`이 경계한 *"exceptions_10m 단독 발동"* 계열인지 확인 필요.
  단 오늘은 `latency=745ms`도 함께 높아 단독 발동은 아니다.
- **신규 여부**: 신규(2026-08-12는 0건). §12 `degraded` 행은 `✅ 변동`(OFF×179, ON×3)이라
  고착은 아니다.

#### 1-6. `[Brier] 과신 경고` 8건 — `CB_BRIER_WARN` 임계 접촉

```
11:14:57 [WARNING] SYSTEM: [Brier] 과신 경고 | 이동평균=0.358 > 0.35
11:15:57 … 0.367 > 0.35   11:16:57 … 0.367   11:17:57 … 0.366
```
경고만 발생, 조치 없음. 08-12(0.352)보다 높다.

#### 1-7. `WeightCollapse` 75건 — 미배포 호라이즌 4종이 66건

```
09:07:58 [WARNING] SIGNAL: [WeightCollapse] 실질 가중합 0 (1연속) — 활성기대=['3m'] 중 미배포=['3m'] → flat_score=1.0 안전망 발동
```
미배포 조합 분포: `['10m','15m','3m','5m']`×66 · `['3m']`×6 · `['3m','5m']`×3.
시간당 12건으로 **균등**(5분 주기). `ensemble_decisions.weight_collapsed=1` **75행**과 일치.
471차 후속7이 신설한 채널 **[51] ConstOut 호라이즌 건강도**의 관측 대상이다.

> ⚠ **호라이즌별 예측 커버리지 편차는 신규 이상이 아니다.** 오늘
> `1m 367 / 3m 244 / 5m 145 / 10m 71 / 15m 49 / 30m 369` 인데, 최근 5거래일이 전부 같은
> 패턴이다(08-14 `368/245/146/72/49/369`, 08-13 `368/245/148/74/49/368`, 08-12 `364/240/147/69/49/365`,
> 08-11 `370/247/148/74/49/370`). 설계상 호라이즌별 발행 주기 차이로 보이며, **오늘의 이상이 아니다.**

#### 1-8. `mc-conf 괴리` 경보 — 5거래일 평균 진입후보 49분 < 하한 60분

```
15:40:15 [WARNING] SYSTEM: [경보] mc-conf 괴리: 최근 5거래일 평균 진입후보 49분/일 < 하한 60분 — 금일 103분.
```
오늘(103분)은 하한을 넘겼으나 **5일 평균이 미달**이다. 08-15·08-17 휴장으로 표본이 3일치라
평균이 눌린 것인지 확인 필요.

#### 1-9. 미커밋 3건 + 당일 커밋 0건 + **점검 세션 3개 동시 실행 → 차수 `475차` 충돌**

- 미커밋: `docs/…/weekly_prompt.txt`(M) · `MIREUK_DAILYCHECK_HANDOFF.md`(??) ·
  `docs/…/MW0602/0816_주간회의_검토보고_MW0602.md`(??) — **라이브 코드 경로 아님**.
- `evidence_MW0602-20260818_pre.md`(16:32:16) · `_intra.md`(16:31:59) · `_post.md`(16:31:54)
  세 다이제스트가 **같은 분에 생성**됐다. 473차 후속2 [4]가 등록한 **G-3 점검 세션 동시 실행**
  문제의 재현이며 `dev_memory/.dailycheck.lock` 은 여전히 미구현이다.
- 🔴 **결과로 차수가 충돌했다.** 세 세션이 각각 원격 git(HEAD `90c66cb`, 최대 474차) 기준으로
  독립 산출해 **전부 `475차`** 를 얻었고, `DECISION_LOG.md`·`NEXT_TODO.md` 양쪽에 같은 날
  `475차` 헤더가 복수 존재한다(국면 라벨로만 구분). 392차 관행을 **셋 다 지켰는데** 충돌했다 —
  2026-07-02 두 PC "273차" 충돌과 같은 형태이나 이번은 **한 PC 안에서** 났다.
- ✅ **유실은 없다.** append 순서대로 전부 남았다(본 세션 post 16:48 → pre 16:50).
  → G-3 범위에 **차수 충돌 회피**를 추가했다(§2 F-5). **소급 수정하지 않는다.**
- ⚠ **수집기 §2·§11의 미커밋 건수는 오늘 신뢰할 수 있다**(3건). 473차 후속2 [5]가 경고한
  "454건 CRLF 아티팩트"는 이번 실행에서 재현되지 않았다 — 수집기가
  `git -c core.autocrlf=true status --porcelain`로 측정한다고 §2에 자체 명기하고 있다.

---

### 확인 필요 (확정 아님)

| # | 항목 | 무엇을 더 보면 판정되는가 |
|---|---|---|
| a | P1-2 `ofi_norm` 축퇴 ↔ `ofi❌` 인과 | `[CORE준비도] 축퇴 1/6` 구간과 `0/6` 구간의 진입건 `ofi` 통과율을 **일자단위 5거래일** 비교. 313차 ①(일자단위) 필수 |
| b | P1-4 §12 `전략판정` 오탐 여부 | `collect_evidence.py:265` 패턴 원천 확인 + `strategy_registry.db:strategy_live_snapshots` 당일 행 조회 |
| c | 보호트레일 조기절단 가설 | 오늘 8건 평균 `+0.44pt` vs TP2 5건 `+2.87pt`. **1일 표본** — §5 R-1의 섀도 계측으로만 |
| d | P2-1 degraded 발동의 지연 원인 | 14:20~14:35 `[PipePerf]` 원값(`raw=…ms`, 462차 P2)과 `PIPE_LATENCY_EXCLUDE_MODEL_SWAP` 차감분 대조 |
| e | P2-8 5일 평균 49분 | 08-15·08-17 휴장 제외 시 실제 거래일 3일 평균인지 |

> **313차 원칙 적용**: a·c는 관측일 1일이므로 **확정 결론 보류**. 각각 5거래일·20거래일이
> 필요하다고 §6에 판정 예정일을 명기했다.

---

### 이미 반영된 사안 (신규 아님) — 함정 ① 방지

이 절을 비워두지 않았다. **확인했고, 아래는 전부 이미 코드에 있다.**

| 관측 | 이미 반영된 근거 | 신규 안건 아님 |
|---|---|---|
| `[CB] 연속 손절 3회`가 떴는데 정지 안 함 | `CB_CONSEC_STOP_LIMIT=9999` (모의투자 한정 예외, CLAUDE.md 절대원칙 §2) | 정상 동작. 재검토 기한 **2026-08-29** |
| `[MarginCap] 3→2계약` 발생 → `MAX_CONTRACTS=3` 미구속 | **470차**가 규명 완료 — "증거금 상한이 2에서 먼저 자른다", `MAX_CONTRACTS`는 현재 죽은 파라미터 | 되돌리지 말 것 |
| `[28] sizing_inversion_watch` 표본 미적립 | 470차 결론 — "표본이 천천히 쌓이는 중이 아니라 **⑧ 해제 전까지 켜지지 않는 상태**" | 오늘 `qty≥3` 체결 **0건**으로 재확인 |
| `JointGateBlock — meta=0.50 tox=0.70 joint=0.350` 24건 (곱셈) | **431차가 의도적으로 남긴 것** — "JointGateBlock 차단 기준 `size_multiplier`는 무변경 ([7] PASS 게이트 보존)". min() 합성은 **사이징 경로만** | 오늘 `sizing_trace`가 `meta_size_sizing=1.0`(중립화)·`meta_size_raw=0.567`로 두 경로 분리를 실증 |
| `ZoneBanShadow` 2건 (12:21·12:23, zone=OTHER인데 전 게이트 통과) | 462차 P1-a — `ZONE_ENTRY_BAN_ENFORCE=False`, 섀도만. 채널 [53] `min_samples=20` 대기 | 집행=OFF 정상. 표본 7→9 진전 |
| `[Calibration] 축퇴` WARNING 179건 | 473차 후속2 [5] — **만성이며 신규 아님**(08-10 157 / 08-11 148 / 08-12 145 / 08-13 141 / 08-14 144 / 08-17 143) | 오늘 179는 거래일 정상 범위 |
| `[SHAP] … CORE안전=✅` | 468차 F-3 이후 정상. §12 `CORE안전` = `✅ 변동`(⚠️×67, ✅×21) | 08-13·08-14의 `⚠️` 고착에서 회복 |
| `TOXICITY_SEVERE_SPREAD_BLOCK_ENABLED=False` | 311차 근거 `config/settings.py:4770-4781` + 473차 F-8 Phase A `INSUFFICIENT` | 적신호 아님 (`documented_disabled_flags`) |

---

## 2. Fix 작업 구현계획

> 오늘은 **장후**이며 본체는 15:40:31에 정상 종료했다(`[Shutdown] … (auto_shutdown)`).
> 코드 변경은 **오늘 밤 적용 가능**하다. 단 F-1·F-2는 **EOD 재학습이 이미 끝난 뒤**여야 하는데
> 15:47:44에 완주했으므로 조건 충족.

### F-1. `[ConfFloorGuard] state=` 를 존과 무관하게 매분 기록 — P1 · 대응 1-1

- **원인 가설**: 상태 샘플 emit 지점이 `zone_entry=False` 분기 **안쪽**에 있어, 진입 허용
  존에서는 엣지 로그(`하한 도달 불가` / `하한 도달 가능 복구`)만 나가고 상태 샘플이 스킵된다.
  실측이 이를 지지한다 — 80샘플 = 금지 존 체류 80분과 **정확히 일치**하고 허용 290분은 0건.
- **변경 파일**:
  - `strategy/` 내 `ConfFloorGuard` 구현부(정확한 경로는
    `grep -rn "ConfFloorGuard\] state=" --include=*.py`로 확정) — 상태 샘플 emit을
    zone 분기 **바깥**의 매분 훅으로 이동. 상태값을 `OK` / `BLOCKED` / `ZONE_BLACKOUT`
    3분류로 유지하고, 허용 존에서는 `출력상한`·`필요` 실값을 채운다(현재 `N/A`).
  - ⚠ **로그 문구는 바꾸지 않는다** — `collect_evidence.py:275`의
    `r"\[ConfFloorGuard\] state=(?P<v>\w+)"` 정규식이 그대로 물려 있다.
- **회귀 위험**: 매분 1행 증가(하루 +290행 ≈ SIGNAL.log +30KB). 무시 가능.
  `ZONE_BLACKOUT` 값 자체는 유지되므로 §12 시계열이 끊기지 않는다.
- **절대원칙 저촉**: 없음 — 로그 추가일 뿐 게이트 판정 로직 불변. COM 콜백 밖.
- **런타임**: `py37_32` (장중 본체).
- **검증**: 다음 거래일 `grep -c "ConfFloorGuard\] state=" logs/2026XXXX_SIGNAL.log` ≥ **300**
  이고 `state=` 값이 **2종 이상**일 것. 오늘 실측(80건·1종)이 판정 기준선.
- **섀도 계측 필요**: 불필요(계측 자체의 수정이며 차단 로직 신설 아님).
- **적용 시점**: 오늘 밤(EOD 완주 후) — 다음 거래일 08:40 기동부터 유효.
- **결정 필요**: 없음.

### F-2. 일일 전략 리포트 배너에 **자본 대비 MDD 병기** — P1 · 대응 1-3

- **원인 가설**: 461차가 판정식(`_compute_verdict`)과 캠페인 리포트는 고쳤으나
  `daily_exporter`의 텍스트 배너는 범위 밖이었다.
- **변경 파일**:
  - `strategy/ops/daily_exporter.py:70` — `MDD=%.1f%%(누적손익peak대비)` →
    `MDD=%.2f%%(자본대비) [peak대비 %.1f%%]` 형태로 **자본 대비를 앞**에 둔다.
  - `:79` 롤링20일 줄도 동일. 분모 자본은 `SIZING_TARGET_CAPITAL_KRW`.
- **회귀 위험**: `tests/test_465_retrain_scope_and_tp1_flag.py:306~307`이 현재 문자열
  (`"MDD=%.1f%%(누적손익peak대비)"` · `"누적손익peak대비%s"`)을 **정확히 검사**한다 →
  **테스트를 함께 갱신해야 한다.** 갱신 없이 배포하면 CI가 깨진다.
- **절대원칙 저촉**: 없음(표시 변경).
- **런타임**: `py37_32`.
- **검증**: `pytest tests/test_465_retrain_scope_and_tp1_flag.py` 통과 +
  다음 거래일 `strategy_report_*.txt`에 `(자본대비 2.89%)` 형태가 찍히는지.
- **적용 시점**: 오늘 밤.
- **결정 필요**: 두 값을 **병기**할지 자본 대비로 **교체**할지.
  → **권고: 병기.** 461차가 남긴 `mdd_pct_of_peak` 하위호환 별칭이 존재하고,
  계측 4원칙 ③(탈락 가시화)상 값을 없애는 것보다 순서를 바꾸는 편이 안전하다.

### F-3. 수집기 §12 `전략판정` 원천 정합 — P1 · 대응 1-4 (**조사 선행**)

- **원인 가설 (미확정)**: §12가 읽는 원천과 `daily_exporter` 출력의 **시점이 다르다**.
- **변경 파일**: 조사 결과에 따라
  `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py:265` 패턴 또는 원천 경로.
- **회귀 위험**: 낮음(수집기는 표준 라이브러리 전용, 라이브 경로 아님).
- **절대원칙 저촉**: 없음.
- **런타임**: 수집기는 어느 파이썬이든 무방.
- **검증**: 08-18 날짜로 `--phase post` 재실행 시 §12 `전략판정`에 `INSUFFICIENT`가
  포함되는지.
- **적용 시점**: 조사 완료 후. **⚠ 지금 패턴만 고치면 안 된다** — 471차 F-3→G-1 순서
  교훈("문자열을 고치기 전에 구조체를 만들면 틀린 값이 굳는다")과 같은 계열이다.
- **결정 필요**: 없음(조사가 먼저).

### F-4. `docs/정기점검` untracked 산출물 추적 편입 — P2 · 대응 1-9

- 어제 NEXT_TODO **"정기점검 산출물 untracked 16종 추적 편입"** 과 **동일 항목**이다.
  **중복 착수 금지** — 그쪽 항목에 오늘 2건(`MIREUK_DAILYCHECK_HANDOFF.md`,
  `0816_주간회의_검토보고_MW0602.md`)을 합류시킨다.
- CLAUDE.md "주간 산출물 위치 규약"이 리포트 커밋을 **의도적 예외**로 정한 근거가 있다.

### F-5. G-3(473차) 범위에 **세션 차수 충돌 회피** 추가 — P2 · 대응 1-9

- **원인**: 세션 차수를 원격 git 기준으로 산출하는 392차 관행은 **동시 실행을 상정하지 않는다.**
  오늘 세 세션이 관행을 전부 지켰는데도 같은 `475차`를 얻었다.
- **변경**: 473차 후속2가 등록한 `dev_memory/.dailycheck.lock` 구현 시 —
  잠금 획득 후 같은 날·같은 PC의 기존 헤더를 스캔해 `475차-2` 형태로 접미하거나,
  국면 라벨(`pre`/`intra`/`post`)을 차수의 일부로 승격.
  스테일 잠금은 mtime 10분 초과 시 무시(471차 `.git/index.lock` 전례).
- **⚠ 소급 수정 금지** — 오늘 항목들은 국면 라벨로 식별 가능하고, 1.9MB 누적 파일을
  되짚어 고치는 편이 유실 위험이 크다.
- **절대원칙 저촉**: 없음(점검 도구).
- **적용 시점**: 이번 주. **473차 G-3과 같은 커밋** — 중복 착수 금지.

### 적용 순서와 커밋 계획

1. **커밋 ①** — F-1 (ConfFloorGuard 상태 샘플 존 무관화)
   `[MW0602] 475차: ConfFloorGuard 상태 샘플 존 무관 기록 — 470차 L2 조건부 고착 해소`
2. **커밋 ②** — F-2 + 테스트 갱신 (동일 커밋에 묶는다 — 분리하면 CI가 깨진 채 남는다)
   `[MW0602] 475차: 일일 전략 리포트 배너 자본대비 MDD 병기 (461차 잔여 경로)`
3. **커밋 ③** — F-4 (문서 추적 편입) — 어제 항목과 합류 후 일괄
4. F-3은 **조사 완료 전까지 커밋하지 않는다.**

---

## 3. 고도화 방안

### G-1. 수집기 §5 거래일 요약에 **gross / 수수료 / net 3열** 상설

- **관측 근거**: 오늘 총 pt가 **+0.16pt(gross +8,000원)** 인데 수수료 **33,084원**이
  붙어 **순 -25,084원**이 됐다. 승률 67%(12/18 포지션)인데 적자다.
  수집기 §5는 순액만 보여 주므로 *"방향은 맞혔는데 비용에 졌다"* 와
  *"방향을 틀렸다"* 가 같은 숫자로 보인다.
- **제안**: `collect_evidence.py` §5 포지션 표에 `gross_pnl_krw` · `commission_krw` ·
  `net_pnl_krw` 3열을 추가하고, 합계 줄에 **`수수료/총이익` 비율**을 병기.
  `trades` 스키마에 세 컬럼이 **이미 있다**(신규 배선 불필요).
- **기대 효과**: "수수료가 손익을 뒤집은 날"을 그날 즉시 식별. 오늘 비율은
  33,084 / 8,000 = **413%**.
- **비용·위험**: 수집기 단독 수정, 라이브 무관. 30분.
- **선행 조건**: 없음.
- **우선순위**: **이번 주.**

### G-2. §12 고착 지표에 `binding_gate` 축 추가

- **관측 근거**: 471차 후속6 `sizing_trace`가 오늘 처음 라이브로 쌓였고(65행),
  **binding_gate 실측이 처음 나왔다**: `tox`×29(45%) · `hurst`×13(20%) · `meta`×8(12%) ·
  `None`×15(23%). 즉 **오늘 사이즈를 가장 많이 구속한 것은 ToxicityGate**다.
  이 축은 §12 감시 목록에 없어서, 한 게이트가 상시 binding으로 굳어도 잡히지 않는다.
- **제안**: `collect_evidence.py` `stuck_indicators`에 `binding_gate` 추가.
  원천은 로그가 아니라 **DB(`ensemble_decisions.sizing_trace` JSON)** 이므로
  §12에 DB 경로를 하나 여는 첫 사례가 된다.
  ⚠ 수집기는 표준 라이브러리 전용 — `sqlite3`·`json`은 표준이라 제약 없음.
- **기대 효과**: 431차가 min() 합성으로 바꾼 뒤 "무엇이 실제로 구속하는가"를 매일 관측.
  한 게이트 100% 고착 시 316차 HurstGate(63% 차단)와 같은 방식으로 조기 발견.
- **비용·위험**: 낮음. ⚠ `sizing_trace`는 **2026-08-14 이후 행에만** 있다 —
  그 이전은 NULL이며 **미측정이지 "압력 0"이 아니다**(계측 4원칙 ②).
- **선행 조건**: 없음(오늘 스키마 라이브 확인 완료).
- **우선순위**: **이번 주.**

### G-3. 존 체류시간 × 계측 샘플수 **자동 대조**

- **관측 근거**: P1-1을 발견한 방법이 *"80샘플 = 금지 존 80분"* 이라는 **산술 일치**였다.
  이 대조는 사람이 우연히 한 것이지 계측이 아니다. 470차 C1'이 등록한
  "열린 채 끝난 지표"와 같은 계열이지만, **탐지 규칙이 다르다** — 이쪽은
  *"샘플수가 특정 존 체류시간과 정확히 같으면 그 계측은 그 존에서만 돈다"* 이다.
- **제안**: 수집기 §12에 보조 열 `샘플/거래분` 추가. 매분 샘플러를 표방하는 지표의
  이 값이 **1.0에서 크게 벗어나면** `⚠조건부 의심`으로 표기.
  오늘 `ConfFloor` = 80/371 = **0.22** → 즉시 걸린다.
- **기대 효과**: "무조건 상태 샘플"이라 주장하는 계측의 실제 무조건성을 자동 검증.
  292·303·311·470차가 전부 사람이 뒤늦게 발견한 계열이다.
- **비용·위험**: 낮음. ⚠ 오탐 가능(설계상 조건부인 지표) → `benign` 화이트리스트 병행.
- **선행 조건**: F-1(먼저 고쳐야 기준선이 생긴다).
- **우선순위**: **26주 WFA 주기** — 468차 G-2·470차 C1'~C5'와 같은 목록에 편입.

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| ① 모의투자 4주 통산 양수 | 미표기 | **현재 충족 (20거래일 +836,870원)** 로 기록 | 오늘 실측(`entry_source='SYSTEM_AUTO'`, 포지션 단위). ⚠ 일변동 -473,373 ~ +1,074,209 → **④는 미충족** |
| ② CB 실발동 확인 | 미충족 | 유지 | §12 `CB_state` = `NORMAL`×2955 정상고착. 15:10 경로 실집행 여전히 **전기간 0건** (`exit_reason LIKE '%15:10%'` = 0) |
| ② 선행 F-1R (15:10 리허설) | 08-18로 이월 | **08-19로 재이월** | 사용자 실행 필요 항목. 오늘 15:05~15:09 진입이 없어 기회 없음 |
| ⑨ TOX-SEVERE-SPREAD | 473차 F-8 Phase A `INSUFFICIENT` | 유지 | 오늘 `spread_ticks ≥20` **1건(진입 아님)**, 진입 18건 중 `<8` 17 · `8-12` 1. **표본 진전 0** — ETA 7.1개월 진단 그대로 |
| 26주 WFA 재검증 목록 | 470차 C1'~C5' | **G-3 추가** | "조건부 샘플러 자동 탐지" |

---

## 4. 진입 승패 사후검증 (장후)

**4-0. 선행 확인**

| 항목 | 결과 |
|---|---|
| `daily_close` 완료 | ✅ `data/daily_close_done_20260818.txt` = `2026-08-18T15:40:16.064040` |
| EOD 재학습 완주 | ✅ `호라이즌 교체=6/6 가드보류=0 재학습=134.6s 로드=26.2s 합계=161.2s` · `Python 3.10.20 64-bit`(정상) · rows 39438 × 105열 |
| P8 스케일러 재적합 | ✅ `[P8] 스케일러 재적합 완료 n=500봉` · `[P8] session_state p8_last_success_date + eod_retrain_ok_date 기록 완료` (15:47:44) |
| DB 접근 | ✅ 오류 없음. 읽기전용 조회만 수행 |
| 수집기 stderr | ✅ 없음 |

> 🟢 **어제(08-17) P1 2건이 오늘 해소됐다.** 473차 후속2 [1] *"EOD 재학습 미완주"* 와
> [2] *"`session_state.json` P8 스탬프 2키 소실"* 이 오늘 정상 완주·정상 기록으로 확인됐다
> (NEXT_TODO **O-1 · O-2 완료**). ⚠ **O-3**(08-19 08:41 기동 **후에도** 두 키가 남아 있는지)는
> **미판정** — 내일 장전 점검으로 이월.

**4-1. 오늘 할일 4분류**

| # | NEXT_TODO 항목 | 당일 커밋 | 코드 실재 확인 | 상태 |
|---|---|---|---|---|
| O-1 | EOD 재학습 완주 확인 | — (관측) | `logs/retrain_eod_20260818.log` 17.4KB | ✅ **완료** — 완주 |
| O-2 | `session_state.json` P8 키 재생성 | — (관측) | `[P8] session_state … 기록 완료` 15:47:44 | ✅ **완료** |
| O-3 | P8 키 잔존(C-2 2단계) | — | — | ⬜ **08-19 장전으로 이월** |
| F-1H | 하트비트 1일차 | `b01f565`(471차) | `main.py` `[SchedForceExit]` | ✅ **완료** — 15:11:15 출현, `bar_pass=2회` |
| F-4M | 스키마 마이그레이션 1일차 | `90c66cb`(플레이스홀더 54개 정정) | `ensemble_decisions` **56컬럼** | ✅ **완료** — §4-2 참조 |
| F-1R | 15:10 강제청산 리허설 | — | — | ⬜ **미착수** (사용자 실행 필요) — 08-19 재이월 |
| G-1(471) | sizer_pressure 실측화 | `cc6a234` | `sizing_trace` **65행** | ✅ **완료** — 첫 실측 6.2% |
| F-8B(473) | spread 섀도 배선 | `d43d825` | `spread_ticks` **369행** | ✅ **완료** — max 28.0틱 |
| F-9(471) | entry_mode 폴백 가시화 | `7285026` | `entry_mode_fallback` **369행** | ✅ **완료** |
| G-3(473) | 점검 세션 직렬화 `.dailycheck.lock` | 없음 | 미구현 | ⬜ **미착수** — 오늘 3세션 동시 실행 재현(P2-9) |
| CB② | 08-29 주간회의 상정 | — | `9999` 유지 확인 ✅ | 🔄 진행 중 (§7) |

> ⚠ 상태가 `⚠완료주장-커밋없음`인 항목은 **없다.**

**4-2. 3원 대사 (포지션 단위)** — ✅ **완전 일치**

| 원천 | 질의 | 값 |
|---|---|---|
| 로그 | `[Position] 진입` | **18** |
| 로그 | `[진입체크]` 통과 | **18** |
| 로그 | `[체결진입]` | **18** |
| `ensemble_decisions` | `entry_executed=1` | **18** |
| `trades` | `COUNT(DISTINCT entry_ts)` | **18** |
| (참고) `trades` | `COUNT(*)` — 청산 레그 | 20 (부분청산 2 포함 — **불일치 아님**) |
| (참고) `ensemble_decisions` | 당일 총 행 | 369 (매분 루프 371분과 정합) |

**4-3. 케이스 카드 — 대표 4건** (전 18건은 §4-4 집계로 대체)

```
[CASE-03] 진입 10:08:58 SHORT · 2계약 (등급 A/원시 C · horizon=3m · hurst=mean-revert)
          → 손절1차 조기축소(-1.29pt) → 하드스톱(틱)[TP1미도달](-2.69pt)
          → 포지션 합 -3.98pt / -202,392원 · 보유 2분
 진입근거  10:08:58 [진입체크] SHORT→SHORT 2계약 A급(원시C) | sign✅ conf✅ vwap✅ cvd❌ ofi✅ fore❌ prev❌ time✅ risk✅ chas✅ coun✅ | conf=39.4%
           09:38:00 sizing_trace: qty_sizer_raw=3 → qty_auto=2, binding_gate=hurst(0.50), kelly=0.60
 예측 vs 실현  ATR=2.021 → 의도 손절폭 3.03pt. 실현 -2.69pt = **준수율 89%** (손절선 안쪽)
 승패판정  ❌ 패 — `tp1_reached=0` 확인 (진짜 손절)
 주 요인   E(손절 준수) 정상 · 방향 오판
 부 요인   2계약 — 당일 유일한 2계약 손실 2건 중 하나
 반사실    1계약이었으면 -101,196원 (손실 절반)

[CASE-11] 진입 14:03:01 SHORT · 1계약 (등급 A/원시 C · horizon=3m · hurst=mean-revert)
          → TP2(전량)(+2.54pt) → +125,360원 · 보유 3분
 승패판정  ✅ 승 — TP2 도달. 당일 TP2 5건 중 첫 건
 주 요인   추세 순응 (14시대 하락 가속 구간)

[CASE-13] 진입 14:19:01 SHORT · 1계약 (등급 A/원시 C · horizon=3m · hurst=neutral)
          → 하드스톱(틱)[TP1보호](+0.26pt) → +11,369원 · 보유 3분
 승패판정  ✅ 승 (**패 아님** — `tp1_reached=1`, `exit_trigger='보호트레일'`, `exit_outcome='이익'`)
 주 요인   TP1 도달 후 보호 스톱 히트
 반사실    protect_offset(ATR×0.25 ≈ 0.31pt)이 넓었으면 TP2까지 갔을 가능성 — **미검증**

[CASE-06] 진입 11:35:58 SHORT · 1계약 (등급 C/원시 C · horizon=5m · hurst=trend)
          → 하드스톱(틱)[TP1미도달](-4.78pt) → -240,642원 · 보유 3분 · **당일 최대 손실**
 예측 vs 실현  ATR=2.606 → 의도 손절폭 3.91pt. 실현 -4.78pt = **준수율 122% (초과)**
 승패판정  ❌ 패 — 당일 유일한 손절폭 초과 건
 주 요인   E(손절 준수 실패) — 슬리피지 또는 갭 히트
```

**4-4. 요인 집계** (청산 레그 20 = 포지션 18 + 부분 2)

| 청산 트리거 | 결과 | 건수 | 합계pt | 순손익(원) | 건당pt |
|---|---|---|---|---|---|
| `TP2` | 이익 | 5 | **+14.36** | +709,843 | +2.87 |
| `보호트레일` | 이익 | 8 | **+3.53** | +163,211 | **+0.44** |
| `손절1차` | 손실 | 2 | -2.66 | -136,395 | -1.33 |
| `하드스톱` | 손실 | 5 | **-15.07** | -761,743 | **-3.01** |
| **합계** | | **20** | **+0.16** | **-25,084** | |

**손익 구조 — 오늘의 핵심**

```
gross_pnl  = +8,000원   (총 +0.16pt × 50,000원)
commission = -33,084원  (20레그 × 평균 1,654원)
net_pnl    = -25,084원   ← 수수료가 손익을 뒤집었다
```

- 승률: 포지션 **12/18 = 66.7%** · 레그 13/20 = 65.0%
- 손익비: 이익 13레그 평균 **+1.38pt** vs 손실 7레그 평균 **-2.53pt** → **0.55**
- ⚠ **`exit_reason='하드스톱(틱)'` 13건 중 8건이 이익 청산**이다(`tp1_reached=1`).
  `exit_reason`만으로 세면 "손절 13건"이 되어 정반대 결론이 나온다 —
  465차 `tp1_reached` · 468차 G-3 로그 태그가 정확히 이것을 위한 것이다.

**손절 준수율** (진짜 손절 5포지션, 의도 = ATR×1.5)

| 진입 | 실현pt | ATR | 의도 손절폭 | 준수율 |
|---|---|---|---|---|
| 10:08:58 | -2.69 | 2.021 | 3.03 | 89% |
| 11:35:58 | -4.78 | 2.606 | 3.91 | **122%** ⚠ |
| 13:04:58 | -2.70 | 2.250 | 3.38 | 80% |
| 13:34:59 | -2.50 | 2.037 | 3.06 | 82% |
| 13:45:58 | -2.40 | 1.779 | 2.67 | 90% |

평균 92.6% · **초과 1/5(20%)**. 417차가 유일하게 유의했던 축(진입수량 vs 손절폭 초과비율)은
오늘 **전부 1계약**이라 재현 검정 불가.

**진입 프로파일**

- 방향: **18/18 SHORT (100%)**. 당일 1132.52 → 1079.70(**-52.8pt**) 추세 하락일이며
  레인지 73.1pt(5일평균 32.3pt). 편향이 아니라 시장 방향과 일치.
- 등급: `A급(원시C)`×12 · `C급`×6 — **`raw_grade`가 20건 전부 `C`**, 원시 A는 0건.
  상향은 체크리스트 통과 수 기반이며 **`vwap✅`가 전제**임을 12건 전부에서 확인
  (예: `10:15:58 … A급(원시C) | sign✅ conf✅ vwap✅ cvd✅ ofi✅ fore❌ prev✅ time✅ risk✅ chas✅ coun✅`).
  → 절대원칙 ③ **위반 없음**.
- horizon: 3m×12 · 5m×3 · 1m×3. **10m·15m·30m 0건** —
  CLAUDE.md 474차가 확정한 *"중기·장기 그룹은 구조적 도달 불가"* 와 일치(이상 아님).
- 계약수: 1계약×16 · 2계약×2. `qty≥3` **0건** — 470차 진단(증거금 상한이 2에서 먼저 자름) 재확인.

**4-5. 313차 가드 통과 여부**

| 갈래 | 적용 결과 |
|---|---|
| ① 일자단위 판정 | **관측일 1일 — 판정 보류.** §1의 P1-2·§5 R-1 모두 확정 결론을 내지 않았다 |
| ② overlap 보정 | 오늘 진입 18건 중 **8건이 14:03~14:45 42분에 집중** — 같은 레짐·같은 모델 스냅샷. 독립 관측치 아님. 손익비 0.55는 **1일 관측치 1개**로 취급 |
| ③ 이상치 분해(drop-worst) | 최대 손실 1건(11:35 -240,642원) 제거 시 순손익 **+215,558원**(흑자 전환). 최대 이익 1건(14:37 +168,377원) 제거 시 **-193,461원**. **부호가 양쪽으로 뒤집힌다 → 확정 결론 금지** |
| ④ 사전등록 — 기준 불변 | 합격선·`min_samples` 무변경. ⑨ 채널 문턱을 낮추지 않았다(진입 `≥20틱` 0건 → **INSUFFICIENT 유지**) |
| ⑤ 부호 일관성 | 최근 10거래일 일자 부호: `- - + + - - - - - +` (양수 **3일**: 08-04·08-12·08-13). **일관되지 않음** → 오늘 적자를 추세로 읽지 않는다 |

---

## 5. 수익률 향상방안

> ⚠ 아래 **R-1 하나만** 6칸을 채운다. 나머지는 아이디어 단계라 §3 고도화로 보냈다.

### R-1. TP1 보호트레일 `protect_offset` 감도 — **섀도 계측 등록 (집행 금지)**

| 칸 | 내용 |
|---|---|
| 근거 | CASE-13 외 보호트레일 8건(10:00:16 · 10:05:41 · 10:22:13 · 10:51:21 · 13:43:53 · 14:22:47 · 14:25:43 · 14:45:51). 요인 태그 **E(청산 집행)**. 8건 건당 **+0.44pt**인데 같은 날 TP2 도달 5건은 **+2.87pt** — 6.5배 |
| 변경 대상 | `main.py` `[SingleContractTP1Arm]` 산출부 — `protect_offset_pts` 현재 `ATR×0.25`(14:25:43 실측 `atr=1.2429 → 0.3107`). **섀도 컬럼만 추가**: `trades`에 `protect_offset_pts` + `offset×0.5 가정 시 청산가` 기록 |
| 기대효과 | 오늘 8건 중 되돌림이 0.31~0.62pt 구간이었던 건이 몇 건인지로 상한 추정. **오늘 표본으로는 정량화 불가** — 틱 단위 되돌림 폭이 DB에 없다 |
| 회귀 위험 | offset을 넓히면 TP1 도달 후 **이익이 손실로 뒤집히는** 건이 생긴다. 오늘 8건 중 최소 이익 +0.26pt(14:19)는 offset 0.62pt면 **적자 전환**했을 수 있다 |
| 검증 | 섀도 **20거래일** 누적 → `min_days` 충족 후 일자단위 부호 검정(`_sign_test_p()`). 합격선은 **사전등록 후 불변** |
| 표본 상태 | n=8, **관측일=1**. **313차 가드 ③에서 부호가 뒤집힌다(§4-5)** → **채택 보류.** 집행 변경 금지, 계측만 |

> 462차 P4 원칙 그대로다 — **"처방이 아니라 등록이다."**

---

## 6. 다음 거래일(2026-08-19) 관측 예정

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| O-1 | `grep -c "ConfFloorGuard\] state=" logs/20260819_SIGNAL.log` | F-1 적용 시 **≥300** 이고 값 **2종 이상**. 오늘 기준선 80건·1종 | 08-19 장후 |
| O-2 | `[CORE준비도] 축퇴 N/6` 중 `ofi_norm` 지속 분수 | 오늘 29/42(69%). **5거래일 누적**으로 일자단위 비교 후 판정 | 08-25 |
| O-3 | `session_state.json` `p8_last_success_date`·`eod_retrain_ok_date` | 08-19 08:41 기동 **후에도** 두 키 잔존 → C-2 종결. 소실 시 **P0 승격** | 08-19 장전 |
| O-4 | `[SchedForceExit]` 하트비트 | 15:11 출현 + `bar_pass≥1`. 미출현이면 471차 F-2 회귀 | 08-19 장후 |
| O-5 | `ensemble_decisions.sizing_trace` 행수 / `binding_gate` 분포 | 오늘 65행 · `tox`45%. **tox가 2일 연속 최다면** G-2 우선순위 승격 | 08-19 장후 |
| O-6 | `spread_ticks ≥20` **진입** 건수 | 오늘 0건. 누적 6→7이 되면 ⑨ ETA 재산출 | 08-19 장후 |
| O-7 | `[MarginCap]` 건수 · `qty≥3` 체결 | `qty≥3` 발생 시 [28] 채널 각성 신호 — 오늘 0건 | 08-19 장후 |
| O-8 | `degraded=ON` 재발 여부 | 2일 연속이면 §12 `degraded` 재평가 (현재 `✅ 변동`) | 08-19 장후 |
| O-9 | 점검 세션 다이제스트 생성 시각 3개 | 오늘 16:31:54/16:31:59/16:32:16 — 동시 실행 재현. G-3 우선순위 근거 | 08-19 |
| O-10 | `[Brier] 과신` 이동평균 | 오늘 최대 0.367. 0.40 접근 시 P1 승격 | 08-19 장후 |
| F-1R | **15:10 강제청산 리허설** (사용자 실행) | 15:05~15:09 모의 1계약 진입 → 15:10 `[ForceExitPass]`→`[TimeExit]`→`[ExitAttempt]` 출현 + `안전망 발동`(ERROR) **미출현** | 사용자 지정 |

---

## 7. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| **CB② 복원** (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | **2026-08-29** | **11일** | 오늘 `9999` 유지 확인 ✅. **표본 진전 있음** — 연속 손절 카운터가 1→2→3까지 정상 증가했고 CB②가 켜져 있었다면 13:36:20에 **당일 정지**됐을 것이다. 그 이후 진입 **10건**(TP2 **5건** 전부 포함) 순 **+657,691원**이었다. → **08-29 주간회의에 이 반사실을 근거로 상정할 것.** ⚠ 1일 표본 — 확정 결론 금지 |
| ⑨ TOX-SEVERE-SPREAD 처분 | 미정 | — | 473차 F-8 Phase A `INSUFFICIENT`(ETA 7.1개월). 오늘 표본 진전 **0**. **주간회의 안건** (선행조건 유지 vs 26주 WFA 이관) |
| 브랜치 격차 처분 | 미정 | — | `v9-dev` ↔ `origin/dev` = **320/315**. 오늘 MW0602 `dev`의 설정 불변식 `미발견` 0건이 473차 후속2 [5] 진단을 뒷받침. **주간회의 안건** |
| ⑧ 사이징 재설계 — G-1 라이브 확인 | 미정 | — | ⚠ **미충족 유지.** `사이즈 축소 ×0.6` 오늘도 확인 필요하나, `[Sizer]` 65회 전부 `신뢰도배수=0.6`이다. **이것은 G-1의 `_pre_retrain_done` ×0.6이 아니라 별개의 신뢰도 배수**다 — 혼동 금지(invariants §2-B). ⑧ 해제 진행 금지 |

---

## 8. dev_memory 반영

- `DECISION_LOG.md` 추가: `## 2026-08-18 (MW0602 475차 — 장후 점검)`
- `NEXT_TODO.md` 추가 체크박스: **14건** (F-1~F-4 · G-1~G-3 · O-1~O-10 중 신규분)
- 완료 처리한 기존 항목: **O-1**(EOD 완주) · **O-2**(P8 키 재생성) · **F-1H**(하트비트 1일차) ·
  **F-4M**(스키마 마이그레이션 1일차) — 4건
- 이월: **O-3**(P8 키 잔존) · **F-1R**(15:10 리허설) · **G-3**(세션 직렬화)
