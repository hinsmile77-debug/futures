# 미륵이 일일 점검 — MW0602 / 2026-08-18 / 장전(pre)

- 점검 시각: **16:32 KST** · PC `MW0602` · 브랜치 `dev` ✅
- HEAD `90c66cb` · 미커밋 **3건**(CRLF 제외 실측) · 당일 커밋 **0건**
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260818_pre.md` (74.7KB, 수집기 정상 종료)
- 수집기: `--pc MW0602` override 정상 (`MW0602 (override · host=claude)`) — 산출물 `UNKNOWN` 아님

> ⚠ **파일명에 `-pre` 접미사를 붙였다.** 같은 16:32에 `pre`/`intra`/`post` 세 국면 다이제스트가
> 동시에 생성됐다(§1 P2-2). 470차 `483d41a`(동시 세션 reset 유실) 전례가 있어 날짜본 단일
> 파일에 append 하지 않고 국면본으로 분리했다.

> ⚠ **이 점검은 장 마감(15:40) 이후인 16:32에 실행됐다.** 예약 트리거가 08:57±5분이 아니었다
> (§1 P2-1). 따라서 "장전 자격"은 **08:40~09:00 로그의 사후 판독**으로 판정했다 —
> 실시간 개입 여지는 없었다. 판정 자체는 로그가 전량 남아 있어 유효하다.

---

## 0. 한 줄 결론

**2026-08-18 미륵이는 절대원칙 6종을 위반하지 않았고 장전 기동 자격도 충족했다(P0 0건).
다만 "안전장치가 켜져 있다"와 "작동한다"가 갈리는 계측 결함 2건이 새로 확정됐다 —
G-1 사이드카 `label_scheme`이 EOD 경로에서 구조적으로 항상 `None`이고(실전전환 ⑧ 선행조건이
영영 안 풀린다), 470차 L2 ConfFloorGuard 상태샘플이 잘못된 calibrator를 참조해 진입 허용
290분 전체가 미계측이다.**

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | **정상** | 08:40:41 기동(PID 2836) · RF 6호라이즌 `ready=True` · 08:45 실시간 사전구독 · 08:55 canary z경고 11/12 · 08:58:16 레짐 `NEUTRAL`(VIX=14.2) · 08:58:16 `장전 준비 완료 ✅` · 설정 불변식 24행 전부 `일치`·`미발견` 0종 |
| 장중 | 참고(본 리포트 범위 밖) | 매분 루프 커버리지 **371/371분 (100.0%)** · 진입 18포지션 · 5초 초과 블로킹 0건 |
| 장후 | 참고(본 리포트 범위 밖) | 15:40 `daily_close_done` · 15:47:44 EOD 재학습 완주(6/6, 17.4KB) · 잔여 포지션 0 |

**절대원칙 준수 여부**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | **준수 (청산 대상 없음)** | 최종 청산 14:45:51. `15:11:15 [SchedForceExit] 15:11 점검 — status=FLAT engine=0ct broker_cached=0ct bar_pass=2회 → 청산 대상 없음(정상)`. **"청산 로그 없음"이 아니라 "청산할 것이 없었음"이다.** 471차 F-2 하트비트 첫 라이브 관측 성공(§1 확정사항) |
| ② | Circuit Breaker 5종 | **준수(발동 없음이 정상)** | `[CB] 연속 손절 1/2/3회` 카운터는 올라갔으나 `CB_CONSEC_STOP_LIMIT=9999`라 정지 없음 — 설계대로. CB①④⑤ 미발동, 5초 초과 파이프 지연 0건(최대 2,297ms). §12 `CB_state` NORMAL×2955 |
| ③ | CORE 피처 호라이즌 그룹별 | **준수** | 진입 18건의 체크리스트 미통과 항목은 `ofi`×14 `chas`×12 `cvd`×7 `prev`×6 `fore`×5 — **`vwap` 미통과 진입 0건**(강제 X 규칙 유지). 진입 호라이즌 전량 1m/3m/5m = 단기 그룹(474차 도달성 구조와 일치) |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | **준수** | `0xC0000409`·`STACK_BUFFER` 0건. `logs/crash_fault.log` 기동 등록(08:40:41), 무응답 흔적 없음. 15:40:31 `[Shutdown] 정상 종료 플래그 기록` |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | **해당 사건 없음** | Cybos TR 수신 정상(`CpTd0723`·`CpSvrNew7221`·`CpSvr8111`), 파라미터 혼동 징후 로그 0건 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | **준수** | 자동 백테스트 큐·자동 통합 로그 0건 |

**설정 불변식** — 수집기 §3 표 **24행 전부 `일치`, `불일치` 0건, `미발견` 0건. 이상 없음.**

> 📌 2026-08-17 MW0601 장전이 보고한 **`미발견` 5종은 MW0602 `dev`에서 재현되지 않는다.**
> 473차 후속2 [5]가 원인을 "브랜치 분기"로 확정했는데, 오늘 `dev` 실측이 그 반증 방향의
> 근거가 된다(해당 5종이 `dev`에는 있고 `v9-dev`에는 없다). **새 안건이 아니다** —
> `NEXT_TODO` "브랜치 격차 처분을 주간회의 안건으로"에 이미 등록돼 있다.

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**해당 없음.** 절대원칙 6종 전항 준수, 장전 기동 자격 충족.

---

### P1 — 정확성·관측 훼손

#### 1-1. G-1 사이드카 `label_scheme`이 EOD 경로에서 **구조적으로 항상 `None`** — 실전전환 ⑧ 선행조건이 열리지 않는다

- **증상**: 468차 G-1(`MODEL_LABEL_STATE_UNLOCK_ENABLED`)의 판정 재료인 사이드카
  `label_scheme`이, **오늘 EOD가 새로 쓴 6개 파일 전부에서 `null`**이다. 그래서 장전
  게이트가 `불일치 6/6`으로 판정한다. 이 값은 **내일도, 그 다음날도 바뀌지 않는다.**

- **근거**:
  ```
  2026-08-18 08:55:13 [INFO] SYSTEM: [EntryGate] 모델 레이블 체계 미확인(장전) — 사이즈 제한 유지 (×0.6)
    | 불일치 6/6 — 1m:label_scheme없음(구버전), 3m:label_scheme없음(구버전), 5m:label_scheme없음(구버전) …
  2026-08-18 08:55:13 [INFO] SYSTEM: [EntryGate] 전일 EOD 모델 적재 확인 — 사이즈 제한 해제 (×0.6 → ×1.0, 재학습 불요)
  ```
  `logs/20260818_SYSTEM.log`

  그런데 사이드카는 **구버전이 아니다.** 오늘 15:45~15:46 EOD가 새로 썼고, 키는 있는데 값이 없다:
  ```json
  // model/horizons/gbm_1m_meta.json  (mtime 2026-08-18 15:45:35)
  { "horizon":"1m", "written_at":"2026-08-18 15:45:35", "source":"eod",
    "train_end_ts":"2026-08-18 14:38:00", "acc":0.4327, "acc_kind":"cv",
    "label_scheme": null, "label_param": null }
  ```
  6호라이즌(1m·3m·5m·10m·15m·30m) 전부 `label_scheme: null` — 실측 확인.

- **원인 (코드 실측으로 확정)**:
  ```python
  # retrain_eod.py:320  ← EOD 주경로. X와 y_dict를 직접 만들어 넘긴다
  result = retrainer.retrain_now(X=X, y_dict=y_dict, feature_names=feature_names,
                                 force=False, intraday=False, full_cv=True)

  # learning/batch_retrainer.py:507-509
  if X is not None and y_dict is not None:
      self._last_label_scheme = None        # ← 여기서 항상 None으로 초기화
      self._last_label_params = {}

  # learning/batch_retrainer.py:1246  ← 사이드카 기록
  "label_scheme": getattr(self, "_last_label_scheme", None),
  ```
  값을 채우는 두 지점(`:1683` Phase 2 경로, `:2190` 레이블 생성 루프)은 **둘 다
  `retrain_now`가 X/y를 직접 만들 때만 지나간다.** `retrain_eod.py`는 X/y를 밖에서
  만들어 넘기므로 그 두 지점을 건너뛴다 → `:508`이 `None`으로 리셋 → 사이드카에 `null`.

  `batch_retrainer.py:1681-1683` 주석이 이 사고를 **정확히 예고**하고 있었다:
  > *"여기를 빼먹으면 EOD 주경로(Phase 2)로 저장된 사이드카가 `label_scheme=None`이 되고,
  > 런타임은 '모른다'로 읽어 사이즈 제한을 영영 안 푼다(보수 폴백이라 안전하되 **G-1이
  > 무력화된다**)."*

  부수 결함: `learning/model_meta.py:56`이 이 상태를 `"%s:label_scheme없음(구버전)"`으로
  찍는다 — **원인을 오귀속한다.** 사이드카는 최신본이고 값만 비어 있다.

- **기준 위반**: CLAUDE.md 실전 전환 기준 **⑧ 선행조건**(`invariants.md` §2-B —
  *"G-1 검증은 `불일치 0/6`이 찍히고, F-1 마커 없이도 해제되는 것을 봐야 끝난다"*).
  9단계 STEP 3(EOD 재학습) 산출물의 메타 무결성.

- **영향**: 사이즈 축소 `×0.6`은 **F-1**(`PRE_RETRAIN_DONE_BY_EOD_ENABLED`, 전일 EOD 마커)로
  해제되므로 **오늘 매매에는 영향이 없다**(로그 두 번째 줄, 그리고 `[Sizer]` 65회 전량
  `안전배수=1.00(정상)`). 훼손된 것은 **관측**이다 — ⑧ 해제 선행조건인 G-1 라이브 검증이
  **원리적으로 도달 불가**한 상태로 5거래일째(08-14 배포 이후) 방치됐고, 그대로 두면
  다음 세션이 "아직 표본이 안 쌓였다"로 오독한다.

- **신규 여부**: **신규**(원인 규명 기준). 증상 자체는 `invariants.md` §2-B가
  *"G-1 최초 검증 기회는 2026-08-17(월) 장전"*으로 예고했고 08-17이 휴장이라 미검증으로
  남아 있었다. **오늘 08-18은 08-14 EOD 사이드카가 존재하는 첫 정상 거래일이며, 그 기회에서
  `불일치 6/6`이 나온 것이 "기다리면 풀린다"를 반증한다.**

- ⚠ **실패 계열**: FP-CRITICAL(학습분포 저장 함수 미호출 → PSI=0.0 2개월) ·
  15:10 1차 경로(구조적 도달 불가 6개월) · TOX-SEVERE-SPREAD 섀도(배선 부재 1개월)와
  **같은 유형**이다 — "켜져 있는데 도달 불가".

---

#### 1-2. 470차 L2 ConfFloorGuard 상태샘플이 **잘못된 calibrator를 참조** — 진입 허용 290분 전량 미계측

- **증상**: "매분 **무조건** 1줄"로 설계된 상태 샘플이 오늘 **80줄**만 남았고,
  **80줄 전부 `state=ZONE_BLACKOUT`**이다. 80분은 진입 금지 존(`OTHER` 70분 +
  `EXIT_ONLY` 10분)과 **정확히 일치**한다. 즉 **진입 허용 290분에는 샘플이 0건**이다.

- **근거**:
  ```
  $ grep -o "ConfFloorGuard] state=[A-Z_]*" logs/20260818_SIGNAL.log | sort | uniq -c
       80 ConfFloorGuard] state=ZONE_BLACKOUT
  ```
  (`OK` / `BLOCKED` / `RAW` / `NO_CAL` / `UNKNOWN` **0건**)
  첫 샘플 `11:50:58`, 마지막 `15:09:00`.
  같은 날 `[Ensemble] dir=` 는 **364건** — `compute()` 자체는 매분 돌았다.
  참고로 **2026-08-14는 `state=` 샘플 0건**(L2 배포일)이라 오늘이 첫 라이브 표본이다.

- **원인 (코드 실측으로 확정)**:
  ```python
  # model/ensemble_decision.py:294   self.calibrator = None  # ← horizon_calibrator 주입 슬롯
  # model/ensemble_decision.py:297   self.ensemble_calibrator = PredictionCalibrator(method="platt")
  # main.py:464                      self.ensemble.calibrator = self.horizon_calibrator
  #                                  → MultiHorizonCalibrator (learning/calibration.py)

  # ❌ _sample_conf_floor_state (470차 L2, :416~427)
  _cal = getattr(self, "calibrator", None)      # ← MultiHorizonCalibrator 를 집는다
  ...
  elif not _cal.is_fitted:      # MultiHorizonCalibrator.is_fitted(self, horizon) 는 **메서드**
                                #  → 바운드메서드는 항상 truthy → not ... == False → 통과
  ...
  _out_max = _cal.output_max    # MultiHorizonCalibrator 에 이 속성이 **없다** → AttributeError

  # ✅ 형제 함수 _check_conf_floor_consistency (403차, :355)
  _cal = self.ensemble_calibrator                # ← 이쪽이 맞다 (output_max 보유: calibration.py:735)
  ```
  예외는 같은 함수의 `except`가 삼키고 **`logger.debug`로만** 남긴다(`:438`) — DEBUG는
  운영 레벨에서 억제되므로 `logs/20260818_DEBUG.log`에 `상태 샘플 실패` **0건**,
  즉 **실패조차 보이지 않는다.**
  진입 **금지** 존은 `if not zone_allows_entry:` 조기 분기(`:414-416`)가 `_cal`을 건드리기
  전에 `ZONE_BLACKOUT`을 확정하므로 **그 80분만 살아남았다.**

- **기준 위반**: 468차 G-2 계측 원칙(*"조건부 로그는 넣지 말 것 — 무조건 찍히는 상태
  샘플이 있어야 감시가 성립한다"*, `ensemble_decision.py:399` docstring 자체 인용).
  L2가 고치려던 결함을 L2가 그대로 재현했다.

- **영향**: 수집기 §12 `ConfFloor` 행이 `ZONE_BLACKOUT`×80으로 채워진다. 오늘은
  `⚪ 표본부족`(관측일 1)이라 넘어갔지만, **관측일이 쌓이면 `🔴 고착(ZONE_BLACKOUT 100%)`으로
  뜬다** — 그러면 "블랙아웃 구간이 정상적으로 감지되고 있다"는 양성 신호로 오독되기 쉽다.
  정작 이 계측이 잡으려던 것(**진입 허용 구간에서 어떤 신호도 자동진입 하한을 못 넘는 상태**,
  2026-08-11 오전 88신호 전량 grade=X 사례)은 **한 건도 계측되지 않는다.**
  오늘 실제로 `09:22:57`·`10:24:57`에 엣지 트리거 WARNING이 두 번 떴는데, 상태 샘플은
  그 시각에 아무것도 남기지 않았다.

- **신규 여부**: **신규.** 470차 L2(2026-08-14 배포)가 도입한 회귀. 08-14는 샘플 0건이라
  드러날 수 없었고, 08-17은 휴장이었다.

---

#### 1-3. 미푸시 커밋 12건 — 멀티PC 공유 통로가 끊겨 있다

- **증상**: 로컬 `dev`가 `origin/dev`보다 **12커밋 앞서 있고 0커밋 뒤처져 있다.**
  그중 **11건이 `[MW0601]` 태그**인데, 전부 2026-08-17 17:57~18:09에 **이 저장소에서
  직접 생성**됐다(머지 커밋 0건).

- **근거**:
  ```
  $ git rev-list --left-right --count origin/dev...dev
  0	12
  $ git log -1 --format="%h %ci %s" origin/dev
  a8d9d8a 2026-08-14 19:54:01 +0900 [MW0602] 470차 후속8: 동시 세션 충돌 3건을 NEXT_TODO에 등록

  90c66cb 2026-08-17 18:09:08 [MW0602] 471차 이관 정합 수정: ensemble_decisions INSERT 플레이스홀더 54개로 정정
  0d3ccfc 2026-08-17 18:07:33 [MW0601] 471차 후속8 …
  … (총 12건, 머지 0건)
  cc6850a 2026-08-17 17:57:56 [MW0601] 472차 …
  ```

- **기준 위반**: CLAUDE.md 「멀티PC 작업 컨벤션」 —
  *"PC간 공유가 필요한 컨벤션은 반드시 이 CLAUDE.md처럼 git으로 커밋되는 문서에 적어야
  양쪽 PC 세션이 모두 따를 수 있다"*. **커밋했지만 push 하지 않으면 공유되지 않는다.**
  또 470차 등록 규약 *"다른 PC 브랜치의 커밋을 cherry-pick 하면 DECISION_LOG에 원 커밋 sha ·
  원 PC · 가져온 이유 세 가지를 남긴다(문서 체리픽도 예외 아님)"* 대상이다 —
  11건이 `[MW0601]` 태그로 `dev`에 들어왔는데, `DECISION_LOG.md`에서 이 11개 sha에 대한
  **원 sha·이유 기록을 찾지 못했다**(`grep`으로 `f57dc91`/`d43d825`/`cc6850a`/`b01f565`
  전부 0건).

- **영향**: 471~474차 구현(15:10 도달성 복구, F-8 배선, sizing_trace, [51] 채널)이
  다른 PC에서 보이지 않는다. `origin/dev` 기준으로 세션 차수를 맞추는 392차 관행도
  기준점을 잃는다. 2026-08-12 MW0601 브랜치 사고와 같은 계열의 씨앗이다.

- **신규 여부**: **부분 신규.** `NEXT_TODO`에 *"커밋 대기 — 473차 실질 변경 10파일"*과
  *"브랜치 격차 처분을 주간회의 안건으로"*가 있으나, **push 미실행 12건**과
  **[MW0601] 11건의 이관 무기록**은 별도 항목으로 등록돼 있지 않다.

---

### P2 — 운영 부담·기술부채

#### 2-1. 예약 점검이 08:57이 아니라 **16:32**에 돌았다 (O-7 판정)

`pre` 국면 다이제스트 생성 시각 `2026-08-18 16:32:16`. `NEXT_TODO` **O-7**
(*"예약 점검 시각(C-3) — 장전 점검이 08:57±5분에 도는지"*)의 판정 결과는 **❌ 미충족**.
장전 점검의 존재 이유(개장 전 자격 판정 → 필요시 개입)가 성립하지 않는다.
※ 예약작업은 PC별로 따로 등록해야 하며 git으로 공유되지 않는다(SKILL.md 함정 ③).

#### 2-2. 점검 세션 3개 동시 실행 — **2거래일 연속 재발** (O-11 판정)

```
evidence_MW0602-20260818_pre.md    16:32
evidence_MW0602-20260818_intra.md  16:32
evidence_MW0602-20260818_post.md   16:32
```
473차 후속2 [4]가 08-17에 같은 사고(16:48/16:49/16:55)를 보고하고 **G-3
(`dev_memory/.dailycheck.lock`)** 를 등록했으나 미구현 상태다. 오늘은 **세 국면이 같은
분(16:32)에 겹쳐** 08-17보다 위험이 크다 — `DECISION_LOG.md`(1.8MB)·`NEXT_TODO.md`(956KB)
동시 append 충돌 가능. MW0602 `483d41a`(동시 세션 reset으로 유실 → 재커밋) 전례 있음.
**본 리포트가 `-pre` 접미사를 쓴 이유가 이것이다.**

> 🔴 **추정이 아니라 실제로 충돌했다 — 본 세션은 커밋을 실행하지 못했다.**
> ```
> $ ls -la .git/index.lock
> -rwx------ 1 … 0 Aug 18 16:32 .git/index.lock      ← 0바이트 · 16:32(세 다이제스트와 같은 분)
> $ date  → 2026-08-18 16:50:44   (18분 경과, 45초 뒤 재확인에도 잔존)
> ```
> 471차 전례 기준(**mtime 10분 초과 = 스테일**)으로는 무시하고 제거해도 되는 잠금이나,
> **다른 세션이 아직 살아 있을 가능성을 배제할 수 없어 강제 제거하지 않았다.**
> → **산출물 3종(리포트·다이제스트·dev_memory)은 디스크에 기록됐고 커밋만 미실행이다.**
> 다음 세션 또는 사용자가 아래 명령으로 마무리할 것(§2 F-3의 push와는 별개, push 불필요):
> ```
> git add "docs/정기점검/매일점검/MW0602-20260818-점검리포트-pre.md" \
>         "docs/정기점검/매일점검/evidence_MW0602-20260818_pre.md" \
>         dev_memory/DECISION_LOG.md dev_memory/NEXT_TODO.md
> git commit -m "[MW0602] 475차: 장전 점검 — G-1 사이드카 label_scheme 구조적 소실 + ConfFloorGuard 상태샘플 분기편향"
> ```
> 이 관측으로 G-3(b) `.dailycheck.lock` 의 우선순위를 **이번 주 → 즉시**로 올린다.

#### 2-3. `20260818_BACKFILL.log` 부재 (O-9 판정)

`NEXT_TODO` **O-9**는 *"0바이트면 F-6을 P2 → P1 승격"*이었다. 실측은 **0바이트가 아니라
파일 자체가 없다** — 최근 생성분은 `20260814_BACKFILL.log`(0B, 08-14 18:45)가 마지막이고
08-13·08-18은 아예 없다. 과거 5개(`0809`~`0814`)는 **전부 0바이트**다.
→ 백필 스크립트가 "돌고 아무것도 안 썼는지" / "안 돌았는지"를 구분할 수 없다.
**F-6(기동·종료 흔적 로깅)의 필요성은 확정**이나, 판정 기준이 "0바이트"였으므로
문자 그대로 적용하면 판정 불가다. **승격 조건을 "0바이트 또는 파일 부재"로 정정**해야 한다.

#### 2-4. 장전 매크로·자본 로그 중복 실행

```
08:58:16 [System] 매크로 수집 완료 | VIX=14.2 SP500=-0.52% KRW=+0.26%
08:58:47 [System] 매크로 수집 완료 | VIX=14.2 SP500=-0.52% KRW=+0.26%   ← 31초 뒤 동일 값 재수집
```
`[Regime] NEUTRAL` · `PreOpen 투자자 warmup fetch 완료`도 각 2회.
`[Capital]`·`[CybosDailyPnl]`도 08:41:12~13에 각 2회. **값은 동일**하므로 판정 영향은
없으나, 외부 API를 두 번 때리고 있으며 로그 카운트 기반 계측이 2배로 뜬다.

#### 2-5. 실효자본(5천만원) ↔ 실제잔고(2,962만원) 괴리 **-40.7%** — MarginCap이 실제 binding

```
08:41:13 [Capital] 기준자본=50,000,000원(target) | 실제잔고=29,625,145원 | 괴리=-40.7%
                   | base_risk=1,500,000원(3.0%) | MAX_CONTRACTS=3 최대노출=164,472,000원 (레버리지 3.29x)
10:15:58 [MarginCap] SHORT 산출=3계약 → 증거금상한=2계약으로 축소
```
잔고는 최근 6거래일 안정적이다(08-10 29.76M → 08-18 29.63M) — **새 드로다운이 아니라
만성 상태**다. 다만 470차가 확정한 *"`MAX_CONTRACTS=3`은 현재 한 번도 구속하지 않는 죽은
파라미터이고 증거금 상한이 2에서 먼저 자른다"*가 **오늘도 그대로 재현**됐다
(사이저 3계약×4 → 실제 진입 최대 2계약, `[MarginCap]` 1건 + `[SizerMatch]` 2건).
→ 실전전환 ⑧의 남은 조건 1(실전 자본 재설정) 전까지 채널 [28]은 표본을 못 쌓는다.
**신규 안건 아님** — 470차 결론의 5거래일차 재확인이다.

---

### 확인 필요 (확정 아님)

- **1-2의 예외 발생 지점은 코드 대조로 특정했으나 런타임 스택으로는 미확인이다.**
  `_cal.output_max`에서의 `AttributeError`가 유력하지만, `_cal.is_fitted` 접근 단계일
  가능성도 배제하지 못했다(둘 다 같은 수정으로 해소된다). **판정 방법**: F-2 적용 후
  다음 거래일 `state=OK|BLOCKED` 샘플이 진입 허용 분 수(≈290)만큼 나오는지 확인.
- **1-3의 11개 `[MW0601]` 커밋이 cherry-pick인지, MW0601 세션이 이 PC에서 직접 커밋한
  것인지 확정하지 못했다.** 로컬에 `v9-dev` 참조가 없어(`git rev-list v9-dev...` 실패)
  원 sha 대조가 불가능하다. **판정 방법**: `git fetch --all` 후 `origin/v9-dev`와
  `git cherry` 또는 `git log --format=%H %s`로 제목 대조.
- **§12 `전략판정` `UNDERPERFORM` 100%(7건/7일) 고착**은 오늘도 유지됐다
  (`strategy_report_20260818_154016.txt` 판정 `INSUFFICIENT`, 권고 *"데이터 부족(60일) —
  5일 이후 재판정"*). **표본 7일 · 313차 원칙에 따라 확정 결론 보류.**
  단 461차가 분모를 `mdd_pct_of_capital`로 고친 뒤에도 배너가 `MDD=143.3%(누적손익peak대비)`를
  표시하는 점은 **표기 축이 남아 있는 것이며 판정에는 안 쓰인다**(461차 규약대로) — 오독 주의.
- **`ScalerFloor` 1,308건 · `Model` 극단 z-score 144건 · `WeightCollapse` 75건**은
  당일 변동성이 이례적이었던 것(레인지 73.1pt vs 5일평균 32.3pt, `IntradayRegime`이
  `CRASH`에 3회 진입)과 분리되지 않는다. **1일 표본으로 판정 금지** — 추세를 볼 것.

### 이미 반영된 사안 (신규 아님)

이 절을 비워두지 않았다. **확인한 결과 아래는 전부 이미 코드/기록에 있다.**

- **`TOXICITY_SEVERE_SPREAD_BLOCK_ENABLED=False`** — 근거는 `config/settings.py:4770-4781`
  311차 주석에 상세히 있다. 468차가 `documented_disabled_flags`에 등록해 적신호에서 뺐다.
  **"근거 없는 비활성 게이트"로 다시 올리지 말 것**(`invariants.md` §3-B 오탐 종결 항목).
- **`CB_CONSEC_STOP_LIMIT=9999`로 CB② 미발동** — 모의투자 한정 예외. `[CB] 연속 손절 3회`가
  찍히고도 정지 안 하는 것이 **정상**이다.
- **`FP_CRITICAL_GRADE_BLOCK_ENABLED=False`·`CB3_P4_GRADE_BLOCK_ENABLED=False`** — 한시예외 유지.
  오늘 PSI=0.034 (CLEAR), `cvd=0.238`로 371차 분위수 재설계 이후 정상 범위에서 오르내림.
- **[6] Hurst 완화** — 2026-07-15(333차 후속) 배포 완료. 리포트가 다시 권고해도 신규 안건 아님.
- **사이저 3계약 → 실제 2계약 눌림(수집기 §11 적신호 1)** — 470차가 원인을 `MarginCap`으로
  확정했다. **게이트 배수 문제가 아니다**(오늘 `binding=hurst(0.50)` 1건 /
  `binding=없음(안전군·상한·증거금 경로)` 1건).
- **`전략판정 UNDERPERFORM` 고착(적신호 10)** — §12가 이미 `🔴 고착`으로 표시 중이며
  `INSUFFICIENT`(60일차) 사유가 리포트에 명시돼 있다.
- **수집기 §2·§11의 "미커밋" 건수** — 이 리포트는 `git -c core.autocrlf=true` 기준
  **3건**을 인용했다. 샌드박스 raw `git status`의 528건은 **CRLF 아티팩트이며 재인용 금지**
  (473차 후속2 [5] 확정, F-5로 등록됨).

---

## 2. Fix 작업 구현계획

> 본 점검은 **장 마감 후(16:32)** 실행됐다. 라이브 프로세스는 15:40:31에 정상 종료했으므로
> 적용 가능 시간대이나, **F-1·F-2는 EOD 재학습(15:47:44) 이후이므로 오늘 밤 적용해도
> 08-19 EOD가 새 코드로 돈다.** 08-19 장전 기동 전까지 적용을 권고한다.

### F-1. EOD 경로에서 `label_scheme` 소실 — P1 · 대응 이상점 1-1

- **원인 가설**: `retrain_eod.py`가 X/y를 **밖에서** 만들어 `retrain_now(X=…, y_dict=…)`로
  넘기므로, `batch_retrainer.py:507-509`의 "호출자 주입 시 `None` 리셋" 가드에 걸린다.
  그 가드 자체는 **옳다**(직전 학습 값이 이번 pkl의 규칙인 양 새겨지는 것을 막는다).
  빠진 것은 **호출자가 자기가 쓴 규칙을 함께 넘기는 경로**다.

- **변경 파일**:
  - `learning/batch_retrainer.py` — `retrain_now()` 시그니처에
    `label_scheme: Optional[str] = None`, `label_params: Optional[dict] = None` 추가.
    `:507-509` 블록을 *"호출자가 넘겼으면 그 값을, 안 넘겼으면 `None`"* 으로 변경:
    ```python
    if X is not None and y_dict is not None:
        self._last_label_scheme = label_scheme          # 넘겼으면 그 값, 아니면 None
        self._last_label_params = dict(label_params or {})
    ```
    ⚠ **기본값을 `None`으로 유지**하는 것이 핵심이다 — 모르면 제한 유지라는 보수 폴백
    (`settings.py:477`)을 깨뜨리지 않는다.
  - `retrain_eod.py` — 레이블을 만드는 지점(`y_dict` 생성부)에서 실제로 쓴 규칙을 캡처해
    `:320` 호출에 `label_scheme=…, label_params=…`로 전달.
  - `learning/model_meta.py:56` — 메시지를 원인별로 분기:
    `label_scheme없음(구버전)` → 키 자체가 없을 때만. 키는 있고 값이 `null`이면
    `label_scheme=null(기록경로 누락)`. **오귀속을 없애는 것이 목적이다.**

- **회귀 위험**: `retrain_now` 호출부가 여럿이다(`retrain_intraday.py:92`,
  `scripts/eod_retrain.py:95`, 장중 서브프로세스). **키워드 인자 + 기본값 `None`**이므로
  기존 호출부는 무변경으로 현행 동작을 유지한다.
  ⚠ 반환 시그니처는 건드리지 않는다(457차 주석 — 3-튜플 언팩 호출부가 여럿이라 조용히 깨진다).

- **절대원칙 저촉 여부**: **없음.** 6종 어느 것도 건드리지 않는다.
  게이트를 **여는** 변경이 아니라 게이트의 **입력을 채우는** 변경이다 — G-1이 실제로
  해제될지는 사이드카 값이 채워진 뒤 런타임이 판정한다.

- **런타임 주의**: `retrain_eod.py`·`batch_retrainer.py`는 **`py310_64` 전용**(191차 OOM 결정).
  `model_meta.py`는 `py37_32` 런타임(main.py)에서 로드된다 — **두 런타임 모두에서 컴파일
  확인 필요.** COM 콜백 밖이므로 절대원칙 ④ 무관.

- **검증 방법**:
  1. 08-19 EOD(15:45~) 후 `model/horizons/gbm_*_meta.json` 6개에
     `label_scheme` ∈ {`fixed`,`rolling_sigma`,`atr`} · `label_param` non-null.
  2. 08-20 장전 08:55 `[EntryGate] … 불일치 **0/6**` 관측.
  3. ⚠ **그것만으로 G-1 검증 완료가 아니다.** `invariants.md` §2-B가 못박은 대로
     *"F-1 마커 없이도 해제되는 것"*을 봐야 끝난다 — `data/eod_retrain_done_*.txt`가
     없는 날(예: 재학습 실패일)에도 `×1.0`이 유지되는지 확인. 그 전에는 **⑧ 해제 금지.**

- **섀도 계측 필요 여부**: **불필요.** 게이트 신설이 아니라 기존 게이트의 입력 복구다.

- **적용 시점**: **오늘 밤(08-19 장 시작 전).** 하루 미루면 검증 기회가 하루 밀린다.

- **결정 필요 사항**: 없음.

---

### F-2. ConfFloorGuard 상태샘플의 calibrator 오참조 — P1 · 대응 이상점 1-2

- **원인 가설**: `_sample_conf_floor_state`가 형제 함수와 **다른 속성**을 읽는다.
  `self.calibrator`는 `main.py:464`가 주입하는 `MultiHorizonCalibrator`(호라이즌별 2차 압축)
  이고, `output_max`를 가진 것은 `self.ensemble_calibrator`(`PredictionCalibrator`,
  `learning/calibration.py:721 is_fitted` / `:735 output_max`)다.

- **변경 파일**:
  - `model/ensemble_decision.py` — `_sample_conf_floor_state()` (`:416`):
    `getattr(self, "calibrator", None)` → `getattr(self, "ensemble_calibrator", None)`.
    **1줄.** 형제 함수 `_check_conf_floor_consistency`(`:355`)와 동일 원천이 된다.
  - `model/ensemble_decision.py` — 같은 함수 `:437-438` `except` 처리:
    `logger.debug` → **`logger.warning`** 으로 승격하고 예외 타입·메시지를 함께 찍는다.
    ⚠ 이것이 F-2의 절반이다 — 오늘 결함이 **한 달 아니라 하루 만에** 잡힌 이유는
    80/290 비대칭이 눈에 띄었기 때문이고, 그마저 없었으면 조용히 죽었다.
    로그량은 실패 시에만 늘고 정상 시 0이다.
  - (선택) `_check_conf_floor_consistency`와 `_sample_conf_floor_state`가 같은
    `_resolve_conf_calibrator()` 헬퍼를 쓰게 해 **두 번째 오참조를 구조적으로 막는다.**

- **회귀 위험**: 낮음. 진입 허용 구간에 하루 ≈290행이 새로 생긴다(설계 예상치
  *"≈370행/일"*보다 적다 — docstring 추정과 일치). 판정·집행 경로 무영향(로그 전용).

- **절대원칙 저촉 여부**: **없음.** 순수 관측 계층.

- **런타임 주의**: `py37_32`(장중 본체). COM 콜백 밖.

- **검증 방법**: 08-19 `logs/*_SIGNAL.log`에서
  `grep -o "ConfFloorGuard] state=[A-Z_]*" | sort | uniq -c` 결과에
  **`OK` 또는 `BLOCKED`가 200건 이상** 출현. `ZONE_BLACKOUT` 건수는 그날 금지 존 분 수와
  일치해야 한다(오늘 기준 80). 실패 시 새 `WARNING`이 원인을 직접 말해준다.

- **섀도 계측 필요 여부**: 불필요.

- **적용 시점**: **오늘 밤.** F-1과 같은 커밋으로 묶어도 무방하나 파일이 겹치지 않으므로
  별도 커밋 권장(회귀 추적 용이).

---

### F-3. 미푸시 12건 push + `[MW0601]` 이관 기록 — P1 · 대응 이상점 1-3

- **변경 파일**: 코드 변경 없음. `dev_memory/DECISION_LOG.md`에 이관 기록 추가
  (원 sha 11건 · 원 PC `MW0601` · 가져온 이유 — CLAUDE.md 470차 규약 3요소).
- **절차**:
  1. `git fetch --all` → `origin/v9-dev` 확보 후 11건의 원 sha 대조(§확인 필요 참조).
  2. 대조 결과를 DECISION_LOG에 기록.
  3. `git push origin dev`.
- **회귀 위험**: push 자체는 무해하나 **다른 PC가 pull 하면 코드 세대가 바뀐다.**
  471~474차는 스키마 변경(`ensemble_decisions` 컬럼 추가)을 포함하므로 상대 PC의
  마이그레이션 상태를 먼저 확인할 것.
- **적용 시점**: **push는 주간회의 확인 후.** 브랜치 격차 처분이 이미 주간회의 안건이므로
  단독 결정 사안이 아니다. **DECISION_LOG 기록(1·2단계)은 즉시.**
- **결정 필요 사항**: `[MW0601]` 커밋 11건을 `dev`에 유지할지 — **주간회의 안건.**

---

### F-4. O-9 판정 기준 정정 — P2 · 대응 이상점 2-3

- **변경 파일**: `dev_memory/NEXT_TODO.md` — O-9 승격 조건을
  *"0바이트면"* → **"0바이트이거나 파일 자체가 없으면"** 으로 정정.
- **사유**: 오늘 실측이 "파일 부재"라 문자 그대로 적용하면 판정 불가다.
  `계측 4원칙 ②` — **"미측정"과 "0"은 다르다.** 판정문이 그 구분을 못 담고 있었다.
- **적용 시점**: 즉시(문서).

---

### 적용 순서와 커밋 계획

1. **커밋 ①** — F-1 (`batch_retrainer.py` + `retrain_eod.py` + `model_meta.py`)
   `[MW0602] 475차: EOD 경로 label_scheme 소실 복구 — G-1 도달성 확보`
2. **커밋 ②** — F-2 (`ensemble_decision.py` 1줄 + except 승격 + 헬퍼)
   `[MW0602] 475차: ConfFloorGuard 상태샘플 calibrator 오참조 수정 + 실패 가시화`
3. **커밋 ③** — F-3(기록)·F-4 + dev_memory
   `[MW0602] 475차: 장전 점검 기록 — 이관 무기록 · O-9 판정기준 정정`
4. **push는 3번 커밋 이후, 주간회의 확인 뒤.**

---

## 3. 고도화 방안

### G-1. "도달 불가 안전장치" 자동 탐지 — 수집기 §12의 **세 번째 축**

- **관측 근거**: 오늘 하루에 같은 유형이 **둘** 나왔다.
  ①(1-1) G-1 사이드카가 EOD 경로 구조상 영원히 `null`, ②(1-2) L2 상태샘플이 진입 허용
  구간에서 예외로 죽어 있음. 앞선 사례는 FP-CRITICAL(2개월)·15:10 1차 경로(6개월)·
  TOX 섀도(1개월)다. **매번 사람이 뒤늦게 발견했다.**
  §12는 *"한 값에 고착"*(죽은 지표)을, 470차 C1'은 *"열린 채 끝남"*을 잡는다.
  **셋째 형태 — "특정 조건 분기에서만 관측되는 지표"** 는 아무도 안 본다.
- **제안 내용**: `collect_evidence.py`에 `scan_branch_asymmetry()` 신설.
  `state=`/`불일치 N/M` 형태의 상태 샘플 태그에 대해 **관측 건수를 그날의 기대 분모와
  대조**한다. 기대 분모는 이미 수집기가 갖고 있다 —
  §7 매분 루프 커버리지(371분), §5 진입 가능/금지 시간 예산(290/80).
  `관측건수 / 기대분모 < 0.5` 이고 **값 분포가 단일**이면 `⚠️ 분기편향`으로 올린다.
  오늘이면 `ConfFloor: 80/370 (21.6%) · 값 1종` 이 즉시 걸렸다.
- **기대 효과**: 1-2 같은 결함의 발견 지연을 **1거래일 이내**로 압축. 정량 기준:
  신설 후 회고 적용 시 08-14(0/364)·08-18(80/370) 둘 다 탐지되어야 한다.
- **비용·위험**: 수집기 단독 변경(표준 라이브러리 전용 유지). 오탐 위험이 있으므로
  §12와 동일하게 **`benign` 화이트리스트**를 둔다(예: 조건부 발동이 설계인 태그).
  ⚠ 468차 G-2 경고대로 **조건부 로그는 애초에 감시 목록에 넣지 않는다** — 이 검사는
  *"무조건이라고 선언된 것이 무조건인가"* 만 본다.
- **선행 조건**: F-2(오늘의 실례가 고쳐진 뒤라야 회고 검증에서 전후 대비가 나온다).
- **우선순위 제안**: **이번 주.** 470차 C1'·C2'가 이미 미구현으로 밀려 있으므로
  같은 커밋으로 묶어 한 번에 처리할 것(중복 구현 금지).

### G-2. `retrain_now` 사이드카 필드 **완전성 검사** — EOD 자체 검증

- **관측 근거**: 오늘 사이드카 6개가 `label_scheme:null`인 채로 **정상 완료 마커
  (`eod_retrain_done_20260818.txt`, `horizons_replaced: 6/6`)** 를 남겼다.
  EOD는 자기 산출물이 반쪽인 것을 모른다. 468차 F-1이 만든 완료 마커가
  *"돌았다"* 만 증명하고 *"제대로 썼다"* 는 증명하지 않는다.
- **제안 내용**: `retrain_eod.py` P8 직후, 방금 쓴 6개 `gbm_*_meta.json`을 다시 읽어
  필수 키(`label_scheme`·`train_end_ts`·`acc_kind`·`source`)의 non-null을 검사하고,
  결손이 있으면 마커에 `meta_incomplete: 1m,3m,…` 줄을 추가 + `[EOD_RETRAIN] WARNING`.
  수집기 §4가 마커 전문을 이미 인용하므로 **다음 날 장전 점검에서 자동으로 보인다.**
- **기대 효과**: F-1이 회귀하면 다음 EOD가 스스로 신고한다. 검증 가능 형태:
  마커에 `meta_incomplete` 줄이 없는 날 = 사이드카 6/6 완전.
- **비용·위험**: `retrain_eod.py` 20줄 내외. `py310_64`. 실패해도 재학습 결과에
  영향 없도록 `try/except`로 감싸되 **`except`는 `logger.warning`**(F-2 교훈).
- **선행 조건**: F-1.
- **우선순위 제안**: 이번 주.

### G-3. 장전 점검 예약 시각 복구 + 국면 직렬화 — 운영

- **관측 근거**: 2-1(16:32 실행)·2-2(3국면 동시, 2일 연속).
- **제안 내용**: (a) MW0602 예약작업의 `pre` 트리거를 **08:57 KST**로 재설정
  (PC별 등록이며 git 공유 안 됨 — 등록 사실을 `dev_memory/NEXT_TODO.md`에 남길 것).
  (b) 473차 후속2가 등록한 **G-3 `.dailycheck.lock`** 구현(스테일 잠금 mtime 10분 초과 무시).
- **기대 효과**: 장전 점검이 개장 전 판정으로 기능. append 충돌 0건.
- **비용·위험**: 낮음. (b)는 이미 등록된 항목이라 **신규 안건 아님 — 미착수 상태 확인**이다.
- **선행 조건**: 없음.
- **우선순위 제안**: (a) 즉시 / (b) 이번 주.

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| 실전전환 ⑧ 선행조건 G-1 | `invariants.md` §2-B *"⏳ 미검증"* | **"⏳ 미검증 → 🔴 도달불가(F-1 적용 전)"** 로 격상 표기 | "기다리면 검증된다"가 반증됐다. F-1 없이는 관측 기회가 오지 않는다 |
| `CB⑤` 문서-코드 드리프트 | `invariants.md` §3-B **미해소** | 유지(재확인) | 오늘도 `CB_API_LATENCY_LIMIT` 미사용 · `CB_PIPE_PAUSE_MS=5_000` 실동작. 468차 F-5 범위 밖 — **발견하면 계속 보고**하라는 규약대로 재보고 |
| 26주 WFA 재검증 — 470차 C1'·C2' | 미구현 | **G-1(본 리포트)과 통합**해 이번 주 처리 | 셋 다 "감시 목록이 낡거나 비는 것"을 잡는 같은 계열. 따로 만들면 중복 |
| `MAX_CONTRACTS=3` 죽은 파라미터 | CLAUDE.md ⑧ 주석(470차) | 유지 | 오늘 5거래일차 재확인. **값 자체는 타당하니 되돌리지 말 것** |

---

## 4. 진입 승패 사후검증 (장후 한정)

**해당 없음(장후 전용).**

> 참고: 본 세션과 동시에 `post` 국면 다이제스트가 생성됐다
> (`evidence_MW0602-20260818_post.md`). 사후검증은 그쪽 세션의 몫이다 —
> **중복 착수 금지**(2-2 동시 세션 이슈).

## 5. 수익률 향상방안 (장후 한정)

**해당 없음(장후 전용).** §4 요인 집계가 없으므로 억지로 채우면 일반론이 된다(SKILL.md §5).

---

## 6. 다음 거래일 관측 예정

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| O-1 | `model/horizons/gbm_*_meta.json` 6개의 `label_scheme` | 6/6 non-null(`fixed` 등) → F-1 성공 | 2026-08-19 EOD 후 |
| O-2 | `[EntryGate] … 불일치 N/6` | **0/6** → G-1 1단계 통과. 6/6 유지면 F-1 미적용 또는 추가 결함 | 2026-08-20 08:55 |
| O-3 | `grep -c "ConfFloorGuard] state=OK\|BLOCKED"` | **≥200** → F-2 성공. 0이면 새 `WARNING` 전문 확인 | 2026-08-19 장후 |
| O-4 | `[ConfFloorGuard] 상태 샘플 실패` (WARNING 승격 후) | 0건이 정상. 출현 시 예외 타입이 원인을 특정 | 2026-08-19 장후 |
| O-5 | `eod_retrain_done_20260819.txt`의 `meta_incomplete` 줄 | 부재 = 사이드카 완전(G-2 적용 시) | 2026-08-19 EOD 후 |
| O-6 | 다이제스트 생성 시각 | **08:57±5분** → 2-1 해소 | 2026-08-19 08:57 |
| O-7 | `evidence_MW0602-20260819_*.md` 3종의 생성 시각 | 서로 다른 분 → 2-2 해소(G-3 lock) | 2026-08-19 |
| O-8 | `logs/20260819_BACKFILL.log` | 존재 + 0바이트 아님 → F-6 불요. 부재/0B면 F-6 **P1 승격** | 2026-08-19 |
| O-9 | `[SchedForceExit] … bar_pass=N회` | 매 거래일 출현. `안전망 발동`(ERROR) 미출현 | 상시 |
| O-10 | `ensemble_decisions.sizing_trace` non-null 일별 | 증가 지속(오늘 65) → F-4M 정착 | 2026-08-19 |
| O-11 | §12 `CORE안전` | 오늘 종일 `✅`. ⚠️ 재출현 여부(468차 F-3 이후 개선 유지 확인) | 2026-08-19 |
| O-12 | `[MarginCap]`·`[SizerMatch]` 건수와 `qty≥3` 체결 | `qty≥3` 0건 지속 예상. 발생하면 470차 결론 재검토 | 상시 |

**F-1R(15:10 강제청산 리허설) — 08-19로 재이월.** 오늘도 15:10 이전에 전 포지션이
닫혀 실집행 관측 기회가 없었다(마지막 청산 14:45:51). **사용자 실행 필요**:
15:05~15:09 모의 1계약 진입 후 15:10 관측. 정상 = `[ForceExitPass]` → `[TimeExit]` →
`[ExitAttempt]`가 15:10:00~05에 출현하고 `[SchedForceExit] … 안전망 발동`(ERROR) 미출현.
비용 약 1,600원. **실전전환 기준 ②의 선행 확인사항이다.**

**F-1H(하트비트 1일차) — ✅ 판정 완료.** `15:11:15 [SchedForceExit] 15:11 점검 —
status=FLAT engine=0ct broker_cached=0ct **bar_pass=2회** → 청산 대상 없음(정상)`.
471차 F-1(1차 경로 도달성 복구)·F-2(안전망 하트비트)가 **라이브에서 처음 확인**됐다.
단 이것은 *"경로가 살아 있다"* 이지 *"주문이 나간다"* 가 아니다 — F-1R이 여전히 필요하다.

**F-4M(스키마 마이그레이션 1일차) — ✅ 판정 완료.**
`ensemble_decisions` 56컬럼 · 08-18 신규 369행 · `sizing_trace` non-null **65**(사이저 호출
65회와 일치) · `spread_ticks`/`spread_extreme_shadow` 각 **369**(전 분봉).
471차 후속6(G-1 사이징 계보) · 473차 F-8 Phase B(섀도 배선) 정상 동작.
⚠ `binding_gate` 컬럼은 없다 — 해당 정보는 `sizing_trace` JSON 안에 있다
(`binding=hurst(0.50)` 등 `[SizerMatch]` 로그와 일치).

---

## 7. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| **CB② 복원** (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | 2026-08-29 | **11일** | 오늘 `9999` 유지 확인. 주간회의 상정 필요. ⚠ 오늘 `[CB] 연속 손절 3회`가 실제로 찍혔다 — **복원하면 오늘 같은 날 당일 정지**다. 복원 판단 시 이 날짜(08-18, 진입 18건 중 진짜 손절 5건)를 표본으로 쓸 것 |
| CB③-P4 재검토 (⑥) | 실전 전환 전 | — | `False` 유지. 30m 재도입/CB③ 기준 호라이즌 교체와 한 묶음 |
| FP-CRITICAL 재검토 (⑦) | 실전 전환 전 | — | `False` 유지. 오늘 PSI=0.034 CLEAR — 371차 재설계 후 정상 구간 오르내림이 계속 관측됨(누적 근거) |
| TOX-SEVERE-SPREAD 처분 (⑨) | 주간회의 | — | 473차 F-8 Phase A `INSUFFICIENT`(ETA 7.1개월). **문턱 낮추지 말 것.** 26주 WFA 이관 여부가 안건 |
| 실전전환 ⑧ 조건 1 (실전 자본 재설정) | 미정 | — | **G-1 라이브 확인이 선행조건**인데 F-1 적용 전까지 도달 불가(1-1). ⑧ 진행 금지 |

---

## 8. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-08-18 (MW0602 475차 — 장전 점검)`
- `NEXT_TODO.md` 추가 체크박스: **12건**(F-1~F-4, G-1~G-3, O-1~O-12 요약, F-1R 이월)
- 완료 처리한 기존 항목: **F-1H**(하트비트 1일차) · **F-4M**(스키마 1일차) ·
  **O-7**(예약 시각 — ❌ 판정) · **O-9**(BACKFILL — 기준 정정 필요) · **O-11**(3국면 동시 — 재발 확인) ·
  **O-12**(`CORE안전` — 종일 ✅)
