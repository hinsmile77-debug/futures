# 미륵이 일일 점검 — MW0601 / 2026-08-19 / 장전(pre)

- 점검 시각: 08:59~09:10 KST · PC `MW0601` · 브랜치 `v9-dev` (**정상** — MW0601 운영 브랜치)
- HEAD `624a275` · 당일 커밋 0건 · 미커밋 **459건 → 실질 4건** (§1 P2-2 참조)
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260819_pre.md`
- 예약 실행(08:57 KST). **코드 변경 0건 · 라이브 DB 미접근**(로그·설정·JSON 파일만)

## 0. 한 줄 결론

**⚠ P0 없음 — 오늘 거래 자격은 충족된다.** 전일 EOD 재학습 6/6 교체 성공, Cybos 모의투자 접속·잔고 정상,
08:45 선행구독·08:55 매크로·08:58:25 레짐 확정까지 설계 순서대로 밟았다.
다만 **단기 CORE 피처 `ofi_norm`의 스케일러가 전 호라이즌에서 identity로 강제되는 상태가 2거래일째**이고,
**EOD→익일 인계용 `session_state` 키 4종이 매일 소멸해 마커파일 폴백으로만 연결되고 있다.**

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | **조건부 정상** | 기동·접속·재학습·레짐 전부 정상. P1 3건(관측·피처 품질) |
| 장중 | — | 본 리포트 범위 밖 |
| 장후 | — | 본 리포트 범위 밖 |

**절대원칙 준수 여부**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | 해당없음 | 기동 시 `[Position] 저장 상태가 어제 데이터 — 무시` (08:41:13) · 포지션 FLAT |
| ② | Circuit Breaker 5종 | 정상 | 장전 발동 0건. CB⑤ 관련 최대 메인스레드 블로킹 **2,797ms** < 임계 5,000ms (`CB_PIPE_PAUSE_MS`) |
| ③ | CORE 피처 호라이즌 그룹별 | **⚠ 주의** | 단기 CORE `ofi_norm` 전 호라이즌 identity 강제 — **1-2** |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 정상 | `[FaultHandler] 활성화 PID=21612` (08:40:46) · 크래시·0xC0000409 0건 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 해당없음 | `broker=cybos` — Kiwoom COM 경로 미사용 (아래 참고 참조) |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 해당없음 | 장전 알파봇 로그 0건 |

> **참고 — 예약작업 지시문의 "선물 분봉 TR이 `OPT50029`인지" 항목은 Cybos 경로에서 해당 없다.**
> `OPT50029`는 `collection/kiwoom/api_connector.py`에만 존재한다(전수 grep: py 파일 5개소 전부 kiwoom).
> 오늘 `[Capability] broker=cybos … server=Cybos 모의투자`이므로 이 항목은 **`해당없음(Cybos)`** 이 정답이며,
> 오늘 로그 전체에 `OPT#####` 토큰 **0건**인 것은 결함이 아니다.
> → 장전 체크리스트(`references/phases.md` A절)에 브로커 분기를 명시하는 편이 낫다(**P2-3**).

**설정 불변식** — 수집기 §3 표에서 `일치` 22행, `미발견 ⚠` 5행.
5행은 전부 **`dev` 브랜치 전용 상수**이며 MW0601(`v9-dev`)에는 코드 자체가 없다. **기존 P1의 재발**이지
회귀가 아니다 — **1-3** 참조.

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**없음.**

장전 필수 확인 전항목 통과:

| 항목 | 결과 | 근거 |
|---|---|---|
| 브랜치 | ✅ `v9-dev` | `git branch --show-current` — 2026-08-12 오전환 사고 재발 없음 |
| 전일 EOD 재학습 | ✅ 성공 | `logs/retrain_eod_20260818.log` — `호라이즌 교체=6/6 가드보류=0 … 합계=340.2s` (15:50:48) |
| 재학습 런타임 | ✅ py310_64 | `Python : 3.10.20 64-bit` (15:45:08) — CLAUDE.md 191차 결정대로. **이상 아님** |
| 본체 런타임 | ✅ py37_32 | `launcher_20260819_084001_22417.log:12` `32-bit Python 확인: …\envs\py37_32\python.exe` |
| Cybos 접속 | ✅ | `[Capability] broker=cybos connect=Y/Y balance=Y/Y order=Y/N … server=Cybos 모의투자` (08:41:21) |
| 08:45 선행구독 | ✅ | `[EarlyWarmup] Cybos RT 08:45 선행 구독 시작 (프리장 봉 15봉 확보)` (08:45:21) |
| 08:55 매크로 | ✅ | `[System] 장 전 매크로 수집 시작 (1단계 — seed fetch)` (08:55:21) → 2단계 08:58:25 |
| 레짐 판정 | ✅ NEUTRAL | `[Regime] NEUTRAL (점수=0) \| VIX=15.2 (저공포) \| SP500=-0.69% (하락) \| USD/KRW +0.11% (중립)` (08:58:25) |
| 모델 6종 로드 | ✅ | `[Model] 1m~30m 로드 성공` (08:40:59) · `[RF] 로드 완료: 6호라이즌 ready=True` (08:41:02) |
| CB⑤ 여유 | ✅ | 최대 블로킹 2,797ms — 임계 5,000ms 대비 56% |

---

### P1 — 정확성·관측 훼손

#### 1-1. `session_state.json` EOD 인계 키 4종이 익일 기동 시 소멸 — 1차 경로 사실상 사망, 폴백만 살아 있다

- **증상**: 08-18 EOD가 기록한 4개 키(`p8_last_success_date`, `eod_retrain_ok_date`,
  `gbm_last_retrain`, `gbm_total_retrain_count`)가 08-19 기동 후 파일에서 **전부 사라졌다.**
  `auto_shutdown_done_date`도 `""`로 비어 있다.

- **근거**:

  기록된 것 — `logs/retrain_eod_20260818.log`
  ```
  2026-08-18 15:50:48,750 [INFO] EOD_RETRAIN: [EOD] 재학습 이력 저장: 2026-08-18 15:50 (7회)
  2026-08-18 15:50:50,057 [INFO] EOD_RETRAIN: [P8] session_state p8_last_success_date + eod_retrain_ok_date 기록 완료
  ```
  → 이것이 08-18의 **마지막** session_state 쓰기다(이후 로그 없음).

  남아 있는 것 — `data/session_state.json` (mtime 08-19 08:46)
  ```json
  {"date": "2026-08-19", "count": 1, "reverse_entry_enabled": false,
   "tp1_single_contract_mode": "atr_profit", "auto_shutdown_done_date": "", "today_open": 1026.48}
  ```

  폴백이 받아낸 흔적 — `logs/20260819_SYSTEM.log`
  ```
  2026-08-19 08:55:21 [INFO] SYSTEM: [PreRetrain] EOD 마커 파일 직접 확인 (1일 전: 2026-08-18)
                                     → PreRetrain 스킵 (session_state 미기록 보완)
  2026-08-19 08:55:21 [INFO] SYSTEM: [PreRetrain] 08:55 사전 재학습 스킵 — 1영업일 전(2026-08-18)
                                     EOD 재학습 성공 (동일 데이터 중복 불필요) | WarmupRetrain 예산 소진
  ```
  1차 경로(`main.py:5057~5072`)가 남겨야 할 `[PreRetrain] EOD 재학습 날짜 복원: …` 은 **0건**.
  `[SessionState] load failed` 경고도 **0건** — 파일이 깨진 게 아니라 조용히 기본 dict로 되돌아갔다.

- **기준 위반**: CLAUDE.md **계측 4원칙 ④(폴백 가시화)**. 또한 `retrain_eod.py:177~181` 주석이
  *"근본 수정: retrain_eod.py 완료 시점(15:47+)에 두 키를 모두 기록"* 이라고 선언한 수정이
  **실효를 내지 못하고 있다** — 고쳤다고 적힌 것이 안 고쳐진 상태.
  구조는 471차 F-1(15:10 강제청산 1차 경로 구조적 도달 불가)과 **같은 유형**이다.

- **영향**:
  1. **오늘 실손해 0.** 마커파일 폴백(`data/eod_retrain_done_20260818.txt`)이 정상 동작했고,
     마커는 `2026-07-01`분부터 34개가 남아 있어 정리 대상이 아니다(`scripts/monthly_cleanup.py`에
     `data/*.txt` 규칙 없음 확인). **폴백이 로그를 남기므로 은닉도 아니다.**
  2. 다만 **대시보드 자가학습 탭의 "마지막 재학습 / 재학습 횟수"가 매일 리셋**된다
     (`gbm_total_retrain_count`가 08-18에 7회까지 갔다가 오늘 0). 누적 관측 훼손.
  3. 폴백이 5영업일 창(`for _d in range(1, 6)`)에 의존하므로, 연휴가 5영업일을 넘거나 마커가
     한 번이라도 유실되면 **PreRetrain이 08:55에 중복 재학습을 돌려 09:00 파이프라인을 지연**시킨다.

- **신규 여부**: **어제 등록된 관측항목의 답** —
  `MW0601-20260818-점검리포트-post.md` §6 `O-3 P8 키 잔존 … 유지되는가` → **판정: 미잔존.**
  원인은 미규명이므로 아래 §2 F-1에 장후 조사 절차로 등록.

- **확정하지 않은 것**: 누가 지웠는지. `session_state.json`을 쓰는 코드는 `main.py`(4개소)와
  `retrain_eod.py`(2개소)뿐이고 **전부 read-merge-write**라 키를 지울 수 없다.
  `count` 키를 증가시키는 코드는 repo 전체에 없는데 현재 `count: 1`이다 — 즉 **미지의 쓰기 주체가
  있거나 파일이 한 번 사라졌다.** 장후 조사 대상(F-1).

---

#### 1-2. 단기 CORE `ofi_norm` 스케일러 identity 강제 — 장전 국한에서 **종일·전 호라이즌**으로 확산 (2거래일째)

- **증상**: `ofi_norm`의 raw std가 임계 0.05 미만이라 스케일러가 `mean=0, scale=1` identity로
  강제되는 사건이, 예전에는 프리장 warmup 몇 회에 그쳤는데 08-18부터 **거의 모든 refresh 이벤트**에서
  발생한다. 오늘 장전은 **7/7 = 100%**.

- **근거**:

  ```
  2026-08-19 08:45:21 [WARNING] SIGNAL: [ScalerRefresh] 1m CORE 'ofi_norm' raw_std≈0(0.0280) → identity(0,1) 강제 (FLAT 100% 방지)
  … 3m/5m/10m/15m/30m 동일 …
  2026-08-19 08:59:01 [WARNING] SIGNAL: [ScalerRefresh] 30m CORE 'ofi_norm' raw_std≈0(0.0441) → identity(0,1) 강제
  ```
  `logs/20260819_SIGNAL.log` · 장전 30건(5이벤트 × 6호라이즌) · 08:45:21 ~ 08:59:01

  일자별 추이 (`grep -c "CORE 'ofi_norm' raw_std"`):

  | 날짜 | refresh 이벤트 | CORE identity 총건 | 그중 `ofi_norm` | ofi_norm 비율 |
  |---|---|---|---|---|
  | 08-10 | 41 | 42 | 30 | 5/41 이벤트 |
  | 08-11 | 43 | 48 | 36 | 6/43 |
  | 08-12 | 44 | 18 | **0** | 0 |
  | 08-13 | 53 | 12 | **0** | 0 |
  | 08-14 | 47 | 18 | **0** | 0 |
  | 08-17 | **0** | 0 | 0 | ⚠ **미측정**(휴장 — refresh 이벤트 자체가 0) |
  | 08-18 | 48 | 216 | **204** | **34/48 = 71%** |
  | 08-19(장전) | 7 | 66 | **42** | **7/7 = 100%** |

  08-18은 오전에 그치지 않고 **14:56:59까지** 이어졌고, 결정적으로 **EOD 500봉 재적합에서도** 걸렸다:
  ```
  2026-08-18 15:50:50,041 [WARNING] SIGNAL: [ScalerRefresh] 30m CORE 'ofi_norm' raw_std≈0(0.0312) → identity(0,1) 강제
  2026-08-18 15:50:50,050 [INFO]    SIGNAL: [ScalerRefresh] ts=15:50 trigger=E_EOD … n=500 bars
  ```
  → 프리장 30봉 표본 부족 탓이 아니다. **최근 500봉 기준으로도 `ofi_norm`은 사실상 상수다.**

- **미측정 ≠ 0 확인(계측 4원칙 ②)**: 이 로그줄은 **139차(`1b85d98`)** 부터 존재한다
  (`git log -S"raw_std≈0" -- model/multi_horizon_model.py`). 따라서 08-12~08-14의 `0`은
  **진짜 0**(측정했고 정상)이다. **08-17만 미측정**이다.

- **대조군**: 같은 08:45 refresh에서 `cvd_divergence`(0.0202) · `vwap_position`(0.0000) ·
  `above_vwap`(0.0000)도 identity로 걸리지만 **08:45 1회뿐**(각 6건)이다 — 프리장 VWAP 미형성이라
  설명 가능하고 개장 후 해소된다. **`ofi_norm`만 09:02(0.0321)에도 해소되지 않았다.**

- **기준 위반**: CLAUDE.md **절대원칙 §3** — `OFI 불균형`은 단기 그룹(1m·3m·5m) CORE 피처이며
  `_CORE_MASK_EXEMPT_BY_HZ`로 AutoMask 면제까지 받는 보호 대상이다.
  또한 371차가 잔여 이슈로 등록한 *"`cvd_divergence`·`ofi_norm` 학습표본의 91%가 정확히 0.0인 점질량"*
  과 같은 현상의 라이브 발현으로 보인다.

- **영향**:
  - identity 강제는 FLAT 100% 고착을 막는 **의도된 보호장치**이므로 그 자체는 오작동이 아니다.
    실제로 오늘 FLAT 고착은 없다.
  - 그러나 결과적으로 `ofi_norm`은 **표준화 없이 원값(≈0 근방 상수)으로 GBM에 들어간다.**
    단기 CORE 3종 중 1종이 정보를 싣지 못하는 상태가 이틀째이고, 이것이 진입 등급 판정
    (`strategy/entry/checklist.py` 단기 그룹 체크리스트)의 한 축을 무력화할 수 있다.
  - **손익 영향은 미판정.** 표본 2거래일(그중 1일은 장전만) — 313차 원칙에 따라 확정 결론 보류.

- **신규 여부**: **신규.** `MW0601-20260818-점검리포트-{pre,intra,post}.md` 전문에
  `ScalerRefresh`·`raw_std` 검색 결과 **0건** — 08-18 당일에는 포착되지 않았다.
  `DECISION_LOG.md`의 `ofi_norm` 최근 언급(19057~21053행)은 전부 311차 SHAP 계측 결함 건이며
  **스케일러 축퇴와는 별개 사안**이다.

---

#### 1-3. 불변식 감시표 5행 `미발견` — **기존 P1의 6일째 재발**(F-1 미적용). 신규 아님

- **증상**: 수집기 §3에서 `MODEL_LABEL_STATE_UNLOCK_ENABLED` · `PRE_RETRAIN_DONE_BY_EOD_ENABLED` ·
  `ZONE_ENTRY_BAN_ENFORCE` · `ZONE_ENTRY_BAN_SHADOW_ENABLED` · `PIPE_LATENCY_EXCLUDE_MODEL_SWAP`
  5종이 `미발견 ⚠`로 뜨고 §11 자동 적신호 7건 중 **1~5번을 점유**한다.

- **근거**: `config/settings.py` 전문 grep 0건. repo 전체 grep에서도 코드 파일에는 없고
  스킬 참조문서·`collect_evidence.py`·`dev_memory`에만 존재.
  `git log -S` 결과 5종 모두 **`[MW0602]` 커밋 계열**(462차 `c63b4ee`, 468차 `f2332be`/`ce3d9d9`)이며
  `origin/dev` 전용이다. 감시표를 늘린 469차 스킬 커밋(`a581231`)만 `v9-dev`로 체리픽됐다.

- **기준 위반**: 계측 4원칙 **③(탈락 가시화)** — "이 브랜치에 없는 항목"과 "있는데 값이 틀어진 항목"을
  같은 `미발견`으로 표기하고 있다.

- **영향**: 매 점검이 상시 적신호 5건을 달고 나와 **진짜 불일치 1건이 묻힌다.**
  ⚠ 그리고 실질 기능 격차가 실재한다 — `invariants.md`가 **실전전환 ⑧의 선행조건**으로 못박은
  `MODEL_LABEL_STATE_UNLOCK_ENABLED`가 MW0601에 없다. **MW0601 관측만으로 ⑧을 판단하면 틀린다.**

- **신규 여부**: **기존**. `DECISION_LOG.md` 2026-08-14(470차 계열) `[신규 P1] 불변식 감시표 5행이
  '미발견'` + 2026-08-17 재확인 항목. **F-1(수집기 브랜치 스코프 분리)이 아직 미적용**이며,
  방금 확인 결과 `collect_evidence.py`에 `branch` 키·`해당없음` 렌더링 **미도입**이다.
  → 조치 우선순위를 올린다(§2 F-2).

---

### P2 — 운영 부담·기술부채

#### 2-1. `CybosProbe` 후보 1종이 COM 초기화 누락으로 **한 번도 실제 시험된 적이 없다**

- **근거** — `logs/20260819_PROBE.log` (10건, 08:58:25 / 08:58:55 2회전)
  ```
  [CybosProbe] CpSysDib.CpSvrNew7221 dispatch/request failed: (-2147221008, 'CoInitialize가 호출되지 않았습니다.', None, None)
  [CybosProbe] Dscbo1.FutureTrader  dispatch/request failed: (-2147221005, '잘못된 클래스 문자열입니다.', None, None)
  [CybosProbe] CpSysDib.FutureTrader / Dscbo1.FutureTrade / CpSysDib.FutureTrade — 동일 -2147221005
  ```
- **해석**: 5개 후보 중 4개는 `-2147221005`(클래스 문자열 없음) = **후보가 실제로 없다**는 정당한 결론.
  그러나 `CpSysDib.CpSvrNew7221` 하나는 `-2147221008` = **워커 스레드가 `CoInitialize`를 호출하지 않아
  디스패치 자체를 시도조차 못 했다.** 이 실패가 `[CybosInvestor] … reason=Cybos 선물 투자자 TR 미발견`
  결론에 합산되고 있다.
- **기준 위반**: 계측 4원칙 **②(미측정 ≠ 0)** — "없다"와 "못 봤다"가 같은 결론으로 흘렀다.
- **영향**: 선물 투자자 수급(외인/기관/개인) 미수집 상태가 계속된다
  (`investor=Y/N(fetch=0,runtime=N)`, `foreign=+0 individual=+0 institution=+0`, 미결제 `oi=149194`만 제공).
  **오늘 실손해 0** — 해당 피처는 이미 `program_*` 3종 폐기(451차) 계열로 학습에서 빠져 있다.
  다만 "TR이 없다"는 판정의 근거 1/5가 무효다.

#### 2-2. 수집기 §2 "미커밋 459건"은 **CRLF 아티팩트** — 실질 4건

- **근거**: `file CLAUDE.md` → `with CRLF line terminators`. `git diff --stat CLAUDE.md` = `749 insertions(+), 749 deletions(-)`
  (전파일 재작성). `git diff --ignore-all-space --stat` → **4 files changed**:
  `.claude/skills/mireuk-daily-check/SKILL.md`, `…/scripts/collect_evidence.py`,
  `config/dailycheck_targets.json`, `scripts/monthly_cleanup.py`.
- **원인**: 코웍의 bash는 리눅스 샌드박스라 `core.autocrlf`가 비어 있다(=false). 윈도우 git은
  정상적으로 clean을 보고할 것이다. **repo 상태 이상이 아니라 실행 환경 아티팩트다.**
- **영향**: §11 자동 적신호 7번 오탐. 진짜 미커밋 4건이 459 속에 묻힌다(계측 4원칙 ③).

#### 2-3. 장전 체크리스트의 `OPT50029` 항목이 브로커 분기를 반영하지 않는다

- **근거**: `grep -rn "OPT50029" --include=*.py .` → 전부 `collection/kiwoom/api_connector.py`.
  오늘 `broker=cybos`. 오늘 로그의 `OPT#####` 토큰 0건은 정상.
- **영향**: 점검 세션이 매번 "TR 확인 불가"로 오판하거나 불필요히 조사할 수 있다.

---

### 확인 필요 (확정 아님)

- **`session_state.json` 4키 소멸의 정확한 주체.** 쓰기 코드 6개소 전부 read-merge-write이고,
  `count` 키를 증가시키는 코드는 repo에 없는데 현재 `count: 1`이다. **장후 F-1 절차로 판정.**
- **`ofi_norm` raw_std 급락의 원인.** 후보 ①틱 수집 경로 변화(08-19 `[FeatureBuilder] tick_size 갱신: 0.0200`),
  ②`features/technical/ofi.py` 산출 로직, ③실제 시장 미시구조(호가 1틱 깊이 축소).
  ⚠ **판정에 라이브 DB 전수 스캔이 필요하므로 장후로 미룬다**(CLAUDE.md 장중 DB 분석 금지).
  최소 3거래일 관측 후 결론(313차).
- **`[ModelLive]` 10m·15m 4일차**(어제 20.8%/21.3%) — 장중·장후 확인 항목.

### 이미 반영된 사안 (신규 아님) — 함정 ① 방지

- **`CB_CONSEC_STOP_LIMIT = 9999`** — 기대값 그대로. 2026-08-01 결산의 4주 연기 결정,
  재검토 기한 **2026-08-29까지 10일**. 08-29 주간회의 상정 항목으로 이미 `NEXT_TODO`에 등록됨.
- **`CB3_P4_GRADE_BLOCK_ENABLED=False` / `FP_CRITICAL_GRADE_BLOCK_ENABLED=False` /
  `TOXICITY_SEVERE_SPREAD_BLOCK_ENABLED=False`** — 전부 CLAUDE.md 한시예외 기대값과 `일치`.
  ⑨(TOX-SEVERE-SPREAD)는 473차 F-8 Phase A에서 `INSUFFICIENT`(ETA 7.1개월) 판정 후
  **주간회의 안건**으로 이관 대기 — 오늘 새로 제기할 사안 아님.
- **`MAX_CONTRACTS = 3`** — 431차 10→3 배포 완료. ⑧의 조건 2는 이미 충족.
- **`HURST_WINDOW_N=90` / `HURST_MAX_LAG=9`** — 317차 재보정 배포분. 26주 WFA 재검증 대상.
- **`RegimeFingerprint` PSI 매분 영속**(477차 후속 F-5) — 오늘 `09:00:00 [RegimeFingerprint] PSI=0.009
  level=0 (heartbeat)` 로 **라이브 확인됨**(G-A/G-B 관측항목 충족 시작).
- **`[Calibration] 축퇴` 125건** — 이상 아님. 일별 추이 08-12 102 / 08-13 97 / 08-14 99 / 08-18 98 /
  08-19 90 으로 **평상 수준**이며, 즉시 `축퇴 해소 → 보정 재적용`이 뒤따른다.
- **`O-H 장전 점검 08:57±5분 복귀`** — 오늘 **08:59:54 수집 완료**로 **복귀 확인**.
  어제 13:44(4h47m 지연) 대비 정상화. G-3 승격 불필요.

### 오탐 정정 — 수집기 §11 자동 적신호 7건

| # | 적신호 | 판정 |
|---|---|---|
| 1~5 | 불변식 5종 `미발견` | **오탐(브랜치 차이)** — 1-3. 코드 무결성 문제 아님 |
| 6 | `LEARNING.log` 축퇴 8건 | **오탐(평상 수준)** — 위 참조 |
| 7 | 미커밋 459건 | **오탐(CRLF)** — 2-2. 실질 4건 |

→ **오늘 §11 적신호 중 진짜는 0건.** 본 리포트의 P1 3건은 전부 §11이 못 잡은 것이다
(1-1 폴백 성공, 1-2 WARNING 대량, 1-3 오탐 속의 실질 격차) — 스킬 §2의
*"§10 자동 적신호는 출발점이지 결론이 아니다"* 가 오늘도 그대로 성립한다.

---

## 2. Fix 작업 구현계획

> **⚠ 본 계획은 수립만 한다. 장전(개장 3분 전) 실행이므로 코드 변경·커밋·재기동을 하지 않는다.**
> 전 항목 **15:10 강제청산 이후(장후) 적용**을 권고한다.

### F-1. `session_state` EOD 인계 키 소멸 — 원인 규명 + 폴백 가시화 — **P1** · 대응 이상점 1-1

- **원인 가설**: (A) 미지의 쓰기 주체가 기본 dict로 덮어씀, (B) 파일이 기동 전 삭제/이동됨,
  (C) `_write_session_state`가 `_state_persist_enabled=False` 경로에서 `pop` 외 부작용.
  현재 근거로는 **어느 것도 확정 불가** — 조사가 먼저다.
- **변경 파일 (조사 단계, 코드 0건)**:
  - 장후 15:40 이후 `data/session_state.json`을 **EOD 직후(15:51)·다음 기동 직전·기동 직후 08:42**
    3시점에 스냅샷 후 diff. 스냅샷 스크립트는 신설하지 않고 `copy`로 충분.
  - `grep -rn "session_state" --include=*.py --include=*.bat .` 를 `.bat`·`scripts/`까지 확장
    (오늘은 `.py`만 확인했다).
- **변경 파일 (조치 단계, 원인 확정 후)**:
  - `main.py` — `_read_session_state()`: 파일 부재/기본 dict 반환 시 **`[SessionState] 기본값 생성
    (파일 부재)` INFO 로그를 반드시 남긴다.** 현재는 조용히 기본값을 준다 — **계측 4원칙 ④ 위반**.
  - `main.py:5057~5072` — 1차 경로가 실패하고 폴백으로 넘어갈 때 `[PreRetrain] 1차 경로 실패
    (session_state 키 부재) → 마커 폴백` 을 **WARNING**으로. 지금은 폴백 쪽 INFO만 있어
    "1차가 죽었다"는 사실이 로그 등급으로 드러나지 않는다.
  - `retrain_eod.py:184` — 쓰기 직후 **read-back 검증 1줄** 추가
    (`json.load` 후 두 키 존재 확인, 없으면 WARNING).
- **회귀 위험**: 낮음(로그 추가 위주). `_read_session_state`의 반환 계약은 바꾸지 않는다 —
  바꾸면 `_restore_*` 4개 함수가 전부 영향받는다.
- **절대원칙 저촉 여부**: 없음. COM 콜백 밖이며 6종 어디에도 해당 없음.
- **런타임 주의**: `main.py`는 **py37_32**, `retrain_eod.py`는 **py310_64**. f-string은 양쪽 다 가능.
- **검증**: 장후 적용 → 다음 기동(08-20 08:41)에 `[SessionState]` 신규 로그가 뜨는지 +
  `data/session_state.json`에 4키가 남는지. **판정 기한: 08-20 장전 점검.**
- **섀도 계측 필요 여부**: 불필요(집행 무변경).
- **적용 시점**: **장후**. 조사(스냅샷)는 오늘 15:51부터.
- **결정 필요 사항**: 없음.

### F-2. 수집기 불변식 감시표를 브랜치 스코프와 분리 — **P1** · 대응 이상점 1-3

- **원인 가설**: 감시표가 브랜치 개념을 갖고 있지 않다. 이미 2회(08-14, 08-17) 등록됐으나 미적용.
- **변경 파일**:
  - `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py` — 불변식 스펙 dict에
    **선택 키 `"branch": ["dev"]`** 도입. 현재 브랜치가 목록 밖이면 판정을 `해당없음(dev 전용)`으로
    렌더링하고 **§11 자동 적신호에서 제외.**
    ⚠ **키 부재 = 전 브랜치 적용**이 기본값이어야 한다(기존 22행이 조용히 빠지면 새 은폐 수단이 된다).
    ⚠ 표 하단에 **`타브랜치 미적용 N종`** 을 반드시 남긴다.
  - `.claude/skills/mireuk-daily-check/references/invariants.md` §2-B — `적용 브랜치` 열 신설.
  - (같은 커밋) `collect_evidence.py` §2 — `git diff --ignore-all-space` 기준 **실질 변경 건수**를
    괄호 병기(`미커밋 459건 (실질 4건)`). **2-2 오탐 제거.**
- **회귀 위험**: 낮음. 수집기는 읽기 전용이며 라이브 경로와 무관.
- **절대원칙 저촉 여부**: 없음.
- **런타임 주의**: 표준 라이브러리만 사용(py37_32/py310_64 무관).
- **검증**: `v9-dev`에서 재실행 시 §11 적신호 **7건 → 0~1건**. `dev` 체크아웃 시 5행이 다시 `일치`.
- **적용 시점**: **장후**(수집기라 장중에도 무해하나, 오늘은 코드 변경 금지 지시를 따른다).
- **결정 필요 사항**: **`v9-dev`가 `dev`의 315커밋을 정렬받을지는 주간회의 안건**(별건).
  ⚠ 특히 `MODEL_LABEL_STATE_UNLOCK_ENABLED`는 실전전환 ⑧의 선행조건이라 못박힌 항목이므로,
  **MW0601 단독 관측으로 ⑧을 판정하지 말 것.**

### F-3. `CybosProbe` 워커 스레드 `CoInitialize` 누락 — **P2** · 대응 이상점 2-1

- **원인 가설**: 프로브가 별도 스레드에서 도는데 `pythoncom.CoInitialize()`를 호출하지 않는다.
- **변경 파일**: `collection/cybos/investor_data.py`(또는 프로브 진입 함수) —
  스레드 시작부 `pythoncom.CoInitialize()` / 종료부 `CoUninitialize()`.
  실패 시 로그를 **`미시험(COM 초기화 실패)`** 과 `미발견(클래스 없음)`으로 **분리 표기**
  (계측 4원칙 ②). `[CybosInvestor] reason=` 문자열도 두 원인을 구분하도록.
- **회귀 위험**: ⚠ **절대원칙 ④ 확인 필수** — 이 코드가 **COM 콜백 내부가 아님**을 반드시
  확인하고 넣을 것. 콜백 내부면 `dynamicCall`·`emit` 금지 규칙과 인접해 위험하다.
  현재 근거로는 프로브 워커(콜백 밖)로 보이나 **적용 전 호출 스택 확인**.
- **런타임 주의**: **py37_32** · Cybos COM.
- **검증**: 다음 기동에서 `CpSvrNew7221`의 오류코드가 `-2147221008` → `-2147221005`(진짜 없음) 또는
  성공으로 바뀌는지. 후자면 **선물 투자자 수급 수집 가능성이 열린다.**
- **섀도 계측 필요 여부**: 수집 성공 시 새 피처는 **학습 투입 전 섀도 관찰 필수**
  (CLAUDE.md 97개 동결 슈퍼셋 문제 — 458차 P2-B 피처 온보딩 절차를 따를 것. 여기서 임의로 넣지 말 것).
- **적용 시점**: **장후**.

### F-4. 장전 체크리스트 브로커 분기 명시 — **P2** · 대응 이상점 2-3

- **변경 파일**: `.claude/skills/mireuk-daily-check/references/phases.md` A절 —
  `선물 분봉 TR` 항목을 `Cybos: 해당없음 / Kiwoom: OPT50029`로 분기.
- **회귀 위험**: 없음(문서).
- **적용 시점**: **장후** (F-2와 같은 커밋).

### 적용 순서와 커밋 계획

1. **커밋 ①** — F-2 + F-4 (점검 도구·문서) —
   `[MW0601] 478차: 불변식 감시표 브랜치 스코프 분리 + 실질 미커밋 건수 + 장전 TR 브로커 분기`
2. **커밋 ②** — F-1 조사 결과 + 로그 보강 —
   `[MW0601] 478차 후속: session_state EOD 인계키 소멸 원인 + 폴백 가시화(4원칙 ④)`
3. **커밋 ③** — F-3 (호출 스택 확인 후에만) —
   `[MW0601] 478차 후속2: CybosProbe CoInitialize 배선 + 미시험/미발견 분리 표기`

> ⚠ F-3은 절대원칙 ④ 인접 영역이다. 확인이 끝나지 않으면 **커밋 ③을 다음 세션으로 넘긴다.**

---

## 3. 고도화 방안

당일 관측에서 출발한 것만 쓴다.

### G-1. CORE 피처 축퇴 상시 감시 채널 `core_scaler_degeneracy_watch` (섀도)

- **관측 근거**: 1-2. `ofi_norm` identity 강제가 08-10 5/41 → 08-18 34/48(71%) → 08-19 장전 7/7로
  **추세적으로 악화**됐는데, 3거래일 연속 어떤 점검에도 걸리지 않았다.
  이 WARNING은 하루 200건씩 나오지만 **어디에도 집계되지 않아** 수집기 §4에서
  `ScalerRefresh ×59` 라는 컴포넌트 카운트로만 스쳐간다.
  FP-CRITICAL이 "PSI=0.0 고정으로 2개월 죽은 코드"였던 것, TOX-SEVERE-SPREAD가 "한 달 넘게 죽은
  섀도"였던 것과 **같은 결함 패턴**(계측은 도는데 소비처가 없다).
- **제안 내용**: `[ScalerRefresh] … raw_std≈0` 을 파싱해 **CORE 피처별 · 호라이즌별 · 일자별
  identity 강제율**을 산출하는 읽기 전용 스크립트
  (`scripts/core_scaler_degeneracy_watch.py`, GR-1과 같은 소급형).
  `VALIDATION_CAMPAIGN`에 채널 등록하고 **사전등록 판정문**을 함께 박는다:
  > *"단기 그룹 CORE 1종의 일별 identity 강제율이 **5거래일 연속 50% 초과**면 해당 피처의
  > CORE 지위를 주간회의 안건으로 올린다. 그 미만이면 현행 유지."*
  ⚠ `min_samples`·임계는 **지금 사전등록하고 이후 움직이지 않는다**(313차 ④, 458차 D6).
- **기대 효과**: 절대원칙 §3이 보호하는 5개 CORE 항목이 **실제로 정보를 싣고 있는지**가
  매일 1행으로 관측된다. 30m 행이 "지정만 존재"였던 것(458차)과 같은 유형의 공동화를 조기 발견.
- **비용·위험**: 로그 파싱 전용, 라이브 경로 무변경, 반나절. **위험 낮음.**
  ⚠ 집행(차단·등급)에는 절대 연결하지 않는다 — 섀도 관찰 후 승격.
- **선행 조건**: 없음. 다만 F-1/F-2보다 뒤.
- **우선순위 제안**: **이번 주.**

### G-2. `ofi_norm` 축퇴 원인 3분기 진단 (1회성 조사, 장후)

- **관측 근거**: 1-2. 08-18 EOD **500봉** 재적합에서도 raw_std=0.0312 — 표본 부족이 아니다.
  같은 날 `[FeatureBuilder] tick_size 갱신: 0.0200 (spread_ticks 계산 기준)`이 있었다.
- **제안 내용**: 장후 `raw_features` 소급으로 (a) `ofi_norm` 원값 분포의 **0 점질량 비율**
  일자별 추이, (b) 상류 `ofi_imbalance`·`mlofi_norm` 동반 축퇴 여부, (c) `tick_size` 변경 시점과의
  정렬을 본다. **1개 스크립트, 판정 아님 — 원인 후보를 3개 중 1개로 좁히는 것이 목표.**
  ⚠ 반드시 `utils/analysis_db.guard_intraday()` + `connect_ro()` 경유(CLAUDE.md 장중 DB 금지).
- **기대 효과**: 코드 결함이면 즉시 고칠 수 있고, 시장 미시구조 변화면 **CORE 재검증(26주 WFA
  피처셋 재검증 항목)의 조기 트리거**가 된다.
- **비용·위험**: 읽기 전용 1회성. 낮음.
- **선행 조건**: 15:10 이후. G-1보다 먼저 해도 무방.
- **우선순위 제안**: **이번 주**(단, G-1이 3일치를 더 모은 뒤가 효율적).

### G-3. `session_state`를 "EOD 인계 계약"으로 명시 — 스키마 + read-back

- **관측 근거**: 1-1. 6개 쓰기 주체가 전부 read-merge-write인데도 키가 사라진다.
  키 목록이 어디에도 정의돼 있지 않아 **무엇이 있어야 정상인지를 코드가 모른다.**
- **제안 내용**: `config/settings.py`에 `SESSION_STATE_REQUIRED_KEYS`(익일 인계 필수 4키) 선언 →
  기동 시 부재 키를 **1줄 WARNING**으로 열거. 수집기 §3에 `session_state 인계키` 행 추가.
- **기대 효과**: 같은 유실이 다시 생기면 **다음날 08:41에 즉시 드러난다**(지금은 폴백이 가려서
  장전 점검이 파일을 직접 열어야 알 수 있다).
- **비용·위험**: 낮음. 집행 무변경.
- **선행 조건**: **F-1의 원인 규명이 먼저** — 원인을 모른 채 스키마만 넣으면 경고만 늘어난다.
- **우선순위 제안**: F-1 완료 직후.

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| CORE 피처 축퇴 감시 | 없음 | **26주 WFA 재검증 항목**에 G-1 채널 편입 | CLAUDE.md "호라이즌 방향예측 피처셋 재검증"(455차)과 같은 성격 — IC 드리프트의 **선행 지표** |
| 실전전환 ⑧ 판정 주체 | CLAUDE.md ⑧ | **"MW0601 단독 관측 불가"를 명기** | `MODEL_LABEL_STATE_UNLOCK_ENABLED`가 `v9-dev`에 부재(1-3) — 지금 상태로 ⑧을 판정하면 틀린다 |
| CB② 복원 | 2026-08-29 재검토 | 변경 제안 없음 | 기한 **10일** 남음. 오늘 `9999` 유지 확인 |

---

## 4. 진입 승패 사후검증

**해당 없음(장후 전용).** 장전 시점 진입·청산·차단·사이저 호출 **전부 0건**(수집기 §5).

## 5. 수익률 향상방안

**해당 없음(장후 전용).**

---

## 6. 다음 국면(오늘 장중·장후) 관측 예정

- [ ] **1-2 후속 — `ofi_norm` identity 강제율 종일 집계.** 08-18의 71%를 넘는가.
      09:02 시점 `raw_std=0.0321`. 15:50 E_EOD 재적합에서도 걸리면 **3거래일 연속**
- [ ] **1-1 후속 — 15:51 `data/session_state.json` 스냅샷** (F-1 조사 1단계)
- [ ] **GR-3V — `[ProfitGuard] 진입 차단` 줄에 `| src=` 토큰이 붙는가.**
      장전 차단 0건이라 **미확인**. L2-Tier/L3-Afternoon 차단 줄에서 먼저 보일 것
- [ ] **G-A/G-B — `[RegimeFingerprint] PSI` 하트비트.** 09:00:00 `PSI=0.009 level=0` **1건 확인됨**.
      장후에 `ensemble_decisions.fp_psi` 비-NULL 행수가 당일 행수와 같은지
- [ ] **G-C — 15:40 `[DriftAdjuster]`.** 오늘 기동 `alpha=0.01000, 이력 10일, 마지막 액션=DRIFT_UP`.
      F-1(477차 후속) 적용분이 포화를 가시화하는지 — 미적용이면 `alpha 0.01000→0.01000` **7번째**
- [ ] **G-D — 15:40 `[BrokerPnl] EOD 확정 — gross … − 수수료 … = net …`**
- [ ] **O-G IntradayRegime 전이 4일차.** 09:02:00 이미 `NORMAL → CRASH (day=-1.11% atr=1.00 z=3)`
      — 08-14 19회 / 08-18 18회 대비
- [ ] **`[ModelLive]` 10m·15m 5일차** (어제 20.8%/21.3%)
- [ ] **F-1R 15:10 강제청산 리허설 — ⚠ 사용자 실행 필요.** 전환기준 ②의 유일한 해소 경로
- [ ] **CB② 복원 안건** — 2026-08-29까지 **10일**

---

## 부록. 본 세션 자체 검증

- [x] **브랜치를 가장 먼저 확인**했다 — `v9-dev`(MW0601 정상). 2026-08-12 오전환 사고 재발 없음
- [x] 코드 변경 **0건** · 커밋 **0건** · 재기동 **0건**
- [x] **라이브 DB 미접근** — `raw_data.db`·`predictions.db`·`trades.db` 쿼리 0건.
      로그 파일·`config/settings.py`·`data/session_state.json`·git 메타만 읽었다
      (CLAUDE.md 장중 DB 분석 금지 구간 준수. 2026-08-10 CB⑤ 자가유발 전례)
- [x] 모든 이상점에 **로그 시각과 원문 인용**을 붙였다
- [x] 각 이상점을 **CLAUDE.md 조항에 대응**시켰다 — 1-1→계측 4원칙 ④, 1-2→절대원칙 §3,
      1-3→계측 4원칙 ③, 2-1→계측 4원칙 ②, 2-2→계측 4원칙 ③
- [x] **재인용 금지 수치 미사용** — 2026-06-25 SHAP=0 / 0801 §9-3 이벤트 단위 사이징 통계 4종 /
      CB③ "35%" / 417차 "379건 중 86건" / 476차 §3 "133건·29.6%" 및 "binding 20분·≈+0.00pt"
      **전부 인용하지 않았다**
- [x] **함정 ① 확인** — 한시예외 4종·`MAX_CONTRACTS`·Hurst·PSI 영속을 `config/settings.py`와
      커밋 이력 실물로 대조한 뒤 "이미 반영된 사안"으로 분류했다. 신규 안건으로 올리지 않았다
- [x] **미측정 ≠ 0 구분** — `ofi_norm` 08-12~08-14의 `0건`은 로그줄이 139차부터 존재함을
      `git log -S`로 확인한 뒤 **진짜 0**으로, 08-17은 **미측정**으로 표기했다
- [x] **표본 부족 항목에 확정 결론 없음**(313차) — 1-2의 손익 영향, `ofi_norm` 원인 3후보
- [x] 수집기 §11 자동 적신호 7건을 **전부 해석**했다(진짜 0건 · 오탐 7건)
- [x] dev_memory 갱신 완료 · **`git commit`·`git push` 미실행**
