# 미륵이 일일 점검 — MW0602 / 2026-08-27

> 하루에 이 파일 하나다. 장전이 만들고 장중·장후가 **뒤에 append** 한다.
> 앞 국면 절의 본문은 고치지 않는다(정정은 뒤 국면 절에서 하고 앞 절에는 한 줄 포인터만 단다).

---

# 장전(pre)

- 작성 2026-08-27 09:0x KST · PC **MW0602** · 브랜치 **`dev`** (HEAD `53c665f`)
- 증거 다이제스트: `docs/정기점검/매일점검/evidence_MW0602-20260827_pre.md` (67.7KB, 생성 09:00:30)
- 국면은 **인자로 지정된 `pre`** 다(추론 아님).

## 0. 오늘 한눈에

**판정: 조건부 정상 — 오늘 거래할 자격은 됩니다. 개장 전에 반드시 해야 할 일은 없습니다.**

프로그램은 아침 8시 40분에 정상으로 켜졌고, 어제 저녁의 학습과 마무리 작업이 모두
성공한 상태로 이어받았습니다. 시장 분위기 판정(보통)도 제때 끝났고, 예측에 쓰는
숫자 보정 작업도 개장 전에 마쳤습니다. **지금 당장 위험한 것은 없습니다.**

다만 오늘 새로 확인된 것이 하나 있습니다. **프로그램을 켤 때 "이미 켜져 있는 옛
프로그램이 있으면 꺼라"라고 시키는 장치가, 실제로는 아무것도 찾지 못한 채 "찾았다"고
말하고 있었습니다.** 어제 만들어 둔 계측 장치가 오늘 아침 처음으로 그 사실을 실측으로
잡아냈습니다. 오늘은 실제로 옛 프로그램이 없었으므로 **오늘 거래에는 영향이 없습니다.**
문제는 앞으로입니다 — 진짜로 프로그램이 두 개 뜨는 날이 오면, 그 장치는 그것도 못 잡고
못 끕니다. 고치는 것은 장이 끝난 뒤에 합니다.

그 밖에 세 가지가 "숫자는 맞는데 기록이 남지 않아 나중에 확인할 수 없는" 상태입니다.
① 오늘 어느 로그인 경로로 접속했는지(= 수수료가 얼마로 계산되는지)가 기록에 남지 않습니다.
② 관측 항목에 붙이는 번호가 세 개나 겹쳐 있어, 나중에 "그 항목 끝났다"고 적으면 엉뚱한
항목이 닫힙니다. ③ 이미 두 번이나 확인이 끝난 사항이 참고 문서에는 아직 "확인 안 됨"으로
남아 있습니다. 셋 다 오늘 돈에는 영향이 없고, 나중에 판단을 틀리게 만드는 종류입니다.

| 구분 | 건수 |
|---|---|
| **P0**(오늘 거래에 직접 위험) | **0건** |
| P1(정확성·관측 훼손) | 3건 (`1-1` `1-2` `1-3`) |
| P2(운영 부담·기술부채) | 3건 (`1-4` `1-5` `1-6`) |
| 확인 필요(확정 아님) | 2건 (`1-7` `1-8`) |

---

## 0-A. 시작 전 3종 확인 (SKILL.md §0)

| 항목 | 결과 |
|---|---|
| ① 정본 SKILL.md 선독 | ✅ `.claude/skills/mireuk-daily-check/SKILL.md` (`rev: 2026-08-26e`, 664줄) 를 먼저 `Read` 했고 **정본을 따랐다**. 이 세션에 로드된 앱 사본은 `rev: 2026-08-26 (494차)` 로 **한 세대 낡았다** → 이상점 `1-3` |
| ② 모든 git 호출에 `--no-optional-locks` | ✅ 이 세션이 친 git 명령은 `branch --show-current` · `log --format --since` 두 개이며 **둘 다 옵션을 붙였다.** 세션 시작·종료 시점 `.git/index.lock` **없음**(수집기 §2 2점 샘플도 「없음」) |
| ③ 병행 세션 확인 | ✅ **당일 커밋 0건.** 당일 산출물은 이 세션이 만든 `evidence_MW0602-20260827_pre.md` 하나뿐. 딥다이브·특별조사 파일 없음. 직전 산출물은 `MW0602-20260826-점검리포트.md`(08-26 18:46) |
| ④ 수집기 `--pc MW0602` | ✅ 다이제스트 첫 줄 `PC **MW0602** (MW0602 (override · host=claude))` — 정상 |

**수집기 실행 결과**: 정상 종료(67.7KB 저장). 단 stderr 에 한 줄 —
`[collect_evidence] DB 스냅샷 생략(디스크 여유 부족): data/db/predictions.db` → 이상점 `1-8`.

---

## 0-B. 장전 체크리스트 A 통과표 (`references/phases.md` §A)

| 항목 | 판정 | 근거(로그 시각) |
|---|---|---|
| **A-0** 브랜치 = `dev` | ✅ 확인됨 | `git --no-optional-locks branch --show-current` → `dev`. HEAD `53c665f`(500차) |
| **A-0** origin 대비 behind | ⚠ 미확인 | 샌드박스에서 `fetch` 를 하지 않았다(장전에 네트워크 git 조작을 하지 않는다는 규약 준수). 당일 커밋 0건이므로 **오늘 새로 벌어진 격차는 없다** |
| **A-1** 기동 시각·PID·중복 | ✅ 확인됨 | 런처 `08:40:01` PID `18596` → main.py PID `7336`(`08:40:43 [FaultHandler] … PID=7336`). 중복 기동 **없음**(`[GUARD] kill-target count=0 probe=ok`) |
| **A-1** 런타임 py37_32 | ✅ 확인됨 | 런처 `[INFO] 32-bit Python 확인: …\envs\py37_32\python.exe` |
| **A-1** Cybos/CREON 접속 | ✅ 확인됨 | 런처 `[OK] creon API 세션 준비 확인` → `[OK] creon preflight 통과` → `[OK] creon 준비 완료`. COM 구독 `08:41:16 [CybosSub] dispatch ok progid=Dscbo1.CpFConclusion` |
| **A-1** scipy 1.5.4 / sklearn 1.0.2 / joblib 1.1.1 | ⚠ 미기록 | **오늘 로그에 버전 문자열이 한 줄도 없다.** 32-bit 여부만 찍힌다 → 이상점 `1-6` |
| **A-1** 모델 번들 로드 | ✅ 확인됨 | `08:40:58 [RF] 로드 완료: 6호라이즌 ready=True` |
| **A-1** 전일 EOD 재학습 성공 | ✅ 확인됨 | `data/eod_retrain_done_20260826.txt` 존재(08-26 15:48) · `session_state.json` `eod_retrain_ok_date=2026-08-26` · `gbm_last_retrain=2026-08-26 15:48` |
| **A-1** 전일 P8(스케일러 재적합) | ✅ 확인됨 | `session_state.json` `p8_last_success_date=2026-08-26` |
| **A-1** `[PreRetrain]` 08:55 | ✅ **스킵이 정상** | `08:55:19 [PreRetrain] 08:55 사전 재학습 스킵 — 1영업일 전(2026-08-26) EOD 재학습 성공 (동일 데이터 중복 불필요)`. **"재학습 0회"를 이상으로 읽지 않는다** |
| **A-2** 매크로 수집 → 레짐 확정 | ✅ 확인됨 | `08:58:23 [Regime] NEUTRAL (점수=1) \| VIX=15.4 (저공포) \| SP500=-0.02% (보합) \| USD/KRW +0.10% (중립)`. 개장 **97초 전** 확정 |
| **A-2** 실시간 구독 사전 시작 | ✅ 확인됨 | `08:45:18 [EarlyWarmup] Cybos RT 08:45 선행 구독 시작 (프리장 봉 15봉 확보)`. 09:00 이후 시작이 아니다 |
| **A-2** 옵션 체인 로드 | ✅ 확인됨 | `08:41:18 [OptionChain] 초기화 완료: 체인 캐시 5242 종목` |
| **A-2** 선물 분봉 TR = `OPT50029` | ⚪ 해당 없음 | `OPT50029`/`OPT10080` 은 **키움 TR 코드**다. 이 PC는 Cybos/CREON 경로(`Dscbo1.*` COM)로 돌고 있어 두 문자열 모두 로그에 없다. **금지된 `OPT10080` 사용 0건** 확인으로 갈음한다 |
| **A-3** 설정 불변식 26행 | ✅ 전행 `일치`/`값 확인` | 수집기 §3 — `불일치` **0건**. `CB_CONSEC_STOP_LIMIT=9999` · `CB3_P4_…=False` · `FP_CRITICAL_…=False` · `TOXICITY_SEVERE_SPREAD_…=False` · `MAX_CONTRACTS=3` · `SIZING_TARGET_CAPITAL_KRW=50,000,000` 전부 의도대로 |
| **A-3** 한시예외 재검토 기한 | ✅ 도래 없음 | **CB② 는 날짜 기한이 아니다**(489차 D2 — 「⑧ 해제와 동일 커밋」이라는 **사건 기한**). 종전 `2026-08-29` 를 기한으로 인용하지 않았다. CB③-P4·FP-CRITICAL·TOX-SEVERE-SPREAD 도 사건/26주 WFA 기한이며 오늘 도래분 없음 |
| **A-4** 미커밋 변경 | ⚠ 1건 | `?? docs/프롬프트/MW0602_장후자동조치_설치지침_20260826.md`(신규 문서, untracked). 코드 아님 → P2 미만, 사용자 조치 2번 |
| **A-4** PC명 태그 | ✅ 확인됨 | 최근 12커밋 전부 `[MW0601]`/`[MW0602]` 접두 |
| **A-5** 리포트 파일 개시 | ✅ | 이 파일. `-pre` 접미사 만들지 않음. 이상점 번호 `1-1` 부터 |

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**해당 없음 (0건).**

---

### P1 — 정확성·관측 훼손

#### 1-1. 런처 중복실행 방지장치(GUARD)의 탐지·종료 두 갈래가 죽어 있다 — 500차 계측이 첫날에 확정했다

- **증상**: 오늘 아침 런처가 *"이미 실행 중인 main.py 프로세스가 감지됐습니다"* 라고 경고하고
  종료 절차까지 밟았는데, **같은 시점에 돌린 프로브는 대상 0개를 보고했다.**

  ```
  08:40:0x [GUARD] 기존 main.py 프로세스 체크...
  08:40:0x [WARN] 이미 실행 중인 main.py 프로세스가 감지됐습니다.
  08:40:0x [WARN] 이중 실행 시 GBM pkl 파일 경합 및 중복 주문이 발생할 수 있습니다.
  08:40:0x [GUARD] 장전 [0840] -- 10초 후 자동 Y (스케줄러 자동 기동)
  08:40:0x [GUARD] kill-target count=0 probe=ok        ← 500차 F-3 이 어제 심은 계측
  08:40:0x [GUARD] 기존 main.py 프로세스 종료 중...
  08:40:0x [GUARD] 기존 프로세스 종료 완료 -- 새 인스턴스 시작.
  ```
  (`logs/Mireuk_batch/launcher_20260827_084001_18596.log:24-30`)

- **근거 — 원인은 배치파일의 `!` 소실이다.** `start_mireuk_CREON.bat` 은 지연확장
  (`!PY32!` 사용)을 켠 상태에서 파이썬 한 줄 코드를 `"…"` 로 넘긴다. 그 안의
  `p.pid != os.getpid()` 에서 `!` 가 변수 참조 시작으로 먹혀
  **`p.pid = os.getpid()`** 가 되고, 리스트 내포 조건절의 대입은 `SyntaxError` 다.
  - `:377` **탐지** 호출 — `sys.exit(1 if procs else 0)` 이 실행되기 전에 죽는다.
    `2>NUL` 이 오류를 삼키고 종료코드 1 만 남아 배치는 *"프로세스 있음"* 으로 읽는다.
    **오늘 로그에 `[GUARD] Running main.py processes: N` 이 한 줄도 없는 것**이 그 방증이다
    (이 줄은 같은 파이썬이 정상 실행됐을 때만 나온다).
  - `:409` **종료** 호출 — 같은 `!=` 를 쓴다. 즉 **아무것도 terminate 하지 않는다.**
  - `:407` **프로브**(500차 F-3 신설) — `p.pid not in (me,)` 로 `!` 를 한 글자도 쓰지 않아
    **혼자 정상 동작한다.** 배치 자신의 주석 `:405` 가 이 결함을 이미 명문화하고 있다.
- **기준**: 계측 4원칙 ④(폴백 가시화 — 기본값이 쓰였으면 그 사실을 남긴다) ·
  절대원칙과 직접 저촉은 아니나 **이중 주문 방지장치**라는 성격상 집행 안전 계열.
- **판정 — `O-45`(500차, F-3) 를 오늘 닫는다: `count=0` → `!=` 결함 확정.**
  `dev_memory/NEXT_TODO.md` 가 사전등록한 판정문 그대로다 —
  *"`count=0` 이면 위 `!=` 결함이 확정(탐지가 아무것도 못 찾았는데 발화했다는 뜻)"*.
- **동반 정정 — `O-17(493차)` 「런처 GUARD 10거래일 연속이면 P1 승격」은 이 사실 위에서 다시 읽어야 한다.**
  수집기 §12 `launcher_guard` 는 오늘 포함 **9거래일 100% `발화`** 다(0817·0818·0819·0820·
  0821·0824·0825·0826·0827). 그러나 그 100% 는 *"전일 세션이 안 죽었다"* 가 아니라
  **탐지 코드가 매일 SyntaxError 로 죽어서 만들어진 상수**다.
  🔴 **분자가 오염돼 있었다** — `NEXT_TODO.md` 500차 절이 예고한 그대로다.
  → **10거래일 P1 승격 판정을 그대로 집행하지 않는다.** 지표의 의미가 바뀌었으므로
  `!=` 수정 후 다시 세는 것이 맞다(Fix `F-1`, 관측 `O-47`).
- **영향**: 프로그램이 실수로 두 개 떠 있는 날이 오면, 지금 장치는 그것을 찾지도 못하고
  끄지도 못한 채 *"껐습니다"* 라고 말합니다. 그러면 주문이 두 번 나가거나 학습 파일을
  두 프로그램이 동시에 건드릴 수 있습니다. **오늘은 실제로 옛 프로그램이 없었으므로
  오늘 거래에는 아무 영향이 없습니다.**
- **영향(기술)**: 단일 인스턴스 보장이 6개월+ 무효였다. 동시 실행 시 GBM pkl write 경합
  (191차 OOM 계열과 다른 손상 경로) + 중복 주문 노출. 다만 **오늘 실측 `count=0`**
  이므로 당일 노출은 0. `probe=ok` 축은 신뢰 가능(별도 파이썬, `!` 미사용).
- **심각도 P1** (P0 아님 — 오늘의 실제 노출이 0으로 실측됐다).

#### 1-2. 어떤 로그인 채널로 접속했는지가 로그에 남지 않는다 — 수수료율이 조용히 바뀌어도 알 수 없다

- **증상**: 495차 후속이 `FUTURES_COMMISSION_RATE` 를 **로그인 채널 감지 파생**으로 바꿨는데
  (`config/constants.py:detect_broker_channel()` → `config/settings.py:5532`),
  **감지 성공 시에는 아무 로그도 남지 않는다.**
  ```
  $ grep -rn "BrokerChannel" --include=*.py .
  ./config/settings.py:5537:    _sys.stderr.write("[BrokerChannel] 감지 실패 — CREON 폴백 적용 …")
  ```
  즉 코드 전체에서 이 축을 기록하는 곳은 **실패 경로 한 줄뿐**이다.
  오늘 로그(11개 파일)에 `[BrokerChannel]` **0건** — 감지가 성공했다는 뜻이지만,
  **CREON 이었는지 CYBOS 였는지는 로그로 확인할 수 없다.**
- **근거**: `constants.py:_STARTER_LOG_CANDIDATES` 4경로의 **mtime 최신**이 이긴다.
  두 채널의 요율은 `CREON 0.000019` vs `CYBOS 0.000098104` — **5.16배 차이**다.
  간접 방증은 있다: 런처가 `Broker : creon` / `BROKER=creon` 을 찍는다.
  ⚠ 그러나 **그 값과 `detect_broker_channel()` 의 입력은 다른 원천**이다
  (런처 인자 vs 스타터 로그 mtime). 둘이 어긋나도 아무 데서도 대조되지 않는다.
- **기준**: 계측 4원칙 ②(미측정 ≠ 0) · ④(폴백 가시화) · ⑤(대사는 **모든 축**을 걸어라).
  CLAUDE.md 26주 재검증 「선물 수수료율」 ⓓ 가 *"`BROKER_CHANNEL_SOURCE` 가 `fallback` 이면…"*
  이라고 쓰지만 **그 값을 볼 수 있는 로그가 없다.**
- **왜 이것이 P1 인가 — 이미 한 번 6개월을 잃었다.** 493차가 규명한 사고
  (키움 요율이 브로커 전환 후 6개월간 남아 실제의 1/6.54만 차감)의 **재발 형태**가
  정확히 이것이다: 값이 틀려도 크래시가 안 나고, 틀린 방향이 항상 낙관이며,
  `[NetRecon]`(F-2, 매 EOD) 이 잡기 전까지 아무도 모른다.
  **실전 전환 기준 ①(브로커 실측 net 판정)의 상류에 있다.**
- **영향**: 오늘 수수료가 얼마로 계산되고 있는지를 기록만 봐서는 확인할 수 없습니다.
  어느 날 다른 방식으로 로그인하면 수수료가 5배로 바뀌는데, 그 사실이 어디에도 안 남습니다.
- **영향(기술)**: `BROKER_CHANNEL` / `BROKER_CHANNEL_SOURCE` 가 import 시점 파생 후 휘발.
  소급 감사 불가 · 일자별 요율 세대 대조 불가(`trades.commission_rate_used` 는 행별 요율만
  담고 **채널명은 없다**).
- **심각도 P1.** Fix `F-2`.

#### 1-3. 관측 ID 가 세 개 겹쳤다 — `O-42` `O-45` `O-46` 이 각각 두 가지 뜻이다

- **증상**: `dev_memory/NEXT_TODO.md` 실측(2026-08-27):

  | ID | 뜻 ① | 뜻 ② |
  |---|---|---|
  | `O-42` | (498차 후속2, `:17704`) 다이제스트 §2 **세 줄** 라이브 검증 | (499차, `:17821`) `SGD표본게이트` 5m conf vs 0.52 **10거래일** |
  | `O-45` | (499차, `:17829`) `[2-A]`·`[2-B]`·사이드카 **코드 실재 확인** | (500차, `:18002`) 런처 GUARD `kill-target count=N` |
  | `O-46` | (499차, `:17832`) `GuardFair gap_bars` **변동성** 10거래일 | (500차, `:18006`) §12b **5행** 출현 |

- **근거**: 수집기 §11 채번 안내는 *"다음 관측 ID = **O-47** … 기지 충돌 **3건**(`O-16`,
  `O-17`, `O-18`)"* 이라고만 말한다. **위 3건은 그 목록에 없다.** 즉 충돌 검출기가
  이 세 건을 **놓쳤다**(정의줄 표기가 `**O-45**` 와 `` **`O-45`** `` 두 형태로 섞여 있는 것이
  유력한 원인 — `O-16/17/18` 은 `(NNN차)` 접미가 붙어 있어 잡혔다).
- **기준**: 계측 4원칙 ③(탈락 가시화) — 검출기가 놓친 항목의 존재를 알리지 않는다.
- **오늘 당장의 실해**: 이 리포트가 `O-45`(GUARD) 를 닫는다. 같은 이름의 **다른 `O-45`**
  (`[2-A]`·`[2-B]` 코드 실재 확인)는 **여전히 미판정**이다. 표기를 구분하지 않으면
  다음 세션이 "O-45 닫힘"만 보고 후자를 함께 닫는다.
- **영향**: 확인할 항목에 붙인 번호가 세 개 겹쳐 있어서, "그 번호 끝났다"고 적으면
  엉뚱한 항목이 함께 끝난 것으로 처리됩니다.
- **영향(기술)**: 관측 레지스트리의 키 유일성이 깨져 판정 이력이 오귀속된다.
  소급 재라벨은 미측정 구간을 만들므로(계측 4원칙 ②) **번호를 다시 매기지 않고
  `(NNN차)` 접미로 구분**하는 것이 기존 관례다.
- **심각도 P1.** Fix `F-3`.

---

### P2 — 운영 부담·기술부채

#### 1-4. 이미 두 번 검증이 끝난 `G-1` 이 참조 문서에는 아직 「라이브 미검증」이다

- **증상**: `MODEL_LABEL_STATE_UNLOCK_ENABLED`(468차 G-1, 실전 전환 기준 ⑧의 선행조건)의
  라이브 검증은 **끝났다.**
  - 2026-08-21 장전 `P-2` **통과** (`MW0602-20260821-점검리포트.md:312`)
  - 2026-08-26 장후 `C-2` **해소 — 긍정 증거로 성립** (`MW0602-20260826-점검리포트.md:730`,
    `sizing_trace.quality_mults` 에 `pre_retrain` 키 부재까지 확인)
  - 오늘까지 **5거래일 연속** 해제 로그 출현:
    `08:55:19 [EntryGate] 모델 레이블 체계 확인(장전) — 사이즈 제한 해제 (×0.6 → ×1.0) | 6/6 호라이즌 현행 레이블(fixed) 학습본`
    (0821·0824·0825·0826·0827. 0819·0820 은 `불일치 6/6` 으로 미해제였다 — 전환점이 명확하다)
- **그런데 두 문서가 아직 옛 상태다**:
  - `.claude/skills/mireuk-daily-check/references/invariants.md` §2-B — `⏳ 라이브 미검증`
  - `CLAUDE.md` 실전 전환 기준 ⑧ 내 468차 각주 — `⚠ **라이브 검증은 아직이다**(2026-08-14 O-1 …)`
- **기준**: CLAUDE.md 함정 ①(판정 ≠ 결정) 의 거울상. 그리고 `invariants.md` §2-B 자신이
  못박은 규약 — *"검증 기록과 계획 기록을 같은 칸에 섞지 마라."*
- **영향**: 이미 확인이 끝난 일이 문서에는 "아직 확인 안 됨"으로 남아 있어,
  다음 사람이 같은 확인을 또 하거나 반대로 "아직 막혀 있다"고 잘못 읽습니다.
- **영향(기술)**: ⑧ 해제 선행조건의 상태가 문서와 실측에서 반대로 읽힌다. 다만 방향이
  **보수적**(실제 통과 → 문서 미통과)이라 노출을 늘리는 쪽 오독은 아니다 → P2.
- **심각도 P2.** Fix `F-4`.

#### 1-5. `[PreMarket] … 잔존=` 이 개수와 이름 수를 다르게 말한다 — 오늘 5개라 적고 4개만 나열했다

- **증상**:
  ```
  08:47:57 [PreMarket] Phase1 refit 완료 n=30봉 z경고 22→7개 | 잔존=atr,avg_volume,cancel_add_ratio,cvd_slope,institution_futures_net,spread_ticks   ← 7개인데 6개
  08:58:59 [PreMarket] Phase4 refit 완료 n=30봉 z경고  8→5개 | 잔존=atr,avg_volume,cancel_add_ratio,ofi_reversal_speed                              ← 5개인데 4개
  ```
- **근거 — 코드상 버그는 아니다.** `main.py:4292`
  `_persist = sorted(set(_zf) & set(_z_feats_after))` — **refit 전·후 교집합**이다.
  따라서 `잔존` 은 언제나 `_z_after` 의 **부분집합**이고, refit 후에 **새로** z경고가 된
  피처는 개수에만 들어가고 이름에는 안 나온다. 오늘 그 차가 Phase1 1개 · Phase4 1개다.
- **기준**: 계측 4원칙 ③(**탈락 가시화** — 절단할 거면 잔여 개수를 명시하라).
  457차 C6(*"7개 미가용이라 적고 5개만 출력"*)과 **같은 형태**다.
- **영향**: 개장 전 마지막 점검 로그가 "문제 있는 항목 5개"라고 적으면서 4개만
  이름을 보여줍니다. 나머지 1개가 무엇인지 로그만 봐서는 알 수 없습니다.
- **영향(기술)**: 숨는 쪽이 **신규 발생분**이라 진단 가치가 오히려 높다. `institution_futures_net`
  처럼 원천 결손과 연결된 피처가 은닉될 수 있다(0824 리포트 §298 이 정확히 그 추적이었다).
- **심각도 P2.** Fix `F-5`.

#### 1-6. 32-bit DLL 충돌 회피 조합(scipy 1.5.4 / sklearn 1.0.2 / joblib 1.1.1)이 로그에 한 줄도 안 남는다

- **증상**: 오늘 로그 11개 파일 전체에서 `scipy` · `sklearn` · `joblib` 버전 문자열 **0건**.
  런처가 남기는 것은 `[INFO] 32-bit Python 확인: …\envs\py37_32\python.exe` 하나뿐이다.
- **기준**: `phases.md` A-1 이 이 조합을 **장전 확인 항목으로 명시**하는데, 확인할 원천이 없다.
  계측 4원칙 ②(미측정 ≠ 0) — 「확인함」과 「확인할 수 없었음」을 구분해야 한다.
- **영향**: 라이브러리 버전이 바뀌어 프로그램이 이상해져도, 기록만으로는 그 사실을
  알 수 없습니다. 매일 점검표에 있는 항목인데 매일 "확인 불가"로 지나갑니다.
- **영향(기술)**: `py37_32` 환경에서 scipy 1.5.4 이탈은 DLL 충돌로 이어진 이력이 있다
  (CLAUDE.md 운영 환경 표). conda 환경이 조용히 갱신되면 감지 경로가 없다.
- **심각도 P2.** Fix `F-6`.

---

### 확인 필요 (확정 아님)

> 아래 둘은 **확정된 결함이 아니다.** 오늘 관측만으로는 갈리지 않는다.

#### 1-7. 개장 전 마지막 상태가 「이상 있음」인데 두 계측이 서로 다른 숫자를 말한다

- 08:55:19 `[Canary] scaler 노후=0h  z경고피처=15개 (EarlyWarmup 완료 — 임계 12개) ⚠ z경고 폭증`
  → 즉시 재적합 → `08:55:19 [Canary] 장전 재적합 완료 n=30봉 z경고 →3개 ✓ 임계 이하` (자가 해소 ✅)
- 그런데 **그 뒤** `08:58:59 [PreMarket] Phase4 refit 완료 … z경고 8→5개` — Canary 가 3개라고
  한 축과 Phase4 가 8→5 라고 한 축이 **다른 숫자**다(측정 시점·창이 다를 가능성).
- **z경고 15개는 최근 7거래일 최댓값**이다: 0819 7 · 0820 8 · 0821 8 · 0824 **13⚠** ·
  0825 **13⚠** · 0826 8 · 0827 **15⚠**. 7일 중 3일이 임계(12) 초과다.
- **왜 확정하지 않는가**: 표본 7일 · 두 계측의 정의 차이 미확인 · 상승 추세인지 진동인지
  불명. **313차 원칙 — 확정 결론 금지 구간.**
- **무엇을 더 보면 닫히는가**: ① 두 계측의 피처 집합 정의(`_canary_load_z_warn_features`
  vs Phase refit 후 재계산)가 같은지 **코드 확인**(장중 열람 가능, 변경은 장후)
  ② 개장 후 `CORE축퇴_개장해소` 축이 오늘 `해소`인지 `미해소`인지 → **관측 `O-48`**
- **언제 닫는가**: 오늘 장중 절.

#### 1-8. 수집기가 `predictions.db` 스냅샷을 「디스크 여유 부족」으로 건너뛰었다

- **증상**: `[collect_evidence] DB 스냅샷 생략(디스크 여유 부족): data/db/predictions.db`
- **귀결**: §12 표에 `⚠ data/db/predictions.db — immutable=1 폴백으로 읽었다. WAL 미반영이라
  최신 수 분이 누락될 수 있다` 가 붙었다. 즉 **§12 · §12b 의 DB 기반 행은 수 분 뒤처진 값**이다.
- **왜 확정하지 않는가**: ① 이것이 **샌드박스 마운트의 여유 부족**인지 **실제 PC 디스크**인지
  구분되지 않는다 ② 폴백 자체는 정상 동작(계측 4원칙 ④ 준수 — 폴백 사실을 남겼다).
- **오늘 판정에 미치는 영향**: **없다.** 장전 판정은 로그 축으로 했고, DB 축(§12b 5행)은
  10거래일 누적이라 수 분 누락에 둔감하다.
- **무엇을 더 보면 닫히는가**: 장후 수집 때 같은 줄이 또 나오는지 + 실제 디스크 여유량.
  → **관측 `O-49`**.

---

### 이미 반영된 사안 (신규 아님 — 함정 ① 확인 완료)

`grep` 으로 실물을 확인하고 **신규 안건으로 올리지 않은** 것들:

| 항목 | 왜 신규가 아닌가 |
|---|---|
| 수집기 `--no-optional-locks` 부착 | `scripts/collect_evidence.py` `run_git()` 에 **이미 있다**(483차 P1-1). 오늘 §2 가 `git -c core.autocrlf=true status --porcelain` 로 정상 측정 |
| 인덱스락 2점 샘플 | 498차 후속2 `74bc775` **배포됨**. 오늘 §2 「없음(실행 시작·렌더 시점 **2점** 확인)」 출력 — **`O-42`(498차) 판정: ① 락 2점 샘플 ✅ ② 정본 rev 정합 ✅ ③ 사본 대조 ✅ 출력됨** → 세 줄 다 살아 있다 |
| `[GUARD] kill-target` 계측 | 500차 F-3 `53c665f` **배포됨**. 오늘이 **첫 라이브 발화** — 위 `1-1` 이 그 결과다 |
| §12b 다섯 번째 임계쌍 `SGD표본게이트` | 500차 G-5 **배포됨**. 오늘 §12b 에 **5행** 정상 출현(`관측일 9 · max 0.5966 · 임계 0.52 · ✅ 최근 1일 미도달`) → **`O-46`(500차) 판정 ✅ 통과** |
| `[Calibration] 축퇴 감지` 132건(장전) | 494차 G-2 `Calib_폴백사유` 채널이 **이미 계측 중**. 5거래일 실측 118~132건으로 **정상 범위**(0821 128 · 0824 118 · 0825 120 · 0826 120 · 0827 132) — 오늘 이상 아님 |
| 매크로 수집 2회 · 레짐 확정 2회 | **7거래일 전부 2회**다(0819~0827 전수 확인). 구조적 정상이며 오늘의 이상이 아니다 |
| `[CybosProbe]` 경고 10건 | 0824 리포트가 이미 다룬 만성 항목. 오늘 10건은 **로그가 09:00 까지뿐**이라서이지 감소가 아니다(계측 4원칙 ②) |
| `CORE준비도 축퇴 1/6 — ofi_norm×6hz` | §12 `CORE축퇴_피처` 채널이 계측 중이며 **6거래일 중 5일 동일**(0821만 0/6). 만성 상태 · 원인축 채널(`CORE축퇴_원인축`) 이 `<0.50(진짜 저변동)` 로 이미 가르고 있다 |
| `sgd_alpha` · `CORE안전` · `GuardFair_유효` · `전략판정` 고착 | 전부 §12 에 **기지 등록** — 0824 `1-13` · 468차 G-2 계열. 신규 아님 |

---

## 2. Fix 작업 구현계획

> 🔴 **장전이므로 오늘은 아무것도 실행하지 않는다.** 코드 변경·커밋·배포·재기동 없음.
> 아래는 **장후 적용 권고**다. 각 항목은 `grep` 으로 「이미 있는가」를 확인한 뒤 썼다(함정 ①).

### F-1. 런처 GUARD `!=` 소실 수정 — P1 · 대응 `1-1`

| 칸 | 내용 |
|---|---|
| **변경 대상** | `start_mireuk_CREON.bat:377`(탐지) · `:409`(종료). 자매 파일 `start_mireuk.bat` 동일 위치 |
| **변경 내용** | `p.pid != os.getpid()` → **`p.pid not in (me,)`** (`me=os.getpid()` 선언 추가). `:407` 프로브가 이미 쓰는 **검증된 형태를 그대로 복사**한다. `!` 를 한 글자도 쓰지 않는다 |
| **회귀 위험** | 🔴 **탐지가 되살아나면 GUARD 가 실제로 프로세스를 죽이기 시작한다.** 지금까지는 무연산이었다. 스케줄러 자동 기동(장전 0840)에서 `10초 후 자동 Y` 경로가 살아 있으므로, **잘못 탐지하면 방금 띄운 자기 자신을 죽일 수 있다** → `me` 제외가 반드시 함께 가야 한다(프로브와 동일 조건) |
| **검증** | ⓐ 새 테스트 `tests/test_501_guard_bang_removed.py` — 두 `.bat` 에 `!=` 부재 + `not in (me,)` 존재 + ASCII·CRLF 보존. ⓑ 기존 `tests/test_500_guard_kill_target_log.py::test_legacy_guard_lines_still_carry_the_bang_defect` 는 **결함 존재를 고정**하는 테스트이므로 **이 수정과 함께 삭제**한다(NEXT_TODO 500차 절이 그렇게 지시한다). ⓒ `.bat` 단독 재현으로 `[GUARD] Running main.py processes: 0` 출력 확인 |
| **표본 상태** | 원인 확정(코드 + 오늘 실측 `count=0` + 배치 자신의 주석). **표본 부족 아님** |

### F-2. 브로커 채널·요율 무조건 상태 샘플 — P1 · 대응 `1-2`

| 칸 | 내용 |
|---|---|
| **변경 대상** | `main.py` 기동 직후 1회(`[System] 미륵이 초기화` 인접) |
| **변경 내용** | 무조건 1줄: `[BrokerChannel] state=OK channel=CREON source=starter_log:C:\CREON\STARTER\ncstarter.log rate=0.000019 effective_from=2026-08-26 launcher_broker=creon` — **성공·폴백 모두** 찍는다(계측 4원칙 ④). `launcher_broker` 는 환경변수/인자에서 읽어 **대사 축을 하나 더 건다**(계측 4원칙 ⑤: 파생값을 대사하려면 구성요소를 각각 걸어라) |
| **회귀 위험** | 낮음(로그 1줄 추가). 🔴 **요율 값·감지 로직은 건드리지 않는다** — 495차 후속이 정한 파생 구조 그대로 |
| **검증** | 다음 거래일 장전 로그에 1줄 출현 + `channel` 이 런처 `BROKER=` 와 일치. 수집기 §12 에 `broker_channel` 고착 지표로 등록(감시목록은 26주 WFA 「선물 수수료율」 ⓓ 와 연결) |
| **표본 상태** | 계측 신설이므로 표본 무관. **판정에는 관여하지 않는다**(관측 전용) |

### F-3. 관측 ID 충돌 3건 표기 분리 + 검출기 보강 — P1 · 대응 `1-3`

| 칸 | 내용 |
|---|---|
| **변경 대상** | ⓐ `dev_memory/NEXT_TODO.md` 6줄(정의 2 + 참조 4) ⓑ `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py` 채번·충돌 검출부 |
| **변경 내용** | ⓐ **번호를 다시 매기지 않는다**(소급 재라벨은 미측정 구간을 만든다 — 계측 4원칙 ②). 기존 관례대로 `(NNN차)` 접미를 붙인다: `O-42(498차)`/`O-42(499차)` · `O-45(499차)`/`O-45(500차)` · `O-46(499차)`/`O-46(500차)`. ⓑ 검출기의 ID 추출 정규식을 **백틱 유무·볼드 유무에 둔감**하게 고쳐, 같은 파일 안의 중복도 잡게 한다 |
| **회귀 위험** | 낮음. ⓑ 는 충돌 목록이 3 → 6건으로 늘어 §11 이 시끄러워질 수 있다 → **`known` 화이트리스트**로 기지 3건을 눌러 늑대소년을 피한다(500차 G-5 가 쓴 것과 같은 방식) |
| **검증** | 다음 다이제스트 §11 채번 줄에 **충돌 6건**이 열거되는지. `tests/test_501_observation_id_collision.py` |
| **표본 상태** | 실측 확정(6줄 인용). **표본 부족 아님** |

### F-4. `G-1` 검증 완료를 참조 문서 2곳에 반영 — P2 · 대응 `1-4`

| 칸 | 내용 |
|---|---|
| **변경 대상** | ⓐ `.claude/skills/mireuk-daily-check/references/invariants.md` §2-B `MODEL_LABEL_STATE_UNLOCK_ENABLED` 관측란 ⓑ `CLAUDE.md` 실전 전환 기준 ⑧ 내 468차 각주 |
| **변경 내용** | `⏳ 라이브 미검증` → `✅ 2026-08-21 장전 P-2 통과 · 2026-08-26 장후 C-2 해소(sizing_trace.quality_mults 에 pre_retrain 키 부재까지 확인) · 0821~0827 5거래일 연속 해제 로그`. ⓑ 도 같은 문장으로. **과거 서술은 지우지 않고 그 아래에 갱신을 덧붙인다**(사료 보존) |
| **회귀 위험** | 🔴 **이것은 ⑧ 을 해제하지 않는다.** ⑧ 의 남은 관문은 여전히 **실전 자본 재설정(사용자 결정)** 하나이며, 489차 D6 이 걸어둔 **선행 잠금(①·④ 미충족 · [28] 판정 불가 · 손절 준수율 미치료)** 도 그대로다. 문장에 그 사실을 **명시적으로 병기**한다 |
| **검증** | `grep -n "라이브 미검증" CLAUDE.md .claude/skills/.../invariants.md` → 0건 |
| **표본 상태** | 두 리포트의 확정 판정 인용. **표본 부족 아님** |

### F-5. `잔존=` 절단 가시화 — P2 · 대응 `1-5`

| 칸 | 내용 |
|---|---|
| **변경 대상** | `main.py:4292-4297` |
| **변경 내용** | `_new = sorted(set(_z_feats_after) - set(_zf))` 를 추가하고 로그를 `… \| 잔존={','.join(_persist)}({len(_persist)}) 신규={','.join(_new)}({len(_new)})` 로. **개수 축과 이름 축이 항상 같은 수를 말하게 한다** |
| **회귀 위험** | 낮음(로그 문자열만). ⚠ **`CORE준비도`·`CORE축퇴_피처` 채널은 다른 로그 줄을 파싱**하므로 영향 없음 — 다만 `잔존=` 을 파싱하는 소비처가 있는지 `grep -rn "잔존="` 로 **변경 전 전수 확인**(현재 코드 1곳·문서 1곳뿐) |
| **검증** | 다음 장전 Phase1~4 로그에서 `잔존(N) + 신규(M)` 합이 `z경고 …→X개` 의 `X` 와 일치 |
| **표본 상태** | 오늘 2건(Phase1·Phase4) 실측. 재현성은 코드로 확정 |

### F-6. 런타임 라이브러리 버전 1줄 기록 — P2 · 대응 `1-6`

| 칸 | 내용 |
|---|---|
| **변경 대상** | `main.py` 기동부(F-2 와 같은 자리 — **한 커밋으로 묶는다**) |
| **변경 내용** | `[Runtime] python=3.7.x-32bit scipy=1.5.4 sklearn=1.0.2 joblib=1.1.1 numpy=…` 무조건 1줄. 기대값과 다르면 같은 줄에 `⚠ 기대={…}` 병기 |
| **회귀 위험** | 낮음. ⚠ import 비용은 이미 다 치른 뒤라 기동 지연 없음 |
| **검증** | 다음 장전 로그 1줄 + `phases.md` A-1 의 해당 체크박스가 「미기록」에서 벗어남 |
| **표본 상태** | 계측 신설 |

### 적용 순서와 커밋 계획

1. **장후에** `F-1`(GUARD) 단독 커밋 — 유일하게 **동작을 바꾸는** 변경이라 섞지 않는다.
2. `F-2` + `F-6` 한 커밋(둘 다 기동부 무조건 상태 샘플).
3. `F-3` + `F-4` + `F-5` 한 커밋(문서·로그 표기).
- 커밋 제목 첫 단어 **`[MW0602]`**. 🔴 **이 예약 실행은 커밋하지 않는다.**
- ⚠ `F-1` 은 `.bat` 이라 **인코딩(UTF-8 no-BOM)·개행(CRLF)을 바이트 단위로 보존**할 것
  (500차가 같은 파일에서 지킨 규약).

---

## 3. 고도화 방안

> 전부 **오늘 관측에서 출발**했다. 일반론 없음.
> §13 확정 결정 레지스트리(19건)와 대조해 **같은 질문의 신규 계측 제안이 아님**을 확인했다.

### G-1. 「집행 장치가 무연산인가」를 재는 여섯 번째 죽음의 형태 — 26주 WFA 신규 등재 제안

- **오늘의 근거**: `1-1` 은 지금까지 정리된 어떤 죽음의 형태에도 안 들어맞는다.
  §12 고착(한 값에 붙박임) · §12 무기록(계측 중단) · 475차 분기편향 · §12c 스냅샷 정체 ·
  §12b 임계 좌초 — 다섯 가지 모두 **관측 축**의 죽음이다. `1-1` 은 **집행 축**이
  죽었는데 로그가 성공을 말한 경우다(*"기존 프로세스 종료 완료"*).
- **같은 계열이 이미 여럿 있다**: 471차 F-1(15:10 강제청산 1차 경로 6개월 도달 불가) ·
  474차(CORE mid/long 그룹 6개월 도달 불가) · 311차 TOX 죽은 섀도. **전부 사람이 우연히
  발견했고 3~6개월이 걸렸다.**
- **제안**: 26주 WFA 「고착 지표 감시목록」(468차 G-2)에 **「집행 무연산 점검」**을 병렬 등재.
  판정 방식 = *"성공을 보고하는 로그마다, 그 성공이 **몇 개를 대상으로** 이루어졌는지
  세는 짝 로그가 있는가"*. 500차 F-3 의 `count=N probe=ok` 가 그 원형이다.
- ⚠ **이번 리포트에서 결정하지 않는다** — 감시목록 확장은 26주 WFA 소관.

### G-2. 「무조건 상태 샘플」을 기동부에 한데 모으는 `[Startup]` 블록

- **오늘의 근거**: `1-2`(브로커 채널) · `1-6`(라이브러리 버전) 이 **같은 형태**다 —
  기동 시점에 결정되고 하루 종일 변하지 않는데 **어디에도 안 남는다.**
  0824 리포트가 같은 성격으로 `[Capability] … investor=Y/N(fetch=0,runtime=N)` 을 물었고,
  오늘도 `[CybosInvestor] futures supported=False … foreign=+0 individual=+0 institution=+0`
  가 그 축의 미해결분으로 남아 있다.
- **제안**: 기동 직후 `[Startup]` 접두로 **한 블록**에 모아 찍는다 — 브로커 채널·요율 ·
  런타임 버전 3종 · 모델 번들 세대(`label_scheme` 6/6) · 전일 EOD/P8 도장 · 활성 피처 수 ·
  차단 게이트 off 개수. 수집기 §2 가 이 블록 하나만 읽으면 「오늘의 자격」이 재구성된다.
- **기대효과**: 장전 체크리스트 A-1·A-3 의 상당 부분이 **로그 1블록 대조**로 자동화된다.
- **회귀 위험**: 낮음(전부 읽기). ⚠ **값을 판정에 쓰지 않는다 — 관측 전용.**
- ⚠ 표본 무관(계측 신설). **F-2·F-6 이 이 방향의 첫 두 조각**이며, 전체 블록화는
  주간회의 승인 후 별도 작업으로 제안한다.

### G-3. 관측 ID 를 사람이 매기지 않게 한다

- **오늘의 근거**: `1-3` 의 충돌 3건은 **세션이 다이제스트 §11 의 「다음 ID」를 손으로 읽고
  손으로 적는 구조**에서 나왔다. 499차 절 머리글이 *"채번은 다이제스트 §11 「다음 관측
  ID = O-42」에 따름"* 이라고 적어둔 그대로 따랐는데도 충돌했다 — **규율의 문제가 아니라
  구조의 문제다.**
- **제안**: `scripts/` 에 `next_observation_id.py` 를 두고 `--allocate N` 으로 **원자적 발급**
  (파일 잠금 + `NEXT_TODO.md` 재파싱). 세션은 손으로 숫자를 쓰지 않는다.
- ⚠ **이번에 결정하지 않는다 — 설계 안건**이다. 최소 조치는 `F-3` ⓑ(검출기 보강)이며,
  그것만으로도 *"충돌이 생겼다"* 는 다음 날 아침에 보인다.

### 로드맵·실전 전환 기준 반영 제안

| 항목 | 현 상태 | 오늘 제안 | 근거 |
|---|---|---|---|
| **⑧ 사이징 재설계** — 선행조건 `G-1` | CLAUDE.md·invariants.md 가 `⏳ 라이브 미검증` | **문서만** ✅ 로 갱신(`F-4`) | 0821 P-2 · 0826 C-2 · 5거래일 연속 실측. 🔴 **⑧ 자체는 해제하지 않는다** — 489차 D6 잠금(①·④ 미충족) 유효 |
| **①·④** | 미충족(489차 D6) | **변경 없음** | 오늘은 장전이라 손익 축 실측이 없다. 라이브 DB 스캔 금지 규약 준수 |
| **26주 WFA — 선물 수수료율 ⓓ(채널 감지)** | 재검증 절차만 있고 **일상 관측 원천이 없다** | `F-2` 로 원천 신설 후 §12 감시목록 등재 | `1-2` |
| **26주 WFA — 고착 지표 감시목록**(468차 G-2) | 5가지 죽음의 형태 | **여섯 번째(집행 무연산)** 등재 검토 | `G-1` |

---

## 4. 다음 국면 관측 예정

> 채번 근거: 다이제스트 §11 「다음 관측 ID = **O-47**」. 여기서부터 발급한다.
> ⚠ **`O-45`·`O-46` 은 오늘 판정했으나 각각 동명이인이 있다**(`1-3`) — 아래 표기 그대로 쓸 것.

### 오늘 닫은 것

| ID | 판정 | 근거 |
|---|---|---|
| **`O-45`(500차, F-3)** | ✅ **판정 — `!=` 결함 확정** | `[GUARD] kill-target count=0 probe=ok`. 사전등록 판정문 그대로 |
| **`O-46`(500차, G-5)** | ✅ **통과** | §12b **5행** 출현 · `SGD표본게이트(5m conf≥0.52)` 관측일 9 · max 0.5966 · `✅ 최근 1일 미도달`. 0826 기준값과 일치 |
| **`O-42`(498차 후속2)** | ✅ **통과 — 세 줄 전부 생존** | §2 에 ① 락 2점 샘플(「없음, 2점 확인」) ② 정본 rev 정합(`2026-08-26e` · 최종커밋 `2026-08-26` · **정합**) ③ 사본 대조(🔴 **드리프트** 검출) 가 **모두** 출력됐다. ▸ 부수 판정: 사본 대조가 `미제출` 이 **아니다** — 이번 예약 프롬프트가 `--skill-rev` 를 넘겼기 때문. 다만 **예약 프롬프트 3종 중 장전 하나만** 그렇다(§9 ③ 여전히 미완) |
| **`O-34`** 크래시 서명 영속화 | ✅ **통과** | `08:40:43 [FaultHandler] 크래시 서명 보존 — 신규 1건 (누적 키 12, crash_signatures.log)`. `logs/crash_signatures.log` 에 `[SIG] 2026-08-27T08:40:43 src=crash_fault.log session=2026-08-26T08:40:42 PID=9548 exc=access violation torn=0 key=19717:6c288ef5a00d` 실재. **분자가 `[SIG]` 줄이라는 494차 F-9 규약대로다** |
| **`O-17`(493차)** 런처 GUARD 10거래일 P1 승격 | ↩️ **판정 보류 — 지표 무효화** | 오늘 9거래일 연속이나 **분자가 `!=` 결함이 만든 상수**임이 확정(`1-1`). 사전등록 판정을 그대로 집행하지 않고, `F-1` 수정 후 **`O-47` 로 다시 센다.** ⚠ 이것은 문턱 완화가 **아니다** — 계측 대상 자체가 유효하지 않았다 |

### 오늘 등록 — 장중·장후가 닫는다

| ID | 무엇을 보는가 | 닫는 국면 | 판정 기준(사전등록) |
|---|---|---|---|
| **`O-47`** | `F-1` 배포 **후** 첫 GUARD 발화일의 `[GUARD] Running main.py processes: N` 과 `kill-target count=N` **일치 여부** | `F-1` 배포 다음 거래일 | 두 값이 같으면 탐지 복구 ✅. `Running…` 이 여전히 미출현이면 수정 실패 |
| **`O-48`** | 장전 CORE 축퇴(`ofi_norm×6hz`)가 **개장 후 풀리는가** + `1-7` 두 계측 정의 일치 여부 | **오늘 장중** | §12 `CORE축퇴_개장해소` 가 `해소`/`무축퇴` 면 정상. `미해소` 면 축퇴 상태로 첫 예측에 들어간 것 |
| **`O-49`** | 수집기 DB 스냅샷 「디스크 여유 부족」 재발 여부 | **오늘 장후** | 장후 다이제스트에 같은 줄이 또 나오면 P2 등록. 1회성이면 종결 |
| **`O-39`**(500차 F-5, **이월**) | `[BrokerNet] state=` 09:00~15:10 **≥1건** + EOD `state=OK` 전이 | **오늘 장중·장후** | 🔴 `SKIP_BLANK_TR` 100% 고착이면 브로커 net 축 전체가 죽은 것이며 원인은 상류(`CpTd6197` 헤더) |
| **`O-38`**(**이월**) | `[HealthPolicy] settings.py 핫리로드 실패` 건수 | **오늘 장후** | 오늘 장전 **0건**. ⚠ 이 로그는 **실패 시에만 나오는 조건부 로그**라 장전 0건은 「정상」이 아니라 **「판정 불가」**다(계측 4원칙 ②). 0826 은 장전 0건 · 하루 총 **194건**(11:57 설정 수정 후 마감까지 연속 실패)이었다. 오늘은 재기동으로 `BROKER_CHANNEL_SPECS` import 가 해소됐을 것으로 **예상**하나 실측 전까지 확정 금지 |
| **`O-40`②**(**이월**) | `trades.commission_rate_used` 라이브 INSERT — 재기동 후 **첫 청산** 행이 NULL 아닌가 | **오늘 장후** | 🔴 장중 DB 스캔 금지. 장후에 확인 |
| **`O-43`·`O-44`**(499차, **이월**) | `[Bias⚠]` FL 편향률 + `WeightCollapse` / 오후 진입 0건 기회 축 | 장후 | 기존 기준 유지 |

**미처분 0건** — 장전이 등록·이월한 항목을 전부 위 두 표에 담았다.

---

## 5. 기한 도래 항목

| 항목 | 기한 | 오늘 상태 |
|---|---|---|
| CB② 복원(`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | 🔴 **날짜 아님 — 「⑧ 해제와 동일 커밋」이라는 사건 기한**(489차 D2) | 미도래. **종전 `2026-08-29` 를 기한으로 인용하지 않았다** |
| 캠페인 채널 체인 `O-20·O-21(0821)` · `O-26~O-29(489차)` | **2026-08-28(금)** | **내일** — 주간 EOD 체인에서 |
| 주간회의 안건 ①②③⑦·E | **2026-08-28(금)** | **내일**. 오늘 `G-1`(여섯 번째 죽음) · `G-3`(ID 자동 채번) 을 안건 후보로 추가 제안 |
| `O-13(492차)` DB pruning · `O-20(0821)` 월간 로그 정리 | **2026-09-01(월간)** | 미도래 |
| 26주 WFA 재검증 항목 전체 | 주기 도래 시 | 미도래 |

---

## 6. dev_memory 반영

- `dev_memory/DECISION_LOG.md` — `## 2026-08-27 (MW0602 501차 — 장전 일일 점검)` **append 완료**
- `dev_memory/NEXT_TODO.md` — 501차 절 **append 완료**(Fix `F-1`~`F-6` · 관측 `O-47`~`O-49` ·
  `O-45/O-46/O-42` 판정 기록 · `O-17` 보류 사유)
- 🔴 **커밋하지 않았다**(예약 실행 규약, SKILL.md §6).

---

## 사용자 조치

> 오늘 장전에 **당장 해야 하는 일은 없습니다.** 아래는 장후·내일 몫입니다.

### 1. (장후) 런처 GUARD 수정 승인 — `F-1`

- **지금 할 일**: 장 마감 후 `F-1`(배치파일 두 줄 수정) 적용 여부를 승인해 주세요.
  자동조치 예약(17:23)이 대신 할 수 있으나, **이 변경만은 동작을 바꾸므로**(지금까지 무연산이던
  종료 명령이 실제로 프로세스를 죽이기 시작합니다) 사람 승인을 권합니다.
- **걸리는 시간**: 승인 1분 / 구현·검증 20분(자동조치가 수행)
- **안 하면**: 프로그램이 두 개 뜨는 날 아무도 막지 못합니다 — 중복 주문과 학습 파일 경합.
  지금까지 6개월+ 그 상태였고, 다만 실제로 두 개가 뜬 날은 아직 관측되지 않았습니다.

### 2. (아무 때나) 미커밋 문서 1건 처리

- **지금 할 일**: `docs/프롬프트/MW0602_장후자동조치_설치지침_20260826.md` 를 커밋할지 결정.
  🔴 **`git add .` 를 쓰지 마세요** — 샌드박스에서 보는 `git status` 는 개행 차이로 수백 건을
  「변경」으로 표시합니다. **경로를 명시해서** `git add "docs/프롬프트/MW0602_장후자동조치_설치지침_20260826.md"`.
- **걸리는 시간**: 1분
- **안 하면**: 어제 만든 설치지침이 다른 PC·새 클론에 전달되지 않습니다.

### 3. (내일 주간회의) 안건 2건 추가 상정 제안

- **지금 할 일**: 2026-08-28 주간회의 안건에 `G-1`(집행 무연산 점검을 26주 WFA 감시목록에
  여섯 번째 형태로 등재) 과 `G-3`(관측 ID 자동 채번) 을 올릴지 결정.
- **걸리는 시간**: 회의 중 5분
- **안 하면**: `1-1`·`1-3` 과 같은 계열이 또 3~6개월 뒤에 우연히 발견됩니다.

### 4. (이월, 미해소) 앱 저장 스킬 교체 + 예약 프롬프트 3종 — `§9 ②·③`

- **지금 할 일**: 498차 후속2 · 500차 ④ 가 두 번 올린 항목이며 **오늘도 그대로**입니다.
  ▸ ② 앱 저장 스킬을 정본(`rev: 2026-08-26e`)으로 교체 — `save_skill` 도구가 **이 세션에도
  없어** 세션이 할 수 없습니다. ▸ ③ 예약 프롬프트 **장중·장후 2종**에 `--skill-rev` 배선
  (장전은 이번에 배선돼 §2 사본 대조가 정상 작동했습니다 — 그래서 `1-3` 의 드리프트가 보였습니다).
- **걸리는 시간**: 10분
- **안 하면**: 이 세션처럼 **한 세대 낡은 사본**으로 점검이 돌고(오늘 §2 가 실제로 🔴 드리프트를
  찍었습니다), 장중·장후는 그 사실조차 못 봅니다 — 2026-08-26 사고의 재발 경로입니다.

### 5. (이월, 사용자 몫) 증권사 HTS 에서 VKOSPI 현재값 1회 확인

- **지금 할 일**: HTS 에서 VKOSPI 현재값을 눈으로 확인해 알려주세요.
  프로그램이 읽는 값 55~70 vs 동류 해외 지표 15.8 — **3.5배 차이**이며 코드로는 판별 불가입니다.
  (참고: 오늘 매크로가 읽은 **미국 VIX 는 15.4** 로 정상 범위입니다. VKOSPI 와는 다른 지표입니다.)
- **걸리는 시간**: 1분
- **안 하면**: 변동성 축 피처의 스케일이 틀린 채로 계속 학습됩니다.

### 6. (상시) 장중(09:00~15:10) 설정 파일 수정 금지

- 0826 에 11:57 수정 후 핫리로드가 마감까지 **194회 연속 실패**했습니다(그날 피해는 0).

---

_`.git/index.lock` · `.git/HEAD.lock` — 세션 시작·종료 시점 **둘 다 없음**(수집기 §2 2점 샘플 + 직접 `ls` 확인). **사용자 조치 없음.**_
