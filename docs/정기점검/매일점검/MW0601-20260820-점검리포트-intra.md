# 미륵이 일일 점검 — MW0601 / 2026-08-20 / 장중

- 점검 시각: 12:32 KST · PC `MW0601` · 브랜치 `v9-dev` ✅
- HEAD `7a59796` · 미커밋 459건(CRLF 잡음 — 아래 §1 P2 참조) · 당일 커밋 2건
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260820_intra.md`
- 관측 창: 08:40 기동 ~ 12:31 (수집 시점). 15:10 이후 사안은 장후 점검 대상.

> **라이브 프로세스가 돌고 있다.** 본 점검은 로그 전용이며 **라이브 DB에 쿼리를 돌리지
> 않았고, 코드를 변경·재기동하지 않았다.** 아래 Fix 계획은 전부 **장후 적용**이다.

---

## 0. 한 줄 결론

**매분 9단계 파이프라인은 09:01~12:31 209분 전량 커버로 정상 가동 중이고 절대원칙 위반은
없다(P0 0건).** 다만 당일 유일 진입 1건이 손실로 끝났고, 그 손실의 **45%가 수집기 §5
거래일 요약에서 구조적으로 보이지 않는다**(부분청산 레그 미집계 — 계측 4원칙 ① 위반).

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 정상 | 481차 장전 점검 참조 (`MW0601-20260820-점검리포트-pre.md`) |
| 장중 | **조건부 정상** | 파이프라인·CB·게이트 정상. 관측(계측) 결함 2건 = P1 |
| 장후 | — | 미도래 (15:10 강제청산·EOD는 장후 점검 대상) |

**⚠ 즉시판단 필요 사항: 없음.** 12:31:33 기준 `포지션=FLAT`, 파이프라인 생존
(`[PipePerf][DBG] total=388ms` @12:27:00), 미결제 주문 없음. 사람이 장중에 개입해야 할
포지션 위험이나 CB 연속 발동은 관측되지 않았다.

**절대원칙 준수 여부**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | 판정 유보(미도래) | 12:31 현재 `포지션=FLAT` — 15:10에 청산할 대상이 없을 가능성이 높다. 471차 F-1R 리허설 미실시 |
| ② | Circuit Breaker 5종 | ✅ 정상 | CB① 신호반전 0건 / CB② 카운터 2회(정지 없음 — `9999` 한시예외대로) / CB③ HALT 0건 / CB④ 0건 / CB⑤ 경고 5건·**5초 초과 0건** |
| ③ | CORE 피처 호라이즌 그룹별 | ✅ 정상 | 10:55 진입 `vwap✅ cvd✅ ofi✅` — 단기(3m) 그룹 CORE 3종 전량 통과. **`vwap` 미통과 진입 0건** |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | ✅ 정상 | `0xC0000409`·`STACK_BUFFER` 0건. `FaultHandler` 활성 PID 13140 단일 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | ✅ 정상 | `-2147221008` 0건, TR 수신 정상(`CpSvrNew7221 supported=True`) |
| ⑥ | 알파 리서치 봇 자동통합 금지 | ✅ 정상 | 장중 자동통합·백테스트 큐 흔적 0건 |

**설정 불변식** — 수집기 §3에서 `불일치` **0건**. `미발견` 5건은
`MODEL_LABEL_STATE_UNLOCK_ENABLED` · `PRE_RETRAIN_DONE_BY_EOD_ENABLED` ·
`ZONE_ENTRY_BAN_ENFORCE` · `ZONE_ENTRY_BAN_SHADOW_ENABLED` ·
`PIPE_LATENCY_EXCLUDE_MODEL_SWAP` — **7일차 기존 오탐**이다. 481차 [4]가 "5개 상수가
`collect_evidence.py` 기대표에만 존재하고 repo 소스 어디에도 없음"을 실측 확인했고
`F-4`(수집기 브랜치 스코프 분리)로 이미 등록돼 있다. **신규 아님.**

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**없음.**

---

### P1 — 정확성·관측 훼손

#### 1-1. 수집기 §5 거래일 요약이 부분청산 레그를 세지 않는다 — 당일 실현손실의 45%가 은닉

- **증상**: 수집기 §5가 *"청산 1건 · 승 0 · 합계 -2.92pt (**-147,628원**)"* 로 보고하지만,
  같은 포지션의 실제 실현손익은 **-269,884원**이다. `[Position] 체결부분청산` 2레그
  (-61,628 · -60,628)가 집계에서 통째로 빠진다.

- **근거**:
  ```
  10:55:00 [Position] 진입 LONG 3계약 @ 1085.54 | 손절=1082.83 1차=1086.44(×0.50) 2차=1088.25 horizon=3m hurst=neutral
  10:55:17 [Position] 체결부분청산 1계약 @ 1084.22 | 잔여=2계약 | PnL=-1.20pt (-61,628원) | 손절1차 조기축소
  10:55:17 [Position] 체결부분청산 1계약 @ 1084.24 | 잔여=1계약 | PnL=-1.18pt (-60,628원) | 손절1차 조기축소
  10:56:05 [Position] 체결청산 LONG @ 1082.5 | PnL=-2.92pt (-147,628원) | 하드스톱(틱)
  ```
  `logs/20260820_TRADE.log` · 10:55:00~10:56:05

  원인은 수집기의 정규식 하나다 —
  `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py:133`
  ```python
  "exit": r"\[Position\] 체결청산 (?P<dir>\w+) @ ...",
  ```
  **`체결부분청산`을 매칭하지 않는다.** TP1/TP2/TP3 분할익절도 같은 접두를 쓰므로
  **이익 포지션은 원리상 더 크게 은닉된다**(오늘은 우연히 손실 쪽만 드러났다).

  독립 대사: `[Sizer]` 로그의 `실제잔고`가 10:55 **50,610,589** → 10:57 **50,313,671**
  = **-296,918원**. 레그 합계(-269,884)와 **27,034원** 차이가 남는데 이 잔차의 정체는
  장중 DB 조회 금지로 확인하지 못했다(수수료·정산 차이 추정, 장후 확인 항목 O-8).

- **기준 위반**: CLAUDE.md **계측 4원칙 ①**(단위 명시 — "손익·승패 집계는 포지션 단위가
  기본이다"). 9단계 STEP 8(청산 트리거 5단계) 관측.

- **영향**: 세 갈래다.
  ① **일일 손익이 체계적으로 과소보고**된다. 오늘은 45% 은닉.
  ② §5가 자체 안내문으로 지시하는 **손절 준수율**(실현손실 ÷ ATR×1.5) 계산의 분자가
     틀어진다 — 417차 재분해에서 **유일하게 유의했던 축**이 바로 이 값이다.
  ③ `승 0 (0%)` 같은 승률 표기가 레그 단위가 되어, 417차가 무효화한 것과 **같은 형태의
     오류**를 점검 리포트가 매일 재생산한다.

- **신규 여부**: **신규.** `체결부분청산` 문자열은 `dev_memory/DECISION_LOG.md`·
  `NEXT_TODO.md` 통틀어 **0건**이다(실측). 계측 4원칙 ①은 문서에 있으나 수집기 자신이
  그것을 어기고 있었다.

---

#### 1-2. 등급 인플레(원시C → A)가 CB③-P4의 유일한 사정권(C급)을 구조적으로 비운다

- **증상**: 10:26에 `acc30m` 단계가 **RESTRICTED**로 전이(acc=20.0%)했고, 29분 뒤인
  10:55에 **확신도 원시 C** 신호가 **체크리스트 A급**으로 진입해 손실로 끝났다.
  CB③-P4는 "RESTRICTED 구간에서 C등급 차단"이 목적인데, **판정 시점의 등급이 이미
  A로 덮여 있어** 이 신호는 P4의 대상이 아니다.

- **근거**:
  ```
  10:26:00 [WARNING] SYSTEM: [CB③-P4] acc30m 단계 전환: NORMAL → RESTRICTED (acc=20.0%)
  10:55:00 [INFO] TRADE: [진입체크] LONG→LONG 3계약 A급(원시C) | sign✅ conf✅ vwap✅ cvd✅ ofi✅ fore❌ prev✅ time✅ risk✅ chas❌ coun✅ | conf=39.7%
  ```
  `logs/20260820_WARN.log` · `logs/20260820_TRADE.log`

  코드 순서가 원인이다 — `main.py:7598`이 앙상블 원시 등급을 체크리스트 등급으로
  **덮어쓴 뒤**, `main.py:7720`의 P4 게이트가 그 덮어쓴 값만 본다:
  ```python
  _final_grade = _cr["grade"]            # main.py:7598 — 체크리스트 등급이 원시를 덮는다
  ...
  if (runtime_settings.CB3_P4_GRADE_BLOCK_ENABLED
          and _final_grade == "C" and self.circuit_breaker.is_grade_restricted()):
  ```

- **기준 위반**: CLAUDE.md 절대원칙 §2 CB③-P4 항목 / 실전 전환 기준 **⑥**
  (`CB3_P4_GRADE_BLOCK_ENABLED` 재검토).

- **영향**: 🔴 **"P4를 켰다면 오늘 손실을 막았다"는 뜻이 아니다 — 정반대다.** 켜져
  있었어도 `_final_grade == "A"`라 통과했을 것이다. 즉 **⑥의 재검토가 "플래그를 True로
  되돌릴 것인가"만 다루면 답이 안 나온다.** 30m 재도입/기준 호라이즌 교체와 별개로,
  **P4의 판정 입력이 원시 등급인지 최종 등급인지**를 함께 정해야 한다. 지금 상태에서
  P4를 True로 되돌려도 등급 인플레 경로는 그대로 열려 있다.

- **신규 여부**: **부분 신규.** "등급 인플레(원시 C → A급)" 축 자체는
  `NEXT_TODO.md:14076` **R-후보**로 08-19에 등록됐다(관측 1일차, 2건 승1 패1).
  **오늘이 관측 2일차** — 1건 추가(패), 누적 **3건 승1 패2**이고 **이틀 연속 패 쪽이
  당일 손실의 전부**다. ⚠ 관측 2일 / 5거래일 목표 → **313차 원칙에 따라 확정 결론 보류.**
  신규인 것은 **"그 인플레가 CB③-P4를 무력화한다"는 기전**이며, 이는 R-후보 항목에
  기록돼 있지 않다.

---

### P2 — 운영 부담·기술부채

#### 1-3. 스킬 참조문서 `phases.md` B-1이 CLAUDE.md 474차와 정면으로 충돌한다

- **증상**: 점검 절차서가 30m 채점을 "점검거리"로 지시하는데, CLAUDE.md는 같은 것을
  **중단 금지**로 못박고 있다.

- **근거**:
  - `.claude/skills/mireuk-daily-check/references/phases.md` B-1:
    *"**T-30은 퇴역 대상**이다 — 30m이 앙상블에서 빠졌으므로 여기 남아 있다면 그 자체가
    점검거리"*
  - `CLAUDE.md` 절대원칙 §3 (474차):
    *"🔴 **30m 학습을 중단하면 안 된다 — CB③의 유일한 입력원이다.** … 30m 예측이 끊기면
    절대원칙 §2의 CB③이 입력을 잃는다."*
  - 오늘 실측: `logs/20260820_LEARNING.log` 30m 채점 **179건**(적중 47 / 미적중 132) 정상 발생.

  같은 파일 §1-②-③이 CB③ 임계를 *"30분 정확도 **< 35%**"* 로 적고 있는 것도 같은 유형이다
  — CLAUDE.md 461차가 **"2026-06-02부터 틀린 문구 — 재인용 금지"** 로 정정한 값이며,
  올바른 값은 `CB_ACCURACY_MIN_30M = 0.28`(방향성 예측만 채점)이다. `invariants.md` §3-B가
  이를 드리프트로 적어두긴 했으나, **§1은 여전히 35%를 "절대원칙 원문"으로 제시**한다.

- **기준 위반**: 스킬 SKILL.md 자체 규약 — *"값이 CLAUDE.md와 어긋나 보이면 CLAUDE.md가
  옳다 — 이 문서가 낡은 것이다. 갱신하고 넘어가라."*

- **영향**: 점검자가 절차서를 따르면 **정상 동작(30m 채점)을 이상점으로 올리게 된다.**
  반대로 35% 문구를 그대로 인용하면 재인용 금지 규정 위반이다. 매 국면 3회 × 매 거래일
  반복 노출되는 경로라 오판 확률이 낮지 않다.

- **신규 여부**: **신규**(스킬 참조문서 범위). CLAUDE.md 본문 정정은 461차·474차에
  이미 완료됐고, 파생 문서만 따라가지 못했다.

#### 1-4. 미커밋 459건 — CRLF 잡음(3일차)

- **증상**: 수집기 §11 적신호 13번 *"미커밋 변경 459건"*.
- **근거**: `git diff --stat CLAUDE.md ROADMAP.md` → **2,200 insertions / 2,200 deletions**
  (완전 대칭 = 줄바꿈 차이). 481차 [4]가 `git diff --ignore-cr-at-eol --stat` **출력 없음**
  = 실질 소스 변경 0건을 실측했다.
- **신규 여부**: **기존** — `NEXT_TODO.md` **F-5**(수집기 CRLF 내성)로 등록됨. 08-19 2-2가
  1일차, 08-20 장전이 2일차, 본 건이 **3일차**. 카운트만 갱신한다.
  ⚠ 481차 경고 재확인: *"어제는 실질 4건이 묻혀 있었으므로 오늘 무해했던 건 우연이다."*

---

### 확인 필요 (확정 아님)

- **[16] `chase_foreign_combo_watch` 표본 1건 추가.** 오늘 유일 진입이 정확히 이 조합
  (`fore❌` + `chas❌`)이고, 등급 A, 결과 패(**-269,884원**, 포지션 단위).
  MW0601 쪽 A등급 누적 손실 방향에 1건이 더 붙는다.
  🔴 **재상정 금지 — 확정 결정이 존재한다.** `config/settings.py:VALIDATION_CAMPAIGN_DECISIONS`
  `chase_foreign_combo_watch`(449차, 2026-08-08 주간회의 승인) = *"활성화 금지 유지 — PC간
  부호 역전"*. MW0601 −451,249원 vs MW0602 **+2,099,082원**으로 두 PC가 정반대를 말한다.
  **재검토 조건은 "양 PC A등급 누적 부호 수렴"** 이며 오늘 표본은 그 수렴을 만들지 못한다.
  → 표본만 축적하고 판단은 주간회의로 넘긴다.

- **acc30m 표본이 ConstOut 재적합 주기에 잠식되는가.** 오늘 `[CB③]` 버퍼 리셋 이벤트 4회
  (09:37 기존 0건·10:11 14건·11:41 28건은 **기아 방지로 스킵**, 11:02는 **실제 리셋**
  — `쿨다운=15샘플`). 즉 CB③이 판정에 쓸 30건 문턱을 **하루에 한 번 넘고 곧바로 초기화**
  됐다. 기아 방지 로직이 최악은 막고 있으나, "CB③이 실질적으로 몇 분간 유효했는가"는
  로그만으로 답이 안 나온다 → **장후 DB 확인 항목**(O-9). 313차 원칙에 따라 판정 보류.

- **잔고 델타 27,034원 잔차**(위 1-1). 장중 DB 금지로 미확인 → 장후.

- **`_tick_header` 블로킹 최대 4,750ms(12:23:05)** — `CB_PIPE_PAUSE_MS = 5_000`의 **95%**.
  오늘 14건 중 4초대가 **6건**(4750/4735/4547/4453/4406/4344ms). 5초 초과 0건이라
  이상점은 아니나, 임계에 붙어 있어 하루 프로파일이 조금만 나빠져도 CB⑤ 실발동 구간이다.
  장후에 종일 분포를 확인할 것(O-10). 참고: 08-19 동결 사고 이후 도입한 FZ-1 워치독은
  `stall_sec=180.0`이라 이 대역과 무관하며, `heartbeat_MW0601_20260820.json`이
  `beat_age_sec 3.0 / strikes 0 / fired false`로 정상이다(FZ-11 오탐 0건 관측 진행 중).

---

### 이미 반영된 사안 (신규 아님)

**이 절은 비어 있지 않다** — 아래 5건은 오늘 로그에 뚜렷하지만 전부 기존 등록분이다.

| 관측 | 오늘 실측 | 기존 등록 |
|---|---|---|
| `ConstOut` 3m 상수출력 → 스케일러 재적합 | **4회** (09:36 · 10:10 · 11:01 · 11:39), 전부 `['3m']`, 해소까지 1~2분 | **08-14 F-8 등록분.** `DECISION_LOG.md:25327` *"전부 ConstOut 트리거… 이미 등록됨. 신규 아님"* / `:26215` *"최근 5거래일 ConstOut 14건 중 13건이 `['3m']` — 3m 편중"*. 08-19도 4회로 **동수** |
| 장중 재학습 4회 → S0 모델 리로드 → CB⑤ 경고 | S0 **2,342~2,632ms**, 파이프라인 2,659~3,114ms, CB⑤ 경고 4건(09:38·10:12·11:03·11:42), `HealthPolicy Degraded 선제차단` 4회 → `[차단] 자동진입 Degraded 최소신뢰도 62.0% 미달` **3건** | 462차 P2 `PIPE_LATENCY_EXCLUDE_MODEL_SWAP`가 이 정확한 경로를 겨냥했으나 **v9-dev에 미배선**(481차 [4] 실측). `F-4`에 물려 있다 |
| `WeightCollapse` 43건 (`미배포=['3m']`) | 09:08~12:23, `flat_score=1.0` 안전망 정상 발동 | `DECISION_LOG.md` 16건 언급 — 기존 관측 축 |
| `ConfFloorGuard` 1건 (09:06, 출력상한 0.3528 < 필요 0.3790) | 개장 초 1회, 이후 `FQAdj`로 min_conf 완화되며 해소 | `DECISION_LOG.md`·`NEXT_TODO.md` 각 11건 — 기존 |
| CB② 카운터가 **한 포지션**에서 `1회`·`2회` 둘 다 | 10:55:17 손절1차 조기축소 → `연속 손절 1회`, 10:56:05 하드스톱 → `2회` | `NEXT_TODO.md:14085` **[08-29 CB② 보강]** *"시간창 없음 + 레그 단위 카운트"*의 라이브 실례. 08-19 CASE-02에 이어 **2일차** |

추가로 확인했으나 이상 없던 것: `degraded=ON` **0건** / `[Brier] 과신 경고` 0건 /
`SHAP 슬로우` 0건 / `MemoryError`·OOM 0건 / 장중 코드 배포·재기동 흔적 0건 /
`PSI=0.008~0.009 level=0`(FP-CRITICAL 섀도 정상).

---

## 2. Fix 작업 구현계획

> 🔴 **장중이다. 본 계획은 수립만 하고 적용하지 않는다.** 라이브 프로세스(PID 13140)가
> 돌고 있다. **전 항목 15:10 강제청산 이후 적용**을 권고한다. 커밋도 장후에 한다.

### F-1. 수집기 §5에 부분청산 레그를 포지션 단위로 합산 — P1 · 대응 이상점 1-1

- **원인 가설**: `exit` 정규식이 `체결청산`만 매칭한다. `체결부분청산`은 다른 로그 문자열이라
  **정규식 한 줄이 통째로 놓친다.** 익절 경로(TP1~TP3)도 같은 문자열이므로 이익 포지션은
  더 크게 은닉된다.
- **변경 파일**:
  - `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py`
    - `:133` `PATTERNS["exit"]` — `체결(부분)?청산` 으로 확장하고 `partial` 그룹 캡처 추가
      (기존 그룹명 `dir/px/pt/won/reason` 유지 → 하위 소비부 무변경).
    - `:911` 부근 §5 렌더러 — 표를 **2단**으로 낸다:
      `청산 이벤트 n건(레그)` / **`포지션 n건 · 포지션 합계 ±원`**.
      포지션 경계는 `[Position] 진입` ~ `잔여=0` 또는 `[청산 완료]`로 잡는다
      (`[청산 완료] PnL=` 라인이 최종 레그와 짝을 이룬다).
    - 컬럼 머리글에 **단위 접미사**를 박는다 — `pnl_won_per_leg` / `pnl_won_per_position`
      (계측 4원칙 ①이 명시적으로 요구하는 형식).
  - 같은 파일 §5 안내문 — "손절 준수율" 문장 옆에 **포지션 단위 값을 쓰라**고 못박는다.
- **회귀 위험**: 낮음. 수집기는 표준 라이브러리만 쓰는 **읽기 전용 스크립트**이고 라이브
  경로와 무관하다. 다만 **과거 날짜본 리포트의 §5 수치와 불연续**이 생긴다 →
  다이제스트 §5 상단에 `[집계 개정 2026-08-20]` 마커를 남겨 461차 MDD 개정과 같은 처리를 한다.
- **절대원칙 저촉 여부**: 없음(6종 어디에도 저촉되지 않음 — 계측 코드).
- **런타임 주의**: 수집기는 `py37_32`/`py310_64` 무관. COM 콜백 밖.
- **검증 방법**: 08-19·08-18 로그로 재실행해 ① 포지션 합계가 `[청산 완료]` 라인 합과
  일치하는지 ② 익절 포지션(TP 분할)이 1포지션으로 묶이는지 확인. 08-20분 재실행 시
  기대값 = **포지션 1건 · -269,884원**.
- **섀도 계측 필요 여부**: 불필요(게이트 신설 아님).
- **적용 시점**: **장후.**
- **결정 필요 사항**: 없음.

### F-2. `phases.md` B-1 / `invariants.md` §1 을 CLAUDE.md 461·474차에 맞춘다 — P2 · 대응 1-3

- **원인 가설**: CLAUDE.md 본문은 461차(CB③ 28%)·474차(30m 유지)에 정정됐으나 스킬
  참조문서가 그 이전 세대 서술을 그대로 들고 있다.
- **변경 파일**:
  - `.claude/skills/mireuk-daily-check/references/phases.md` B-1 —
    *"T-30은 퇴역 대상"* 삭제 → *"**30m 채점은 유지가 정상이다** — 앙상블 가중은 0(296차
    퇴역)이나 `main.py:5539`가 CB③ `record_accuracy()`의 입력으로 쓴다(CLAUDE.md 474차).
    **30m 채점이 끊긴 것**이 점검거리다"* 로 뒤집는다.
  - 같은 파일 B-4 확률 판단 기준 항목 — `conf` 절대값을 구 표에 대지 말라는 경고를
    본문 체크박스로 승격(현재는 SKILL.md 쪽에만 있다).
  - `.claude/skills/mireuk-daily-check/references/invariants.md` §1-②-③ —
    *"30분 정확도 < 35%"* → *"**30분 방향성** 정확도 < **28%**(`CB_ACCURACY_MIN_30M`,
    FLAT 예측 제외 — 98차/461차). ⚠ 35%는 재인용 금지"*.
- **회귀 위험**: 없음(문서). 다만 §3-B의 드리프트 표에서 해당 행이 중복되므로 §3-B는
  "이력"으로 표기만 조정.
- **절대원칙 저촉 여부**: 없음.
- **런타임 주의**: 해당 없음.
- **검증 방법**: 다음 점검 세션이 30m 채점을 이상점으로 올리지 않는지 확인.
- **적용 시점**: **장후**(F-1과 같은 커밋 가능).
- **결정 필요 사항**: 없음.

### F-3. CB③-P4 판정 입력을 원시 등급으로 볼지 결정 — P1 · 대응 1-2 · **조사만, 코드 변경 없음**

- **원인 가설**: `main.py:7598`의 등급 덮어쓰기가 P4(`:7720`)보다 앞선다. 설계 의도가
  "확신도 저하 구간에서 저확신 신호를 막는다"였다면 입력은 원시 등급이어야 하고,
  "체크리스트 종합 판단"이었다면 현행이 맞다. **로그만으로는 어느 쪽인지 알 수 없다.**
- **변경 파일**: **없음(이번 회차).** 조사 산출만:
  - `_entry_raw_grade`(`main.py:9162`)와 `trades.raw_grade` 컬럼이 이미 존재하므로
    **장후에 DB로** `raw_grade='C' AND grade IN ('A','B')` 진입의 건수·손익을 뽑는다.
  - 표본이 `min_samples` 미달이면 **판정하지 않는다**(313차). 문턱을 낮추지 말 것(458차 D6).
- **회귀 위험**: 해당 없음(조사).
- **절대원칙 저촉 여부**: 없음. ⚠ 향후 P4 입력을 바꾸는 것은 **매매 정책 변경**이므로
  주간회의 안건이며, 도입한다면 **섀도 계측을 먼저** 거친다(SKILL.md 실행 규약 7).
- **적용 시점**: 조사는 **장후**, 코드 변경은 **주간회의 결정 후**.
- **결정 필요 사항**: 🔴 **실전 전환 기준 ⑥의 문언 확장 여부.** 현행 ⑥은 "30m 재도입
  또는 CB③ 기준 호라이즌 교체"만 말하는데, **P4 판정 입력(원시 vs 최종)** 이 정해지지
  않으면 플래그를 True로 되돌려도 오늘 같은 경로는 그대로 통과한다. 주간회의 상정 권고.

### 적용 순서와 커밋 계획 (전부 장후)

1. **커밋 ①** — F-1 + F-2 (수집기 집계 정정 + 참조문서 정합) —
   `[MW0601] 481차 후속: 수집기 §5 포지션 단위 집계 + 스킬 참조문서 461·474차 반영`
2. **커밋 ②** — dev_memory 기록 (본 리포트 §8) —
   `[MW0601] 481차 후속: 0820 장중 점검 — 부분청산 미집계 · P4 등급인플레 기전`
3. F-3은 장후 조사 결과가 나온 뒤 **별도 세션**에서 다룬다.

---

## 3. 고도화 방안

### G-1. 포지션 조립기(position assembler)를 수집기의 1급 구조로 승격 — 이번 주

- **관측 근거**: 오늘 §5가 손익의 45%를 놓쳤다(1-1). 이것은 정규식 하나의 실수처럼
  보이지만, **같은 오류가 이미 두 번 났다** — 417차의 이벤트 단위 사이징 통계 4종
  무효화(재인용 금지 목록 §3-2), 그리고 오늘. 원인이 같다: `trades`/로그의 자연 단위가
  **레그**인데 의미 있는 단위는 **포지션**이다.
- **제안 내용**: `collect_evidence.py`에 `assemble_positions(lines) -> List[Position]`을
  두고 §5의 모든 손익·승률·수량·보유시간 집계가 **그 함수만** 쓰게 한다(레그 표는 참고용
  하단으로 내린다). 진입(`[Position] 진입`) → 부분청산 n → 최종청산(`[청산 완료]`)을
  하나로 묶고, 미종결 포지션은 `open=True`로 남겨 장중 점검에서도 안전하게 쓰이게 한다.
- **기대 효과**: 측정 가능 — 재실행 시 §5 포지션 합계가 `[청산 완료]` PnL 합과 **오차 0**.
  이후 손절 준수율·qty별 손익이 417차 정정본과 **같은 단위**로 자동 산출된다.
- **비용·위험**: 수집기 내부 리팩터 반나절. 라이브 무영향. 과거 날짜본과 불연속(→ 마커).
- **선행 조건**: F-1(정규식·렌더러). G-1은 F-1의 일반화이므로 같은 주에 이어서 한다.
- **우선순위 제안**: **이번 주.**

### G-2. `[Sizer]` 배수 상수화 감시 — 이번 주 (F-1과 같은 커밋 가능)

- **관측 근거**: 오늘 사이저 호출 9건의 배수 조합이 **전부 동일**하다 —
  `신뢰도배수=0.6 레짐배수=1.0 안전배수=1.00`(수집기 §5 *"배수 조합 상위 —
  `conf=0.6 regime=1.0 safe=1.00`×9"*). phases.md B-4가 *"배수 조합이 고정이면 신뢰도·레짐
  배수가 실질적으로 상수처럼 작동하는 것은 아닌지 본다"*고 지시하는 바로 그 상태이며,
  `[ConfShadow: 1.0→2계약]` 섀도가 3건 붙어 "0.6이 아니었다면 계약수가 달랐다"를 명시한다.
- **제안 내용**: 수집기 §5에 **배수별 고유값 개수**를 한 줄 추가 —
  `conf: {0.6×9} (unique=1)` 형태. 3거래일 연속 `unique=1`이면 §11 적신호로 올린다.
  ⚠ **계측만이다** — 배수 자체를 건드리는 것은 사이징 정책 변경이며 실전 전환 기준 ⑧에
  묶여 있다(`MAX_CONTRACTS=3`은 431차 배포분, **재상정 금지**).
- **기대 효과**: "곱셈 게이트가 상수처럼 굳어 사이저 출력이 무의미해지는" 상태를
  417차처럼 사후에 발견하지 않고 3일 안에 잡는다.
- **비용·위험**: 수집기 20줄. 라이브 무영향.
- **선행 조건**: 없음.
- **우선순위 제안**: **이번 주.**

### G-3. ConstOut→재적합→acc30m 리셋 연쇄의 CB③ 유효시간 계측 — 다음 주 (선행: 장후 O-9)

- **관측 근거**: 오늘 3m ConstOut 4회가 각각 스케일러 재적합을 트리거했고, 그 뒤
  `[CB③] acc30m 버퍼 리셋` 판단이 4회 돌았다(3회 기아방지 스킵 / **11:02 실제 리셋**).
  CB③은 절대원칙 §2의 다섯 중 하나인데 **그 입력 버퍼가 하루에도 몇 번 초기화 후보가
  된다.** 기아 방지 로직이 최악은 막고 있으나 "CB③이 오늘 몇 분간 판정 가능 상태였는가"는
  **아무 데도 기록되지 않는다.**
- **제안 내용**: `[CB③]` 로그에 **표본 수와 판정가능 여부를 항상 병기**하고
  (`n=28 ready=False` — 계측 4원칙 ②·④의 "미측정 ≠ 0"), 수집기 §5에 일일
  **`CB③ ready 분 수 / 전체 장중 분 수`** 를 낸다.
- **기대 효과**: FP-CRITICAL이 "저장 함수가 호출된 적 없어 2개월간 PSI=0.0"이었던 것과
  **같은 형태의 침묵**을 CB③에서 사전 차단한다. 실전 전환 기준 ②(CB 정상 작동 확인)의
  근거 품질도 함께 오른다.
- **비용·위험**: 로그 1줄 + 수집기 집계. 라이브 코드를 건드리므로 **장후 적용 + 다음
  기동부터 유효**.
- **선행 조건**: 🔴 **장후 O-9**(CB③ 실제 표본 추이 확인)가 먼저다. 원인 모르는 채
  비율만 자동화하면 481차 G-3이 경고한 *"숫자는 있는데 뭘 할지 모르는 채널"* 이 는다.
- **우선순위 제안**: **다음 주.**

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| 실전 전환 기준 **⑥** | *"30m 재도입 또는 CB③ 기준 호라이즌 교체 시 True"* | **"+ CB③-P4의 판정 입력(원시 등급 vs 최종 등급)을 함께 정할 것"** 추가 | 1-2 — 등급 인플레가 P4의 유일 사정권(C급)을 비워, 플래그만 되돌리면 오늘 같은 경로가 그대로 통과한다 |
| 계측 4원칙 ① | 코드 규약 | **점검 수집기 자신에게도 적용**을 명시 | 1-1 — 원칙을 담은 문서를 만든 도구가 그 원칙을 어기고 있었다 |
| 주간회의 안건 | — | **[16] `chase_foreign_combo_watch`** 표본 갱신 보고(오늘 A급 패 1건 추가, PC 부호 역전 미해소) | 449차 확정 결정 유지 상태에서 표본만 축적 |

---

## 4. 진입 승패 사후검증 (장후 한정)

**해당 없음(장후 전용).**

단, 장후 세션이 이어받을 케이스 골격만 남긴다 — 12:31 현재 당일 포지션은 이 1건뿐이다.

```
[CASE-01] 진입 10:55:00 LONG · 3계약 (등급 A / 원시 C · horizon=3m · hurst=neutral)
          → 10:55:17 부분청산 1ct @1084.22 (-61,628)
          → 10:55:17 부분청산 1ct @1084.24 (-60,628)   [손절1차 조기축소]
          → 10:56:05 하드스톱(틱) 1ct @1082.5 (-147,628)
          = 포지션 합 -269,884원 · 보유 65초
 진입근거   [진입체크] fore❌ chas❌ (나머지 9항 ✅) | conf=39.7% · [Sizer] conf=0.6 regime=1.0 safe=1.00 → 3계약(=MAX_CONTRACTS 상한 도달)
 손절설계   진입 1085.54 · 손절 1082.83 = 2.71pt (= ATR 1.8071 × 1.5 ✅ 설계대로)
 슬리피지   진입 힌트 1085.54 → 체결 1085.42 / 하드스톱 stop=1082.71 → 체결 1082.5
 승패판정   ❌ 패 — TP1(1086.44) 미도달, `tp1_reached=False` 로 확인 필요(장후)
 미확정     손절 준수율·요인 분류는 장후 DB 대사 후
```

## 5. 수익률 향상방안 (장후 한정)

**해당 없음(장후 전용).**

---

## 6. 다음 국면(장후)·다음 거래일 관측 예정

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| O-7 | 15:10 강제청산 경로 — `[ForceExitPass]`→`[TimeExit]`→`[ExitAttempt]` | 12:31 현재 FLAT이라 **미발생이 정상**. `[SchedForceExit] … 안전망 발동`(ERROR)이 뜨면 P0 | 2026-08-20 장후 |
| O-8 | 잔고 델타 잔차 27,034원 (1-1) | `trades` 포지션 합 vs 잔고 델타 일치 여부 | 2026-08-20 장후 |
| O-9 | `[CB③]` acc30m 표본 추이 — 종일 리셋/스킵 횟수와 `ready` 구간 | ready 구간이 장중의 50% 미만이면 G-3 착수 | 2026-08-20 장후 |
| O-10 | `_tick_header` 블로킹 종일 분포 | 5,000ms 초과 1건 이상이면 CB⑤ 실발동 — 사유 추적 필수 | 2026-08-20 장후 |
| O-11 | `ConstOut ['3m']` 종일 횟수 | 6회 이상이면 08-14 F-8 재발 강도 상향 (08-19·08-20 오전 각 4회) | 2026-08-20 장후 |
| O-12 | 등급 인플레(원시C→A) R-후보 **3일차** | 5거래일 누적 전까지 **확정 결론 금지**(313차) | 2026-08-21 |
| O-13 | FZ-11 워치독 오탐 — `heartbeat_MW0601_20260820.json` `beat_age_sec` | 종일 0~5초면 오탐 0건 확정(관측 1일차) | 2026-08-20 장후 |
| O-14 | 로컬 커밋 push (`origin/v9-dev` 대비 ahead) | MW0602가 480차 후속을 보는가 | 2026-08-20 장후 |

## 7. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| CB② 복원 (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | **2026-08-29** | **9일** | 미도래. 08-29 주간회의 상정 준비 — `NEXT_TODO.md` **[08-29 CB② 보강]**(레그 단위 카운트 · 시간창 없음)에 **오늘 실례 1건 추가**(한 포지션이 `1회`·`2회`를 둘 다 생성, 2일 연속) |
| CB③-P4 재검토 (전환기준 ⑥) | 기한 없음 | — | **1-2로 안건 확장 필요** (F-3 참조) |
| TOX-SEVERE-SPREAD (전환기준 ⑨) | 기한 없음 | — | 473차 F-8 Phase A `INSUFFICIENT`(ETA 7.1개월). 주간회의 안건 유지 — 오늘 신규 표본 없음(진입 1건) |

---

## 8. dev_memory 반영

- `DECISION_LOG.md` 추가: `## 2026-08-20 (MW0601 481차 후속 — 장중 점검 · 분석만, 코드 0건)`
  — 항목 3건([1] 부분청산 미집계 / [2] 등급인플레 × CB③-P4 / [3] 참조문서 드리프트)
  + 재발 카운트 갱신 2건 + 정상 확인 절
- `NEXT_TODO.md` 추가 체크박스: **11건** (F-1·F-2·F-3 / G-1·G-2·G-3 / O-7~O-11 중 신규분)
- 완료 처리한 기존 항목: 없음
- 🔴 **git commit·push는 하지 않았다** (장중 금지 규약)
