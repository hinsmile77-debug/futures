# 미륵이 일일 점검 — MW0601 / 2026-09-01 (화)

- 브랜치 `v9-dev` (MW0601 정상) · HEAD `c5eddda`
- 증거 다이제스트: `docs/정기점검/매일점검/evidence_MW0601-20260901_pre.md` (생성 09:00:46 KST)
- 이 파일은 **하루 한 파일**이다. 장전이 만들고 장중·장후가 이어 붙인다.

---

## 0. 오늘 한눈에

**판정: 정상 (개장 전 위험 신호 없음)**

- 어젯밤(8월 31일) 마감 재학습이 성공했고, 오늘 아침 프로그램은 그 성공을 확인하고
  중복 학습을 정상적으로 건너뛰었습니다.
- 어제 하루 종일 자동매매 기능이 잠겨 있었던 문제(어제 저녁에 원인을 찾아 고치는
  코드를 이미 배포했습니다)가 오늘 아침에도 재발하는지가 가장 중요한 확인 사항이었는데,
  **재발하지 않았습니다.** 프로그램을 켜자마자 "계좌에 남은 포지션 없음"을 확인하고
  잠금을 즉시 풀었습니다.
- 계좌 잔고를 프로그램이 확인한 결과 포지션은 없고(빈 계좌), 어제 아침 있었던 것과
  같은 이월 포지션 사고는 오늘은 없습니다.
- 증권사 프로그램(Cybos) 접속, 실시간 시세 수신, 오전 8시 55분 매크로 지표 수집과
  시장 분위기 판정(중립)까지 순서대로 정상 진행됐습니다.
- 개장 순간 화면이 약 6.3초 멈춘 흔적과, 일부 계산이 임시로 값을 우회 처리한 흔적이
  있었지만 둘 다 매일 개장 시점에 반복되는 익숙한 현상이라 이상으로 보지 않았습니다.
  다만 오전 9시 1분에 "지금 신뢰도가 낮아 잠시 자동 진입을 걸러낸다"는 안전장치가
  한 번 작동했는데, 이것이 실제로 진입을 막았는지는 오전 장중에 마저 확인해야 합니다.
- 이전부터 미뤄지고 있던 결정 두 가지(연속 손실 시 매매를 멈추는 안전장치를 다시 켤지,
  그리고 주간회의에 올릴 안건 3건)는 오늘도 아직 정해지지 않았습니다 — 새로 생긴
  문제는 아니고, 사람이 정할 차례를 기다리는 중입니다.
- **개장 전 조치가 필요한 급한 문제는 없습니다.**

---

# 제1부. 장전 (pre) — 2026-09-01 08:57~09:01

## 병행 세션 확인 (자기 절을 쓰기 전에)

```
git --no-optional-locks log --format="%ci %h %s" --since="2026-09-01 00:00"   → (당일 커밋 없음)
ls -lt docs/정기점검/매일점검/                                                  → 09-01 산출물 없음(이 세션이 처음)
```

오늘(2026-09-01) 새 커밋·새 산출물 없음. 마지막 커밋은 어제(08-31) 19:09:29 `c5eddda`
(508차 — F-6 배포: Restart Armistice 고착 해소). 병행 세션 없음 — 이 절이 오늘 첫 기록이다.

`.git/index.lock` 없음 (수집기 자가점검: "이 수집 실행은 락을 만들지 않았다").

## 전일(08-31) 리포트가 남긴 관측·조치 — 오늘 아침 판정

08-31 장후 리포트(`MW0601-20260831-점검리포트.md`)가 다음 거래일(오늘) 장전 판정을
요구한 항목이 하나 있다. 나머지는 장중·장후 대상이라 아래 표에서 이월만 표시한다.

| 번호 | 무엇을 | 오늘 판정 | 근거 |
|---|---|---|---|
| **O-t2** | Restart Armistice(재시작 뒤 걸리는 임시 진입 잠금) 재현 여부 | **✅ 판정 완료 — 재현 안 됨(정상)** | `logs/20260901_SYSTEM.log:23~33` — 08:41:07 startup sync 시작 → 08:41:08 `armistice cleared`. 어제(08-31)는 이 해제가 하루 종일 한 번도 안 되고 47건이 막혔던 것과 대조적이다. 08-31 508차(F-6)가 배포한 승격 조건(`sync_count<2` AND `time_ok` AND 브로커 동기화 검증완료)이 오늘 정상 경로로 작동했다 |
| O-t3 | EOD 치유 실효(`update_live` 예외 소멸·`fp_psi` non-NULL) | 부분 확인 — 판정은 장중 | 오늘 09:00:00 `[RegimeFingerprint] PSI=0.003 level=0`으로 실값이 찍혔다(NULL 아님). `update_live` 예외 소멸 여부는 아직 장중 표본이 더 필요하다 |
| O-t1 | net 대사 잔차의 지배 축 | 이월 — 장후 판정 | 오늘 거래가 없어 장후에나 값이 생긴다 |
| O-t4 | 15m 호라이즌 EOD 교체 보류 반복 | 이월 — 장후 판정 | 동일 |
| P-1 | 엔진 자동 진입 재개 여부 | 이월 — 장중 판정 | 09:01 기준 아직 첫 진입 판단 창이 지나지 않았다(`ensemble_decisions.entry_executed` 미확인) |
| P-2 | 외부 진입 재유입 여부 | 이월 — 장중 판정 | 동일 |
| O-p2(08-31 이월) | `[CybosOrder] ret=` 코드 의미 | 이월 — 발생 시 | 오늘 아직 미발생 |

**미처분 없음** — 오늘 장전이 판정할 수 있는 것(O-t2)은 판정했고, 나머지는 정의된
대로 장중·장후로 넘긴다.

## 장전 필수 확인 (SKILL.md §0 체크리스트)

| 확인 항목 | 결과 | 근거(로그 인용) |
|---|---|---|
| 전일 EOD 재학습 성공 여부 | ✅ 정상 | `08:55:08 [PreRetrain] 08:55 사전 재학습 스킵 — 1영업일 전(2026-08-31) EOD 재학습 성공(동일 데이터 중복 불필요)` — 08:55 사전 재학습이 스킵된 것은 전날 성공의 정상 결과다 |
| py37_32(32비트) 런타임 | ✅ 정상 | `08:41:08 [Runtime] Python 3.7.13 32bit \| scipy=1.5.4 \| sklearn=1.0.2 \| joblib=1.1.0 \| numpy=1.21.6` |
| Cybos Plus 접속 | ✅ 정상 | `08:45:08 [CybosRT-START] snapshot begin/end code=A0569 price=1068.56` — 시세 스냅샷·틱/호가 구독 정상 |
| 선물 분봉 원천(OPT50029 해당 없음 — Cybos 브로커는 실시간 틱 + 자체 집계) | ✅ 정상 | SYSTEM 로그에 `[BAR-CLOSE]` 16건 확인 — 실시간 틱을 받아 자체 분봉을 만드는 정상 경로 |
| 08:55 매크로 수집 → 레짐 판정 | ✅ 정상 | `08:55:08 매크로 seed fetch 완료 — 레짐 확정은 08:58 2단계로 연기` → `08:58:12 [Regime] NEUTRAL(점수=1) \| VIX=14.4(극저공포) \| SP500=-0.58%(하락) \| USD/KRW -0.44%(중립)` — 2단계 지연은 설계된 정상 흐름 |
| 실시간 구독 사전 시작 | ✅ 정상 | `08:41:07`~`08:45:08` `[CybosSub]` 체결·틱·호가 구독 각각 create→dispatch→subscribe 성공 |
| 한시 예외 재검토 기한 경과 여부 | ⚠ 지속(신규 아님) | `CB_CONSEC_STOP_LIMIT=9999`(CB②, 연속 손실 시 매매 정지 안전장치가 사실상 꺼진 상태) 재검토 기한이 **2026-08-29로 이미 3일 지났다.** 어제 리포트가 이미 사용자 조치 5번으로 올린 항목 — 새로 발견한 문제 아님, 아래 사용자 조치에서 이어서 안내 |
| 계좌·포지션 확인 | ✅ 정상 | `08:41:08 [BrokerSync] 무포지션 확인(blank rows): FLAT → FLAT` — 이월 포지션 없음. 어제 아침 546만원 손실을 만든 것과 같은 유형의 사고는 오늘 없다 |

## 새로 확인된 이상점

**없음.** 오늘 장전 구간(08:40~09:01)에서 절대원칙·설계 문서 위반으로 볼 만한 새 결함은
발견되지 않았다.

## 관측 등록 (다음 거래일 아님 — 오늘 장중이 판정·보류·이월 중 하나로 닫는다)

| 번호 | 무엇을 | 근거 | 닫는 조건 |
|---|---|---|---|
| **O-p1** | 개장 직후 "신뢰도 낮음 예비차단"(HealthPolicy Degraded 선제차단)이 실제 진입을 막았는가 | `09:01:00 [WARNING] [HealthPolicy] Degraded 선제차단: streak=1.00+1.00 ≥ 2 (latency=1651ms quality=0.86 cache=0s exc10m=0) \| cause=S5(1207ms)` — 개장 버스트로 파이프라인 처리시간이 순간적으로 늘어난 것이 원인. 이 패턴은 거의 매일 개장 직후 반복되는 것으로 `dev_memory`에 이미 여러 차례 기록돼 있다(신규 유형 아님) | `ensemble_decisions`에서 이 시각 실제 판정 사유가 `등급X`(무관한 이유)였는지, 이 예비차단이 직접 원인이었는지 대조 |
| **O-p2** | "자동진입 하한 도달 불가"(ConfFloorGuard) 상태가 언제 정상(OK)으로 복귀했는가 | `09:00:01 [WARNING] [ConfFloorGuard] 자동진입 하한 도달 불가 — 보정기 출력상한 0.3479 < 필요 0.4240` — 개장 직후 보정기 출력 범위가 좁아 어떤 신호도 진입 기준을 못 넘는 상태. 매일 개장 시 반복되는 익숙한 패턴(`dev_memory/NEXT_TODO.md`에 O-4·O-5 계열로 다수 기록) | 09:01 이후 로그에서 `state=OK` 복귀 시각 확인 |

## Fix 작업 구현계획

없음 — 새로 발견된 결함이 없어 신규 Fix 항목 없음. 기존 미해결 항목(CB② 복원 여부 등)은
아래 사용자 조치에서 계속 추적한다.

## 고도화 방안

없음 — 장전 구간 관측만으로는 새 고도화 근거가 부족하다(일반론 금지 원칙).

## 이 절이 발급한 번호

이상점 없음 · 관측 O-p1~O-p2 · Fix 없음 · 고도화 없음. O-t2 판정 완료(08-31 발급분 종결).

---

## 사용자 조치

**오늘 개장 전 급한 조치는 없습니다.** 아래는 이미 알려진, 계속 열려 있는 항목입니다
(새로 생긴 문제 아님 — 어제 리포트에서 이어옵니다).

### 1. 연속 손실 시 매매를 멈추는 안전장치, 다시 켤지 아직 정해지지 않았습니다 — **소요 10분 · 이번 주 중**

재검토하기로 한 날짜(8월 29일)가 지난 지 3일째입니다. 지금은 사실상 꺼져 있어서
(연속 손절 제한이 사실상 무제한), 하루 안에 손실이 여러 번 반복돼도 프로그램이
스스로 멈추지 않습니다.

**안 하면**: "다시 켤지 정하기로 했는데 안 정함" 상태가 계속 쌓입니다.

### 2. 주간회의에 올릴 안건 3건이 아직 그대로 있습니다 — **소요 0분(기록만) · 다음 주간회의**

① 승률 기준(전환 조건의 승률 53%)이 수수료 반영 전/후 중 어느 쪽인지 ② 프로그램
밖에서 들어온 매매가 섞인 날의 손익을 어떻게 처리할지 ③ 모델 교체 도중의 지연을
안전장치 계산에서 뺄지 — 세 가지입니다. 어제 리포트에 상세 내용이 있습니다.

### 3. 커밋 — **소요 1분 · 오늘 아무 때나**

**커밋 대기 파일** (이 세션이 만들거나 수정):

```
docs/정기점검/매일점검/MW0601-20260901-점검리포트.md
docs/정기점검/매일점검/evidence_MW0601-20260901_pre.md
dev_memory/DECISION_LOG.md
dev_memory/NEXT_TODO.md
```

⚠ **`git add .`를 쓰지 마십시오.** 현재 미커밋 513건 중 **실질 변경 0건·코드(.py) 0건·
줄바꿈 문자 차이(EOL) 513건**입니다(리눅스 환경에서 파일을 열람만 해도 이렇게 표시될
수 있습니다). 위 4개 경로를 **명시해서** add 해 주십시오.

`.git/index.lock`은 남아 있지 않습니다(수집기 자가점검 통과).

---

*이 절이 발급한 번호: 관측 O-p1~O-p2. 이상점·Fix·고도화 신규 없음.*

*장전 09:01 기준 작성. 장중·장후가 이어 붙일 것이다.*

---

## 0-B. 12:30 갱신

**판정: 조건부 (오전 한때 원인이 불분명한 매매로 손실이 났으나, 지금은 정상으로 돌아왔습니다)**

- 오늘 오전 9시 9분부터 9시 33분 사이, 프로그램이 스스로 판단해서 낸 것이 아닌 매매가
  계좌에서 7번 체결됐고, 합쳐서 약 121만원의 손실이 났습니다. 이 매매들은 프로그램이
  평소에 신호를 확인하고 승인하는 절차를 거치지 않은 채 체결 사실만 나중에 확인된
  것들이라, 프로그램 자신이 낸 매매가 아니라고 보고 있습니다. 이런 일이 일어난 적이
  이전에도 있었고, 이미 다음 주간회의에 올려야 할 안건으로 기록돼 있던 사안입니다 —
  다만 오늘 다시, 이번에는 상당한 손실 규모로 재발했습니다.
- 그 여파로 프로그램 내부에서 경고성 기록이 순간적으로 몰려 쌓였고, 그 결과 오전
  9시 24분부터 9시 43분까지 약 19분 동안 프로그램이 스스로 "지금 상태가 안 좋다"고
  판단해 이 시간 동안은 새로운 자동매매를 걸지 않도록 스스로 잠갔습니다. 19분 뒤
  정상으로 돌아왔습니다.
- 반대로, 프로그램이 정상 절차를 거쳐 스스로 판단해서 낸 매매는 오전 11시 38분
  단 한 건뿐이었고, 이 매매는 소폭(약 1만원) 이익으로 끝났습니다. 어제 저녁 배포한
  "재시작 후 자동매매가 계속 잠겨 있던 문제"의 수정은 오늘 정상 작동을 확인했습니다.
- 이 원인 불명 매매가 정확히 어디서 왔는지는 계좌 기록을 자세히 대조해야 알 수 있는데,
  장 마감 전에는 안전상 그 대조 작업(데이터베이스 조회)을 하지 않기로 돼 있어 장
  마감 후 점검으로 넘깁니다.
- 어제부터 열려 있던 결정 대기 항목(연속 손실 안전장치 재검토, 주간회의 안건 3건)은
  오늘도 그대로 열려 있습니다 — 새로 생긴 것은 아닙니다.
- **지금 당장 사람이 개입해야 할 위험 신호는 없습니다.** 다만 오전 손실 121만원의
  원인이 아직 확인되지 않은 채로 남아 있다는 점은 장 마감 후 반드시 확인이 필요합니다.

---

# 제2부. 장중 (intra) — 2026-09-01 09:00~12:30

## 병행 세션 확인 (자기 절을 쓰기 전에)

```
git --no-optional-locks branch --show-current                                → v9-dev (정상, MW0601)
git --no-optional-locks log --format="%ci %h %s" --since="2026-09-01 00:00"  → (당일 커밋 없음, 마지막 커밋 08-31 19:09:29 c5eddda)
ls -lt docs/정기점검/매일점검/                                                  → 09-01 산출물은 이 세션의 pre 리포트/증거뿐(병행 세션 없음)
```

`.git/index.lock` 없음(수집기 §2 자가점검 통과 — 이 실행이 락을 만들지 않았다).
당일 새 커밋·새 산출물 없음 — 병행 세션 없음, 이 절이 오늘 두 번째 기록이다.

미커밋 515건 중 **실질 변경 2건**(`dev_memory/DECISION_LOG.md`·`dev_memory/NEXT_TODO.md`,
장전 세션이 남긴 것) · 나머지는 EOL(줄바꿈 문자) 차이 511건 — 코드 변경 0건.

## 이월 처리표 — 장전이 남긴 항목을 전부 처분한다

| 번호 | 무엇을 | 처분 | 근거 |
|---|---|---|---|
| O-t2 | Restart Armistice 재현 여부 | (장전에서 이미 판정 완료) | 재처분 불요 — 08-31 507차 등록분, 오늘 장전이 "재현 안 됨"으로 종결 |
| **O-t3** | EOD 치유 실효 — `update_live` 예외 소멸 · `fp_psi` non-NULL | 🔄 **부분 판정** | 예외 축: `logs/20260901_*.log` 전체에서 `update_live` 문자열 **0건** — 소멸 확인. DB 축(`ensemble_decisions.fp_psi` non-NULL)은 **장중 라이브 DB 스캔 금지 원칙**(CLAUDE.md, 2026-08-10 CB⑤ 자가유발 전례)에 따라 조회하지 않았다 — **장후로 이월**. `[RegimeFingerprint] PSI=0.003 level=0 (heartbeat)`가 09:00·09:05·09:11·09:16에 반복 정상 출력된 것은 참고 정황일 뿐 DB 컬럼 확인을 대체하지 않는다 |
| O-t1 | net 대사 잔차의 지배 축 | 🔄 지속 | 정의상 장후 판정 대상(오늘 거래가 있어 이제 값이 생겼다) — DB 조회 필요, 장후로 |
| O-t4 | 15m 호라이즌 EOD 교체 보류 반복 | 🔄 지속 | 정의상 장후 판정 대상, 변동 없음 |
| **P-1** | 엔진 자동 진입 재개 여부 | ✅ **판정 완료 — 재개됨** | `logs/20260901_TRADE.log:116~122` — 11:38:01 `[진입체크] SHORT→SHORT 1계약 A급(원시C) … conf=38.7%` → `[Position] 진입 SHORT 1계약 @ 1063.96 … horizon=3m` → `[체결진입] SHORT 1계약 @ 1064.16` 순서로 **정상 3단 경로**가 오늘 처음 완주했다. 결과 `[Position] 체결청산 SHORT @ 1063.74 \| PnL=+0.42pt (+10,560원) \| 하드스톱(틱)`(11:39:59) |
| **P-2** | 외부 진입 재유입 여부 | ✅ **판정 완료 — 대규모 재유입 확인 → 아래 이상점 1-1로 등록** | 09:09~09:33 사이 `[진입체크]`/`[Position] 진입` 로그 없이 `[체결진입]`만 찍힌 포지션 **7건**(레그 12행), 순 손실 **-1,216,234원**. 상세는 이상점 1-1 |
| **O-p1**(오늘) | HealthPolicy Degraded 선제차단이 실제 진입을 막았는가 | ✅ **판정 완료 — 영향 없음(이중 차단)** | `logs/20260901_SIGNAL.log:216` — 같은 분(09:01:00) `[ZeroDiag] 진입X 원인: FLAT수렴 / conf미달(0.000<mc0.424)`. 신뢰도 자체가 0.000으로 이미 진입 임계(0.424)에 한참 못 미쳐, 09:01:00 `[HealthPolicy] Degraded 선제차단`(`logs/20260901_WARN.log:23`)이 없었어도 그 분은 어차피 차단됐다. **이중 차단이며 이 게이트 하나가 진입을 좌우한 사례는 아니다** |
| **O-p2**(오늘) | ConfFloorGuard 정상(OK) 복귀 시각 | ✅ **판정 완료** | `logs/20260901_SIGNAL.log` — 09:00:01 최초 차단 → **10:42:00 최초 복귀**(`하한 도달 가능 복구 — 출력상한 0.3809 ≥ 필요 0.3780`) → 10:55:00 재차단 → 11:13:00 재복귀. 하루 한 번의 복귀가 아니라 **오전 내내 2회 반복**된 패턴 |
| O-p2(08-31 이월) | `[CybosOrder] ret=` 코드 의미 | 🔄 지속(발생 시 판정) | `grep "CybosOrder\] ret=" logs/20260901_*.log` — 0건, 오늘도 미발생 |

**미처분 0건.**

## 신규 이상점

### 1-1 (P1) — 09:09~09:33 원인 불명 "외부" 진입 7건 · -1,216,234원, 그 여파로 19분간 자동진입 스스로 정지

**증상**: 오전 9시 9분부터 9시 33분 사이 24분 동안, 프로그램이 신호를 확인하고 승인하는
정상 절차(체크리스트 통과 → 진입 등록 → 체결)를 거치지 않은 매매 7건이 계좌에
체결됐다. 이 매매들은 합쳐서 -1,216,234원 손실을 냈고, 그 부작용으로 9시 24분부터
9시 43분까지 19분간 프로그램이 스스로 "상태가 나쁘다"고 판단해 자동매매를 걸지
않도록 자체 잠갔다.

**근거(로그 인용 + 시각)**:
- `logs/20260901_TRADE.log:6~105` — 09:09:04·09:20:27·09:20:28·09:22:03(×3)·09:25:03(×2)·
  09:28:00·09:32:24·09:33:32·09:33:44에 `[체결동기화] 외부진입` 12행. 같은 시각 `[Position]
  체결진입`은 있으나, **선행돼야 할 `[진입체크]`·`[Position] 진입` 로그가 이 12행 어디에도 없다**
  (11:38:01 엔진 진입 1건만 3단 로그가 완주했다 — 위 P-1 근거와 대조).
- `logs/20260901_WARN.log:30~278` — `[OrderSync] 미추적 체결 감지 (pending_miss)`가 같은
  시각에 order_no=462·700·746·752·845·955·1035·1052·1071·1075로 10회 발생. 주문번호가
  순차 증가하는 낮은 숫자대이며 프로그램이 스스로 낸 주문과 같은 채번 체계를 쓴다 —
  즉 **다른 프로세스가 아니라 같은 계좌에 꽂힌 정체불명 주문**일 가능성이 높다(단,
  발생원은 로그만으로 특정 불가 — DB·계좌 원장 대조가 필요, 아래 Fix 참조).
- `docs/정기점검/매일점검/evidence_MW0601-20260901_intra.md` §5 — 포지션 8건 중 **외부 7건
  -1,216,234원 vs 엔진 1건 +10,560원**으로 소계가 완전히 갈린다. 정합성 확인: 레그합
  -1,205,674원 = 포지션합 -1,205,674원 → OK(계측 4원칙① 포지션 단위로 집계, 레그 12행).
- `logs/20260901_HEALTH.log:1~27` — exceptions_10m(최근 10분 SYSTEM WARNING 이상 로그
  개수, `main.py:2739` `exception_density_10m`)가 09:21:00 4건 → 09:23:01 13건(CRITICAL
  진입) → 09:29:00 21건(최고)까지 치솟았다가, 외부진입 소동이 끝난 09:33:44 이후
  10분 뒤인 09:44:00에 1건으로 떨어지며 `degraded=OFF`로 정상 복귀했다 — **외부진입이
  쏟아낸 OrderSync·ExitCooldown·CB 경고 로그 더미가 그대로 exceptions_10m을 밀어올린
  것**으로 시간이 정확히 들어맞는다.
- `config/settings.py:HEALTH_DEGRADED_BLOCK_AUTO_ENTRY=True`(켜져 있어야 정상) — degraded=ON인
  09:24:00~09:43:00(19분) 동안은 이 설정에 따라 **엔진 자동진입 자체가 차단 대상**이었다.

**기준 위반**: 계측 4원칙 ②(미측정 ≠ 0) — 이 7건은 손익·수량은 체결 실측이라 정확하지만
`entry_horizon`·등급이 원천적으로 없어 "추정귀속"으로만 잡힌다. 절대원칙 §6(알파 리서치
봇 자동 통합 금지)과 직접 연결되는 증거는 없지만, 발생원이 로그만으로는 배제도 확정도
안 되므로 함께 조사 대상에 올린다.

⚠ **함정① 확인 결과 — 이것은 새로 발견한 문제가 아니라 이미 등록된 사안의 재발이다.**
`dev_memory/NEXT_TODO.md`에 **F-15(P2, 🔴 주간회의 안건) "외부 진입을 브로커 net 판정
경로에서 분리"**가 이미 등록돼 있고, `dev_memory/DECISION_LOG.md`(줄 34713 부근)에
과거 다른 거래일 "장중 외부진입분 13건 5승 38.5% -1,015,954원"이 기록돼 있다 —
**패턴 자체는 반복이지만, 오늘 손실 규모(-1,216,234원, 7건)와 자동진입 19분 차단이라는
파생 영향은 이번이 처음 구체적으로 확인된 사례**이므로 이상점으로 신규 등록하되
Fix는 F-15 안건에 합류시킨다.

**영향**: 오늘 오전 계좌에서 프로그램이 승인하지 않은 매매 7건이 체결돼 121만원
손실이 났고, 그 부작용으로 19분 동안 프로그램의 정상적인 자동매매 기능도 함께
멈췄습니다. 프로그램이 직접 낸 매매(1건, +1만원)와는 정반대의 결과입니다.

**영향(기술)**: 포지션 손익 외부 7건 -1,216,234원 vs 엔진 1건 +10,560원(소계 완전
분리). exceptions_10m 급증(4→21) → `HEALTH_DEGRADED_BLOCK_AUTO_ENTRY=True` 조건 충족
→ 09:24:00~09:43:00(19분) 자동진입 차단. `entry_horizon` 미기록으로 다운스트림
`[PositionFallback]` 12건(TP1 배수 폴백 1.00, 아래 1-2)도 동반 발생.

**표본 상태**: 오늘 1일 관측. 과거 유사 사례 1건(다른 거래일, 규모는 다름) 확인 —
**확정 결론 금지**(313차 원칙). 발생원(계좌 레벨 원인)은 로그만으로 특정 불가.

### 1-2 (P2) — PositionFallback: entry_horizon 미설정 → TP1 배수 폴백 1.00 (12건)

**증상**: 위 1-1의 외부진입 7건(레그 12행)이 체크리스트를 거치지 않아 `entry_horizon`이
설정되지 않았고, 그 결과 TP1(1차 목표가) 배수가 설계된 호라이즌별 값 대신 **폴백
1.00**(호라이즌별 설계값의 최대 2배)으로 적용됐다.

**근거**: `logs/20260901_TRADE.log:199` — `2026-09-01 09:09:04 [WARNING] TRADE:
[PositionFallback] entry_horizon 미설정 → TP1 배수 폴백 1.00 적용 (호라이즌별 설계값의
최대 2배). status=LONG qty=1 entry=1063.54 — 진입 경로가 파라미터를 넘기지 않았다(F-5 대상)`.
같은 태그가 09:09:04~09:33:44 사이 총 **12건**(`evidence_MW0601-20260901_intra.md` §4
TRADE.log WARNING 태그 집계).

**기준 위반**: CORE 피처 §3 관련 진입 파라미터 전달 규약(F-5 계열, `dev_memory/NEXT_TODO.md`
"F-5" 다건 중 이 코드 주석이 가리키는 항목은 세션 종료 전 특정 못함 — Fix에서 grep 필요).

**영향**: TP1(1차 이익실현) 목표가가 설계보다 최대 2배 멀리 잡혀, 실제로는 더 빨리
청산했어야 할 자리에서 계속 들고 있게 될 수 있습니다. 다만 이번 7건은 대부분 TP1
도달 전에 하드스톱으로 먼저 청산돼 실제 손익에 영향은 제한적이었습니다.

**영향(기술)**: `TP1 배수=1.00` 폴백 vs 설계값(호라이즌별 상이, ATR_HORIZON_TP1_MULT
1m=0.3/3m=0.5/5m=0.7) — 최대 2배 이격. 12행 전부 1-1과 같은 뿌리(외부진입의
`entry_horizon` 누락)이므로 **1-1의 원인이 해소되면 이 항목도 함께 해소된다**(별도
Fix 불필요, 진행상황만 추적).

**표본 상태**: 오늘 12건, 전량 1-1 파생. 확정 결론 금지 불필요(원인이 이미 1-1로 특정됨).

## Fix 작업 구현계획 (P0/P1/P2 — 장중이므로 계획만, 적용은 장후)

**장전·장중에는 코드 변경·커밋·배포·재기동을 하지 않는다(라이브 프로세스 운영 중).**
아래는 장후 적용을 전제로 한 계획이다.

- **P1 — 외부진입 7건(1-1)의 발생원 규명**: 장 마감 후 `data/db/predictions.db`·
  `data/db/trades.db`를 조회해 order_no(462·700·746·752·845·955·1035·1052·1071·1075)를
  `ensemble_decisions`·`trades.entry_source`와 대조한다. 이미 F-15 안건에 예정된
  "출처 필터"(`entry_source` 컬럼) 설계와 같은 작업이므로 **별도 신규 스크립트를 만들기
  전에 `grep -n "entry_source" main.py utils/db_utils.py`로 이미 있는 구현을 먼저
  확인**(함정①). 없다면 `dev_memory/NEXT_TODO.md`의 F-15 표(안 A/B/C)를 따라 장후
  세션에서 최소 진단 스크립트를 작성.
- **P2 — HealthPolicy degraded 진입 차단과 exceptions_10m 급증의 인과를 로그에 명시**:
  `main.py:_emit_runtime_health()`(2723~2799행)의 `[Health]` 로그 라인에 degraded 전환
  시 `auto_entry_blocked=True/False`를 함께 찍으면, 다음에 같은 상황이 나도 "자동진입이
  실제로 막혔는지"를 이번처럼 사후 재구성하지 않고 바로 알 수 있다. 계측 4원칙 ④(폴백
  가시화)와 같은 취지.
- **P2 — 1-2(PositionFallback)**: 1-1 원인 해소에 종속. 별도 코드 변경 불필요, 1-1 Fix
  적용 후 `[PositionFallback]` 재발 여부만 다음 거래일 확인.

## 고도화 방안 (오늘 관측 기반)

- 오늘 관측에서 드러난 것: **exceptions_10m 급증 → Health degraded → 자동진입 차단**의
  사슬이 실시간 로그만으로는 바로 안 보이고, 이번처럼 여러 로그 파일(HEALTH·WARN·TRADE)을
  시간축으로 맞춰 재구성해야 드러났다. 위 Fix P2(`auto_entry_blocked` 필드)가 그 재구성
  비용을 없앤다 — 실측 근거: 이번 재구성에 로그 3종 대조가 필요했다.
- `[OrderSync] 미추적 체결 감지`가 발생하는 시점에 **order_no의 출처를 즉시 태깅**(예:
  최근 5분 내 이 계정에서 나간 자체 주문 목록과 대조해 "자체 채번 범위 내인데 매칭
  안 됨" vs "채번 범위 밖" 구분)하면, 1-1 같은 사례를 장중에 바로 "엔진 자체 로직
  오류"와 "순수 외부 유입"으로 구분할 수 있다. 지금은 둘 다 "외부"로 뭉뚱그려진다
  (일반론이 아니라 오늘 order_no가 자체 채번 범위 안에 있었다는 관측에서 출발).

## 이 절이 발급한 번호

이 절이 발급한 번호: 이상점 1-1~1-2 · 관측 신규 없음(O-p1·O-p2 오늘분은 판정 완료로 종결,
O-t3는 부분 판정) · Fix 2건(P1×1, P2×2) · 고도화 2건.

---

## 사용자 조치 (누적 — 장전 항목 포함)

### 1. 오늘 오전 외부진입 손실 -121만원 원인 확인 — **소요: 장후 점검이 자동 수행(5분 확인만) · 오늘 장 마감 후**

프로그램이 승인하지 않은 매매 7건이 체결돼 121만원 손실이 났습니다. 원인 규명에는
장 마감 후 데이터베이스 조회가 필요해 장중에는 하지 않았습니다. 장후 점검이 이어서
확인합니다 — 결과가 나오면 확인해 주십시오.

**안 하면**: 같은 유형의 손실(과거에도 한 번 있었던 패턴)이 원인 불명인 채로 반복될
수 있습니다.

### 2. 연속 손실 시 매매를 멈추는 안전장치, 다시 켤지 아직 정해지지 않았습니다 — **소요 10분 · 이번 주 중** (장전에서 이월)

재검토 기한(8월 29일)이 3일 지났습니다. 지금은 하루 안에 손실이 반복돼도 프로그램이
스스로 멈추지 않습니다. 오늘도 "연속 손절 2회" 카운트까지 올라갔습니다(정지 없음,
정상 — 임계 자체가 사실상 무제한이기 때문).

**안 하면**: 결정 대기 상태가 계속 쌓입니다.

### 3. 주간회의에 올릴 안건 — 이제 4건입니다 (F-15 포함) — **소요 0분(기록만) · 다음 주간회의**

기존 3건(승률 기준 정의·외부 매매 손익 처리·모델 교체 지연 처리)에 더해, 오늘 손실로
**F-15(외부진입을 브로커 손익 판정에서 어떻게 분리할지)**가 실제 손실 사례와 함께
다시 확인됐습니다.

### 4. 커밋 — **소요 1분 · 오늘 아무 때나**

**커밋 대기 파일** (장전+장중 세션이 만들거나 수정):

```
docs/정기점검/매일점검/MW0601-20260901-점검리포트.md
docs/정기점검/매일점검/evidence_MW0601-20260901_pre.md
docs/정기점검/매일점검/evidence_MW0601-20260901_intra.md
dev_memory/DECISION_LOG.md
dev_memory/NEXT_TODO.md
```

⚠ **`git add .`를 쓰지 마십시오.** 미커밋 515건 중 실질 변경은 위 5개 파일뿐이고
나머지는 줄바꿈 문자(EOL) 차이입니다. 위 경로를 **명시해서** add 해 주십시오.

`.git/index.lock`은 남아 있지 않습니다(수집기 자가점검 통과).

---

*이 절이 발급한 번호: 이상점 1-1~1-2 · O-t2 재처분 불요 · O-t3 부분판정 · O-t1/O-t4 지속 ·
P-1/P-2 판정 완료 · O-p1/O-p2(오늘) 판정 완료 · O-p2(08-31) 지속.*

*장중 12:30 기준 작성. 장후가 이어 붙일 것이다.*

---

# 제2부-B. 장중 재점검 (13:40) — 13:22:05 이후 진입·청산 흐름 딥다이브

> 사용자 요청("13:22:05 이후 진입과 청산 흐름을 조사하고 이상점 딥다이브")에 따른
> 오늘 두 번째 장중 절이다. 앞 절(12:30)을 고쳐 쓰지 않고 이어 붙인다.

## 0-C. 13:40 갱신 — ⚠ **P0 (오늘 거래에 직접 위험)**

**판정: 이상 — 오후 1시 28분부터 2분 24초 동안, 손실을 끊는 안전장치가 실제로 작동하지
못했습니다.**

- 오후 1시 28분 9초, 가격이 미리 정해둔 손절선을 넘어서 프로그램이 "지금 즉시
  팔아서 손실을 끊어라"라는 주문을 냈습니다. **증권사가 그 주문을 거절했습니다.**
  거절 사유는 "모의투자 주문가능금액이 부족합니다"였습니다.
- 프로그램은 그 뒤 **163번 같은 주문을 다시 냈고, 163번 모두 거절당했습니다.**
  그동안 계약은 계속 손실 상태로 열려 있었습니다.
- 결국 그 계약을 닫은 것은 프로그램이 아니라, **1시 26분에 계좌에 이미 걸려 있던
  정체불명의 매도 주문**이 1시 30분 50초에 뒤늦게 체결되면서였습니다. 즉 오늘
  이 포지션은 **우연히** 닫혔습니다.
- **가장 중요한 점**: 손절을 막은 것과 똑같은 조건이 오후 3시 10분 강제청산에도
  그대로 적용됩니다. 코드를 확인한 결과 3시 10분 청산 주문도 거절당하면 **한 번
  시도하고 끝**이며, 3시 18분 최후 안전망도 같은 통로를 쓰기 때문에 함께 막힙니다.
  그렇게 되면 포지션을 밤새 들고 가게 되는데, 이것은 이 시스템에서 절대 허용하지
  않기로 한 첫 번째 원칙입니다.
- **지금(오후 1시 40분) 계좌는 매도 2계약을 들고 있고**, 값은 진입가보다 약 1포인트
  불리한 쪽(약 10만원 평가손실)에 있습니다. 다행히 지금은 청산 가능 수량이 막혀
  있지 않아 즉시 팔 수 있는 상태입니다.

**지금 사람이 해야 할 일이 하나 있습니다** — 대신증권 화면에서 **아직 체결되지 않고
걸려 있는 주문이 있는지 확인하고, 있다면 취소**해 주십시오. 그런 주문이 남아 있으면
프로그램의 손절과 3시 10분 강제청산이 오늘처럼 다시 막힙니다. 소요 1분.

---

## 병행 세션 확인 (자기 절을 쓰기 전에)

```
git --no-optional-locks branch --show-current                               → v9-dev (정상, MW0601)
git --no-optional-locks log --format="%ci %h %s" --since="2026-09-01 00:00" → 당일 커밋 0건 (마지막 c5eddda, 08-31 19:09)
ls -lt docs/정기점검/매일점검/                                                → 09-01 산출물 3개(리포트·pre·intra 증거) — 전부 오늘 이 스킬 세션 산출물
```

12:30 이후 새 커밋·새 산출물 **없음** — 병행 세션 없음. `.git/index.lock` 없음.
이 절은 코드를 변경하지 않았고 라이브 DB(`raw_data.db`·`predictions.db`·`trades.db`)를
조회하지 않았다(장중 금지 원칙). 근거는 **전부 로그·설정·프로세스 목록**이다.

## 이월 처리표 — 12:30 절이 남긴 항목의 처분

| 번호 | 무엇을 | 처분 | 근거 |
|---|---|---|---|
| **O-p2(08-31 이월)** | `[CybosOrder] ret=` 코드 의미 | ✅ **판정 완료 — 오늘 실물 발생** | 12:30 절의 "0건, 오늘도 미발생"은 **그 시점에는 정확했다**(첫 발생 13:28:09). 13:28:09~13:30:33에 `[CybosOrder] 주문 실패` **163건** 발생. 관측된 코드는 두 종류 — `ret=-1 status=-1 msg=94025모의투자 주문가능금액이 부족합니다` **142건**, `ret=4 status=0 msg=(빈 문자열)` **21건**. 상세는 이상점 1-3·1-5 |
| **1-1** | 원인 불명 외부 진입 | 🔄 **지속 · ⬆️ 확대** | 12:30 절은 09:09~09:33 **7건**으로 기록했다. 13시대에 더 유입돼 오늘 `[체결동기화] 외부진입` 레그가 **총 22행**(09시대 12행 + 13시대 10행: 13:03:07·13:05:34·13:09:08×2·13:09:49·13:18:31·13:22:05×2·13:31:40×2). 번호는 계승한다(새 번호를 붙이지 않는다) |
| **1-2** | PositionFallback (entry_horizon 미설정) | 🔄 **지속 · ⬆️ 확대** | 12:30 절 12건 → 13시대 외부진입 10레그마다 동반 발생해 누적 증가. 원인은 1-1 그대로 |
| O-t1 · O-t3(DB 축) · O-t4 | net 대사 · `fp_psi` non-NULL · 15m 교체 보류 | 🔄 지속(장후 판정) | 정의상 DB 조회가 필요하다 — 장중 금지 원칙에 따라 변동 없음 |

**미처분 0건.**

---

## 13:22:05 이후 진입·청산 흐름 — 시각별 재구성

| 시각 | 주문번호 | 무슨 일이 일어났나 | 출처 |
|---|---|---|---|
| 13:21:46~47 | 3739 | **엔진 정상 완주** — `[TickStop]` → `[PendingOrder] set` → `[ExitSendOrderResult] ret=0` → `[주문요청] 하드스톱(틱) 청산 SHORT 1계약 @ 1074.21` → 접수 → 체결 → `[청산 완료] PnL=-1.44pt (-82,525원)`. 이어 `[CB] 연속 손절 2회` | 엔진 |
| **13:22:05** | 3740 | LONG **2계약** 체결. `[진입체크]`·`[Position] 진입` 로그 **없음** → `[ChejanMatch] pending='NONE' pending_matched=False` → `[OrderSync] 미추적 체결 감지 (pending_miss)` ×2 → `[체결동기화] 외부진입` ×2. `[PositionFallback]` ×2 동반 | **외부** |
| 13:26:41 | **3752** | SHORT **2계약 접수**. `[ChejanFlow] … fill_qty=2 … position='LONG 2계약' pending='NONE'` — **접수만 되고 체결 안 됨(4분 9초 대기)**. 엔진은 시장가(`ORDER_HOGA_MARKET="2"`)만 내므로 이 대기성 주문은 엔진 것이 아니다 | **외부** |
| **13:28:09** | — | 틱 1073.00이 손절선 1073.03을 하향 이탈 → `[TickStop] 스톱 히트 감지` → `[PendingOrder] set EXIT_FULL LONG qty=2` → **`[CybosOrder] 주문 실패 ret=0 status=-1 msg=94025모의투자 주문가능금액이 부족합니다 side=매도 qty=2`** → `[PendingOrder] clear` → `[Exit] 하드스톱(틱) 주문 실패 ret=-1` | 엔진(거부) |
| 13:28:09~**13:30:33** | — | 위 사이클이 **163회 반복**. 분당 48 / 56 / 59건. 전부 `side=매도 qty=2`. 도중 13:29:05부터 `ret=4 status=0 msg=(빈값)`이 **21건** 섞여 나옴 | 엔진(거부) |
| **13:30:50~51** | 3752 | 4분 전 걸려 있던 외부 주문이 **뒤늦게 체결** → `[체결부분청산]` → `[Position] 체결청산 LONG @ 1074.2 \| 사유=미추적체결(pending_miss)` → `[청산 완료] 포지션 합계 -36,080원 (레그 2)` | **외부** |
| 13:31:40 | 3769 | SHORT **2계약** 체결 — 다시 `[진입체크]` 없이 외부진입 ×2, `[PositionFallback]` ×2. **현재 보유 포지션** | **외부** |
| 13:37(관측 시점) | — | 평균가 1073.71 / 현재가 1074.66~1074.80(평가손실 ≈ **-10만원**). `closable_qty=2`(선점 없음). `[PipePerf] total=326ms`(정상) | — |

**한 줄 요약**: 13:22:05 이후 진입은 **전부 외부**(3740·3769), 청산 중 **엔진이 성공한 것은
0건**이다. 13:21:47의 성공이 오늘 13시대 마지막 엔진 청산이었고, 그 뒤 유일한 청산은
외부 주문 3752가 우연히 채워 준 것이다.

### 손익 (계측 4원칙 ① — 포지션 단위, 엔진 net 기준)

| 구간 | 포지션 | 합계 |
|---|---|---|
| 오늘 전체(09:00~13:31) | 13건 | **-1,444,961원** |
| 13:00 이후 | 5건 | **-239,286원** |
| 엔진이 3단 경로를 완주한 진입 | **1건**(11:38:01) | +10,560원 |

⚠ **엔진 net이다** — 브로커 실측 net(`daily_broker_pnl.broker_net_krw`)이 아니다.
장중이라 DB를 열지 않았고, 수수료는 엔진 가정(`FUTURES_COMMISSION_RATE`, 이 PC는
CYBOS 채널 0.0098104%)을 쓴다. 최종 판정은 장후 브로커 실측으로 다시 낼 것.
평가손실(현재 보유 SHORT 2계약 약 -10만원)은 위 합계에 **포함돼 있지 않다**.

---

## 신규 이상점

### 1-3 (**P0**) — 13:28:09~13:30:33, 하드스톱 청산 주문 163회 전량 거부 · 2분 24초간 손절 무력화. **같은 결함이 15:10 강제청산에도 그대로 있다**

**증상**: 손절선을 이탈해 발동한 전량청산 주문이 증권사에서 연속 거부돼, 2분 24초 동안
포지션을 끊지 못했다. 포지션을 실제로 닫은 것은 엔진이 아니라 계좌에 걸려 있던
외부 주문이었다.

**근거(로그 인용 + 시각)**:
- `logs/20260901_WARN.log` 13:28:09 —
  `[ERROR] SYSTEM: [CybosOrder] 주문 실패 ret=0 status=-1 msg=94025모의투자 주문가능금액이 부족합니다. (ordfs.cstdvtvord) account=333044256 code=A0569 side=매도 qty=2`
  → 직후 `[ExitSendOrderResult] ret=-1 kind=하드스톱(틱) direction=LONG qty=2`
  → `[PendingOrder] clear` → `[Exit] 하드스톱(틱) 주문 실패 ret=-1`
- 집계(실측): `grep -c "CybosOrder] 주문 실패" logs/20260901_WARN.log` = **163**.
  분포 13:28 **48건** / 13:29 **56건** / 13:30 **59건**. 전량 `side=매도 qty=2`.
  코드 내역 — `ret=-1`(94025) **142건**, `ret=4`(status=0·msg 빈값) **21건**.
- 대조군: **같은 날 같은 방향(매도) 청산이 정상 전송된 적이 있다** —
  `[ExitSendOrderResult] ret=0 kind=하드스톱(틱) direction=LONG qty=1`(09:10:49·09:33:09).
  ⇒ "매도라서 거부된다"가 아니다.
- `94025`는 **오늘 처음 나온 코드**다 — `grep -l "94025" logs/*_WARN.log` = `20260901_WARN.log` **단 하나**.

**기준 위반**: **절대원칙 §1(오버나이트 금지 — 15:10 강제청산)의 집행 경로**와
매분 9단계 **STEP 8(청산 트리거 감시)**. 오늘 실제로 무력화된 것은 하드스톱이지만,
아래에서 보듯 **동일 코드 형태가 15:10 경로에도 있다**.

**원인 가설(로그 정합 · 미확정)**: 13:26:41에 접수된 외부 매도 2계약(3752)이
**청산가능수량 2를 전량 선점**해, 13:28:09의 엔진 매도 2계약이 브로커에게는
*신규 매도*로 보여 증거금을 요구했고 → 모의계좌 주문가능금액 부족(94025).
정합 근거 4가지:
① 실패 **시작**(13:28:09)이 3752 **접수**(13:26:41) 뒤다.
② 실패 **종료**(13:30:33)가 3752 **체결**(13:30:50) 직전이다.
③ 같은 날 매도 청산이 ret=0으로 나간 적이 있다(위 대조군) — 방향 문제가 아니다.
④ 오늘 실현손실은 -144만원으로 모의계좌 규모 대비 미미하다 — 단순 잔고 고갈이 아니다.
🔴 **확정 결론 금지**(313차 원칙). 브로커 원장의 *주문가능금액·청산가능수량 시계열*을
직접 보지 않았다. 확정은 장후 `CpTd6197`·`trades.db` 대조로 한다.

🔴 **전방 위험 — 15:10 강제청산도 같은 형태다(코드 실측)**:
`main.py:15033` 부근 시간청산 경로가
```
ret = self._send_broker_exit_order(_force_qty)
… if ret == 0: [주문요청] 로그
   else:       self._clear_pending_order(); [Exit] 시간청산 주문 실패 ret=… (ERROR)
```
로 **한 번 시도하고 끝난다** — 재시도·수량 축소·미체결 주문 취소·에스컬레이션이 없다.
> ↩️ 14:12 정정 — 이 "한 번"은 **분봉 패스 단위**다(15:10~15:18 약 8회 재시도).
> P0 판정은 유지된다. 부록-B 참조.
15:18 `FINAL_CLOSE` 안전망(`_ts_broker_direct_force_exit`)도 **같은 `send_order` 통로**를
쓰므로 같은 사유로 함께 거부된다.
⚠ 진입 쪽에는 증거금 가드가 **이미 있다** — `main.py:17869 _apply_margin_cap()` /
`[MarginBlock] … 신규주문가능수량=0 — 증거금 부족 추정, 진입 차단`. **청산 쪽에만 없다.**
즉 이것은 "만들어야 할 새 개념"이 아니라 **비대칭으로 빠진 자리**다.

**영향**: 손실을 자동으로 끊어 주는 장치가 2분 24초 동안 실제로 작동하지 않았습니다.
오늘은 우연히 다른 주문이 대신 닫아 줘서 손실이 커지지 않았지만, 같은 상황이 오후
3시 10분에 벌어지면 계약을 밤새 들고 가게 됩니다. 그것은 이 시스템에서 절대 허용하지
않기로 한 규칙입니다.

**영향(기술)**: 하드스톱(틱) 경로 무력화 144초. 그 사이 포지션 LONG 2계약이
1073.03(스톱) → 최저 1072.28까지 열려 있었다(최대 추가 이격 **0.75pt ≈ 계약당 약
3.75만원**). 실제 청산은 외부 3752가 1074.20에 채워 결과적으로 스톱보다 유리했으나
**설계된 통제의 결과가 아니다**. 15:10/15:18 경로에 동일 형태가 존재해
절대원칙 §1 위반 가능성이 열려 있다.

**표본 상태**: 오늘 1일 · 사건 1건(거부 163회). `94025` 과거 발생 0일.
**확정 결론 금지** — 재현 조건(외부 미체결 주문이 청산가능수량을 선점)이 성립해야 한다.

---

### 1-4 (P1) — 청산 주문 실패가 TRADE 로그에 **한 줄도 남지 않는다** (계측 4원칙 ③·④)

**증상**: 운영자가 보는 `TRADE` 로그에는 `[TickStop-S0C] … → 주문 전송` 이 **167줄**
찍혔고, 그중 **163건이 실패했다는 사실은 어디에도 없다.** 실패는 `SYSTEM` 로거로만
나가 `WARN.log`에 들어간다. 사용자가 이 세션에 붙여 온 로그가 정확히 그 화면이다 —
"주문 전송"만 수십 줄 반복되고 무슨 일이 일어났는지 알 수 없다.

**근거**: `main.py:4536` 부근 —
```
log_manager.trade(f"[TickStop-S0C] … → 주문 전송")   ← 전송 "전"에 TRADE로 기록
ret = self._send_broker_exit_order(_tk_qty)
log_manager.system(f"[ExitSendOrderResult] ret={ret} …", "WARNING")   ← 결과는 SYSTEM
if ret == 0: log_manager.trade("[주문요청] … 체결대기")               ← 성공만 TRADE
else:        log_manager.system("[Exit] … 주문 실패", "ERROR")        ← 실패는 SYSTEM
```
실측 대조: `grep "\[주문요청\]" logs/20260901_TRADE.log` = **9건**(전부 성공분),
`grep -c "TickStop-S0C" logs/20260901_TRADE.log` = **167건**.

**기준 위반**: 계측 4원칙 **③ 탈락 가시화**(제외 개수·사유를 남긴다) ·
**④ 폴백 가시화**(기본값/실패가 정상처럼 보이면 안 된다). "→ 주문 전송"은 **의도**이지
**결과**가 아닌데 결과처럼 읽힌다.

**영향**: 손절이 안 나갔는데 화면에는 "주문 전송"만 계속 찍혀서, 사람이 로그를 봐도
문제가 생긴 줄 모릅니다. 오늘도 사용자가 직접 이상하다고 느껴서 물어보기 전까지
드러나지 않았습니다.

**영향(기술)**: TRADE 채널 단독 관측 시 실패율 163/167 = **97.6%가 은닉**된다.
1-3의 발견 지연 요인이며, 향후 EOD 사후검증에서도 TRADE 기반 집계는 이 실패를
못 센다.

**표본 상태**: 코드 형태로 확정 — 표본 논의 불필요.

---

### 1-5 (P1) — 재시도 상한·백오프가 없어, 실패가 **스스로 브로커 요청 제한을 유발**했다

**증상**: 거부된 뒤에도 틱이 손절선을 넘을 때마다 무제한 재발동해 144초에 163회를
쐈고, 13:29:05부터 반환코드가 `ret=-1`(증거금)에서 **`ret=4 status=0 msg=(빈값)`**
으로 21건 바뀌었다.

**근거**: `logs/20260901_WARN.log` 13:29:05 —
`[CybosOrder] 주문 실패 ret=4 status=0 msg= account=… side=매도 qty=2`.
`ret=4`는 13:29:05~13:30:19 구간에만 나타나며 `ret=-1`과 뒤섞인다.
멱등 가드는 있으나(`_tick_stop_triggered`를 맨 위에서 즉시 클리어) **실패 후 재무장을
막지 않는다** — `_process_tick_stop()`의 else 분기는 `_clear_pending_order()`만 하고
쿨다운을 걸지 않는다(`main.py:4560` 부근).

⚠ **`ret=4`의 의미는 이 저장소에 문서화돼 있지 않다.** `docs/CyBos ref/`·코드 전체에
BlockRequest 반환코드 매핑이 없다(실측 grep 0건). Cybos 규약상 4는 *주문 요청 제한
초과*로 알려져 있으나 **이 저장소의 근거로는 확정 금지** — 장후에 대신 API 문서로
확인해 `api_connector.py`에 매핑 상수를 넣을 것(O-p2 후속).

**기준 위반**: 계측 4원칙 ④(폴백·실패 가시화)의 운영 확장. 안전장치의 재시도가
안전장치 자신의 통로를 막았다.

**영향**: 주문이 거절당했는데도 프로그램이 쉬지 않고 같은 주문을 계속 던져서,
증권사 쪽에서 아예 요청을 받아 주지 않는 상태까지 갔습니다. 상황을 스스로 더
나쁘게 만든 셈입니다.

**영향(기술)**: 163회/144초 = 평균 **1.13회/초**. `ret=4` 21건은 이 폭주가 만든
2차 실패로 추정된다(발생 구간이 폭주 구간 안에만 있다). 실패 처리 경로에
백오프·상한·대체수단이 없다.

**표본 상태**: 오늘 1건. `ret=4` 해석은 **미확정**(위 ⚠).

---

## Fix 작업 구현계획 (장중이므로 **계획만** — 라이브 프로세스 운영 중, 코드 변경·커밋·재기동 없음)

### P0-1. 청산 주문 실패에 **단계적 대응**을 붙인다 (1-3)

**변경 대상**

| 파일·함수 | 지금 | 바꿀 것 |
|---|---|---|
| `main.py:4553` `_process_tick_stop()` else 분기 | `clear_pending` + ERROR 1줄 | 실패 핸들러 호출 |
| `main.py:15033` 부근 시간청산(15:10) else 분기 | 동일 | 동일 핸들러 |
| `main.py` `_ts_broker_direct_force_exit()`(15:18) | 동일 통로 | 동일 핸들러 |
| `collection/cybos/api_connector.py:860` | ret/status만 반환 | 거부코드(94025 등)를 **구조화 반환** |

**핸들러 단계**(위험도 낮은 것부터):

1. **기록** — `log_manager.trade()`로 실패를 TRADE에도 남긴다(1-4와 공통).
2. **수량 축소 재시도** — `closable_qty`(이미 `[ChejanFlow]`로 매 체결마다 들어온다)를
   상태로 보관하고, 그 수량으로 재시도. 0이면 3단계로.
3. **백오프 + 상한** — 지수 백오프(1s/2s/4s), 상한 3회. 상한 도달 시 4단계.
4. **미체결 주문 취소 후 재시도** — `CYBOS_FUTURES_CANCEL_PROGID = "CpTrade.CpTd6833"`
   가 이미 정의돼 있다(`api_connector.py:88`).
   🔴 **여기는 사용자 승인이 필요하다** — 취소 대상이 사용자의 수동 주문일 수 있다.
   자동조치 C등급(주문·청산 실행 경로)에 해당하므로 **주간회의/사용자 결정 사항**으로
   올리고, 승인 전에는 3단계까지만 구현한다.
5. **경보** — 상한 초과 시 대시보드 "경보" 탭 + 프로세스 밖에도 남긴다
   (F-2 가드 `scripts/force_flat_guard.py`, 15:12 FLAT 가드 — **알림 전용, 주문 없음**).

**회귀 위험**: 재시도가 중복 체결을 만들 수 있다. → pending 선등록 순서(305차 2026-07-09
유령포지션 사고 재발방지 규약)를 **깨지 않는다**. 재시도는 `[PendingOrder] clear` **뒤**
새 pending을 등록하는 형태로만 한다.
**검증**: `tests/`에 실패 주입 테스트 추가(`send_order`가 -1/4를 반환하도록 스텁) —
① 상한 3회 후 멈추는가 ② 실패가 TRADE에 남는가 ③ pending이 새지 않는가.
**절대원칙 4**: 취소·재주문은 **COM 콜백 밖**에서만(`dynamicCall`·`emit` 금지).

### P1-1. 실패를 TRADE 로그에 남긴다 (1-4) — **가장 싸고 즉시 효과**

`main.py:4553`·`14585`·`14628`·`14896`·`15033`의 else 분기 5곳에
`log_manager.trade(f"[주문실패] {kind} 청산 {direction} {qty}계약 ret={ret} 사유={msg}")`
한 줄씩. `msg`는 `api_connector._last_order_error`에 **이미 보관돼 있다**(`:860` 부근).
**회귀 위험 거의 없음**(로그 추가). **검증**: 실패 주입 시 TRADE에 1줄이 뜨는지.

### P1-2. `[TickStop-S0C] … → 주문 전송`을 **결과 기준 문구로** 바꾼다 (1-4)

전송 **전** 로그는 `→ 주문 시도`로, 성공 시에만 기존 `[주문요청] … 체결대기`.
**회귀 위험**: 기존 리포트·집계가 `→ 주문 전송` 문자열을 세고 있으면 깨진다 →
`grep -rn "주문 전송" scripts/ docs/` 로 소비처를 먼저 확인할 것.

### P2-1. BlockRequest 반환코드 매핑 상수 (1-5 · O-p2)

`api_connector.py`에 `ORDER_RET_MEANING = {0:…, 1:…, 2:…, 3:…, 4:…}` 신설 +
로그에 의미 병기. **선행**: 대신 API 공식 문서 확인(이 저장소에 근거 없음).

---

## 고도화 방안 (오늘 관측 기반 — 일반론 아님)

**① `closable_qty`를 상태로 승격한다 (계측 4원칙 ⑤ — 모든 축을 걸어라)**
`[ChejanFlow]`가 `closable_qty`를 **이미 매 체결마다 실어 보내고 있다**(오늘 실측:
13:22:05 `closable_qty=1`→`2`, 13:26:41 `closable_qty=0`, 13:31:40 `0`→`1`→`2`).
그런데 이 값을 **아무도 보관하지 않는다.** 보관만 하면 스톱 발동 **전에**
"지금 청산 가능한가"를 알 수 있고, 0이면 즉시 경보를 낼 수 있다.
— FP-CRITICAL 죽은 게이트·TOX 죽은 섀도·CpTd6197 예탁현금과 **정확히 같은 계열**이다:
*없던 데이터가 아니라 아무도 안 본 데이터.*
· **근거**: 위 실측 3개 시각. · **변경대상**: `main.py` Chejan 처리부 + `ensemble_decisions`
컬럼 1개. · **기대효과**: 1-3을 사전 탐지. · **회귀위험**: 없음(읽기·저장만).
· **검증**: 추가 후 1주 적재율 100% 확인. · **표본상태**: 즉시 적립 시작(매 체결).

**② 외부 주문 실시간 감시 채널**
오늘 외부 진입 22레그·외부 접수 1건(3752)이 모두 `pending_matched=False`로 들어왔다.
이 신호는 이미 100% 검출된다 — 다만 **사후 동기화만 하고 경보를 안 낸다.**
"외부 미체결 주문이 청산가능수량을 선점 중"이라는 상태를 대시보드에 띄우는 것만으로
1-3의 재현을 사람이 막을 수 있다.
· **근거**: 오늘 `[ChejanMatch] pending_matched=False` 전량. · **변경대상**: 대시보드 경보 탭.
· **기대효과**: 15:10 강제청산 차단 위험의 조기 경보. · **회귀위험**: 없음(표시만).
· **검증**: 13:26:41~13:30:50 구간 재생 시 경보 출현. · **표본상태**: 오늘 1건 —
**확정 결론 금지**, 우선 섀도 관측으로.

**③ 15:10 리허설(F-1R)의 판정 기준에 "주문 거부"를 넣는다**
현행 F-1R(26주 WFA 항목)은 `[ForceExitPass]`→`[TimeExit]`→`[ExitAttempt]` 출현과
15:11 ERROR 미출현으로 판정한다. 오늘 사례는 **`[ExitAttempt]`는 뜨고 주문만 거부되는**
경우가 있음을 보여준다 — 판정 기준에 `[ExitSendOrderResult] ret==0`을 **명시 추가**할 것.
· **근거**: 오늘 13:28 사례. · **변경대상**: `dev_memory/NEXT_TODO.md` F-1R 항목 + CLAUDE.md
26주 재검증 절. · **기대효과**: 리허설이 오늘 같은 실패를 통과시키지 않는다.
· **회귀위험**: 없음(문서). · **검증**: 문구 반영. · **표본상태**: 해당 없음(판정 규칙 변경).

---

## 관측 등록 (장후가 판정·보류·이월 중 하나로 닫는다)

| 번호 | 무엇을 볼 것인가 | 닫는 조건 |
|---|---|---|
| **O-i1** | 현재 SHORT 2계약의 하드스톱이 발동할 때 94025가 **재현**되는가 | 오늘 장 마감까지 `[CybosOrder] 주문 실패` 추가 발생 여부 |
| **O-i2** | **15:10 강제청산이 실제로 체결되는가** — 오늘은 이 경로를 시험할 조건이 우연히 만들어졌다 | `[ExitSendOrderResult] ret=0 kind=시간청산` 출현 + `[FinalClose]` 미출현 |
| **O-i3** | 원인 가설(외부 미체결 주문의 청산가능수량 선점) 확정 | 장후 `trades.db`·`CpTd6197` 대조 — 13:26:41~13:30:50 구간의 주문가능금액·청산가능수량 |
| **O-i4** | 외부 주문 22레그의 **발생원** | 배제 완료: 미륵이 2중 기동 아님(`futures\main.py` 프로세스 **1개**, PID 17924) · 메시아 G2 아님(`fuoption/logs/g2_daily_20260901.log` 전 사이클 `DecisionEmitted … side="NO_TRADE"`, 페이퍼 운영). **남은 후보: HTS 수동주문 / 다른 기기 접속** — 사용자 확인 필요 |
| **O-i5** | `[BalanceUI]` summary 키 매핑 의심 — `'총평가': '-3.05'`(백분율로 보임) vs `'총평가수익률': '42158121'`(금액으로 보임), `'추정자산': '-5906000'`(전일손익 gross와 동일값) | 장후 `dashboard/main_dashboard.py` 매핑 코드 확인. **버그로 단정하지 않는다** — 의도적 재사용일 수 있다 |

---

## 이 절이 발급한 번호

이상점 **1-3~1-5** · 관측 **O-i1~O-i5**.
1-1·1-2는 **번호를 계승**했다(신규 발급 아님 — 같은 뿌리의 확대).
O-p2(08-31 이월)는 **판정 완료**로 닫았다.

---

## 사용자 조치 (13:40 시점 — 앞 절 항목 포함, 급한 순)

### 1. 🔴 **지금** 대신증권 화면에서 미체결 주문 확인·취소 — **소요 1분 · 지금 즉시**

- **지금 할 일**: 대신증권(CYBOS/HTS) 주문 화면에서 **아직 체결되지 않고 걸려 있는
  주문**이 있는지 보고, 있으면 취소해 주십시오. 특히 미니선물(A0569) 매도/매수 주문.
- **걸리는 시간**: 1분.
- **안 하면 생기는 일**: 그 주문이 "청산 가능 수량"을 선점하고 있으면, 오늘 오후 1시
  28분에 일어난 것과 똑같이 **미륵이의 손절 주문이 다시 거절**됩니다. 더 중요한 것은
  **오후 3시 10분 강제청산도 같은 이유로 막힐 수 있다**는 점이고, 그러면 계약을 밤새
  들고 가게 됩니다(절대 금지 규칙).

### 2. 🔴 오후 3시 10분 전후 계좌를 한 번 봐 주십시오 — **소요 2분 · 15:08~15:12**

- **지금 할 일**: 3시 10분에 포지션이 실제로 0으로 정리되는지 확인. 3시 12분에도
  계약이 남아 있으면 **수동으로 청산**해 주십시오.
- **걸리는 시간**: 2분.
- **안 하면 생기는 일**: 오늘 확인된 결함(청산 주문 1회 시도 후 포기)이 실현되면
  오버나이트 포지션이 됩니다. 프로그램 밖 감시(`force_flat_guard.py`, 3시 12분)는
  **알림만 하고 주문은 내지 않습니다** — 사람이 손으로 닫아야 합니다.

### 3. 오늘 계좌에 들어온 정체불명 매매가 직접 하신 것인지 알려 주십시오 — **소요 1분 · 오늘 중**

- **지금 할 일**: 09:09~13:31 사이 계좌에서 미니선물을 직접 매매하셨는지 알려 주십시오.
- **걸리는 시간**: 1분(답만 주시면 됩니다).
- **안 하면 생기는 일**: 직접 하신 것이면 원인 조사를 여기서 끝낼 수 있습니다.
  아니라면 **계좌 접속 이력 확인이 필요**합니다(다른 기기에서 같은 모의계좌에
  접속했을 가능성). 프로그램 쪽 후보는 이미 배제했습니다 —
  미륵이는 한 개만 돌고 있고(PID 17924), 옵션 프로젝트(메시아 G2)는 오늘 하루 종일
  "거래 안 함"만 냈으며 애초에 페이퍼 운영입니다.

### 4. 연속 손실 시 매매를 멈추는 안전장치 재검토 — **소요 10분 · 이번 주 중** (장전·12:30에서 이월)

변동 없음. 앞 절 참조.

### 5. 주간회의 안건 — **이제 5건입니다** — **소요 0분(기록만) · 다음 주간회의**

기존 4건(F-15 포함) + **신규 1건**:
> 🔴 **청산 주문이 거부됐을 때 미체결 주문을 자동 취소해도 되는가**
> (P0-1 4단계 · `dev_memory/NEXT_TODO.md` **F-18**, 본체 구현은 **F-17**). 취소 대상이 사용자의 수동 주문일 수 있어 자동조치 C등급에 해당한다.
> 승인 전에는 3단계(백오프·수량축소·경보)까지만 구현한다.

### 6. 커밋 — **소요 1분 · 오늘 아무 때나**

```
docs/정기점검/매일점검/MW0601-20260901-점검리포트.md
docs/정기점검/매일점검/evidence_MW0601-20260901_pre.md
docs/정기점검/매일점검/evidence_MW0601-20260901_intra.md
dev_memory/DECISION_LOG.md
dev_memory/NEXT_TODO.md
```

⚠ **`git add .`를 쓰지 마십시오** — 미커밋 다수는 줄바꿈 문자(EOL) 차이입니다.
위 경로를 **명시해서** add 해 주십시오. `.git/index.lock` 없음.

---

*장중 13:40 기준 작성(재점검). 장후가 이어 붙여 하루를 닫을 것이다.*

---

## 부록 — 13:50 실시간 후속 (이 절 작성 중 관측)

리포트를 쓰는 사이에도 **외부 매매가 계속되고 있다.** 13:37(본문 관측 시점) 이후 실측:

| 시각 | 주문번호 | 사건 | 출처 |
|---|---|---|---|
| **13:39:17~18** | **3816** | **엔진 하드스톱 정상 완주** — `[주문요청] 하드스톱(틱) 청산 SHORT 2계약 @ 1075.09 체결대기` → 접수 → 부분청산 → `[청산 완료] PnL=-1.36pt (-157,067원) (레그 2)` | **엔진** |
| 13:41:14~15 | 3835 | 외부진입 LONG 2계약 @ 평균 1073.49 (`[PositionFallback]` 동반) | 외부 |
| 13:43:26~27 | 3868 | 외부진입 LONG 1계약 추가 → **보유 3계약** @ 1073.4667 | 외부 |
| 13:44:40~43 | 3883 | 외부 SHORT 3계약으로 전량 청산 → `[청산 완료] 포지션 합계 -38,593원 (레그 3)` | 외부 |

**O-i1 — 재현되지 않았다(부분 판정).** 13:39:17의 엔진 하드스톱은 **정상 전송·체결**됐고,
13:30:33 이후 주문 거부는 **0건**이다
(`awk '$2>"13:30:33"' logs/20260901_WARN.log | grep -c "CybosOrder] 주문 실패"` = 0).
⇒ **원인 가설을 지지하는 방향이다** — 그 시점에는 청산가능수량을 선점한 외부 미체결
주문이 없었고, 그러자 같은 경로가 문제없이 나갔다. 단 **확정은 아니다**(장후 O-i3).

- **13:44:43 시점 포지션 FLAT**(13:49 확인). 13:44:43 이후 신규 진입 없음.
- 매매 양상(수 분 단위 진입→부분청산 반복, 접수 후 대기하는 지정가성 주문)은
  **엔진의 행동 패턴이 아니다**(엔진은 시장가 전량 + 체크리스트 3단 로그 완주).
  O-i4의 "HTS 수동주문 / 타 기기 접속" 후보를 더 지지한다 — **확정은 사용자 확인 후**.
- **갱신 손익(엔진 net · 포지션 단위)**: 오늘 **15포지션 -1,640,621원**(13:44:43까지 실현).
  본문 손익표의 "13건 -1,444,961원"은 **13:31 시점 값**이며 그대로 둔다(이월 규약).

---

## 부록-B — Fix 구현 결과 (14:12, 사용자 지시로 장중 구현)

> 사용자가 "fix 제안하고 구현계획 수립하고 구현 진행해"라고 지시해 **장중에 코드를
> 변경했다.** 스킬 규약(§5-1 "장전·장중에는 코드 변경을 제안만 하고 실행하지 않는다")의
> 예외이며, 사용자 지시가 근거다. 다만 그 규약이 막으려던 위험은 그대로 남으므로
> 아래 「배포 상태」를 반드시 읽을 것.

### ⚠ 배포 상태 — **오늘 15:10에는 이 수정이 적용되지 않는다**

| | 상태 |
|---|---|
| 파일 변경 | ✅ 완료 (`main.py` +259/−3 · `config/settings.py` +44 · 새 테스트 1개) |
| 라이브 프로세스(PID 17924) | ❌ **구코드로 계속 돌고 있다** — 파이썬은 실행 중 소스를 다시 읽지 않는다 |
| 재기동 | ❌ 하지 않았다 (장중 재기동은 포지션·상태 복원 리스크) |
| 커밋 | ❌ 하지 않았다 (사용자 확인 후) |

⇒ **오늘 오후 3시 10분 강제청산은 여전히 구코드로 실행된다.** 「사용자 조치 2번」
(15:08~15:12 계좌 확인)은 **그대로 유효하며 반드시 필요하다.**
⇒ 다만 장중에 프로세스가 죽어 런처가 재기동하면 그때부터 신코드가 뜬다 — 그래서
아래 「회귀 안전성」을 먼저 확보했다(테스트 42개 전부 통과, 기존 관련 테스트 5종 무회귀).

### ↩️ 본문 이상점 1-3 정정 — "한 번 시도하고 끝난다"는 **분봉 패스 단위**다

본문 1-3의 전방 위험 문단에서 15:10 경로를 *"한 번 시도하고 끝난다"* 라고 썼다.
**코드 블록 안에서는 맞지만, 운영 단위로는 정확하지 않다** — `should_force_exit()`는
15:10 이후 계속 True이고 일회성 완료 플래그가 없어, **매 분봉 패스마다 재시도된다**
(15:10~15:18 사이 약 8회). 471차 F-1이 그 도달성을 복구해 뒀고
`tests/test_471_force_exit_reachability.py`가 고정한다.

**그래도 P0 판정은 유지된다** — 근거가 재시도 횟수가 아니기 때문이다:
① 8회가 **모두 같은 사유로 거부**되면(오늘 163회 연속 거부가 그 사유의 지속성을
보여준다) 15:18 `FINAL_CLOSE`도 **같은 `send_order` 통로**라 함께 막힌다.
② 그 8회 실패가 **TRADE 로그에 한 줄도 안 남아** 사람이 알 수 없다.
③ 진입 쪽에는 증거금 가드가 있는데 청산 쪽에만 없다는 비대칭은 그대로다.

### 구현 범위 — **무엇을 했고 무엇을 일부러 안 했는가**

| ID | 내용 | 등급 | 이번 처리 |
|---|---|---|---|
| **F-19** | 청산 주문 실패를 **TRADE 채널에** 기록 (사유 원문·ret 의미·청산가능수량·연속 횟수) | 로그 추가 | ✅ **배선** |
| **F-21** | 실패 후 **백오프 + 연속 카운터 + 경보 1회** | 동작 변경(주문 빈도 억제) | ✅ **배선** |
| **F-22** | `EXIT_ORDER_RET_MEANING` — ret 의미 병기 | 로그 전용 | ✅ **배선**(미검증 표기) |
| **G-6** | `closable_qty` 스냅샷 상태 승격 | 읽기·저장만 | ✅ **배선** |
| F-17 ② | **수량 축소 재시도** | 🔴 청산 실행 경로 = 자동조치 C등급 | ⛔ **섀도만**(`[ExitRejectShadow]`) |
| F-18 | **미체결 주문 자동 취소** | 🔴 사용자 수동주문을 취소할 수 있음 | ⛔ **주간회의 안건** |
| F-20 | `→ 주문 전송` 문구 변경 | 과거 리포트 대조 문자열 | ⛔ **보류**(아래 사유) |

**왜 F-17 ②를 섀도로 남겼는가** — 실측이 반대 근거를 준다.
오늘 13:26:41 주문접수 Chejan은 `closable_qty=0`인데 **실제 보유는 LONG 2계약**이었다.
그 값을 믿고 수량을 줄이면 **과소청산**이 된다 — 손절이 절반만 나가는 것은
안 나가는 것 다음으로 나쁘다. 그래서 잔고 Chejan(balance 행)에서만 값을 받아
보관하고, 줄일 수량 후보를 **로그로만** 남긴다. 정합성이 확인되면 승격한다
(SKILL.md §7 "게이트·차단 로직 신설은 섀도 계측을 거친 뒤 승격").

**왜 F-20을 보류했는가** — F-19가 문제를 이미 해소한다. `→ 주문 전송` 바로 뒤에
`[주문실패] …`가 붙으므로 더 이상 결과를 오해할 수 없다. 반면 문자열을 바꾸면
과거 점검 리포트·증거 다이제스트의 대조 문자열(실측 8개 문서에서 인용 중)과
어긋나 **이력 비교가 깨진다.** 이득보다 비용이 크다.

### 변경 상세

**`config/settings.py` (+44)** — 상수 5종 + 사건 경위 주석

| 상수 | 값 | 뜻 |
|---|---|---|
| `EXIT_REJECT_BACKOFF_SEC` | `[1.0, 2.0, 4.0, 8.0]` | 연속 거부 n회째 다음 시도까지 대기. 끝값 유지 |
| `EXIT_REJECT_WINDOW_SEC` | `60.0` | 이보다 오래 조용하면 연속 카운터 리셋 |
| `EXIT_REJECT_ALERT_STREAK` | `3` | 연속 3회 도달 시 경보 **1회** |
| `EXIT_REJECT_QTY_REDUCE_SHADOW` | `True` | F-17 ② 섀도 기록 on/off (**실적용 아님**) |
| `EXIT_CLOSABLE_QTY_FRESH_SEC` | `30.0` | 이보다 낡은 `closable_qty`는 **미측정** 취급 |

8초 상한 = 분당 약 7회 재시도. 오늘 실측 **1.13회/초(163회/144초)를 약 1/9로** 줄이면서도
"성공할 수 있는 시도"는 8초 안에 반드시 돌아온다. **영구 차단은 없다.**

**`main.py` (+259/−3)**

1. `TradingSystem.__init__` — 상태 7개 **명시 초기화**
   (`_exit_reject_streak`·`_kind`·`_last_at`·`_alerted`, `_exit_retry_block_until`,
   `_broker_closable_qty`, `_broker_closable_qty_at`).
   `_broker_closable_qty`의 초기값은 `0`이 아니라 **`None`** 이다 — 계측 4원칙 ②
   (미측정 ≠ 0). 계측 4원칙 ④에 따라 `getattr(self, "_x", default)` 폴백을 쓰지 않으며,
   `tests/test_457_fallback_visibility.py`가 이를 전수 검사한다(재실행 7 passed).
2. 모듈 전역 헬퍼 6개 — `_ts_exit_ret_text` · `_ts_last_order_error_msg` ·
   `_ts_closable_qty_snapshot` · `_ts_exit_retry_block_remaining` ·
   `_ts_reset_exit_reject_state` · **`_ts_on_exit_order_reject`**(공통 후처리).
3. `_send_broker_exit_order(qty, *, throttle=True)` —
   ⓐ 백오프 중이면 **브로커로 나가지 않고** `-98`(`EXIT_RET_THROTTLED`) 반환
   ⓑ `ret==0`이면 거부 연쇄 리셋.
   **`throttle=False`는 절대원칙 §1 경로 전용**(15:10 시간청산) — 오버나이트를 막는
   주문을 우리 억제 장치가 늦추면 안 된다.
4. **청산 실패 지점 8곳 전부**에 거부 훅 배선 — 틱 하드스톱 · 분봉 하드스톱 ·
   손절1차 · 손절1차qty1 · TP부분청산 · 수동청산 · 15:10 시간청산 · 브로커직접청산.
5. **틱 하드스톱은 `_set_pending_order` 전에** 백오프로 거른다. 뒤에서 걸러도 되지만
   그러면 호출자가 매번 `[Exit] … 주문 실패` **ERROR**를 남겨 `exceptions_10m`을
   밀어 올린다 — **안전장치가 헬스 degraded를 자체 유발하는 2차 사고**가 된다
   (오늘 09시대에 정확히 그 경로로 자동진입이 19분 막혔다). 같은 이유로
   `[ExitRejectShadow]`는 **INFO**로 남긴다.
6. 잔고 Chejan에서 `closable_qty` 스냅샷 보관(G-6).

**`tests/test_511_exit_order_reject.py` (신규)** — 검사 **42개 전부 통과**.
T1~T8 동작 · T9~T12 소스 회귀(새 청산 경로가 훅 없이 추가되면 깨진다) ·
T13 런타임 스모크(헬퍼가 클래스보다 뒤에 있어 소스 검사로는 못 잡는 `NameError`를
실제로 태워서 확인). COM·브로커·DB 불필요.

### 회귀 안전성

- **pending 선등록 규약을 깨지 않았다** — 거부 훅은 `_clear_pending_order()` **뒤**에만
  호출되고 **주문을 내지 않는다**(305차 2026-07-09 유령포지션 사고 재발방지).
- **절대원칙 4(COM 콜백)** — 콜백 안에서 하는 일은 `closable_qty` 속성 대입 하나뿐.
  `dynamicCall`·`emit` 없음.
- **성공 경로 무변경** — `ret==0` 분기는 한 줄도 건드리지 않았다.
- **기존 테스트 무회귀**: `test_471_force_exit_reachability`(전부 통과) ·
  `test_457_fallback_visibility`(7 passed) · `test_phantom_hardstop`(ALL PASSED) ·
  `test_473_core_group_reachability`(4 passed) · `test_entry_qty_invariant`(전부 통과).
- **라이브 DB 조회 0건** — 장중 금지 원칙 준수. 실행한 테스트는 전부 5초 이내.

### 남은 위험 (정직하게)

1. **오늘 15:10에는 안 걸린다** — 재기동 전까지 구코드다. 사용자 조치 2번 필수.
2. **근본 원인은 아직 못 고쳤다.** 이 수정은 *거부됐을 때 조용히 실패하지 않게* 만든
   것이지, *거부되지 않게* 만든 것이 아니다. 거부 자체를 없애려면 F-17 ②(수량 축소)
   또는 F-18(미체결 취소)이 필요하고, 둘 다 승인·섀도 관측이 선행이다.
3. **백오프가 손절을 늦출 수 있다** — 최대 8초. 거부가 아니라 **일시적** 장애였다면
   그 8초만큼 늦어진다. 오늘 실측(163회 연속 전량 거부)에서는 재시도 자체가 무의미했고
   오히려 `ret=4`를 유발했으므로 이 트레이드오프는 타당하다고 판단했으나,
   **표본 1건이다 — 확정 결론 금지**(313차). O-i1에서 계속 관측한다.

### 이 부록이 발급한 번호

없음(본문 1-3~1-5의 구현 결과). NEXT_TODO 항목: **F-17**(핸들러 본체·부분 이행) ·
**F-18**(주간회의) · **F-19/F-21/F-22/G-6**(이행 완료) · **F-20**(보류) ·
**G-7**(외부 미체결 경보, 미착수).

### 사용자 조치 추가 — 커밋 대상 갱신

앞의 「사용자 조치 6번」 목록에 아래 3개가 추가된다:

```
main.py
config/settings.py
tests/test_511_exit_order_reject.py
```

**배포(재기동) 시점은 장 마감 후를 권합니다.** 지금 재기동하면 포지션·세션 상태
복원 경로를 장중에 태우게 되고, 그것이 오늘 고친 것보다 큰 위험입니다.
