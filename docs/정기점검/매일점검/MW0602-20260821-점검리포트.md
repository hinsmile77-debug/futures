# 미륵이 일일 점검 — MW0602 / 2026-08-21

> 📄 **이 파일은 하루에 하나뿐이다.** 장전이 만들고 · 장중이 이어 붙이고 · 장후가 이어 붙여
> 그날의 종합 완성본이 된다. **앞 국면 절의 본문은 고치거나 지우지 않는다** — 정정은 뒤
> 국면 절에서 하고 앞 절에는 한 줄 포인터만 단다. (스킬 대원칙 B)
>
> 이상점 번호는 하루 안에서 이어진다: 장전 `1-1~` → 장중 이어받음 → 장후 이어받음.

---

# 장전(pre)

- 점검 시각: **08:59 KST** · PC `MW0602` · 브랜치 `dev` (운영 브랜치 일치 ✅)
- HEAD `04ff17b` · 미커밋 **3건** · 당일 커밋 **2건**
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260821_pre.md`
  (다이제스트 첫 줄 `MW0602 (override · host=claude)` — PC 지정 정상)
- 수집기 실행: **성공** (stderr 없음, 56.5KB 생성)

---

## 0. 오늘 한눈에

**오늘 거래를 시작해도 되는 상태입니다. 당장 위험한 문제는 없습니다.**

프로그램은 오늘 아침 8시 40분에 정상적으로 켜졌고, 예측에 쓰는 6개 모델이 모두 제대로
읽혔으며, 증권사 접속·시세 수신·아침 경제지표 수집까지 순서대로 다 됐습니다. 어제 밤에
돌린 학습도 성공했고, 그 결과가 오늘 아침에 제대로 반영된 것까지 확인했습니다.

**좋은 소식이 세 가지 있습니다.**

1. 지난 이틀간 "고쳤는데 실제로 되는지는 내일 봐야 안다"고 미뤄뒀던 수정 두 건이
   **오늘 아침 실제로 작동하는 것이 확인됐습니다.** 그중 하나는 매매 크기를 정하는 방식을
   손보려면 먼저 통과해야 했던 관문이라, 그 논의를 이제 시작할 수 있게 됐습니다.
2. 점검 프로그램이 장이 열리기 **전에** 돌기 시작했습니다. 지난 5거래일 동안은 장이 열린
   뒤인 9시 7분에 돌아서 "장 열기 전 점검"이 사실상 불가능했는데, 어제 저녁에 실행
   시각을 고친 것이 효과를 냈습니다. 다만 아직 하루치라 사흘 연속 확인이 필요합니다.
3. 아침 준비 과정에서 잠깐 어긋났던 판단 재료 두 개가 **장이 열리기 전에 스스로 정상으로
   돌아왔습니다.**

**한 가지 새로 발견한 문제가 있습니다.**

매매 확률을 보정하는 장치가 만들어 놓은 결과물이 **8월 11일 이후 열흘 가까이 한 번도
저장되지 않았습니다.** 매일 저장을 시도하긴 하는데, 특정 조건에서는 조용히 저장을 건너뛰고
그 사실을 아무 데도 남기지 않습니다. 그래서 아무도 몰랐습니다. 결과적으로 프로그램은
지금 **8월 11일자 낡은 보정값**으로 돌고 있고, 그 뒤로 쌓인 자료는 매일 밤 버려지고
있습니다. 오늘 당장 손해가 나는 종류의 문제는 아니지만, 판단 품질이 조용히 낡아갑니다.
장이 끝난 뒤에 고칠 것을 권합니다.

**오늘 사람이 할 일은 두 가지입니다** — 자세한 내용은 맨 끝 `## 9. 사용자 조치`에
모아 두었습니다. (① 장 마감 후 고치기 지시 ② 오늘 주간회의 안건 3건 확인)

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | **조건부 정상** | 기동·모델·접속·매크로 전부 정상, 전일 이월 관측 7건 중 5건 통과. 단 앙상블 보정기 영속화가 6거래일 무저장(P1, 오늘 거래 자체는 가능) |
| 장중 | — | 미실시 (12:30 예정) |
| 장후 | — | 미실시 (16:30 예정) |

**절대원칙 준수 여부** (장전 시점 — 대부분 장중·장후에 판정된다)

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | **판정 불가(장후)** | 기동 시 `[BrokerSync] startup sync 무포지션 확인(blank rows): FLAT -> FLAT` (08:41:15) — 전일 이월 포지션 **0** 확인. §12 `강제청산_경로` 3거래일 `≥1(생존)` 유지 |
| ② | Circuit Breaker 5종 | **판정 불가(장중)** | §12 `CB_state` 최근 8거래일 `NORMAL`×2,956 — 정상고착. 장전 발동 0건 |
| ③ | CORE 피처 호라이즌 그룹별 | **준수(조건부)** | 08:45:15에 CORE 2종(`cvd_divergence`·`opt_chain_pcr`) 6호라이즌 축퇴 → **08:48:03에 0/6 정상 복귀**. 진입 개시(09:00) 전 해소 ✅ (§1-5 참조) |
| ④ | COM 콜백 내 `dynamicCall`·`emit` 금지 | **준수** | `0xC0000409`·`STACK_BUFFER` 로그 0건. `logs/crash_fault.log` 신규 기록 없음. 프로세스 무응답 흔적 없음 |
| ⑤ | `GetRepeatCnt`/`GetCommData` 파라미터 구분 | **준수** | TR 수신 정상(`[CybosDailyPnl] validate={...}` 인덱스 매핑 정상, 08:41:15) |
| ⑥ | 알파 리서치 봇 자동통합 금지 | **준수** | 알파봇 실행·통합 로그 0건 |

**설정 불변식** — 수집기 §3 전 25행 `일치`(또는 의도된 `값 확인`). **`불일치`·`미발견` 0건.**
차단 게이트 전수 29개 중 7개 `False`는 전부 `기록됨`/`기능토글`로 근거가 등록돼 있다 —
신규 이상 없음.

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**해당 없음.** 확인했으나 없었다.

> ⚠ **레벨 집계만 보고 쓴 결론이 아니다**(함정 ④ — 미륵이는 ERROR를 거의 남기지 않는다.
> 오늘도 **ERROR 0건**이지만 그것을 근거로 쓰지 않았다).
> 수집기 **§5 거래일 요약**을 읽었고 전 항목이 0이다 — 진입체크 통과 0 · 진입 등록 0 ·
> 체결 0 · **포지션 0** · 청산 레그(최종/부분) 0 · 차단 0 · 사이저 호출 0 · `[SizerMatch]` 0 ·
> `[MarginCap]` 0. **분모를 함께 봤다**: 진입 가능 시간 예산이
> `PRE_MARKET 🚫금지 370분 / 100%`, **진입 가능 0분**이다. 즉 이 0들은 "판단이 막혔다"가
> 아니라 **"아직 장이 열리지 않았다"** 이다. 장중 절에서 다시 분해한다.
> WARNING은 총 **202건**(`Calibration` 174 · `LiveDBG` 5 · `ScalerRefresh` 12 ·
> `CORE준비도` 1 · `CybosProbe` 10)이며 전부 §1-1·§1-5·§1-8에서 처분했다.

### P1 — 정확성·관측 훼손

#### 1-1. 앙상블 보정기 저장본이 6거래일 동안 한 번도 갱신되지 않았다 — 실패가 조용하다

- **증상**: 확률 보정 장치(앙상블 Platt 보정기)의 저장 파일
  `data/ensemble_calibrator.pkl`이 **2026-08-11 15:40 이후 갱신되지 않았다.**
  그 결과 매일 기동 시 복원되는 값이 **7거래일 연속 6개 필드 전부 동일**하다
  (08-12·13·14·18·19·20·21. ⚠ **08-17은 거래일이 아니다** — 프로세스는 떴지만
  `logs/20260817_SYSTEM.log` `18:11:56 [System] 대기 중 | 공휴일·휴장일`. 기동 횟수로는
  8회지만 **거래일로는 7일**이며, 이 리포트는 전부 거래일 기준으로 센다).
  저장을 시도하는 코드는 매일 실행되는데, **실패했다는 로그가 어디에도 없다.**

- **근거**:

  ① 기동 복원 로그가 **7거래일(기동 8회) 동안 바이트 단위로 동일**하다 (`logs/{날짜}_SYSTEM.log`):
  ```
  2026-08-12 … [Calibration] 앙상블 보정기 복원 완료 n=1183 fitted=True degenerate=False unreachable=False span=0.02133 auc=0.574 out_max=0.3897
  2026-08-13 … (동일)   2026-08-14 … (동일)   2026-08-17 … (동일 · 휴장일 기동)
  2026-08-18 … (동일)   2026-08-19 … (동일)   2026-08-20 … (동일)
  2026-08-21 08:41:08 [INFO] SYSTEM: [Calibration] 앙상블 보정기 복원 완료 n=1183 fitted=True degenerate=False unreachable=False span=0.02133 auc=0.574 out_max=0.3897
  ```
  그 이전에는 **3~4거래일마다 계단식으로 갱신**됐다 —
  `n=296`(07-31~08-03) → `n=592`(08-04~08-07) → `n=887`(08-09~08-11) → `n=1183`(08-12~). 
  즉 "원래 이랬다"가 아니라 **08-12부터 멈춘 것**이다.

  ② 저장 성공 로그가 마지막으로 찍힌 날은 08-11이다:
  ```
  2026-08-11 15:40:07 [INFO] SYSTEM: [Calibration] 앙상블 보정기 저장 완료 n=1183
  ```
  `08-12·13·14·18·19·20` 6거래일 **전부 0건**(`logs/{날짜}_SYSTEM.log` 전수 검색).

  ③ 파일 시각도 일치한다: `data/ensemble_calibrator.pkl` — **2026-08-11 15:40**.

  ④ **예외가 난 것이 아니다.** `[Calibration] 보정기 저장 실패` 경고는 08월 전 기간 **0건**.

  ⑤ **일일 마감 자체는 그 지점을 지나갔다.** 같은 `daily_close()` 안에서 보정기 저장
  **바로 다음 줄**에 있는 MetaConf 저장의 산출물 `data/meta_conf_state.pkl`은
  **2026-08-20 15:40**로 정상 갱신돼 있다. 즉 "마감이 도중에 죽어서 저장을 못 했다"가
  아니라 **저장 함수가 `False`를 돌려주고 조용히 넘어갔다.**

- **원인(코드 실재 확인)**:
  ```python
  # learning/calibration.py:626-629
  def save(self, path: str) -> bool:
      if not _SKLEARN_OK or not self._fitted:
          return False          # ← 로그 없음
  ```
  ```python
  # main.py:11151-11159  (11150은 import 줄)
  if self.ensemble.ensemble_calibrator.save(ENSEMBLE_CALIBRATOR_PATH):
      log_manager.system("[Calibration] 앙상블 보정기 저장 완료 n=…", "INFO")
      # ← else 분기가 없다
  ```
  `_fitted`는 장중 재적합에서 축퇴(`learning/calibration.py:507`) 또는
  하한 도달불가(`:533`) 판정이 나면 `False`가 된다. 15:40 마감 시점에 그 상태였으면
  저장이 통째로 건너뛰어지고, **양쪽 어디에도 부정 분기 로그가 없어 관측되지 않는다.**

- **기준 위반**: 매분 9단계 **STEP 2(SGD 온라인 자가학습)**·**STEP 6(앙상블 진입 판단)**의
  입력 신선도. 절대원칙 6종 저촉은 **없다**(매매 차단·오버나이트·CORE·COM 어느 것도 아니다).
  더 중요하게는 **403차 P0-1의 취지에 대한 기능적 회귀**다 — 그 결정의 원문
  (`main.py:3333` docstring)이 *"저장 n이 누적되지 않고 매일 300건 안팎에서 다시
  시작했다"*를 고친 것인데, 지금 **n이 1183에서 얼어붙어 같은 결과(누적 정지)**가 됐다.
  ⚠ **재발이 아니다** — 403차는 호출 위치 문제였고 이번은 `save()` 내부 게이트다.
  증상만 같고 원인이 다르므로 "재발 P0"로 올리지 않는다.

- **영향**: 프로그램이 확률을 다듬을 때 쓰는 기준표가 **8월 11일에 멈춰 있습니다.**
  그 뒤 열흘간 시장에서 배운 것은 매일 밤 버려지고 다음 날 아침 다시 옛날 기준표로
  시작합니다. 오늘 당장 손실이 나거나 매매가 막히는 문제는 아니지만, 시간이 갈수록
  판단 기준이 실제 시장과 어긋납니다.

- **영향(기술)**: `ensemble.ensemble_calibrator`는 `load()`가 무조건 `_fitted=True`로
  세우므로(`learning/calibration.py:658`) 매 기동 시 **08-11 스냅샷의 Platt 계수가 실제로
  적용된다**(`model/ensemble_decision.py:1143-1144` `is_fitted` 분기). 진입 차단은 없다
  — 미fit이면 raw 통과하는 보수적 폴백이므로 `False`여도 매매는 돈다. 손실은
  **관측·학습 축**이다: `probs`/`labels`/`n`/`clean_probs`/`clean_labels`/`artifact_n`
  6종이 전부 휘발돼 [45] `cal_guard_flap_watch`(진행 중인 캠페인 채널)의 하루 단위 대조
  표본도 함께 사라진다.

- **신규 여부**: **신규.** `dev_memory/DECISION_LOG.md`·`NEXT_TODO.md`에서
  `ensemble_calibrator` 언급 20건을 전수 확인했으나 **영속화 실패를 다룬 항목은 없다**
  (기존 3건은 전부 *내용* 분석 — 2026-07-30 저장본 `n=296` 실측 인용). 
  §12 고착 지표 감시목록에도 이 지표는 **등록돼 있지 않다.**

- ⚠ **[45] `cal_guard_flap_watch`와 혼동하지 말 것 (함정 ①·C-6 관문).**
  [45]는 *"보정 가드가 하루에 몇 번 뒤집히는가"*를 재고, **확정 결정은 「전면 보류 —
  EOD 1회 고정 미배포, 조치 트리거만 사전등록」(2026-08-08)** 이다(다이제스트 §13).
  본 항목은 *"뒤집힌 상태로 마감을 맞으면 저장이 통째로 사라진다"*는 **영속화 축**이며,
  §2 F-1은 **히스테리시스를 재제안하지 않는다.** 두 축이 인과로 이어져 있다는 사실만
  기록하고, 가드 설계 변경은 [45]의 주간회의 소관으로 남긴다.

### P2 — 운영 부담·기술부채

#### 1-2. 어제 장후 산출물 3건이 커밋되지 않은 채 남아 있다

- **증상**: 미커밋 3파일 148줄 추가 — `dev_memory/DECISION_LOG.md`(+54) ·
  `dev_memory/NEXT_TODO.md`(+24) · `docs/정기점검/매일점검/MW0602-20260820-점검리포트.md`(+70).
- **근거**: `git -c core.autocrlf=true status --porcelain` 3건.
  마지막 커밋 `04ff17b`는 **08-21 00:08**, 리포트 파일 최종기록은 **08-21 00:14** —
  즉 커밋 **직후에** 어제 장후 세션이 §10(예약작업 3종 시각 교정 기록)을 덧붙이고
  커밋하지 않은 채 끝났다. **코드 변경은 0건, 전부 문서다.**
- **기준 위반**: `phases.md` A-4(미커밋 변경을 실전 PC에 남기지 않는다) ·
  CLAUDE.md 「멀티PC 작업 컨벤션」(PC간 공유가 필요한 내용은 반드시 git에 커밋되는 문서에
  적는다 — 402차 사고 원칙).
- **영향**: 어제 저녁에 고친 내용의 기록이 이 PC에만 있고 다른 PC(MW0601)에서는 보이지
  않습니다. 그쪽 세션이 같은 문제를 다시 진단할 수 있습니다.
- **영향(기술)**: 특히 위험한 내용이 들어 있다 — 미커밋 diff에
  *"`F-4(bat)` 실행은 무효, 진짜 원인은 cron `2 9`"* 라는 **정정**이 들어 있다.
  이것이 공유되지 않으면 다른 세션이 `TASK_CLAUDE_WAKE_INSTALL.bat` 실행을
  사용자에게 다시 요청한다(0820 리포트 §9p 표 2번을 그대로 읽는 경로).
- **신규 여부**: 신규(어제 장후 세션의 마감 누락). 본 세션이 §8에서 함께 커밋한다.

#### 1-3. 전일 프로세스가 살아남아 런처가 강제 종료했다 — 10거래일 연속

- **증상**: 기동 시 이전 `main.py` 프로세스가 아직 떠 있어 런처 가드가 죽였다.
- **근거**: `logs/Mireuk_batch/launcher_20260821_084001_29656.log`
  ```
  [GUARD] 기존 main.py 프로세스 체크...
  [WARN] 이미 실행 중인 main.py 프로세스가 감지됐습니다.
  [WARN] 이중 실행 시 GBM pkl 파일 경합 및 중복 주문이 발생할 수 있습니다.
  [GUARD] 기존 프로세스 종료 완료 -- 새 인스턴스 시작.
  ```
- **기준 위반**: `phases.md` A-1(중복 기동 없는가). 절대원칙 저촉은 없다 —
  가드가 정상 작동해 이중 실행은 **일어나지 않았다.**
- **영향**: 프로그램이 전날 밤에 완전히 꺼지지 않고 남아 있다가, 아침에 강제로 정리됐습니다.
  오늘 매매에 지장은 없습니다. 다만 매일 반복되고 있어 원인을 찾아 두는 편이 좋습니다.
- **영향(기술)**: **O-3 지속(10/10 거래일)**. 477차 G-3 `_dump_surviving_threads()` 실독에서
  비-데몬 스레드가 `MainThread` 하나뿐임이 확인돼(3거래일) 원인 후보가
  *"이벤트 루프 종료 후 MainThread 미반환(COM 해제 유력)"* 으로 좁혀진 상태다.
  ⚠ **313차 원칙** — 3거래일 표본이므로 **확정 결론 금지**. `O-5(장후)` 비-데몬 개수
  추적으로 계속 축적한다.
- **신규 여부**: 기존 — `NEXT_TODO.md` O-3 (10거래일 연속), 477차 G-3 항목.

#### 1-4. 관측 항목·기한 표기에 셋 다 이름·날짜가 어긋나 있다 (문서 정정 3건)

- **증상**: `dev_memory/NEXT_TODO.md`에 다음 세 가지 표기 오류가 있다.
  1. **`O-5`가 두 개다** — `O-5(매 장전)` 다이제스트 생성 시각(0820 장후 등록) vs
     `O-5(매 장후)` `[Shutdown]` 잔존 비-데몬 스레드 수(477차 등록). 어제 리포트가
     `F-4` 이름 충돌을 경고하며 정정한 바로 그 형태가 **같은 문서 안에서 재발**했다.
  2. **`### 🔵 기한 — 주간회의(2026-08-22 금)`** — **2026-08-22는 토요일**이다.
     금요일은 **오늘(2026-08-21)**.
  3. **"이번 금요일이 기한 전 마지막 주간회의다"**(CB② 항목) — **틀렸다.**
     기한 2026-08-29는 토요일이고, 그 전 금요일은 **08-21(오늘)과 08-28 두 번**이다.
     남은 거래일은 08-21·24·25·26·27·28 = **6거래일**.
- **근거**: `dev_memory/NEXT_TODO.md:14632`(주간회의 날짜) · `:14634`·`:14760`(CB② 기한
  서술) · `:14789`와 `:14829`(O-5 이중 등록). 요일 검산:
  `2026-08-21 Friday · 2026-08-22 Saturday · 2026-08-28 Friday · 2026-08-29 Saturday`.
- **기준 위반**: 기준 문서 자체의 정확성. CLAUDE.md 「한시 예외는 "잊고 방치"가 가장 큰
  위험」 원칙 — 기한 표기가 틀리면 그 방치가 구조화된다.
- **영향**: 다음 점검이 "관측 항목 O-5"라고만 적힌 것을 보면 둘 중 어느 것인지 알 수 없고,
  회의 날짜가 하루씩 밀려 적혀 있어 마감을 놓칠 수 있습니다.
- **영향(기술)**: 특히 3번은 방향이 위험한 오류다 — "오늘이 마지막 기회"라고 읽으면
  CB② 재검토를 **오늘 무리하게 결론 내리도록 압박**한다. 실제로는 08-28 주간회의가
  한 번 더 있으므로, 오늘은 **안건 상정**만 하고 표본을 한 주 더 보는 선택이 열려 있다.
- **신규 여부**: 신규(표기 오류). §8에서 `NEXT_TODO.md`를 정정한다.

### 확인 필요 (확정 아님)

- **1-5. 아침 첫 스케일러 재적합에서 CORE 2종이 6호라이즌 전부 축퇴했다 — 개장 전 해소됨.**
  ```
  08:45:15 [WARNING] SIGNAL: [ScalerRefresh] 1m CORE 'cvd_divergence' raw_std≈0(0.0228) → identity(0,1) 강제 (FLAT 100% 방지)
  08:45:15 [WARNING] SIGNAL: [ScalerRefresh] 1m CORE 'opt_chain_pcr' raw_std≈0(0.0236) → identity(0,1) 강제 (FLAT 100% 방지)
  … 3m·5m·10m·15m·30m 동일 (총 12건)
  08:45:15 [WARNING] SIGNAL: [CORE준비도] trigger=A_WARMUP 축퇴 2/6 — cvd_divergence×6hz opt_chain_pcr×6hz | 정상 진입 전 해소 여부를 확인하라
  ```
  → `08:48:03`·`08:49:57`·`08:54:57`·`08:58:59` **4회 연속 `축퇴 0/6 — CORE 스케일 정상`**.
  **진입 개시(09:00) 전에 해소됐다.** 절대원칙 ③ 위반 아님.
  ⚠ 단 `opt_chain_pcr`은 **장기(30m) 그룹 CORE**이고, CLAUDE.md 474차 각주가 확인한 대로
  중기·장기 CORE 그룹은 `select_entry_horizon()`이 `1m/3m/5m/None`만 반환해 **구조적으로
  도달 불가**다 — 즉 `opt_chain_pcr` 축퇴는 실집행 영향이 없다. 실질 대상은
  `cvd_divergence`(단기 CORE, 미통과 시 등급 하락) 하나뿐이다.
  **판정 보류 — 1일 표본.** §12 `CORE준비도`가 4관측일 `1`×98 `0`×26 …로 여전히
  `표본부족` 구간이다(계측 08-14~). **무엇을 더 보면 닫히나**: 20거래일 축적 후
  "축퇴일의 09:00~09:30 정확도"를 일자단위로 비교(317차 교훈, `invariants.md` §12 각주).

- **1-6. `MarginCap_state` 무기록 — 오늘이 계측 배포 첫날이라 장전 0건은 예상된 것.**
  수집기 §11 자동 적신호 4번이 `🔴 무기록`으로 올렸으나, 이 지표는 477차 후속 G-2가
  `dfc4a3d`(08-20 21:29)로 배포해 **`measured_since=2026-08-21`** 이다. 장전에는
  주문가능수량 조회가 없으므로 0건이 정상이다. **판정 보류 → 장중에 판정한다**(O-11).
  ⚠ 장중에도 0건이면 그때는 진짜 계측 실패다.

- **1-7. `PredictionCalibrator.load()`가 `_fitted=True`를 무조건 세운다.**
  `learning/calibration.py:658`. 오늘은 무해하다 — 복원된 08-11 저장본이
  `degenerate=False unreachable=False`이기 때문이다. 다만 1-1의 저장 게이트를 고칠 때
  **축퇴 상태의 저장본을 만들면 다음 기동에서 그것이 되살아난다.** §2 F-1은 이 두 지점을
  **한 커밋에서 같이** 다룬다. 단독 확인 항목으로는 **판정 보류**(현행 무해).

- **1-8. Cybos 선물 투자자 TR 미지원 — 상시·기존.**
  `[CybosInvestor] futures supported=False … reason=Cybos 선물 투자자 TR 미발견; 미결제약정만 제공`
  (08:58:19·08:58:49) + `[CybosProbe] … dispatch/request failed` 10건.
  전일들과 동일한 수준이다(08-18·19·20 각 798건/일, 오늘 장전분 10건).
  `dev_memory`에 19회 언급된 **기존 상태**이며 신규 이상이 아니다. 기록만 한다.

### 이미 반영된 사안 (신규 아님)

**함정 ① 관문 — 아래는 오늘 로그·리포트가 지적하지만 이미 처분된 것이다. 재제안 금지.**

1. **`TASK_CLAUDE_WAKE_INSTALL.bat` 실행 요청 → 무효.**
   `NEXT_TODO.md:14708` `F-4(bat)`과 0820 리포트 `§9p 사용자 조치` 표 2번은
   **어제 장후 조치로 대체됐다** — P-7의 진짜 원인은 `.bat` 미실행이 아니라 예약작업
   cron이 `2 9`(09:02)여서 지터 253초를 먹으면 구조적으로 개장을 넘기는 것이었고,
   `55 8 * * 1-5`로 교정해 직접 닫았다. **오늘 그 효과가 실측됐다**(§1-9 / O-5).
   ⚠ 항목 자체는 삭제하지 않는다 — cron 교정이 실패하면 다시 유일한 경로다.
2. **앙상블 A/B 등급 임계 미도달(1-9, 0819 등록)** — 오늘 기동 로그의
   `out_max=0.3897`은 그 사실의 **방증일 뿐 신규 발견이 아니다.**
   §12b가 `앙상블A(conf≥0.70)`·`앙상블B(conf≥0.60)` **9일 연속 미도달(max 0.5878)**로
   자동 추적 중이고, CLAUDE.md 「확률 판단 기준」에 476차 병기가 이미 들어가 있다.
   처분은 **주간회의 안건(3안 중 택일)** 이다.
3. **보정 가드 히스테리시스 도입** — [45] `cal_guard_flap_watch` **「전면 보류」 확정
   (2026-08-08)**. §2 F-1은 이 질문을 건드리지 않는다(위 1-1 경고 참조).
4. **CB②가 발동하지 않는 것** — `CB_CONSEC_STOP_LIMIT=9999`는 **의도된 모의투자 한시
   예외**다(CLAUDE.md 절대원칙 §2). 발동 없음이 정상이며 이상점이 아니다.

---

### 1-9. 전일 등록 관측 항목 처분표 (장전 소관분)

> 앞 국면(어제 장후)이 등록한 `P-*`/`O-*` 중 **장전에 판정되는 것**을 전부 닫는다.
> 장중·장후 소관은 사유를 적고 이월한다. (대원칙 B-②)

| ID | 항목 | 판정 기준 | 실측 | 처분 |
|---|---|---|---|---|
| **P-2** | `[EntryGate] … 6/6 호라이즌 현행 레이블(fixed) 학습본` 출현 **+** `[EntryGate] 전일 EOD 모델 적재 확인 …` 미출현 | 둘 다 | `08:55:16 [EntryGate] 모델 레이블 체계 확인(장전) — 사이즈 제한 해제 (×0.6 → ×1.0) \| 6/6 호라이즌 현행 레이블(fixed) 학습본` **출현** / `전일 EOD 모델 적재 확인` **전 로그 0건** | ✅ **통과** |
| **P-7** | 장전 점검 실행 시각 09:00 이전 | — | 다이제스트 생성 **08:59:48** (5거래일 연속 09:07 → 개장 전 진입) | ⬇️ **완화 P1→P2, 종결 진행** (O-5 1/3일) |
| **O-1** | `[PreRetrain] EOD 마커 파일 직접 확인 …(session_state 미기록 보완)` **소멸** 여부 | 소멸 | 해당 문구 **0건**. 대신 `08:55:16 [PreRetrain] EOD 재학습 날짜 복원: 2026-08-20 (1일 전)` — **session_state 경로로 정상 복원** | ✅ **통과** |
| **O-2** | `session_state.json` 3키 생존 + `circuit_breaker_state`·`profit_guard_state` 부재 | 4불변식 | `{"date":"2026-08-21","count":1,…,"gbm_total_retrain_count":2,"p8_last_success_date":"2026-08-20","eod_retrain_ok_date":"2026-08-20","today_open":1068.4}` — 3키 **생존**, CB/ProfitGuard 키 **부재**, `count` 리셋 후 증가, `auto_shutdown_done_date` `""` | ✅ **통과(4/4)** |
| **O-3** | 런처 `[GUARD] 기존 프로세스 종료 완료` 연속일 | 추이만 | 오늘도 출현 → **10/10** | 🔄 **지속** → §1-3 |
| **O-4** | `[Capital]` 괴리율 추이 (단일일 판정 금지) | 추이만 | `08:41:15 [Capital] 기준자본=50,000,000원(target) \| 실제잔고=28,930,730원 \| 괴리=-42.1%` — 전일 -42.1%(28,930,722원)에서 **+8원, 사실상 무변동** | 🔄 **기록만** |
| **O-5(장전판)** | 다이제스트 생성 시각 09:00 이전 · **3거래일 연속이면 P-7 종결** | 3일 | **1/3일 (08:59:48)** — 마진 **12초** | 🔄 **지속(1/3)** |
| P-8 · P-9 · O-5(장후판) · O-6 ~ O-9 | 손익·강제청산·스레드·MarginCap 대사·메타/손절/GuardFair | — | 장중·장후 소관 | ⏭️ **이월** |

> 🔴 **O-5(장전판) 마진 경고.** 어제 장후가 예측한 발화 마진은 47초였는데
> **실측 여유는 12초**였다(08:59:48 생성 vs 09:00:00 개장). 다이제스트 생성은 발화 후
> 몇 초 뒤이므로 이 값이 실질 마진이다. **지터가 조금만 커지거나 수집기가 몇 초 느려지면
> 다시 개장을 넘는다.** 재발 시 **cron을 더 앞당기지 말 것** — `phases.md` A-2
> (08:55 매크로 수집 → 레짐 판정) 증거를 잃는다. 오늘 실측 레짐 확정은 `08:58:19`이므로
> 08:58:30 이전 발화는 A-2를 관측할 수 없다.

---

## 2. Fix 작업 구현계획

> ⚠ **장전 국면이다. 아래 계획은 수립만 하고 적용하지 않는다** — 라이브 프로세스가 돌고
> 있고, 대상 파일이 장중 매분 경로에 있다. **15:10 강제청산 이후 적용**을 권고한다.

### F-1. 앙상블 보정기 영속화 복구 — P1 · 대응 이상점 1-1

- **원인 가설**: `save()`가 "모델이 fit 상태일 때만 저장"으로 설계돼 있어, 마감 시각의
  축퇴/도달불가 상태가 **누적 표본까지 함께 버린다.** 모델 계수와 누적 표본은 수명이
  다른 데이터인데 한 게이트에 묶여 있다.

- **변경 파일**:
  1. `learning/calibration.py` — `save()` (626~648행):
     `if not _SKLEARN_OK or not self._fitted: return False` →
     `if not _SKLEARN_OK: return False` 로 완화하고, **payload에 `"fitted": self._fitted`,
     `"degenerate": self._degenerate`, `"unreachable": self._unreachable` 3키를 추가**한다.
     `_model`이 `None`이면(한 번도 fit된 적 없음) 종전대로 `False` 반환.
  2. `learning/calibration.py` — `load()` (650~; 특히 `:658` `self._fitted = True`):
     **`state.get("fitted", True)`로 바꾼다.** 구버전 저장본에는 키가 없으므로
     기본값 `True`가 종전 동작을 보존한다(하위호환). `degenerate`·`unreachable`도 같은
     방식으로 복원한다. → **이상점 1-7을 같은 커밋에서 닫는다.**
  3. `main.py` — **11151행**(`if save(...)`, 감싸는 `try` 블록 11149~11159): `if save(...)` 를 **`_ok = save(...)`로 받아
     `else` 분기에 `WARNING` 로그**를 단다:
     `[Calibration] 앙상블 보정기 저장 건너뜀 — fitted=%s degenerate=%s unreachable=%s n=%d`.
     **부정 분기 무로그를 없애는 것이 이 F의 핵심**이다 — 코드를 안 고치더라도 이 로그
     하나만 있었으면 6거래일이 아니라 하루 만에 발견됐다.

- **회귀 위험**: **축퇴 상태의 저장본이 다음 기동에 되살아나는 것**이 유일한 실질 위험인데,
  변경 ②가 정확히 그것을 막는다(`fitted=False`로 복원 → `calibrate()`가 raw 통과 =
  현행 보수적 폴백과 동일). 나머지는 파일 크기 증가(3키, 수십 바이트)뿐이다.
  ⚠ **`degenerate`/`unreachable` 판정 임계와 히스테리시스는 건드리지 않는다** —
  [45] `cal_guard_flap_watch`의 확정 결정(전면 보류) 영역이다.

- **절대원칙 저촉 여부**: **6종 어느 것에도 저촉되지 않음.**
  ①(15:10 청산) 무관 · ②(CB) 무관 · ③(CORE 피처) 무관 — 보정기는 CORE 체크리스트가
  아니라 앙상블 confidence 후처리다 · ④ COM 콜백 밖 · ⑤ TR 파라미터 무관 ·
  ⑥ 알파봇 무관. **매매 게이트 신설이 아니므로 섀도 계측 불요**(노출 무영향 —
  `calibrate()` 동작은 `fitted` 값에 대해 종전과 동일하다).

- **런타임 주의**: `learning/calibration.py`·`main.py` 둘 다 **`py37_32` 장중 본체**에서
  돈다. 재학습(`py310_64`) 경로가 아니다. `joblib.dump(..., protocol=4)`는 **현행 유지**
  (py310_64가 쓴 산출물을 py37_32가 읽는 호환 규약 — 191차).

- **검증 방법**:
  1. `tests/test_484_calibrator_persistence.py` 신설 — 불변식 4종:
     ① `_fitted=False`(축퇴)에서 `save()`가 `True`를 반환하고 파일이 생긴다
     ② 그 파일을 `load()`하면 `is_fitted is False`
     ③ 구버전 payload(3키 없음)를 `load()`하면 `is_fitted is True`(하위호환)
     ④ `_model is None`이면 `save()`가 `False`
  2. **다음 거래일 관측** — `[Calibration] 앙상블 보정기 저장 완료 n=` 이 **1183보다 큰
     값**으로 출현하거나, 건너뛴 경우 **새 `저장 건너뜀` WARNING**이 사유와 함께 출현.
     둘 중 하나가 반드시 찍혀야 한다(무로그 = fix 실패).
  3. `data/ensemble_calibrator.pkl` mtime이 당일로 갱신되는가.

- **적용 시점**: **장후(15:10 이후)**. 이유 — `learning/calibration.py`는 장중 매분
  `record()`/`fit()` 경로에 있고, `main.py:11151`은 `daily_close()` 안이라 오늘 15:40
  마감 전에 바꾸면 **오늘 마감이 첫 실행이 되는데 검증 없이 라이브 마감을 태우게 된다.**

- **결정 필요 사항**: **없음.** 부정 분기 로그 추가와 상태 필드 영속화는 판정 기준
  변경이 아니다(사전등록 원칙 위반 아님).

### F-2. `NEXT_TODO.md` 표기 정정 3건 — P2 · 대응 이상점 1-4

- **원인 가설**: 관측 항목이 국면별로 따로 등록되면서 ID 네임스페이스가 겹쳤고,
  기한은 요일 검산 없이 적혔다.
- **변경 파일**: `dev_memory/NEXT_TODO.md` (코드 변경 없음)
  - `O-5(매 장전)` → **`O-10(매 장전)`** 으로 개명한다.
    (⚠ **`O-5(매 장후)`는 그대로 둔다** — 477차가 먼저 쓴 번호다.)
  - `### 🔵 기한 — 주간회의(2026-08-22 금)` → **`(오늘 2026-08-21 금 · 다음 2026-08-28 금)`**
  - CB② 항목의 *"이번 금요일이 기한 전 마지막 주간회의다"* →
    *"기한 2026-08-29(토) 전 금요일 주간회의는 **오늘(08-21)과 08-28 두 번**. 남은 거래일 6."*
- 🔴 **반영 방식 — 과거 항목을 고쳐 쓰지 않았다.** `dev_memory`는 append 전용이므로
  (SKILL.md §6 「기존 내용을 덮어쓰지 않는다」), 정정은 **484차 절에 새로 쓰고 그 절부터
  새 표기를 쓰는** 방식으로 반영했다. 08-20 이전 줄의 `O-5(매 장전)`·`(2026-08-22 금)`
  표기는 **그대로 남아 있다** — 그것이 "그때 그렇게 적었다"는 기록이기 때문이다.
  484차 절이 `O-10 (매 장전, 구 `O-5` 장전판)`으로 연결고리를 남긴다.
- **회귀 위험**: 없음(문서). ⚠ 다음 세션이 **옛 줄만 보고** `O-5`를 장전 항목으로 읽을
  잔여 위험은 남는다 — 그래서 §6 관측표와 484차 절 양쪽에 `구 O-5` 병기를 넣었다.
- **절대원칙 저촉 여부**: 없음. **런타임 주의**: 해당 없음.
- **검증 방법**: 다음 세션이 `O-5`를 검색해 항목이 하나만 나오는가.
- **적용 시점**: **즉시**(문서라 라이브 무영향). 본 세션 §8에서 수행.
- **결정 필요 사항**: 없음.

### F-3. 어제 장후 산출물 커밋 — P2 · 대응 이상점 1-2

- **변경 파일**: 없음(기존 미커밋 3파일을 그대로 커밋).
- **회귀 위험**: 없음. **절대원칙 저촉**: 없음. **런타임 주의**: 해당 없음.
- **검증 방법**: `git status --porcelain` 0건 + `git log`에 `[MW0602]` 접두 확인.
- **적용 시점**: **즉시**(본 세션 §8). 오늘 리포트·dev_memory와 **한 커밋**으로 묶는다.
- **결정 필요 사항**: 없음. ⚠ `git push`는 사용자 확인 후 — §9 참조.

### 적용 순서와 커밋 계획

1. **커밋 ①(본 세션·즉시)** — F-2 + F-3 + 오늘 리포트 + `DECISION_LOG`/`NEXT_TODO` 갱신
   → `[MW0602] 484차: 08-21 장전 점검 — 앙상블 보정기 영속화 6거래일 무저장 발견(1-1) + P-2·O-1·O-2 통과`
2. **커밋 ②(장후·사용자 지시 후)** — F-1 + 회귀 테스트
   → `[MW0602] 484차 후속: 앙상블 보정기 영속화 복구 (F-1) — 저장 게이트 분리 + 부정 분기 로그`

---

## 3. 고도화 방안

당일 관측에서 출발한 것만 쓴다.

### G-1. §12 고착 지표에 「기동 복원 스냅샷 동일성」 축을 추가한다

- **관측 근거**: 오늘 1-1은 **§12가 잡을 수 있었어야 했는데 못 잡았다.**
  `[Calibration] 앙상블 보정기 복원 완료 n=1183 … out_max=0.3897`이 **7거래일 연속**
  **6개 필드 전부 동일**했는데, 감시목록에 이 지표가 없어 아무 표에도 안 떴다.
  §12가 잡는 세 가지 죽음(고착·무기록·분기편향) 중 **어디에도 해당하지 않는 네 번째
  형태**다 — 로그는 매일 정상 출력되고, 값도 "정상"이며, **다만 어제와 똑같다.**
- **제안 내용**: 수집기 `stuck_indicators`에 **`snapshot_identity` 판정**을 추가한다.
  기존 `patterns`가 *하나의 상태 토큰*을 추출해 분포를 보는 것과 달리, 이쪽은
  **정규식 캡처 그룹 전체(다중 필드)를 한 튜플로 묶어 일자별로 비교**하고,
  `연속 동일일수 ≥ N`이면 표에 `🟠 스냅샷 정체(n일)`로 올린다.
  초기 등록 후보 3종(전부 "매일 기동 시 1줄, 갱신되면 값이 변해야 하는" 성질):
  | 지표 | 원천 로그 | 갱신 기대 주기 | 제안 `N` |
  |---|---|---|---|
  | `앙상블보정기_스냅샷` | `[Calibration] 앙상블 보정기 복원 완료 n=… span=… auc=… out_max=…` | 매 EOD (실측 정체 이력 **2·4·2거래일**) | **6** |
  | `MetaConf_스냅샷` | `[MetaConf] 상태 복원 완료: … (fitted=[…], total=…, ver=…)` | 매 EOD | 실측 후 결정 |
  | `앙상블가중_스냅샷` | `[EnsembleGater] 저장된 가중치 복원: …` (파일 mtime 병기) | 상시 | 실측 후 결정 |
  ⚠ `measured_since` 마커를 함께 등록한다.
  🔴 **`N`을 오늘 사건에 맞춰 고르지 않았다(313차 ④).** 도출은
  **「과거 실측 정체 구간의 최댓값 × 1.5」** 한 규칙뿐이다 — 앙상블보정기는
  `n=296`(2거래일 유지) · `n=592`(4) · `n=887`(2)이므로 **최댓값 4 × 1.5 = 6**.
  나머지 둘은 **정체 이력을 먼저 실측한 뒤** 같은 규칙으로 정한다. 사후 조정 금지.
- **기대 효과**: 정량 — 이 규칙을 소급 적용하면 `앙상블보정기_스냅샷`은 정체 6번째
  거래일인 **08-20 장전 다이제스트에서 자동으로 떴을 것**이다(정체 진행:
  08-12 ①·13 ②·14 ③·18 ④·19 ⑤·20 ⑥·21 ⑦). 실제 발견은 오늘 08-21이므로
  **발견 지연 1거래일 단축.** ⚠ 소급 계산은 **예시일 뿐 N의 근거가 아니다**(근거는 위
  1.5배 규칙). **진짜 값어치는 단축 폭이 아니라 자동화**다 — 오늘은 사람이 로그를
  눈으로 훑다가 우연히 잡았고, 그 방식은 다음번에 반복된다는 보장이 없다.
- **비용·위험**: 수집기 단독 변경(라이브 무영향, `py` 버전 무관). 공수 소(小).
  위험은 **오탐** — 정상적으로 며칠 안 바뀌는 스냅샷이 있으면 매일 주황이 뜬다.
  → §12의 기존 `benign` 화이트리스트와 같은 장치를 이 축에도 둔다.
- **선행 조건**: 없음. F-1과 독립이다(F-1이 원인을 고치고, G-1은 **다음번 다른 지표**를 잡는다).
- **우선순위 제안**: **이번 주**. 다음 26주 WFA 때 `invariants.md` §5에
  「고착 지표 감시목록」 행과 나란히 갱신 대상으로 등록한다.

### G-2. 예약 점검 발화의 실측 마진을 매일 기록한다

- **관측 근거**: O-5(장전판) 마진이 예측 47초 → **실측 12초**였다(§1-9 경고).
  cron·지터는 어제 한 번 손으로 조회한 값이고, `jitterSeconds=253`이 계속 고정이라는
  보장은 없다. 지금 상태로는 **마진이 줄어드는 것을 개장을 넘긴 뒤에야 안다.**
- **제안 내용**: 수집기 다이제스트 헤더에 이미 있는 `생성 HH:MM:SS`를 이용해,
  §1(당일 파일 인벤토리) 바로 아래에 **`발화 마진 = 09:00:00 − 생성시각`** 한 줄을
  찍고 최근 5거래일 추이를 병기한다. 저장은 기존 산출물만으로 되므로 새 상태파일이 없다.
- **기대 효과**: 마진이 12초 → 한 자리로 줄어드는 것을 **넘기기 전에** 본다.
  판정 기준: `마진 < 30초`가 2거래일 연속이면 리포트 §1에 P2로 자동 등록.
- **비용·위험**: 수집기 단독, 공수 극소. 위험 없음(관측 전용).
- **선행 조건**: 없음.
- **우선순위 제안**: **이번 주** — O-5 3거래일 판정(예정 08-25)보다 먼저 들어가야 의미가 있다.

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| **실전 전환 기준 ⑧** (사이징 재설계) | 조건 1(실전 자본 재설정) 미충족 · **선행조건 G-1(모델 레이블 상태 해제) 라이브 미검증** | **선행조건 해제** — `invariants.md` §2-B의 `MODEL_LABEL_STATE_UNLOCK_ENABLED` 관측란을 `⏳ 라이브 미검증` → **`✅ 2026-08-21 장전 실측(P-2 통과)`** 으로 갱신 | §2-B가 명시한 두 조건이 **오늘 둘 다** 관측됐다: `6/6 호라이즌 현행 레이블(fixed)` 출현(=불일치 0/6) **and** F-1 경로 로그(`전일 EOD 모델 적재 확인`) 미출현. ⚠ **`×0.6` 소멸을 근거로 쓰지 않았다** — §2-B가 금지한 오독 경로다. ⚠ **남은 해제 조건은 조건 1(실전 자본 재설정) 하나**이며 그것은 **사용자 결정 사항**이다. 본 리포트는 ⑧을 해제하지 않는다 |
| **주기적 재검증 항목** (26주 WFA) | 「고착 지표 감시목록」(468차 G-2) · 「관측 계층 5종」(470차) | **G-1 `snapshot_identity` 축을 같은 목록에 신설 등록** | 468차 G-2가 *"감시 대상 목록 자체가 낡는 것은 일일 점검이 못 잡는다"*고 등록한 바로 그 이유. 오늘 1-1은 **목록에 없어서** 6거래일 늦게 발견됐다 |
| **CB② 복원 재검토** (전환기준 ⑤) | 기한 2026-08-29 | **오늘 주간회의에 상정하되 결론을 강요하지 않는다** — 08-28 주간회의가 한 번 더 있다(§1-4) | 절대원칙 §2가 *"무기한 유예가 아니다"*로 못박았으므로 상정은 필수. 다만 "오늘이 마지막"이라는 전제는 **날짜 오기**였다 |

---

## 4. 진입 승패 사후검증

**해당 없음(장후 전용).**

## 5. 수익률 향상방안

**해당 없음(장후 전용).**

---

## 6. 오늘 장중·장후 관측 예정

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정 |
|---|---|---|---|
| **O-10** (구 O-5 장전판) | 다이제스트 생성 시각 · **발화 마진** | 09:00 이전 · **3거래일 연속이면 P-7 종결** | **1/3** (오늘 08:59:48, 마진 12초). 08-22·08-25 |
| **O-11** 🆕 | `[MarginCap] … state=OK\|CAP\|BLOCK` (477차 후속 G-2, 조회 성공 사이클마다 무조건 1줄) | 장중 **1건 이상**. 0건이면 계측 실패(P1) | **오늘 장중** |
| **O-12** 🆕 | `[Calibration] 앙상블 보정기 저장 완료 n=` / (F-1 적용 후) `저장 건너뜀` | 오늘 EOD에 **둘 중 하나는 반드시** 출현. **오늘은 F-1 미적용이므로 무로그가 예상값** — 그 자체가 1-1의 재확인이다 | **오늘 장후(15:40)** |
| **O-13** 🆕 | `data/ensemble_calibrator.pkl` mtime | 오늘 EOD 후에도 **08-11이면 1-1 지속 확정**(7거래일) | **오늘 장후** |
| **O-3** | 런처 `[GUARD] 기존 프로세스 종료 완료` 연속일 | 추이만 · 단일일 판정 금지 | 매 장전 (현재 10/10) |
| **O-4** | `[Capital]` 괴리율 | 추이만 · 단일일 판정 금지 | 매 장전 (오늘 -42.1%) |
| **1-5** | `[CORE준비도] 축퇴 N/M` | 09:00 이후 `0/6` 유지 여부 | 오늘 장중 |
| P-8·P-9·O-5(장후)·O-6~O-9 | 손익·`bar_pass`·비데몬 스레드·`entry_qty_final` 대사·메타/손절/GuardFair | 각 항목 기준 유지 | 오늘 장후 |

---

## 7. 기한 도래 항목

| 항목 | 기한 | 남은 거래일 | 조치 |
|---|---|---|---|
| **CB② 복원** (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3, 전환기준 ⑤) | **2026-08-29(토)** | **6** (08-21·24·25·26·27·28) | 🔴 **오늘 주간회의 상정.** 기한 전 금요일 회의는 **오늘과 08-28 두 번** — 오늘 결론을 강요하지 않는다(§1-4 정정). 상정 시 병기할 사실: CB②가 9999인 동안 **전환기준 ②는 CB①·③·④·⑤로만 충족 가능**하며, CB③-P4는 차단 비활성이다 |
| **전환기준 ⑨** (TOX-SEVERE-SPREAD) — 선행조건 유지 vs 26주 WFA 이관 | 기한 없음(주간회의 안건) | — | §12b `TOX스프레드(진입≥20틱)` **3일 연속 미도달(max 9.003틱)**. 473차 F-8 판정 `INSUFFICIENT`, 도달 ETA 약 7.1개월. ⚠ **문턱을 낮춰 판정하지 말 것**(458차 D6 계열 사전등록 위반) |
| **1-9 등급 임계 재설계** (앙상블 A/B) | 기한 없음(주간회의 안건) | — | §12b **9일 연속 미도달**(max 0.5878 vs 임계 0.60/0.70). 3안 중 택일 |
| **⑧ 사이징 재설계** | 기한 없음 | — | ⬆️ **선행조건 P-2 오늘 통과.** 남은 관문은 **실전 자본 재설정(사용자 결정)** 하나 |
| CB③-P4 재검토(⑥) · FP-CRITICAL 재검토(⑦) | 실전 전환 전 | — | 변동 없음. §3 설정 불변식 전부 `일치` |

---

## 8. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-08-21 (MW0602 484차 — 장전 일일 점검)`
- `NEXT_TODO.md`:
  - 완료 처리 **4건** — P-2 ✅ / O-1 ✅ / O-2 ✅ / (P-7 ⬇️ 완화·O-10으로 승계)
  - 신규 체크박스 **7건** — F-1(P1) · F-2(P2) · F-3(P2) · G-1 · G-2 · O-11 · O-12/O-13
  - 표기 정정 **3건**(F-2) — `O-5` 이중 등록 해소(장전판 → `O-10`) · 주간회의 날짜 ·
    CB② "마지막 회의" 서술. ⚠ **append 전용 규약대로 과거 줄은 고치지 않았다**
    (484차 절에 새 표기를 쓰고 `구 O-5` 연결고리를 병기)
  - `invariants.md` 정정 2건은 **문서 정정 항목으로 등록만** 했다(§2-B 관측란 · §6 ⑧ 행) —
    본 세션은 리포트·dev_memory 밖의 파일을 고치지 않는다
- 세션 차수: **484차** (원격 기준 최신 커밋 `04ff17b` = 483차 → +1, 392차 관행)
- 커밋 제목 접두: `[MW0602]`
- 📄 **스킬 템플릿 메모**: 본 리포트는 `references/report_template.md`가 지시한
  하루 한 파일·append 규약을 그대로 따랐다(`-pre` 접미사 미사용). 템플릿과 SKILL.md
  대원칙 A·B 사이에 충돌은 발견되지 않았다.

---

## 9. 사용자 조치 (장전분 — 장후 `§9p-3`에서 하루치로 통합됩니다)

| # | 지금 할 일 | 걸리는 시간 | 안 하면 생기는 일 |
|---|---|---|---|
| **1** | **오늘 장 마감(15:10) 뒤에 "F-1 구현해"라고 말씀해 주세요.** 확률 보정 장치가 매일 밤 결과를 저장하도록 고치고, 저장을 건너뛸 때는 반드시 기록을 남기게 합니다. | 제가 **약 15분**, 회귀 테스트 포함 **25분** | 판단 기준표가 8월 11일자에 계속 묶여 있고, 오늘 쌓인 자료도 밤에 버려집니다. 무엇보다 **다음에 같은 일이 생겨도 또 아무도 모릅니다.** |
| **2** | **오늘 주간회의(금요일 점검)에서 안건 3건을 확인해 주세요** — ① CB②(연속 손실 자동정지) 복원 여부, 기한 8/29 ② 극단 스프레드 차단 장치를 계속 실전 전환 조건으로 둘지 ③ 등급 A/B 기준값 재설계 3안 중 택일. | **회의 중 확인** | ①은 절대원칙이 "반드시 재론"으로 못박은 항목입니다. 다만 **오늘이 마지막 기회는 아닙니다** — 8월 28일 회의가 한 번 더 있습니다. |

> ℹ️ **하지 않아도 되는 일**: `TASK_CLAUDE_WAKE_INSTALL.bat` 실행은 **더 이상 필요 없습니다.**
> 어제 저녁 예약 시각 교정으로 원인이 직접 해결됐고, 오늘 아침 08:59:48 실행으로 효과가
> 확인됐습니다. (0820 리포트 `§9p` 표 2번은 무효 — 같은 표 3번은 여전히 유효합니다.)

> ℹ️ **커밋은 제가 했습니다** (`9e61293`). `git push`는 하지 않았습니다 — 원격 반영이
> 필요하시면 말씀해 주세요.
>
> ⚠ **작은 뒷정리 하나**: 커밋 때마다 `.git` 폴더 안에 **빈 잠금 파일**이 하나씩 남았습니다
> (`.git/index.lock.*`, `.git/HEAD.lock.*` — 커밋 4회로 십여 개).
> 제가 있는 환경에서는 파일 **삭제** 권한이 없어 이름만 바꿔 비켜 둔 것입니다.
> **git 동작에는 영향이 없습니다** — git은 정확히 `index.lock`·`HEAD.lock` 이라는 이름만
> 보고, 그 두 이름은 **지금 존재하지 않는 것을 확인했습니다**(작업 트리도 깨끗합니다).
> 지우고 싶으시면 `del .git\*.lock.*` 한 줄이면 됩니다.
>
> **당일 커밋 2건**: `9e61293`(점검 본문·dev_memory·어제 미커밋분) ·
> `443d036`(리포트 보완).

---

*본 리포트는 예약 점검(자동 실행)으로 작성됐다. **라이브 코드·설정 변경은 하지 않았다** —
산출은 보고서 · dev_memory 갱신 · 문서 정정(F-2) · 미커밋분 커밋(F-3)까지다.*
