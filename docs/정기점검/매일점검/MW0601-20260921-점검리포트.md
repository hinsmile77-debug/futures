# 미륵이 일일 점검 — MW0601 / 2026-09-21

# 장전(pre)

- 점검 시각: 09:00~09:10 KST (예약은 08:57 발화, 실제 확인은 개장 직후 로그까지 포함) · PC `MW0601`
- 브랜치 `v9-dev`(정상 — MW0601 운영 브랜치와 일치) · HEAD `3067c8b` · 미커밋 707건(실질 변경은 3건뿐 — 1-4 참조) · 당일 커밋 0건(개장 전이라 정상)
- ⚠ **`.git/index.lock` STALE 확정 — 이 점검 세션이 회수 시도했으나 권한 없어 실패 (1-1 참조, 사용자 조치 1번)**
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260921_pre.md`

## 0. 오늘 한눈에

오늘 아침 시스템은 정상적으로 켜졌고, 프로그램이 지켜야 할 안전 규칙들도 지금까지는 전부 제자리에 있습니다. 아직 장이 시작한 지 2~3분밖에 안 지나서 실제로 사고판 거래는 없습니다(정상입니다 — 아직 이른 시간입니다). 오늘 거래 자체에 위험한 문제는 없습니다. 다만 이번 점검 작업 도중에 저장소에 파일 잠금이 하나 남았는데, 이 점검 세션이 있는 환경에서는 그 잠금을 스스로 풀 권한이 없어 사용자가 직접 지워줘야 합니다(코드 변경 내용과는 무관하고, 지우기 전까지는 이 저장소에 새로운 커밋을 할 수 없는 상태입니다). 그리고 며칠째 이어지고 있던 작은 결함 하나가 이번엔 다른 시점(휴장일인 지난 일요일)에 다시 나타난 것을 확인했는데, 이것도 실제 돈에는 영향이 없습니다. 정리하면 사용자가 지금 해야 할 일은 파일 잠금 해제 한 가지이고, 나머지는 장중·장후 점검에서 계속 지켜볼 사항들입니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 정상 | 브랜치·환경·안전장치 설정 전부 일치, P0 없음. 개장 직후라 거래 실적은 아직 없음 |
| 장중 | (장중 점검에서 채움) | |
| 장후 | (장후 점검에서 채움) | |

**절대원칙 준수 여부** (장전 시점 — 장중·장후 실측이 필요한 항목은 "장중/장후 판정"으로 표기)

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | 장후 판정 | 아직 15:10 전 — 판정 대상 아님 |
| ② | Circuit Breaker 5종 | 정상(설정) | `CB_CONSEC_STOP_LIMIT=3`(복원 완료) 등 관련 상수 전부 기대값과 일치. 발동 여부는 장중 관찰 |
| ③ | CORE 피처 호라이즌 그룹별 | 정상(설정) | 단기/중기/장기 CORE 설정 파일 구조 이상 없음. 실제 체크리스트 통과 여부는 장중 판정 |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 해당 없음 | 오늘 코드 변경 없음(장전 규칙상 코드 미접촉) |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 해당 없음 | 오늘 코드 변경 없음 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 정상 | 자동 통합 흔적 없음(로그에 관련 활동 없음) |

**설정 불변식** — 수집기 §3 표(절대원칙·한시예외 상수 24종) 전부 `일치`. 34개 진입/차단 게이트 중 9개가 의도적으로 꺼져 있는 상태(CB③-P4, FP-CRITICAL, TOX-SEVERE-SPREAD 등 — 전부 CLAUDE.md에 근거가 있는 기존 한시예외)이며, 오늘 새로 바뀐 값은 없음. **이상 없음.**

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

없음.

### P1 — 정확성·관측 훼손

#### 1-1. 이번 점검 세션이 저장소에 파일 잠금(`.git/index.lock`)을 남겼다 — 사용자가 직접 지워야 한다

- **증상**: 점검 작업 도중 09:01:49에 `.git/index.lock`(0바이트)이 생성됐다. 이 점검 세션은 계속 `--no-optional-locks` 옵션을 붙여 git 명령을 실행했음에도 생겼다 — 정확히 어느 명령이 만들었는지는 특정하지 못했다. `scripts/git_lock_guard.py --check`로 스테일(사용 중이 아닌 오래된 락) 확정까지는 했지만, 이 점검 세션이 도는 환경(리눅스 샌드박스, 마운트 경유)에서는 파일 삭제 권한 자체가 없어 직접 회수를 시도했으나 실패했다.
- **근거**:
  ```
  $ python scripts/git_lock_guard.py --check
  futures                  STALE 스테일 확정 — 0바이트 · 0.2시간 · git 프로세스 0개 -> 이 저장소는 커밋 불가 상태다
  (종료코드 2)

  $ python scripts/git_lock_guard.py --reclaim
  futures                  STALE 회수 실패: [Errno 1] Operation not permitted:
    '/sessions/.../futures/.git/index.lock'
  (종료코드 2)
  ```
- **기준 위반**: SKILL.md §0 "git 호출 규약" — 이 세션 자신이 규약을 지켰는데도 락이 남은 사례. `collect_evidence.py` 자체 실행(09:00경)은 자가점검에서 "이 수집 실행은 락을 만들지 않았다"고 기록했으므로, 락은 그 이후 이 세션이 직접 실행한 후속 확인 명령들 중 하나에서 생긴 것으로 보인다(특정 실패).
- **영향**: 오늘 거래에는 영향이 없다(장전 규칙상 어차피 이 세션은 커밋을 하지 않는다). 다만 락이 남은 채로 있으면 **사용자를 포함해 이 저장소에 대한 어떤 커밋도 불가능**하다 — 오늘 장중·장후 점검이나 다른 작업이 커밋을 시도하면 그때 막힌다.
- **영향(기술)**: `git_lock_guard.py --check`가 `STALE`(종료코드 2)로 확정했으므로 판정보류(HOLD)는 아니다 — 즉 지워도 실행 중인 git을 방해하지 않는다. 회수만 이 세션의 리눅스 샌드박스 마운트 제약(`unlink` EPERM)으로 실패했을 뿐이다.
- **신규 여부**: 신규 — 원인(어느 명령이 만들었는지)은 미상이나, 같은 유형의 사고가 2026-08-21·08-24·08-26·09-17·09-18에도 있었던 반복 패턴이다.

#### 1-2. 세션 전환 시 완료 마커가 사라지는 결함(SessionStateDrop) — 이번엔 휴장일(일요일)에 발생해 오늘 아침 점검으로는 안 보였다

- **증상**: 미륵이는 프로그램을 재시작할 때마다 "어제 학습이 잘 끝났는지"를 알려주는 표식 두 개(`p8_last_success_date`, `eod_retrain_ok_date`)를 새 날짜로 넘겨받아야 하는데, 날짜가 바뀌는 순간 새 기록을 통째로 새로 만들면서 이 표식들을 이어받지 못하고 잃어버리는 결함이 2026-09-04부터 계속 재현되고 있다. 어제(9/18, 금요일) 점검 시점 기준 11거래일 연속 재현이었고, 오늘 아침 점검에서는 이 결함을 알리는 경고 로그(`[SessionStateDrop]`)가 **전혀 보이지 않았다.** 언뜻 "드디어 고쳐졌나" 싶지만, 실제로 확인해보니 그렇지 않다 — **결함은 어제가 아니라 지난 일요일(9/20, 거래 없는 날)에 이미 한 번 더 일어났고**, 그 결과 오늘 아침 재시작 시점에는 잃어버릴 표식이 애초에 하나도 남아있지 않아서 경고 자체가 뜰 일이 없었을 뿐이다.
- **근거**:
  ```
  # 9/20(일, 휴장일) 14:09:42 — 실제 재현 시점
  logs/20260920_WARN.log:8: [WARNING] SYSTEM: [SessionStateDrop] 완료 마커 소실
    ['p8_last_success_date', 'eod_retrain_ok_date'] — 날짜 전환(2026-09-18 → 2026-09-20)이
    새 딕셔너리를 만들면서 이어받지 않았다 (호출부=session_recovery_service.py:increment_session)

  # 9/21(월) 08:41:10 — 오늘 아침. "이어받은 키 5개 / 새로 초기화된 키 0개"로 찍혀
  # 잃어버릴 표식이 이미 없었음을 보여준다(경고가 안 뜬 이유)
  logs/20260921_SYSTEM.log:49: [INFO] SYSTEM: [SessionRollover] 2026-09-20 → 2026-09-21
    전환 — 이어받은 키(5개)=[...] / 새로 초기화된 키(0개)=없음

  # 현재 상태 파일에도 두 표식이 아예 없음(값이 0/거짓이 아니라 키 자체가 없음)
  data/session_state.json: {"date":"2026-09-21","count":1,"reverse_entry_enabled":false,
    "tp1_single_contract_mode":"atr_profit","auto_shutdown_done_date":"","today_open":1094.06}
  ```
- **기준 위반**: 계측 4원칙 ④(폴백 가시화 — 상태 소실이 조용히 일어나면 안 된다). 관련 항목: `dev_memory/NEXT_TODO.md` F-1(538-4), 최초 등록 2026-09-04(532차).
- **영향**: 실제 매매나 손익에는 영향이 없다 — 시스템은 이 표식이 없어도 EOD(장마감 후) 재학습이 끝났는지를 파일로 다시 한번 직접 확인하는 예비 경로가 있어서(`[PreRetrain] EOD 마커 파일 직접 확인 (3일 전: 2026-09-18) → PreRetrain 스킵`, 오늘 08:55:10 정상 동작 확인), 오늘 아침 재학습을 중복으로 돌리는 실수는 일어나지 않았다. 다만 이 표식은 원래 "재학습이 왜 이상하게 됐는지" 진단할 때 쓰는 것이라, 문제가 생겼을 때 원인 추적이 어려워지는 것이 계속되는 부작용이다.
- **영향(기술)**: `session_recovery_service.py:increment_session()`이 날짜 전환 시 `data = {...}` 로 새 dict를 통째로 생성해 `_MARKER_KEYS`(`p8_last_success_date`, `eod_retrain_ok_date`)를 계승하지 않는 구조적 결함(F-1/538-4)이 이번엔 정규 거래일이 아닌 9/20(일) 14:09:42의 비정규 기동(다른 세션이 9월 백필 작업 중 프로세스를 띄운 것으로 추정 — 같은 날 607·608차 커밋과 시각상 인접)에서 발생했다. `[PreRetrain]`의 파일 기반 폴백(`main.py:6118` 부근)이 기능적 피해를 막고 있으나, F-1(538-4) 마커 캐리오버 수정 자체는 여전히 **사용자 승인 대기** 상태다.
- **신규 여부**: 기존 TODO(F-1/538-4, 2026-09-04 최초 등록) — **재발 지속**. 다만 "재현 시점이 정규 거래일이 아닌 휴장일이었다"는 것은 이번에 새로 확인된 사실이다.

### P2 — 운영 부담·기술부채

#### 1-3. 장 시작 직후 헬스 정책이 두 차례 선제 차단 신호를 냈다 — 개장 초반의 흔한 지연 패턴으로 보이나 장중에 계속 지켜볼 것

- **증상**: 개장 1~3분 뒤인 09:01:01, 09:03:01에 시스템의 자체 건강 감시 기능(HealthPolicy)이 "최근 지연이 계속 쌓이고 있다"고 판단해 선제적으로 저신뢰 진입을 막는 신호를 두 번 냈다.
- **근거**:
  ```
  logs/20260921_WARN.log:200: 09:01:01 [WARNING] SYSTEM: [HealthPolicy] Degraded 선제차단:
    streak=1.00+1.00 ≥ 2 (latency=2169ms quality=0.86 cache=0s exc10m=0) | cause=S6(1155ms)
  logs/20260921_WARN.log:504: 09:03:01 [WARNING] SYSTEM: [HealthPolicy] Degraded 선제차단:
    streak=1.70+0.35 ≥ 2 (latency=1085ms quality=0.94 cache=0s exc10m=0) | cause=S5(534ms)
  ```
  같은 시간대 실제 헬스 상태 로그는 `degraded=OFF`로 찍혀 있어(`20260921_HEALTH.log`), 전면 진입금지까지는 가지 않았다.
- **기준 위반**: 명시적 위반은 아님 — 헬스 정책은 설계대로 동작(`HEALTH_DEGRADED_BLOCK_AUTO_ENTRY=True`가 정상값). 다만 장중 확인 필요.
- **영향**: 오늘 아직 진입이 한 건도 없는 시간대(개장 2~3분)라 실제로 놓친 기회가 있었는지는 알 수 없다. 다만 이 패턴이 개장 직후 파이프라인 지연(파이프라인 총 소요 2169ms, 기준 1000ms 초과 — 이른바 "장시작 버스트" 구간)과 겹쳐 있어, 장 시작 후 몇 분간은 신뢰도 낮은 신호가 자동 진입에서 한 번 더 걸러지는 효과가 있을 수 있다.
- **영향(기술)**: `CB⑤`(파이프라인 지연 서킷브레이커)는 개장 버스트 구간에서 임계값이 9초로 완화되어 미발동(정상)이지만, `HealthPolicy`의 선제차단 스트릭 카운터는 별도 로직이라 CB⑤와 무관하게 쌓인다. cause가 `S0`(모델 리로드)이 아니라 `S6`·`S5`(다른 파이프라인 단계)인 점이 기존에 문서화된 "모델 선로드 지연" 패턴과는 다르다 — 원인 단계가 다르므로 기존 F-1p류 항목과 동일시하지 말 것.
- **신규 여부**: 신규 관찰(O-p1로 등록, 장중 절에서 판정) — 개장 초반 흔한 패턴이지만 cause 단계가 기존 사례와 달라 지켜볼 필요.

#### 1-4. 미커밋 코드 3건 지속 — 다른 세션 작업 중, 오늘도 그대로

- **증상**: `features/levels/levels_store.py`, `features/levels/premarket_levels.py`, `scripts/cybos_autologin.py` 3개 파일에 실질적인 코드 변경(줄바꿈이 아닌 진짜 내용 변경)이 커밋되지 않은 채 남아 있다.
- **근거**: `git diff -w`(줄바꿈 차이 제외) 기준 이 3개 파일만 실질 변경으로 잡힘 — `levels_store.py` +69/-1줄, `premarket_levels.py` +24/-7줄, `cybos_autologin.py` +33/-14줄. 나머지 707건은 전부 리눅스 환경의 줄바꿈(CRLF/LF) 차이로 생긴 표시상의 변경이며 실제 내용은 그대로다(`git diff --stat -w` 결과 위 3개 외 0건).
- **기준 위반**: 절대원칙 위반은 아님. 이미 2026-09-18 점검부터 "타 세션 작업 중"으로 확인·기록된 항목.
- **영향**: 없음 — 다른 작업 세션이 진행 중인 코드이며, 라이브 프로세스는 이미 저장된 코드로 정상 동작 중이다.
- **영향(기술)**: `dev_memory/DECISION_LOG.md`·`NEXT_TODO.md`(606-3)에 "타 세션 작업 중 + 사용자가 diff 검토 후 커밋" 으로 이미 등록되어 있다.
- **신규 여부**: 기존 TODO(606-3) — 지속.

### 확인 필요 (확정 아님)

- 없음 — 오늘 아침 시점에서 표본 부족으로 판정을 보류해야 하는 항목은 없었다(거래 실적 자체가 아직 없어 판정 대상이 성립하지 않는 항목들은 위 표에서 "장중/장후 판정"으로 이미 구분해 두었다).

### 이미 반영된 사안 (신규 아님)

- **`[출처축] 미분류 entry_source ['PHANTOM_STATE_ARTIFACT']` 경고 2건(08:41:09)** — 언뜻 새로운 이상 거래출처로 보이지만, `PHANTOM_STATE_ARTIFACT`는 2026-08-xx(554차)에 이미 "테스트 과정에서 심어진 상태파일 흔적이며 실제 거래가 아니다"로 분류·등록되고 시스템 성과 집계에서 제외 처리된 라벨이다(`config/constants.py:249`, `dev_memory/DECISION_LOG.md` 43271행 부근에 "기존 등록 사안, 신규 아님"으로 명시). 이 경고는 그 과거 데이터에 대한 분류 점검이 매 기동 시 반복 실행되며 나오는 것으로, 신규 결함이 아니다.
- **`[ChartDBG] paintEvent slow` 대량 경고(09:00~09:02 사이 386건)** — 대시보드 화면 그리기 지연 경고로, 이미 `dev_memory/NEXT_TODO.md`에 F-1p(P1)로 등록되어 3일 이상 증가 추세가 추적 중인 기존 이슈다. 오늘 3분간 386건은 최근 며칠간(하루 3만~4만 건 수준)에 비하면 적은 편으로, 새로운 악화는 아니다.

---

## 2. Fix 작업 구현계획

오늘 아침 시점에서 즉시 코드 수정이 필요한 P0/P1 결함은 없다. 아래는 **계획만** 세워두고 실행하지 않는다(장전 규칙 — 라이브 프로세스가 돌고 있어 코드를 건드리지 않는다).

### F-1. `SessionStateDrop` 마커 캐리오버 수정 — 이미 계획은 서 있음, 실행은 사용자 승인 대기

- 이 항목은 이번 세션이 새로 설계하는 것이 아니라, 2026-09-04(532차)부터 이미 `dev_memory/NEXT_TODO.md`에 F-1(538-4)로 구현 방향까지 정리되어 있고 **사용자 승인만 기다리는 상태**다. 오늘 새로 밝혀진 것은 "재현 시점이 이번엔 정규 거래일이 아니라 지난 일요일이었다"는 사실뿐이므로, 별도의 새 Fix를 만들지 않고 기존 F-1(538-4) 승인 여부를 다시 한번 사용자에게 확인 요청하는 것으로 처리한다(아래 "사용자 조치" 참조).
- **결정 필요 사항**: F-1(538-4) 구현 승인 여부 — 12일 넘게 미결.

### 적용 순서와 커밋 계획

- 오늘 아침 세션에서 커밋할 코드 변경 없음.

---

## 3. 고도화 방안

당일 관측에서 새로 나온 제안 없음. 개장 2~3분 시점의 장전 점검이라 손익·요인 데이터가 아직 없다(제5부 수익률 향상방안은 장후에 작성).

---

## 4. 진입 승패 사후검증 (장후 한정)

해당 없음(장전) — 장후 절에서 작성.

## 5. 수익률 향상방안 (장후 한정)

해당 없음(장전) — 장후 절에서 작성.

---

## 6. 다음 거래일 관측 예정

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| O-p1 | `[HealthPolicy] Degraded 선제차단` 발생 빈도·`cause` 단계 | 하루 종일 소수(개장 버스트 구간 한정)면 정상, 장중 지속·자동진입 실제 차단 발생 시 P1 격상 | 오늘 장중·장후 |
| O-p2 | `[SessionStateDrop]` 다음 재현 시점 | 다음 재현이 정규 거래일 기동인지 비거래일 기동인지 — F-1(538-4) 승인 우선순위 판단 자료 | 다음 재현 시 |

## 7. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| CB② 발동 1회 관측 (값 복원은 2026-09-02 완료, `=3`) | 상시 관측 | — | 아직 라이브 발동 미관측 — 인위적으로 유발하지 말 것(CLAUDE.md 명시) |
| ATR_MIN_ENTRY 재보정 (2026-09-07 등록, 초회 실행 FAIL) | 다음 26주 WFA 주기 | 미도래 | 조치 불필요(주기 항목) |

---

## 8. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-09-21 (MW0601 609차 — 장전 점검)`
- `NEXT_TODO.md` 추가 체크박스: 2건(O-p1, O-p2)
- 완료 처리한 기존 항목: 없음(F-1/538-4는 여전히 승인 대기 — 닫지 않음)

---

## 사용자 조치 (장전 시점 — 거래 위험은 없으나 1번은 저장소 커밋을 막고 있다)

| # | 할 일 | 명령·경로 | 걸리는 시간 · 안 하면 생기는 일 |
|---|---|---|---|
| 1 | `.git/index.lock` 회수 | Windows PC에서 `python scripts\git_lock_guard.py --reclaim` (이미 STALE 확정됨 — 그대로 지워도 안전) | 1분. 안 하면 이 저장소에 어떤 커밋도 할 수 없다(오늘 장중·장후 점검·다른 작업 전부 영향) |
| 2 | `SessionStateDrop` 마커 캐리오버 수정(F-1/538-4) 승인 여부 결정 — 2026-09-04부터 12거래일 이상(휴장일 재현 포함) 미결 | `dev_memory/NEXT_TODO.md` F-1(538-4) 항목 참조, 구현은 `strategy/runtime/session_recovery_service.py` | 검토 5~10분. 실손해는 없지만, 승인 없이 두면 앞으로도 계속 같은 결함이 반복 보고된다 |
| 3 | 미커밋 코드 3건(`levels_store.py`·`premarket_levels.py`·`cybos_autologin.py`) diff 검토 후 커밋 여부 결정 | `git --no-optional-locks diff -w -- features/levels/levels_store.py features/levels/premarket_levels.py scripts/cybos_autologin.py` | 검토 10분. 미루면 다른 세션 작업과 계속 혼재된 상태로 남는다(실거래에는 영향 없음) |

> **언제**: 이번 장전 점검이 `python scripts/git_lock_guard.py --check`로 **STALE(종료코드 2)**를 확인했는데, 이 점검 세션(리눅스 샌드박스)은 마운트 경유 `unlink`가 `EPERM`으로 거부돼 락을 회수할 권한이 원천적으로 없다(`--reclaim` 시도 결과: `Operation not permitted`). Windows PC에서 직접 실행하면 정상 회수된다.
> ⚠ 판정보류(HOLD·종료코드 3)였다면 이 항목을 넣지 않았을 것이다 — 지금은 STALE이 확정된 경우에만 해당한다.

이 절이 발급한 번호: 이상점 1-1~1-4 · 관측 O-p1~O-p2

---

# 장중(intra)

- 점검 시각: 09:00~12:35 KST (증거 수집 12:27, 원본 로그 재확인은 12:35경까지) · PC `MW0601`
- 브랜치 `v9-dev`(정상) · HEAD `850cfad` · 당일 커밋 4건(장전 이후 3건 추가 — 아래 1-5 참조)
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260921_intra.md`

## 0i-C. 이월 처리 — 장전 항목 처분

| 원 번호 | 제목(줄임) | 원 국면 | 처분 | 근거 |
|---|---|---|---|---|
| 1-1 | `.git/index.lock` 회수 불가(스테일) | 장전 | ✅ 해소 | `python scripts/git_lock_guard.py --check` 재실행 결과 `OK 정상 — 락 없음`(12:27 확인). 이 점검 세션은 이번엔 락을 만들지 않았고, 장전이 남긴 락도 사라졌다 — 사용자가 직접 지웠거나, 12:16·12:24의 프로세스 재기동(아래 1-5) 과정에서 다른 git 동작이 우연히 정리한 것으로 보인다(정확한 회수 주체는 미상이나 결과는 확인됨). 사용자 조치 1번 해제 |
| 1-2 | `SessionStateDrop` 재발(9/20 비거래일) | 장전 | 🔄 지속(원인 미해결) | 오늘 로그 8종 전량(`grep -rn SessionStateDrop logs/20260921_*.log`) 재확인 — 0건. 다만 이것은 "해소"가 아니다 — 넘겨받을 마커(`p8_last_success_date`·`eod_retrain_ok_date`) 자체가 이미 없어서 오늘의 5차례 재기동(1-5 참조) 중 단 한 번도 "드롭"이 일어날 수 없었을 뿐이다(`data/session_state.json` 12:35 확인 — 두 키 여전히 부재). 근본 원인(F-1/538-4, `session_recovery_service.py:increment_session()`)은 그대로다. 원 번호 계승 |
| 1-3 | 개장 초반 HealthPolicy 선제차단(O-p1) | 장전 | 🔄 지속 | 장전 2건(09:01·09:03)에 이어 장중에 2건 추가 확인 — 09:08:00(cause=S7, 920ms), **11:42:01(cause=S4, 861ms)**. 즉 "개장 버스트 구간 한정"이 아니었다 — 11:42는 점심시간대다. 다만 하루 총 4건으로 빈도는 낮고, 오늘은 애초에 진입이 0건(체크리스트 통과 0건)이라 이 선제차단이 실제로 어떤 자동진입을 막았는지는 인과적으로 분리할 수 없었다. 장후에 최종 판정(P1 격상 여부) |
| 1-4 | 미커밋 코드 3건 지속 | 장전 | 🔄 지속 | `git --no-optional-locks diff --stat -w` 재확인 — 동일 3파일(`features/levels/levels_store.py`·`premarket_levels.py`·`scripts/cybos_autologin.py`)만 실질 변경, 변경량 동일. 타 세션 작업 계속 진행 중으로 판단 |
| O-p1 | HealthPolicy 선제차단 판정 | 장전 | 판정 보류 → 장후 이월 | 위 1-3 참조. 장후에 하루 전체 건수·자동진입 실제 차단 여부로 최종 판정 |
| O-p2 | SessionStateDrop 다음 재현 시점 | 장전 | 판정 보류 → 다음 재현 시까지 이월 | 오늘은 재현 자체가 없었음(위 1-2 참조 — 넘길 마커가 없어 드롭이 발생할 수 없는 상태가 지속) |

**미처분 0건.**

## 0i. 오늘 한눈에 (갱신)

정오를 넘긴 지금까지 실제로 사고판 거래는 한 건도 없습니다(체크리스트를 통과한 진입 신호 자체가 0건 — 오늘 아침부터 변동성이 낮아 대부분의 신호가 "믿을 만큼 확실하지 않다"는 이유로 스스로 걸러졌습니다. 이것은 프로그램이 고장난 것이 아니라 설계대로 조심하고 있는 것입니다). 장전 점검에서 남겼던 파일 잠금 문제는 지금 확인해보니 저절로 풀려 있어 더 이상 조치할 필요가 없습니다.

다만 점검 중에 새로 발견한 것이 하나 있습니다 — 낮 12시 13분부터 12시 24분 사이에 이 프로그램과는 별개로 진행 중이던 다른 개발 작업이, 실제로 거래 중인 프로그램에 새 기능(옵션 시장의 개인 투자자 동향을 더 정확히 수집하는 기능)을 반영하면서 프로그램을 세 번 새로 켰습니다. 그런데 그 과정에서 코드 오타 하나 때문에 미결제약정(시장에 아직 청산되지 않고 남아있는 계약 수 — 시장 분위기를 읽는 데 쓰이는 참고 지표 중 하나) 갱신 기능이 약 7~8분간 멈췄습니다. 다행히 이 시간 동안 실제 거래는 전혀 없었고, 프로그램의 핵심 매매 판단 기능 자체는 계속 정상적으로 돌고 있었습니다. 문제를 만든 쪽에서 스스로 발견해서 12시 23분에 바로 고쳤고, 재확인한 결과 12시 29분부터는 정상적으로 다시 수집되고 있습니다. 실제 손해는 없었지만, 거래시간 중에 이런 변경과 재시작이 있었다는 사실 자체는 기록해 둡니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 정상 | (장전 절 참조) |
| 장중 | 조건부 정상 | P0 없음·실손해 없음. 다만 장중 라이브 코드 배포·재기동 3회(그중 1회는 8분간 보조 데이터 수집 중단을 유발) 및 HealthPolicy 선제차단 재확인(O-p1)이 관측됨 — 아래 1-5 참조 |
| 장후 | (장후 점검에서 채움) | |

**절대원칙 준수 여부** (장중 시점)

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 | 장후 판정 | 아직 12:35 — 판정 대상 아님 |
| ② | Circuit Breaker 5종 | 정상 | 오늘 실제 발동(HALT) 0건. CB⑤(파이프라인 지연) 경고 10건은 전부 09:00~11:41 사이 "장시작 버스트" 구간 완화 임계(9초) 안에서 소화됐고, 하나는 이 점검 자신의 원인 조사 과정(11:41, 자가유발)이었다 — CLAUDE.md 명시대로 실전전환기준 ②의 근거로 쓰지 않는다 |
| ③ | CORE 피처 호라이즌 그룹별 | 판정 대상 없음 | 오늘 진입 자체가 0건이라 체크리스트 통과 사례에서 위반 여부를 확인할 대상이 없다. `vwap` 미통과 진입(강제 X 위반) 0건 |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 정상 | `0xC0000409`·`STACK_BUFFER` 관련 로그 0건 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 해당 없음 | 오늘 이 경로 관련 코드 변경 없음(1-5의 변경은 무관한 파일) |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 정상 | 관련 흔적 없음 |

---

## 1. 이상점 정리보고 (장중 신규분)

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

없음.

### P1 — 정확성·관측 훼손

#### 1-5. 장중에 라이브 프로세스가 세 차례 새 코드로 재기동됐고, 그중 한 번은 코드 오류로 보조 데이터 수집이 약 8분간 멈췄다

- **증상**: 이 점검 세션과 무관한 별도 작업 세션이 정규장 중(09:56~12:23) 코드를 3차례 커밋하고 그때마다 라이브 프로세스를 재기동했다(오늘 하루 총 재기동 5회 — 08:41 최초 기동 포함, 세부는 아래). 그중 세 번째(12:13 커밋 → 12:16:24 재기동)는 신설한 "옵션 위클리 수급 수집" 함수 안에서 설정 모듈 이름을 잘못 참조한 `NameError`가 있었고, 이 예외가 try 블록 밖에서 터지는 바람에 그 뒤에 있던 미결제약정(OI) 동기화 코드까지 실행되지 못했다. 같은 오류가 12:16:24부터 12:23:24까지 8분간 분당 2회(총 16회) 반복됐고, 12:23:03에 원인을 수정한 커밋이 올라간 뒤 12:24:07~12:24:29에 네 번째 재기동으로 반영됐다. 이후 같은 오류는 재발하지 않았고, 보조 수집 DB(`option_flow.db`)도 12:29에 갱신이 재개된 것을 확인했다.
- **근거**:
  ```
  # 오늘 재기동 타임라인 (logs/20260921_WARN.log · logs/20260921_SYSTEM.log)
  08:40:33  최초 기동 (PID 3604)
  09:59:50  재기동 (PID 15032) — 09:56:46 커밋 1a16a15(609차, 표시 전용) 반영
  10:21:47  재기동 (PID 15336) — 10:19:02 커밋 45938ef(610차, 표시 전용) 반영
  12:16:00  재기동 (PID 26276) — 12:13:34 커밋 da2506d(611차, 옵션 위클리 수급 수집 신설) 반영
             → 12:16:24 [ERR-DEGRADED] investor_timer_fetch: name 'settings' is not defined
                (main.py:4659 _fetch_weekly_option_flow, self._fetch_weekly_option_flow() 호출이
                 OI 동기화 코드보다 앞에 있어 그 뒤 코드가 실행되지 않음)
             → 같은 오류 12:16:24~12:23:24, 8분간 16회(분당 2회) 반복
  12:24:07  재기동 (PID 26196) — 12:23:03 커밋 850cfad(611차 후속, 수정) 반영
             → 이후 ERR-DEGRADED 재발 0건(12:35 현재까지 재확인)
             → data/db/option_flow.db 최종 갱신 12:29 (수정 반영 후 정상 재개 확인)
  ```
  `logs/20260921_HEALTH.log` 12:20:01~12:23:00 구간에 `exc_tags=[ERR-DEGRADED]×4~7`이 누적으로 찍혀 있으나 `degraded=OFF`(전면 진입금지 아님)는 유지됐다.
- **기준 위반**: 국면별 체크리스트 B-7("장중 코드 배포·재기동 흔적 없는가")에 해당하는 사건 — 단, 배포·재기동을 실행한 것은 이 점검 세션이 아니라 별도 작업 세션이다. 계측 4원칙 ④(폴백 가시화)와도 맞닿아 있다 — `NameError`가 try 블록 밖에 있어 수집기 자신의 실패 경고(`[OptionFlow] 수집기 기동 실패`)조차 못 찍고 엉뚱한 이름(`ERR-DEGRADED investor_timer_fetch`)으로만 드러났다(해당 세션이 커밋 메시지에 이미 이 문제를 인지·기록함).
- **영향**: 실제 손해는 없었다. 이 8분 동안(12:16~12:23) 진입 신호 자체가 없었고(오늘 진입 0건은 하루 종일 그렇다), 매매 판단을 내리는 핵심 파이프라인(피처 생성→예측→진입 판단)은 이 문제와 무관하게 계속 정상 동작했다(같은 시간대 `Health` 로그 `degraded=OFF`, 파이프라인 지연 지표 정상). 영향을 받은 것은 "옵션 시장에서 개인 투자자가 얼마나 사고팔았는지"를 보여주는 보조 참고 지표 하나뿐이며, 이 지표는 아직 매매 판단 모델에 반영되지도 않은 신규 기능(오늘 처음 도입, 수집 전용 단계)이었다.
- **영향(기술)**: `main.py:_fetch_investor_data()` 호출 체인에서 `_fetch_weekly_option_flow()`(신규, 4621행에서 호출)가 `self.investor_data._open_interest = oi` 갱신 라인보다 **앞**에 배치돼 있었고, 그 함수 내부의 `getattr(settings, "WEEKLY_OPTION_FLOW_ENABLED", False)`가 `settings`라는 미정의 이름을 참조해(`main.py`의 설정 별칭은 `runtime_settings`) `NameError`를 던졌다. 이 예외가 상위 `except`에서 `investor_timer_fetch` 라벨로 뭉뚱그려 잡히면서 원인 파악이 늦어졌다. 수정 커밋(`850cfad`)이 ① 이름을 `runtime_settings`로 정정 ② 함수 전체를 자체 `try`로 감싸 실패를 `[OptionFlow]` 이름으로 명확히 드러나게 함 ③ 호출 순서를 OI 동기화 **뒤**로 이동 — 세 가지를 함께 처리했고, 회귀 테스트 `tests/test_611_option_flow_wiring.py`(5건)로 재발 방지 장치를 남겼다. 신설 플래그 `WEEKLY_OPTION_FLOW_ENABLED=True`는 오늘 처음 켜졌다(증거 §3 차단 게이트 인벤토리에서 확인) — 아직 `features/`·`model/`에 연결되지 않은 수집 전용 단계다.
- **신규 여부**: 신규(오늘 최초 발생·최초 수정, 별도 세션의 자체 발견·자체 수정). 이 점검 세션은 발생 사실을 사후 관측만 했다.

### P2 — 운영 부담·기술부채

#### 1-6. 장중 재학습 2회, 표시 전용 커밋 2회 — 정상 범위의 활동

- **증상**: 오늘 장중재학습(`retrain_intraday`)이 10:00:11과 12:16:21 두 차례 돌았다(둘 다 6개 호라이즌 성공, 각 41.8초·42.3초 소요). 이는 STEP 3 재학습이 "30분 주기"가 아니라 `[WarmupRetrain]`(세션 재시작 감지) 같은 이벤트로 트리거된다는 CLAUDE.md 483차 정정과 일치한다 — 10:00·12:16 재기동 직후에 각각 발동했다. 10:22 재기동 직후에는 재학습 로그가 없는데, 이는 "최근 재학습 직후면 스킵"(10:00 재학습이 22분 전) 정상 스킵 조건에 해당하는 것으로 보인다.
- **근거**: `logs/retrain_intraday_20260921_100011.log`, `logs/retrain_intraday_20260921_121621.log` — 둘 다 `성공=6/6 호라이즌`. `data/session_state.json`의 `gbm_total_retrain_count=2`가 이를 뒷받침.
- **기준 위반**: 없음 — 설계대로 동작.
- **영향**: 없음.
- **영향(기술)**: 없음.
- **신규 여부**: 정상 활동 기록(이상점이 아니라 참고 사실로 등재).

### 확인 필요 (확정 아님)

- 없음.

### 이미 반영된 사안 (신규 아님)

- **수집기 자동 적신호 §11-3·4 "매분 루프 커버리지 210/371분(56.6%)" 및 "12:30~15:10 연속 161분 공백"** — 실제 결함이 아니다. 증거 수집이 12:27~12:29에 실행됐고 장은 아직 15:10에 마감하지 않았으므로, 12:30 이후 구간은 "아직 일어나지 않은 시간"이라 로그가 없는 것이 당연하다. 자동 적신호 산식이 국면 종료 전 실행을 고려하지 않아 생기는 오탐이며, 실제 매분 루프 공백이 아니다(§7 앵커 표에서 08:55~15:12 구간 "10분 이상 공백 0건"으로 별도 확인됨).

---

## 2. Fix 작업 구현계획

이번 장중 점검에서 이 세션이 직접 낼 새 Fix는 없다 — 1-5의 원인은 이미 해당 작업 세션이 스스로 진단·수정·회귀테스트까지 완료했고(커밋 `850cfad`), 이 점검 세션이 추가로 더할 것이 없다. 다만 재발 방지를 위한 제안 하나를 고도화 방안(G-1)으로 남긴다.

### 적용 순서와 커밋 계획

- 이번 장중 세션에서 커밋할 코드 변경 없음(장중 규칙 — 코드 변경 실행 금지).

---

## 3. 고도화 방안

### G-1. 장중 라이브 배포 시 "회귀 테스트 선행 + 저부하 시간대 우선" 관행을 문서화 제안

- **관측 근거**: 오늘 1-5에서, 이미 회귀 테스트(`tests/test_611_option_flow_wiring.py`)까지 갖춘 커밋임에도 실제 배포(재기동) 이후에야 `NameError`가 드러났다. 테스트가 "설정 키 존재 확인"은 검증했지만 "그 코드가 실제로 살아있는 런타임 이름공간에서 도는가"까지는 잡지 못한 것으로 보인다(테스트 환경과 `main.py` 라이브 실행 환경의 이름 바인딩 차이).
- **제안 내용**: 장중 배포가 불가피할 때(오늘처럼 자체적으로 판단해 진행하는 경우), 재기동 직후 1~2분은 해당 신규 경로의 실제 로그 출력(오늘이면 `[OptionFlow]` 또는 그 실패 태그)을 확인하는 짧은 체크리스트를 커밋 메시지의 "남은 것" 절에 이미 쓰고 있는 관행을 — dev_memory나 별도 체크리스트 파일로 표준화해, 다음에도 "재기동 후 실제 로그 확인"이 누락되지 않게 한다.
- **기대 효과**: 오늘처럼 8분 안에 자체 발견하는 것이 최선이었지만, 표준화된 체크리스트가 있었다면 12:16:24 첫 발생 시점에 더 빨리 알아챘을 수 있다(현재는 12:23까지 8분 소요).
- **비용·위험**: 낮음 — 새 코드나 게이트가 아니라 운영 문서화 제안.
- **선행 조건**: 없음.
- **우선순위 제안**: 낮음(이번 건 자체는 이미 잘 처리됨 — 재발 시를 위한 예방적 제안).

### 로드맵·실전전환 기준 반영 제안

없음 — 오늘 관측이 실전 전환 기준에 직접 영향을 주지 않는다.

---

## 4. 진입 승패 사후검증 (장후 한정)

해당 없음(장중) — 장후 절에서 작성.

## 5. 수익률 향상방안 (장후 한정)

해당 없음(장중) — 장후 절에서 작성.

---

## 6. 다음 거래일 관측 예정 (갱신)

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| O-p1 | `[HealthPolicy] Degraded 선제차단` 발생 빈도·`cause` 단계 (장후 최종 판정) | 하루 총 4건 수준이면 정상, 실제 자동진입 차단과 연결되면 P1 격상 | 오늘 장후 |
| O-p2 | `[SessionStateDrop]` 다음 재현 시점 | 다음 재현이 정규 거래일 기동인지 비거래일 기동인지 | 다음 재현 시 |
| O-i1 (신규) | `[OptionFlow]` 실패 태그 재발 여부·`option_flow.db` 적재 지속성 | 장후까지 실패 태그 0건 + DB 갱신 지속이면 완전 해소로 판정 | 오늘 장후 |
| O-i2 (신규) | `[LiveDBG] _fetch_investor_data 지연` 경고 증가 여부 | 611차 커밋이 자체 예고한 관찰 항목(메인 스레드 약 131ms 추가) — 늘어나면 스레드 분리 검토 | 오늘 장후·내일 |

## 7. 기한 도래 항목

(장전과 동일 — 변경 없음)

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| CB② 발동 1회 관측 (값 복원은 2026-09-02 완료, `=3`) | 상시 관측 | — | 아직 라이브 발동 미관측 — 인위적으로 유발하지 말 것(CLAUDE.md 명시) |
| ATR_MIN_ENTRY 재보정 (2026-09-07 등록, 초회 실행 FAIL) | 다음 26주 WFA 주기 | 미도래 | 조치 불필요(주기 항목) |

---

## 8. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-09-21 (MW0601 609차 후속 — 장중 점검)`
- `NEXT_TODO.md` 추가 체크박스: 2건(O-i1, O-i2)
- 완료 처리한 기존 항목: 없음(1-5는 이미 해당 세션이 자체 완료 — 이 점검이 새로 여는 항목 아님)

---

이 절이 발급한 번호: 이상점 1-5~1-6 · 관측 O-i1~O-i2

---

# 장후(post)

- 점검 시각: 15:10~16:20 KST(증거 수집 16:18, 원본 로그·DB 재확인은 16:20경까지) · PC `MW0601`
- 브랜치 `v9-dev`(정상) · HEAD `c48c415` · 당일 커밋 7건(장중 이후 3건 추가 — 09:56·10:19·12:13·12:23은 장중에 이미 반영, **15:34·15:41 2건이 신규**)
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260921_post.md`

## 0p-C. 이월 처리 — 장전+장중 항목 처분

| 원 번호 | 제목(줄임) | 원 국면 | 처분 | 근거 |
|---|---|---|---|---|
| 1-1 | `.git/index.lock` 회수 불가(스테일) | 장전 | ✅ 해소(장중 확정) | 지금도 락 없음(§2 자가점검 "락 없음") — 변동 없음 |
| 1-2 | `SessionStateDrop` 재발(9/20 비거래일) | 장전 | 🔄 지속(원인 미해결, 오늘은 재현 자체가 성립 불가) | 오늘 08:41:10 `[SessionRollover]` 재확인 — "이어받은 키(5개)=[...]" 목록에 `p8_last_success_date`·`eod_retrain_ok_date`가 애초에 없다(둘 다 날짜 전환 로그에 등장하지 않음). 즉 오늘도 넘길 마커가 없어 드롭이 발생할 수 없는 상태가 계속됐다 — "해소"가 아니라 "재현 조건 자체가 없음"이다. 근본 원인(F-1/538-4)은 여전히 사용자 승인 대기(12거래일째, `dev_memory/NEXT_TODO.md` 확인) |
| 1-3 / O-p1 | 개장 초반 HealthPolicy 선제차단 | 장전 | 판정 완료 — 정상 | 하루 전체 재확인 결과 총 **5건**(09:01·09:03·09:08·11:42·14:18, `logs/20260921_WARN.log`) — 개장 버스트 구간(3건) 외에 오전 11시대·오후 2시대에도 산발적으로 1건씩 더 있었다. cause 단계는 S6·S5·S7·S4·S0으로 매번 다르다. 다만 **오늘은 진입 자체가 하루 종일 0건**이라(다른 게이트가 먼저 걸러 체크리스트 통과 신호 자체가 없었음, §5 진입퍼널 참조) 이 선제차단이 실제로 어떤 자동진입을 막았는지는 오늘도 인과적으로 분리할 수 없다. 빈도(5건/371분)가 낮고 특정 시간대에 쏠리지 않아 **정상 판정**으로 닫는다 |
| 1-4 | 미커밋 코드 3건 지속 | 장전 | 🔄 지속(범위 확대) | 원래 3파일(`levels_store.py`·`premarket_levels.py`·`cybos_autologin.py`)은 여전히 그대로 남아 있고, 여기에 **이 점검 세션과 무관한 별도 개발 세션(612차 대시보드 작업)**이 15:41 커밋 이후 이어서 수정 중인 12개 파일(`dashboard/main_dashboard.py`·`dashboard/panels/option_flow_delta_chart.py`·`main.py`·`utils/db_utils.py`·`dev_memory/*`·테스트 4개)이 실질 변경으로 추가됐다(`git diff --stat -w` 재확인, 15개 파일 838줄 추가/373줄 삭제). 아래 1-9 참조 |
| 1-5 | 옵션 위클리 수급 NameError(8분 수집 중단) | 장중 | ✅ 해소 | `[OptionFlow]` 실패 태그 재발 0건(하루 전체 재확인), `option_flow.db` 최종 갱신 15:34(정상 지속 갱신 확인). O-i1 판정 완료 |
| 1-6 | 장중 재학습 2회 | 장중 | ✅ 해소(참고 기록 유지) | 오후에 2회 더 발생(14:16·15:50 P8) — 전부 이벤트 트리거 정상(아래 참고) |
| O-p1 | HealthPolicy 선제차단 최종판정 | 장전 | 판정 완료 → 정상(위 1-3 참조) | — |
| O-p2 | SessionStateDrop 다음 재현 시점 | 장전 | 판정 보류 → 다음 재현까지 이월 | 오늘도 재현 조건 성립 안 함(위 1-2 참조). F-1(538-4) 사용자 승인이 유일한 근본 해결책 |
| O-i1 | `[OptionFlow]` 실패 태그 재발 여부 | 장중 | ✅ 해소 | 위 1-5 참조 |
| O-i2 | `_fetch_investor_data 지연` 경고 증가 여부 | 장중 | 판정 완료 — 정상(재기동 종속) | 오늘 5건 관측(524ms~2912ms, 09:49·12:24·15:02·15:08·15:27) — 그중 4건이 재기동 직후 3~22초 이내에 몰려 있어 **매분 지속 누적이 아니라 재기동 시 1회성 초기화 비용**으로 확인됨. 오늘 재기동이 유난히 잦아(아래 1-8) 이 패턴이 평소보다 자주 관측됐을 뿐, 611차가 우려한 "메인 스레드 상시 131ms 추가"의 누적 악화는 아니다 |

**미처분 0건.**

## 0p. 오늘 한눈에 (최종)

오늘은 아침부터 장 마감까지 실제로 사고판 거래가 단 한 건도 없었던 하루입니다. 이유는 프로그램이 고장 나서가 아니라, 오늘 시장 변동성이 유난히 작아서(가격이 최근 며칠보다 훨씬 좁은 범위 안에서만 움직여서) "이 정도로는 확신을 갖고 들어갈 만하지 않다"고 프로그램 스스로 계속 판단했기 때문입니다. 실제로 오늘 걸러진 신호 66건 중 가장 흔한 사유가 "변동성이 너무 작다"였고, 다음으로 흔한 사유가 "신뢰도가 기준에 못 미친다"였습니다. 이것은 설계대로 조심하고 있는 것이며, 며칠 전부터 이미 알려진 계절적 특징(9월 들어 변동성이 낮아지는 구간이 잦다는 것)과도 일치합니다. 실제 돈에는 영향이 없었고(진입이 없었으니 손익도 0원), 장 마감 전 반드시 지켜야 할 안전 규칙(오후 3시 10분 전 강제 청산)도 문제없이 통과했습니다 — 애초에 청산할 포지션 자체가 없었기 때문입니다.

다만 오늘 새로 짚어둘 만한 것이 두 가지 있습니다. 첫째, 프로그램의 안전 감시 기능 하나(시장 변화를 탐지하는 지표, PSI)가 실제로는 하루 동안 여러 차례 정상적으로 측정에 성공했는데도, 장 마감 시점에 프로그램이 스스로 "오늘은 한 번도 측정에 성공하지 못했다"고 잘못 보고했습니다. 원인은 오늘 프로그램이 유난히 자주(총 9~10회) 다시 켜졌기 때문인데, 그때마다 "오늘 몇 번 성공했는지" 세는 내부 계수기가 초기화돼 버렸습니다. 실제 감시 기능 자체는 잘 작동했지만, 그 결과를 요약해서 보고하는 부분에 결함이 있다는 뜻이라 사람이 참고할 때 혼동할 수 있습니다. 둘째, 프로그램이 오늘 유난히 여러 번(9~10회) 다시 켜졌는데, 그중 상당수는 이 매매 프로그램과는 별개로 진행 중인 다른 화면(대시보드) 개발 작업이 낮 시간 중에 실거래 프로그램을 반복해서 재시작시켰기 때문입니다. 실제 거래에는 영향이 없었지만(어차피 오늘은 거래가 없었습니다), 장이 열려 있는 동안 이런 재시작이 이렇게 잦았다는 사실 자체는 기록해 둘 필요가 있습니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 정상 | 브랜치·환경·안전장치 설정 전부 일치, P0 없음 |
| 장중 | 조건부 정상 | P0 없음·실손해 없음. 다만 라이브 코드 배포·재기동 3회(1회는 8분간 보조 데이터 수집 중단) 관측 |
| 장후 | 조건부 정상 | P0 없음·거래 0건(정상 판단)·15:10 강제청산 이행(대상 없음)·EOD+P8 성공. 다만 PSI 측정 카운터 결함(1-7)과 잦은 재기동(1-8) 신규 관측 |
| **당일 최종** | **정상(거래 없음)** | 하루 종일 진입 0건 — 저변동성 구간에서 설계대로 관망했다. 실손익 0원. 절대원칙 6종 전부 위반 없음. 다만 운영 관측 결함 2건(PSI 카운터 리셋, 잦은 재기동)은 다음 세션이 이어서 볼 것 |

**당일 최종 손익(종가 기준)**: 0포지션(진입 0건) · 0pt · net 0원(gross 0 / 수수료 0) — **포지션 단위**. 브로커 실측(`daily_broker_pnl`/`strategy_report`) 동일하게 `broker(gross) +0원 · 수수료 0원 · net +0원`으로 확인(전략 리포트 §5 인용).

**절대원칙 6종 최종 판정**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산(오버나이트 금지) | 준수 | `[SchedForceExit] 15:11:15 점검 — status=FLAT ... 청산 대상 없음(정상)`, 15:27:50 재확인 동일. 애초에 하루 종일 포지션이 없었으므로 청산할 것이 없었음(로그로 확인 — phases.md C-1 구분) |
| ② | Circuit Breaker 5종 | 준수 | HALT 발동 0건(정상 — CB③ 판정가능시간 0분이라 판정 자체가 없었음). CB②는 `일간 리셋 완료` ×2만 찍힘(정상 카운터 리셋, 발동 아님) |
| ③ | CORE 피처 호라이즌 그룹별 | 해당 없음 | 진입 0건이라 체크리스트 통과 사례 자체가 없음. `vwap` 미통과 진입(강제 X 위반) 0건 |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 준수 | `0xC0000409`·`STACK_BUFFER` 로그 0건 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 준수 | 오늘 관련 코드 변경/오류 없음 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 준수 | 관련 흔적 없음 |

**9단계 STEP 판정**: STEP1(과거예측검증)·STEP2(SGD)·STEP4(피처생성)·STEP5(예측)·STEP9(DB저장) 정상 동작(§5·§6 로그 확인). STEP3(GBM 배치재학습)은 이벤트 트리거로 오늘 4회(10:00·12:16·14:16·EOD 15:50 P8) 정상 발동 — "30분 주기"가 아니라는 483차 정정과 일치. STEP6(진입판단)은 0건 진입으로 귀결(등급X 판정 다수). STEP7(진입실행)은 대상 없음. STEP8(청산 5단계)은 하드스톱~TP1~TP3 대상 없음, 15:10 강제청산·15:18 안전망 둘 다 "대상 없음(정상)"으로 확인.

**EOD 재학습·P8**: `data/eod_retrain_done_20260921.txt` 15:53:28 완료(rows=16288, cols=97, horizons_replaced=6/6, phase2_fallback=false). `session_state.json`의 `p8_last_success_date`·`eod_retrain_ok_date` 둘 다 `2026-09-21`로 정상 기록. Python 3.10.20 64-bit(정상, py37_32 아님 — CLAUDE.md 명시대로 정상 환경).

---

## 1. 이상점 정리보고 (장후 신규분)

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

없음.

### P1 — 정확성·관측 훼손

#### 1-7. PSI(시장 구조 변화 탐지) 측정 성공 여부를 세는 내부 계수기가 프로그램 재기동마다 초기화돼, 장 마감 요약이 "오늘 하루 종일 측정 실패"라고 잘못 보고했다

- **증상**: 15:40:20에 프로그램이 스스로 `[RegimeFingerprint] 🔴 오늘 PSI 측정 성공 0회 — 하루 종일 미측정이다`라고 경고했다. 그런데 실제 로그를 하루 전체(09:00~15:09) 훑어보면, 5분 간격으로 찍히는 하트비트 로그가 68회 모두 실제 측정값(`PSI=0.001 level=0`)을 출력하고 있어 "측정 실패"가 아니라 "정상적으로 낮은 값으로 측정 성공"이었음을 보여준다. 즉 마감 보고와 실제 로그가 정반대로 어긋난다.
- **근거**:
  ```
  # 하루 중 정상 측정(예시, 총 68건 중 5건)
  logs/20260921_SYSTEM.log:210: 09:00:00 [INFO] SYSTEM: [RegimeFingerprint] PSI=0.001 level=0 (heartbeat)
  logs/20260921_SYSTEM.log:302: 09:05:00 [INFO] SYSTEM: [RegimeFingerprint] PSI=0.001 level=0 (heartbeat)
  logs/20260921_SYSTEM.log:5659: 15:09:00 [INFO] SYSTEM: [RegimeFingerprint] PSI=0.001 level=0 (heartbeat)

  # 그런데 마감 시점 경보는 "0회"
  logs/20260921_WARN.log:25982: 15:40:20 [WARNING] SYSTEM: [RegimeFingerprint] 🔴 오늘 PSI 측정 성공 0회 —
    하루 종일 미측정이다(0.000/CLEAR 가 아니다). 기준선 키 불일치·라이브 표본 미달·update_live 예외 중 하나다.

  # 원인 — main.py 소스 확인
  main.py:1383:  self._fp_measured_count_today: int = 0   # __init__ 에서만 초기화
  main.py:7406:  if _fp_psi is not None: self._fp_measured_count_today += 1  # 매분 증가
  main.py:13028: if self._fp_measured_count_today == 0: logger.warning("... 0회 ...")
  main.py:13041: self._fp_measured_count_today = 0        # 매일 마감 후 리셋
  ```
  `heartbeat` 로그의 `%.3f` 포맷 값(`0.001`)은 `_fp_psi is not None`일 때만 찍힌다(코드상 `_fp_psi is None`이면 "미측정(기준선 없음 또는 라이브 표본 미달)" 문자열이 찍히도록 분기돼 있다 — `main.py:7392~7395`). 따라서 68건의 하트비트는 전부 실측 성공이었다.
  오늘 프로세스가 **9~10회** 재기동됐고(아래 1-8), `_fp_measured_count_today`는 클래스 인스턴스 변수라 재기동(=새 인스턴스 생성)마다 0으로 되돌아간다. 15:40 마감 시점에 살아 있던 마지막 프로세스는 15:27:20에 막 재기동된 것이라(위 이월표 참조), 15:40까지 13분밖에 없어 이 프로세스에서는 실제로 측정에 성공하지 못했을 가능성이 높다 — 마감 로그의 "0회"는 **이 마지막 프로세스 기준으로는 사실**이지만, **그날 전체로는 거짓**이다.
- **기준 위반**: 계측 4원칙 ②(미측정 ≠ 0 — 여기서는 반대 방향으로, "여러 번 측정 성공"이 "0회 실패"로 위장됨) 및 ④(폴백 가시화 — 재기동이라는 폴백 경로가 카운터를 조용히 지웠는데 그 사실이 로그에 안 남음)와 동일 계열의 결함.
- **영향**: 오늘 실거래에는 영향이 없다(어차피 이 지표는 차단에 관여하지 않는 감시 전용 계측 — FP-CRITICAL 차단은 이미 한시예외로 비활성). 다만 CLAUDE.md 실전 전환 기준 ⑦("정상 구간에서 PSI가 오르내리는 것을 실측 확인한 뒤 True로 복원")의 판단 근거가 되는 일일 요약이, 재기동이 잦은 날마다 "측정 실패"로 왜곡될 수 있다는 뜻이다. 오늘처럼 재기동이 유난히 잦았던 날에는 이 왜곡이 특히 크게 나타났다.
- **영향(기술)**: `_fp_measured_count_today`가 프로세스 재기동에도 이어지지 않는 순수 인스턴스 상태이기 때문. 계측 4원칙 ④의 규약("리셋과 저장이 같은 함수에 있으면 리셋 전에 스냅샷을 잡아라")과 유사하게, 이 카운터도 날짜 경계 리셋(`main.py:13041`)은 있으나 **프로세스 재기동 경계에서의 승계**가 없다. `SessionStateDrop`(1-2)이 "날짜 전환 시 마커 유실"이라면, 이것은 "같은 날 안에서 재기동 시 카운터 유실"로 같은 결함 패턴의 다른 변종이다.
- **신규 여부**: 신규. 다만 재기동이 드문 평소에는 잘 드러나지 않았을 것으로 추정된다(오늘처럼 마감 직전 재기동이 있는 날에만 증상이 뚜렷하게 나타남).

### P2 — 운영 부담·기술부채

#### 1-8. 오늘 정규장 중 라이브 프로세스가 9~10회 재기동됐다 — 대부분 이 매매 프로그램과는 별개인 대시보드 개발 작업 때문이었다

- **증상**: `data/session_state.json`의 `count` 필드가 오늘 최종 **10**을 기록했다(재기동 총 횟수 근사치). 실제 재기동 로그 기준으로는 08:41(최초 기동) 외에 09:59:50·10:21:47·12:16:00·12:24:07·14:25:55·15:02:32·15:08:42·15:27:17 총 8회의 추가 재기동이 관측됐고, 15:40 정상 자동 종료(`shutdown_normal`) 이후 16:14:05에 별도 런처가 한 번 더 기동했다(거래 종료 후라 매매와 무관). 이 중 09:56·10:19·12:13·12:23은 이미 장중 절 1-5에서 확인된 옵션 위클리 수급 수집 신설·수정 커밋에 대응하고, **15:34·15:41의 새 커밋(612차 — 대시보드 다이버전스·포지션 탭 리모델링, GEX 100배 소급 정정)에 대응하는 재기동은 로그에 명시적 커밋 시각 대응 없이(14:25·15:02·15:08·15:27) 코드 작업 중간중간 화면을 확인하려고 반복 재기동한 것으로 보인다** — 즉 커밋 전 로컬 변경 상태에서도 라이브 프로세스를 여러 차례 열었다 닫았다.
- **근거**:
  ```
  # 재기동 시각 전수 (logs/20260921_SYSTEM.log · logs/20260921_WARN.log)
  08:41  최초 기동
  09:59:50 / 10:21:47 / 12:16:00 / 12:24:07  — 609~611차 커밋(장중 1-5 기 확인) 대응
  14:25:55 / 15:02:32 / 15:08:42 / 15:27:17  — 대응 커밋 시각 없음(작업 중간 재기동 추정,
    15:34·15:41 커밋보다 앞섬)
  16:14:05 — 15:47:14 정상 자동종료 이후 별도 재기동(거래일 종료 후)

  # 각 재기동은 36분·6분·16분 등 짧은 수명으로 다시 종료됨(런처 로그)
  logs/Mireuk_batch/launcher_20260921_084001_27124.log:36622:  Mireuk 종료. RestartCnt=0 Runtime=36min
  logs/Mireuk_batch/launcher_20260921_084001_27124.log:36946:  Mireuk 종료. RestartCnt=0 Runtime=6min
  logs/Mireuk_batch/launcher_20260921_084001_27124.log:37822:  Mireuk 종료. RestartCnt=0 Runtime=16min
  logs/Mireuk_batch/launcher_20260921_084001_27124.log:37825: [AUTO-RESTART] 15:10 이후 종료 -- 재시작 안 함 (오버나이트 금지)
  ```
  마지막 줄에서 보듯 15:10 이후 종료된 것에 대해서는 런처가 **스스로 재시작을 거부**했다(오버나이트 금지 원칙을 지킴) — 이후 15:26·16:14 재기동은 런처의 자동 재시작이 아니라 **사람이 수동으로 다시 실행**한 것이다.
- **기준 위반**: 국면 체크리스트 B-7("장중 코드 배포·재기동 흔적 없는가")에 해당 — 단 이 재기동을 실행한 것은 이 점검 세션이 아니라 별개의 대시보드 개발 세션이며, 오늘 장중 절 1-5가 이미 확인했듯 그 자체가 금지 규정 위반은 아니다(장중 배포 자체를 실제 매매 결정자가 판단한 것). 다만 오늘은 그 빈도(9~10회)가 유난히 높아 참고 기록으로 남긴다.
- **영향**: 실거래 영향 없음 — 하루 종일 포지션이 없었고(0건), 재기동 사이 8분 전례(1-5)를 빼면 나머지 재기동에서 매매 핵심 경로(피처 생성→예측→진입 판단)가 끊긴 흔적은 없다(각 재기동 직후 `[Capability]`·`[Runtime]` 정상 로그 확인). 다만 `ForceFlatGuard`가 15:26:30·16:14:20 두 차례 "라이브 프로세스 정지"로 오인해 경보를 냈다(`data/force_flat_alert_20260921.txt`) — 실제로는 프로세스가 죽은 것이 아니라 사람이 재기동하는 사이의 공백을 감시기가 "정지"로 해석한 것이다. 포지션이 FLAT이라 실손해는 없었다.
- **영향(기술)**: 재기동마다 `FreezeWatchdog`·`ScalerFloor`·`ConstOut` 워밍업 등 초기화 비용이 반복되고(1-7·O-i2가 그 파생 증상), EOD 재학습 직전 마지막 재기동(15:27:17)이 하필 `_fp_measured_count_today`를 비운 시점과 겹쳐 1-7이 발생했다. `GuardGhost`(§4 EOD 로그)가 "배포된 pkl이 CV 미검증 intraday 모델"이라고 반복 경고한 것도 잦은 재기동이 만든 잦은 장중재학습(오늘 4회)의 연쇄 결과다 — 단 이 가드는 설계대로(457차 F7 ⑤안) 안전하게 작동해 잘못된 비교를 스스로 걸러냈다(문제 없음, 정보성).
- **신규 여부**: 신규 관찰(빈도 측면). 개별 재기동 자체가 새로운 유형은 아니다.

#### 1-9. 별도 개발 세션이 장중 옵션 그릭스(GEX) 계산 오류를 발견해 과거 데이터를 소급 정정했다 — 정상 절차를 따랐음을 확인

- **증상**: 15:38:15경 별도 세션이 `raw_features`(17,523행)·`raw_features_horizon`(17,665행)의 `opt_gex_bn` 컬럼이 2026-05(249차 도입) 이래 100배 과대 계산돼 있었음을 발견하고 소급 정정했다(백분율 단위인 옵션 그릭스를 나누기 100 하지 않은 결함).
- **근거**: `strategy_events` 테이블 id=104(`METRIC_REDEFINITION`, 2026-09-21 15:38:49) — 정정 사실·범위·근거(delta 범위, ATM gamma 비율 104.1)가 정상적으로 기록됐다. 정정 전 백업 `data/db/raw_data.db.bak_20260921_153815_pre_gex_612` 존재 확인. 커밋 메시지(`d04c5d0`)는 이 컬럼이 "학습 피처셋에 미포함이라 train/serve skew 없음"이라고 명시했다.
- **기준 위반**: 없음 — 오히려 CLAUDE.md 계측 4원칙(단위 명시·폴백 가시화) 및 461차 mdd_pct 교훈("재보정 시 불연속 마커를 남겨라")을 정확히 따른 사례다. 커밋 메시지 자체가 "마커 기록이 한 번 조용히 실패했다"고 자진 신고했는데, 이 점검 세션이 `strategy_events` 테이블을 직접 조회해 **id=104가 15:38:49에 정상 기록되어 있음을 확인**했다 — 즉 최초 시도 실패 후 재시도가 성공한 것으로 보이며, 현재 상태는 정상이다.
- **영향**: 없음(매매 미사용 피처). 다만 `2026-09-21 15:38` 이전·이후의 `opt_gex_bn` 시계열은 서로 다른 스케일이므로 향후 이 값을 인용할 때는 불연속 경계를 확인해야 한다(이미 커밋 메시지 자체가 경고함).
- **영향(기술)**: 없음 — 위 참조.
- **신규 여부**: 참고 기록(이상점이 아니라 정상 절차 확인 사례로 등재). 이 점검 세션이 발생 사실을 사후 관측·검증만 했다.

### 확인 필요 (확정 아님)

- 없음 — 오늘은 진입 0건이라 승패 관련 표본 자체가 성립하지 않는다(제4부 참조).

### 이미 반영된 사안 (신규 아님)

- **`[RegularFresh] 결손 1일 — 2026-09-21`(EOD 재학습 로그 15:50:04)** — 언뜻 오늘 정규 수집이 빠진 것처럼 보이지만, 이 점검이 생성 시각을 대조한 결과 `logs/20260921_REGULAR_COLLECT.log`가 **15:52:19~20**에 실행되어 오늘(2026-09-21) 411봉(08:45~15:44, 완전한 하루치)을 정상 수집한 것을 확인했다. 즉 EOD 체인 내부의 결손 점검(15:50:04)이 정규 수집 완료(15:52)보다 **먼저 실행돼 생긴 순서상의 오탐**이다 — 장중 절 1-6 하단에서 이미 확인한 "매분 루프 커버리지 오탐"(증거 수집이 장마감 전에 실행돼 생긴 것)과 같은 유형의 결함이다. 실제 결손은 없다.
- **EOD 재학습의 `GuardGhost`·`GuardFair` 경고 6개 호라이즌 전부 발생** — 새로운 결함이 아니라 457차 F7 ⑤안이 의도한 설계대로 작동한 것이다. 오늘 장중재학습이 유난히 잦아(1-8 참조) "배포된 모델이 CV 미검증 상태에서 비교당하는" 상황이 더 자주 만들어졌을 뿐이며, 가드가 그때마다 정상적으로 "이 비교는 무효"라고 판정해 CV 검증된 새 모델로 교체했다(판정 결과에 문제 없음).

---

## 2. Fix 작업 구현계획

### F-2. `_fp_measured_count_today` 재기동 승계 — P1 · 대응 이상점 1-7

- **원인 가설**: 인스턴스 변수라 프로세스 재기동(= 새 객체 생성)마다 `__init__`에서 0으로 되돌아간다. `SessionStateDrop`(F-1/538-4)이 이미 등록한 "날짜 경계 마커 유실" 문제와 원인 계열은 같으나, 트리거가 "날짜 전환"이 아니라 "같은 날 재기동"이라는 점이 다르다.
- **변경 파일**:
  - `strategy/regime_fingerprint.py` 또는 `main.py` — `_fp_measured_count_today`를 `data/session_state.json`(또는 별도 파일)에 매분 스냅샷하고, 재기동 시 당일 값이 있으면 승계(F-1과 동일한 캐리오버 패턴 재사용 검토).
  - 대안(더 저비용): 마감 로그(`main.py:13028`)에서 "재기동 이후 경과분"과 "당일 총 하트비트 성공분(로그 grep 기반)"을 함께 찍어, 재기동이 있었던 날에는 이 숫자가 프로세스 단위임을 명시.
- **회귀 위험**: 낮음 — 감시 전용 계측이라 차단 로직에 영향 없음(FP_CRITICAL_GRADE_BLOCK_ENABLED=False로 이미 분리돼 있음).
- **절대원칙 저촉 여부**: 없음.
- **런타임 주의**: py37_32(`main.py`, 라이브 프로세스).
- **검증 방법**: 재기동이 있는 날에 마감 로그가 "N회(이번 프로세스 기준, 재기동 M회)" 형태로 재기동 사실을 함께 표기하는지 확인. 회귀 테스트로 "인위적 재기동 시 카운터 승계" 유닛 테스트 신설 검토.
- **섀도 계측 필요 여부**: 불필요(감시 로직 자체 수정, 차단 게이트 아님).
- **적용 시점**: 장후 권고 — 사용자 승인 후 다음 세션에서 구현.
- **결정 필요 사항**: F-1(538-4·SessionStateDrop)과 통합 수정할지, 별도 항목으로 둘지 — 두 문제가 "재기동/날짜전환 시 인메모리 상태 유실"이라는 같은 패턴이므로 한 번에 검토할 것을 제안.

### 적용 순서와 커밋 계획

- 이번 장후 세션은 계획만 세우고 실행하지 않는다(사용자 승인 필요 — F-1과 묶어 검토 권고).

---

## 3. 고도화 방안

### G-2. 대시보드 등 부속 개발 작업의 "장중 라이브 프로세스 반복 재기동" 관행에 대한 가이드 마련 제안

- **관측 근거**: 오늘 1-8에서 대시보드 개발 작업이 정규장 중 8~9회 라이브 프로세스를 재기동했다. 매번 짧게(6~36분) 살다 다시 재기동돼, `ForceFlatGuard`가 두 차례 오탐 경보를 냈고(1-8), PSI 카운터 리셋(1-7)의 직접 계기가 됐다.
- **제안 내용**: 대시보드/UI 전용 코드 변경은 별도의 오프라인 미리보기 환경(예: 저장된 스냅샷 데이터로 화면만 띄우는 모드)에서 검증하고, 라이브 프로세스 재기동은 실제 매매 로직 변경 검증이 필요할 때로 제한하는 관행을 문서화하는 안.
- **기대 효과**: 재기동 빈도가 줄면 1-7류 계측 왜곡과 ForceFlatGuard 오탐이 함께 준다.
- **비용·위험**: 낮음(운영 문서화 제안) — 단, 대시보드 코드가 라이브 데이터 소스와 강하게 결합돼 있다면 별도 미리보기 모드 구현에 별도 공수가 필요할 수 있음.
- **선행 조건**: 없음.
- **우선순위 제안**: 낮음(오늘 실손해는 없었음 — 예방적 제안).

### 로드맵·실전전환 기준 반영 제안

없음 — 오늘 관측이 실전 전환 기준 수치를 직접 바꾸지 않는다. 다만 F-2(PSI 카운터)는 실전 전환 기준 ⑦의 "정상 구간 관측 확인" 근거 데이터 신뢰성과 관련되므로, ⑦ 재검토 시 이 결함을 먼저 고치는 것이 순서상 맞다.

---

## 4. 진입 승패 사후검증

**4-0. 선행 확인**: EOD(15:40 `daily_close`) 완료(`data/daily_close_done_20260921.txt` 15:40:24) · P8(`p8_last_success_date`) 완료(2026-09-21) — 둘 다 확인됨. 수집기 실행·DB 접근은 `trades.db`는 정상 조회됐고, `predictions.db`는 이 세션의 샌드박스 환경에서 `disk I/O error`로 직접 쿼리가 실패했다(892MB 대용량 파일 — 마운트 환경 한계로 추정, 라이브 락 문제로 보이지 않음). 대신 프로그램이 스스로 생성한 `strategy_report_20260921_154024.txt`의 진입 퍼널 집계(로그·자체 DB 집계 기반)로 대체 확인했다.

**4-1. 오늘 할일 4분류**

| # | NEXT_TODO 항목 | 당일 커밋 | 코드 실재 확인 | 상태 |
|---|---|---|---|---|
| F-1(538-4) SessionStateDrop 마커 캐리오버 | 없음 | `strategy/runtime/session_recovery_service.py`(수정 계획만 존재, 미착수) | ⬜ 미착수(12거래일째 사용자 승인 대기 — 이월) |
| 606-3 미커밋 3파일 diff 검토·커밋 | 없음(이 3파일은) | `features/levels/levels_store.py` 등 diff 그대로 잔존 | ⬜ 미착수(지속) |
| 611차: 옵션 위클리 수급 수집 신설 | `da2506d`(12:13) | `collection/cybos/weekly_option_flow.py` 신설 확인 | ✅ 완료-커밋확인 |
| 611차 후속: NameError 수정 | `850cfad`(12:23) | `main.py` 호출순서 이동·try 보강 확인(1-5) | ✅ 완료-커밋확인 |
| 612차: 대시보드 리모델링 + GEX 100배 정정 | `d04c5d0`(15:34)·`c48c415`(15:41) | `dashboard/`·`data/db/raw_data.db`(백업+마커 확인, 1-9) | ✅ 완료-커밋확인 |
| 612-5 RV-IV 카드 근본 재설계 | 없음(신규 등록만) | `dev_memory/NEXT_TODO.md` 612-5 항목 확인 | ⬜ 미착수(정상 이월 — 오늘 등록된 것) |
| 612-6 위클리 옵션 코드 수집 경로 | 없음(신규 등록만) | `dev_memory/NEXT_TODO.md` 612-6 항목 확인 | ⬜ 미착수(정상 이월) |

> ⚠ 함정① 게이트: 위 항목 중 "완료"로 확인된 것은 전부 오늘 커밋 해시와 실제 코드 diff가 함께 확인된 것만 표시했다. `SessionStateDrop`·미커밋 3파일은 여러 세션째 반복 이월 중인 항목으로, 새로 발견한 것처럼 쓰지 않았다.

**4-2. 3원 대사 (포지션 단위)**

| 원천 | 질의 | 값 |
|---|---|---|
| 로그 | `[Position] 진입` | **0** |
| `ensemble_decisions` | `entry_executed=1` | **조회 불가**(disk I/O error, 892MB 파일 — 아래 참조) — 대체로 자체 생성 전략 리포트의 진입퍼널 `진입 0` 인용 |
| `trades` | `COUNT(DISTINCT entry_ts)` | **0** |
| (참고) `trades` | `COUNT(*)` — 청산 레그 | **0** |

세 값(로그·trades·전략리포트 자체집계) 모두 **0 = 0 = 0**으로 일치. `ensemble_decisions` 직접 쿼리만 이 세션의 샌드박스 I/O 한계로 실패했으나, 프로그램 자신이 15:40:24에 생성한 진입 퍼널(`FLAT 266 → conf미달 72 → CoherenceGate 6 → 게이트차단 18 → 후보 1 → 진입 0`, "정합성: OK — 칸합계·진입·JointGateBlock 3종 일치")이 같은 결론을 독립적으로 뒷받침한다. **불일치 없음 — P1 아님.**

**4-3. 케이스 카드**: 없음(진입 0건).

**4-4. 요인 집계**: 없음(진입 0건이라 요인 태그를 붙일 대상 자체가 없다). 오늘 차단 66건의 사유 분해는 §5(장전·장중·이 절 상단)에서 이미 다뤘다 — 최다 사유는 `등급X — 미통과 항목: 2_confidence`(14건), 다음은 다양한 임계의 `ATR X.XXpt < 1.0pt — 변동성 부족`(약 24건 합산). 특정 게이트 하나가 압도적이지 않아 확정 결론은 보류하되(313차 원칙), ATR 변동성 부족 사유가 여러 건 흩어져 있는 것은 CLAUDE.md에 이미 등록된 26주 WFA 재검증 항목 `ATR_MIN_ENTRY`(2026-09-07 539차 등록, 초회 실행 FAIL)와 방향이 일치하는 관찰이다 — 새 항목으로 만들지 않고 그 등록 항목의 근거 자료로만 남긴다.

**4-5. 313차 가드 통과 여부**

| 갈래 | 적용 결과 |
|---|---|
| ① 일자단위 판정 | 관측일 0일(진입 자체가 없음) — 판정 대상 없음 |
| ② overlap 보정 | 해당 없음 |
| ③ 이상치 분해(drop-worst) | 해당 없음 |
| ④ 사전등록 — 기준 불변 | 해당 없음(오늘 표본으로 어떤 기준도 바꾸지 않음) |
| ⑤ 부호 일관성(기간·PC 교차) | 해당 없음 |

---

## 5. 수익률 향상방안

§4-4에서 도출할 요인 집계가 없다(진입 0건) — **신규 방안을 내지 않는다**(일반론 금지 원칙).

**누적 대장 확인**(`docs/정기점검/수익률향상_누적대장.md`) — 먼저 열어 확인함. P5-01~P5-16 전부 오늘(09-21) 진입 0건이므로 **표본 무변동**이다(09-11·09-17·09-18 등 과거 무진입일과 같은 패턴). 대장 파일에 다음 한 줄만 이력으로 남긴다(6칸을 다시 쓰지 않음, §5-B 규약):

> **2026-09-21 (MW0601 614차 장후)**: **오늘 진입·체결·청산 전부 0건**(3원 대사 로그=DB체결=전략리포트 진입퍼널 0=0=0 일치, `ensemble_decisions` 직접 쿼리는 샌드박스 I/O 오류로 실패했으나 결론에 영향 없음). 원인은 저변동성 구간(ATR<1.0pt 다수) + 신뢰도 미달(`2_confidence`)이 차단 66건 중 대다수를 차지 — CLAUDE.md 26주 WFA 재검증 항목 `ATR_MIN_ENTRY`(539차 등록)의 문제의식과 일치하는 관찰이나 별도 채널로 등록하지 않음(이미 등록된 항목이라 함정① 대상). P5-01~16 전 채널 표본 무변동.

(이 한 줄은 다음 세션이 대장 파일에 직접 append할 것 — 이 점검 세션은 dev_memory에만 기록하고 대장 파일 자체는 8절 dev_memory 반영과 함께 별도로 갱신 여부를 사용자 조치에 남긴다.)

---

## 6. 다음 거래일 관측 예정

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| O-t1 | `_fp_measured_count_today` 관련 마감 로그 — 재기동이 있는 날 다시 "0회"로 오보고되는지 | F-2 적용 전까지는 재기동 있는 날마다 재현 예상. 재현되면 F-2 우선순위 근거로 누적 | 다음 재기동 발생일 |
| O-t2 | 라이브 프로세스 장중 재기동 빈도(대시보드 작업발) | 평소 수준(1~2회)으로 돌아오는지, 계속 높은 빈도(5회+)로 지속되는지 | 대시보드 작업 완료 시점까지 |
| O-t3 | `ATR_MIN_ENTRY` 관련 저변동성 차단 빈도 | 26주 WFA 재검증 스크립트(`atr_min_entry_recalibration.py`) 대상 항목 — 이번 주기 도래 시 오늘 관찰치도 함께 반영 | 다음 26주 WFA 주기 |

## 7. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| CB② 발동 1회 관측 (값 복원은 2026-09-02 완료, `=3`) | 상시 관측 | — | 아직 라이브 발동 미관측 — 오늘도 손절 자체가 없어 관측 기회 없음. 인위적으로 유발하지 말 것(CLAUDE.md 명시) |
| ATR_MIN_ENTRY 재보정 (2026-09-07 등록, 초회 실행 FAIL) | 다음 26주 WFA 주기 | 미도래 | 조치 불필요(주기 항목) — 오늘 관찰(4-4)이 참고 자료로 축적됨 |

---

## 8. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-09-21 (MW0601 614차 — 장후 점검)`
- `NEXT_TODO.md` 추가 체크박스: 3건(F-2, O-t2, O-t3 — O-t1은 F-2와 통합 관찰이라 F-2 항목에 병기)
- 완료 처리한 기존 항목: 1-1(git lock)·1-3/O-p1(HealthPolicy)·1-5/O-i1(OptionFlow)·O-i2(투자자데이터 지연) 판정 완료로 종결. F-1(538-4)·1-4(미커밋)·1-2/O-p2(SessionStateDrop)는 여전히 열려 있어 종결하지 않음.

---

## 9p. 종합 — 오늘 하루

### 9p-1. 종합 판정

| 국면 | 판정 | 한 줄 근거 |
|---|---|---|
| 장전 | 정상 | 브랜치·환경·안전장치 설정 전부 일치, P0 없음 |
| 장중 | 조건부 정상 | 라이브 재기동 3회(1회 8분 보조수집 중단, 자체 복구) 관측, 실손해 없음 |
| 장후 | 조건부 정상 | 진입 0건(저변동성 정상 관망), 15:10 강제청산·EOD·P8 전부 정상. PSI 계측 카운터 결함(1-7)·잦은 재기동(1-8) 신규 관측 |
| **당일 최종** | **정상 — 거래 없는 날** | 하루 종일 진입 0건으로 손익도 0원이었고 절대원칙 6종 전부 준수했다. 새로 발견한 것은 실거래와 무관한 계측·운영 관측(PSI 카운터, 재기동 빈도)뿐이며 둘 다 P1/P2 수준으로 다음 세션이 이어서 볼 사항이다 |

**당일 최종 손익(종가 기준)**: 0포지션 0승0패 · 0.00pt · net 0원(gross 0 / 수수료 0) — **포지션 단위**, 브로커 실측과 엔진 집계 동일(전략리포트 §5 인용, `daily_broker_pnl` 계열 확인 — broker/engine 괴리 없음, 애초에 거래가 없어 대사 대상 자체가 0).

**절대원칙 6종 최종 판정**: ①~⑥ 전부 **준수**(위 0p절 표 참조 — 종가 기준과 동일, 하루 중 변동 없음).

### 9p-2. 당일 이상점 통합 대장

| 번호 | 제목 | 최초 국면 | 심각도 | 최종 상태 |
|---|---|---|---|---|
| 1-1 | `.git/index.lock` 스테일(회수 불가) | 장전 | P1 | ✅ 해소(장중 확인, 원인 불명이나 결과 확인) |
| 1-2 | `SessionStateDrop` 마커 유실 재현(비거래일) | 장전 | P1 | 🔄 지속 → `NEXT_TODO` F-1(538-4)로 이월(12거래일째 사용자 승인 대기) |
| 1-3 | 개장 초반 HealthPolicy 선제차단 | 장전 | P2 | ✅ 판정 완료 — 정상(하루 5건, 진입 자체가 없어 인과 분리 불가하나 빈도 낮고 산발적) |
| 1-4 | 미커밋 코드 3건(타 세션 작업 중) | 장전 | P2 | 🔄 지속(범위 확대 — 오늘 대시보드 작업 12파일 추가) |
| 1-5 | 옵션 위클리 수급 NameError(8분 보조수집 중단) | 장중 | P1 | ✅ 해소(별도 세션이 8분 내 자체 발견·수정·회귀테스트 완료) |
| 1-6 | 장중재학습 2회(정상) | 장중 | 정상기록 | ✅ 정상 확인(오후 2회 추가, 전부 이벤트 트리거) |
| 1-7 | PSI 측정 카운터 재기동 시 리셋 — EOD 요약 오보고 | 장후 | P1 | 🆕 신규 → F-2로 등록(사용자 승인 후 적용 권고) |
| 1-8 | 라이브 프로세스 9~10회 재기동(대시보드 작업발) | 장후 | P2 | 🆕 신규 → G-2로 등록(운영 관행 제안) |
| 1-9 | GEX 100배 소급 정정(타 세션, 정상 절차) | 장후 | 정상기록 | ✅ 정상 확인(마커·백업 정상 기록) |

- P0 0건 · P1 4건(1-1·1-2·1-5·1-7, 그중 1-1·1-5 해소·1-2 지속·1-7 신규) · P2 3건(1-3·1-4·1-8, 그중 1-3 해소·1-4 지속·1-8 신규) (당일 누계)

### 9p-3. 사용자 조치 — 오늘 할 일

| # | 할 일 | 명령·경로 | 걸리는 시간 · 안 하면 생기는 일 |
|---|---|---|---|
| 1 | `SessionStateDrop` 마커 캐리오버 수정(F-1/538-4) 승인 여부 결정 — 12거래일째 미결 | `dev_memory/NEXT_TODO.md` F-1(538-4) 항목, 구현은 `strategy/runtime/session_recovery_service.py` | 검토 5~10분. 실손해는 없지만 승인 없이는 계속 같은 항목이 반복 보고됨 |
| 2 | 미커밋 코드(원래 3건 + 오늘 대시보드 작업 12건) diff 검토 후 커밋 여부 결정 | `git --no-optional-locks diff --stat -w` 로 대상 파일 확인 후 경로 명시해 `git add` | 검토 15~20분(오늘 대시보드 작업분 포함이라 평소보다 김). 미루면 다음 세션도 "미커밋 지속"으로 계속 보고 |
| 3 | PSI 측정 카운터 재기동 승계 수정(F-2) 적용 여부 결정 | `dev_memory/NEXT_TODO.md` F-2 항목, 구현 후보는 위 제2부 F-2 참조 | 검토 5분. 급하지 않음(감시 전용 계측, 차단 로직 무관) — 다만 재기동이 잦은 날마다 계속 오보고될 것 |
| 4 | 대시보드 등 부속 개발 작업의 라이브 재기동 관행 검토(G-2) | `dev_memory/NEXT_TODO.md` G-2 항목 | 검토 5분. 급하지 않음(예방적 제안) |
| 5 | 누적 대장에 오늘(0건) 한 줄 이력 append 여부 확인 | `docs/정기점검/수익률향상_누적대장.md` — 위 제5부에 준비된 문구 그대로 사용 가능 | 1분. 안 해도 실손해는 없으나, 누락되면 다음 세션이 "절차 누락"으로 오인할 수 있음(2026-09-17 601차가 실제로 겪은 혼동과 같은 유형) |

이 절이 발급한 번호: 이상점 1-7~1-9 · 관측 O-t1~O-t3
