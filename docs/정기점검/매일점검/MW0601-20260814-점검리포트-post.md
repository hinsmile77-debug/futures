# 미륵이 일일 점검 — MW0601 / 2026-08-14 / 장후(post)

- 점검 시각: **16:2x KST** · PC `MW0601` · 브랜치 `v9-dev` ✅ (운영 브랜치 규약 일치)
- HEAD `e8a56ea` · 미커밋 444건(**실변경 2건** = 오늘 pre·intra 점검의 dev_memory append. 나머지 442건은 CRLF 개행차 — `git diff --ignore-all-space --stat` = `dev_memory/DECISION_LOG.md` + `dev_memory/NEXT_TODO.md` 2건뿐)
- 당일 커밋 0건 (최근 7건은 전부 `[MW0602]` — 원격에서 받은 것)
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260814_post.md`
- 선행 리포트: `-pre.md`(09:08) · `-intra.md`(12:40). 본 리포트는 그 둘의 **장후 확인항목 O-1~O-8을 종가 기준으로 마감**한다.

> ⚠ **부수 산출물 1건** — 증거 수집기 실행 중 `evidence_MW0601-20260814_pre_1622.md`(65.8KB)가
> 잘못 생성됐다. 원인: 수집기가 `v9-dev`에 `--pc` 인자가 없어(아침 pre F-3) `platform.node()`를
> 대체하는 런처 래퍼로 우회하는데, **직전 pre 세션이 남긴 래퍼 스크립트(`--phase pre` 고정)가
> 샌드박스 `/tmp`에 남아 있었고 다른 사용자 소유라 덮어쓰지 못해** 그대로 실행됐다.
> 361차 덮어쓰기 방지 로직이 작동해 기존 `_pre.md`는 보존됐고 `_1622` 접미사본이 새로 생겼다.
> 파일 삭제는 샌드박스 권한 밖이라 **수동 삭제 필요**(내용은 오늘 09:00 pre본의 16:22 재수집본으로 무해).

---

## 0. 한 줄 결론

**P0 없음. 미륵이는 오늘 하루 뼈대(08:40 기동 → 08:55 매크로 → 09:00 개장 → 매분 9단계
371/371분 100% → 15:10 파이프라인 정지 → 15:40 마감 → 15:45 EOD 재학습 6/6 성공 →
15:40:39 정상 종료)를 빠짐없이 완주했고, 절대원칙 6종을 모두 지켰다. 진입 1건은
포지션 단위 1승 +28,730원이다. 다만 오늘 새로 확인된 P1 2건은 둘 다 "안전·계측 경로가
자기 자신에 대해 증거를 남기지 않는다"는 같은 종류다 — 15:10 강제청산의 1차 경로가
구조적으로 도달 불가로 보이고(2차 안전망만 생존), 사이징 축소 원인을 로그가 지목하는
게이트가 실제 원인 게이트와 다르다.**

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 정상 | `-pre.md`(09:08) — P0 없음 · 08:40:45 기동(PID 4232) · 08:55 매크로/구독 사전 시작 |
| 장중 | 조건부 정상 | `-intra.md`(12:40) — 매분 루프 공백 0 · P1 1건(SizerMatch 오귀속) · P2 4건 |
| 장후 | **정상** | 15:40:16 `일일 마감 \| 승=1 패=0 PnL=+28,730원` · 15:40:24 `daily_close_done` · **15:48:45 EOD 재학습 성공(6/6 호라이즌, py310_64)** · 15:48:45 P8 스케일러 재적합 · 15:48~16:05 검증 캠페인 10스텝 전부 OK · 15:40:39 `[CLEAN EXIT]` |

**오늘 신규 이상점: P0 0건 / P1 2건 / P2 3건 / 확인 필요 2건**

### 절대원칙 준수 여부

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | **준수 (대상 없음)** | 유일한 포지션이 11:48:55에 완전 청산(`[청산 완료] PnL=-0.03pt`)됐고 이후 진입 0건. `trades` 당일 마지막 `exit_ts=2026-08-14 11:48:55`, 미청산 행 0. 15:10:00 이후 파이프라인은 설계대로 정지(`is_force_exit_time` 가드, `main.py:4402`). **"강제청산 로그 없음"은 미이행이 아니라 대상 없음이다** — 단, 그 둘을 로그로 구분할 수 없다는 것이 아래 **P1-1**이다 |
| ② | Circuit Breaker 5종 | **준수** | CB① 신호반전 0건 · CB② `[CB] 연속 손절 1회`(11:48:55) 카운터만 — `CB_CONSEC_STOP_LIMIT=9999` 예외로 미정지가 정상 · CB③ HALT 0건, acc30m 단계전이 5회 추적 정상(`CB3_P4_GRADE_BLOCK_ENABLED=False`로 차단만 비활성) · CB④ ATR 3배 0건 · CB⑤ `PipePerf total` 최대 **2,830ms** < `CB_PIPE_PAUSE_MS=5,000` → **미발동이 정상** |
| ③ | CORE 피처 호라이즌 그룹별 | **준수** | 진입 1건(11:48:00, `horizon=3m` 단기그룹)에서 `vwap✅`(미통과 시 강제 X 규칙 해당 없음), `ofi❌`·`chas❌`는 `pass_count` 감점으로 처리 — 등급하락 규칙대로. `[AutoMasked]` 7건 전부 CORE 제외 면제 정상 |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | **준수** | `logs/crash_fault.log` 오늘 행 2줄뿐 — `[START] 08:40:45 PID=4232 Python 3.7.13 32bit` / `[CLEAN EXIT] 15:40:39`. `0xC0000409`·access violation·Traceback 0건 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | **위반 없음(무증상)** | `CybosInvestorRaw` 1,574건 정상 수신, TR 파라미터 오류 0건. 선물 분봉 TR `OPT50029` 유지. 파라미터 자체는 로그로 관측 불가 — 무증상을 준수로 읽는다 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | **준수** | 알파봇 실행·백테스트 큐·자동통합 로그 **0건**(`grep -c 알파\|AlphaResearch` = 0) |

### 설정 불변식 — `미발견` 5행

수집기 §3의 `미발견` 5행(`MODEL_LABEL_STATE_UNLOCK_ENABLED` · `PRE_RETRAIN_DONE_BY_EOD_ENABLED` ·
`ZONE_ENTRY_BAN_ENFORCE` · `ZONE_ENTRY_BAN_SHADOW_ENABLED` · `PIPE_LATENCY_EXCLUDE_MODEL_SWAP`)은
**오늘 아침 pre 1-1에서 이미 규명·등록됐다** — 감시표가 `dev` 세대(462·468차) 스펙인데 코드는
`v9-dev`라서 생긴 **브랜치 격차**이며 설정 변조가 아니다. 승계 항목: **F-1**(감시표 브랜치 인식형) ·
**F-1B**(5종 체리픽 심사). 나머지 21행은 전부 `일치` — 절대원칙·한시예외 4종(CB②·CB③-P4·
FP-CRITICAL·TOX-SEVERE-SPREAD) 모두 의도된 상태 그대로다.

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**해당 없음.**

---

### P1 — 정확성·관측 훼손

#### 1-1. 15:10 강제청산의 **1차 경로(STEP 8)가 구조적으로 도달 불가**로 보인다 — 2차 안전망만 살아 있다

- **증상**: 15:10 강제청산은 설계상 STEP 8(분봉 구동) 안의 `should_force_exit()` 분기가
  1차 집행자이고, 453차 D2 스케줄러 안전망(`_ts_scheduler_force_exit_net`, 15:11 발화,
  ERROR + 🚨 알림)이 2차 방어선이다. 그런데 **1차 경로가 실행될 수 있는 시각 창이 존재하지
  않는 것으로 보인다.**

- **근거** — 세 가지가 맞물린다.

  ① 분봉 마감 콜백이 조기 반환한다.
  ```python
  # main.py:4401~4403
  # 15:10 강제 청산 이후 예측 파이프라인 중단 (TimeRouter·앙상블 불필요 실행 방지)
  if is_force_exit_time(now):     # utils/time_utils.py:84 → dt.time() >= 15:10
      return
  ```

  ② 15:09 분봉의 마감 콜백은 **정확히 15:10:00에 온다**(오늘 실측).
  ```
  2026-08-14 15:10:00 [INFO] SYSTEM: [BAR-CLOSE][CYBOS] ts=15:09 O=1096.58 H=1097.08 L=1096.12 C=1097.08 V=94
  ```
  `logs/20260814_SYSTEM.log:5284` — 즉 `is_force_exit_time(15:10:00)=True`로 즉시 반환되어
  이 봉의 STEP 8은 돌지 않는다. STEP 8이 마지막으로 도는 것은 15:08 봉(15:09:00)이고
  그 시점 `should_force_exit()`는 False다.

  ③ 15:10 이후의 보조 경로도 전부 같은 가드로 막혀 있다 — 워치독(`main.py:11686`)·
  파이프라인 복구/유지보수 패스(`main.py:11938`) 둘 다 `if is_force_exit_time(...): return`.
  `_check_exit_triggers()` 호출부는 `main.py:9399`(STEP 8)와 `main.py:14584`(유지보수 패스)
  **두 곳뿐**이며 둘 다 15:10 이후 도달 불가다.

  ④ 로그·DB 어디에도 1차 경로가 돈 흔적이 없다.
  ```
  [TimeExit] 15:10 강제 청산 트리거     → 오늘 0건 (strategy/exit/time_exit.py:36, WARNING)
  [ExitAttempt] 시간청산 status=...      → 오늘 0건 (main.py:13455, WARNING, FLAT여도 무조건 기록)
  ```
  `trades.exit_reason = '15:10 강제청산'` → **2026-06-10~2026-08-14 전 기간 0건.**
  15:00 이후 청산은 사상 1건뿐이며 그것도 수동(`2026-06-22 15:00:38 수동 청산 33%→전량`).

- **기준 위반**: CLAUDE.md **절대원칙 §1**(오버나이트 금지 — 15:10 강제청산) · 매분 9단계 **STEP 8**
  (청산 트리거 감시 5단계 중 4단계). 계측 4원칙 ④(폴백 가시화) — 2차 안전망이 사실상 1차가 됐는데
  그 사실이 어디에도 기록되지 않는다.

- **영향**:
  1. 포지션이 15:10까지 남는 날, 청산은 **최소 60초 늦게**(15:11 스케줄러 틱) 이뤄지고
     그 경로는 `ERROR` 레벨 + `🚨 미륵이 시간청산 안전망 발동` 알림을 동반한다.
     즉 **정상 마감이 매번 사고처럼 보고된다** → 경보 피로로 진짜 사고를 놓친다.
  2. 절대원칙 §1 자체는 **뚫리지 않는다** — 453차 D2가 피드 무관하게 30초 QTimer에서 돌고,
     `is_market_open()`(일반일 15:35 / 만기일 15:20) 상한 안에서 재시도한다. 그래서 **P0가 아니다.**
  3. 다만 D2는 "완전 피드 스톨" 시나리오를 상정해 만든 최후 방어선이다. 그것이 상시 1차가 되면
     설계 의도상 **이중화가 사라진다**(단일 실패점화).

- **왜 지금까지 안 드러났나**: 진입 표본이 모두 15:10 이전에 하드스톱·TP·손절로 닫혔다.
  오늘도 11:48:55에 닫혔다. **1차·2차 경로 어느 쪽도 실전에서 한 번도 실행된 적이 없다.**

- **신규 여부**: **신규**. 453차 D2 docstring(`main.py:14590~14592`)이
  *"15:10 강제청산·15:18 FINAL_CLOSE는 전부 STEP 8(분봉 구동) 안에만 있었다"* 라고
  이미 STEP 8 의존성을 지적했으나, **STEP 8 자체가 15:10에 도달 불가라는 점은 지적하지 않았다.**

- ⚠ **확정 아님 — 코드 정독 기반 구조적 추론이다.** 라이브에서 15:10까지 포지션을 보유한 날이
  한 번도 없어 실측 반증·확증이 모두 없다. 판정에는 **F-1의 리허설 테스트**가 필요하다.

#### 1-2. `[SizerMatch]` 축소원인 오귀속 — 실제 원인 게이트(Hurst)가 **출력 목록에 아예 없다**

- **증상**: 오늘 유일한 진입에서 사이저 3계약 → 실제 2계약으로 줄었는데, `[SizerMatch]`가
  지목한 원인(`meta=0.50`)은 **같은 초에 중립화됐다고 기록된 값**이고, 실제로 0.50을 낸
  게이트(Hurst)는 출력 문자열에 존재하지 않는다.

- **근거** — 같은 초(11:48:00) 세 줄, `logs/20260814_SIGNAL.log:3165~3167`
  ```
  11:48:00 [MetaGate]  action=reduce meta_conf=43.7% size_mult=0.50 reason=meta_reduce
                       size_mult_sizing=1.00(무정보폴백→중립)
  11:48:00 [HurstGate] 하드차단 대신 사이즈축소: hurst=0.203 < 0.45 size_mult=0.50
                       (§3-6 FAIL 완화, 333차 후속) quality_min=0.50
  11:48:00 [SizerMatch] sizer=3계약 → actual=2계약 (gap=1) | kelly=0.60 meta=0.50 tox=1.00 exec=1.00
  ```
  `[Sizer]`(`logs/20260814_TRADE.log:3`)는 `신뢰도배수=0.6 레짐배수=1.0 안전배수=1.00 → 3계약`.
  품질군 min() 합성(431차)에서 실제로 최소값을 만든 것은 **HurstGate의 0.50**이며
  (`quality_min=0.50` 자체가 그 값이다) `3 × 0.50 = 1.5 → 반올림 2계약`이다.
  MetaGate는 **사이징 경로에서 1.00으로 중립화**됐다고 스스로 로그에 적고 있다.

- **기준 위반**: 계측 4원칙 ①·④ · CLAUDE.md 실전 전환 기준 ⑧의 근거 채널
  `VALIDATION_CAMPAIGN["sizing_inversion_watch"]`[28]이 읽는 값.

- **영향**: [28] 채널은 "사이저 출력 대비 실제 진입이 왜 줄었는가"를 이 문자열로 판정한다.
  현행 출력대로면 **MetaGate 탓으로 집계**되고 HurstGate는 감시망에 잡히지 않는다.
  ⑧ 해제 시 `MAX_CONTRACTS` 재산출이 이 오염된 근거 위에서 이뤄진다.
  ⚠ 오늘 표본은 **진입 1건**이다 — 313차 원칙에 따라 "Hurst가 상시 주범"이라는 확정 결론은 내지 않는다.

- **신규 여부**: **기존**(오늘 12:40 intra 리포트 P1, `NEXT_TODO` **F-5**로 등록).
  **본 장후 점검이 추가한 것**: ① `size_mult_sizing=1.00` 중립화 로그와의 직접 모순을 확정,
  ② **Hurst가 출력 목록에 아예 없다**는 사실(=값이 틀린 게 아니라 필드가 누락) — F-5의 범위가
  "meta 라벨 수정"이 아니라 **"품질군 전량 출력"** 으로 넓어진다.

---

### P2 — 운영 부담·기술부채

#### 1-3. WeightCollapse 비율이 **집계 방식에 따라 2배로 갈린다** — 오늘 실측 21.7%, 장중 리포트 43.7%

- **증상**: 장중 점검이 남긴 관측 O-1은 `90/206 = 43.7%`(12:32 기준)였고 판정기준은
  *"3거래일 연속 40% 초과면 앙상블 가중 산출 딥다이브"* 였다. 같은 시각 같은 로그를
  **분봉 단위**로 다시 세면 `46/207 = 22.2%`다. **기준선(40%)의 양쪽으로 갈린다.**

- **근거**
  ```
  [Ensemble] dir= 행 총 364건 중 [WeightCollapse] 태그 79건 → 21.7%  (12:32 시점 46/207 = 22.2%)
  [WeightCollapse] 실질 가중합 0 … WARNING 행 = 79건  (SIGNAL 로그 중복 0.0%)
  ```
  원인: `WeightCollapse`는 **한 분봉당 두 줄**(`[Ensemble] … [WeightCollapse]` INFO + `[WeightCollapse] 실질 가중합 0` WARNING)이 찍힌다.
  문자열 등장 횟수로 세면 분모는 분봉 수인데 분자만 2배가 된다(79×2=158 ≈ 관측된 90의 스케일).
  **EOD 자체 집계가 정답을 알고 있다** — `logs/20260814_SIGNAL.log:348`
  ```
  [ModelHealth] date=2026-08-14 앙상블유효가동률=76.7% | 파이프라인 369분 | ConstOut 5회/7분 {"3m": …} | WeightCollapse 79분 | 장중재학습 4회
  ```
  교차검증: 검증 캠페인 [20] BAR_ONLY_RELAX 채널 = `완화후 collapse=21.8% (기준선 44.9%, 목표 20%±7%p)`.
  **오늘 21.7%는 목표 밴드 안이다.**

- **기준 위반**: 계측 4원칙 ①(단위 명시 — 분봉 단위인가 언급 단위인가).

- **영향**: O-1 트리거(40%)가 방식에 따라 발동/미발동이 뒤집힌다. 오늘 값은 **정상 범위**이며
  **43.7%는 폐기한다 — 재인용하지 말 것.**

- **신규 여부**: 신규(관측 방법론 결함). 기존 O-1 항목의 값을 정정하며 승계.

#### 1-4. CB③-P4 상태전이 로그 — **`→ NORMAL` 복귀만 무기록**임이 실측으로 특정됐다

- **증상**: 오늘 전이 로그 5회 중 `RESTRICTED → WATCH`는 찍히는데, **`→ NORMAL` 복귀는 한 줄도 없다.**
  그래서 `NORMAL → RESTRICTED`가 두 번 찍히는 모순이 생긴다 — 그 사이 NORMAL로 돌아간 사실이 무기록이다.

- **근거** — `logs/20260814_WARN.log`
  ```
  10:56:59 [CB③-P4] acc30m 단계 전환: NORMAL → RESTRICTED (acc=13.3%)
  12:20:59 [CB③-P4] acc30m 단계 전환: NORMAL → RESTRICTED (acc=0.0%)   ← 그 사이 NORMAL 복귀가 무기록
  12:58:59 [CB③-P4] acc30m 단계 전환: RESTRICTED → WATCH (acc=30.0%)
  13:51:00 [CB③-P4] acc30m 단계 전환: WATCH → RESTRICTED (acc=26.7%)
  14:59:00 [CB③-P4] acc30m 단계 전환: RESTRICTED → WATCH (acc=30.0%)
  ```
  또한 `[CB③] acc30m 버퍼 리셋`이 오늘 4회(09:36:58 스킵 / 10:37:58 스킵 / 11:21:59 리셋 / 15:00:59 리셋)
  — **ConstOut 재적합마다 버퍼가 리셋**되므로 acc30m 시계열은 구간이 끊긴 값이다.

- **기준 위반**: CLAUDE.md 절대원칙 §2 CB③ / 계측 4원칙 ③(탈락 가시화).

- **영향**: RESTRICTED **지속시간**을 산출할 수 없다. 실전 전환 기준 ⑥(CB③-P4 복원 판정)이
  "얼마나 오래 RESTRICTED에 붙박였는가"를 근거로 삼아야 하는데 그 값을 만들 수 없다.
  TOX-SEVERE-SPREAD가 섀도 계측 부재로 복원 판단 불가였던 것과 **같은 함정**이다.

- **신규 여부**: **기존**(오늘 intra 점검 P2, `NEXT_TODO` **F-7**). 장후가 추가한 것은
  *"한 방향만 기록된다"*를 → *"`WATCH`로의 전이는 기록되고 `NORMAL` 복귀만 누락"* 으로 정밀화한 것.

#### 1-5. `entry_mode` 예외 폴백이 **가장 관대한 값**으로 조용히 떨어진다

- **증상**: 대시보드 조회가 실패하면 `entry_mode`가 `"manual"`로 폴백하는데, `manual`은
  **A·B·C 전 등급을 허용**하는 가장 넓은 모드다. 폴백이 일어나도 로그가 없다.

- **근거** — `main.py:8277~8287`
  ```python
  entry_mode = "manual"
  if getattr(self, "dashboard", None) is not None:
      try:
          entry_mode = self.dashboard.get_entry_mode()
      except Exception:
          entry_mode = "manual"          # ← 예외를 최광의 모드로 흡수, 로그 없음
  allowed_grades = {"auto": ["A"], "hybrid": ["A","B"], "manual": ["A","B","C"]}
  ```
  DB 실측(`predictions.db:ensemble_decisions`, 2026-07-01~): `manual` 11,590행 / `hybrid` 35행.
  **정상 설정값도 `manual`이므로 폴백 발생분과 구분이 불가능하다.**
  ※ `hybrid` 35행이 존재하므로 이 컬럼이 죽은 것은 아니다 — 문제는 **구분 불가**다.

- **기준 위반**: 계측 4원칙 ④(폴백 가시화 — *"`getattr`/예외 폴백으로 런타임 상태를 읽지 마라,
  폴백 시점에 로그를 남겨라"*).

- **영향**: 대시보드 예외가 진입 허용 등급을 `["A"]`에서 `["A","B","C"]`로 **조용히 넓힐 수 있다.**
  오늘 그 일이 일어났다는 증거는 없다(진입 1건은 최종 A등급이라 어느 모드에서도 통과).
  사후 분석에서 `entry_mode`로 수동/자동을 가르면 틀린다 — 같은 행에 `auto_entry=1`,
  `trades.entry_source='SYSTEM_AUTO'`가 함께 기록돼 있다.

- **신규 여부**: **신규**.

---

### 확인 필요 (확정 아님)

- **O-3 종결(부분) — ConstOut→S0→Degraded 자기유발 사슬은 오늘 기회손실 0이었다.**
  intra가 기록한 Degraded 선제차단 3건(+장후 15:03 1건 추가, 총 4건)의 해당 분봉을
  `ensemble_decisions`에서 조회한 결과:

  | 시각 | dir | conf | min_conf | 실제 기록된 차단사유 |
  |---|---|---|---|---|
  | 09:39 | -1 | 0.343 | 0.392 | 등급X — 미통과 2_confidence |
  | 10:39 | +1 | 0.361 | 0.383 | 자동진입 Degraded 최소신뢰도 62.0% 미달 |
  | 11:24 | -1 | 0.362 | 0.383 | 등급X — 미통과 2_confidence |
  | 15:03 |  0 | 0.384 | 0.970 | (EXIT_ONLY 구간 — 진입 대상 아님) |

  **네 분봉 모두 `conf < min_conf`라 Degraded가 없었어도 진입하지 못했다.**
  → F-1B(`PIPE_LATENCY_EXCLUDE_MODEL_SWAP` 체리픽)는 **손익 근거가 아니라 위생 근거**로 격하한다.
  ⚠ **1거래일 관측이다 — 313차 원칙에 따라 확정 결론 보류.** 5거래일 누적 후 재판정.
  ⚠ 부수 관측: 09:39·11:24는 로그가 `Degraded 선제차단`이라 찍었는데 DB에 남은 최종 사유는
  `등급X`다. **두 계측이 같은 분봉에 다른 이름을 붙인다** — F-4로 등록.

- **EOD `GuardFair`가 6/6 호라이즌 전부 "판정 불가"다 — 사이드카가 아직 비어 있다.**
  ```
  15:45:53 [GuardFair] 1m 판정 불가 — 오염 홀드아웃 1850봉 중 1509봉(82%)이 현행 학습구간
           … | 사이드카=현행 train_end_ts 없음 — 오염 여부 불명 — 판정 보류 (구모델 pkl mtime=2026-08-13 15:46)
  ```
  (3m만 사유가 다르다 — `사이드카=현행이 홀드아웃 학습함 … (source=intraday)`)
  457차 모델 메타 사이드카(`68d31a6`)가 **오늘 새로 저장된 모델부터** train_end_ts를 남기는
  구조라면 **내일 EOD에서 1m·5m·10m·15m·30m의 "train_end_ts 없음"이 사라져야 한다.**
  사라지지 않으면 FP-CRITICAL·TOX-SEVERE-SPREAD와 **동일한 "계산만 하고 저장하지 않는 섀도"**
  결함이다. → **O-1(내일 판정)** 으로 등록. 오늘은 판정하지 않는다.

---

### 이미 반영된 사안 (신규 아님) — 함정 ① 방지

확인했고, **아래 5건은 이미 코드에 있다. 신규 안건으로 올리지 않는다.**

| 사안 | 오늘의 실재 확인 | 근거 |
|---|---|---|
| **459차 F1 — 승패 집계 단위 레그→포지션** | `daily_stats(2026-08-14)`: `trades=1 wins=1 pnl_krw=28730 **win_formula_version=2**` · 로그 `15:40:16 일일 마감 \| 승=1 패=0 PnL=+28,730원`. **레그 2건이 포지션 1건으로 정확히 접혔다** | 커밋 `e8a56ea` |
| **431차 — MetaGate 무정보 폴백 사이징 중립화** | `11:48:00 [MetaGate] … size_mult=0.50 … **size_mult_sizing=1.00(무정보폴백→중립)**` — 사이징 경로만 중립화, 차단 기준값은 0.50 유지. 설계대로 동작 | DECISION_LOG 431차 |
| **461차 — Live MDD 분모 자본대비 정합** | 배너 `Live(20일): Sh=0.71 **MDD(자본대비)=3.3%**` + `MDD(peak대비)=199.8%` **병기**. 판정은 자본대비 사용 | 커밋 `c68e7b4` |
| **404차 후속11 — 확정 결정 레지스트리 렌더링** | `validation_campaign_report_20260814.md` 요약표에 📌 마커 **12개 채널**([2][3-B][4][5][6][8][13][16][18][21][23-B][25][25-B]) 정상 렌더 | `VALIDATION_CAMPAIGN_DECISIONS` |
| **371차 — RegimeFingerprint 분위수 PSI (O-7 종결)** | 배너 `PSI : **0.007 (CLEAR)** / PSI/feat: cvd=0.151 vwap_position=0.007 ofi=0.002`. 출처는 `strategy/ops/daily_exporter.py:116~130` → `strategy.regime_fingerprint.get_psi()` — **FP-CRITICAL과 같은 계측기다.** EOD 15:48:45 학습분포 갱신도 정상(8970행 기준) | 371차 |

> **O-7 종결**: intra가 *"채널 로그에 PSI 출력 0건"* 을 우려했으나, **333차 후속5 설계대로
> 대시보드 반복 표시만 제거되고 값은 살아 있다.** 오늘 PSI=0.007은 CRITICAL 임계(0.30)의 1/43이며,
> 2026-07-07·08의 "하루 종일 CRITICAL 고착(임계 대비 최대 4배)"과 질적으로 다르다.
> 실전 전환 기준 ⑦이 요구하는 *"정상 구간에서 PSI가 오르내리는 것을 실측 확인"* 의
> **라이브 경로 1일차 근거**다. 다만 `cvd=0.151`은 WATCH 밴드(≥0.10)에 있고 이는 371차가
> 잔여 이슈로 남긴 `cvd_divergence` 91% 점질량과 일치한다 — 계속 관측한다.

---

## 2. Fix 작업 구현계획

> **본 계획은 수립만 한다. 코드는 변경하지 않았다.** 사용자가 지시하면 별도 세션에서 구현한다.

### F-1. 15:10 강제청산 1차 경로 복구 — **P1** · 대응 이상점 1-1

- **원인 가설**: `_on_bar_close`의 `is_force_exit_time` 가드가 **예측 파이프라인 중단**을
  의도한 것인데, STEP 8(청산 감시)까지 함께 끊는다. 15:09 봉 콜백이 정확히 15:10:00에
  도착하므로 가드가 항상 먼저 걸린다.
- **변경 파일**
  - `main.py:4401~4403` — `_on_bar_close()`: 조기 반환 **직전에 청산 전용 패스 1회 호출**을 삽입.
    ```python
    if is_force_exit_time(now):
        _ts_run_force_exit_pass(self, candle)   # 신설: STEP 8만 평가, 예측·DB쓰기 없음
        return
    ```
  - `main.py:14521 근방` — `_ts_run_maintenance_pass()`를 참조해 `_ts_run_force_exit_pass()` 신설.
    유지보수 패스와 동일하게 `_maint_ctx` 재사용, `notify_pipeline_ran()`·`_last_real_pipeline_dt`
    **미갱신**(228차 취지 유지), `pending` 가드 최상단.
  - `main.py:14586` — `_ts_scheduler_force_exit_net()`: 발화 시각을 15:11 유지하되,
    **FLAT일 때도 1일 1회 하트비트를 남긴다**(아래 F-2와 통합).
- **회귀 위험**:
  ① 15:10 이후 STEP 8이 매 분 돌면서 15:18 FINAL_CLOSE 안전망과 **이중 주문**을 낼 수 있다
     → STEP 8 최상단의 `_has_pending_order()` 가드가 이미 막지만, 리허설로 확인 필요.
  ② `_maint_ctx`가 비어 있으면 `atr` 기본 0.5로 동작 — 손절가 재계산이 아니라 **시간청산**이므로
     ATR 의존이 없음을 코드에서 확인할 것.
  ③ 워치독/ExchangeCB 시계를 건드리면 228차 에스컬레이션이 깨진다 → 시계 미갱신 필수.
- **절대원칙 저촉 여부**: 없음. §1을 **강화**하는 방향이다. COM 콜백 내부가 아니므로 §4 무관.
- **런타임 주의**: `py37_32` 라이브 경로. COM 콜백 아님(분봉 마감 슬롯). 주문 전송은
  기존 `_send_broker_exit_order` 재사용 — 신규 COM 호출 0줄.
- **검증 방법**
  1. `tests/`에 시각 주입 유닛테스트 신설 — `is_force_exit_time`을 15:10:00으로 고정하고
     `position.status='SHORT'`일 때 `[ExitAttempt] 시간청산` 로그와 주문 호출이 1회 발생하는지.
  2. **리허설**: 다음 거래일 15:10 직전에 수동 1계약 진입 후 관측(사용자 판단 필요).
  3. 관측 로그 패턴: `[TimeExit] 15:10 강제 청산 트리거` + `[ExitAttempt] 시간청산` 이
     15:10:00~15:10:05에 출현, `[SchedForceExit]` 은 **미출현**이어야 정상.
- **섀도 계측 필요 여부**: 불필요(기존 게이트 신설이 아니라 도달성 복구).
- **적용 시점**: **다음 기동 전(장 마감 후)**. 라이브 청산 경로 변경이므로 장중 금지.
- **결정 필요 사항**: 리허설 진입을 실제로 할지(권고: **한다** — 이 경로는 6개월간 한 번도
  실행된 적이 없어 코드 리뷰만으로는 검증이 끝나지 않는다).

### F-2. 강제청산·안전망 **하트비트 로그** 신설 — **P2** · 대응 이상점 1-1

- **원인 가설**: 포지션이 FLAT이면 강제청산 경로가 **한 줄도 남기지 않아**,
  "대상 없음"과 "코드 사망"이 로그상 동일하다. 수집기 §11 적신호 6번이 매일 이걸 띄우고
  매번 사람이 수동 판별한다(오늘·어제 연속).
- **변경 파일**
  - `main.py:14618~14622` — `_ts_scheduler_force_exit_net()`의
    `if not _pos_open and _broker_cached <= 0: return False` 분기에서,
    **당일 최초 1회만** INFO 기록:
    ```
    [SchedForceExit] 15:11 점검 — status=FLAT engine=0ct broker_cached=0ct → 청산 대상 없음(정상)
    ```
    (`self._sched_force_exit_heartbeat_date`로 1일 1회 제한 — 30초마다 찍히면 로그가 죽는다)
  - `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py` — §11 적신호 6번의 판정을
    "`강제청산` 문자열 부재"에서 "**하트비트 또는 실집행 로그 중 하나가 있는가**"로 교체.
- **회귀 위험**: 없음(로그 1줄 추가).
- **절대원칙 저촉 여부**: 없음.
- **런타임 주의**: `py37_32`, QTimer 슬롯. COM 호출 없음.
- **검증 방법**: 다음 거래일 `logs/*_SYSTEM.log`에 `[SchedForceExit] 15:11 점검` 1건 출현.
- **적용 시점**: 다음 기동 전. **F-1과 같은 커밋**(같은 뿌리).

### F-3. `[SizerMatch]` 품질군 전량 출력 — **P1** · 대응 이상점 1-2 (기존 F-5 확장)

- **원인 가설**: `[SizerMatch]` 출력이 `kelly/meta/tox/exec` **4종 고정 리스트**라
  ① 사이징에 실제 쓰인 `size_mult_sizing`이 아니라 게이트 원값(`size_mult`)을 찍고,
  ② 품질군의 다섯 번째 축인 **Hurst가 아예 목록에 없다.**
- **변경 파일**
  - `main.py` `[SizerMatch]` 출력부(현행 9002~9010 근방) —
    `_quality_mults` 딕셔너리를 **전량 순회 출력**하고, `min()`을 만든 키를
    `binding=<key>`로 명시. 값은 **사이징 경로에 실제 투입된 값**을 쓴다.
    ```
    [SizerMatch] sizer=3계약 → actual=2계약 (gap=1) | binding=hurst
                 kelly=0.60 meta=1.00(raw0.50·무정보중립) tox=1.00 exec=1.00 hurst=0.50
    ```
- **회귀 위험**: [28] 채널이 이 문자열을 파싱한다면 **파서 동시 수정 필요**.
  → G-1(구조체 저장)로 넘기면 파싱 의존이 사라진다. **F-3은 G-1의 선행이다.**
- **절대원칙 저촉 여부**: 없음(로그 출력만).
- **런타임 주의**: `py37_32`, 분봉 파이프라인 STEP 7. COM 콜백 아님.
- **검증 방법**: 다음 진입에서 `binding=` 키가 실제 최소값 게이트와 일치하는지 1건 대조.
- **적용 시점**: 다음 기동 전.
- **결정 필요 사항**: 없음.

### F-4. Degraded 선제차단과 최종 차단사유의 **이름 불일치** 정합 — **P2** · 대응 "확인 필요" 항목

- **원인 가설**: `HealthPolicy`가 `Degraded 선제차단`을 WARNING으로 찍는 시점과
  `entry_block_reason`이 확정되는 시점이 달라, 최종 DB에는 더 앞단 사유(`등급X`)가 남는다.
  09:39·11:24가 그 사례이고 10:39만 Degraded로 남았다.
- **변경 파일**
  - `main.py` `entry_block_reason` 확정부 — 차단 사유를 **단일 문자열이 아니라 목록**으로
    누적하고(`;` 구분), 선제차단 플래그를 별도 컬럼 `health_preblock`(0/1)로 분리.
  - `utils/db_utils.py:402` 근방 스키마 — `ensemble_decisions`에 `health_preblock INTEGER` 추가.
- **회귀 위험**: **스키마 변경 — EOD 창(15:40~15:45)에서만 적용.** 기존 행은 NULL.
- **절대원칙 저촉 여부**: 없음.
- **런타임 주의**: `py37_32`. 장중 DB 스키마 변경 절대 금지(2026-08-10 전례).
- **검증 방법**: 다음 Degraded 발생일에 같은 분봉의 로그·DB 사유가 일치하는지.
- **적용 시점**: 장후 EOD 창. **F-1~F-3보다 후순위.**

### 적용 순서와 커밋 계획

1. **커밋 ①** — F-1 + F-2 (15:10 강제청산 도달성 복구 + 하트비트) —
   `[MW0601] 471차: 15:10 강제청산 1차 경로 도달성 복구 + 안전망 하트비트`
2. **커밋 ②** — F-3 (SizerMatch 품질군 전량 출력) —
   `[MW0601] 471차 후속: [SizerMatch] binding 게이트 명시 + 품질군 전량 출력`
3. **커밋 ③** — F-4 (차단사유 정합, 스키마 변경 동반) — EOD 창에서만
4. 승계: F-1(불변식 감시표 브랜치 인식형) · F-1B(462·468차 5종 체리픽 심사) ·
   F-3(수집기 `--pc`) · F-6(로그 이중기록) · F-7(CB③-P4 양방향) · F-8(STEP 3 문서 정정)
   — 오늘 pre·intra 등록분, 변경 없음

---

## 3. 고도화 방안

### G-1. 사이징 축소 원인을 **구조체로 저장** (기존 G-4 승계 · 근거 보강)

- **관측 근거**: 오늘 유일한 진입에서 문자열 파싱 기반 [28] 채널이 **원인 게이트를 잘못 지목**했고
  (P1-2), 실제 원인(Hurst)은 문자열에 존재조차 하지 않았다. 문자열을 고쳐도 다음에 게이트가
  하나 늘면 같은 일이 반복된다.
- **제안 내용**: 진입 시점 `_quality_mults` 전량 + `_meta_size_sizing` · `_qty_sizer_raw` ·
  `_qty_auto` + `binding_gate`를 `ensemble_decisions.sizing_trace`(JSON 컬럼)에 저장.
  [28] 채널 판정을 문자열 파싱 → 구조화 질의로 전환.
- **기대 효과**: 실전 전환 기준 ⑧의 근거 무결성 확보. 게이트 추가 시 자동 반영(파서 수정 불필요).
- **비용·위험**: 스키마 변경(EOD 창 한정). 기존 행 NULL → [28] 채널이 NULL 구간을 제외하도록 동반 수정.
- **선행 조건**: **F-3**(오귀속을 고치기 전에 만들면 틀린 값이 굳는다).
- **우선순위 제안**: 이번 주.

### G-2. `ConstOut(호라이즌)` 일별 카운트를 **호라이즌 건강지표로 승격** (기존 G-6 승계 · 종가값 확정)

- **관측 근거**: 오늘 `['3m']` 상수출력 확정 **4회**(09:35:59 / 10:36:59 / 11:20:59 / 15:00:00,
  intra 시점 3회에서 1회 추가). EOD 자체집계는 `ConstOut 5회/7분 {"3m": {"events":5,"minutes":7}}`.
  이 4회가 재학습 4회 → S0 스파이크(최대 2,570ms) → Degraded 선제차단 4회를 연쇄로 유발했고,
  **오늘 유일한 진입의 호라이즌도 3m이었다.** 즉 3m의 불안정이 시스템 전체 자원을 소모한다.
- **제안 내용**: `[ModelHealth]` EOD 라인에 이미 있는 `ConstOut {호라이즌: {events, minutes}}`를
  DB 일별 테이블로 영속화하고, N회 이상인 날의 해당 `entry_horizon` 진입을 **별도 버킷으로 태깅**(차단 아님).
- **기대 효과**: "3m ConstOut이 잦은 날의 3m 진입은 성적이 다른가"를 사후에 물을 수 있다.
- **비용·위험**: 낮음(집계 저장만, 라이브 거동 무변경). ⚠ **EOD에서만 집계** — 장중 DB 스캔 금지.
- **선행 조건**: 없음.
- **우선순위 제안**: 이번 주.

### G-3. 강제청산·안전망 경로의 **연 1회 리허설**을 운영 절차로 등록

- **관측 근거**: `trades.exit_reason='15:10 강제청산'`이 **2026-06-10~08-14 전 기간 0건**이고,
  `[TimeExit]`·`[ExitAttempt]`·`[SchedForceExit]` 모두 오늘까지 라이브 출현 0건이다.
  절대원칙 §1의 집행 경로가 **한 번도 실행되지 않은 채 6개월** 지났다.
- **제안 내용**: 26주 Walk-Forward 재검증 주기에 *"15:10 강제청산 리허설(모의 1계약)"* 을 편입.
  별도 캘린더를 만들지 않는다(CLAUDE.md "주기적 재검증 항목" 도입 취지 — 관리 포인트가 늘면 방치된다).
- **기대 효과**: 실전 전환 기준의 암묵 전제("§1은 코드에 있으니 지켜진다")를 실측으로 대체.
- **비용·위험**: 리허설 1건당 수수료 + 슬리피지(오늘 실측 왕복 수수료 3,270원/2계약 수준).
- **선행 조건**: **F-1**.
- **우선순위 제안**: 26주 WFA 주기.

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| 실전 전환 기준 ② (CB 1회 이상 정상 작동 확인) | CB③~⑤로만 충족 중 | **"15:10 강제청산 경로 실집행 1회 확인"을 별도 선행 확인사항으로 추가** | CB는 확인됐으나 절대원칙 §1 집행 경로는 6개월간 0회 실행 (P1-1·G-3) |
| 실전 전환 기준 ⑥ (CB③-P4 재검토) | `NEXT_TODO`에 "RESTRICTED 지속시간 계측 배선" 선행조건 등록됨 | **유지** — 오늘 근거 정밀화(`→NORMAL`만 누락)로 F-7 범위 확정 | ⑨ TOX-SEVERE-SPREAD와 동일 함정(섀도 계측 부재로 복원 판단 불가) |
| 실전 전환 기준 ⑦ (FP-CRITICAL 재검토) | False 유지, 라이브 관찰 중 | **관찰 근거 1일차 축적 — 유지** (PSI=0.007 CLEAR, feat별 0.002~0.151) | 371차 분위수 재설계 후 라이브 경로 첫 정상 구간 실측 |
| 실전 전환 기준 ⑧ ([28] 채널 근거 무결성) | `NEXT_TODO`에 선행 확인사항으로 등록됨 | **F-3 → G-1 순서 명기** | 문자열 수정 없이 구조체를 만들면 틀린 값이 굳는다 |

---

## 4. 진입 승패 사후검증 (장후) — 절차 `references/postmortem.md`

**4-0. 선행 확인**

| 관문 | 결과 |
|---|---|
| EOD `daily_close` (15:40) | ✅ `data/daily_close_done_20260814.txt` = `2026-08-14T15:40:24.014864` |
| **P8** (`p8_last_success_date`) | ✅ `15:48:45 [P8] 스케일러 재적합 완료 n=500봉 elapsed=0.04s horizons=['1m','3m','5m','10m','15m','30m']` + `[P8] session_state p8_last_success_date + eod_retrain_ok_date 기록 완료` |
| EOD 재학습 | ✅ `호라이즌 교체=6/6 가드보류=0 재학습=181.6s 로드=38.6s 합계=220.7s` · **`Python : 3.10.20 64-bit`(정상)** · RF 6/6 (OOB 0.451~0.530) |
| 검증 캠페인 10스텝 | ✅ 전부 `OK` (게이트 ablation · conf층화 · 판정 · 피처셋건강 · CVD앵커 · [49] · [40-B] · 섀도TB · 분위회귀 · 메타라벨) |
| DB 접근 | ✅ 실패 없음. 15:35 이후이므로 장중 DB 분석 차단 구간 아님 |
| 증거 수집기 | ⚠ `--pc` 미지원으로 래퍼 우회(위 머리말 참조). stderr 없음 |

**4-1. 오늘 할일 4분류**

오늘 pre·intra 세션이 등록한 항목은 **전부 "장후 적용" 조건부**로 등록됐고, 당일 커밋은 0건이다.

| # | NEXT_TODO 항목 | 당일 커밋 | 코드 실재 확인 | 상태 |
|---|---|---|---|---|
| F-1 | 불변식 감시표 브랜치 인식형 | 없음 | 미확인 | ⬜미착수(장후 적용 예정) |
| F-1B | 462·468차 스위치 5종 체리픽 심사 | 없음 | `config/settings.py` 5종 부재 확인 | ⬜미착수 — **근거 확정**(O-3 결과로 위생 근거화) |
| F-2 | `invariants.md` CB③ 35% 문구 정정 | 없음 | 미확인 | ⬜미착수 |
| F-3 | 수집기 `--pc` / `MIREUK_PC_ID` | 없음 | **미반영 확인** — `argparse: unrecognized arguments: --pc` | ⬜미착수 · **오늘 재발**(래퍼 우회 + 부수파일 1건) |
| F-5 | `[SizerMatch]` 축소원인 오귀속 | 없음 | `main.py` 출력 그대로 — 11:48:00 로그로 재확인 | ⬜미착수 · **범위 확장**(→ 본 리포트 F-3) |
| F-6 | 안전장치 로그 이중기록 제거 | 없음 | **실측**: WARN 중복 18/158행(11.4%) · SYSTEM 809/5,534행(14.6%) · SIGNAL 0.0% | ⬜미착수 |
| F-7 | CB③-P4 상태전이 양방향 기록 | 없음 | 전이 5회 중 `→NORMAL` 0건 확인 | ⬜미착수 · **근거 정밀화** |
| F-8 | STEP 3 재학습 주기 문서 정정 | 없음 | 오늘 장중 재학습 **4회**(09:36:59 / 10:37:59 / 11:21:59 / 15:01:00), 간격 61·44·219분 — **30분 주기 아님** 재확인 | ⬜미착수 |

> ⚠ 항목 없음 — "완료 주장인데 커밋 없음"에 해당하는 건은 **0건**이다(전부 미착수로 정직하게 등록됨).

**4-2. 3원 대사 (포지션 단위)**

| 원천 | 질의 | 값 |
|---|---|---|
| 로그 | `[Position] 진입` (`20260814_TRADE.log:6`) | **1** |
| `ensemble_decisions` | `entry_executed=1 AND date(ts)='2026-08-14'` | **1** (ts=11:47:00) |
| `trades` | `COUNT(DISTINCT entry_ts)` | **1** (entry_ts=11:48:01) |
| (참고) `trades` | `COUNT(*)` — 청산 레그 | **2** (TP1 부분청산 + 하드스톱) |

**세 값 일치 ✅.** 레그 2 > 포지션 1은 불일치가 아니다(계측 4원칙 ①).
`ensemble_decisions.ts`(11:47:00, 결정 분봉)와 `trades.entry_ts`(11:48:01, 체결 시각)의
1분 차이는 정상(분봉 마감 → 다음 분 주문).

**4-3. 케이스 카드**

```
[CASE-01] 진입 11:48:01 SHORT · 2계약 (등급 A / 원시 C · horizon=3m · hurst=mean-revert · regime=RISK_ON)
          → TP1 부분청산 33% 1ct @1089.18 (+0.67pt / +31,865원, 11:48:51)
          → 하드스톱(틱) 1ct @1089.88 (-0.03pt / -3,135원, 11:48:55)
          → 포지션 합 +0.64pt / **+28,730원** · 보유 **54초**

 진입근거   11:48:00 [진입체크] SHORT→SHORT 2계약 A급(원시C)
              | sign✅ conf✅ vwap✅ cvd✅ ofi❌ fore✅ prev✅ time✅ risk✅ chas❌ coun✅ | conf=43.8%
            11:48:00 [Position] 진입 SHORT 2계약 @ 1089.82 | 손절=1091.57 1차=1089.24(×0.42) 2차=1088.07
            11:48:00 [Sizer] 실효잔고=50,000,000 기본리스크=1,500,000 신뢰도배수=0.6 레짐배수=1.0 안전배수=1.00 → 3계약
            11:48:00 [SizerMatch] sizer=3 → actual=2 (gap=1) | kelly=0.60 meta=0.50 tox=1.00 exec=1.00
                     ⚠ 실제 binding 게이트는 HurstGate(quality_min=0.50) — P1-2 참조

 예측 vs 실현  conf=0.4377 (raw 0.4377 — 보정 미적용) / min_conf=0.383 → **+0.055 초과 통과**
            meta_size_mult=0.50(raw 0.0 = 무정보) → 사이징에는 1.00 중립 적용(431차)
            toxicity: action=pass, size_mult=1.00 · weight_collapsed=0 · meta_gate_horizon=3m
            ⚠ conf 절대값을 CLAUDE.md "확률 판단 기준" 표에 대지 않는다(2026-07-31 스케일 불연속).
               당일 min_conf 대비 상대 위치로만 읽는다.

 승패판정   ✅ **승** (포지션 단위 +28,730원)
            ⚠ `exit_reason='하드스톱(틱)'` 이지만 **TP1 도달 후의 본전 보호 청산**이다:
              진입 1089.85 → 스톱 1089.85 (`[TickStop-S0C] … stop=1089.85`, S0C=Stop-to-Cost).
              **의도 손절가 1091.57은 건드리지도 않았다.** 이 건을 손절 준수율 표본에 넣으면 오염된다
              (`postmortem.md` 주의: "하드스톱에 TP1 보호 이익 청산이 섞여 있다").

 주 요인    A(진입 판단 적중 — SHORT 방향으로 0.67pt 즉시 확보)
 부 요인    ① TP1 즉시 도달(51초) 후 4초 만에 되돌림 → 잔여 1계약이 본전에서 끊김
            ② 사이징 2계약 중 1계약만 이익 실현 (TP1 33% 규칙이 2계약에서는 1계약 = 50%로 반올림)
 반사실     ① TP1 보호전환 offset이 `atr_lock_0.50`이었다면 잔여 1계약도 이익으로 끊겼을 수 있다
              — 단 캠페인 [25]가 **"미적용 유지 — 주간회의 확정(2026-08-08)"** 이므로 신규 안건이 아니다.
            ② `MAX_CONTRACTS=3`·품질군 min()이 없었다면 3계약 → TP1 1계약(33%)·잔여 2계약 구조가
              됐을 것이나, **1건으로는 판정 불가**(313차).
```

**4-4. 요인 집계**

| 요인 | 승 | 패 | 순손익 | 평균 보유(분) | 관측 거래일 수 |
|---|---|---|---|---|---|
| A(진입 판단 적중) | 1 | 0 | +28,730원 | 0.9분 | **1일** |

> ⚠ **n=1 / 1거래일. 313차 원칙에 따라 어떤 확정 결론도 내지 않는다.**
> 참고 맥락(캠페인 리포트 20거래일): 포지션 139건 · [49] 페이오프 `edge_gap` 원화 **+5.20%p** PASS ·
> [47] 시뮬 재현충실도 PASS(bias +0.018R).

**4-5. 313차 가드 통과 여부**

| 갈래 | 적용 결과 |
|---|---|
| ① 일자단위 판정 | **관측 1일 — 판정 보류.** 일자단위 부호 확인 자체가 불가 |
| ② overlap 보정 | 해당 없음(포지션 1건, 중첩 없음) |
| ③ 이상치 분해(drop-worst) | 해당 없음(관측 1건 — 제거하면 표본 0) |
| ④ 사전등록 — 기준 불변 | ✅ 준수. 본 리포트는 `VALIDATION_CAMPAIGN` 합격선을 인용만 하고 변경 제안 없음 |
| ⑤ 부호 일관성(기간·PC 교차) | **미실시** — MW0602 최신 리포트는 `MW0602-20260808` (6일 격차, 코드 세대 상이). 대조는 금요일점검에서 |

---

## 5. 수익률 향상방안

> §4-4가 **n=1 / 1거래일**이다. `report_template.md` 규정상 6칸을 다 채울 수 있는 방안이
> 오늘 표본에서 나오지 않는다. **아이디어를 방안으로 위장하지 않는다.**

### R-1. (후보 — 표본 미달) TP1 이후 잔여계약 보호전환 offset

| 칸 | 내용 |
|---|---|
| 근거 | CASE-01 · 요인 A / 부요인 ① — TP1 도달 51초 후 4초 만에 되돌려 잔여 1계약이 본전(-3,135원)에서 끊김 |
| 변경 대상 | `strategy/exit/` TP1 보호전환 offset (현행 `breakeven`/`atr_profit` 혼재 — 캠페인 [24] 참조) |
| 기대효과 | **산출 불가** — 당일 표본 1건, 반사실 시뮬 미실행 |
| 회귀 위험 | offset을 넓히면 보호 실패(이익 반납) 증가. [24] 실측 중앙 반납 0.34pt |
| 검증 | 캠페인 [25] 채널이 이미 A/B 중 — **신규 계측 불필요** |
| 표본 상태 | n=1(당일) / 관측일 1일 / **313차 가드 미통과** — 채택 보류 |

> ⛔ **[25]는 2026-08-08 주간회의에서 "미적용 유지 — atr_lock_0.75만 감시 후보로 승격"으로
> 확정됐다**(`VALIDATION_CAMPAIGN_DECISIONS` 📌). 따라서 R-1은 **신규 안건이 아니라
> 기존 확정 결정의 당일 사례 1건 추가**다. 함정 ① 방지를 위해 명시한다.

---

## 6. 다음 거래일 관측 예정

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| **O-1** | EOD `[GuardFair]` 의 `사이드카=현행 train_end_ts 없음` 건수 | 오늘 6/6 → **내일 0~1건**이면 457차 사이드카 정상 자기치유. 그대로 6건이면 "계산만 하고 저장 안 함" 결함 확정 | 2026-08-18(월) |
| **O-2** | `WeightCollapse` **분봉 단위** 비율(`[ModelHealth]` EOD 라인의 `WeightCollapse N분` ÷ 파이프라인 분) | 오늘 **79/369=21.4%**. 캠페인 [20] 목표 20%±7%p. **3거래일 연속 27% 초과**면 앙상블 가중 산출 딥다이브. ⚠ 문자열 등장 횟수로 세지 말 것 | 2026-08-20 |
| **O-3** | `3m ConstOut` 확정 횟수 | 오늘 **4회**(전일 대비 확인 필요). 3거래일 연속 3회 이상이면 3m 피처셋 딥다이브 | 2026-08-20 |
| **O-4** | `IntradayRegime` 일일 전이 횟수 | 오늘 **32회**(NORMAL↔DAY_RISK_OFF 18 · DAY_RISK_OFF↔CRASH 10 · NORMAL↔CRASH 4). intra 12:32 시점 19회 → 종가 32회. **5거래일 누적 후 G-5(히스테리시스) 판정** | 2026-08-21 |
| **O-5** | Degraded 선제차단 분봉의 `conf` vs `min_conf` | 오늘 4/4 모두 `conf < min_conf`(기회손실 0). **5거래일 중 1건이라도 `conf ≥ min_conf`면** F-1B 체리픽을 손익 근거로 승격 | 2026-08-21 |
| **O-6** | `_tick_header ≥ 5,000ms` 일일 건수 | 오늘 **6건**(최대 9,125ms, 최다는 09:01:07 장시작 버스트). 15건 초과면 G-3 상향. ⚠ **CB⑤ 판정 대상 아님** — CB⑤는 `[PipePerf] total`(오늘 최대 2,830ms)을 본다 | 매일 |
| **O-7** | RegimeFingerprint `PSI` 및 `PSI/feat` | 오늘 **0.007 CLEAR**(cvd=0.151 / vwap=0.007 / ofi=0.002). **10거래일 중 CLEAR~WATCH 사이를 오르내리면** 실전 전환 기준 ⑦ 복원 판단 근거 성립. 하루라도 CRITICAL 고착이면 재설계 재검토 | 2026-08-28 |
| **O-8** | `[50] 방향 편향` — SHORT 초과 발행 | 오늘 앙상블 방향 분포 **SHORT 142 / LONG 33 / FLAT 194**(SHORT:LONG = 4.3:1). 캠페인 [50] 🟠 ALERT_BIAS(3m·5m Bonferroni 생존). 3거래일 연속 3:1 초과면 레이블/학습창 딥다이브 | 2026-08-20 |

---

## 7. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| **CB② 복원** (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | **2026-08-29** | **15일** | 2026-08-01 결산에서 4주 연기된 건. `min_samples` 미달 채널이 다수라 표본이 더 필요하다는 것이 연기 사유였다. 오늘 캠페인 기준 `[0] 표본 기아 경보 = OK(주간 32건)`이나 `[28] 사이징 역예측`은 여전히 `⏳INSUFFICIENT`(qty1=110·qty3+=15, 각 20 필요). **8/29 주간회의 안건으로 반드시 상정** |
| CB③-P4 재검토 (기준 ⑥) | 기한 없음(실전 전환 전) | — | 선행 **F-7**(RESTRICTED 지속시간 계측) 미착수 |
| FP-CRITICAL 재검토 (기준 ⑦) | 기한 없음(실전 전환 전) | — | 라이브 관찰 1일차 축적(O-7). 오늘 PSI=0.007 CLEAR |
| TOX-SEVERE-SPREAD 재검토 (기준 ⑨) | 기한 없음(실전 전환 전) | — | 선행 **섀도 계측 배선**(`spread_extreme_shadow` 영속화, `NEXT_TODO` F-8) 미착수 — 461차 등재 후 2일째 |
| 26주 WFA 재검증(Hurst N/max_lag · PSI 임계 · 호라이즌 피처셋) | 다음 WFA 주기 | — | 오늘 조치 없음. **G-3(강제청산 리허설) 편입 제안** |

---

## 8. dev_memory 반영

- `DECISION_LOG.md` 추가: `## 2026-08-14 (MW0601 470차 후속 — 장후 점검)` — **append 완료**
- `NEXT_TODO.md` 추가 체크박스: **10건** (F-1~F-4 · G-1~G-3 · O-1·O-5·O-8)
- 값 정정 처리: O-1 WeightCollapse **43.7% → 21.7%**(방식 오류, 43.7% 재인용 금지) ·
  O-4 ConstOut 3회 → **4회** · O-6 IntradayRegime 19회 → **32회**
- 종결 처리: **O-7 FP-CRITICAL PSI**(살아 있음 확인) · **O-8 CASE-01 3원 대사**(3원 일치) ·
  **O-3 Degraded 반사실**(오늘 기회손실 0 — 단 1일차)
- **git commit 하지 않았다.** 사용자 확인 후 커밋.
