# MW0601 — 2026-08-11 일일점검 리포트

> 대상: 08:40:48 기동 ~ 15:49:06 EOD 체인 종료 (전 구간)
> 근거: `logs/20260811_*.log` 11종, `retrain_eod_20260811.log`, `retrain_intraday_*.log` 5종,
> `predictions.db`(370 결정 / 1,254 예측), `trades.db`, 커밋 `54ae0c3`~`a675e20`(456차 Wave 0~4)
> ⚠ 모든 DB 조회는 장 종료(15:40) 이후 수행 — 절대원칙 §장중 DB 분석 금지 준수

---

## §0. 한 줄 요약

**청산은 정상 작동했고, 손실은 전적으로 진입 방향에서 나왔다.**
+2.0% 상승 마감일에 앙상블이 SHORT를 LONG의 **8배** 냈고(152 vs 19),
그 편향이 10:09 랠리 점화 봉 직전에 정확히 꽂혀 당일 유일한 패 -193,894원이 됐다.
동시에 **3m 모델이 하루 6번 상수 출력으로 붕괴**했고, 그 복구 재학습이
메인 스레드를 최대 4.4초 블로킹해 **신규 진입이 5회 차단**됐다.

| 지표 | 값 |
|---|---|
| 손익 | **-3,760원** (gross +5,000 / 수수료 -8,760) |
| 거래 | 3포지션 2승 1패 (승률 66.7%) · 청산 레그 6건 |
| 시장 | 964.84(08:45) → 985.36(15:33), **장중 저점 964.42 → 고점 992.86 (+2.9%)** |
| 예측 적중률 | 1m 32.6% / 3m 39.2% / 5m 39.3% / 10m 35.2% / 15m 39.1% / **30m 29.4%** (랜덤 33.3%) |
| CB⑤ 경고 | 9회 (최대 4,661ms — 즉시청산 임계 5,000ms의 **93%**) |
| Degraded 진입차단 | 5회 (전부 장중 재학습 직후) |

---

# §1. 작업흐름 종합정리

## 1-1. 장전 (08:40:48 ~ 09:00) — **정상**

| 시각 | 이벤트 | 결과 |
|---|---|---|
| 08:40:48 | FaultHandler 활성화 (PID 25048, 행감지 30s) | ✅ |
| 08:41:01 | DB 초기화 · FeatureBuilder 전일 종가 버퍼 384봉 로드 | ✅ |
| 08:41:14 | Calib 사전학습 18,000건 | ✅ |
| 08:41:15 | 앙상블 보정기 복원 n=296 **fitted=False degenerate=True** auc=0.491 | ⚠ 403차 축퇴 가드 정상 동작(설계대로) |
| 08:41:22 | BrokerSync — blank rows → FLAT 확정, armistice 해제 | ✅ |
| 08:41:22 | WarmupRetrain "즉시 재학습 예약" | ⚠ **소진 로그 없음** (§2 C8) |
| 08:41:23 | Capital 기준자본 5천만 / 실잔고 50,027,130 (괴리 +0.1%) / **MAX_CONTRACTS=3** | ✅ 431차 배포 반영 |
| 08:45:22 | EarlyWarmup (scaler 노후 17h) → 30봉 재적합 + RT 선행 구독 | ✅ |
| 08:47~08:54 | PreMarket Phase1→2→3 refit — z경고 **24 → 7 → 5 → 4개** | ✅ 단조 수렴 |
| 08:55:22 | 매크로 seed fetch | ✅ |
| 08:55:23 | **"🌡 Canary 이상 감지" 알림 → 같은 초에 "✓ 임계 이하" 해소** | ⚠ 알림 순서 결함 (§2 C7) |
| 08:55:23 | PreRetrain 스킵 (1영업일 전 EOD 성공) | ✅ 정상 |
| 08:58:25 | 레짐 확정 **RISK_ON** (VIX 14.9 극저공포 / SP500 -0.06% / KRW +0.18%) | ✅ |

> 장전 시퀀스 자체는 설계문서(`CLAUDE.md` 매분 파이프라인 08:55 항목)와 완전히 일치하며
> 이상 없음. 스케일러 3단 refit이 z경고를 24→4로 줄인 것은 371차 워밍업 설계가
> 의도대로 동작한 결과다.

## 1-2. 장중 (09:00 ~ 15:10)

### 시장 컨텍스트 — 오늘은 "상승 추세일"이었다

```
08:45  964.84  ─┐
10:08  964.66  ─┘ 저점권 횡보 (83분)
10:09  968.42   ← 점화 (단봉 +4.0pt, V=629)
10:14  980.38   ← 5분간 +12pt
10:23  991.86   ← 14분간 +23pt (V=1144 피크)
10:24  992.86   ← 당일 고점
15:33  985.36     고점 대비 -7.5pt 되돌림 후 마감 (+2.0% 종가 기준)
```

### 신호 생성 — **SHORT 8:1 편향**

| 시간대 | dir=-1 (SHORT) | dir=+1 (LONG) | dir=0 (FLAT) |
|---|---|---|---|
| 09시 | **34** | **0** | 21 |
| 10시 (랠리 구간) | **27** | 5 | 28 |
| 11시 | 23 | 5 | 31 |
| 12시 | 21 | 2 | 37 |
| 13시 | 21 | 2 | 37 |
| 14시 | 26 | 2 | 32 |
| 15시 | 0 | 3 | 8 |
| **합계** | **152 (41%)** | **19 (5%)** | **199 (54%)** |

호라이즌별 예측 방향 vs 실제 라벨 (`predictions.db`, 채점 완료분):

| 호라이즌 | 예측 UP:DN | 실제 UP:DN | 적중률 | 판정 |
|---|---|---|---|---|
| 1m | 115 : 147 (1:1.3) | 115 : 112 | 32.6% | 편향 경미, 적중 랜덤 이하 |
| 3m | 33 : 133 (**1:4.0**) | 78 : 80 | 39.2% | **실제는 균형인데 4배 SHORT** |
| 5m | 17 : 73 (**1:4.3**) | 52 : 46 | 39.3% | **실제는 UP 우위인데 4배 SHORT** |
| 10m | 11 : 47 (**1:4.3**) | 22 : 21 | 35.2% | 동일 |
| 15m | 9 : 10 | 12 : 7 | 39.1% | 표본 46건, 판단 보류 |
| 30m | 22 : 145 (**1:6.6**) | **131 : 68** | **29.4%** | **역방향 확정 — 랜덤 이하** |

> **이것이 오늘 최대 이상점이다.** 30m은 실제가 UP:DN = 1.9:1 인데 예측은 DN:UP = 6.6:1로
> 정확히 뒤집혀 있고, 적중률 29.4%는 3분류 랜덤(33.3%)보다도 낮다. 3m·5m·10m도 같은
> 방향의 편향을 공유한다. 1m만 편향이 경미하다 — **편향의 원천이 단기 미시구조 피처가
> 아니라 중·장기 호라이즌 레이블/모델 쪽에 있음을 시사**한다.

### 진입 판단 깔때기 (370분 중)

```
앙상블 결정          370
├─ grade X          308  (83.2%)
├─ grade C           60  (16.2%)
├─ grade B            2
└─ grade A            0   ← 앙상블이 직접 A를 준 적이 하루 종일 0건

MetaGate            skip 232 / reduce 111 / take 27
ToxicityGate        pass 216 / reduce 148 / block 6
WeightCollapse      82분 (22.2%) — conf 0.85 고정 "판단 불가" 행

진입 시도             4
├─ 실행               3   ← 전부 raw_grade=C → A 승격 (F10-B 대상)
└─ JointGateBlock     1   (13:24, meta=0.50 × tox=0.70 = 0.350)
```

주요 차단 사유: `등급X — 미통과 항목: 2_confidence` **95회**(최다),
`3_vwap` 계열 조합 27회, `14:50 이후 신규진입 금지` 5회, 청산 후 쿨다운 7회.

진입 호라이즌 라우터 선택 분포: **3m 122회(82%)** / 5m 15회 / 1m 12회 →
F10-A가 지적한 "ATR 밴드만으로 3m 편중"이 오늘도 그대로 재현됐다(계획서 실측 73% → 오늘 82%).

### 거래 3건 딥다이브

| # | 진입 | 방향/수량 | 진입가 | 청산1 | 청산2 | 순손익 | 보유 |
|---|---|---|---|---|---|---|---|
| 1 | 10:03:00 | SHORT 2ct (3m) | 966.07 | TP1 965.20 **+42,051** | 하드스톱(틱) 966.48 **-21,949** | **+20,102** | 52초 |
| 2 | 10:08:59 | SHORT 2ct (3m) | 964.84 | 손절1차 966.14 **-66,447** | 하드스톱(틱) 967.36 **-127,447** | **-193,894** | 48초 |
| 3 | 10:49:59 | LONG 2ct (5m) | 989.00 | TP1 990.86 **+91,516** | 하드스톱 990.60 **+78,516** | **+170,032** | 61초 |

**거래 2 — 당일 유일한 패, 그리고 유일하게 중요한 케이스**

- 체크리스트 10/11 통과 (`chas❌`만 실패), conf 56.0%, raw C → A 승격
- 진입 시각 10:08:59 = **점화 봉(10:09, O=965.02 H=968.54 V=629)의 1초 전**
- 83분간 964~968 박스권을 SHORT로 진입 → 다음 봉에서 +4pt 급등
- **청산 시스템은 옳게 작동했다**: 3m 호라이즌 만료(10:12) 시점 가격은 972.32로
  진입 대비 **-7.5pt**였다. 손절1차(360차) + 하드스톱(틱, 348차)이 -2.52pt에서 끊어
  약 5pt를 방어했다.
- → **처방 경로는 청산이 아니라 진입에 있다.**

**거래 1·3 — 승리 2건도 "이익 절단"으로 단정할 수 없다**

- 거래 1: 러너를 966.48(-0.41pt)에서 끊었으나 3m 만료 시점(10:06 C=966.02)은 +0.05pt.
  즉 홀딩해도 실익 0.05pt — 절단 손실 사실상 없음.
- 거래 3: 러너를 990.60(+1.60pt)에서 끊었고 5m 만료 시점(10:55 C=990.00)은 +1.00pt.
  **조기 청산이 오히려 유리했다.** 다만 구간 최대(992.80, +3.80pt) 대비 포착률은 42%.
- → 오늘 1일 표본으로 F4(보호스톱 지연) 방향을 정할 근거는 **없다**. 섀도 10거래일 유지가 맞다.

**수수료 구조** *(457차 정정 — §2 A3 참조)*

gross +5,000원 / 수수료 **8,760원** → net -3,760원.
3포지션이 6레그로 분해됐지만 **레그 분할은 수수료와 무관하다** — 수수료는
계약수에만 비례한다(부분청산 없이 3레그였어도 8,760원, 차이 0원).
문제는 **회전율 대비 엣지**다: 왕복 손익분기 **0.0290 pt/계약**인데
당일 gross는 0.10pt ÷ 6계약-청산 = **0.017 pt/계약**으로 미달이었다.

### 장중 안정성 사고 — CB⑤ 9회 / 진입차단 5회

| 시각 | total | 지배 구간 | 직전 이벤트 | 결과 |
|---|---|---|---|---|
| 09:38:04 | **4,661ms** | S0=4,381ms | 장중재학습 09:37:45 종료 | Degraded 차단 09:38:59 |
| 10:21:03 | 3,830ms | S0=3,585ms | 장중재학습 10:20 | Degraded 차단 10:21:59 |
| 11:13:03 | 3,823ms | S0=3,197ms | 장중재학습 11:12 | Degraded 차단 11:13:59 |
| 12:22:00 | 1,081ms | S6=809ms | — | — |
| 12:26:01 | 1,289ms | S6=1,048ms | — | HealthTrend 3.0배 선제경보 |
| 12:44:03 | 3,643ms | S0=3,401ms | 장중재학습 12:43 | Degraded 차단 12:45:00 |
| 13:14:02 | 3,211ms | S0=2,979ms | 장중재학습 13:13 | Degraded 차단 13:15:00 |
| 13:46:00 | 1,241ms | S6=709ms | — | — |
| 14:31:00 | 1,010ms | S6=679ms | — | — |

**S0 스파이크 5건은 장중 재학습 5건과 5/5 시각 일치**한다 — 우연이 아니라 결정론적 인과다.
**S6 스파이크 4건은 전부 SHAP 계산**이다(`[S6Detail] shap=460~521ms`가 매분 상시).

> ✅ **456차 Wave 0(F8) 검증 게이트 통과.** 계획서 §7이 요구한 "다음 거래일 장중
> CB⑤ **자가유발** 0건"은 충족됐다 — 오늘 9건 중 분석 세션 기인은 0건이고 전부
> 재학습/SHAP 내부 요인이다.

### 3m 모델 붕괴 루프 (오늘의 숨은 축)

```
09:35:59  ConstOut 3m 상수출력 5분 감지 (range=0.0000) → 앙상블 제외
09:36:59  → 스케일러 재적합 + GBM 재학습 예약
09:37:45  장중 재학습 완료 (4,800행, CV 없음)
09:38:04  모델 리로드 S0=4,381ms → CB⑤ + 진입차단
09:38:04  ConstOut 해소 → 앙상블 복귀
   … 10:17 / 11:11 / 12:41 / 13:12 / 13:21 에 3m 반복, 11:10에 5m 1회
```

**3m 상수 출력 6회 / 5m 1회.** 진입 호라이즌의 82%를 담당하는 모델이
하루 6번 "출력이 상수로 굳어" 앙상블에서 퇴출됐다 반복 복귀했다.

## 1-3. 장후 (15:10 ~ 15:49) — **정상, 단 계측 결함 3건 잔존**

| 시각 | 이벤트 | 결과 |
|---|---|---|
| 15:10 | 강제청산 — 포지션 FLAT이라 미발동 | ✅ (절대원칙 §1 위반 없음) |
| 15:34 | 마지막 분봉 수신 종료 | ✅ |
| 15:40:22 | 마감 통계 `trades=3 wins=2 losses=1 win_rate=0.667` | ✅ **F1(승패 집계 단위) 정상 동작 확인** |
| 15:40:23 | Calibration 저장 n=592 · CB/EKS/SHS 일일 리셋 | ✅ |
| 15:40:23 | DriftDetector 종합 **CLEAR** (pnl/wr/pf 전부) | ✅ |
| 15:40:25 | WAL 체크포인트 6개 DB | ✅ |
| 15:40:40 | 정상 종료 플래그 기록 후 자동 종료 | ✅ |
| 15:45:04 | EOD 재학습 시작 (Python 3.10.20 64bit) | ✅ 191차 규약 준수 |
| 15:45:51 | DB 로드 39,826행 × 97피처 (2026-02-10 ~ 08-11 14:39) | ✅ |
| 15:46~15:48 | 6호라이즌 CV — **교체 5 / 보류 1(3m)** | ⚠ §2 A2 |
| 15:48~15:49 | RF 6호라이즌 OOB 45.8~52.8% | ✅ |
| 15:49:06 | RegimeFingerprint 학습분포 갱신 (7,944행, 백필 31,882행 제외) | ✅ |
| 15:49:06 | P8 스케일러 최종화 500봉 | ✅ |

**EOD 가드 판정 6/6 전부 "판정 보류"**

```
[GuardFair] {h} 판정 불가 — 오염 학습행 타임스탬프 미상(외부 주입 X 경로) — 판정 보류
[GuardGhost] {h} 비교 기준이 유령이다 — 배포된 pkl은 CV 미검증 intraday 모델
             (학습 2026-08-11 12:42:00까지)인데 acc.txt=…는 다른 모델의 성적이다.
```

456차 Wave 3(F6+F7)이 어제 배포됐고 오늘이 **첫 관측일**이다.
사이드카 생성·GuardGhost 경고는 정상 작동하나, **F6의 오염 검출은 구조적으로
"미상"만 반환**하고 **F7의 유령 판정은 교체 정책에 반영되지 않는다**(§2 C1·C2).
결과: **3m 교체 보류 7거래일 연속**(08-03·04·05·06·07·10·11).

---

# §2. 이상점 및 Fix 구현계획

> 등급: **A**=손익 직결 / **B**=운영 안정성 / **C**=계측·관측성
> 배포 제약은 456차 계획서 §6을 승계한다.

## A1. 방향 예측 SHORT 편향 — 전 호라이즌 (30m 최악)

| 항목 | 내용 |
|---|---|
| 증상 | 상승 +2.0% 마감일에 SHORT:LONG = 152:19. 30m은 예측 DN:UP 6.6:1인데 실제 UP:DN 1.9:1, 적중률 29.4% (랜덤 이하) |
| 범위 | 3m·5m·10m·30m 공통. 1m만 경미 |
| 미확정 | **원인 미규명.** 후보 3개 — ① 레이블 임계(`USE_FIXED_LABEL_THRESHOLD=True`) 비대칭 ② `make_sample_weight` 클래스 가중 ③ 26주 학습창(2026-02-10~)의 하락 편중 |

**계획 — Step 1 진단만 (조치 금지)**

```
S1-a  학습 레이블 분포 산출: 26주 X에서 호라이즌별 UP/DN/FLAT 비율
      → 학습 분포가 이미 DN 편중이면 원인 ①③, 균형이면 원인 ② 또는 모델
S1-b  오늘 예측 확률 3분위 분해: up_prob/down_prob/flat_prob 평균을
      호라이즌×시간대로. down_prob가 상수적으로 높으면 절편 편향
S1-c  30m 단독 재현: rejected/ 폴더 모델과 배포 모델의 홀드아웃 방향분포 대조
```

- 산출: [scripts/direction_bias_diagnosis.py](scripts/direction_bias_diagnosis.py)
  (신규, **읽기 전용**, `guard_intraday` 배선 완료)
- 리스크: 0 (계측)
- **조치는 진단 확정 후 별건** — 313차 원칙상 1일 표본으로 레이블 체계를 바꾸지 않는다
- 배포: 장후/장전

### ✅ 진단 결과 (2026-08-11 실행, 60거래일 2026-05-15~08-11, n=85,263)

**S1-a 실현 레이블 — 편중 아님.** 일자단위 skew(UP−DN) 평균 −0.009~−0.044,
6개 중 5개가 비유의(5m만 p=0.045 경계). **학습 라벨은 균형이다.**

**S1-b 예측 확률 — 전 호라이즌 DN 편중, 전부 유의.**

| 호라이즌 | up_prob | down_prob | gap | t | p |
|---|---|---|---|---|---|
| 1m | 0.347 | 0.361 | −0.014 | −2.43 | 0.015 |
| **3m** | 0.295 | 0.358 | **−0.063** | **−6.26** | **<0.0001** |
| **5m** | 0.283 | 0.355 | **−0.072** | **−6.97** | **<0.0001** |
| 10m | 0.285 | 0.334 | −0.049 | −3.43 | 0.0006 |
| 15m | 0.261 | 0.319 | −0.058 | −3.44 | 0.0006 |
| 30m | 0.285 | 0.336 | −0.051 | −3.30 | 0.0010 |

**S1-c 시장 대비 편향 — 3m·5m가 Bonferroni 생존** (Δ=−0.171 t=−5.32 / Δ=−0.141 t=−4.90).

> ### 결론: **모델 절편 편향이다. 레이블도 학습창도 아니다.**
> 라벨은 균형인데(S1-a) 확률 출력이 전 호라이즌에서 DN 쪽으로 치우쳐 있다(S1-b).
> 원인 후보는 **클래스 가중(`make_sample_weight`) 또는 보정기**로 좁혀진다.
> 8/11의 8:1은 이 상시 편향의 극단 발현이었다(60일 누적으로는 3m 기준 4:1 수준).

> ### ⚠ 반전은 답이 아니다 — 성급한 처방 배제
> 역방향 검정에서 예측을 뒤집으면 적중률이 **오히려 떨어진다**
> (3m 0.347 → 0.337, 5m 0.348 → 0.340). **역방향 알파는 없다.**
> "SHORT 편향이니 뒤집자"는 조치는 근거가 없으며, 적중률도 랜덤 대비 유의하게
> 높지 않다(1m t=+3.14만 유의). 편향 교정은 방향 반전이 아니라 **절편 보정** 축이다.

> ⚠ 30m은 296차에서 앙상블·CoherenceGate·CascadeCoherence에서 **이미 퇴역**한 호라이즌이다.
> 따라서 30m 29.4%가 오늘 손익에 직접 기여했다는 증거는 없다. 다만 **같은 편향이
> 실제 진입을 담당하는 3m·5m에도 있다**는 점이 문제다. 진단은 3m·5m을 1순위로 한다.

## A2. 3m 모델 붕괴 루프 — ConstOut 6회 + EOD 교체 7일 연속 보류

| 항목 | 내용 |
|---|---|
| 증상 | 3m이 하루 6회 상수 출력(`range=0.0000`)으로 앙상블 퇴출·복귀 반복. 매 회 장중 재학습 유발 |
| 연쇄 | ConstOut → 스케일러 재적합 → GBM 재학습 → 모델 리로드 S0 3~4.4초 → CB⑤ → **진입차단 5회** |
| 근원 | EOD 교체가 **유령 acc.txt 기준선**(0.4651, 07-22자)에 7거래일 연속 막혀 배포본이 계속 **CV 미검증 장중 모델**이다. 검증 안 된 모델이 굳고 → 붕괴하고 → 또 검증 안 된 모델로 덮인다 |
| 노출 | 진입 호라이즌 선택의 **82%가 3m** |

**계획 — C1·C2 선행 필수 (아래 참조). 이 항목 단독 조치 금지.**

C1(F6 정상화) + C2(F7 판정 반영)가 배포되면 3m EOD 교체가 재개되고,
그때부터 5거래일 ConstOut 발생 횟수를 재측정한다.
5거래일 후에도 3일 이상 ConstOut이 발생하면 **모델 자체 문제**로 승격해
`gbm_3m` 하이퍼파라미터·피처셋(현행 97→12개 슬라이싱)을 별건 조사한다.

- 검증 게이트: 배포 +5거래일 ConstOut 3m ≤ 2회/일 · EOD 3m 교체 최소 1회 성립
- 배포: **장중 금지** (계획서 §6 F7 제약 승계)

## A3. 회전 비용 엣지 — gross +5,000원 < 수수료 8,760원

> 🔴 **[2026-08-11 457차 자체 정정] 이 항목의 최초 진단은 틀렸다.**
>
> 초판은 *"TP1 33% 부분청산이 레그 수를 2배로 만들어 회전 비용을 정확히 2배화한다"*
> 고 썼다. **거짓이다.** 수수료 공식은
> `entry_price × quantity × pt_value × RATE × 2` ([db_utils.py:101](utils/db_utils.py#L101))
> 로 **레그 수가 아니라 계약수에만 비례**한다. 실측 대조:
>
> | 가정 | 수수료 |
> |---|---|
> | 실제 6레그(qty=1씩) | **8,760원** |
> | 부분청산 없이 3레그(qty=2씩) | **8,760원** |
> | 차이 | **0원** |
>
> → **부분청산은 비용 축에서 무해하다.** 이 불변식은
> `tests/test_457_daily_fixes.py:test_commission_is_leg_split_invariant`로 고정했다.
> 아래는 정정된 진단이다.

| 항목 | 내용 |
|---|---|
| 증상 | gross **+5,000원** / 수수료 **8,760원** → net **-3,760원** (승률 66.7%인데 적자) |
| 손익분기 | 왕복 = 진입가 × RATE × 2 ≈ 966 × 0.000015 × 2 = **0.0290 pt/계약** |
| 당일 실적 | gross +0.10pt ÷ 6계약-청산 = **0.017 pt/계약** — 손익분기 **미달** |
| 반복성 | 08-10 8승2패 **-244,067원** / 08-03 15승9패 **-730,619원** — 같은 패턴 |
| 진짜 원인 | **회전율 대비 엣지 부족.** 현행 게이트 어디에도 *"이 진입의 기대 이동폭이 왕복 비용을 넘는가"* 를 묻는 축이 없다 |

**계획 — 사전등록 섀도 채널 (조치 금지)**

`quantile_expected_pt`(MetaGate 분위회귀)가 **이미 기대 이동폭을 pt로 내고 있다** —
계산은 하는데 비용과 대조하지 않는다. 그것이 비용 커버를 예측하는지 먼저 잰다.

```
축1 비용 커버율   포지션별 gross_pt/계약 > 왕복비용 인 비율      (사후 서술)
축2 사전 예측력   quantile_expected_pt 가 축1을 예측하는가       ← 핵심
```

- 사전등록: `config/settings.py:VALIDATION_CAMPAIGN["cost_edge_watch"]`
  (min_samples=40 · min_days=10 · rho>0.20 · |t|>2.26 · 3분위 격차>15%p, `data_start=2026-08-12`)
- 하네스: [scripts/cost_edge_shadow.py](scripts/cost_edge_shadow.py) — 읽기 전용,
  포지션 단위(417차) · 일자단위(372차) · **실체결가만** 사용
- 리스크: 0 (계측)
- 배포: 장후/장전 · 판정은 10거래일 후

**소급 참고치 (2026-06-22~08-11, 170포지션 29일) — 판정 아님**

| 축 | 값 |
|---|---|
| 비용 커버율 | 61.2% (일자평균 60.8%) |
| gross 중앙값 | 0.4200 pt/계약 (손익분기 0.0331) |
| 순손익 합계 | **-1,241,242원** (gross -704,465 / 수수료 536,777) |
| 축2 rho (일자단위) | 0.156, t=0.96 — **0 배제 실패** |
| 3분위 커버율 격차 | **-2.4%p** (상위군이 오히려 낮음) |
| 소급 산출 | `REJECTS_HYP` — **`quantile_expected_pt`는 비용 커버를 예측하지 못한다** |

> ⚠ `data_start` 이전 구간이라 스크립트가 `BACKFILL_ADVISORY`로 강등해 출력한다.
> 사전등록의 요점은 "결과를 보고 기준을 고치지 않는 것"이므로 소급 실행을 판정으로
> 부르지 않는다. 다만 방향은 분명하다 — **처방은 게이트 신설이 아니라 분위회귀 재설계**
> 쪽일 가능성이 높다. G6(비용 엣지 게이트)의 전제가 흔들린다는 뜻이다.

> ⚠ 오늘 데이터만으로 "TP1 부분청산을 없애자"는 결론은 낼 수 없다 — 애초에 비용
> 근거가 없었고, 거래 3은 부분청산이 **유리했다**(러너 +1.60pt > 5m 만료 +1.00pt).
> 부분청산의 **손익 기하**는 [1] `tp1_protect_forgone_mfe`(456차)가 MFE 관측으로 담당한다.

## B1. 장중 재학습 모델 리로드가 메인 스레드 3.0~4.4초 블로킹

| 항목 | 내용 |
|---|---|
| 위치 | [main.py:4808-4826](main.py#L4808) `[S0] 재학습 완료 모델 교체` |
| 원인 | `self.model._load_all()` (GBM pkl 6개) + `self.rf_model.load_all()` (RF pkl 6개) + `_rebuild_sgd_feat_indices()`를 **Qt 메인 스레드에서 동기 실행** |
| 실측 | 5/5 재학습 직후 S0 = 2,979 / 3,197 / 3,401 / 3,585 / **4,381ms** |
| 위험 | 4,661ms는 CB⑤ **즉시청산** 임계 5,000ms의 93%. 포지션 보유 중 발생하면 **의도치 않은 강제 청산** |
| 피해(실측) | HealthPolicy Degraded 선제차단 **5회** — 신규 진입이 5개 구간에서 봉쇄됨 |

**계획 — 워커 스레드 선로드 + 메인 스레드 원자 스왑**

```
현재:  [메인] _pending_model_reload → joblib.load × 12 (3~4.4s 블로킹) → 참조 교체
개선:  [워커] joblib.load × 12 → 완성된 객체를 _deferred_callbacks.put(("model_ready", obj))
       [메인] S0-B drain에서 참조만 교체 (수 μs)
```

- **선례 있음**: ConstOut 재적합이 이미 정확히 이 패턴이다
  ([main.py:4765](main.py#L4765) `S0-B ConstOut 재적합 완료 콜백 큐 drain`).
  새 메커니즘이 아니라 **기존 패턴의 재사용**이다.
- 절대원칙 §4 저촉 없음 — COM 콜백이 아니라 순수 파일 IO + pickle 워커다
- 차원 불일치 검사(`_bad = self.model._load_all()`)는 **워커에서 수행**하고,
  실패 시 스왑을 취소한 뒤 메인 스레드에서 `_start_manual_retrain(force=True)` 재트리거
- 리스크: **중** — 예측 경로가 참조하는 모델 객체를 바꾼다. predict 중 스왑 방지를 위해
  기존 193차 설계(파이프라인 실행 중 교체 금지)의 안전 지점 제약은 그대로 유지
- 검증: 배포 다음 거래일 재학습 직후 S0 < 200ms · CB⑤ S0 기인 0건
- 배포: **장후/장전** (장중 배포 금지)
- 규모: 중

## B2. S6 내 SHAP 계산이 매분 460~520ms 상주

| 항목 | 내용 |
|---|---|
| 실측 | `[S6Detail] ensemble=1ms checklist_pre=9ms meta_gate=5ms gates=0ms **shap=478ms** corr=4ms` |
| 영향 | CB⑤ 9건 중 **4건(12:22·12:26·13:46·14:31)의 단독 원인**. HealthTrend 3.0배 선제경보 유발 |
| 성격 | SHAP은 **진입 판단에 쓰이지 않는다** — 대시보드 표시 + 피처 심사용이다 |

**계획 — 진입 경로에서 분리**

```
① SHAP 계산을 S6에서 제거하고 별도 QTimer(예: 5분 주기)로 이관
② 또는 워커 스레드 비동기화 (B1과 동일 패턴)
```

- ①이 단순하고 위험이 낮다. SHAP 갱신 주기가 1분 → 5분으로 늘어나지만
  피처 심사는 EOD 15:40에 수행되므로 **실운영 영향 없음**
- 이미 슬로우 스로틀(`_shap_skip_until_cnt`, 435차)이 존재 — 그 임계를 조이는 것으로도 완화 가능
- 리스크: 저
- 검증: 배포 다음 거래일 S6 중앙값 < 100ms · CB⑤ S6 기인 0건
- 배포: 장후/장전
- 규모: 소

## C1. 🔴 F6 무력화 — EOD 경로가 자기가 만든 타임스탬프를 스스로 폐기한다

**어제(456차 Wave 3) 배포한 F6이 프로덕션 EOD 경로에서 동작 불가 상태다.**

```python
# retrain_eod.py:298
X, y_dict, feature_names = retrainer._load_from_db(RETRAIN_WEEKS_BACK)
#   └─ batch_retrainer.py:2219  self._train_row_ts = [rec[0] for rec in records]  ← 채워진다

# retrain_eod.py:320
result = retrainer.retrain_now(X=X, y_dict=y_dict, ...)
#   └─ batch_retrainer.py:542-543
#         else:
#             self._train_row_ts = None    # ← 같은 인스턴스의 ts를 지운다
```

같은 `BatchRetrainer` 인스턴스가 방금 채운 행↔ts 정렬을, "외부 주입 X"라는 이유로
버린다. 결과:

- `_holdout_contamination()` → `(-1, "학습행 타임스탬프 미상(외부 주입 X 경로)")`
- `_measure_fair_holdout()` → `old_acc=None` → **6/6 호라이즌 판정 보류**
- 계획서 §7 Wave 3 게이트 *"GuardFair가 오염 시 판정 보류"*는 통과한 것처럼 보이지만,
  **실제 오염이 검출된 게 아니라 검출 자체가 불가능했던 것**이다
- `EOD_GUARD_VERDICT_SOURCE = "acc_txt"`(settings.py:575)라 폴백 경고조차 안 뜬다

**계획 — 행 수 일치를 불변식으로 (2줄)**

```python
# batch_retrainer.py:541-543
else:
    # [456차 F6 후속] 외부 주입이라도 이 인스턴스가 방금 로드한 것과 행 수가 같으면
    # 정렬이 보장된다(retrain_eod.py가 _load_from_db 직후 그대로 주입하는 경로).
    # 행 수가 다르면 정렬 미상 → 종전대로 폐기한다.
    if not (self._train_row_ts and len(self._train_row_ts) == len(X)):
        self._train_row_ts = None
```

- 리스크: **저** — 조건이 거짓이면 현행 동작과 완전 동일. 참일 때만 새 정보가 생긴다
- 검증: 다음 EOD에서 `[GuardFair] … 오염 N봉 중 M봉(P%)` 형태의 **정량 메시지**가 뜰 것
  (F6 설계 문구). "미상"이 사라지면 성공
- ⚠ **정상화되면 진짜 오염이 드러날 가능성이 높다** — intraday 학습창 4,800봉 ⊃
  홀드아웃 1,850봉이므로 계획서가 예측한 "100% 오염"이 실측으로 확인될 것이다.
  그 경우 판정 보류는 **정당한** 보류가 되고, C2가 유일한 출구가 된다
- 배포: 장후/장전 (거래 경로 무관)
- 규모: 소

## C2. 🔴 F7 유령 판정이 교체 정책에 반영되지 않음 — 3m 7일 연속 보류

```python
# batch_retrainer.py:920-927
_ghost = (not intraday) and (_inc_source == "intraday")
if _ghost:
    logger.warning("[GuardGhost] … 이 판정은 존재하지 않는 모델과의 비교다.")
# ← _ghost가 이후 어디에서도 사용되지 않는다. 954행 evaluate_model_replace()는
#   그대로 acc.txt(유령 값)로 판정한다.
```

시스템이 **"이 판정은 무효다"라고 스스로 로그를 찍은 뒤 그 판정을 그대로 집행**한다.
오늘 결과: 3m이 `acc 0.4651 → 0.4200` 하락(허용 0.0250 초과)으로 보류 —
그런데 기준값 0.4651은 존재하지 않는 모델의 성적이다.

**계획 — 주간회의 결정 필요 (계획서 §9-2 미결 안건)**

| 안 | 동작 | 장점 | 단점 |
|---|---|---|---|
| **⑤안** | `_ghost=True`면 가드 우회 → 교체하고 `"가드 미적용 — 현행이 미검증 장중 모델"` 기록 | 3m 즉시 정상화. CV 검증된 모델이 배포됨 | 하루 한 번은 가드 없이 교체됨 |
| **⑥안** | `acc_stale.txt`로 "현행 CV 성적 미상"을 자료구조화 후 보류 유지 | 가드 원칙 불변 | **3m 보류가 계속된다** — 오늘로 7일째 |

- **권고: ⑤안.** 근거 — ⑥안은 현 상태의 명문화일 뿐이고, 그 현 상태가 A2(3m 붕괴 루프)와
  B1(진입차단 5회)의 뿌리다. 가드의 목적은 "검증된 모델을 지키는 것"인데
  지금 지켜지고 있는 것은 **검증되지 않은 장중 모델**이다. 가드가 목적과 반대로 작동 중이다.
- ⑤안 채택 시 안전장치: 교체 후 `rejected/`에 구모델 보존 + 알림 레벨 WARNING 유지
- 리스크: **중** — 모델 교체 정책 변경. C1과 **동반 배포 강제**(계획서 §2 의존관계)
- 배포: **장중 금지**
- 규모: 소 (코드) / 중 (의사결정)

## C3. 🟠 `_entry_horizon_pre` 미할당 — MetaGate가 항상 1m으로 채점

```
main.py:6578   entry_horizon = getattr(self, "_entry_horizon_pre", "1m") or "1m"   ← 읽기
main.py:6603   horizon       = getattr(self, "_entry_horizon_pre", "1m") or "1m"   ← 읽기
grep "self._entry_horizon_pre" main.py  →  위 2줄이 전부. 할당부 0건
```

**이 속성은 코드베이스 어디에서도 할당되지 않는다.** 항상 폴백 `"1m"`이 쓰인다.

실측 확증: `ensemble_decisions.meta_gate_horizon` = **370/370건 전부 `'1m'`**.
같은 날 실제 진입 호라이즌은 3m·3m·5m였다.

영향 3가지:

1. `MetaGate.evaluate(horizon=)` → `entry_quality_prob`·`quantile_estimate`가
   **항상 1m 스코어러**로 산출된다. 3m/5m 진입의 품질을 1m 모델로 재고 있다.
2. `EntryChecklist(entry_horizon=)` 선행 평가 → 절대원칙 §3의 CORE 그룹 결정 인자.
   오늘은 1m·3m·5m가 **같은 단기 그룹**이라 우연히 무해했으나,
   **10m/15m/30m 진입 시에는 잘못된 CORE 세트로 심사한다.**
3. `meta_gate_horizon`은 `meta_labels(ts×horizon)` **조인 키**다
   ([meta_gate.py:123](strategy/entry/meta_gate.py#L123) 주석 명시).
   → 검증 캠페인 [2] Meta-Gate 채널이 **잘못된 호라이즌으로 조인**돼 왔다.

**계획**

```python
# main.py — select_entry_horizon() 호출(6819행)이 STEP6 말미라 pre 시점에는 아직 없다.
# threshold_feasibility는 STEP4(피처)에서 이미 확정되므로 STEP6 진입 직후 계산 가능.
# 6200행 근처(_entry_horizon = None 초기화 지점)에 추가:
self._entry_horizon_pre = select_entry_horizon(
    float(features.get("threshold_feasibility", 1.0)), 1.0
) or "1m"
```

- 리스크: **중** — MetaGate 스코어링 호라이즌이 실제로 바뀌므로 **진입 판단이 변한다**
- ⚠ **시계열 불연속 발생**: `meta_gate_horizon` 컬럼의 의미가 바뀐다.
  캠페인 [2] 채널의 과거 데이터와 **혼용 금지** — `DECISION_LOG.md` 기록 필수
- 검증: 배포 다음 거래일 `meta_gate_horizon` 분포가 ATR-Horizon 로그 분포
  (오늘 기준 3m 82% / 5m 10% / 1m 8%)와 일치
- 배포: **장중 금지**
- 규모: 소 (코드) / 중 (영향)

## C4. 🟡 `self._entry_horizon` 미할당 — SHAP 패널 CORE 그룹 고정

```
main.py:1977   entry_horizon=getattr(self, "_entry_horizon", "1m") or "1m"
grep "self._entry_horizon" main.py  →  이 1줄. 할당부 0건
```

6819행이 만드는 것은 **지역변수** `_entry_horizon`이지 `self._entry_horizon`이 아니다.
→ 대시보드 SHAP 탭의 CORE 그룹 표시가 항상 단기(1m·3m·5m)로 고정.

**계획**: C3과 같은 지점에서 `self._entry_horizon`도 함께 갱신 (STEP6 말미 6819행 뒤).
표시 전용이므로 리스크 저. C3과 **같은 배치**로 처리.

## C5. 🟡 `daily_stats` 2개 컬럼이 리셋 후 저장 — 8일 연속 죽은 값

```
main.py:10562   self.online_learner.reset_daily()      ← 버퍼 비움
main.py:10612   self._verified_today = 0               ← 카운터 0
   ...
main.py:10677   "sgd_accuracy":   self.online_learner.recent_accuracy(),   ← 빈 버퍼 → 폴백 0.5
main.py:10678   "verified_count": self._verified_today,                    ← 항상 0
```

DB 실측 (8거래일 전량):

| date | trades | wins | sgd_accuracy | verified_count |
|---|---|---|---|---|
| 08-11 | 3 | 2 | **0.5** | **0** |
| 08-10 | 10 | 8 | **0.5** | **0** |
| 08-07 | 11 | 10 | **0.5** | **0** |
| … 08-03까지 동일 | | | **0.5** | **0** |

`online_learner.recent_accuracy()`는 버퍼가 비면 `0.5`를 반환한다
([online_learner.py:370](learning/online_learner.py#L370)). 실제 SGD 정확도는
오늘 1m 기준 32.6%대다 — 0.5는 **폴백 상수이지 측정값이 아니다**.
대시보드 추이 패널이 이 컬럼을 읽으므로(`main_dashboard.py:6056·6103`)
**8일치 자가학습 추이가 전부 가짜 평선**이다.

**계획 — 리셋 전 캡처 (같은 파일에 이미 선례 있음)**

```python
# main.py:10433 이 이미 리셋 전 값을 잡아두고 있다:
_today_accuracy  = self.online_learner.recent_accuracy()
_today_n_samples = self.online_learner.sample_count
# main.py:10688 도 같은 패턴을 쓴다:
_ccf_today = self._checklist_conf_fail_count   # [P3] 리포트 전달용 — 리셋 전에 캡처

# → 10612 직전에 추가:
_verified_today_snapshot = self._verified_today

# → 10677-10678 을 스냅샷으로 교체:
"sgd_accuracy":   _today_accuracy,
"verified_count": _verified_today_snapshot,
```

- 리스크: **없음** (기록 전용, 거래 경로 무관)
- ⚠ **과거 8일 행 소급 수정 금지** — 계획서 §8 *"daily_stats 과거 행 소급 수정 안 함"* 승계.
  추이 패널에 **불연속선이 생기는 것이 정상**이며 `DECISION_LOG.md`에 경계일을 기록한다
- 검증: 다음 EOD `daily_stats`에서 `sgd_accuracy ≠ 0.5` 또는 `verified_count > 0`
- 배포: 장후/장전
- 규모: 소

## C6. 🟡 FeatureReg 미가용 목록 무표식 절단

```
[FeatureReg] 30m: 7개 피처 미가용 (need_add) → 제외:
  ['opt_gex_bn', 'opt_chain_pcr', 'opt_atm_call_oi', 'opt_atm_pcr', 'threshold_feasibility']
                                                                     ← 5개만. 2개 은닉
```

[horizon_feature_registry.py:128](features/horizon_feature_registry.py#L128) `missing[:5]` —
절단 표식이 없어 운영자가 "7개 = 나열된 5개"로 오독한다.
**하필 30m은 `opt_chain_pcr`(절대원칙 §3 장기 CORE)의 지위가 F9로 심사 중인 호라이즌**이다.

**계획**: `missing[:5]` → 전량 출력, 또는 `f"{missing[:5]} 외 {len(missing)-5}개"`.
계획서 §4 **G3-A③ "탈락 가시화"** 규약의 직접 사례이므로 그 규약과 함께 처리.
- 리스크: 없음 / 규모: 극소 / 배포: 아무 때나

## C7. 🟡 Canary 이상 알림이 해소보다 먼저 발송

```
08:55:23  [Notify] ⚠️ 🌡 Canary 이상 감지
08:55:23  [ScalerWarmup] 프리장 refit 완료 확인 → 스킵 (중복 방지)
08:55:23  [Canary] 장전 재적합 완료 n=30봉 z경고 →3개 ✓ 임계 이하
```

같은 초에 경보와 해소가 연달아 난다. 장전 refit(08:47~08:54)이 이미 z경고를 4개로
줄여놨는데 Canary가 그 결과를 반영하기 전에 판정·알림한 것으로 보인다.
**허위 경보는 진짜 경보의 신뢰를 깎는다.**

**계획**: Canary 판정을 프리장 refit 완료 확인 **이후로 이동**하거나,
알림 전송을 최종 판정 시점으로 지연. 리스크 없음 / 규모 소.

## C8. 🟢 WarmupRetrain 예약의 소진 로그 부재 (확인 필요)

```
08:41:22  [WarmupRetrain] 세션 재시작 감지 → GBM 즉시 재학습 예약
   … 이후 "실행" / "스킵" 어느 로그도 없음
09:36:59  [ConstOut] ['3m'] → GBM 재학습 예약    ← 첫 실제 재학습은 ConstOut 기인
```

[main.py:3321-3380](main.py#L3321)의 3개 분기(장중 재시작 / 14:30 이후 / 이미 재학습)는
**전부 장중 조건**이라 08:41 장전 기동에서는 어느 것도 타지 않는다.
`_warmup_retrain_pending = True`가 로그 없이 남는다.

- **기능 결함이라 단정할 수 없다** — 장전 기동은 EOD 모델이 최신이라 재학습이 불필요할 수 있고,
  실제로 08:55 PreRetrain이 "1영업일 전 EOD 성공"으로 정상 스킵했다.
- **필요한 것은 판단이 아니라 로그다.** 장전 분기에 명시 로그를 추가:
  `[WarmupRetrain] 장전 기동 — 09:00 이후 조건 재평가로 이월` 또는 스킵 사유
- 리스크: 없음 / 규모: 극소

## C9. 🟢 문서-현실 괴리: CLAUDE.md 확률 판단 기준

`CLAUDE.md` **확률 판단 기준** 표는 *"60~70% 진입 고려 / 70% 이상 적극 진입 / 55% 미만 관망"*이다.
오늘 실제 3건 진입 conf: **40.1% / 56.0% / 44.0%** — 표 기준으로는 전부 "관망" 구간이다.

이는 진입 로직의 오류가 아니다. `dailycheck_prompt.txt` 상설 경고대로 conf는
2026-07-31 축퇴 가드 배포로 **스케일 의미가 바뀌었고**, DynMC(`min_conf` = conf 분포 p65,
base_mc 0.344→0.369)가 분포 기반으로 동작하므로 40.1%도 정상 통과다.

→ **문서가 낡았다.** F9(CLAUDE.md 정정) 배치에 이 표를 포함시켜,
현행 DynMC 분위수 기준을 반영하거나 "이 표는 07-31 이전 스케일 기준"을 명시할 것.
- 리스크: 없음 (문서) / 규모: 소

## C10. 🟢 예측 저장 빈도 불균형 (관측 사실, 원인 미확인)

| 호라이즌 | 1m | 3m | 5m | 10m | 15m | **30m** |
|---|---|---|---|---|---|---|
| 저장 건수 | 369 | 247 | 147 | 73 | 48 | **370** |

1m→15m은 호라이즌 길이에 반비례해 저장 빈도가 줄어드는데(설계된 카덴스로 보임),
**30m만 매분 저장**돼 패턴을 깬다. 8/10도 동일(369/246/147/73/49/369)이므로
오늘 발생한 이상은 아니다.

부작용: 10m·15m 일일 표본이 73·48건뿐이라 **일자단위 통계 검정력이 매우 낮다**
(오늘 15m 39.1%는 n=46 — 신뢰구간이 ±14%p 수준).
30m은 296차에서 퇴역했음에도 저장 부하만 최대다.

- 계획: 저장 카덴스 정책을 **문서화 후** 조정 여부 판단. 조사 우선, 조치 보류
- 규모: 소 (조사)

---

## §2-1. 실행 파동 편성

| Wave | 항목 | 배포 | 선행 |
|---|---|---|---|
| **W-A (즉시, 무위험)** | C5 · C6 · C7 · C8 · C9 | 장후/장전 | 없음 |
| **W-B (안정성)** | **B2** → **B1** | 장후/장전 | B2 먼저(위험 낮음)로 S6 제거 효과 확인 후 B1 |
| **W-C (계측 정합, 동반 강제)** | **C1 + C2** | **장중 금지** | C2는 **주간회의 ⑤/⑥안 결정 필요** |
| **W-D (진단만)** | **A1** 진단 하네스 · **A3** 섀도 컬럼 | 장후/장전 | A3 합격선 **수집 전 사전등록** |
| **W-E (영향 큼)** | **C3 + C4** | **장중 금지** | W-C 이후 (3m 정상화 후 MetaGate 호라이즌 변경) |
| **W-F (선행 의존)** | **A2** 재측정 | — | W-C 배포 +5거래일 |

### 왜 이 순서인가

1. **W-A 먼저** — 리스크 0인데 C5가 8일치 추이 패널을 되살리고, C6·C8은
   이후 모든 진단의 관측성을 올린다.
2. **B2가 B1보다 먼저** — SHAP 분리는 위험이 낮고 CB⑤ 9건 중 4건을 즉시 없앤다.
   그 뒤 B1(모델 리로드 비동기화)의 효과를 깨끗하게 측정할 수 있다.
3. **C1+C2 동반** — 계획서 §2 의존관계 승계. C1 단독은 "진짜 오염"만 드러내고
   3m 보류를 오히려 정당화한다. C2 없이 배포하면 상황이 나빠진다.
4. **C3은 W-C 뒤로** — MetaGate 스코어링 호라이즌 변경과 3m 모델 교체 재개를
   같은 날 배포하면 **효과 귀속이 불가능해진다**.
5. **A2는 측정 대상이지 조치 대상이 아니다** — W-C의 결과다.

### 배포 제약

| 구분 | 항목 |
|---|---|
| **장중 배포 금지** | C1, C2, C3, C4 |
| 장후/장전 가능 | C5~C10, B1, B2, A1·A3 계측 |
| **주간회의 승인 필요** | **C2 ⑤안 vs ⑥안**(계획서 §9-2 미결) · A3 사전등록 합격선 · A1 조치안(진단 후) |
| **MW0602 동시 확인** | **C1·C3·C4·C5는 공통 코드** — 그쪽도 동일 결함. 배포 전 대조 필수 |

### 검증 게이트

| Wave | 게이트 | 상태 |
|---|---|---|
| W-A | 다음 EOD `daily_stats.verified_count > 0` · FeatureReg 30m 7개 전량 출력 | ⏳ 다음 EOD |
| W-B | 다음 거래일 S6 중앙값 < 100ms · 재학습 직후 S0 < 200ms · **CB⑤ 0건** | ⏳ 다음 장중 |
| W-C | GuardFair가 "미상"이 아닌 **정량 오염 메시지** 출력 · **3m EOD 교체 성립** | ⏳ 다음 EOD |
| W-D | A1 진단 3항(S1-a/b/c) 산출물 · A3 섀도 신규 행 발생 확인 | ✅ A1 완료 / ⏳ A3 08-12~ |
| W-E | `meta_gate_horizon` 분포 = ATR-Horizon 로그 분포 | ⏳ 다음 장중 |
| W-F | ConstOut 3m ≤ 2회/일 (5거래일 평균) | ⏳ W-C +5거래일 |

**오프라인 선검증 (2026-08-11 실행)**

| 항목 | 결과 |
|---|---|
| B1 S0 단축 | `stage_reload` **555ms**(워커로 이동) / `apply_staged` **3.6ms**(S0 잔여) → **99.4% 감소** |
| B1 동작 동일성 | 실 pkl로 `_load_all()` vs `stage+apply` 대조 — models·feature_names·bad 전부 일치 |
| 회귀 테스트 | `tests/test_457_daily_fixes.py` 15건 통과 · 전체 스위트 **259건 통과** |

### 하지 않기로 한 것

| 항목 | 이유 |
|---|---|
| TP1 부분청산 폐지 | 오늘 거래 3에서 **유리했다**. 1일 표본 금지(313차) |
| 하드스톱 ATR×1.5 완화 | 거래 2에서 5pt를 방어했다. 오늘 데이터는 **완화 반대** 근거다 |
| 30m 호라이즌 재도입/폐기 결정 | 296차 퇴역 상태 유지. A1 진단 전 판단 불가 |
| `daily_stats` 과거 8행 소급 수정 | 추이 불연속 — 계획서 §8 승계 |
| SHORT 편향 즉시 보정(임계 조정 등) | **원인 미규명.** 진단 없이 절편을 만지면 반대 편향만 생긴다 |
| 오늘 CB⑤ 9건을 실전전환기준 ②의 근거로 사용 | 자가유발(재학습·SHAP 내부 요인) |

---

# §3. 고도화 방안

> Fix에 흡수되지 않는 독립 항목. 456차 계획서 §4(G1~G3)와 중복은 제외하고
> **오늘 작업흐름에서 새로 도출된 것**만 싣는다.

## §3-0. 구현 현황 (2026-08-11 배포)

| ID | 내용 | 상태 | 성격 |
|---|---|---|---|
| **G4** | `direction_bias_watch` 상시 감시 → 캠페인 채널 **[50]** | ✅ 배포 | 계측 |
| **G5** | 모델 건강도 지표 → `scaler_daily` 7컬럼 + **앙상블 유효 가동률** | ✅ 배포 | 계측 |
| **G6** | 회전 비용 게이트 | ⛔ **A3로 흡수 · 전제 흔들림** | — |
| **G7** | 라우터 × 모델 건강도 | ✅ **섀도만** 배포 (정책 무변경) | 계측 |
| **G8** | 계측 4원칙 ④ 폴백 가시화 | ✅ 배포 (`CLAUDE.md` + 자동검출) | 규약 |
| **G9** | HealthPolicy 원인 태그 | ✅ **관측만** 배포 (분기 보류) | 계측 |

**전부 계측·규약이고 매매 정책을 바꾸는 것은 하나도 없다.** 의도한 것이다 —
313차 원칙상 효과가 확인되지 않은 상태에서 진입 분포를 건드리지 않는다.

### 배포 중 확정된 것

- **G8 검출기가 즉시 5번째 사례를 찾았다.** `_futures_code`(읽기 19회). 조사 결과
  `broker_runtime_service.py:76`이 **교차모듈로 주입**하는 정상 경로였다 —
  결함은 아니지만 정적 추적이 끊기는 취약 결합이라 `_CROSS_MODULE` 목록에 근거와
  함께 고정했다. 새 항목이 조용히 늘면 테스트가 알려준다.
- **검출기 실효 검증**: 457차 **이전** `main.py`(커밋 `a675e20`)에 돌려
  `_entry_horizon`·`_entry_horizon_pre`를 정확히 잡고 `_futures_code`는 교차모듈로
  올바르게 분류함을 확인했다. "검출기가 죽었는데 깨끗하다고 보고하는" 상태를
  막는 자기검사(`test_detector_actually_detects`)도 함께 넣었다.
- **G7의 전제가 실측으로 약해졌다.** 8/11 ConstOut 활성 구간(6개)과 진입 시도
  시각(10:02·10:04·10:08·10:49·13:24)의 **교집합이 0건**이다(13:24는 13:23:01
  해소 59초 뒤). 즉 "ConstOut 호라이즌을 라우터에서 빼자"는 조치가 그날은
  **아무것도 바꾸지 않았을 것**이다. → 정책이 아니라 빈도 계측부터 한다.
- **G4 채널은 캠페인 창에서 아직 `PASS`다.** 캠페인 시작일(2026-07-05) 이후
  26거래일에서는 3m만 Bonferroni 생존(1개 < 문턱 2). A1의 60거래일 진단
  (3m·5m 생존)보다 보수적이다 — 창이 짧아 검정력이 낮은 것이고, 거래일이
  쌓이면 자연히 올라간다. **결과를 보고 사전등록 기준을 낮추지 않았다.**
- **G5 실측**: 8/11 앙상블 유효 가동률 **74.3%** (370분 중 ConstOut 13분 +
  WeightCollapse 82분 손실).

### 검증 게이트 (다음 거래일)

| ID | 게이트 |
|---|---|
| G4 | 다음 금요일 캠페인 리포트에 `[50] 방향 편향 상시 감시` 행이 뜨는지 |
| G5 | EOD `[ModelHealth] date=… 앙상블유효가동률=…` 1줄 + `scaler_daily` 값 |
| G7 | `router_health_shadow` 신규 행 발생 · `chosen_degraded=1` 빈도 |
| G9 | `[HealthPolicy] Degraded 선제차단: … \| cause=SN(NNNms)` 태그. **B1 이후에도 Degraded가 남는지**가 핵심 관측점 |

## G4. 방향 편향 상시 감시 채널 — `direction_bias_watch`

오늘 SHORT 8:1 편향은 **EOD 리포트 어디에도 경보로 잡히지 않았다.**
DriftDetector는 pnl/wr/pf 3축만 보고 **CLEAR**를 냈고, Brier 과신 경고 3건은
방향이 아니라 확신도만 재고, CB③-P4는 30m 정확도만 본다.
**"신호 방향 분포가 실현 방향 분포와 체계적으로 어긋나는가"를 보는 감시자가 없다.**

| 항목 | 내용 |
|---|---|
| 계측 | 일자단위 × 호라이즌: `pred_up/pred_dn`, `actual_up/actual_dn`, 편향비 로그오즈, 이항검정 p |
| 경보 | 연속 N거래일 같은 방향으로 유의(다중비교 보정 후) → WARNING |
| 왜 일자단위인가 | 372차가 확인한 대로 신호단위 n은 독립이 아니다. 일자단위(진짜 독립관측)로 볼 것 |
| 자원 | 검증 캠페인 신규 채널로 편입 (별도 캘린더 신설 금지 — 317차 사용자 원칙) |
| 합격선 | **수집 시작 전 사전등록** (캠페인 §9) |

> 이 채널이 있었다면 오늘의 30m 6.6:1 역방향을 **당일이 아니라 며칠 전에** 잡았을 것이다.
> A1 진단의 상시화 버전이며, A1 결과와 무관하게 가치가 있다.

## G5. 모델 건강도 지표 — ConstOut을 사건이 아니라 지표로

현재 ConstOut은 **발생 시 경고**만 남고 추세가 축적되지 않는다.
오늘 3m 6회는 `scaler_monitor.db`에도 `daily_stats`에도 집계되지 않는다.

| 항목 | 내용 |
|---|---|
| 계측 | 호라이즌×일자: `const_out_count`, `const_out_total_minutes`(앙상블 제외 시간), `retrain_triggered_count` |
| 저장 | 기존 `scaler_monitor.db` 일별 집계에 컬럼 추가 (신규 DB 금지) |
| 활용 | ① EOD 리포트 표시 ② A2 검증 게이트의 측정 근거 ③ 26주 WFA 시 모델 안정성 추이 |
| 파생 지표 | **"앙상블 유효 가동률"** = 1 − (ConstOut 제외 분 + WeightCollapse 분) / 총 분<br>오늘 실측 추정: WeightCollapse 82분 + ConstOut 제외 구간 → **가동률 약 70%대** |

> 오늘 370분 중 82분(22%)이 WeightCollapse(conf 0.85 고정 "판단 불가")였다.
> 이 수치가 어디에도 일일 지표로 남지 않는다는 것이 문제다.

## G6. 회전 비용을 진입 판단에 내재화 — ⚠ **A3로 흡수 + 전제 흔들림**

> **[2026-08-11 457차 갱신] 이 항목은 별도 구현하지 않는다.**
>
> ① 아래가 제안한 "섀도 컬럼 추가"는 A3의 `cost_edge_watch` 채널이 그대로
> 수행한다(축2 = `quantile_expected_pt`의 사전 예측력). **중복 구현 금지.**
>
> ② 그리고 소급 참고치가 이 항목의 **전제를 흔든다**. 170포지션 29일 실측:
> 일자단위 rho **0.156** (t=0.96, 0 배제 실패), 상·하위 3분위 커버율 격차
> **−2.4%p**(상위군이 오히려 낮다). 즉 `quantile_expected_pt`는 비용 커버를
> **예측하지 못한다.** 이 값으로 게이트를 세우면 무작위로 진입을 자르는 셈이다.
>
> → 사전등록 판정(`data_start=2026-08-12`, min_samples 40·min_days 10)을 기다리되,
> `REJECTS_HYP`가 확정되면 처방은 **게이트 신설이 아니라 분위회귀 재설계**다.
> 주간회의에 G2-C(손익비 명시화)와 **함께** 올린다 — 둘 다 "무엇을 근거로 진입
> 크기·자격을 정하는가"라는 같은 질문의 두 면이다.

<details><summary>원 제안 (참고 보존)</summary>

오늘 gross +5,000원 / 수수료 8,760원. **수수료가 gross의 175%**다.
현행 게이트(MetaGate·ToxicityGate·EntryChecklist)는 **방향과 품질만** 보고
**"이 진입이 왕복 비용을 넘길 기대값이 있는가"를 묻지 않는다.**

| 항목 | 내용 |
|---|---|
| 착안 | `quantile_expected_pt`(MetaGate 분위회귀)가 이미 기대 이동폭을 pt로 낸다 — 이미 계산 중이다 |
| 제안 | `expected_pt × pt_value × qty` vs `왕복수수료 × 예상레그수`를 비교하는 **섀도 컬럼**을 `ensemble_decisions`에 추가 |
| 판정 | 10거래일 후, "비용 미달 진입"의 실현 손익이 유의하게 나쁜지 검정 |
| 조치(가정) | 유의하면 EntryChecklist에 11번 항목 `cost_edge` 신설. **섀도 판정 전 게이트화 금지** |
| 선행 | A3(수수료 섀도 계측)와 **같은 배치** — 같은 데이터를 두 각도에서 본다 |

> 456차 계획서 **G2-C**(손익비 명시화, 암묵 6:1)와 짝을 이룬다. G2-C가 "TP/스톱 배수"를
> 다룬다면 G6은 "진입 자격"을 다룬다. 주간회의에 **함께** 올릴 것.

</details>

## G7. 진입 호라이즌 라우터 — ATR 단독 → 모델 신뢰도 결합 (F10-A 확장)

오늘 라우터가 3m을 82% 선택했는데, 하필 3m이 **하루 6번 붕괴하고 EOD 교체가 7일 막힌**
가장 관리가 안 되는 모델이다. 계획서 F10-A가 이미 지적했으나, 오늘 데이터는
**한 걸음 더 나간 제안**을 뒷받침한다.

| 현행 | `feasibility = atr / threshold_1m` **단독** — 모델 품질과 무관한 순수 ATR 밴드 함수 |
|---|---|
| 제안 | 라우터 후보 호라이즌에 **모델 건강도 필터**를 곱한다:<br>① ConstOut 활성 중인 호라이즌은 **후보에서 제외**<br>② `gbm_{h}_meta.json`(F6 사이드카)의 `cv_acc = null`(미검증 장중 모델)이면 **감점** |
| 왜 지금 가능한가 | **F6 사이드카가 어제 생겼다.** 모델의 검증 상태를 런타임에서 처음으로 읽을 수 있다 |
| 선행 | C1·C2(사이드카가 신뢰할 수 있게 된 뒤) → F10-A 재보정과 **동일 배치** |
| 리스크 | 중 — 진입 분포가 바뀐다. 섀도 선행 필수 |

> ①은 사실상 무료다 — ConstOut은 이미 앙상블에서 그 호라이즌을 제외하고 있는데,
> **라우터는 그 사실을 모르고 여전히 그 호라이즌을 진입 대상으로 고른다.**
> 오늘 3m ConstOut 구간(총 약 30분)에 진입 시도가 있었는지는 별도 확인 필요.

## G8. 계측 3원칙(G3-A)에 **④ 폴백 가시화** 추가

오늘 하루에 같은 계열 결함 4건이 나왔다:

| 결함 | 패턴 |
|---|---|
| C3 `_entry_horizon_pre` | `getattr(self, "x", 기본값)` — 할당부 0건인데 **조용히** 기본값 |
| C4 `self._entry_horizon` | 동일 |
| C5 `recent_accuracy()` | 빈 버퍼 → **조용히** 0.5 반환 |
| C5 `_verified_today` | 리셋 후 읽기 → **조용히** 0 |

전부 **"폴백이 정상값처럼 보인다"**는 하나의 패턴이다.
C5의 0.5는 8거래일간 대시보드에 정상 수치로 표시됐고, C3의 '1m'은
370건 전부 DB에 정상 값처럼 기록됐다.

**제안 — `CLAUDE.md` 계측 규약(G3-A)에 4번째 항목 신설**

```
④ 폴백 가시화 — 기본값·폴백이 실제로 쓰였으면 그 사실을 값과 함께 남긴다.
   · getattr(self, "x", default) 형태로 런타임 상태를 읽지 않는다.
     __init__에서 명시 초기화하고, 미설정을 None으로 두어 폴백 시점에 로그를 남긴다.
   · "표본 없음"과 "측정값이 0.5"를 같은 반환 타입으로 표현하지 않는다
     (None 반환 또는 *_measured 동반 — G3-A② "미측정 ≠ 0"의 확장).
   · DB 컬럼에 폴백값을 쓸 때는 폴백 여부 플래그를 같은 행에 쓴다.
```

- 자동 검출 가능: `grep -n 'getattr(self, "_' *.py`로 할당부 없는 속성을 CI에서 잡을 수 있다
- 계획서 G3-A(①단위 명시 ②미측정≠0 ③탈락 가시화)의 자연스러운 확장이며,
  **오늘 4건이 그 필요성의 실증**이다

## G9. HealthPolicy Degraded 판정에 원인 태그 부여

오늘 Degraded 선제차단 5회는 **전부 자기 유발**(장중 재학습)이었는데,
정책은 원인을 구분하지 않고 진입을 막았다.

| 현행 | `streak=1.00+1.00 ≥ 2 (latency=4661ms …)` — latency의 **출처를 모른다** |
|---|---|
| 제안 | PipePerf가 이미 구간별(S0~S8) 분해를 갖고 있다. Health 판정에 지배 구간을 태그로 전달 |
| 정책 분기 | `cause=S0_model_reload`처럼 **내부 정비 기인**이면 차단 대신 **경보만**<br>`cause=S1/S4 DB` 또는 `cause=external`이면 종전대로 차단 |
| 근거 | 절대원칙 §장중 DB 분석 금지 항목이 이미 **"지연 프로파일이 원인을 특정한다"**는 방법론을 확립했다. 그 방법론을 사후 분석이 아니라 **런타임 판정**에 쓰는 것이다 |
| 선행 | **B1 배포 후 재평가.** B1이 S0을 200ms 이하로 낮추면 이 문제 자체가 소멸할 수 있다 |
| 우선순위 | 낮음 — B1의 결과를 보고 판단 |

---

# §4. 주간회의 상정 안건

1. ~~**C2 — F7 유령 기준선 처리: ⑤안 vs ⑥안**~~ → ✅ **2026-08-11 사용자 결정: ⑤안(교체 허용).**
   `config/settings.py:EOD_GUARD_GHOST_POLICY = "replace"` 배포 완료.
2. **A1 SHORT 편향 조치안 — 절편 보정 축** *(진단 완료, 조치 미결)*
   진단이 원인을 **모델 절편**으로 좁혔다(라벨 균형 / 확률 전 호라이즌 DN 편중).
   후보: ① `make_sample_weight` 클래스 가중 재산출 ② 호라이즌별 독립 Platt 보정
   ③ 앙상블 보정기 축퇴(403차 가드) 해제 조건 재검토.
   ⚠ **방향 반전은 후보가 아니다** — 역방향 검정에서 적중률이 오히려 떨어졌다.
3. **A3 + G6 사전등록 합격선** — 수수료 섀도 / 비용엣지 섀도. **수집 시작 전 확정 필수**
4. **G7 라우터 모델 건강도 결합** — F10-A와 동일 배치 여부
5. **G8 계측 규약 ④ 폴백 가시화** — `CLAUDE.md` G3-A 확장 승인
6. **MW0602 대조** — C1·C3·C4·C5는 공통 코드. 그쪽 EOD 로그·`daily_stats` 확인 요청

---

# §5. 456차 Wave 검증 게이트 판정 (계획서 §7 대비)

| Wave | 게이트 | 오늘 실측 | 판정 |
|---|---|---|---|
| **0 (F8)** | 장중 CB⑤ **자가유발** 0건 | CB⑤ 9건 전부 재학습·SHAP 내부 기인, 분석 세션 기인 0건 | ✅ **통과** |
| **1 (F1)** | 승패 집계 포지션 단위 | `trades=3 wins=2 losses=1` — 6레그를 3포지션으로 정상 집계 | ✅ **통과** |
| **1 (F2)** | SHS CORE 통과율 | 오늘 SHS 경보 없음 — 표본 부족으로 판정 보류 | ⏸ 보류 |
| **2 (F5)** | AutoMask opt_pcr 격리 0~수 회 · z ±3 이내 | AutoMasked 9회 (어제 67회 대비 대폭 감소) | ⏸ 추가 관측 필요 |
| **3 (F6)** | GuardFair가 오염 시 **정량** 판정 보류 | "미상"만 반환 — **오염 검출 자체가 불가능** | ❌ **미통과 (C1)** |
| **3 (F7)** | 3m이 유령 기준선에 막히지 않음 | **7거래일 연속 보류** | ❌ **미통과 (C2)** |
| **4** | 섀도 3채널 표본 누적 | 진입 3건 — 표본 축적 초기 | ⏳ 진행 중 |

> Wave 3은 **배포는 됐으나 목적을 달성하지 못했다.** C1·C2가 그 완결이다.

---

## 부록. 당일 원시 수치

```
기동            08:40:48 (PID 25048, session #1, cause=STARTUP)
종료            15:40:40 (정상 종료 플래그 기록, _exit_normally)
EOD 체인        15:45:04 ~ 15:49:06 (241.5s: 로드 46.5s + 재학습 194.3s)
분봉            08:45 ~ 15:33 (BAR-CLOSE 정상)
틱              113,117건 수신
앙상블 결정      370분
예측 저장        1,254건 (1m 369 / 3m 247 / 5m 147 / 10m 73 / 15m 48 / 30m 370)
잔고            50,027,130원(시작) → base_risk 1,500,000원(3.0%) / MAX_CONTRACTS=3
RF OOB          1m 51.7% / 3m 46.8% / 5m 45.8% / 10m 48.8% / 15m 49.0% / 30m 52.8%
GBM CV(EOD)     1m .4250 / 3m .4200(보류) / 5m .4126 / 10m .4499 / 15m .4248 / 30m .4269
8월 누적        +352,430원 (7거래일: -730,619 / +667,193 / +26,662 / -53,826 / +690,847 / -244,067 / -3,760)
```
