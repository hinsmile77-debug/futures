# 미륵이 일일 점검 — MW0601 / 2026-08-18 / 장전(pre)

- 점검 시각: **13:43 KST** · PC `MW0601` · 브랜치 `v9-dev` (운영 브랜치 일치 ✅)
- HEAD `7dc14bc` (474차, 2026-08-17 17:54) · `origin/v9-dev`와 동일 · 미커밋 **2파일**(CRLF 보정 후 실측) · 당일 커밋 0건
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260818_pre.md`
- 직전 리포트: `MW0601-20260817-점검리포트-pre.md` (KRX 휴장일)

> 🔴 **본 세션은 예약 시각(08:57)이 아니라 13:43에 실행됐다.** 개장 후 4시간 43분 시점이며
> 진입 7건·청산 7건이 이미 끝난 뒤다. 따라서 이 리포트는 **장전 구간(08:40~09:00)을 사후
> 재구성**한 것이다 — "개장 전에 거래 자격을 판단한다"는 장전 점검의 목적은 오늘 달성되지
> 않았다. 이 사실 자체를 이상점 **1-1**로 올린다.
>
> ✅ **라이브 DB 분석 금지 구간(08:45~15:35) 준수** — `raw_data.db`·`predictions.db`·`trades.db`
> 접근 0회. 로그 전용 수집기와 파일 조회만 사용했다(CLAUDE.md 🔴 장중 라이브 DB 분석 금지).
> ✅ **코드 변경 0건 / 커밋 0건.** 아래 Fix는 전부 계획이며 적용 시점은 **오늘 15:10 이후**다.

---

## 0. 한 줄 결론

**오늘 미륵이의 장전 뼈대(08:40 자동기동 → py37_32 → Cybos → 모델 6종 로드 → 08:45 실시간
사전구독 → 08:55 매크로 → 08:58 레짐 확정)는 전 항목 정상이며 P0는 0건이다.** 다만 장전
점검 자체가 개장 4시간 43분 뒤에 돌아 통제로서 작동하지 않았고, 08-17(휴장일) EOD 중단이
남긴 `session_state.json` 완료 스탬프 2키는 오늘도 복구되지 않아 폴백 경로가 그 자리를
대신하고 있다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | **조건부 정상** | A-0~A-4 전 항목 통과. 단 점검 시각 이탈(1-1) + 완료 스탬프 미복구(1-2) |
| 장중 | 본 세션 범위 밖 | 13:43까지 관측분만 §1 "장중 이월"에 증거로 남김 |
| 장후 | 해당 없음 | |

**절대원칙 준수 여부** (장전 시점 판정 가능한 범위)

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | **미도래** | 13:43 시점. F-1R 리허설·F-1H 하트비트는 여전히 이월 상태 |
| ② | Circuit Breaker 5종 | **준수** | 설정 불변식 표 전 행 `일치`. CB② `9999`(모의 예외) 유지 확인. CB⑤ 미발동이 정상 — 당일 `PipePerf total` 최대 **2,826ms** < `CB_PIPE_PAUSE_MS=5,000` |
| ③ | CORE 피처 호라이즌 그룹별 | **준수** | 진입 7건 전부 `vwap✅` — 강제 X 대상 위반 0건 (`logs/20260818_TRADE.log` `[진입체크]` 7행 전수) |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | **준수** | `0xC0000409`/`STATUS_STACK_BUFFER_OVERRUN` 출현 **0건** (`crash_fault.log` + 당일 전 로그) |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | **해당 없음** | Kiwoom 전용 경로. 오늘 `broker=cybos` (아래 1-4) |
| ⑥ | 알파 리서치 봇 자동통합 금지 | **준수** | 당일 알파봇 실행 흔적 0건, 자동통합 스위치 부재 |

**설정 불변식** — 수집기 §3에서 `불일치` **0행**. `미발견` 5행(`MODEL_LABEL_STATE_UNLOCK_ENABLED` ·
`PRE_RETRAIN_DONE_BY_EOD_ENABLED` · `ZONE_ENTRY_BAN_ENFORCE` · `ZONE_ENTRY_BAN_SHADOW_ENABLED` ·
`PIPE_LATENCY_EXCLUDE_MODEL_SWAP`)은 **전부 `[MW0602]` 커밋 소산으로 `v9-dev`에 미도달** —
08-17 장전 점검이 원인까지 확정했고 처분은 주간회의 안건이다(§1 "이미 반영된 사안").

**한시 예외 재검토 기한** — CB② 복원(2026-08-29)까지 **11일**. 나머지는 기한 미도래(§7).

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**없음.** 장전 뼈대 전 항목이 근거와 함께 통과했다(§1-A 통과 기록 참조).

---

### P1 — 정확성·관측 훼손

#### 1-1. 장전 점검 예약이 08:57이 아니라 13:43에 실행됐다 — 통제로서 무효화

- **증상**: 본 세션의 실제 실행 시각은 **2026-08-18 13:43 KST**다(샌드박스 `date` 실측,
  증거 다이제스트 헤더 `생성 2026-08-18 13:44:15 KST`). 예약 정의는 `08:57 KST — 개장 3분 전`
  이며, 예약 지시문 본문도 *"이 예약은 08:57 KST, 개장 3분 전에 돈다"* 로 그 전제 위에
  코드 변경 금지·라이브 DB 금지 규칙을 세우고 있다.
- **근거**:
  ```
  $ TZ=Asia/Seoul date
  Tue Aug 18 13:43:47 KST 2026
  ```
  ```
  # docs/정기점검/매일점검/evidence_MW0601-20260818_pre.md 1~3행
  - 생성 2026-08-18 13:44:15 KST · PC MW0601 (DeskTop-MW0601)
  ```
  같은 시점 로그 최신행: `13:44:04 [CybosRT-TICK] #109800` — 라이브 본체는 정상 가동 중.
- **기준 위반**: `references/phases.md` A절 서두 — 장전 국면의 정의는 *"~09:00 · 오늘 거래할
  자격이 되는가"* 다. 예약 지시문의 *"오늘 거래 자격에 직접 위험(P0)이 발견되면 …
  **사람이 개장 전에** 판단할 수 있도록"* 이라는 조항이 성립 불가 상태였다.
- **영향**: 오늘은 P0가 0건이라 실손해가 없었다. 그러나 **P0가 있었다면 개입 창이 이미
  닫힌 뒤 보고됐을 것**이다. 09:00~13:43 사이에 자동 진입 7건·청산 7건이 집행됐다.
  부수적으로 본 세션은 라이브 DB 금지 구간(08:45~) 안에서 돌게 되어, 장전 점검이 원래
  쓸 수 있었던 DB 확인 수단도 전부 봉쇄됐다(로그 전용으로 우회 — 준수).
- **신규 여부**: **O-7(08-17 등록)의 첫 판정 = 실패.**
  원문: `dev_memory/NEXT_TODO.md` — *"O-7 예약 점검 시각(C-3) — 장전 점검이 08:57±5분에 도는지."*
  08-17은 휴장일이라 3국면 예약이 전부 16:48/16:49/16:55에 몰려 돌았고(473차 후속2 [4]),
  그것이 휴장일 특수 사정인지 예약 인프라 결함인지 판별할 수 없었다. **오늘 거래일에도
  이탈했으므로 휴장일 요인은 배제된다.**
- **원인 범위**: 미륵이 리포 밖(예약 실행 인프라). 로그로는 판정 불가 — §2 F-1 참조.

#### 1-2. `session_state.json` EOD 완료 스탬프 2키 미복구 — 폴백이 대신 성립시키고 있다

- **증상**: `data/session_state.json`(08-18 13:33 스냅샷)에 `p8_last_success_date`·
  `eod_retrain_ok_date` 두 키가 **여전히 없다**. 현재 키는 8개 전량:
  `auto_shutdown_done_date · count · date · gbm_last_retrain · gbm_total_retrain_count ·
  reverse_entry_enabled · today_open · tp1_single_contract_mode`.
- **근거**: 08:55 PreRetrain 판정이 그 부재를 **명시적으로 우회**했다.
  ```
  2026-08-18 08:55:22 [INFO] SYSTEM: [PreRetrain] EOD 마커 파일 직접 확인 (4일 전: 2026-08-14)
                                     → PreRetrain 스킵 (session_state 미기록 보완)
  2026-08-18 08:55:22 [INFO] SYSTEM: [PreRetrain] 08:55 사전 재학습 스킵 —
                                     1영업일 전(2026-08-14) EOD 재학습 성공 (동일 데이터 중복 불필요)
                                     | WarmupRetrain 예약 소진
  ```
  `logs/20260818_SYSTEM.log`
  키 소실의 원인은 08-17 EOD 중단이며 이미 확정돼 있다 — `logs/retrain_eod_20260817.log`는
  **1,040바이트 / 11행**이고 마지막 행이 `15:46:34 [WaitDC] daily_close() 대기 중 — 잔여 18분
  (16:05:04까지)`에서 끊긴다. 16:05:04 타임아웃 로그조차 없다(= 프로세스가 그 전에 죽었다).
  대조군: `logs/retrain_eod_20260814.log` **34,103바이트**, `16:05:21 [검증 캠페인] 요약: …
  메타라벨 분류기 재학습=OK` 로 완주.
- **기준 위반**: 계측 4원칙 ④(폴백 가시화)는 **충족**했다 — 폴백이 쓰였다는 사실이 로그에
  남았고, 이것이 이 항목을 P0가 아니라 P1에 두는 이유다. 위반은 ②(미측정 ≠ 0) 쪽이다:
  완료 스탬프의 부재가 "EOD를 안 했다"와 "EOD는 했는데 스탬프를 못 남겼다"를 구분하지 못한다.
- **영향**: **오늘 거래 자격에는 영향 없음.** 폴백이 참조한 EOD 마커 파일이 실재하고
  08-14 완주가 독립적으로 확인되므로 판단 자체가 옳다. 남는 위험은 두 가지다 —
  ① 1차 경로(`session_state`)가 죽은 채로 폴백만 살아 있는 구조가 굳는다(FP-CRITICAL PSI가
  2개월간 죽어 있던 것과 같은 형태), ② 폴백 문구가 `4일 전` / `1영업일 전` 두 단위를 인접한
  두 줄에서 섞어 써 낡음 판정 기준이 로그만으로는 재구성되지 않는다.
- **신규 여부**: **기존 미해소** — `C-2`(근본원인 조사) · `O-2`(재생성 여부) · `O-3`(잔존 여부) ·
  `PF-1`(거래일 게이트 + 완주 흔적) · `PF-2`(완료 스탬프 가시화). **재발이 아니다.**
  → **O-2 판정 = 재생성 안 됨.** 08-17 EOD가 완주하지 못했으므로 예상된 결과다.
    진짜 판정은 **오늘(08-18) EOD 완주 후**에 나온다(§6).

#### 1-3. `crash_fault.log` "행감지" 표기 — 436차 주석 정정이 같은 함수 안에서 절반만 적용됐다

- **증상**: 오늘 세션(08:40:47 기동, PID 21660)이 `crash_fault.log`에 **`Timeout (0:00:30)!`
  덤프 616개**를 남겼다. 실행 경과 08:40:47→13:44 = **303분 = 606주기**로, 무조건 30초 주기
  기댓값과 일치한다. 즉 **행(hang)은 616번 나지 않았다.** 마지막 덤프의 전 스레드 스택이
  이를 직접 보여준다 — 전부 대기 상태다:
  ```
  Timeout (0:00:30)!
  Thread 0x00000554: … queue.py line 179 in get / utils/slack_queue.py line 77 in _run
  Thread 0x00001ae0: … queue.py line 170 in get / main.py line 2583 in _db_write_worker
  Thread 0x000054f4: … threading.py line 552 in wait / collection/macro/macro_fetcher.py line 99 in _loop
  Thread 0x00000694: main.py line 12598 in run / line 16842 in main
  ```
  `logs/crash_fault.log` (당일 구간 = 40,648행 이후)
- **여기까지는 신규가 아니다.** `main.py:16732~16741`이 **[MW0602 436차]** 로 이미 정정해 뒀다:
  *"`dump_traceback_later(repeat=True)`는 행 여부를 전혀 보지 않고 타이머가 만료될 때마다
  무조건 덤프한다 … 실측 세션 6.9시간에 덤프 840회 vs 무조건 주기 기댓값 828회 일치"*.
  **신규인 것은, 그 정정이 같은 함수 안에서 20줄 아래로 전파되지 않았다는 사실이다.**
- **근거 (미정정 잔존 5곳 — 전부 `main.py`)**:

  | 위치 | 현재 문구 | 문제 |
  |---|---|---|
  | `16790` | `# ── ② 행 감지 (30초 GIL 미해제 시 트레이스 덤프) ──` | 436차가 무효라 한 바로 그 서술 |
  | `16792` | `_scheduler_tick 중 COM 호출이나 DB 쿼리가 30초 이상 블로킹되면 "TIMEOUT" 덤프 기록` | 조건부 발동으로 읽힌다 |
  | `16793` | `repeat=True: 계속 블로킹 중이면 30초마다 반복 덤프` | 〃 |
  | `16801` | `_fh.cancel_dump_traceback_later()   # 행 감지 타이머 해제` | 〃 |
  | **`16814`** | **운영 로그 문자열 `"행감지=30s all_threads=True"`** | **매 기동마다 로그에 찍힌다 — 가장 널리 읽히는 표면** |

  실제 오늘 로그: `08:40:47 [INFO] SYSTEM: [FaultHandler] 활성화 | file=logs\crash_fault.log
  PID=21660 | 행감지=30s all_threads=True`
- **기준 위반**: 계측 4원칙 서두 — *"이 시스템의 가장 비싼 버그는 크래시가 아니라 '조용히
  그럴듯한 값'이었다."* 그리고 CLAUDE.md가 461차(CB③ "35%")·471차(15:10 1차 경로)에서
  반복 기록한 **문서-코드 드리프트** 유형이다. 436차 주석 자체가 이 오독을 *"419차 '죽은 상수'
  계열의 주석-실동작 괴리"* 로 분류하고 있다.
- **영향**: 거래 위험 없음. 관측 훼손이다 — **진짜 30초 행이 발생해도 616개의 정상 덤프에
  묻혀 구분할 수단이 없다.** 오늘 13:28:36의 `_tick_header 간격 37,875ms`(역대 최대)가
  정확히 그 사각지대에 떨어진다: 37.8초짜리 정체인데 대응 덤프를 특정할 수 없다.
- **신규 여부**: **신규.** `dev_memory` 전문 검색에서 "로그 문자열 `행감지`" 또는
  "16790~16814 주석 잔존"에 해당하는 기록 0건. 436차는 `16732~16741` 블록만 다뤘다.

---

### P2 — 운영 부담·기술부채

#### 1-4. CLAUDE.md 운영환경 표의 `선물 분봉 TR = OPT50029` 행이 Cybos 경로에서 무효

- **증상**: 장전 체크리스트 A-2의 *"선물 분봉 TR이 **`OPT50029`** 인가(`OPT10080` 사용 금지)"*
  항목을 오늘 로그로 확인할 수 없다 — 확인 실패가 아니라 **대상이 존재하지 않는다**.
- **근거**:
  - 당일 전 로그(`SYSTEM`/`SIGNAL`/`DATA`/`TRADE`/`launcher`)에서 `OPT50029`·`OPT10080`
    출현 **각 0건**.
  - 오늘 브로커: `08:41:22 [Capability] broker=cybos connect=Y/Y … server=Cybos 모의투자`
  - 분봉 생성 경로: `08:45:21 [CybosSub] create begin progid=Dscbo1.FutureCurOnly event=tick`
    → 실시간 틱 집계 → `[BAR-CLOSE][CYBOS] ts=… O=… H=… L=… C=… V=…` (당일 299봉)
  - 코드상 `OPT50029` 전 출현이 `collection/kiwoom/` 하위뿐(`api_connector.py:60,599`,
    `realtime_data.py:9,52,66,74,93`). `collection/cybos/`에는 0건.
  - `dev_memory/NEXT_TODO.md:11765` — **`[V1] OPT50029 초기 분봉 로드 확인 [SUPERSEDED 2026-05-04]`**
    *"모의투자 서버에서 OPT50029 rows=0 확인됨 — SetRealReg(A0166000) 전환으로 대체"*
    즉 Kiwoom 시절에도 이미 폐기됐고, 이후 Cybos 전환으로 경로 자체가 사라졌다.
- **기준 위반**: CLAUDE.md "운영 환경" 표 마지막 행. `references/invariants.md` §3-B가
  기록한 문서-코드 드리프트(CB③ 35% / CB⑤ API지연)와 **같은 유형이며 세 번째 사례**다.
- **영향**: 절대원칙이 아니라 운영환경 표이므로 거래 위험 없음. 비용은 두 가지다 —
  ① 장전 점검이 매일 "확인 불가" 항목 하나를 반복 생산한다, ② 신규 세션이 Kiwoom 분봉
  경로를 살아 있는 것으로 오독한다(실제로 `collection/kiwoom/`이 통째로 남아 있다).
- **신규 여부**: **신규**(문서 드리프트로서). 기술적 사실 자체는 2026-05-04에 확정돼 있었다.

#### 1-5. 08:58 레짐 확정 블록이 매 거래일 2회 실행된다 — 5거래일 전수 재현, dev_memory 미기록

- **증상**: 매크로 2단계(레짐 확정) 전체 블록이 **29초 간격으로 두 번** 실행된다.
- **근거** (오늘, `logs/20260818_SYSTEM.log`):
  ```
  08:58:26 [System] 매크로 수집 완료 | VIX=14.2 SP500=-0.52% KRW=+0.26%
  08:58:26 [Regime]  NEUTRAL (점수=1) | VIX=14.2 (극저공포) | SP500=-0.52% (하락) | USD/KRW +0.26% (중립)
  08:58:26 [System] 레짐 확정: NEUTRAL | …
  08:58:26 [System] PreOpen 투자자 warmup fetch 완료 (age_sec 초기화)
  08:58:26 [Notify] ℹ️ [08:58:26] [미륵이] 장전 준비 완료 ✅
  08:58:55 [System] 매크로 수집 완료 | VIX=14.2 SP500=-0.52% KRW=+0.26%     ← 동일 블록 반복
  08:58:55 [Regime]  NEUTRAL (점수=1) | …
  08:58:55 [System] 레짐 확정: NEUTRAL | …
  08:58:55 [System] PreOpen 투자자 warmup fetch 완료 (age_sec 초기화)
  08:58:55 [Notify] ℹ️ [08:58:55] [미륵이] 장전 준비 완료 ✅
  ```
  **전수 재현** — `grep -c "레짐 확정:"` 최근 6거래일 전부 **2건**:

  | 거래일 | 1회차 | 2회차 | 간격 |
  |---|---|---|---|
  | 2026-08-10 | 08:58:20 | 08:58:49 | 29s |
  | 2026-08-11 | 08:58:25 | 08:58:55 | 30s |
  | 2026-08-12 | 08:58:22 | 08:58:52 | 30s |
  | 2026-08-13 | 08:58:20 | 08:58:50 | 30s |
  | 2026-08-14 | 08:58:19 | 08:58:49 | 30s |
  | 2026-08-18 | 08:58:26 | 08:58:55 | 29s |

  `dev_memory/DECISION_LOG.md`·`NEXT_TODO.md` 검색 결과 관련 기록 **0건**.
- **기준 위반**: CLAUDE.md 매분 파이프라인 서두 *"08:55 매크로 수집 → 시장 레짐 판정"* —
  1회 실행을 전제한 서술이다. 계측 4원칙 ④에 인접한다: 두 번 판정하면 **어느 쪽이 최종인지
  로그로 구분되지 않는다**(둘 다 같은 문구, 같은 로그 레벨).
- **영향**: 오늘 두 판정은 **완전히 동일**했다(VIX=14.2 / SP500=-0.52% / KRW=+0.26% / NEUTRAL)
  → 오늘 거래 위험 0. 남는 비용은 ① 외부 매크로 소스 2회 호출, ② `PreOpen 투자자 warmup
  fetch`가 2회 돌아 `age_sec`가 두 번 초기화, ③ 사용자 알림 중복.
  ⚠ **두 호출 사이에 매크로 값이 갈린 사례는 아직 관측되지 않았다 — 확정 결론 금지(313차).**
  값이 갈리는 순간 "레짐 확정" 로그가 두 개의 서로 다른 답을 같은 문구로 말하게 된다.
- **신규 여부**: **신규**(6거래일 전수 재현되는 만성이지만 기록된 적 없음).

---

### 장중 이월 — 본 세션 범위 밖, 증거만 남긴다

본 세션이 13:43에 돌아 09:00~13:43 구간이 눈에 들어왔다. **장전 리포트에서 판정하지 않고**
장중·장후 점검으로 넘긴다. 근거만 고정해 둔다.

- **`_tick_header 간격 37,875ms` @ 13:28:36** — 역대 최대(직전 최대 14,828ms, `NEXT_TODO` O-7/N-4
  계열 관측 이력 기준). 같은 분 파이프라인은 정상: `13:28:59 [PipePerf][DBG] total=652ms`,
  `[BAR-CLOSE] ts=13:28` 정상 마감, 13:20~13:35 분봉 **누락 0봉**. `pipe_elapsed=0`.
  → **`_tick_header`(UI 타이머 간격)와 `PipePerf total`(파이프라인)이 서로 다른 것을 재고 있다**
    는 기존 `G-3`("수집기 적신호에서 둘을 분리")의 근거를 강화한다.
  → ≥5,000ms 건수는 **4건**(12:17:12 · 12:22:03 · 12:30:04 · 13:28:36)으로 기존 상향 기준
    "15건 초과"에 미달 — 건수 기준으로는 정상 범위다. **최댓값만 이상하다.**
- **CB⑤ 미발동이 정상** — 수집기 적신호 9의 답. CB⑤ 실질 구현은 `CB_PIPE_PAUSE_MS=5,000`이고
  판정 입력은 `PipePerf total`이다. 당일 최대 **2,826ms**(09:39:01)로 임계 미도달.
  `_tick_header` 블로킹은 CB⑤ 판정 입력이 **아니다**.
- **`[ZeroDiag] 진입X 원인: CoherenceGate`가 찍힌 분에 진입이 성사됐다** — 10:39:59.
  앙상블은 `grade=X`(CoherenceGate 차단, score=0.50 < min=0.60)였으나 체크리스트가
  `통과 9/11 → 등급 A (자동=True)`로 독립 판정해 `A급(원시X)` 2계약 SHORT 진입.
  결과는 +104,805원 승. 설계상 정당한 경로인지, ZeroDiag 문구가 앙상블 축만 말하는 것인지
  **장후 사후검증(§4 케이스 카드)에서 판정할 것.**
- **`[ChaseForeignComboGuard] (섀도) chase+foreign 동시 실패, 등급=A → C 강등 후보 (미적용)`**
  — 같은 10:39:59. 섀도 게이트가 정상 계측 중임을 확인(승격 전 단계).

---

### 확인 필요 (확정 아님)

- **1-1의 원인** — 예약이 왜 08:57에 돌지 않았는가는 미륵이 로그로 판정 불가다.
  판정하려면 예약 실행 이력(등록 시각·타임존·실행 결과)이 필요하다. §2 F-1.
- **1-5의 위험 실체** — 두 번째 레짐 판정이 첫 번째와 갈린 사례가 있는지. 6거래일 전부
  동일값이었으므로 **표본이 위험을 증명하지 못한다**. 갈린 날이 1회라도 나오면 P2 → P1.
- **1-3과 13:28 정체의 인과** — 30초마다 전 스레드 트레이스백을 수집하는 것 자체가 GIL을
  잡는 작업이므로 블로킹 원인 후보이나, **미검증**이다. 616회 중 4회만 5초를 넘었다는 사실은
  오히려 인과를 약화시킨다. 단정하지 말 것.

---

### 이미 반영된 사안 (신규 아님)

수집기 §11 자동 적신호 15건을 전수 대조했다. **신규 안건은 위 1-3·1-4·1-5뿐이고, 나머지
12건은 전부 기존 등록분이거나 오탐이다.**

| 적신호 | 처리 |
|---|---|
| 1~5 설정 불변식 `미발견` 5종 | **기존.** `[MW0602]` 커밋이 `v9-dev`에 미도달(브랜치 분기 `v9-dev`↔`origin/dev` = 320/315). 08-17 장전 점검이 원인 확정, 처분은 **주간회의 안건**(점검 세션 단독 결정 사안 아님) |
| 6 매분 루프 커버리지 76.8% | **오탐.** 본 세션 실행 시각이 13:44라 아직 오지 않은 시간이다 |
| 7 `13:45~15:10` 86분 공백 | **오탐.** 위와 동일 — 미래 구간 |
| 8 사이저 3계약 → 진입 2계약 | **기존.** `[28] sizing_inversion_watch`. 431차 min() 합성 이후 표본 축적 중. 오늘 사이저 출력 3계약×26 / 2계약×10 / 1계약×4, 실제 진입 2계약×7 |
| 9 블로킹 5초 초과 4건 | **해소.** CB⑤ 미발동이 정상 — 위 "장중 이월" 참조 |
| 10~13 ConstOut · WeightCollapse | **기존 만성.** 오늘 ConstOut 5회(3m×4, 5m×1) 전부 재적합 후 앙상블 복귀 확인(`09:37:58 해소` 등) |
| 14 Calibration 축퇴 154건 | **기존 만성.** 08-17 [5]가 일별 실측 평탄성 기록(08-10 157 / 08-11 148 / 08-12 145 / 08-13 141 / 08-14 144 / 08-17 143). 오늘 **154**로 같은 대역. 기동 워밍업 시퀀스이며 최종 상태 `08:41:14 앙상블 보정기 복원 완료 n=888 fitted=True degenerate=False unreachable=False` |
| 15 미커밋 454건 | **오탐(기존 등록).** Linux 샌드박스 CRLF 아티팩트. 실측 `git diff --ignore-cr-at-eol --stat` = **2파일 / 15+ / 1−** (`docs/정기점검/금요일점검/weekly_prompt.txt`, `docs/정기점검/매일점검/dailycheck_prompt.txt`). 08-17 [5]에 등록·`F-5`로 fix 대기 |

**추가로 확인한 "정상이라 보고 안 함" 항목** (매일 되풀이 방지용 기록):

- `[GUARD] 이미 실행 중인 main.py 프로세스가 감지됐습니다 → 종료 완료` (launcher 08:40) —
  08-17 17:57 기동분이 밤새 살아 있었고 런처 가드가 설계대로 정리했다. 이중 실행 방지 정상.
- **08-17 커밋 대기 10파일 해소 확인** — 08-17 *"커밋 대기 — 473차 실질 변경 10파일"* 이
  474차 커밋 `7dc14bc`에 포함돼 오늘 실질 미커밋은 2파일뿐이다. `NEXT_TODO` 해당 체크박스는
  완료 처리 대상(§8).

---

### 장전 통과 기록 (A-0 ~ A-4)

P0가 0건인 근거를 항목별로 남긴다. *"로그에 없다 = 관측되지 않았다"* 를 피하기 위해
**전 항목에 인용을 붙였다.**

| 항목 | 판정 | 근거 (`logs/20260818_*` / launcher) |
|---|---|---|
| **A-0** 브랜치 | ✅ | `git branch --show-current` = `v9-dev` (MW0601 운영 브랜치). `origin/v9-dev`와 SHA 동일 — behind 0 |
| A-1 기동 시각·PID | ✅ | `08:40:47 [FaultHandler] 활성화 … PID=21660`. 런처 `launcher_20260818_084001_2415.log`, 스케줄러 자동 기동(`[GUARD] 장전 [0840] — 10초 후 자동 Y`) → **O-8 판정 = 정상** |
| A-1 중복 기동 | ✅ | 런처 가드가 잔존 프로세스 종료 후 단일 인스턴스 시작 |
| A-1 Python 런타임 | ✅ | `[START] 2026-08-18T08:40:47 PID=21660 **Python 3.7.13 32bit** RSS=162MB` (`crash_fault.log`). 런처: `[INFO] 32-bit Python 확인: …\envs\py37_32\python.exe` |
| A-1 Cybos 접속·COM | ✅ | `[OK] cybos API 세션 준비 확인` → `[OK] cybos preflight 통과` → `08:41:21 [CybosSub] create/dispatch/input/with-events/subscribe **전 단계 ok** progid=Dscbo1.CpFConclusion` |
| A-1 계좌·자본 | ✅ | `08:41:22 [Capital] 기준자본=50,000,000원(target) | 실제잔고=50,053,027원 | 괴리=+0.1% | base_risk=1,500,000원(3.0%) | **MAX_CONTRACTS=3**` — 431차 값 유지 |
| A-1 모델 번들 로드 | ✅ | `08:41:00 [Model] 1m/3m/5m/10m/15m/30m 로드 성공` **6/6**. `08:41:02 [RF] 로드 완료: 6호라이즌 ready=True`. `pickle protocol=4` 호환 정상 |
| A-1 **전일 EOD 재학습** | ✅ | 직전 **거래일**은 08-14(08-17은 KRX 휴장). `retrain_eod_20260814.log` 34,103B, `16:05:21 … 메타라벨 분류기 재학습=OK` 완주. 08:55 PreRetrain이 이를 재확인(1-2) |
| A-1 스케일러 노후 | ✅ | `08:45:21 [EarlyWarmup] scaler 노후=89h → 08:45 선행 warmup 시작` → `08:45:21 [EarlyWarmup] 완료 n=30봉`. `08:55:22 [Canary] scaler 노후=0h z경고피처=10개 (임계 12개)` |
| **A-2** 08:55 매크로 | ✅ | `08:55:21 [System] 장 전 매크로 수집 시작 (1단계 — seed fetch)` → `08:58:26 [System] 매크로 수집 완료 | VIX=14.2 SP500=-0.52% KRW=+0.26%` |
| A-2 레짐 판정 | ✅ (단 2회 — 1-5) | `08:58:26 [Regime] **NEUTRAL** (점수=1) | VIX=14.2 (극저공포) | SP500=-0.52% (하락) | USD/KRW +0.26% (중립)` |
| A-2 **실시간 구독 사전 시작** | ✅ | `08:45:21 [CybosRT-START] snapshot begin/end code=A0569 price=1117.78` → `tick subscribe end` → `hoga subscribe begin`. **09:00보다 15분 앞서 시작 — 첫 봉 유실 없음.** 08:41 시점 `[System] 장외 시간에는 … 구독을 시작하지 않음`은 설계대로의 대기 상태 |
| A-2 옵션 체인 로드 | ✅ | `08:41:21 [OptionChain] 초기화 완료: 체인 캐시 **5242 종목**`. 당일 `OptionChain` 컴포넌트 38회 |
| A-2 선물 분봉 TR | **N/A** | Cybos 경로에는 TR 개념이 없다 — 실시간 틱 집계(`[BAR-CLOSE][CYBOS]` 299봉). **체크리스트 항목 자체가 낡음 → 1-4** |
| A-3 설정 불변식 | ✅ | `불일치` 0행. `미발견` 5행은 브랜치 분기(기존) |
| A-3 한시예외 기한 | ✅ | CB② 2026-08-29까지 11일 — 미도래. §7 |
| **A-4** 미커밋 | ✅ | 실질 2파일(둘 다 문서). 라이브 경로 미커밋 변경 **0건** |
| A-4 PC명 태그 | ✅ | 최근 12커밋 전부 `[MW0601]` 접두 |
| A-4 타 PC 커밋 pull | ⚠ | `v9-dev`↔`origin/dev` = 320/315 — 기존 주간회의 안건 |

---

## 2. Fix 작업 구현계획

> 🔴 **본 계획은 전부 수립만 한다.** 현재 13:43, 라이브 프로세스 가동 중이며 포지션 관리가
> 돌고 있다. **적용 시점은 오늘 15:10 강제청산 이후**다.
> ⚠ 오늘 밤 EOD(15:45)는 08-17 중단 이후 **첫 완주 시도**다(§6 O-1). EOD 경로를 건드리는
> 변경은 **EOD 완주를 확인한 뒤**로 미룬다 — 08-17 NEXT_TODO의 *"오늘 밤 금지: EOD가
> 미검증 코드로 돈다"* 원칙을 그대로 승계한다.

### F-1. 장전 점검 예약 실행 시각 이탈 — P1 · 대응 이상점 1-1

- **원인 가설**: 미륵이 리포 밖. 후보 3가지 — ① 08:57에 Cowork 앱/에이전트가 미기동이라
  실행이 지연되고 앱 기동 시점(13:43 부근)에 밀린 작업이 소진됨, ② 예약 등록 자체가
  다른 시각/타임존으로 들어감, ③ 예약이 3국면(pre/intra/post) 큐에서 직렬화되며 밀림
  (08-17에 3국면이 16:48/16:49/16:55에 몰린 전례가 ①·③을 동시에 시사한다).
- **변경 파일**: **미륵이 repo 변경 없음.** 예약 정의(스케줄러) 측 조치다.
  - 확인: 예약 등록 목록에서 `mireuk-premarket-check`의 등록 시각·타임존·최근 실행 이력
  - 조치안 A(권장): 예약 시각을 **08:57 → 08:50**으로 앞당겨 여유를 확보하고,
    3국면 예약을 **최소 10분 이상 이격**(pre 08:50 / intra 12:30 / post 15:50)
  - 조치안 B: 실행 지연을 리포트가 **스스로 감지**하도록 배선 — 아래 F-2
- **회귀 위험**: 없음(예약 메타데이터 변경).
- **절대원칙 저촉**: 없음.
- **런타임 주의**: 해당 없음(미륵이 프로세스 무관).
- **검증**: 08-19 장전 점검 리포트 헤더의 `점검 시각`이 **08:50±5분**인지.
- **적용 시점**: 즉시 가능(코드 아님). 다만 **사용자 확인 필요** — 예약 시각 변경은 운영 결정.
- **결정 필요 사항**: 조치안 A의 시각을 08:50으로 할지 08:45로 할지.
  ⚠ **08:45 이후는 라이브 DB 금지 구간**이므로, 장전 점검이 DB를 볼 수 있으려면
  **08:45 이전**이어야 한다. 그러나 08:41 기동 직후는 로그가 아직 안 쌓인다.
  → **권고: 08:50**(로그 확보 우선, DB는 어차피 포기). 사용자 판단 요청.

### F-2. 리포트 자기 시각 검증 — P2 · 대응 이상점 1-1

- **원인 가설**: 실행 시각 이탈이 **리포트 안에서 조용히 지나갔다.** 08-17에도 3국면이
  전부 늦게 돌았지만 리포트가 그것을 자기 결함으로 올리지 않고 `O-7` 관측 항목으로만 남겼다.
- **변경 파일**:
  - `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py` — `main()` 진입부에
    국면별 기대 창(`pre` 08:40~09:05 / `intra` 09:00~15:10 / `post` 15:35~) 상수를 두고,
    현재 시각이 벗어나면 다이제스트 **첫 줄**에 `⚠ 국면-시각 불일치: pre 기대 08:40~09:05,
    실행 13:43` 배지를 찍고 §11 적신호 1번으로 올린다.
  - 같은 파일 `pc_id()` — **`--pc` 인자 추가**(우선순위 `--pc` > `MIREUK_PC_ID` > 호스트명).
    현재 리포 판본은 `--pc`를 받지 않아 코웍 샌드박스(호스트명 `claude`)에서
    `evidence_UNKNOWN-…`이 생긴다. **오늘은 `platform.node()` 몽키패치로 우회했다** —
    이 우회가 필요했다는 사실 자체가 `F-5`(08-17 등록)의 근거를 강화한다.
  - 같은 파일 git 호출부 — `git diff`에 `--ignore-cr-at-eol` 부여(`F-5` 본문).
- **회귀 위험**: 낮음. 수집기는 표준 라이브러리 전용 — 외부 의존 추가 금지.
- **절대원칙 저촉**: 없음.
- **런타임 주의**: 수집기는 py37_32/py310_64 어디서든 동작해야 한다. `datetime`만 사용.
- **검증**: `--phase pre`를 오후에 강제 실행 → 배지·적신호가 뜨는지. `--pc MW0601`로
  실행 시 헤더가 `MW0601 (override · host=…)`인지.
- **적용 시점**: 장후. **`F-5`와 한 커밋으로 묶는다**(같은 파일, 중복 착수 금지).

### F-3. FaultHandler "행감지" 문구 정정 — P1 · 대응 이상점 1-3

- **원인 가설**: 436차가 `main.py:16732~16741`에 정정 블록을 **삽입**했으나, 20줄 아래의
  원 주석과 운영 로그 문자열을 **교체하지 않았다**. 정정이 "추가"로만 이뤄져 원문이 병존한다.
- **변경 파일** (전부 `main.py`, 단일 함수 내):
  - `16790` 주석 → `# ── ② 주기 덤프 (30초마다 무조건 — 행 감지 아님, 16732 참조) ──`
  - `16792~16793` 두 줄 → 436차 정정문에 맞춰 교체:
    *"발동 조건이 없다. 크래시 직전 구간을 30초 해상도로 남기기 위한 무조건 주기 덤프다.
    행 여부 판정에 쓰지 말 것."*
  - `16801` `# 행 감지 타이머 해제` → `# 주기 덤프 타이머 해제`
  - **`16812~16816` 로그 문자열** → `"[FaultHandler] 활성화 | file=%s PID=%d | 주기덤프=30s(행감지 아님) all_threads=True"`
- **회귀 위험**: **없음 — 동작 코드 0줄 변경.** `dump_traceback_later(30, repeat=True)`는
  그대로 둔다(436차 결정: *"30초 해상도가 크래시 직전 구간의 진단 가치이므로 늘리면
  230차가 넣은 목적 자체가 훼손된다"*).
- **절대원칙 저촉**: 없음. ④(COM 콜백)와 무관 — 콜백 밖 초기화 코드다.
- **런타임 주의**: `py37_32`(장중 본체). COM 콜백 내부 아님.
- **검증**: 다음 기동 로그에 `주기덤프=30s(행감지 아님)`이 찍히는지.
  회귀 테스트는 불필요하나, `tests/test_457_fallback_visibility.py` 실행으로 부작용 부재 확인.
- **섀도 계측 필요**: 불필요(게이트 신설 아님).
- **적용 시점**: **장후 즉시 적용 가능** — 문자열만 바꾸므로 EOD 위험 없음.

### F-4. CLAUDE.md 운영환경 표 TR 행 정정 — P2 · 대응 이상점 1-4

- **원인 가설**: Kiwoom → Cybos 전환(및 그 이전 2026-05-04 SetRealReg 전환) 시
  CLAUDE.md 운영환경 표가 함께 갱신되지 않았다.
- **변경 파일**:
  - `CLAUDE.md` "운영 환경" 표 — `선물 분봉 TR | OPT50029` 행을 **삭제하지 말고** 다음으로 교체:
    `선물 분봉 | Cybos 실시간 틱 집계(Dscbo1.FutureCurOnly → [BAR-CLOSE][CYBOS]) — Kiwoom OPT50029/SetRealReg 경로는 2026-05-04 SUPERSEDED`
    ⚠ **삭제 금물** — 계측 4원칙 ③(탈락 가시화). 지우면 Kiwoom 경로 잔존을 다음 세션이 모른다.
  - `.claude/skills/mireuk-daily-check/references/phases.md` A-2 마지막 항목 —
    `[ ] 선물 분봉 TR이 OPT50029인가` → `[ ] 분봉 원천이 Cybos 실시간 틱인가([BAR-CLOSE][CYBOS] 봉수 확인). ※ OPT50029는 Kiwoom 레거시 — Cybos에서는 N/A`
  - `.claude/skills/mireuk-daily-check/references/invariants.md` §3-B 표에 **3번째 행 추가**
    (CB③ 35% · CB⑤ API지연에 이어 TR 행).
- **회귀 위험**: 문서 전용. 없음.
- **절대원칙 저촉**: 없음(운영환경 표는 절대원칙 6종에 포함되지 않는다).
- **런타임 주의**: 해당 없음.
- **검증**: 08-19 장전 점검에서 A-2 항목이 `N/A`가 아니라 `✅`로 판정되는지.
- **적용 시점**: 장후.

### F-5. 08:58 레짐 확정 중복 — **조사 우선, 코드 변경 보류** — P2 · 대응 이상점 1-5

- **원인 가설(미검증)**: 08:55 seed fetch 후 *"레짐 확정은 08:58 2단계로 연기"* 하는 경로와,
  별도 스케줄 틱이 같은 2단계를 한 번 더 부르는 이중 트리거. 29~30초 간격이 일정한 점이
  **타이머 두 개**(또는 한 타이머의 재진입)를 시사한다.
- **변경 파일 (1단계 = 조사만)**:
  - `main.py` — `[System] 매크로 수집 완료` 로그를 내보내는 호출부를 전수 식별하고,
    각 호출부에 **호출 출처 태그**를 붙인다(`caller=scheduler_tick` / `caller=premarket_stage2` 등).
    ⚠ 값 로직은 손대지 않는다.
  - `collection/macro/macro_fetcher.py` — 2단계 fetch 진입 시 **재진입 가드 섀도 로그**
    (`[MacroStage2] 중복 호출 감지 prev=<ts> gap=<n>s` — WARNING, **차단하지 않음**).
- **회귀 위험**: 로그만 추가하므로 낮음. **차단 로직을 지금 넣지 않는 것이 핵심** —
  두 번째 호출이 첫 번째의 실패를 덮는 안전망일 가능성을 배제하지 못했다.
- **절대원칙 저촉**: 없음.
- **런타임 주의**: `py37_32`. `macro_fetcher._loop`은 **별도 스레드**(`crash_fault.log` 스택에서
  `Thread 0x000054f4`로 확인) — COM 콜백 아님, `emit` 금지 조항과 무관.
- **검증**: 08-19~08-21 3거래일 관측 → 두 호출의 `caller` 태그가 서로 다른지, 값이 갈리는
  날이 있는지. **갈리는 사례 1건이라도 나오면 P1 승격 + 차단 게이트 설계 착수.**
- **섀도 계측 필요**: **예 — 그래서 1단계를 섀도로만 한다**(CLAUDE.md 게이트 신설 절차).
- **적용 시점**: 장후.
- **결정 필요 사항**: 없음(조사 단계).

### 적용 순서와 커밋 계획

| # | 커밋 | 포함 | 메시지 초안 | 시점 |
|---|---|---|---|---|
| 1 | 문서 정정 | F-3(문자열) · F-4(CLAUDE.md·phases·invariants) | `[MW0601] 475차: FaultHandler "행감지" 문구 정정 + 분봉 TR 행 Cybos 정합화` | 15:10 이후, EOD 전 가능 |
| 2 | 수집기 | F-2 + **F-5(08-17 등록분: `--pc`·CRLF)** 통합 | `[MW0601] 475차 후속: 수집기 국면-시각 검증 + --pc 인자 + CRLF 정규화` | **EOD 완주 확인 후** |
| 3 | 섀도 계측 | F-5(레짐 중복 조사 로그) | `[MW0601] 475차 후속2: 매크로 2단계 중복 호출 섀도 계측` | EOD 완주 확인 후 |
| — | F-1 | 예약 시각 조정 | **코드 아님 — 사용자 조치** | 사용자 결정 후 즉시 |

> ⚠ 커밋 2·3은 오늘 EOD(15:45) 완주를 확인한 뒤에 한다. 08-17 EOD가 중단된 상태이므로
> 오늘이 **연속 실패 2일차인지 아닌지**가 먼저 확정돼야 한다.

---

## 3. 고도화 방안

### G-1. 장전 점검을 "관측"에서 "게이트"로 — 자동 P0 프리플라이트

- **관측 근거**: 오늘 장전 점검이 13:43에 돌았다(1-1). P0가 0건이라 무사했지만, **점검이
  늦으면 늦은 만큼 무방비**라는 구조가 드러났다. 반면 장전 뼈대 확인 항목(§1 A-0~A-4 표)은
  전부 **로그 문자열 매칭으로 기계 판정이 가능**하다 — 오늘 20개 항목 중 19개가 그렇다.
- **제안 내용**: `scripts/premarket_preflight.py`(신규, 표준 라이브러리 전용)를 만들어
  **08:59에 미륵이 프로세스와 무관하게** 실행. 다음 6개만 판정하고 종료코드로 답한다:
  ① 브랜치 = `v9-dev` ② `[Model] * 로드 성공` 6/6 ③ `[CybosSub] subscribe end` 존재
  ④ 직전 거래일 `retrain_eod_*.log` 완주 마커 ⑤ 설정 불변식 `불일치` 0 ⑥ `[Regime] 레짐 확정` 존재.
  `rc=0` 정상 / `rc=1` 경고 / **`rc=2` P0** — `rc=2`면 데스크톱 알림 + `[Notify]` 발송.
  LLM 점검은 그대로 두되, **거래 자격 판정만 사람/LLM 지연에서 떼어낸다.**
- **기대 효과**: 측정 가능 — 08-19 이후 장전 P0 탐지 시각이 `점검 세션 실행 시각`에서
  **08:59 고정**으로 바뀐다. 오늘 기준 지연 4시간 44분 → 0분.
- **비용·위험**: 구현 반나절. 위험은 **오탐으로 인한 개장 저지**인데, 이 스크립트는
  **아무것도 차단하지 않는다**(알림만) — 그래서 섀도 계측 단계를 건너뛸 수 있다.
- **선행 조건**: F-3·F-4(로그 문자열이 정정된 뒤여야 매칭 패턴이 안정적이다).
- **우선순위 제안**: **이번 주.**

### G-2. `_tick_header` 정체를 crash_fault 덤프와 짝지어 읽는 상관 도구

- **관측 근거**: 오늘 13:28:36 `_tick_header 간격 37,875ms`(역대 최대)가 발생했는데,
  같은 시각의 스레드 스택을 특정할 수 없다 — `crash_fault.log`에 616개 덤프가 **시각
  스탬프 없이** 쌓여 있기 때문이다(faulthandler는 타임스탬프를 찍지 않는다).
  1-3의 실질 피해가 바로 이것이다.
- **제안 내용**: `scripts/fault_dump_index.py`(신규) — `crash_fault.log`를 `[START]` 헤더
  기준으로 세션 분할하고, 덤프가 **정확히 30초 주기**라는 성질을 이용해 각 덤프에
  `추정시각 = START + 30s × n`을 부여한 인덱스를 만든다. `--at 13:28:36 --window 60`으로
  질의하면 해당 구간 덤프의 메인 스레드 스택만 뽑는다.
  ⚠ 추정 시각이므로 **`추정`을 출력에 명시**한다(계측 4원칙 ④ — 폴백 가시화).
- **기대 효과**: `_tick_header ≥5,000ms` 건에 대해 **스택 근거를 붙일 수 있게 된다.**
  현재 이 채널(`O-7`/`N-4`/`O-2`/`O-6`)은 6개월째 "건수와 최댓값"만 세고 원인은 못 짚었다.
- **비용·위험**: 구현 2~3시간. 위험 낮음(읽기 전용 오프라인 도구).
  ⚠ 30초 주기 가정이 깨지면(GIL 정체로 덤프 자체가 밀리면) 추정 시각이 어긋난다 —
  그래서 **판정이 아니라 후보 제시 도구**로만 쓴다.
- **선행 조건**: F-3(문구 정정)과 같은 이해 위에 서야 한다.
- **우선순위 제안**: **이번 주** — 13:28 건이 아직 뜨거울 때.

### G-3. 매크로 소스 이중 판정의 승자 명시 (1-5 후속)

- **관측 근거**: 08:58 레짐 확정 2회, 6거래일 전수 재현. 오늘은 두 판정이 동일했으나
  **다를 경우 로그가 같은 문구로 두 개의 답을 말한다.**
- **제안 내용**: F-5 섀도 계측으로 이중 호출의 출처가 확정되면, 레짐 확정 로그에
  `stage2_seq=1/2` 와 `final=Y/N` 필드를 추가한다. 값이 갈린 경우에만 WARNING 승격.
- **기대 효과**: "어느 판정이 실제로 쓰였는가"가 로그만으로 재구성 가능해진다.
- **비용·위험**: 낮음. 단 **F-5 조사 결과에 종속** — 이중 호출이 안전망이면 설계가 달라진다.
- **선행 조건**: **F-5 3거래일 관측 완료.**
- **우선순위 제안**: F-5 판정 후.

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| 장전 프리플라이트(G-1) | 없음 | **실전 전환 기준 ②의 선행 확인사항으로 등재 검토** | ②는 이미 471차가 "15:10 경로 실집행 1회 확인"을 선행조건으로 붙였다. 개장 전 자격 판정이 사람/LLM 지연에 종속된 구조는 실전에서 더 위험하다 |
| CB② 복원 | 실전전환 ⑤ · 재검토 2026-08-29 | **주간회의 상정(기한 11일)** | 기존 등록. §7 |
| ⑨ TOX-SEVERE-SPREAD | 실전전환 ⑨ | **변경 제안 없음** | 473차 F-8 Phase A `INSUFFICIENT`(ETA 7.1개월). 처분은 주간회의 안건 — 점검 세션 단독 결정 사안 아님 |

---

## 4. 진입 승패 사후검증 (장후 한정)

**해당 없음(장후 전용).** 단 §1 "장중 이월"에 케이스 카드 후보 1건(10:39:59 `A급(원시X)`)을
근거와 함께 남겨 뒀다.

## 5. 수익률 향상방안 (장후 한정)

**해당 없음(장후 전용).** 장전 관측만으로 도출하면 일반론이 된다(SKILL.md §5).

---

## 6. 다음 거래일 관측 예정

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| **O-1** (이월) | `logs/retrain_eod_20260818.log` 크기와 마지막 행 | ≥10KB **且** `[검증 캠페인] 요약:` 행 존재 → 완주. 미달이면 **연속 2일 실패 = P0 승격** | 오늘 15:45~16:10 |
| **O-2** (이월) | `data/session_state.json` 키 목록 | 오늘 EOD 완주 후 `p8_last_success_date`·`eod_retrain_ok_date` **재생성** | 오늘 EOD 직후 |
| **O-3** (이월) | 위 두 키의 잔존 | 08-19 08:41 기동 **후에도** 남아 있는가 | 2026-08-19 |
| **O-7** (이월) | 장전 점검 실행 시각 | 08:57±5분(또는 F-1 조정 후 08:50±5분) | 2026-08-19 |
| **O-13** 🆕 | 장전 점검 리포트 헤더의 `점검 시각` | F-1 미조치 시에도 리포트가 스스로 이탈을 §1 P1으로 올렸는가 | 2026-08-19 |
| **O-14** 🆕 | `[Regime] 레짐 확정` 건수와 두 판정의 값 | 2건 유지 여부 + **두 값이 갈리는가**. 갈리면 1-5 → P1 승격 | 08-19 · 08-20 · 08-21 (3거래일) |
| **O-15** 🆕 | `crash_fault.log` 당일 `Timeout` 덤프 수 ÷ 세션 분수 | 30초 주기 기댓값과의 괴리. **기댓값 대비 +5% 초과면 진짜 정체 발생 신호** | 2026-08-19 |
| **O-16** 🆕 | `_tick_header ≥5,000ms` 건수·최댓값 | 오늘 4건 / **37,875ms**. 건수 15건 초과면 G-3 상향, 최댓값 40,000ms 초과면 P1 승격 | 2026-08-19 |
| **F-1H** (이월) | `[SchedForceExit] … bar_pass=N회` 하트비트 | 15:10 전후 출현. **오늘이 1일차** — 08-17은 본체 부재로 판정 불가였다 | 오늘 15:10 |
| **F-1R** (이월) | 15:10 강제청산 리허설 | **사용자 실행 필요** — 15:05~15:09 모의 1계약 진입 후 15:10 관측 | 사용자 결정 |
| **F-4M** (이월) | 471차 후속2 스키마 신규 행 | 오늘 진입 7건 발생 → **신규 행이 있어야 한다**(08-17은 0건이라 판정 불가) | 오늘 EOD 후 |

---

## 7. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| **CB② 복원** (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | 2026-08-29 | **11일** | **미도래.** 오늘 `9999` 유지 확인. 08-29 주간회의 상정 — 기존 등록분. ⚠ 오늘 CB 이벤트 `연속 손절 1회`(13:08:37)·`연속 손절 2회`(13:19:39) 발생 — 카운터는 오르고 정지는 안 하는 **설계대로의 동작** |
| CB③-P4 재검토 (⑥) | 기한 없음 (30m 재도입 또는 CB③ 기준 호라이즌 교체 시) | — | 미도래. 오늘 `CB③-P4` acc30m 단계 전환 8건(NORMAL↔WATCH) 정상 추적 중 — 차단만 비활성이고 계측은 살아 있다 |
| FP-CRITICAL 재검토 (⑦) | 기한 없음 (PSI 계측 재설계 후) | — | 미도래. 371차 분위수 재설계 후 라이브 관찰 중 |
| TOX-SEVERE-SPREAD (⑨) | **기한 설정 불가** | — | 473차 F-8 Phase A `INSUFFICIENT`(ETA 7.1개월). **주간회의 안건** — 선행조건 유지 vs 26주 WFA 이관 |
| 실전전환 ⑧ 사이징 | 조건 2(`MAX_CONTRACTS`) 충족 / 조건 1(실전 자본) 미충족 | — | 오늘 `MAX_CONTRACTS=3` 유지 확인(`08:41:22 [Capital]`) |

---

## 8. dev_memory 반영

- `DECISION_LOG.md` 추가: `## 2026-08-18 (MW0601 475차 — 장전 점검)` — **append**
- `NEXT_TODO.md` 추가 체크박스: **13건**
  (F-1 · F-2 · F-3 · F-4 · F-5 / G-1 · G-2 · G-3 / O-13 · O-14 · O-15 · O-16 / 커밋 대기)
- 완료 처리한 기존 항목: **2건**
  - `커밋 대기 — 473차 실질 변경 10파일 + untracked 2종` → 474차 커밋 `7dc14bc`에 포함 확인
  - `O-8 08-18 08:40 자동 기동 여부` → **정상**(런처 `[GUARD] 장전 [0840] — 10초 후 자동 Y`)
- 판정된 관측 항목: **O-2 = 실패**(재생성 안 됨, 예상된 결과) · **O-7 = 실패**(1-1)
- **`git commit`·`git push` 미실행.** 사용자 확인 후 커밋한다.

---

---

## 부록 — 작성 중 관측: 점검 세션 동시 실행 재발 (기존 `G-3`)

본 리포트 작성 도중 **장중(intra) 점검 세션이 별도 샌드박스에서 동시에 돌았다.**

```
docs/정기점검/매일점검/evidence_MW0601-20260818_pre.md    생성 13:44:15  리포 /sessions/sharp-gallant-volta/…
docs/정기점검/매일점검/evidence_MW0601-20260818_intra.md  생성 13:49:07  리포 /sessions/awesome-pensive-volta/…
```

리포 경로가 다르므로 **별개 세션**이다. 본 세션은 13:57~13:59에
`dev_memory/DECISION_LOG.md`(1.9MB)·`NEXT_TODO.md`(962KB)에 append 했으며, 그 시점에
intra 세션이 진행 중이었을 가능성이 높다.

- **기존 항목이다** — 08-17 473차 후속2 [4]가 같은 현상(pre 16:48 / intra 16:49 / post 16:55)을
  기록하고 `G-3 점검 세션 동시 실행 직렬화`(`dev_memory/.dailycheck.lock`)를 등록했다.
  MW0602 `483d41a`에 *"동시 세션 reset으로 유실 → 재커밋"* 전례가 있다.
- **오늘 재발**했으므로 `G-3`의 우선순위 근거가 강화된다. 신규 안건으로 올리지 않는다(함정 ①).
- 이번 append 결과는 `git diff --ignore-cr-at-eol --stat`으로 확인했다 —
  `DECISION_LOG.md +172` / `NEXT_TODO.md +136`, **삭제 0줄**. 유실은 발생하지 않았다.
- ⚠ 사용자 확인 사항: intra 세션도 같은 두 파일에 append 했다면 **한쪽이 덮였는지**
  커밋 전 `git diff`로 확인할 것.

---

*본 리포트는 2026-08-18 13:43 KST에 작성됐다. 코드 변경 0건, 라이브 DB 접근 0건.*
