# 미륵이 일일 점검 — MW0602 / 2026-09-02

# 장전(pre)

- 점검 시각: 09:04 KST · PC `MW0602` · 브랜치 `dev`
- HEAD `cac617a` · 미커밋 0건(`-c core.autocrlf=true` 측정 — CRLF 파생 아닌 실질 변경 없음) · 당일 커밋 0건
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260902_pre.md`
- ⚠ 이 리포트는 예약 실행(스킬 트리거)이 개장 1분 5초 후에 시작됐다. 지침서 정본은 이번
  세션 시작 시 `.claude/skills/mireuk-daily-check/SKILL.md`(rev `2026-08-26e`)를 다시 읽어
  따랐다 — 앱에 저장된 사본(rev `2026-08-26`, 494차)은 낡았다(아래 1-2).

## 0. 한 줄 결론

오늘 아침 프로그램은 정상적으로 켜졌고, 접속·모델 로드·장 시작 준비가 모두 제때 끝나
돈을 걸 준비는 돼 있습니다. 다만 시장 변동성을 재는 값(VKOSPI) 하나가 지난 5거래일
내내 비정상적으로 높게 찍히다가 오늘 아침 정상 범위로 돌아왔습니다 — 오늘 손익에는
영향이 없지만, 그 값을 읽어오는 코드 자체가 다른 지수를 잘못 읽고 있었을 가능성이
있어 확인이 필요합니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 조건부 정상 | 기동·접속·레짐판정·CORE피처 정상. VKOSPI 5거래일 이상치 판정일 도래(P1, 아래 1-1) |
| 장중 | (진행 예정) | |
| 장후 | (진행 예정) | |

**절대원칙 준수 여부**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | 해당없음(미도래) | 장전 점검 — 장후 절에서 판정 |
| ② | Circuit Breaker 5종 | 정상 | 오늘 발동 로그 없음. `CB_state=NORMAL` 매분 샘플(수집기 §12) |
| ③ | CORE 피처 호라이즌 그룹별 | 정상 | 08:56~08:59 CORE 축퇴 있었으나 09:00 개장 시점 `축퇴 0/6`으로 해소(§1-3 참조, `logs/20260902_SIGNAL.log`) |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 점검범위 아님 | 정적 코드 규칙 — 장전 로그로 판정 불가 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 점검범위 아님 | 정적 코드 규칙 — 장전 로그로 판정 불가 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 정상 | 관련 로그·흔적 없음 |

**설정 불변식** — 수집기 §3 표 전 20행 `일치`/`값 확인`. `불일치`·`미발견` 없음. 차단 게이트
29개 중 7개 꺼짐(전부 기록된 한시예외/기능토글) — CLAUDE.md 절대원칙 §2 한시예외 목록과 일치.

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

없음.

### P1 — 정확성·관측 훼손

#### 1-1. VKOSPI(변동성 지수) 5거래일 연속 비정상 고값 — 사전등록 판정일 오늘 도래, 오늘 아침엔 정상 복귀

- **증상**: 프로그램이 매분 읽어오는 시장 변동성 지수(VKOSPI, `[IndexPoll]` 로그의
  `vkospi=` 값)가 최근 5거래일 내내 정상 범위를 벗어나 있었습니다. 이 값은 8월 26일부터
  9월 1일까지 매일 45를 넘겼고(최고 56.29), 오늘 아침(9월 2일)에는 42.92~44.03으로
  정상 범위 안으로 돌아왔습니다.
- **근거**:
  ```
  2026-08-26 09:00:16 [INFO] SYSTEM: [IndexPoll] ... vkospi=56.29   (당일 최고 56.29 / 최저 55.51)
  2026-08-27 09:00:18 [INFO] SYSTEM: [IndexPoll] ... vkospi=55.59   (당일 최고 55.59 / 최저 53.02)
  2026-08-28 09:00:19 [INFO] SYSTEM: [IndexPoll] ... vkospi=53.01   (당일 최고 53.01 / 최저 50.05)
  2026-08-31 09:00:29 [INFO] SYSTEM: [IndexPoll] ... vkospi=50.08   (당일 최고 50.08 / 최저 46.04)
  2026-09-01 09:00:19 [INFO] SYSTEM: [IndexPoll] ... vkospi=46.05   (당일 최고 46.05 / 최저 44.01 — 혼재)
  2026-09-02 09:00:26 [INFO] SYSTEM: [IndexPoll] ... vkospi=44.03   (당일 최고 44.03 / 최저 42.92 — 전부 정상범위)
  ```
  `logs/20260826_SYSTEM.log` ~ `logs/20260902_SYSTEM.log`, 각 `[IndexPoll]` 태그
- **기준 위반**: 절대원칙 위반은 아니다. `dev_memory/NEXT_TODO.md` 등록 항목 `O-36`·`C-5`가
  "VKOSPI 값 범위 밖이 5거래일 연속이면 원천 코드 오지정으로 판정하고 `C-5`(증권사 화면 대조
  과제)를 P1으로 승격 — 판정 예정일 **2026-09-02**"로 사전등록해 둔 것을, 오늘이 그 판정일이라
  이 세션이 실행한다.
- **판정**: 사전등록 기준을 문자 그대로 적용하면 — 8/26·8/27·8/28·8/31 나흘은 그날 관측된
  값 전부가 45를 초과했고, 9/1은 하루 안에서 44.01(정상)과 46.05(초과)가 섞였다. "그날
  범위 밖 관측이 있었는가"로 보면 8/26~9/1 **5거래일 연속** 조건이 충족된다. 오늘(9/2)은
  지금까지 관측된 값 전부가 정상 범위라 스트릭이 끊겼다.
  → **313차 원칙(사전등록 기준을 결과 보고 바꾸지 않는다)에 따라, 오늘 정상으로 돌아왔다는
  이유로 판정을 건너뛰지 않는다.** 사전등록된 조치(`C-5`를 P1으로 승격)를 아래 §2 F-1에
  반영한다.
- **영향**: 이 값 자체는 실제 매매 판단에 쓰이는 105개 입력 변수에 포함돼 있지 않아서,
  지난 5일간 이상치가 있었어도 매매 결과에는 영향이 없었습니다. 다만 코드가 다른 지수를
  잘못 읽어오고 있었을 가능성이 있고, 이런 종류의 "값은 정상적으로 나오는데 실은 잘못된
  것을 재고 있다"는 문제는 이 프로젝트에서 여러 번(변동성 계측, 확률 보정기 등) 뒤늦게
  발견된 이력이 있어 미리 확인해 두는 편이 안전합니다.
- **영향(기술)**: `vkospi`·`rv_iv_spread`는 `active_features`(105종) 미포함이라 GBM 예측에는
  영향 0. 다만 원천 코드 `O2901P` 오지정 가설(증권사 코드가 VIX 계열 다른 지수를 가리킬
  가능성)이 사전등록에 명시돼 있고, 아직 실물(증권사 HTS 화면) 대조는 안 됐다.
- **신규 여부**: 신규 판정 실행(사전등록 자체는 496차 기존 TODO `O-36`/`C-5`).

### P2 — 운영 부담·기술부채

#### 1-2. 점검 지침서 앱 사본 드리프트 — 12회째 이월

- **증상**: 이 세션이 스킬 트리거로 처음 로드한 사본은 `rev: 2026-08-26 (494차)`인데,
  저장소 정본은 `rev: 2026-08-26e (494차+498차 후속+499차)`다. 세션 시작 시 정본을 다시
  읽어 이번 점검은 정본 기준으로 진행했으나, **앱에 저장된 사본 자체는 여전히 낡아 있다.**
- **근거**: 수집기 §2 "점검 지침서 세대" — `사본 대조: 🔴 사본 드리프트`
  (`docs/정기점검/매일점검/evidence_MW0602-20260902_pre.md` §2)
- **기준 위반**: SKILL.md 서두 경고(2026-08-26 사고 재발방지) — 사본이 낡으면 최신 규약(예:
  락 rename 우회, 병행 세션 확인)을 놓친 채 점검이 진행될 위험.
- **영향**: 사용자가 매번 "아직 안 고쳐졌나" 신경 쓸 필요는 없습니다 — 이번 점검은 이미
  최신 지침으로 진행됐습니다. 다만 이 세션이 최신 지침을 직접 못 읽고 예전 사본만 로드되는
  경우(예: 세션이 정본을 확인하는 절차 자체를 건너뛴 경우)에는 낡은 규칙으로 점검이 진행될
  위험이 남아 있습니다.
- **영향(기술)**: 앱 저장 스킬은 git으로 전파되지 않는다(멀티PC 함정 ③) — `save_skill
  overwrite:true`로 이 PC에서 직접 갱신해야 하며, 세션이 대신할 수 없다.
- **신규 여부**: 기존 TODO — `dev_memory/NEXT_TODO.md` "사용자 몫 · 11회째 이월"
  (512차 후속 등록) → 오늘로 **12회째**.

### 확인 필요 (확정 아님)

- **`logs/20260902_HEALTH.log`가 0바이트다.** 08:55 생성 이후 09:04(점검 시각)까지 내용이
  없다. 전일(`20260901_HEALTH.log`)·전전일(`20260831_HEALTH.log`)은 하루치 누적이 각각
  3.7KB·2.6KB였다 — 다만 그 값도 하루가 끝난 시점 크기라 지금(장 시작 4분 후) 0바이트인
  것 자체가 이상인지, 단순히 이 로그가 하루 늦게 쌓이기 시작하는 것인지는 **오늘 자료만으로
  구분되지 않는다.** 313차 원칙에 따라 확정하지 않는다 — **장중 절에서 09:00~10:00대에
  내용이 쌓이는지 재확인할 것.**

### 이미 반영된 사안 (신규 아님)

- **`[CybosProbe] dispatch/request failed` 10건**(08:58:30·08:59:00, `PROBE.log`) — 이미
  `dev_memory/NEXT_TODO.md`에 **F-3(P2)**로 등록돼 있고, 매일 반복되는 벤나인 패턴으로
  추적 중(투자자 TR 프로브가 실제 데이터 소스가 뜨는 09:20 전까지 실패하는 것은 상시
  조건 — `O-5` 종결 근거). 오늘 새로 등록하지 않는다.
- **PC명 태그 누락 커밋 `f38a3e3`**(`docs: 익절6차 진입포인트...`) — 이미 2026-08-31
  "1-9(P2)"로 등록됐고 `dev_memory/NEXT_TODO.md` "재제안 금지" 목록에 명시돼 있다.
  오늘 재등록하지 않는다.
- **`challenger_shadow` SLOW 100% 고착**(수집기 §12) — 이미 `O-26`(493차)·`F-6`(P2)으로
  등록·추적 중. 참고치 max 412.0ms(승격선 1,000ms의 41.2%).
- **`GuardFair_유효` 무효 100%**·**`전략판정` INSUFFICIENT 100%**·**`sgd_alpha` 0.01000
  고착** — 전부 수집기 §12에서 "🔴 고착 · benign 아님"으로 표시되지만, 각각 457차
  설계(판정 무영향 섀도)·캠페인 표본부족(사전등록 min_samples 미달)·0824 "1-13"으로
  이미 원인이 규명·등록돼 있다. 신규 이상점으로 올리지 않는다.
- **`CORE안전` ✅ 100% 고착**(9거래일/70건) — 과거(468차 F-3 이전) 6거래일 연속 `⚠️`였던
  것과 달리, 지금은 F-3 조치 이후 **정상값에 고착**된 상태다. 고착 자체는 관찰 대상으로
  남아 있으나 현재는 "안전장치가 정상 작동 중"으로 읽는다.

---

## 2. Fix 작업 구현계획

### F-1. VKOSPI 원천 코드 사전점검 — P1 · 대응 이상점 1-1

- **원인 가설**: 증권사 API 코드 `O2901P`가 VKOSPI가 아닌 다른 변동성 계열 지수를
  가리키고 있을 가능성(사전등록 가설, `dev_memory/NEXT_TODO.md` G-2).
- **변경 파일**: 이번 세션은 **코드를 바꾸지 않는다**(장전 원칙). 장후 적용 대상으로 아래를
  제안한다.
  - `collection/`(IndexPoll 관련 모듈, `O2901P` 코드 사용처) — `grep -rn "O2901P"
    collection/`로 실제 호출부를 특정한 뒤, 증권사 문서상 코드 정의와 대조.
  - `strategy/regime` 또는 매크로 수집 모듈 — `vkospi` 필드를 만드는 계산식에 스케일
    오류(예: 다른 지수의 값을 그대로 통과)가 없는지 확인.
- **회귀 위험**: 낮음 — `vkospi`는 `active_features`(105종) 밖이라 이번 조사·수정이
  GBM 예측·앙상블 판단에 영향을 주지 않는다. 코드를 실제로 바꾸게 되면(원천 재지정)
  레짐 판정 로직에 `vkospi`를 직접 참조하는 곳이 있는지 먼저 확인해야 한다
  (`grep -rn "vkospi" strategy/ model/`).
- **절대원칙 저촉 여부**: 없음.
- **런타임 주의**: 데이터 수집 경로 — `py37_32`(라이브 COM) 쪽 코드로 추정, 확인 필요.
- **검증 방법**: 원천 코드를 특정한 뒤 증권사 HTS 화면의 VKOSPI 실값과 1회 수동 대조
  (기존 `C-5` 과제와 동일 — "사용자 대조" 필요 항목으로 `dev_memory/NEXT_TODO.md`에
  이미 있다). 대조 후 코드가 맞으면 "정상 범위를 벗어나는 시장 상황이었다"로 종결,
  틀렸으면 원천 재지정 커밋.
- **섀도 계측 필요 여부**: 이미 `G-2`(26주 WFA 편입 제안, `dev_memory/NEXT_TODO.md`)로
  "관측만 하는 `[SanityRange]` 경고 로그" 형태가 사전등록돼 있다 — 이번 판정으로 그
  제안의 우선순위를 높일 것을 §3에서 제안한다.
- **적용 시점**: 장후 이후 — 라이브 프로세스가 돌고 있다.
- **결정 필요 사항**: 없음(조사·확인 작업이며 매매 판단에 영향 없음).

### 적용 순서와 커밋 계획

1. (장후 이후) `O2901P` 호출부 확인 — 코드 변경 없이 조사만. 결과를 dev_memory에 기록.
2. 원천이 틀렸다고 확인되면 별도 커밋 — 메시지 초안 `[MW0602] 513차 후속: VKOSPI 원천
   코드 재지정 (O-36/C-5 판정 결과)`.

> 장전 국면이므로 위 F-1은 **계획만 수립**했다. 실행하지 않는다.

---

## 3. 고도화 방안

당일 관측(1-1)에서 출발한 것만 쓴다.

### G-1. 외부 지수 입력 사전등록 타당범위 감시 — 우선순위 상향 제안

- **관측 근거**: 오늘 VKOSPI가 5거래일 연속 정상범위(`[8,45]`)를 벗어난 사실이 로그에는
  전혀 경고 없이 지나갔다 — 매분 `[IndexPoll] state=OK`로만 찍혀 정상 신호처럼 보였다.
  이 프로젝트는 "값은 정상적으로 나오는데 실은 계측이 틀렸다"는 유형의 사고
  (FP-CRITICAL PSI 죽은 게이트, TOX 죽은 섀도)를 반복 경험했다 — 이번이 같은 계열의
  다섯 번째 사례가 될 수 있다.
- **제안 내용**: `dev_memory/NEXT_TODO.md`에 이미 등록된 `G-2`(외부 지수 입력 사전등록
  타당범위 — `vkospi`·`spot`·`VIX`·`SP500`·`USD/KRW` 대상, `WARNING [SanityRange] …
  범위 밖 — 원천/코드 확인 요` 로그, 차단 아님·관측 전용)를 **다음 26주 WFA 주기가
  아니라 더 앞당겨** 구현할 것을 제안한다. 오늘 사전등록 판정이 실제로 도래·발동한
  것이 그 필요성의 실측 근거다.
- **기대 효과**: 이런 이탈이 재발하면 "5거래일 뒤에야 사전등록 판정으로 발견"이 아니라
  **당일 WARNING 로그로 즉시 노출**된다.
- **비용·위험**: 낮음 — 관측 전용(차단 신설 아님), 일 1회 요약 로그로 폭주 방지 설계가
  이미 `G-2` 제안에 포함돼 있다.
- **선행 조건**: `C-5`(증권사 화면 1회 대조 — 이번에 P1으로 승격된 항목).
- **우선순위 제안**: 이번 주(장후 이후 적용 가능한 관측 전용 코드이므로 26주 WFA까지
  미룰 이유가 약하다).

### 로드맵·실전전환 기준 반영 제안

없음 — 이번 관측은 실전 전환 기준에 직접 영향이 없다(비CORE 피처).

---

## 4. 진입 승패 사후검증 (장후 한정)

해당 없음(장후 전용) — 장전 절.

## 5. 수익률 향상방안 (장후 한정)

해당 없음(장후 전용) — 장전 절.

---

## 6. 다음 거래일 관측 예정

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| O-65 | `[IndexPoll] … vkospi=` | 9/3 이후에도 정상 범위(`[8,45]`) 유지되는가. 다시 이탈하면 "일시적 시장 변동"이 아니라 "코드가 간헐적으로 다른 지수를 읽는다" 가설로 전환 | 2026-09-03 |

## 7. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| `O-36`/`C-5` VKOSPI 5거래일 판정 | 2026-09-02(오늘) | 0 — 오늘 처리 | 위 1-1·F-1로 처리. `C-5`를 P1으로 승격, `dev_memory/NEXT_TODO.md`에 반영 예정(§8) |
| CB② 복원 (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | 사건 기한(⑧ 해제와 동일 커밋) — 날짜 아님 | — | 489차 D2. 오늘 해당 사건 없음 |

---

## 8. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-09-02 (MW0602 513차 — 일일 점검·장전)`
- `NEXT_TODO.md` 추가 체크박스: `C-5`를 P1으로 승격 표기 + `O-65` 등록. `O-36` 판정 완료로 종결.
- 완료 처리한 기존 항목: `O-36`(판정 완료 — 5거래일 연속 확인, 오늘 정상 복귀. `C-5`로 승계)

---

# 장중(intra)

- 점검 시각: 12:41 KST · PC `MW0602` · 브랜치 `dev`
- HEAD `cac617a` · 미커밋 3건(`dev_memory/DECISION_LOG.md`·`NEXT_TODO.md` append, 신규 리포트 파일 1건 — 전부 장전 세션이 만든 변경) · 당일 커밋 0건
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260902_intra.md`
- 이 세션도 저장소 정본 `.claude/skills/mireuk-daily-check/SKILL.md`(`rev: 2026-08-26e`)를 다시 읽어 따랐다. 수집기 §2 "사본 대조"는 **일치**로 찍혔다 — 이번 실행은 앱 사본도 최신 세대(`rev: 2026-08-26e`)를 로드했다는 뜻이다(장전이 지적한 1-2는 별개 문제 — 지금 "이 세션이 무엇을 로드했는가"가 아니라 "PC 앱 저장소에 낡은 사본이 남아 있는가"를 가리킨다).
- 병행 세션 확인: 오늘(2026-09-02) 커밋 0건, `docs/정기점검/매일점검/` 최근 파일도 장전 산출물(리포트 09:07, 증거 09:04/09:01)뿐 — 이 점검과 겹치는 별도 세션·딥다이브 없음.
- 인덱스락: 실행 시작·렌더 시점 2점 모두 **없음**.

## 0i. 한 줄 결론

오늘 오전 장은 조용히 정상적으로 돌아갔습니다. 프로그램이 매분 해야 할 일을 빠짐없이 다 했고(장 시작 이후 확인 대상 222분 전부 기록 있음), 안전장치가 잘못 작동하거나 규칙을 어기고 매매한 흔적도 없습니다. 다섯 번 진입해 세 번 이기고 두 번 져서 지금까지 약 21만 원 손실 중인데, 이 손실 자체는 정상적인 매매 과정에서 난 것이지 프로그램 오작동이 아닙니다. 장전에 넘어온 문제 두 가지는 아직 사용자 확인이 필요한 상태 그대로이고, 장전에 "좀 더 지켜봐야 안다"고 했던 항목 하나(상태값 기록 파일이 비어 있던 문제)는 오전 중 정상적으로 채워지기 시작해 해소로 정리합니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 조건부 정상 | 기동·접속·레짐판정·CORE피처 정상. VKOSPI 5거래일 이상치 판정일 도래(P1, 1-1) |
| 장중 | 정상 | 매분 루프 100%(222/222분), CB HALT 0건, 전 진입 5건 vwap 통과, 절대원칙 위반 없음. 신규 P0·P1 없음 |
| 장후 | (진행 예정) | |

**절대원칙 준수 여부 (장중 갱신)**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | 해당없음(미도래) | 12:41 시점 — 장후 절에서 최종 판정 |
| ② | Circuit Breaker 5종 | 정상 | HALT 0건(`halted=True` 0건, `logs/20260902_SYSTEM.log`). `[CB] 연속 손절 1회`×2는 카운터 증가일 뿐 — `CB_CONSEC_STOP_LIMIT=9999`(모의투자 한정 예외, CLAUDE.md 절대원칙 §2)라 정지는 정상적으로 발생하지 않음. CB③-P4는 `acc30m` NORMAL→RESTRICTED 전환 10건(10:40:56~12:28:57) — 30m 퇴역에 따른 기존 등록 현상(296·297차)이며 차단은 비활성 상태(`CB3_P4_GRADE_BLOCK_ENABLED=False`, 수집기 §3 일치) |
| ③ | CORE 피처 호라이즌 그룹별 | 정상 | 08:56~08:59 CORE 축퇴(장전과 동일 현상, 개장 시 해소) 이후 전 시간대 정상. **오늘 진입 5건 전부 `[진입체크]` 로그에서 `vwap✅` 확인**(`logs/20260902_TRADE.log` 10:38:57·10:48:57·11:09:58·11:27:58·11:34:58) — vwap 미통과 진입 0건 |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 점검범위 아님 | 정적 코드 규칙 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 점검범위 아님 | 정적 코드 규칙 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 정상 | 관련 로그·흔적 없음 |

**설정 불변식** — 수집기 §3 표 전 20행 `일치`. 차단 게이트 29개 중 7개 꺼짐 — 장전과 동일 구성, 변경 없음.

**오늘 지금까지의 매매 결과(참고 — 확정 아님, 장중 진행분)**

| | |
|---|---|
| 포지션 | 5건 (레그 8건 — 손익은 포지션 단위로 셈) |
| 승패 | 3승 2패 (60%) |
| 손익 | 합계 -3.96pt / gross -198,000원 / 수수료 15,811원 / **net -213,811원** |
| 방향 | 전 진입 SHORT, 호라이즌 전부 3m, Hurst 전부 mean-revert |

---

## 0i-C. 이월 처리 — 앞 국면(장전) 항목 처분

| 원 번호 | 제목(줄임) | 원 국면 | 처분 | 근거 |
|---|---|---|---|---|
| 1-1 | VKOSPI 5거래일 이상치 — 판정 완료, `C-5` P1 승격 | 장전 | 🔄 지속 | 판정 자체는 장전에 종결됐고, 남은 조치(`C-5` 증권사 화면 대조)는 **사용자 몫**이라 장중 세션이 대신 처리할 수 없음. 오늘 12:41까지 `vkospi` 값은 계속 정상범위(수집기 §6 `IndexPoll` 표본 확인) — 이탈 재발 없음 |
| 1-2 | 점검 지침서 앱 사본 드리프트 — 12회째 이월 | 장전 | 🔄 지속 | 앱 저장 스킬은 git으로 전파되지 않아 세션이 대신 갱신 불가(멀티PC 함정 ③). 이번 장중 세션은 정본(rev `2026-08-26e`)을 직접 읽어 진행했으므로 이번 점검 자체의 정확성에는 영향 없음 |
| (번호없음) | `logs/20260902_HEALTH.log` 08:55 생성 후 09:04까지 0바이트 — 이른 시각 특성인지 진짜 결손인지 판정 보류 | 장전 | ✅ 해소 | 09:29:56부터 `[HealthTrend] 세션 지연 기준선 확정: 195ms (표본 20분)`을 시작으로 12:37:58까지 17행이 정상 누적됨(`logs/20260902_HEALTH.log`, 수집기 §4). 09:00~09:29 사이의 공백은 "20분 표본을 모아야 첫 기준선을 확정한다"는 설계상 워밍업 구간이었던 것으로 확인 — 결손이 아니라 정상 지연 시작 |

**미처분 0건.**

---

## 1i. 이상점 정리보고 (장중)

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

없음.

### P1 — 정확성·관측 훼손

신규 없음. (1-1 지속 — 위 이월표 참조)

### P2 — 운영 부담·기술부채

신규 없음. (1-2 지속 — 위 이월표 참조)

### 확인 필요 (확정 아님)

없음 — 장전이 등록한 확인 필요 항목(HEALTH.log)은 위 이월표에서 해소 처리했다.

### 이미 반영된 사안 (신규 아님)

- **사이저 출력(최대 3계약) vs 실제 진입(최대 2계약) 격차** — 오늘 3건 모두 `[SizerMatch] 3→2계약`으로 축소됐고, 축소 원인은 `binding_gate=hurst`(품질게이트, 증거금 아님 — `[MarginCap]` 조회 10건 중 축소 0건). 이 격차 자체는 이미 실전 전환 기준 ⑧의 `sizing_inversion_watch`([28]) 채널이 매주 감시 중인 항목이다(CLAUDE.md). 오늘 관측은 그 채널의 표본을 늘릴 뿐 새 이상점이 아니다.
- **`[WeightCollapse]` 안전망 발동 92건(09:07~12:37, 산발적)** — `active_horizons`에 포함된 3m 모델이 특정 순간 미배포 상태일 때 실질 가중합 0으로 떨어지면 flat_score=1.0으로 안전하게 대체하는 기존 설계다. `dev_memory/NEXT_TODO.md`에 이미 `O-1`·`O-2`·`O-8`·`O-14`·`O-22`·`O-43` 등 다수의 관측 ID로 등록·추적 중인 지표(밴드 20.3%±5%p 기준)라 오늘 새로 등록하지 않는다. 판정은 해당 관측 ID의 예정일에서 한다.
- **CB③-P4 상태 전환(NORMAL→RESTRICTED) 10건** — 절대원칙 §2에 이미 기록된 "30m 퇴역으로 인한 상시 RESTRICTED" 현상과 같은 계열. 차단 자체는 `CB3_P4_GRADE_BLOCK_ENABLED=False`로 비활성이라 오늘 진입에 실질적 영향 없음.
- **PC명 태그 누락 커밋 `f38a3e3`** — 장전과 동일, 재등록하지 않음.

---

## 2i. Fix 작업 구현계획 (장중)

신규 Fix 없음. 장전 F-1(VKOSPI 원천 코드 사전점검, P1)은 **장후 이후 적용 계획 그대로 유지** — 장중에는 코드를 변경하지 않는다(라이브 프로세스 운영 중).

---

## 3i. 고도화 방안 (장중)

신규 없음. 장전 G-1(외부 지수 입력 사전등록 타당범위 감시 우선순위 상향)은 그대로 제안 유지.

---

## 4i. 진입 승패 사후검증 (장후 한정)

해당 없음(장후 전용) — 장중 절.

## 5i. 수익률 향상방안 (장후 한정)

해당 없음(장후 전용) — 장중 절.

---

## 6i. 다음 거래일 관측 예정

신규 없음. 장전 `O-65`(VKOSPI 9/3 이후 정상범위 유지 여부, 판정 예정일 2026-09-03) 그대로 유지.

## 7i. 기한 도래 항목

오늘 신규 기한 도래 없음. CB② 복원(사건 기한, ⑧ 해제와 동일 커밋)은 오늘 해당 사건 없음 — 장전과 동일.

---

## 8i. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-09-02 (MW0602 514차 — 일일 점검·장중)` — 증상(없음, 정상 확인) → 결정(HEALTH.log 확인필요 항목 해소 처리) → 검증(수집기 §4·§7 커버리지 100% 확인)
- `NEXT_TODO.md` 갱신: 없음(신규 관측 등록 없음). 기존 `C-5`·`O-65`·"사용자 몫 · 12회째 이월" 그대로 유지.
- 완료 처리한 항목: 장전 "확인 필요 — HEALTH.log 0바이트" → ✅ 해소 (위 이월표 참조)

---

# 장후(post)

- 점검 시각: 16:20 KST경 · PC `MW0602` · 브랜치 `dev`
- HEAD `cac617a` · 미커밋 3건(`dev_memory/DECISION_LOG.md`·`NEXT_TODO.md` append, 이 리포트 파일 — 전부 장전·장중 세션이 만든 변경, 지금 이 절 추가로 갱신) · 당일 커밋 0건
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260902_post.md`
- 이 세션도 저장소 정본 `.claude/skills/mireuk-daily-check/SKILL.md`(`rev: 2026-08-26e`)를 다시 읽어 따랐다.
- 병행 세션 확인: 오늘 커밋 0건, `docs/정기점검/매일점검/` 오늘자 다른 파일은 전부 이 세션이 이미 확인한 장전/장중 산출물뿐 — 겹치는 별도 세션 없음.
- 인덱스락: 실행 시작·렌더 시점 2점 모두 **없음**(`.git/index.lock` 부재. `.git/objects/maintenance.lock`은 8/14자 무관 파일).

## 0p. 한 줄 결론

오늘 미륵이는 절대원칙을 전부 지키며 하루를 마쳤습니다. 15시 10분 강제청산 시각에는 이미 포지션이 없어서 청산할 것이 없었고(15:11 점검 로그로 확인), 다섯 번의 안전장치(서킷브레이커) 중 어느 것도 잘못 작동하지 않았습니다. 다만 오늘도 손실이 났습니다 — 6번 매매해서 3번 이기고 3번 져서(포지션 기준), 증권사 실제 잔고 기준으로 약 30만 3천 원을 잃었습니다. 이 손실은 프로그램 오작동이 아니라 정상적인 매매 과정에서 난 것입니다(승리 3건 모두 "1차 목표가 도달 후 보호청산", 패배 3건 모두 예측 방향이 빗나간 순수 손절). 장전에 넘어온 시장 변동성 지수(VKOSPI) 조사는 코드상 근거를 추가로 확인했지만 최종 확인(증권사 화면 대조)은 여전히 사용자 몫으로 남아 있습니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 조건부 정상 | 기동·접속·레짐판정·CORE피처 정상. VKOSPI 5거래일 이상치 판정일 도래(P1, 1-1) |
| 장중 | 정상 | 매분 루프 100%(222/222분), CB HALT 0건, 전 진입 5건 vwap 통과, 절대원칙 위반 없음 |
| 장후 | 정상 | 매분 루프 100%(371/371분, 09:00~15:10), 15:10 시점 잔여 포지션 0 확인, EOD·P8 완료, 6포지션 전부 vwap 통과, 신규 P0·P1 없음 |

**절대원칙 준수 여부 (장후 — 종가 기준 최종 판정)**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | 준수 | `logs/20260902_SYSTEM.log` 15:11:26 `[SchedForceExit] 15:11 점검 — status=FLAT engine=0ct broker_cached=0ct bar_pass=1회 → 청산 대상 없음(정상)`. 마지막 포지션은 14:31:02에 이미 청산 완료. `trades.exit_reason='15:10 강제청산'` 0건은 위반이 아니라 "그 시각에 청산할 포지션이 없었다"는 뜻(`evidence_map.md` §8-4) |
| ② | Circuit Breaker 5종 | 준수 | HALT 0건(하루 종일). CB②(9999, 모의투자 한정 예외) 카운터만 증가하고 정지는 정상적으로 미발생. CB③-P4 `NORMAL→RESTRICTED` 전환 22건(수집기 §6 실측, acc30m=10.0%)은 30m 퇴역에 따른 기존 등록 현상(296·297차)이며 차단 자체는 `CB3_P4_GRADE_BLOCK_ENABLED=False`로 비활성(설정 불변식 §3 일치) — 오늘 진입에 영향 없음. CB①④⑤ 오늘 발동 조건 미충족(미발동 자체가 정상) |
| ③ | CORE 피처 호라이즌 그룹별 | 준수 | 오늘 진입 6건 전부 `[진입체크]` 로그에서 `vwap✅` 확인(10:38:57·10:48:57·11:09:58·11:27:58·11:34:58·14:27:57) — vwap 미통과(강제 X 대상) 진입 0건. `chas`(2건)·`ofi`(1건) 미통과는 등급하락 대상 항목이지 강제 X 대상이 아니므로 위반 아님(다만 세 건 모두 최종 등급이 A로 유지된 경로는 §4p CASE-01·05·06에서 관찰만 등록) |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 점검범위 아님(정적 코드 규칙) | 오늘 `0xC0000409`/`STACK_BUFFER`/프로세스 무응답 흔적 없음(수집기 §6 크래시 서명 신규 0건) |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 점검범위 아님(정적 코드 규칙) | — |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 준수 | 관련 로그·흔적 없음 |

**설정 불변식** — 수집기 §3 표 전 20행 `일치`. 차단 게이트 29개 중 7개 꺼짐 — 하루 종일 변경 없음(장전·장중과 동일 구성).

---

## 0p-C. 이월 처리 — 장전·장중 항목 처분

| 원 번호 | 제목(줄임) | 원 국면 | 처분 | 근거 |
|---|---|---|---|---|
| 1-1 | VKOSPI 5거래일 이상치 — `C-5` P1 승격 | 장전 | 🔄 지속(조사 추가 진행) | 사용자 최종 대조(`C-5`)는 여전히 미완료(AI 세션이 대신할 수 없음). 다만 이번 장후 세션이 F-1에서 지시된 `grep -rn "O2901P" collection/`을 실행해 코드 근거를 추가 확보했다 — 아래 §2p F-1 갱신 참조. 오늘 15:40까지 `vkospi` 재이탈 없음(정상범위 유지) |
| 1-2 | 점검 지침서 앱 사본 드리프트 — 12회째 이월 | 장전 | 🔄 지속 → **13회째** | 앱 저장 스킬은 git으로 전파되지 않아 세션이 대신 갱신 불가. 오늘 세 세션(장전·장중·장후) 모두 저장소 정본을 직접 읽어 진행했으므로 오늘 점검 자체의 정확성에는 영향 없음 |
| (번호없음) | HEALTH.log 08:55~09:04 0바이트 | 장전 | ✅ 해소(장중에 기처분) | 09:29:56부터 정상 누적 확인. 장중 이월표에서 이미 처분 완료 — 장후는 재확인만(15:40까지 계속 정상 누적, 수집기 §4) |

**미처분 0건.**

---

## 1p. 이상점 정리보고 (장후)

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

없음.

### P1 — 정확성·관측 훼손

신규 없음. (1-1 지속 — 위 이월표 참조)

### P2 — 운영 부담·기술부채

신규 없음. (1-2 지속 — 위 이월표 참조)

### 확인 필요 (확정 아님)

#### 1-3. 원시 X등급이 A로 상향돼 진입한 1건 — 패배, 표본 1건이라 판정 보류

- **증상**: 오늘 11:09:58 진입(CASE-03, 아래 §4p)은 원래 판정이 X등급(진입 부적격)이었는데, 메타게이트가 이를 A등급으로 올려 실제 진입까지 갔습니다. 이 매매는 손실로 끝났습니다(-2.42pt, -124,950원).
- **근거**: `trades.raw_grade='X', grade='A'`(entry_ts 2026-09-02 11:09:58) / `ensemble_decisions.checklist_reason='Coherence↓'`(같은 시각) / 같은 시각 단일 3분봉 모델(`predictions` horizon=3m)은 상승(+1, 신뢰도 40.3%)을 예측했는데 최종 진입 판단(앙상블)은 하락(-1, 신뢰도 42.1%)으로 반대 방향이었다 — 다른 호라이즌이 최종 방향을 주도한 것으로 추정되나 오늘 로그만으로는 어느 호라이즌이 주도했는지 확정할 수 없다.
- **기준 위반**: 없음 — 절대원칙 위반이 아니다. 원시 등급을 메타게이트가 올려서 진입시키는 것은 기존에 설계된 정상 경로다(오늘 A급(원시C) 5건, A급(원시X) 1건 — CLAUDE.md·수집기 §5에 이미 매일 나타나는 패턴).
- **영향**: 이 매매 하나만 봐서는 "등급을 올려서 진입시킨 것이 나빴다"고 결론 내릴 수 없습니다. 오늘 하루, 이런 경우가 1건뿐이라 통계적으로 아무것도 말할 수 없는 크기입니다.
- **영향(기술)**: 원시 X→A 상향 진입의 승률·손익이 원시 C→A 상향 진입과 다른지는 표본이 훨씬 더(최소 수십 건) 쌓여야 판정 가능(313차 원칙 — 판정 단위는 거래일이지 건수가 아니다. 오늘은 관측 1거래일·1건).
- **신규 여부**: 신규 관찰 등록. 코드 변경·정책 판단 없음 — §6p에 다음 관측 조건만 등록.

### 이미 반영된 사안 (신규 아님)

- **사이저 출력(최대 3계약) vs 실제 진입(최대 2계약) 격차** — 오늘 3건(10:38:57·10:48:57·11:09:58) 모두 `[SizerMatch] sizer=3계약 → actual=2계약(gap=1) | binding=hurst(0.50)`로 축소됐다(`logs/20260902_SIGNAL.log`). 증거금(`[MarginCap]`) 축소는 0건 — 순수 품질게이트(hurst 배수)가 상한이었다. 이미 실전 전환 기준 ⑧의 `sizing_inversion_watch`([28]) 채널이 매주 감시 중인 항목(CLAUDE.md)이라 신규 등록하지 않는다.
- **CB③-P4 `NORMAL→RESTRICTED` 전환 22건** — 절대원칙 §2 한시예외 그대로. 차단은 비활성이라 오늘 진입에 영향 없음.
- **`GuardFair_유효` 무효 100%·`전략판정` INSUFFICIENT 100%** — 457차 설계(판정 무영향 섀도)·캠페인 표본부족으로 이미 규명·등록됨. `retrain_eod_20260902.log`의 `GuardFair` 6건 무효 판정도 같은 계열(현행 모델이 홀드아웃을 이미 학습해 공정 비교 불가 — 판정에는 영향 없음).
- **PC명 태그 누락 커밋 `f38a3e3`** — 장전·장중과 동일, 재등록하지 않음.

---

## 2p. Fix 작업 구현계획 (장후)

### F-1(갱신). VKOSPI 원천 코드 조사 — P1 · 대응 이상점 1-1

- **오늘 실행한 조사**: `grep -rn "O2901P" collection/ strategy/` — 호출부를 특정했다.
  - `collection/cybos/api_connector.py:56~61` — 상수 정의부 주석: *"[260704 감사 P2] VKOSPI(코스피200변동성지수) 코드 — **사용자가** Cybos Plus 클라이언트 종목코드검색("코스피 200 변동성지수")으로 **직접 확인**(2026-07-05)"*. 즉 `C-5`가 요구하는 성격의 확인이 **2026-07-05에 이미 한 차례 있었다는 기록**이 코드 주석에 남아 있다.
  - `api_connector.py:1201~1220` `get_index_price()` — 294차(2026-07-08 전후) 관리자 권한 세션 실측(`probe_cp_market_eye.py`)으로 `CpSysDib.MarketEye`가 `O2901P`를 조회하면 종목명 `"코스피200 변동성"`, 값 `86.54`를 정상 반환함을 재확인. 게다가 이 함수는 **자체 검증 로직**이 있다 — 조회된 종목명에 `name_contains`("변동성")가 없으면 잘못된 코드로 간주해 `None`을 반환한다(1219~1220줄, "틀린 코드가 조용히 엉뚱한 가격을 흘려보내는 사고를 방지").
  - **다만 이 자체 검증이 한 번 실제로 대량 실패한 전례가 있다**: 2026-07-07(302차), `[CybosIndex] 종목명 검증 실패 — code=O2901P`가 하루 393회(거의 매분) 발생. 원인은 코드 오지정이 **아니라** `CpSysDib.MarketEye`가 반환한 한글 종목명이 CP949→Latin-1로 잘못 디코딩되는 인코딩 버그였다(`_fix_mojibake_kr()`로 같은 날 수정, `dev_memory/DECISION_LOG.md` 2026-07-07 302차 후속).
  - **그럼에도 2026-08-25(493차)에는 "`O2901P`가 VKOSPI 정본 코드인지 미확인"이라는 신중한 판단이 다시 등록돼 있다** — 이유는 별개의 웹 검색 결과(대신증권 Q&A 등)에서 "COM API 코드는 `U180`일 수 있다"는 상반된 정보가 나왔기 때문(`dev_memory/DECISION_LOG.md`, 293차 항목). GUI 종목코드검색(HTS 표시용)과 COM API 실제 입력 코드 체계가 다를 가능성이 완전히 배제되지 않았다.
- **결론(오늘 조사 범위)**: 코드가 틀렸다는 증거도, 완전히 옳다는 최종 확증도 없다. **정황상 O2901P가 맞을 가능성이 우세**(사용자 260704 GUI 확인 + 294차 자동 프로브 이름 일치 2건)하지만, 과거 프로젝트 스스로도 이 정황만으로 "확정"하지 않기로 반복 결정했다(293차·493차) — 이번 조사도 그 판단을 뒤집을 새 증거는 아니다.
- **변경 파일**: 이번 세션은 **코드를 바꾸지 않는다**(조사만). 코드 변경은 `C-5` 대조 결과가 나온 뒤에만 진행한다.
- **회귀 위험**: 낮음 — `vkospi`는 `active_features`(105종) 밖.
- **절대원칙 저촉 여부**: 없음.
- **검증 방법**: `C-5`(증권사 HTS 화면 VKOSPI 실값 1회 대조, 사용자 전용) — 대조 시 위 세 근거(260704 GUI·294차 프로브·미확정 판단)를 함께 참고할 것.
- **섀도 계측 필요 여부**: `G-2`(외부 지수 사전등록 타당범위 감시) 그대로 제안 유지.
- **적용 시점**: `C-5` 완료 후.
- **결정 필요 사항**: 없음(조사 결과 기록만, 매매 판단 영향 없음).

### 적용 순서와 커밋 계획

1. 이번 세션: 코드 변경 0건. `dev_memory/DECISION_LOG.md`에 조사 결과 기록.
2. `C-5` 완료(사용자) 후: 코드가 맞으면 "고변동 시장 상황"으로 `C-5` 종결. 틀리면 `[MW0602] NNN차: VKOSPI 원천 코드 재지정` 커밋.

---

## 3p. 고도화 방안 (장후)

신규 없음. 장전 G-1(외부 지수 입력 사전등록 타당범위 감시, `C-5` 선행조건)은 그대로 유지. 아래 §5p R-1·R-2는 오늘 승패 사후검증에서 새로 도출한 별도 제안이다.

---

## 4p. 진입 승패 사후검증 (EOD+P8)

**4p-0. 선행 확인** — EOD(`daily_close`)·P8(`p8_scaler_refit`) 둘 다 완료 확인: `data/session_state.json` `p8_last_success_date=2026-09-02`, `eod_retrain_ok_date=2026-09-02`. 완료 도장 파일도 존재: `data/daily_close_done_20260902.txt`(15:40) · `data/eod_retrain_done_20260902.txt`(15:47, rows=40096 cols=105 horizons_replaced=4/6). DB 접근·수집기 실행 모두 실패 없음.

### 4p-1. 오늘 할일 4분류

| # | NEXT_TODO 항목 | 당일 커밋 | 코드 실재 확인 | 상태 |
|---|---|---|---|---|
| 1 | `O-36` VKOSPI 5거래일 판정 | 없음(조사 세션) | 장전 세션 실행 완료 | ✅완료-커밋확인 아님(예약 실행이라 커밋 자체를 안 함 — 판정 실행 자체는 완료) |
| 2 | `C-5` 증권사 화면 VKOSPI 대조 | 없음 | 사용자 전용 항목 | ⬜미착수 — 사유: AI 세션이 대신할 수 없음(사용자 몫) |
| 3 | `O-65` 9/3 이후 vkospi 정상범위 유지 확인 | 없음 | 판정 예정일 2026-09-03(미도래) | ⬜미착수 — 사유: 판정일 미도래 |
| 4 | 지침서 앱 사본 갱신 | 없음 | 사용자 전용 항목 | ⬜미착수 — 사유: git 미전파, 사용자 몫(13회째) |

⚠완료주장-커밋없음 항목 없음 · 🔄범위변경 항목 없음.

### 4p-2. 3원 대사 (포지션 단위)

| 원천 | 질의 | 값 |
|---|---|---|
| 로그 | `[Position] 진입` (`logs/20260902_TRADE.log`) | **6** |
| `ensemble_decisions` | `entry_executed=1` (ts like '2026-09-02%') | **6** |
| `trades` | `COUNT(DISTINCT entry_ts)` (exit_ts like '2026-09-02%') | **6** |
| (참고) `trades` | `COUNT(*)` — 청산 레그 | **9**(레그 3건은 TP1/손절1차 부분청산으로 2행씩) |

**6 = 6 = 6 — 일치.** 레그 9는 불일치가 아니다(§4-2·`evidence_map.md` §8-5).

### 4p-3. 케이스 카드 (포지션 6건)

```
[CASE-01] 진입 10:38:57 SHORT · 2계약 @1039.84 (등급 A/원시 C · horizon=3m · hurst=mean-revert)
          → 10:39:16 부분청산 1계약 손절1차 조기축소 (-1.16pt/-59,976원)
          → 10:40:18 최종청산 1계약 하드스톱(틱)[TP1미도달] (-2.26pt/-114,976원)
          포지션 합 -3.42pt / -174,952원 · 보유 1분21초
 진입근거   [진입체크] sign✅ conf✅ vwap✅ cvd✅ ofi✅ fore✅ prev✅ time✅ risk✅ chas❌ coun✅ | conf=54.0%
            [Sizer] 산출 3계약 → [SizerMatch] binding=hurst(0.50)로 2계약 축소(gap=1)
 예측 vs 실현  ensemble conf=0.5403(하락 방향) / 3m 단일모델 direction=-1(하락) conf=0.4629,
            실현 actual=상승(1) correct=0 — 예측 실패
 승패판정   ❌패 — `하드스톱(틱)[TP1미도달]`, tp1_reached=0/0 (레그 둘 다 TP1 미도달)
 주 요인    A(방향예측 실패)
 부 요인    E(최종 레그 실현손실 2.26pt가 설계 손절폭 2.19pt(=1042.03-1039.84)를 약 0.07pt
            초과 — 소폭 슬리피지, 417차 재분해가 지적한 "손절 준수율" 축과 같은 방향이나
            표본 1건으로는 판정 불가)
 반사실     chas❌ 미통과 항목이 없었다면 등급이 더 높았을 뿐 방향 자체는 바뀌지 않았을 것

[CASE-02] 진입 10:48:57 SHORT · 2계약 @1040.20 (등급 A/원시 C · horizon=3m · hurst=mean-revert)
          → 10:50:13 부분청산 1계약 TP1 부분청산 33% (+0.79pt/+37,524원)
          → 10:50:58 최종청산 1계약 하드스톱[TP1보호] (+0.51pt/+23,524원)
          포지션 합 +1.30pt / +61,048원 · 보유 2분1초
 진입근거   [진입체크] 전 항목✅(chas✅ 포함) | conf=37.2%
            [Sizer] 산출 3계약 → [SizerMatch] binding=hurst(0.50)로 2계약 축소(gap=1)
 예측 vs 실현  ensemble conf=0.3723 / 3m 단일모델 direction=-1 conf=0.4012, actual=0(하락 미달),
            correct=0 — 다만 3분봉 채점 시계(3분 뒤 종가 기준)와 실제 청산 시점(약 2분,
            TP1 보호전환)이 달라 이 불일치가 매매 실패를 뜻하지 않는다
 승패판정   ✅승 — `하드스톱[TP1보호]`, tp1_reached=0/1(최종 레그 TP1 보호청산 확인)
 주 요인    C(청산 규칙 — TP1 도달 후 보호전환이 2분 내 걸려 이익 확정)
 부 요인    ?(방향 채점 자체는 불일치라 A로 단정하지 않음)
 반사실     보호전환 오프셋이 더 넓었다면 이익이 더 컸을 수 있으나 그만큼 반대방향 리스크도
            커진다 — [25] 채널 관할, 여기선 관찰만

[CASE-03] 진입 11:09:58 SHORT · 2계약 @1039.92 (등급 A/원시 X · horizon=3m · hurst=mean-revert)
          → 11:10:31 부분청산 1계약 손절1차 조기축소 (-1.08pt/-55,975원)
          → 11:11:23 최종청산 1계약 하드스톱(틱)[TP1미도달] (-1.34pt/-68,975원)
          포지션 합 -2.42pt / -124,950원 · 보유 1분25초
 진입근거   [진입체크] 전 항목✅ | conf=42.1%. checklist_reason='Coherence↓'(원시 X → 메타게이트가
            A로 상향, 위 1-3 참조)
            [Sizer] 산출 3계약 → [SizerMatch] binding=hurst(0.50)로 2계약 축소(gap=1)
 예측 vs 실현  ensemble conf=0.4214(하락 방향, 최종 진입방향) / 3m 단일모델 direction=+1(상승)
            conf=0.4028, actual=상승(1), correct=1 — **단일 3m 모델은 방향을 맞혔는데
            최종 앙상블 판단은 반대(하락)로 진입해 패배**. 다른 호라이즌이 최종 방향을
            주도한 것으로 추정되나 오늘 로그만으로 어느 호라이즌인지 확정 불가
 승패판정   ❌패 — `하드스톱(틱)[TP1미도달]`, tp1_reached=0/0
 주 요인    A(방향예측 실패, 앙상블·단일3m 불일치)
 부 요인    D(원시 X→A 상향 경로 — §1-3에서 관찰 등록만, 오늘 표본으론 인과 판단 불가)
 반사실     3m 단일모델 방향을 그대로 따랐다면(즉 LONG) 이 케이스는 이겼을 것 — 다만 이는
            사후 단일 케이스 비교이며 앙상블 가중치 설계 변경 근거로 쓸 수 없다(313차)

[CASE-04] 진입 11:27:58 SHORT · 1계약 @1041.74 (등급 A/원시 C · horizon=3m · hurst=mean-revert)
          → 11:29:50 최종청산 1계약 하드스톱(틱)[TP1보호] (+0.28pt/+12,020원)
          포지션 합 +0.28pt / +12,020원 · 보유 1분52초
 진입근거   [진입체크] 전 항목✅ | conf=38.4%
            [Sizer] 최소수량 1계약(ConfShadow 참고치 1.0→2계약, 실제 산출은 1) — MarginCap·
            SizerMatch 관여 없음
 예측 vs 실현  ensemble conf=0.3837 / 3m 단일모델 direction=-1 conf=0.3895, actual=하락(-1),
            correct=1 — 방향 적중
 승패판정   ✅승 — `하드스톱(틱)[TP1보호]`, tp1_reached=1
 주 요인    A(방향예측 적중)
 부 요인    C(TP1 보호전환으로 빠르게 확정)
 반사실     해당 없음(정상 승리 케이스)

[CASE-05] 진입 11:34:58 SHORT · 1계약 @1040.40 (등급 A/원시 C · horizon=3m · hurst=mean-revert)
          → 11:39:37 최종청산 1계약 하드스톱(틱)[TP1보호] (+0.30pt/+13,023원)
          포지션 합 +0.30pt / +13,023원 · 보유 4분39초
 진입근거   [진입체크] chas❌(추격진입 방지 미통과, 나머지 전부✅) | conf=39.8%
            [Sizer] 최소수량 1계약(ConfShadow 참고치 1.0→2계약)
 예측 vs 실현  ensemble conf=0.3975 / 3m 단일모델 채점 행 없음(11:34 3m 예측이 DB에서 확인되지
            않음 — 원인 미상, 표본 1건이라 관측 훼손으로 단정하지 않음)
 승패판정   ✅승 — `하드스톱(틱)[TP1보호]`, tp1_reached=1
 주 요인    C(TP1 보호전환)
 부 요인    ?(예측 채점 데이터 없어 A 판정 불가)
 반사실     해당 없음

[CASE-06] 진입 14:27:57 SHORT · 1계약 @1038.80 (등급 A/원시 C · horizon=3m · hurst=mean-revert)
          → 14:31:02 최종청산 1계약 하드스톱(틱)[TP1미도달] (-1.74pt/-88,974원)
          포지션 합 -1.74pt / -88,974원 · 보유 3분5초
 진입근거   [진입체크] ofi❌(단기 CORE 항목 미통과, 나머지 전부✅ — 등급하락 대상이나 강제 X
            대상 아님이라 위반 아님) | conf=46.0%
            [Sizer] 최소수량 1계약(ConfShadow 참고치 1.5→2계약)
 예측 vs 실현  ensemble conf=0.4600 / 3m 단일모델 direction=-1 conf=0.4886, actual=상승(1),
            correct=0 — 예측 실패
 승패판정   ❌패 — `하드스톱(틱)[TP1미도달]`, tp1_reached=0
 주 요인    A(방향예측 실패)
 부 요인    E(CORE 항목 하나(ofi) 결손 상태로 진입 — 절대원칙 위반은 아니나 품질 저하 신호,
            표본 1건이라 인과 단정 불가)
 반사실     해당 없음
```

### 4p-4. 요인 집계

| 요인 | 승 | 패 | 순손익 | 평균 보유(분) | 관측 거래일 수 |
|---|---|---|---|---|---|
| A(방향예측) 주요인 | 1(CASE-04) | 3(CASE-01·03·06) | -376,856원 | 약 2분 | 1 |
| C(청산규칙-TP1보호) 주요인 | 2(CASE-02·05) | 0 | +74,071원 | 약 4분 | 1 |
| **합계** | **3** | **3** | **-302,785원** | — | **1** |

(net -302,785원은 엔진 계산치. 브로커 실측 net은 -302,789원 — 4원 이내 차이는 반올림 수준, §9p-1 참조)

### 4p-5. 313차 가드 통과 여부

| 갈래 | 적용 결과 |
|---|---|
| ① 일자단위 판정 | 관측일 **1일** — 판정 보류. 오늘 하나의 요인 집계로 정책을 바꾸지 않는다 |
| ② overlap 보정 | 6개 포지션이 순차 진입·청산(겹치지 않음) — overlap 자체는 문제 없으나 관측일 1일이라 결론은 여전히 유보 |
| ③ 이상치 분해(drop-worst) | 최대 손실 CASE-01(-174,952원) 제외해도 순손익 -127,833원(여전히 적자) — 특정 1건이 결과를 좌우하지 않음, 손실이 고르게 분산 |
| ④ 사전등록 — 기준 불변 | 오늘 판정은 사전등록된 캠페인 채널 판정이 아니라 즉석 관찰이다. 이 관찰로 어떤 사전등록 기준도 바꾸지 않는다 |
| ⑤ 부호 일관성(기간·PC 교차) | 오늘 하루뿐이라 교차검증 불가 — 판정 보류 |

**결론**: 오늘의 요인 집계(A=1승3패, C=2승0패)는 **관찰**이지 결론이 아니다. 313차 원칙에 따라 확정 결론을 내지 않는다.

---

## 5p. 수익률 향상방안 (장후)

⚠ 쓰기 전 관문 확인 — `config/settings.py:VALIDATION_CAMPAIGN_DECISIONS`와 대조한 결과, 아래 두 제안과 동일한 질문을 다루는 기존 확정 채널은 없다(스톱 준수율은 417차·489차 D4가 **분석**은 했으나 **매 청산 시 자동 기록하는 계측**은 없음 — `grep -rn "손절준수율|stop_adherence" config/ strategy/ main.py` 0건 확인). 원시등급 상향 추적도 동일하게 기존 채널 없음.

### R-1. 하드스톱 청산 시 "손절 준수율"을 매번 로그·DB에 자동 기록

| 칸 | 내용 |
|---|---|
| 근거 | CASE-01(E), CASE-06(E) — 오늘 손절 3건 중 2건에서 실현손실과 설계 손절폭 간 관계를 수기로 계산해야 했다(사후검증 세션이 entry_price·손절가·pnl_pts를 직접 대조) |
| 변경 대상 | 하드스톱 청산 처리 경로(`main.py`의 청산 트리거, 정확한 함수명은 다음 조사 필요) — 청산 로그에 `stop_adherence_pt = abs(실현손실pt) / abs(진입가-손절가)` 한 줄을 계산해 함께 기록. DB(`trades`)에도 컬럼 추가 없이 기존 `exit_reason` 로그 문자열에 값만 덧붙이는 경량 방식부터 검토 |
| 기대효과 | 정성적 — 417차·489차 D4가 매번 수작업으로 재계산하던 지표를 자동화. 손익 자체를 바꾸지 않으므로 당일 표본 기준 손익 변화는 0원(계측 전용) |
| 회귀 위험 | 낮음 — 로그·표시 전용, 매매 판단 경로에 영향 없음(계측 4원칙 위반 여부만 주의 — 폴백 시 `*_measured` 동반) |
| 검증 | 배포 후 5거래일 섀도 관찰 — 값이 기존 수작업 계산과 일치하는지 대조 |
| 표본 상태 | n=2(CASE-01·06), 관측일=1일 — 313차 가드 미통과(관찰 등록 단계, 정책 근거 아님) |

### R-2. 원시 X→A 상향 진입을 별도로 추적하는 섀도 채널 등록

| 칸 | 내용 |
|---|---|
| 근거 | CASE-03(D) — 원시 X등급이 메타게이트로 A까지 상향돼 진입했고 패배했다(위 1-3) |
| 변경 대상 | 신규 계측 채널만(코드 변경 없음) — `dev_memory/NEXT_TODO.md`에 관찰 항목으로 등록해 향후 원시 X→A 상향 진입이 누적될 때마다 승패를 집계 |
| 기대효과 | 정성적 — 표본이 20건(313차 min_samples 관례)에 도달하면 "원시 X→A 상향 진입이 원시 C→A 상향 진입보다 승률이 낮은가"를 처음으로 판정 가능해진다. 지금은 판정 불가 |
| 회귀 위험 | 없음(관찰 전용, 차단 로직 아님) |
| 검증 | 섀도 관찰 — 20건 도달 시 313차 5갈래 적용해 판정 |
| 표본 상태 | n=1(CASE-03), 관측일=1일 — 313차 가드 미통과(관찰 등록만, ETA는 현재 표본 속도로 추정 불가) |

---

## 6p. 다음 거래일 관측 예정

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| O-65 (장전 등록, 유지) | `[IndexPoll] … vkospi=` | 9/3 이후에도 정상 범위(`[8,45]`) 유지되는가 | 2026-09-03 |
| O-66(신규) | `raw_grade='X'` → `grade='A'` 상향 진입 승패 (§1-3·R-2) | 누적 20건 도달 시 원시 C→A 상향군과 승률·손익 비교 | 표본 속도 미상 — 매 장후 점검에서 누적 건수만 추적 |

## 7p. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| `O-36`/`C-5` VKOSPI 5거래일 판정 | 2026-09-02(오늘) | 0 — 오늘 처리 완료 | 판정은 장전에 종결. `C-5`(사용자 대조)만 계속 이월 |
| CB② 복원 (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | 사건 기한(⑧ 해제와 동일 커밋) — 날짜 아님 | — | 489차 D2. 오늘 해당 사건 없음 |

---

## 8p. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-09-02 (MW0602 515차 — 일일 점검: 장후 종합)`
- `NEXT_TODO.md` 추가 체크박스: `O-66`(신규, 원시X→A 상향 진입 추적) 등록. `C-5`·`O-65`·"사용자 몫 · 13회째 이월" 유지.
- 완료 처리한 기존 항목: `O-36`(장전에 이미 종결). F-1(VKOSPI grep 조사)은 오늘 실행 완료 — `C-5`(사용자 대조)만 잔존.

---

## 9p. 종합 — 오늘 하루 (2026-09-02, MW0602)

### 9p-1. 종합 판정

| 국면 | 판정 | 한 줄 근거 |
|---|---|---|
| 장전 | 조건부 정상 | 기동·접속·CORE피처 정상. VKOSPI 5거래일 이상치 판정일 도래(P1) |
| 장중 | 정상 | 매분 루프 100%, CB HALT 0건, 절대원칙 위반 없음 |
| 장후 | 정상 | 15:10 강제청산 시점 잔여 포지션 0 확인, EOD·P8 완료, 절대원칙 위반 없음 |
| **당일 최종** | **정상(조건부 항목 1건 사용자 확인 대기)** | 오늘 미륵이는 절대원칙 6종을 모두 지키며 하루를 마쳤다. 손실이 났으나(포지션 단위 -302,789원) 원인은 정상적인 예측 오류·손절이지 오작동이 아니다. 유일한 미해결 사항은 VKOSPI 원천 코드에 대한 사용자 최종 확인(`C-5`)이며, 이는 매매 손익에 영향을 준 적이 없다 |

**당일 최종 손익(종가 기준)**: 6포지션 3승3패(50%) · **-5.70pt** · net **-302,789원**(브로커 실측 기준, `daily_broker_pnl.broker_net_krw` — gross -285,000원 · 수수료 17,785원. 엔진 계산치 -302,785원과 4원 이내 오차, 유의미한 괴리 아님) — **포지션 단위**(레그 기준으로 세면 9건이나 승패 집계는 포지션 기준). `utils/db_utils.py:fetch_daily_net_for_verdict()`가 이 브로커 우선 조회를 제공한다(코드 존재 확인 완료, CLAUDE.md 493차 F-1 규약과 일치).

**절대원칙 6종 최종 판정**: ① 준수(강제청산 시점 잔여 0) · ② 준수(HALT 0건) · ③ 준수(vwap 미통과 진입 0건) · ④ 점검범위 아님(크래시 흔적 없음) · ⑤ 점검범위 아님 · ⑥ 준수(흔적 없음)

### 9p-2. 당일 이상점 통합 대장

| 번호 | 제목 | 최초 국면 | 심각도 | 최종 상태 |
|---|---|---|---|---|
| 1-1 | VKOSPI 5거래일 연속 비정상 고값 | 장전 | P1 | 지속 → `C-5`(사용자 대조)로 다음 거래일 이월. 오늘 F-1 조사로 코드 근거는 보강됨 |
| 1-2 | 점검 지침서 앱 사본 드리프트 | 장전 | P2 | 지속(13회째 이월) — 사용자 몫 |
| (번호없음) | HEALTH.log 0바이트(08:55~09:04) | 장전 | 확인필요 | 해소(장중에 정상 누적 확인) |
| 1-3 | 원시X→A 상향 진입 1건(패배) | 장후 | 확인필요(관찰 등록) | 신규 관찰 등록 → `O-66`으로 다음 거래일 이월(표본 축적) |

- P0 0건 · P1 1건 · P2 1건 (당일 누계, 확인필요 2건 별도)

### 9p-3. 사용자 조치 — 오늘 할 일

| # | 할 일 | 명령·경로 | 기한 |
|---|---|---|---|
| 1 | 증권사(대신증권 Cybos Plus) HTS 화면에서 VKOSPI(코스피200 변동성지수) 실제 값을 열어, 오늘 프로그램이 읽은 값(42.92~44.03, 정상 복귀)과 지난 5거래일(8/26~9/1, 45~56.29대) 값이 실제 시장 상황과 맞는지 1회 대조. `collection/cybos/api_connector.py:56~61`에 2026-07-05 사용자가 같은 확인을 이미 한 기록이 있으니 그 결과와 오늘 확인이 같은지도 함께 봐주시면 좋습니다. 다르면 코드가 잘못된 지수를 읽고 있다는 뜻이니 재지정이 필요합니다. | `C-5` (`dev_memory/NEXT_TODO.md`) | 정해진 기한 없음(장기 이월 12→13회째) — 빠를수록 좋음 |
| 2 | 점검 지침서 스킬 앱 사본을 최신 버전(rev 2026-08-26e)으로 갱신 | Cowork에서 `save_skill overwrite:true` 실행 (git으로 자동 전파 안 됨) | 정해진 기한 없음 — 13회째 이월 중 |
| 3 | (참고, 조치 불필요) 오늘 손실 -302,789원은 정상적인 매매 오차이며 시스템 이상이 아닙니다. 다만 최근 며칠째 이어지는 손실 추세가 있다면 실전 전환 판단 시 함께 고려해 주세요(CLAUDE.md 실전 전환 기준 ①·④가 이미 미충족 상태로 등록돼 있습니다) | 참고용 — CLAUDE.md 실전 전환 기준 절 | — |

**커밋 대기 목록(이 세션은 커밋하지 않음)**:
- `docs/정기점검/매일점검/MW0602-20260902-점검리포트.md` (append — 장후 절 추가, 이번 갱신)
- `dev_memory/DECISION_LOG.md` (append)
- `dev_memory/NEXT_TODO.md` (append)
- 제외(gitignore): `evidence_MW0602-20260902_pre.md`, `evidence_MW0602-20260902_pre_0901.md`, `evidence_MW0602-20260902_intra.md`, `evidence_MW0602-20260902_post.md`

---

## 장후 자동조치 구현 결과 (17:40 · 자동)

> `mireuk-postmarket-autofix` 예약 작업이 자동 실행한 결과다. 위 본문은 고치지 않았다(append 전용).
> 실행 가드 통과: 장후 절 존재 · 15:35 이후(17:25 착수) · `main.py` 프로세스 부재 · 브랜치 `dev` ·
> `.git/index.lock` 부재 · 오늘 커밋 0건(병행 세션 없음) · 상태파일 날짜 `2026-09-01`(오늘 미처리).

### 1. 결론 — 코드 변경 0건

오늘 리포트가 지시한 코드 작업이 **애초에 없었다**. 꾸며낸 말이 아니라 리포트 자신이 그렇게 적었다 —
`§2p 적용 순서와 커밋 계획` 1번이 *"이번 세션: 코드 변경 0건"*, `§3p 고도화 방안`이 *"신규 없음"* 이다.
그래서 이 자동조치는 **① 리포트가 넘긴 문서 3건을 커밋·푸시**하고, **② 확인 과정에서 나온 새 사실
1건을 기록**하는 것으로 끝냈다. 이틀 연속 코드 변경 0건이지만, 이것은 게으름이 아니라
리포트가 "오늘은 고칠 게 없다"고 판정한 결과다.

### 2. 항목별 처분

| 출처 | 항목 | 등급 | 처분 | 왜 |
|---|---|---|---|---|
| `§9p-3` 1번 | VKOSPI 값 증권사 화면 대조 (`C-5`) | — | **사용자 몫** | 증권사 HTS 화면을 사람이 눈으로 봐야 한다. AI 세션이 대신할 수 없다 |
| `§9p-3` 2번 | 점검 지침서 앱 사본 갱신 (rev `2026-08-26e`) | — | **사용자 몫** | Cowork 앱 저장 스킬은 git으로 전파되지 않는다. 14회째 이월 |
| `§9p-3` 3번 | (참고) 손실 추세 인지 | — | 조치 불필요 | 리포트가 "조치 불필요"로 명시 |
| `§2p` | `F-1` VKOSPI 원천 코드 조사 | — | **이미 완료** | 장후 세션이 `grep -rn "O2901P"` 를 이미 실행하고 결과까지 기록했다. 남은 것은 `C-5`(사용자)뿐 |
| `§3p` / 장전 `G-1` | 외부 지수 타당범위 감시 `[SanityRange]` | A(계측) | **보류** | 리포트가 **선행 조건을 `C-5`로 명시**했고 그 `C-5`가 미완료다. 코드상 미구현은 확인함(`grep -rn "SanityRange" --include=*.py` **0건**) — `C-5`가 끝나면 바로 착수 가능 |
| `§5p` | `R-1` 손절 준수율 자동 기록 | **C** | **구현 금지 · 계획만 등록** | 두 가지가 동시에 걸린다 — ① 변경 대상이 **하드스톱 청산 처리 경로**(주문·청산 실행 경로는 자동조치 범위 밖) ② 표본 `n=2`·관측 1거래일로 **313차 가드 미통과**(리포트 자신이 그렇게 적었다) |
| `§5p` | `R-2` 원시 X→A 상향 진입 추적 | — | **이미 완료** | 리포트가 정한 조치 자체가 "`NEXT_TODO.md`에 관찰 등록"인데, 장후 세션이 이미 `O-66`으로 등록했다 |

### 3. 확인 과정에서 나온 새 사실 — `R-1`은 청산 경로를 건드리지 않고도 오늘부터 잴 수 있다

**왜 이걸 따로 적나.** 이 프로젝트에서 가장 자주 반복된 사고가 *"이미 코드에 있는 것을 또 만드는 것"*
이라, 자동조치는 구현 전에 반드시 실물을 검색해 확인하게 돼 있다. 그 확인에서 나온 결과다.

`§5p` 서두는 `grep -rn "손절준수율|stop_adherence" config/ strategy/ main.py` 가 0건이라 기존 계측이
없다고 적었다. **탐색 범위가 좁았다.** 범위를 저장소 전체로 넓히면 이미 있는 것이 셋이다:

- `scripts/stop_overshoot_shadow.py` (470차) — 같은 축의 **반대편**(손절이 너무 타이트해 노이즈에 걸렸나) 섀도 채널
- `trades.exit_trigger` / `exit_outcome` (468차 G-3) — 신설 사유 세 번째가 문자 그대로 *"손절 준수율을 라벨만으로 계산 불가"* 였다. **이 컬럼들이 R-1을 위해 만들어졌다**
- `loss_tier1_qty1_shadow` — 포지션마다 `entry_ts` · `entry_price` · **`stop_price`(당시 최종 손절가)** 를 이미 적재 중(누적 109행, 오늘 5행)

즉 R-1이 필요로 하는 두 입력(진입가·의도 손절가)이 **이미 DB에 있다.** 오늘 실측으로 확인했다 —
`trades` 를 `entry_ts` 로 조인해 포지션 단위(계약수 가중)로 계산하면 오늘 손실 3건이 바로 나온다:

| 포지션 | 의도 손절폭 | 실현손실(포지션 가중) | 손절 준수율 |
|---|---|---|---|
| 10:38:57 (CASE-01) | 2.1875pt | -1.710pt | **0.782** |
| 11:09:58 (CASE-03) | 1.4407pt | -1.210pt | **0.840** |
| 14:27:58 (CASE-06) | 1.6593pt | -1.740pt | **1.049** (손절폭 초과) |

**이게 왜 중요한가.** R-1 원안은 매매를 실제로 집행하는 청산 코드를 고치는 안이라 위험하고
(자동조치 범위 밖이며 승인이 필요하다), 오늘 표본으로는 판정도 못 한다. 그런데 **읽기 전용
스크립트 한 개**면 같은 값을 과거치까지 소급해 얻을 수 있다 — 집행 코드는 한 줄도 건드리지 않는다.
`R-1`을 다시 검토할 때 원안부터 짜지 말고 이 경로를 먼저 볼 것. `dev_memory/NEXT_TODO.md`
`R-1` 항목에 이 내용을 덧붙여 두었다.

⚠ 위 세 값은 **오늘 하루·3건**이다. 313차 원칙상 이것으로 어떤 결론도 내지 않는다 — 계산이
가능하다는 사실의 증명일 뿐이다.

**안 하면**: 다음에 누군가 R-1을 집으면 `main.py` 청산 경로부터 열게 되고, 그건 승인 대기에
걸려 또 이월된다. 이미 있는 데이터로 오늘 잴 수 있다는 사실이 기록돼 있지 않으면 계속 그렇게 된다.
**걸리는 시간**: 읽기 전용 스크립트 1개 기준 30분~1시간(집행 코드 무변경).

### 4. 테스트

추가 0건 · 실행 0건. **코드를 바꾸지 않았으므로 회귀 대상이 없다.** 변경한 3개 파일은 전부 문서이고,
그중 둘(`DECISION_LOG.md`·`NEXT_TODO.md`)은 append 전용 기록 파일이다.

### 5. 커밋

- 커밋: `e1629e3` — `[MW0602] 515차 후속: 장후 자동조치 — 코드변경 0건 · R-1 기존 데이터로 계산 가능 확인`
- 푸시: `origin/dev` 성공
- 커밋한 파일 3건: 이 리포트 · `dev_memory/DECISION_LOG.md` · `dev_memory/NEXT_TODO.md`
- 커밋하지 않은 것: `evidence_MW0602-20260902_{pre,pre_0901,intra,post}.md` — `.gitignore:117`이
  의도적으로 제외하는 경로다. `git add -f` 로 우회하지 않았다(어제 `O-64`로 규칙화한 그대로).

### 6. 사용자 몫으로 남은 손 작업

1. **VKOSPI 값 대조(`C-5`)** — 대신증권 Cybos Plus 화면에서 코스피200 변동성지수 실값을 한 번 확인.
   프로그램이 읽은 값(오늘 42.92~44.03, 지난 5거래일 45~56.29)과 맞는지만 보면 된다.
   *안 하면*: 이 값이 맞는지 아무도 모르는 상태가 계속되고, 그것에 매달린 `G-1`(범위 이탈 자동 경고)
   도 계속 착수 못 한다. *걸리는 시간*: 2~3분.
2. **점검 지침서 앱 사본 갱신** — Cowork에서 `save_skill overwrite:true` 실행(rev `2026-08-26e`).
   14회째 이월. *안 하면*: 앱 사본으로 점검을 돌리는 세션이 낡은 절차를 따를 수 있다.
   오늘까지는 세 세션 모두 저장소 정본을 직접 읽어 진행해 실피해는 없었다. *걸리는 시간*: 1분.
