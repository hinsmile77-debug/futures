# 미륵이 일일 점검 — MW0602 / 2026-08-19 / 장전(pre)

- 점검 시각: 09:07 KST · PC `MW0602` · 브랜치 `dev` (운영 브랜치 일치 ✅)
- HEAD `32886e7` · 미커밋 7건 · 당일 커밋 0건 · `origin/dev` 대비 ahead/behind 0
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260819_pre.md` (54.6KB)
- 수집기 실행: **부분 실패** — stderr 아래 §0-1
- 세션 차수: **476차** (직전 475차 후속3 = 2026-08-18)

> 같은 날 장중·장후를 돌리면 이 파일에 `## 장중(intra)` / `## 장후(post)` 절을
> **append** 한다. 덮어쓰지 않는다.

---

## 0-1. 수집기 실행 실패 — 먼저 적는다

```
[collect_evidence] 설정 덮어씀: config/dailycheck_targets.json
[collect_evidence] DB 질의 실패(data/db/predictions.db): disk I/O error
[collect_evidence] 저장: docs/정기점검/매일점검/evidence_MW0602-20260819_pre.md (54.6KB)
```

로그 축(§1~§11)은 전부 정상 수집됐고 **DB 축만 실패**했다. 그 결과 §12
`binding_gate` 가 `🔴 무기록 / 관측일 0 / 표본 0` 으로 렌더됐는데 **이는 사실이
아니다** — 아래 P1-2 참조. 이 리포트에서 `binding_gate` 관련 판정은 전부 보류한다.

---

## 0. 한 줄 결론

**오늘 거래할 자격은 있다 — 조건부 정상.** 기동·모델 적재·매크로 수집·레짐 확정·
Circuit Breaker·설정 불변식 22행 전부 정상이고 절대원칙 위반은 0건이다. 다만
468차 G-1(모델 레이블 **상태** 판정)이 실제 EOD 경로에서 **구조적으로 해제될 수 없는
상태**임이 2거래일 연속 확인됐다 — 오늘 거래에는 위험이 없으나(보수 폴백으로 안전한
쪽으로 틀린다) **실전 전환 기준 ⑧의 선행조건이 열리지 않는다.**

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | **조건부 정상** | A-0~A-4 전 항목 통과. G-1 미해제(P1-1) · 수집기 DB축 실패(P1-2) 2건이 조건 |
| 장중 | 해당 없음 | 09:07 시점 — 개장 7분. 진입 0건은 정상 |
| 장후 | 해당 없음 | 미도래 |

**절대원칙 준수 여부**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | **미도래** | 장전. 단 전일 이월 포지션 없음 확인 — `08:41:08 [Position] 저장 상태가 어제 데이터 — 무시` / `포지션=FLAT` (`20260819_TRADE.log`, `20260819_SYSTEM.log`) |
| ② | Circuit Breaker 5종 | **정상** | 발동 0건. §12 `CB_state` = `NORMAL` 매분 샘플(8거래일 2,593건, 관측률 1.02). CB② 는 `9999`로 사실상 비활성 — **발동 안 한 것이 정상** |
| ③ | CORE 피처 호라이즌 그룹별 | **확인 필요** | 위반 아님. 단 단기그룹 CORE `ofi_norm` 이 장전 내내 `identity(0,1)` 강제 상태 — 아래 "확인 필요" 절 |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | **정상** | `0xC0000409` / `STACK_BUFFER` / 무응답 흔적 0건. `08:40:41 [FaultHandler] 활성화 \| file=logs\crash_fault.log PID=14820 \| 행감지=30s` |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | **정상** | 관련 예외 0건. `[CybosProbe]` 초기 실패 10건은 `CoInitialize 미호출`·`잘못된 클래스 문자열` 로 파라미터 축과 무관하며 09:04부터 `ok status=0` 로 복구 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | **정상** | 당일 알파봇 활동 로그 0건 |

**설정 불변식** — 수집기 §3 **22행 전부 `일치` 또는 의도된 `값 확인`. `불일치`·`미발견` 0건.**
차단 게이트 전수 29개 중 7개 `False` 는 전부 `기록됨`/`기능토글` 로 분류돼 있어 신규 적신호 없음.

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**없음.**

### P1 — 정확성·관측 훼손

#### 1-1. G-1(`MODEL_LABEL_STATE_UNLOCK_ENABLED`)이 EOD 경로에서 구조적으로 해제 불가 — 사이드카 `label_scheme=null`

- **증상**: 장전 상태 판정이 `불일치 6/6` 으로 떨어져 사이즈 제한이 유지되고, 곧바로
  F-1(전일 EOD 마커) 폴백이 해제한다. **해제된 것은 F-1이지 G-1이 아니다.**

- **근거 ①** — 오늘 장전 (`logs/20260819_SYSTEM.log:149-150`)
  ```
  2026-08-19 08:55:16 [INFO] SYSTEM: [EntryGate] 모델 레이블 체계 미확인(장전) — 사이즈 제한 유지 (×0.6)
      | 불일치 6/6 — 1m:label_scheme없음(구버전), 3m:label_scheme없음(구버전), 5m:label_scheme없음(구버전) …
  2026-08-19 08:55:16 [INFO] SYSTEM: [EntryGate] 전일 EOD 모델 적재 확인 — 사이즈 제한 해제 (×0.6 → ×1.0, 재학습 불요)
  ```

- **근거 ②** — 사이드카 실물 (`model/horizons/gbm_1m_meta.json`, 6개 호라이즌 전부 동일)
  ```json
  {"horizon":"1m","written_at":"2026-08-18 15:45:35","source":"eod",
   "train_start_ts":"2026-02-19 09:00:00","train_end_ts":"2026-08-18 14:38:00",
   "n_features":10,"acc":0.4327348953578795,"acc_kind":"cv",
   "label_scheme":null,"label_param":null}
  ```
  → **키는 있고 값이 `null`** 이다. 파일 mtime 2026-08-18 15:45~15:46 — 전일 EOD가 새로
  쓴 것이며 "구버전 사이드카"가 아니다. `learning/model_meta.py:54-56` 의
  `if not scheme:` 이 `null` 을 잡아 `"label_scheme없음(구버전)"` 으로 표기한다.

- **근거 ③ — 원인 확정** (코드 실측 3단)
  ```
  logs/retrain_eod_20260818.log:17
    2026-08-18 15:45:29 [INFO] LEARNING: [Retrain] 배치 재학습 시작 (weeks_back=26, phase2=False, intraday=False)
  ```
  스케줄러가 도는 것은 `retrain_eod.py`(15:45, py310_64)이고 **`phase2=False`** 다.
  즉 `_retrain_phase2()`(`batch_retrainer.py:1683` 이 `_last_label_scheme="fixed"` 를
  새기는 유일한 Phase2 경로)를 **타지 않는다.** `EOD_RETRAIN.bat:35` 만
  `--phase2` 를 붙이며 그것은 수동 경로다.

  그리고 실제 경로에서는 값이 **한 번 세워졌다가 지워진다**:
  ```
  retrain_eod.py:298   X, y_dict, feature_names = retrainer._load_from_db(RETRAIN_WEEKS_BACK)
                       #  ↑ _load_from_db 내부 batch_retrainer.py:2190 이 여기서
                       #    self._last_label_scheme = "fixed" 를 정상적으로 새긴다
  retrain_eod.py:320   result = retrainer.retrain_now(X=X, y_dict=y_dict, ...)
  batch_retrainer.py:508-510
                       if X is not None and y_dict is not None:
                           self._last_label_scheme = None      # ← 방금 세운 값을 지운다
                           self._last_label_params = {}
  batch_retrainer.py:1246  "label_scheme": getattr(self, "_last_label_scheme", None)  # → null
  ```
  508행 가드는 468차가 **"호출자가 출처 불명의 X/y를 넘기면 직전 학습의 값이 이번
  pkl의 규칙인 양 새겨진다"** 를 막으려고 넣은 것이다(그 주석 그대로). 그런데 실제
  프로덕션 호출자는 **같은 인스턴스의 `_load_from_db()` 결과**를 되먹이므로 그 값은
  유효했고, 가드가 유효한 값을 지운다. 보수 폴백이라 **안전한 쪽으로 틀리지만**
  G-1은 영영 `True` 를 낼 수 없다.

- **기준 위반**: 절대원칙 위반 아님. **CLAUDE.md "실전 전환 기준 ⑧"** 의 선행조건
  (468차 G-1 라이브 검증) 및 `references/invariants.md` §2-B —
  *"G-1 검증은 `불일치 0/6` 이 찍히고, F-1 마커 없이도 해제되는 것을 봐야 끝난다."*

- **영향**: ⑧(사이징 재설계) 해제가 막힌다. 더 중요한 것은 **실패 형태**다 — 게이트가
  꺼져 있지도, 발동하지도 않고 **매일 조용히 보수 폴백으로 흘러간다.**
  292차(CB③-P4 상시 RESTRICTED)·303차(FP-CRITICAL PSI=0.0 2개월)·311차
  (`spread_extreme_shadow` 한 달 죽은 섀도)와 **같은 실패 계열**이다.

- **신규 여부**: **신규(원인 규명)**. 증상 자체는 `invariants.md` §2-B가
  `⏳ 미검증` 으로 이미 표시하고 있었고 `NEXT_TODO.md` 에 *"⑧ 사이징 재설계 — G-1
  라이브 확인 여전히 미충족"* 으로 남아 있다. **그 문서는 "08-14는 배포 당일이라
  사이드카에 필드가 없는 것이 정상이며, 최초 검증 기회는 08-17(월) 장전"** 이라고
  적었다. 실측 정정 2건:
  - **2026-08-17은 휴장일이다** — `20260817_SYSTEM.log`(48행)이
    `18:11:56 [System] 대기 중 | 공휴일·휴장일 — 다음 KRX 거래일 08:45 재개` 만 남겼고
    `retrain_eod_20260817.log`·`eod_retrain_done_20260817.txt` 둘 다 없다.
    → 실제 최초 검증 기회는 **2026-08-18**이었다.
  - **08-18·08-19 2거래일 연속 `불일치 6/6`** (`20260818_SYSTEM.log`,
    `20260819_SYSTEM.log`). 08-14의 6/6은 문서가 예고한 정상이지만 **뒤의 둘은 아니다.**

#### 1-2. 수집기 DB 축 실패가 §12 `binding_gate` 를 "무기록"으로 위장 — 계측 4원칙 ② 위반

- **증상**: `DB 질의 실패(data/db/predictions.db): disk I/O error` 후, §12가
  `binding_gate | DB | 🔴 무기록 | 관측일 0 | 표본 0` 을 렌더하고 §11 자동 적신호 #5가
  *"계측 중단 또는 로그 문구 변경 의심"* 을 올렸다. **둘 다 사실이 아니다.**

- **근거**: 같은 DB를 스냅샷 복사(db + `-wal` + `-shm`)해 열면 정상 조회된다.
  ```
  ensemble_decisions  총 23,809행 · sizing_trace 컬럼 존재
  (mode=ro 직접 open → OperationalError: disk I/O error
   immutable=1 → 23,801행 — WAL 8행 누락, 스냅샷 → 23,809행)
  data/db/predictions.db-wal  762,232B  (mtime 2026-08-19 09:06)
  ```
  실패 지점은 `collect_evidence.py:962-964`
  ```python
  uri = "file:%s?mode=ro" % p.replace(...)
  con = sqlite3.connect(uri, uri=True, timeout=timeout)
  ```
  DB가 **WAL 모드**라 `mode=ro` 로도 `-shm` 공유메모리 매핑이 필요한데, 이 세션은
  코웍의 **리눅스 샌드박스 마운트**에서 돌아 그 매핑이 실패한다. 어제(0818)는 윈도우
  네이티브 실행이라 성공했다(§12에 `tox×29 · None×15 · hurst×13 · meta×8` 실적 있음).

- **기준 위반**: 계측 4원칙 ② — **미측정을 "0"으로 위장하지 않는다.**
  `binding_gate`의 `무기록` 은 "계측이 조용히 끊겼다"는 심각한 신호인데, 실제 원인은
  수집기가 DB를 못 연 것이다. 방향이 정반대다.

- **영향**: `NEXT_TODO` O-7(`binding_gate` 관측일 2일 증가 → 08-20 첫 판정)·
  O-8(§5 비용 축) 판정 불가. 예약 점검이 샌드박스에서 도는 한 **DB 축은 매일 죽는다.**

- **신규 여부**: **신규.** 단 `NEXT_TODO.md` O-8이 *"`비용 축 미측정`이 뜨면 DB 경로
  문제다"* 라고 정확히 예고한 실패다 — 예고는 맞았고, 다만 렌더가 `미측정` 이 아니라
  `무기록` 이었다.

### P2 — 운영 부담·기술부채

#### 1-3. 장전 점검이 개장 **후**에 실행됨 (09:07:04)

- **증상**: 증거 다이제스트 생성 시각 `2026-08-19 09:07:04 KST`. 장전 점검의 질문은
  *"오늘 거래할 자격이 되는가"* 인데, 판정 시점에 이미 **7분간 거래 자격을 행사한
  뒤**다. 오늘은 진입 0건이라 실해가 없었을 뿐이다.
- **근거**: 다이제스트 헤더 `생성 2026-08-19 09:07:04 KST`. 09:00 이후 정상 매분 루프
  진행 중(`09:06:59 [BAR-CLOSE][CYBOS] ts=09:06`).
- **신규 여부**: **기존 TODO.** `NEXT_TODO.md` 에 이미
  `TASK_CLAUDE_WAKE_INSTALL.bat 실행 (사용자 1회, MW0602)` · `NextRunTime 이 08:47:47`
  · `리허설 미완 — 판정 대기` 로 등록돼 있고 미착수 상태다.
- **영향**: 장전 점검의 의미가 사후 확인으로 변질. P0가 있었다면 발견이 7분 늦는다.

#### 1-4. 미커밋 산출물 7건 (이월)

```
 M docs/정기점검/금요일점검/weekly_prompt.txt
?? MIREUK_DAILYCHECK_HANDOFF.md
?? TASK_CLAUDE_WAKE_INSTALL.bat
?? TASK_CLAUDE_WAKE_VERIFY.bat
?? docs/정기점검/금요일점검/MW0602/0816_주간회의_검토보고_MW0602.md
?? scripts/claude_wake_diag.ps1
?? scripts/claude_wake_task.ps1
```
- **기준**: `phases.md` A-4 — *"실전 PC에 커밋 안 된 수정을 남기지 않는다."*
- **신규 여부**: **기존 TODO** — `NEXT_TODO.md` *"475차 산출물 커밋 (미실행 — 다음
  세션/사용자가 마무리)"*, *"F-4 `docs/정기점검` untracked 산출물 추적 편입"*.
  **중복 착수 금지 항목이므로 새 안건으로 올리지 않는다.**
- ⚠ `0816_주간회의_검토보고_MW0602.md` 는 **PC간 공유가 필요한 문서**다. 커밋되지
  않으면 MW0601 세션이 볼 수 없다(CLAUDE.md 멀티PC 컨벤션).

#### 1-5. CB⑤ 문서-코드 드리프트 — 여전히 미해소 (계속 보고 항목)

`invariants.md` §3-B가 *"발견하면 계속 보고할 것"* 으로 지정한 항목이므로 옮긴다.
`CB_API_LATENCY_LIMIT = 5.0` 은 Kiwoom 레거시(Cybos 미사용)이고 실질 구현은
`CB_PIPE_PAUSE_MS = 5_000`(파이프 지연 → **5분 진입 정지**)이다. CLAUDE.md 절대원칙
§2 ⑤의 문구(*"API 지연 5초 초과 → 즉시 청산"*)와 트리거 대상도 동작도 다르다.
468차 F-5 범위 밖(별건)으로 남아 있다. **신규 아님 — 상태 확인만.**

### 확인 필요 (확정 아님)

- **단기그룹 CORE `ofi_norm` 이 장전 내내 축퇴 상태였다.**
  `20260819_SIGNAL.log` `[CORE준비도]` 8건 중 **7건에 `ofi_norm×6hz` 포함**하며,
  개장 후(`09:00:59 trigger=B_OPEN` · `09:06:59 trigger=D_FORCE`)에도 해소되지 않았다.
  로그 자체가 *"정상 진입 전 해소 여부를 확인하라"* 고 말한다.
  `[ScalerRefresh] … raw_std≈0 → identity(0,1) 강제` 피처별 집계(오늘):
  `ofi_norm 48 · above_vwap 12 · cvd_divergence 6 · opt_chain_pcr 6 · vwap_position 6`
  — **`ofi_norm` 이 압도적 1위**다. 0818도 `[CORE준비도]` 42건 중 29건이 `ofi_norm`
  단독이었다.
  → `ofi_norm` 은 절대원칙 §3 단기그룹(1m·3m·5m) CORE(OFI 불균형, 미통과 시 등급 하락)다.
  **판정 보류(313차 원칙).** §12 `CORE준비도` 지표 자체가 `⚪ 표본부족 · 관측일 2`
  (470차 B4 배포가 08-14 19:47이라 그 이전 로그가 없다)이며, 470차가
  *"차단으로 승격하려면 20거래일 축적 후 '축퇴일의 09:00~09:30 정확도'를 일자단위로
  비교할 것"* 을 이미 사전등록했다.
  **몇 건 더 보면 판정되는가**: 관측 **20거래일**(현재 2일). 다만 `ofi_norm` 단독
  고착이 20거래일까지 이어지면 그 전에 별건으로 다뤄야 한다 — CLAUDE.md 371차 잔여
  이슈(*"cvd_divergence·ofi_norm 학습표본의 91%가 정확히 0.0인 점질량"*)와 같은
  계열일 **가능성**이 있으나 **미검증**이다. 아래 G-2로 등록.

- **`ConfFloorGuard` 상태 샘플 — 부분 개선(O-6 부분 충족).**
  어제 배포한 `bd3bbba`(475차 후속, ConfFloorGuard 상태 샘플 calibrator 오참조 수정)
  효과 확인: `[ConfFloorGuard] 상태 샘플 실패` **0건**(O-2 충족).
  다만 매분 관측률은 **아직 1.0이 아니다** — 09:00~09:11 12개 분봉 중 `state=` 샘플
  **7건(09:05:59~09:11:59)**, 09:00~09:04 **5개 분봉은 무샘플**. 그 직전 로그가
  `09:05:59 [ConfFloorGuard] 하한 도달 가능 복구 — 출력상한 0.3935 ≥ 필요 0.3840` 인
  것으로 보아, **"복구 전" 분기가 여전히 상태 샘플을 내지 않는** 것으로 보인다(가설).
  §12는 2거래일 합산이라 아직 `🟠 분기편향 관측률 0.22` 로 표시된다.
  **몇 건 더 보면 판정되는가**: 장후 재집계로 오늘 하루 전체 관측률을 확인하면 된다
  (기준: ≥0.5 면 분기편향 해소, ≈1.0 이면 완전 해소).

### 이미 반영된 사안 (신규 아님) — 함정 ① 방지

**이 절은 비어 있지 않다. 아래 5건은 확인 결과 이미 반영·확정된 것이므로 안건으로 올리지 않는다.**

1. **§11 자동 적신호 #5 `binding_gate 계측 중단 의심`** — 계측은 살아 있다.
   원인은 P1-2(수집기 DB 축 실패)다. 로그 문구 변경 아님.
2. **차단 게이트 7개 `False`** — `CB3_P4_GRADE_BLOCK_ENABLED` ·
   `FP_CRITICAL_GRADE_BLOCK_ENABLED` · `TOXICITY_SEVERE_SPREAD_BLOCK_ENABLED` ·
   `HEALTH_DEGRADED_BLOCK_MANUAL_ENTRY` 는 전부 CLAUDE.md 절대원칙 §2 한시예외 /
   설계 선택으로 **기록됨**. 나머지 3개는 기능 토글. **복원 제안 금지.**
3. **CB② `9999`** — 2026-08-01 결산에서 복원 **4주 연기 → 재검토 2026-08-29** 확정.
   기한 미도래(§7). "CB② 미발동"을 결함으로 쓰지 않는다.
4. **§13 확정 결정 레지스트리 18건 정상 렌더** — `NEXT_TODO` **O-10 충족**.
   원천 `validation_campaign_report_20260814.md`. `[25] tp1_protect_offset_shadow`
   = *"미적용 유지 — atr_lock_0.75만 감시 후보로 승격"*(2026-08-08 확정).
   **이 질문의 신규 계측 제안 금지**(0818 R-1 폐기 사유).
5. **`GuardFair_유효` `무효`×30** — 457차 설계상 `판정 무영향(섀도)`. 채널 [23] PASS.
   26주 WFA 항목 C3'(무효율 **추세**)로 이미 편입돼 있다.

---

## 2. Fix 작업 구현계획

> 🔴 **장전 국면이며 라이브 프로세스가 돌고 있다. 본 계획은 수립만 하고 적용하지 않는다.**
> F-1은 EOD 재학습 코드 경로라 **15:40 `daily_close` 이후 · 15:45 EOD 재학습 시작 전**에는
> 적용할 수 없다(그날 EOD가 구코드로 돈다). **15:47 EOD 완료 이후 적용 → 익일 EOD가 첫 검증**이다.

### F-1. `retrain_now()` X/y 가드가 자기 `_load_from_db()` 결과까지 지우는 것 — P1 · 대응 이상점 1-1

- **원인 가설**: 468차가 넣은 508-510행 가드는 **"출처 불명의 X/y"** 를 상정했는데,
  프로덕션 호출자(`retrain_eod.py`)는 **같은 인스턴스의 `_load_from_db()` 반환값**을
  되먹인다. 가드가 구분하지 못해 유효한 값을 지운다.
- **변경 파일**:
  - `learning/batch_retrainer.py` — `_load_from_db()`: 레이블 규칙을 인스턴스 필드만이
    아니라 **반환에 동반되는 표식**으로도 남긴다. 최소 변경안은 `_load_from_db` 끝에
    `self._label_state_from_load_db = (id(X), self._last_label_scheme, dict(self._last_label_params))`
    를 기록하는 것. (반환 시그니처는 **바꾸지 않는다** — 같은 파일 2205행 주석이
    *"호출부가 여럿이고 전부 3-튜플 언팩이라 확장하면 조용히 깨진다"* 고 못박고 있다.)
  - `learning/batch_retrainer.py:508-510` — `retrain_now()`: 가드를
    **"모르는 X/y만 지운다"** 로 좁힌다.
    ```python
    if X is not None and y_dict is not None:
        _src = getattr(self, "_label_state_from_load_db", None)
        if _src and _src[0] == id(X):
            # 이 인스턴스의 _load_from_db()가 방금 만든 X다 — 레이블 규칙을 안다.
            self._last_label_scheme, self._last_label_params = _src[1], dict(_src[2])
        else:
            self._last_label_scheme = None       # 종전 동작 (모르면 제한 유지)
            self._last_label_params = {}
    ```
- **회귀 위험**: `id()` 재사용 — X가 GC된 뒤 다른 객체가 같은 id를 받으면 오탐.
  `retrain_eod.py` 는 `X` 를 지역변수로 살려둔 채 `retrain_now` 를 부르므로 실사용
  경로에서는 발생하지 않지만, **더 안전한 대안**은 `_load_from_db` 가 X에
  `X._mireuk_label_state` 를 붙이는 것(numpy 배열은 속성 부착 불가하므로 id 방식이
  현실적) 또는 `_load_from_db` 호출 카운터 + `X.shape` 병행 대조다. 구현 시 택일.
  **틀렸을 때의 방향이 중요하다** — 오탐 시 `None`(=제한 유지)로 떨어지는 쪽이
  기본이어야 한다.
- **절대원칙 저촉 여부**: 6종 어디에도 저촉되지 않음. 사이즈 배수 경로만 건드리며
  진입/청산/CB 로직은 무변경.
- **런타임 주의**: 이 코드는 **`py310_64` 서브프로세스**(`retrain_eod.py` ·
  `retrain_intraday.py`)에서 돈다. `py37_32` 로 되돌리지 말 것(191차 OOM 결정).
  COM 콜백 안이 아니므로 절대원칙 ④ 무관.
- **검증 방법**:
  1. `pytest tests/ -k "model_meta or label"` — 기존 468차 회귀 유지 확인.
  2. 신규 `tests/test_476_eod_label_scheme_roundtrip.py` — `_load_from_db` 를 스텁으로
     둔 뒤 `retrain_now(X=..., y_dict=...)` 를 호출해 사이드카에
     `label_scheme="fixed"` 가 기록되는지, **출처 불명 X는 여전히 `None`** 인지 2케이스.
  3. **다음 EOD 관측**: `model/horizons/gbm_*_meta.json` 6개 전부
     `"label_scheme": "fixed"` · `"label_param"` 이 `HORIZON_THRESHOLDS` 와 일치.
  4. **그 다음 장전 관측(판정)**: `[EntryGate] … 불일치 0/6` 또는 미확인 로그 자체가
     사라지는 것. ⚠ **`×0.6` 이 사라지는 것을 G-1 검증으로 읽지 말 것** — F-1 폴백만으로도
     사라진다(`invariants.md` §2-B 경고).
- **섀도 계측 필요 여부**: **불요.** 게이트 신설이 아니라 **이미 배포된 게이트의
  입력값 복구**다. 게이트 자체(`MODEL_LABEL_STATE_UNLOCK_ENABLED`)는 이미 `True`.
- **적용 시점**: **장후 (15:47 EOD 완료 이후)**. EOD 재학습 중 코드 교체 금지.
- **결정 필요 사항**: **있음 — 사용자/주간회의 판단.**
  F-1이 들으면 G-1이 `True` 를 내기 시작하고, 그러면 **`_pre_retrain_done` 미해제로
  사이즈를 ×0.6으로 눌러 온 구간이 끝난다.** CLAUDE.md ⑧이 *"⑧ 해제로 base_risk가
  오르는 순간 이 배수의 구속력이 커진다 — [28] `sizing_inversion_watch`가 깨어나는
  바로 그 시점"* 이라고 경고한 지점이다.
  **권고**: F-1을 적용하되 `MODEL_LABEL_STATE_UNLOCK_ENABLED` 는 그대로 두고,
  **먼저 `불일치 0/6` 이 실제로 찍히는지 1~2거래일 관측**한 뒤 ⑧ 논의로 넘긴다.
  ⚠ 오늘도 실효 배수는 F-1 폴백으로 이미 ×1.0이므로 **F-1 수정 자체가 노출을 늘리지는
  않는다** — 늘리는 것은 ⑧ 해제다. 둘을 한 커밋에 섞지 말 것.

### F-2. 수집기 `db_rows()` — WAL DB 폴백 체인 + 실패를 `무기록` 이 아니라 `DB미접속(미측정)` 으로 - P1 · 대응 이상점 1-2

- **원인 가설**: `mode=ro` 단독 open은 WAL DB에서 `-shm` 매핑을 요구하는데, 코웍
  샌드박스의 마운트가 이를 지원하지 않는다. 예약 점검은 항상 이 환경에서 돈다.
- **변경 파일**:
  - `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py` — `db_rows()`
    (956~970행): 3단 폴백.
    ```
    ① file:...?mode=ro                    (기존 — 윈도우 네이티브에서 최선)
    ② 스냅샷: db + -wal + -shm 을 tempdir 로 복사 후 일반 open   ← WAL 포함, 값 정확
    ③ file:...?mode=ro&immutable=1        (최후 — WAL 누락, 8행 차이 실측)
    ```
    성공한 단계를 `_db_access_mode` 에 기록하고, ②·③으로 떨어졌으면 다이제스트에
    **한 줄 주석**을 남긴다(③은 "WAL 미반영 — 최신 수 분 누락 가능" 경고 동반).
  - 같은 파일 §12 렌더 — DB 원천 지표는 **접속 실패와 표본 0을 분리**한다.
    현재 둘 다 `🔴 무기록` 인데, 접속 실패는 `⬛ DB미접속(미측정)` 으로 낸다.
    §11 자동 적신호에서도 접속 실패 건은 "계측 중단 의심"이 아니라 "수집기 환경
    문제"로 문구를 바꾼다.
- **회귀 위험**: 스냅샷 복사 비용 — `predictions.db` 는 **917MB**다. 매 질의마다
  복사하면 안 된다. **① 실패 시 1회만 복사해 세션 내 재사용**하도록 캐시하고,
  tempdir 는 종료 시 정리한다. 디스크 여유 확인 실패 시 ③으로 건너뛴다.
- **절대원칙 저촉 여부**: 없음. 점검 도구 전용이며 라이브 코드 무관.
- **런타임 주의**: 수집기는 표준 라이브러리만 쓴다(`py37_32`/`py310_64` 무관).
  이 원칙을 깨지 말 것 — `shutil`·`tempfile` 은 표준이라 문제없다.
- **검증 방법**: 이 샌드박스에서 재실행해 §12 `binding_gate` 가 `무기록` 이 아니라
  실제 값 분포(0818 기준 `tox/hurst/meta/None`)를 내는지. 윈도우 네이티브에서도
  ①로 성공해 종전과 동일 출력인지 대조.
- **섀도 계측 필요 여부**: 불요.
- **적용 시점**: **즉시 가능**(라이브 무관). 다만 아래 커밋 계획대로 F-1과 함께 낸다.
- **결정 필요 사항**: 없음.

### F-3. `model_meta.read_label_state()` 진단 문구 — `null` 과 `키 없음` 을 구분 — P2 · 대응 이상점 1-1

- **원인 가설**: `learning/model_meta.py:54-56` 의 `if not scheme:` 이 **키 부재**와
  **키는 있으나 값이 `null`** 을 같은 `"label_scheme없음(구버전)"` 으로 표기한다.
  오늘 사이드카는 **어제 새로 쓴 것**인데 로그는 "구버전"이라고 말한다 — 이 한 단어가
  원인 추적을 며칠 늦출 수 있었다.
- **변경 파일**: `learning/model_meta.py` — `read_label_state()`
  - `"label_scheme" not in meta` → `"%s:사이드카 구버전(키없음)"`
  - `meta.get("label_scheme") is None` → `"%s:label_scheme=null(기록경로 결함)"`
  - 값이 빈 문자열 → `"%s:label_scheme 빈값"`
- **회귀 위험**: 문자열만 바뀐다. `tests/` 에서 이 문구를 문자열 매칭하는 테스트가
  있으면 함께 갱신할 것.
- **절대원칙 저촉 여부**: 없음. **런타임 주의**: `py37_32` 본체에서 import된다
  (COM 의존 없는 순수 함수 — 468차가 그 목적으로 분리한 모듈).
- **검증 방법**: 기존 회귀 + 다음 장전 로그 문구 확인.
- **적용 시점**: F-1과 동일 커밋(원인·진단 한 쌍).
- **결정 필요 사항**: 없음.

### F-4. 장전 예약 점검 실행 시각 — P2 · 대응 이상점 1-3

- **원인 가설**: 미착수 TODO(`TASK_CLAUDE_WAKE_INSTALL.bat` 미실행 · `NextRunTime`
  `08:47:47`). **원인 단정은 하지 않는다** — 오늘 실측은 "09:07에 돌았다" 뿐이고
  스케줄러 상태를 이 세션에서 직접 확인하지 못했다.
- **변경 파일**: 코드 변경 없음. **사용자 1회 작업**이다.
  1. `TASK_CLAUDE_WAKE_INSTALL.bat` 실행 (관리자 권한)
  2. `TASK_CLAUDE_WAKE_VERIFY.bat` 로 `NextRunTime` 확인 — 목표 **08:30~08:45**
     (매크로 수집 08:55 **이전**, 런처 기동 08:40 **이후**)
  3. `NEXT_TODO.md` 의 `MW0602 등록일: ____` 칸 기입
- **적용 시점**: 즉시(사용자). 라이브 프로세스와 무관.
- **결정 필요 사항**: **목표 시각.** 08:40 런처 기동 직후를 노리면 모델 로드·
  스케일러 상태를 볼 수 있으나 08:55 매크로·`[EntryGate]` 는 못 본다.
  **권고 = 08:58** — `[PreRetrain]`·`[EntryGate]`·레짐 확정까지 다 찍힌 뒤이면서
  개장 전이다(오늘 기준 08:58:19에 레짐 확정 완료).

### 적용 순서와 커밋 계획

1. **커밋 ①** — F-2 (수집기 DB 폴백 + §12/§11 렌더 분리)
   `[MW0602] 476차: 수집기 db_rows WAL 폴백 체인 + DB미접속과 무기록 분리`
   → 라이브 무관, 먼저 낸다. 이게 들어야 F-1의 관측 근거가 매일 쌓인다.
2. **커밋 ②** — F-1 + F-3 (원인 + 진단) + 회귀 테스트
   `[MW0602] 476차: EOD 사이드카 label_scheme 유실 수정 (G-1 부활) + 진단 문구 구분`
   → **15:47 EOD 완료 이후 적용.**
3. **커밋 ③** — 리포트·dev_memory + 이월된 475차 산출물 7건 (1-4 해소)
   `[MW0602] 476차: 0819 장전 점검 리포트 + dev_memory + 475차 산출물 커밋`
4. F-4는 사용자 작업 — 커밋 대상 아님(등록일만 `NEXT_TODO.md` 에 기입).

---

## 3. 고도화 방안

### G-1. EOD 재학습 자체 사후검증 — 사이드카를 쓴 프로세스가 직접 되읽는다

- **관측 근거**: 오늘 P1-1. 사이드카에 `label_scheme=null` 이 **2거래일 연속** 기록됐고,
  그것을 발견한 것은 다음날 장전의 `[EntryGate]` 로그였다. 그마저도
  `"(구버전)"` 이라는 오도 문구 때문에 "예정된 상태"로 읽힐 수 있었다.
  같은 구조의 사고가 이 프로젝트에 반복된다 — 292차·303차·311차·468차 F-3.
- **제안 내용**: `retrain_eod.py` 가 **재학습 완료 직후, 마커 파일을 쓰기 전에**
  방금 저장한 사이드카 6개를 `learning/model_meta.read_label_state()` **로 되읽어**
  결과를 로그와 마커에 남긴다.
  - `logs/retrain_eod_{d}.log` 에 `[SelfCheck] label_state ok=True/False detail=…`
  - `data/eod_retrain_done_{d}.txt` 에 같은 줄을 추가
  - `ok=False` 면 **WARNING** 으로 올린다(ERROR는 쓰지 않는다 — 이 프로젝트는
    ERROR를 안 쓰고, 장전 점검이 WARNING 축을 본다)
  ⚠ **재학습을 실패로 만들지 않는다.** 모델 교체는 그대로 진행하고 관측만 남긴다.
- **기대 효과**: 사이드카 결함 발견 지연이 **1거래일 → 0**(그날 밤 로그에 남는다).
  `read_label_state` 를 쓰는 검증자와 피검증자가 같아지므로, F-1 같은 "쓰는 쪽과
  읽는 쪽의 계약 불일치"가 구조적으로 잡힌다.
- **비용·위험**: 구현 ~20행. 위험 낮음. 사이드카 6개 재읽기(수 KB)라 EOD 시간 영향 무시 가능.
- **선행 조건**: F-1(안 고치면 매일 `ok=False` 만 찍힌다).
- **우선순위 제안**: **이번 주** (F-1과 같은 주에 내야 F-1의 검증 수단이 된다).

### G-2. `ofi_norm` 축퇴를 §12 관측에서 **피처 단위**로 분해

- **관측 근거**: 오늘 `[CORE준비도]` 8건 중 7건이 `ofi_norm`, `identity(0,1)` 강제
  48건으로 2위(`above_vwap` 12)의 4배. 0818도 42건 중 29건이 `ofi_norm` 단독.
  그런데 §12 `CORE준비도` 지표는 **축퇴 개수(0/1/2/4/5)** 만 센다 —
  `1`×35가 전부 `ofi_norm` 인지 매일 바뀌는지 **표에서 알 수 없다.**
- **제안 내용**: `stuck_indicators` 에 `CORE축퇴_피처` 축 신설.
  `[CORE준비도] … 축퇴 N/6 — <피처>×6hz …` 에서 **피처명 집합**을 값으로 삼아
  §12에 값 분포를 낸다(`ofi_norm`×N, `above_vwap`×M …). 한 피처 100% 고착이면
  개수 축이 `1` 로 "정상처럼" 보여도 **그 피처는 상시 무력**임이 드러난다.
- **기대 효과**: 470차가 사전등록한 **20거래일 판정**(축퇴일의 09:00~09:30 정확도
  일자단위 비교)을 할 때, "어느 피처의 축퇴일인가"를 소급해서 셀 수 있다.
  지금 구조로는 20일 뒤에 로그를 다시 파싱해야 한다.
- **비용·위험**: 수집기 정규식 1개 + `stuck_indicators` 항목 1개. 라이브 무관.
  ⚠ **차단으로 승격하지 않는다** — 섀도 관측 축을 하나 더 놓을 뿐이다.
- **선행 조건**: 없음(F-2와 같은 파일이라 함께 내면 편하다).
- **우선순위 제안**: 이번 주.

### G-3. 장전 국면 전용 관문 — "오늘 진입 자격을 이미 행사했는가"

- **관측 근거**: 오늘 장전 점검이 09:07에 돌았다(1-3). 다이제스트는 이것을 **아무데도
  표시하지 않았다** — 헤더에 생성 시각이 있을 뿐, `--phase pre` 인데 이미
  09:00을 지났다는 사실이 판정에 반영되지 않는다.
- **제안 내용**: `collect_evidence.py` 가 `--phase pre` 로 실행됐는데 생성 시각이
  **09:00 이후**면 다이제스트 **최상단에 경고 배너**를 찍는다 —
  `⚠ 장전 점검이 개장 후 N분에 실행됨 — 이 시점까지 진입 M건. 사후 확인이다.`
  (진입 건수는 §5 집계 재사용)
- **기대 효과**: 리포트를 읽는 사람이 "장전 판정"의 성격(사전 자격심사 vs 사후 확인)을
  오해하지 않는다. F-4가 해결되면 배너는 자연히 사라진다 — **해결 여부의 지표**가 된다.
- **비용·위험**: ~10행. 위험 없음.
- **선행 조건**: 없음.
- **우선순위 제안**: 이번 주(F-2와 같은 커밋).

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| 실전 전환 기준 ⑧ 선행조건 (G-1 라이브 검증) | `invariants.md` §2-B `⏳ 미검증` | **`🔴 미충족 — 구조적 결함 확인(476차)`** 으로 격상. "관측을 못 했다"가 아니라 **"현 코드로는 관측될 수 없다"** 임을 명시 | 다음 세션이 `⏳` 를 "기다리면 된다"로 읽으면 F-1이 영영 안 나온다. 이 프로젝트가 반복해 온 실패 형태 |
| `invariants.md` §2-B 각주 "G-1 최초 검증 기회 = 2026-08-17(월)" | 사실과 다름 | **2026-08-18로 정정.** 08-17은 휴장일(`20260817_SYSTEM.log` 실측) | 그 날짜를 기준으로 "아직 기회가 없었다"고 판단하면 2거래일 연속 실패를 놓친다 |
| CLAUDE.md 절대원칙 §2 ⑤ (CB⑤ 문구) | 미해소 드리프트 | 468차 CB③ 병기와 **같은 방식으로 실값 병기** 제안 (문서만 수정, 코드 무변경) | `invariants.md` §3-B가 "발견하면 계속 보고"로 지정. 2회 이상 반복 보고된 항목이다 |
| 26주 WFA 재검증 항목 | C1'~C5' 등록됨 | **C6' 신설 제안 — "사이드카·마커 계약 검증"**: 쓰는 쪽(EOD)과 읽는 쪽(런타임)이 같은 필드를 같은 의미로 쓰는지 | P1-1이 정확히 그 계약 파손이었다. C1'(열린 채 끝난 지표)·C2'(사이징 축)와 같은 계열 |

---

## 4. 진입 승패 사후검증 (장후 한정)

**해당 없음(장후 전용).**

---

## 5. 수익률 향상방안 (장후 한정)

**해당 없음(장후 전용).**

> ⚠ 다음 장후 세션 주의: R-* 를 쓰기 전에 다이제스트 **§13 확정 결정 레지스트리
> 18건**과 `VALIDATION_CAMPAIGN` 키를 대조할 것(0818 R-1 재발 방지).

---

## 6. 다음 거래일 관측 예정

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| O-1 | `model/horizons/gbm_*_meta.json` 의 `label_scheme` | F-1 적용 후 첫 EOD에 **6/6 `"fixed"`**. `null` 이면 F-1이 안 든 것 | F-1 적용 익일 |
| O-2 | `[EntryGate] … 불일치 N/6` (`{d}_SYSTEM.log`) | **`불일치 0/6`** 또는 "모델 레이블 체계 미확인" 로그 자체 소멸. ⚠ `×0.6` 소멸은 **판정 근거 아님**(F-1 폴백만으로도 사라진다) | O-1 다음 장전 |
| O-3 | 수집기 §12 `binding_gate` | F-2 적용 후 `무기록`·`DB미접속` 아닌 **실제 값 분포**. 0818 기준 `tox/hurst/meta/None` 4종 | F-2 적용 익일 |
| O-4 | 수집기 §5 비용 3열(gross/수수료/net) | 렌더될 것. `비용 축 미측정` 이면 F-2가 부족한 것 (기존 O-8 계승) | F-2 적용 익일 |
| O-5 | `[ConfFloorGuard] state=` 관측률 (하루 전체) | 오늘 09:00~09:11 **7/12(0.58)**. 하루 전체로 **≥0.9** 면 `bd3bbba` 완전 성공, 0.5~0.9면 "복구 전 분기 무샘플" 가설 채택 | 오늘 장후 |
| O-6 | `[CORE준비도]` 의 `ofi_norm` 포함률 | 오늘 7/8(87.5%), 0818 29/42(69%). **3거래일 연속 ≥60%** 면 개수 축과 별개 안건으로 승격 | 08-20 |
| O-7 | 장전 점검 실행 시각 (다이제스트 헤더) | F-4 적용 후 **09:00 이전**. G-3 배너 미출현 | F-4 적용 익일 |
| O-8 | `[CybosProbe]` 초기 실패 | 오늘 08:58 10건 → 09:04 복구. 매일 같은 패턴이면 정상(장전 TR 미개시), 09:10 넘겨도 실패면 별건 | 08-20 |

---

## 7. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| **CB② 복원** (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | **2026-08-29** | **10일** (거래일 8일) | 🔴 **2026-08-22(금) 주간회의 안건으로 올릴 것.** 2026-08-01 결산이 4주 연기하며 "무기한 유예가 아니다 — 반드시 재론"이라고 못박았다. Phase 5 조건 ②·⑤가 걸려 있다 |
| CB③-P4 재검토 (`CB3_P4_GRADE_BLOCK_ENABLED`) | 실전 전환 전 | — | 기한 없음. 30m 재도입 또는 CB③ 기준 호라이즌 교체와 **한 묶음** |
| FP-CRITICAL 재검토 (`FP_CRITICAL_GRADE_BLOCK_ENABLED`) | 실전 전환 전 | — | 371차 분위수 재설계 후 **라이브 관찰 중** |
| TOX-SEVERE-SPREAD 재검토 (전환기준 ⑨) | 실전 전환 전 | — | 473차 F-8: 손익축 판정 **ETA 7.1개월**. **주간회의에서 "선행조건 유지 vs 26주 WFA 이관" 결정 필요** |
| ⑧ 사이징 재설계 | 실전 전환 전 | — | **본 리포트 P1-1로 선행조건이 막혀 있음이 확인됨.** F-1 → O-2 통과가 선행 |
| [25] `atr_lock_0.75` watch — MW0601 축적 | 2026-08-21(금) | 2일 | MW0602는 도달(95훅/16일). ⚠ 도달해도 자동 적용 아님 |

---

## 8. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-08-19 (MW0602 476차 — 장전 점검: G-1 사이드카 label_scheme 유실 규명)`
- `NEXT_TODO.md` 추가 체크박스: **13건** (F-1~F-4 · G-1~G-3 · O-1~O-8 중 신규분)
- 완료 처리한 기존 항목: **O-10**(§13 레지스트리 18건 렌더 확인) · **O-2**(ConfFloorGuard
  상태 샘플 실패 0건) — 0818 후속2·후속3이 등록한 관측 항목
- 부분 충족: **O-6**(ConfFloor 관측률 — 0.22 → 오늘 구간 0.58, 장후 재확인 필요)

---

## 9. 🔴 커밋 미실행 — 사용자 조치 필요 (`.git/index.lock` 스테일)

**본 세션은 커밋하지 못했다.** 산출물(리포트·다이제스트·dev_memory 2종)은 디스크에 정상
기록됐으나 git 인덱스가 잠겨 있다.

```
$ git add docs/정기점검/매일점검/MW0602-20260819-점검리포트.md ...
fatal: Unable to create '.../.git/index.lock': File exists.

$ ls -la .git/index.lock
-rwx------ 0 Aug 19 09:07 .git/index.lock      ← 0바이트 · 09:07 생성
$ rm -f .git/index.lock
rm: cannot remove '.git/index.lock': Operation not permitted
```

**원인.** 이 예약 점검은 코웍의 **리눅스 샌드박스 마운트**에서 돈다. 그 마운트는
파일 **생성·추가는 되지만 삭제(unlink)가 안 된다.** 09:07 첫 `git status` 가 인덱스
갱신을 위해 `index.lock` 을 만들었고 스스로 지우지 못했다
(`warning: unable to unlink '.git/index.lock': Operation not permitted`).
**P1-2(수집기 DB 축 실패)와 같은 뿌리** — 예약 점검이 이 환경에서 도는 한 매번 재발한다.

> ⚠ **다른 git 프로세스가 도는 것이 아니다.** 메시지가 그렇게 안내하지만
> 실제 원인은 마운트의 unlink 불가다. 라이브 프로세스를 죽이지 말 것.

**사용자 조치 (윈도우에서, 1회)**

```bat
cd /d C:\Users\pc1\PycharmProjects\futures
del .git\index.lock
git add docs\정기점검\매일점검\MW0602-20260819-점검리포트.md ^
        docs\정기점검\매일점검\evidence_MW0602-20260819_pre.md ^
        dev_memory\DECISION_LOG.md dev_memory\NEXT_TODO.md
git commit -m "[MW0602] 476차: 0819 장전 점검 리포트 + dev_memory (G-1 사이드카 label_scheme 유실 규명)"
```

⚠ 이때 **1-4의 이월 미커밋 7건**(475차 산출물 · `TASK_CLAUDE_WAKE_*` ·
`0816_주간회의_검토보고_MW0602.md`)도 함께 정리하는 것이 좋다. 특히 주간회의 검토보고는
커밋되지 않으면 **MW0601 세션이 볼 수 없다**(CLAUDE.md 멀티PC 컨벤션).

**항구 대책**: F-2(수집기 DB 폴백)와 같은 성격으로, 예약 점검이 샌드박스에서 돌 때는
`git status` 계열을 `git --no-optional-locks status` 로 부르도록 수집기를 고칠 것
(`collect_evidence.py` §2). `--no-optional-locks` 는 인덱스 갱신을 시도하지 않아
`index.lock` 을 만들지 않는다. → `NEXT_TODO` F-5로 등록.


---
---

# 장중(intra)

- 점검 시각: **12:56 KST** (증거 수집 12:40:35) · PC `MW0602` · 브랜치 `dev` (운영 브랜치 일치 ✅)
- HEAD `32886e7` · 미커밋 **10건**(장전 7건 → 476차 장전 산출물 3건 추가) · 당일 커밋 0건
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260819_intra.md` (69.4KB)
- 수집기 실행: **부분 실패(장전과 동일)** — §0-1i
- 세션 차수: **476차** (장전과 동일 세션 차수. 국면만 다름)
- ⚠ **장중 국면이므로 코드 변경·재기동·git 커밋을 일절 하지 않았다.** 읽기 전용 조사만 수행.
  본 절의 Fix 계획은 **장후(15:10 이후) 적용 권고**다.
- 관측 창: **09:00~12:40 (221분)**. 15:10까지의 최종 판정은 장후 점검 몫이다.

## 0-1i. 수집기 실행 — 부분 실패 (장전과 동일 원인, 재발)

```
[collect_evidence] 설정 덮어씀: config/dailycheck_targets.json
[collect_evidence] DB 질의 실패(data/db/predictions.db): disk I/O error
[collect_evidence] 저장: docs/정기점검/매일점검/evidence_MW0602-20260819_intra.md (69.4KB)
```

- 로그 축(§1~§11)은 전부 정상. **DB 축만 실패**했고, 그 결과 §12 `binding_gate` 가
  `🔴 무기록 / 관측일 0 / 표본 0` 으로 렌더됐다 — **사실이 아니다.**
  실제로는 `[SizerMatch]` 로그에 `binding=tox(0.70) / meta(0.74) / hurst(0.50)` 4건이
  또렷이 남아 있다(§1i 확인필요 C-3).
- 단, `trades` 축은 살아 있었다 — §5 비용 3열(gross/수수료/net)이 정상 렌더됐다.
  즉 실패한 것은 **`predictions.db` 만**이다(WAL 상태 추정, 장전 P1-2와 동일).
- **신규 아님** — 장전 P1-2 · `NEXT_TODO` **F-2**로 이미 등록. 본 절에서 재제안하지 않는다.
- 이 리포트에서 §12 `binding_gate` 판정은 **전부 보류**한다.

---

## 0i. 한 줄 결론

**매분 9단계는 설계대로 돌고 있다 — 정상(손익은 음수).**
매분 루프 커버리지 **221/221분(100.0%)**, 10분 이상 공백 0건, 장중 재기동·배포 흔적 0건,
절대원칙 6종 위반 **없음**, P0 **없음**. 진입 3건은 전부 `vwap✅` 로 절대원칙 §3의
강제-X 규칙을 지켰고, 3포지션 모두 **의도 손절폭 이내**에서 닫혔다(417차가 지목한
유일한 유의 축인 손절 준수율 = 3/3). 다만 **오늘 손익은 3포지션 전패 −443,174원(net)** 이고,
3건 모두 `A급(원시C)` — 체크리스트 A와 확신도 C의 괴리다. 이것은 이미 열려 있는
`grade_ev_inversion` 채널의 질문이며 **2026-08-01 "부결" 확정 사안**이므로 신규 안건으로
올리지 않는다(함정 ①). n=3·1거래일이라 **313차 원칙에 따라 확정 결론 보류**.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 조건부 정상 | 위 장전 절 참조 (P0 없음 / P1 2건) |
| **장중** | **정상 (조건부)** | 루프 100% · 절대원칙 위반 0 · P0 0. 조건 = `ofi_norm` 축퇴 100%(§1i P1-6) 미해소, 손익 −443,174원 |
| 장후 | — | 미실시 (15:10 이후) |

**절대원칙 준수 여부** (관측 창 09:00~12:40)

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | **판정 시점 아님** | 12:40 현재 **무포지션**(3포지션 전부 11:41:52까지 청산 완료). 15:10 경로 검증은 장후 몫이며, 471차 F-1 이후 하트비트 `[SchedForceExit] … bar_pass=N회` 출현 여부를 볼 것 |
| ② | Circuit Breaker 5종 | **정상** | `[DBG-CB] state=NORMAL consec_stops=0 acc30m=90.0% latency=0.182s`(12:40:00). CB② 카운터는 09:57:37 `[CB] 연속 손절 1회` 로 정상 증가·복귀. **CB②는 `9999`라 정지 안 하는 것이 정상**. CB⑤ 실질구현 `CB_PIPE_PAUSE_MS=5_000` 대비 최대 블로킹 **1,546ms**(08:41 기동 시) — 미발동 |
| ③ | CORE 피처 호라이즌 그룹별 | **조건부 정상** | 진입 3건 전부 `entry_horizon=3m`(단기 그룹) → CORE = cvd/vwap/ofi. **`vwap✅` 3/3 → 강제-X 규칙 준수(P0 아님).** `cvd❌` 1건(11:35)은 규칙상 "등급 하락"이며 강제-X 아님. ⚠ 단 `ofi_norm` 이 `identity(0,1)` 강제 **192회**·`[CORE준비도]` **32/32(100%)** 축퇴 → §1i P1-6 |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | **정상** | `0xC0000409`·`STATUS_STACK_BUFFER_OVERRUN`·`crash_fault.log` 신규 기록 0건. `[FaultHandler] 활성화 PID=14820`(08:40:41) 이후 크래시 0 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | **정상** | TR 수신 정상 — `[CybosInvestor] fetch#221 futures_supported=True program_supported=True option_supported=True`(12:40:16). 08:58 초기 `supported=False` 는 장전 O-8 등록 패턴(09:04 복구) |
| ⑥ | 알파 리서치 봇 자동통합 금지 | **정상** | 알파봇 관련 로그 0건. 유일한 매칭 `[DriftAdjuster] 로드: alpha=0.01000`(08:41:08)은 학습률 α이며 알파봇과 무관 |

**설정 불변식** — 수집기 §3 전 24행 `일치`/`값 확인`. **`불일치`·`미발견` 0건 → 이상 없음.**
차단 게이트 전수 29개 중 7개 `False` 는 **전부 CLAUDE.md 한시예외·설계선택·기능토글**이며
`documented_disabled_flags` 등록분이다 — 복원 제안 금지(장전과 동일).

---

## 1i. 이상점 정리보고 (장중)

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**없음.**

> 특히 확인한 것: **`vwap` 미통과 상태 진입 0건**(절대원칙 §3, `phases.md` B-4의 P0 조건).
> 진입 3건의 체크리스트는 아래와 같다.
> ```
> 09:45:59 [진입체크] LONG→LONG 2계약 A급(원시C) | sign✅ conf✅ vwap✅ cvd✅ ofi✅ fore❌ prev✅ time✅ risk✅ chas❌ coun✅ | conf=42.9%
> 09:56:00 [진입체크] LONG→LONG 2계약 A급(원시C) | sign✅ conf✅ vwap✅ cvd✅ ofi✅ fore❌ prev✅ time✅ risk✅ chas✅ coun✅ | conf=39.6%
> 11:35:00 [진입체크] SHORT→SHORT 2계약 A급(원시C) | sign✅ conf✅ vwap✅ cvd❌ ofi✅ fore✅ prev✅ time✅ risk✅ chas✅ coun✅ | conf=40.0%
> ```
> `logs/20260819_TRADE.log` · 3건 · 09:45:59~11:35:00

### P1 — 정확성·관측 훼손

#### 1-6. 단기그룹 CORE `ofi_norm` 이 장중 내내 축퇴 — `[CORE준비도]` 32/32(100%) · `identity(0,1)` 강제 192회

- **증상**: 절대원칙 §3의 단기 그룹(1m·3m·5m) CORE 3종 중 `ofi_norm` 이
  **관측 창 전 구간에서 한 번도 축퇴를 벗어나지 못했다.** 장전 리포트가 7/8(87.5%)로
  올린 값이 장중 누적에서 **32/32 = 100%** 로 올라갔다.

- **근거 ①** — 축퇴 판정 (`logs/20260819_SIGNAL.log`, `[CORE준비도]` 32건 전량에 포함)
  ```
  2026-08-19 08:45:16 [WARNING] SIGNAL: [CORE준비도] trigger=A_WARMUP 축퇴 5/6 — above_vwap×6hz cvd_divergence×6hz ofi_norm×6hz opt_chain_pcr×6hz vwap_position×6hz
  2026-08-19 08:49:59 [WARNING] SIGNAL: [CORE준비도] trigger=A_WARMUP 축퇴 1/6 — ofi_norm×6hz
  2026-08-19 12:37:59 [WARNING] SIGNAL: [CORE준비도] trigger=D_FORCE  축퇴 1/6 — ofi_norm×6hz
  ```
  축퇴 개수 분포 `1개×30 · 2개×1 · 5개×1` — **개수 축(§12 `CORE준비도`)으로 보면
  "1로 안정"이라 정상처럼 보인다.** 피처 축으로 갈라야만 보인다(장전 G-2가 제안한 그것).

- **근거 ②** — 스케일러 대체 (`logs/20260819_SIGNAL.log`, `[ScalerRefresh]`)
  ```
  2026-08-19 08:49:59 [WARNING] SIGNAL: 1m CORE 'ofi_norm' raw_std≈0(0.0320) → identity(0,1) 강제 (FLAT 100% 방지)
  ```
  피처별 `identity(0,1)` 강제 횟수: **`ofi_norm` 192 · `above_vwap` 12 ·
  `vwap_position` 6 · `opt_chain_pcr` 6 · `cvd_divergence` 6.**
  `ofi_norm` 이 나머지 4개 합의 **6.4배**다.

- **근거 ③ — 독립 원천의 교차 확인 (신규)**: EOD 전략 배너의 **피처별 PSI**가 같은 것을
  가리킨다. `data/daily_reports/strategy_report_*.txt`
  ```
  20260812  PSI : 0.110 (WATCHLIST)  PSI/feat: cvd=0.244  vwap_position=0.110  ofi=0.003
  20260813  PSI : 0.063 (CLEAR)      PSI/feat: cvd=0.230  vwap_position=0.063  ofi=0.003
  20260814  PSI : 0.036 (CLEAR)      PSI/feat: cvd=0.232  vwap_position=0.036  ofi=0.002
  20260818  PSI : 0.034 (CLEAR)      PSI/feat: cvd=0.238  vwap_position=0.034  ofi=0.006
  ```
  `ofi` PSI 가 **4거래일 연속 0.002~0.006** 으로 사실상 0이다. 다른 두 CORE(cvd 0.23·
  vwap 0.03~0.11)와 자릿수가 다르다. **분포가 안 변한 게 아니라 분포가 (거의) 한 점에
  몰려 있어 변할 여지가 없는 것**으로 읽히며, 이는 CLAUDE.md 371차 잔여이슈
  *"cvd_divergence·ofi_norm 학습표본의 91%가 정확히 0.0인 점질량"* 과 정합한다.

- **기준 위반**: 절대원칙 **§3**(단기 그룹 CORE = CVD/VWAP/**OFI**) 의 실효성.
  9단계 **STEP 4(피처 생성) → STEP 5(예측)**. 진입 차단 규칙 자체는 통과했으므로
  절대원칙 "위반"은 아니나, **CORE 3종 중 1종이 상시 무정보에 가깝다**는 것은
  체크리스트 `ofi✅` 의 의미를 약화시킨다(오늘 3건 전부 `ofi✅` 로 진입).

- **영향**: 단기 그룹 진입 판정의 근거 1/3이 사실상 상수. 다만 **오늘 거래에 직접적
  손실 경로로 이어졌다는 증거는 없다** — 3건 모두 `ofi✅` 였고, 실패 요인은
  `fore❌`×2 / `chas❌`×1 / `cvd❌`×1 쪽이다.

- **신규 여부**: **기존** — 장전 리포트 `확인 필요` + `NEXT_TODO` **O-6**(3거래일 연속
  ≥60% → 별개 안건 승격) · **G-2**(§12 `CORE축퇴_피처` 축 신설). 본 절은 **판정을
  내리지 않고 O-6 카운터를 갱신**한다: **0818 = 69% · 0819 = 100% → 2거래일 연속.**
  **08-20이 ≥60%면 3거래일 연속으로 O-6 승격 조건 충족**이다.
  근거 ③(PSI/feat 교차확인)은 **오늘 새로 붙인 것**이며 O-6의 판정 재료로 추가한다.

- **313차 원칙**: `[CORE준비도]` 는 470차 B4 배포(2026-08-14 19:47) 이후에만 존재해
  **관측일 2일**이다(§12 `⚪ 표본부족`). **확정 결론 금지.**

### P2 — 운영 부담·기술부채

#### 1-7. 스킬 체크리스트 `phases.md` B-2 가 CLAUDE.md 470차 정정을 반영하지 않았다 — "GBM 배치 재학습이 30분마다 도는가"

- **증상**: `phases.md` B-2 가 *"GBM 배치 재학습이 30분마다 도는가"* 를 묻는다.
  오늘 장중 GBM 배치 재학습은 **0건**이다. 이 체크리스트를 곧이곧대로 따르면
  **정상 상태를 결함으로 보고**하게 된다.
- **근거**: 당일 로그의 GBM 관련 라인은 기동 예약 1건뿐.
  ```
  2026-08-19 08:41:16 [INFO] SYSTEM: [WarmupRetrain] 세션 재시작 감지 → GBM 즉시 재학습 예약
  ```
  `logs/20260819_*.log` 전체에서 `[GBM] 배치 재학습` 0건.
- **기준**: **CLAUDE.md 매분 파이프라인 STEP 3 각주(2026-08-14 470차)** —
  *"STEP 3은 '30분마다'가 아니다 — 조건부 트리거다"*. 실제 조건은
  ① DriftRetrain A(5m 정확도 <25% and n≥20 and 60분 경과)
  ② DriftRetrain B(5m <15% and n≥15 and 30분 경과) ③ 주간 월요일 08:50 ④ EOD `daily_close`.
  오늘은 어느 것도 충족되지 않았다 → **0건이 정상.**
  각주는 *"2026-08-14 장중 점검이 이 문구를 근거로 STEP 3을 '판정 불가'로 올렸다"* 는
  **동일 오보 전례**까지 기록해 두었다.
- **영향**: 매 장중 점검마다 같은 오보가 재생산될 수 있다. 라이브 위험은 없다.
- **신규 여부**: **신규**(스킬 참조문서 쪽 드리프트. CLAUDE.md 본문은 이미 정정 완료).
  468차 CB③ 병기·1-5 CB⑤ 드리프트와 같은 계열이며, **코드 무변경 · 문서만** 고치면 된다.

#### 1-8. `[FaultHandler] … 행감지=30s` 라는 로그 문구가 **행 감지기가 아닌 것**을 행 감지기처럼 읽히게 한다

- **증상**: `logs/crash_fault.log` 가 오늘 **2.68MB**(12:52 기준)까지 자랐고 내부에
  `Timeout (0:00:30)!` + 전체 스레드 스택이 **1,343블록** 누적돼 있다. 기동 로그가
  ```
  2026-08-19 08:40:41 [INFO] SYSTEM: [FaultHandler] 활성화 | file=logs\crash_fault.log PID=14820 | 행감지=30s all_threads=True
  ```
  라고 알리므로, 이것만 보면 **"30초 행(hang)이 1,343번 발생했다"** 로 읽힌다.
- **근거 — 오독이다(코드가 스스로 경고하고 있다)**: `main.py:16575` · `:16637`
  ```
  `dump_traceback_later(30, repeat=True)`가 **행과 무관하게 무조건 30초마다** 쓰기
  …  **행 감지기**처럼 읽혔다. 그렇지 않다 —
  ```
  실제 덤프의 메인 스레드 스택도 정상 Qt 루프다
  (`main.py:12525 in run` → `:16745 in main`). **행이 아니다.**
  436차가 크기 기반 로테이션(`main.py:16572`)을 이미 넣어 파일 폭증도 관리되고 있다.
- **기준**: 절대원칙 위반 아님. 468차 G-2 · 470차 C1'~C5' 가 겨냥한 **"계측 문구가
  사실과 다르게 읽히는 것"** 계열이다(문서-코드 드리프트, `invariants.md` §3-B와 동형).
- **영향**: 라이브 위험 **없음**. 다만 다음 점검 세션이 이것을 P0으로 오보할 실질적
  위험이 있다 — 이번 세션도 검증 단계에서야 걸렀다.
- **신규 여부**: **신규**(코드 주석에는 있으나 로그 문구·점검 참조문서에는 없음).

### 확인 필요 (확정 아님)

- **C-1. 진입 3건 전부 `A급(원시C)` 이고 3전 3패 −443,174원(net).**
  체크리스트 등급은 A인데 확신도 등급은 C였다(`main.py:9253` 401차 병기 규칙).
  → ⚠ **`grade_ev_inversion` 채널이 이미 이 질문을 담당하며, §13 확정 결정
  레지스트리에 `부결 — GradeEVGuard 활성화하지 않음 (2026-08-01)` 으로 등재돼 있다.**
  신규 안건·신규 섀도 계측으로 올리지 않는다(0818 R-1 사고의 재발 방지 관문).
  판정은 그 채널의 `min_samples_per_grade=20` 이 찰 때. 오늘 표본 n=3·1거래일 →
  **313차 원칙 ①(일자 단위)에 따라 확정 결론 보류.**

- **C-2. 호라이즌별 채점 건수가 비대칭이다 — 퇴역한 30m만 매분 채점된다.**
  ```
  1m 221건(09:01:59~12:42:03)   3m 146   5m 87   10m 43   15m 27   30m 192(09:30:59~12:42:03)
  ```
  30m 은 첫 채점 가능 시점(09:30)부터 **192분 중 192건 = 매분** 채점된 반면,
  실제 거래 호라이즌인 3m 은 67%, 5m 40%, 10m 20%, 15m 13%에 그친다.
  같은 로그의 `[WeightCollapse]` 가 반복해서
  `활성기대=['10m','15m','3m','5m'] 중 미배포=['10m','15m','3m','5m'] (active_horizons=None)`
  를 찍는 것과 **방향이 일치**하지만, 채점 카운트 차이를 미배포만으로 설명할 수 있는지는
  **확인하지 못했다**(채점 스케줄 자체가 호라이즌별로 다를 가능성).
  → **판정 보류.** 다음에 확인할 것: `main.py` STEP 1 채점 루프가 호라이즌별로
  어떤 조건에서 도는지, 그리고 30m 채점 밀도가 **CB③ 입력(`record_accuracy`,
  `main.py:5539`) 의 분모**를 다른 호라이즌과 다르게 만드는지.
  ⚠ **30m 학습·예측 중단을 제안하지 말 것** — CLAUDE.md가 명시하듯 CB③의 유일한 입력원이다.

- **C-3. `binding_gate` 는 죽지 않았다 — 오늘 4건 모두 값이 있다.**
  §12가 `🔴 무기록` 으로 찍은 것은 §0-1i의 DB 실패 탓이다. 로그 원본:
  ```
  09:45:59 [SizerMatch] sizer=3계약 → actual=2계약 (gap=1) | binding=tox(0.70)   | kelly=0.60 meta=0.90 tox=0.70 exec=1.00
  09:56:00 [SizerMatch] sizer=3계약 → actual=2계약 (gap=1) | binding=meta(0.74)  | kelly=0.60 meta=0.74 tox=1.00 exec=1.00
  10:13:00 [SizerMatch] sizer=3계약 → actual=2계약 (gap=1) | binding=tox(0.70)   | kelly=0.60 meta=1.00(raw0.50·무정보폴백→중립) tox=0.70 exec=1.00
  11:35:00 [SizerMatch] sizer=3계약 → actual=2계약 (gap=1) | binding=hurst(0.50) | kelly=0.60 meta=1.00(raw0.50·무정보폴백→중립) tox=1.00 exec=1.00
  ```
  분포 `tox×2 · meta×1 · hurst×1` — **한 게이트 100% 고착 아님**(316차 HurstGate 형태
  아님). 장전 O-3의 중간 근거로 기록한다. 확정은 F-2 적용 후 DB 축으로.

- **C-4. `ConfFloorGuard` 상태 샘플 관측률 = 186/221 = 0.84 (장중 중간값).**
  분포 `RAW×116 · ZONE_BLACKOUT×41 · BLOCKED×20 · OK×9`.
  장전 O-5 기준(하루 전체 ≥0.9 → `bd3bbba` 완전 성공 / 0.5~0.9 → "복구 전 분기 무샘플"
  가설 채택)에 대해 **현재 0.84 = 가설 채택 구간**이나, **장후 전체값으로 판정**할 것.
  09:14:59 에 `자동진입 하한 도달 불가 — 보정기 출력상한 0.3817 < 필요 0.3840` 1건 발생 후
  09:05·이후 `state=OK 출력상한=0.3935 ≥ 필요=0.3840` 로 복구 — **BLOCKED 고착 아님.**

### 이미 반영된 사안 (신규 아님) — 함정 ① 방지

1. **§12 `binding_gate` 🔴 무기록** → §0-1i·C-3. 계측은 살아 있다. `NEXT_TODO` **F-2**.
2. **§11 적신호 #1 "사이저 최대 3계약 → 실제 최대 2계약"** → CLAUDE.md 실전전환기준 ⑧
   **470차 항목이 이미 규명**했다: 원인은 `MAX_CONTRACTS=3` 도 431차 게이트 체인도 아니고
   **브로커 증거금 상한**이다. 오늘도 그대로 재현:
   ```
   09:53:59 [MarginCap] LONG 산출=3계약 → 증거금상한=2계약으로 축소
   09:54:59 [MarginCap] LONG 산출=3계약 → 증거금상한=2계약으로 축소
   ```
   → `[28] sizing_inversion_watch` 는 **"표본이 천천히 쌓이는 중"이 아니라 "⑧ 해제
   전까지 켜지지 않는 상태"** 다. 신규 제안 금지.
3. **CB② 미발동 / `consec_stops` 카운터만 증가** → `CB_CONSEC_STOP_LIMIT=9999` 한시예외.
   **2026-08-29 재검토 확정** 사안이며 기한 미도래. "CB② 미발동"을 결함으로 쓰지 않는다.
4. **차단 게이트 7개 `False`** → 전부 한시예외/설계선택/기능토글. 복원 제안 금지.
5. **`[TimeRouter] 11:50:09 시간대 전환 → OTHER: 기타 구간 — 진입 금지`** →
   `TIME_ZONES` 11:50~13:00 정의 공백. **462차 P1-a · 채널 [53] `zone_ban_breach_watch`
   (n=7, min_samples=20) 판정 대기 중** · 26주 WFA C5' 등록분. 오늘 `OTHER` 체류 50분
   (관측 창 기준 23%)의 진입 0건은 **이상이 아니다**.
   ⚠ 위반 코호트가 흑자(+596,858원)라 **판정 전 집행(True 전환) 금지**.
6. **`WeightCollapse` 44 고유분 / 221분 = 19.9%** → `weight_collapse_watch` 채널의
   `target_ratio=0.20 ± 0.07` **범위 안(PASS 구간)**. 신규 안건 아님.
   ⚠ **집계는 고유 분(分) 기준이다** — 문자열 등장 횟수로 세면 한 분봉당 2줄이라 2배가 된다
   (`DECISION_LOG` **"43.7% 재인용 금지"** 정정). 오늘 원시 라인 수는 48, 고유 분은 44.
7. **`[SHAP] CORE안전=✅` 4/4** (10:42·11:12·11:45·12:15) → 468차 F-3 이전의
   "6거래일 100% ⚠️ 고착"에서 벗어난 상태가 유지되고 있다. **정상 확인이지 안건 아님.**
8. **FP-CRITICAL PSI 로그 0건** → `main.py:5951` 이 **PSI>0.20 일 때만** WARNING을 찍는
   조건부 로그다. `[RegimeFingerprint] 스킵` DEBUG도 0건이므로 **예외 없이 계산되고 있고
   값이 ≤0.20 이라는 뜻**이다. 0818 세션의 **O-7 이 이미 "PSI는 살아 있다"로 종결**했다
   (`DECISION_LOG` 24404행). 신규 안건 아님 — 다만 §3i **GI-1** 로 *다일(多日) 계열화*만 제안한다.

---

## 2i. Fix 작업 구현계획 (장중 — **수립만. 적용하지 않는다**)

> 🔴 **라이브 프로세스(PID 14820)가 돌고 있다.** 아래는 전부 **장후(15:10 강제청산 이후)**
> 적용 대상이며, 오늘 세션에서는 파일을 고치지도 커밋하지도 않았다.
> 장전 절의 F-1~F-4 와 번호 충돌을 피하려 **`FI-`** 접두를 쓴다.

### FI-1. `phases.md` B-2 "GBM 배치 재학습이 30분마다 도는가" → 조건부 트리거로 정정 — P2 · 대응 이상점 1-7

- **원인 가설**: CLAUDE.md STEP 3 각주는 470차(2026-08-14)에 정정됐으나 스킬 참조문서
  `phases.md` 는 그때 함께 갱신되지 않았다. 468차 CB③ 병기 때와 같은 누락 형태다.
- **변경 파일**:
  - `.claude/skills/mireuk-daily-check/references/phases.md` — **B-2 두 번째 체크박스**
    `"[ ] GBM 배치 재학습이 30분마다 도는가"`
    → `"[ ] GBM 배치 재학습 — **조건부 트리거**다(CLAUDE.md STEP 3 각주, 470차).
       발동 조건: DriftRetrain A(5m acc<25% and n≥20 and 60분 경과) / B(5m acc<15% and
       n≥15 and 30분 경과) / 주간 월 08:50 / EOD daily_close.
       **장중 0건은 조건 미충족(=정상)이며 결함이 아니다** — 2026-08-14 장중 점검이
       이 문구 때문에 STEP 3을 '판정 불가'로 오보한 전례가 있다."`
  - `.claude/skills/mireuk-daily-check/references/invariants.md` — §3-B 표에
    **"STEP 3 재학습 주기"** 행 추가(서술 `30분마다` / 실제 `조건부 트리거` /
    상태 `해소(470차, CLAUDE.md 본문)` · **참조문서 미반영은 476차에 정정**).
- **회귀 위험**: 없음(문서 전용). 수집기·코드 무변경.
- **절대원칙 저촉 여부**: 없음 — 6종 어느 것도 건드리지 않는다.
- **런타임 주의**: 해당 없음(마크다운).
- **검증 방법**: 다음 장중 점검(08-20)에서 GBM 0건을 결함으로 올리지 않는지 확인.
- **섀도 계측 필요 여부**: 불필요(게이트 신설 아님).
- **적용 시점**: **장후.** 라이브 무관하나 오늘은 `.git/index.lock` 스테일로 커밋 불가
  (장전 §9 참조) — 사용자 조치 후 함께 커밋.
- **결정 필요 사항**: 없음.

### FI-2. `[MarginCap]` 을 무조건 상태 샘플로 승격 — P2 · 대응 이상점 (2i "이미 반영" 2번의 관측 결손)

- **원인 가설**: `[MarginCap]` 은 **축소가 발생했을 때만** 찍히는 조건부 로그다.
  그래서 "증거금 상한이 3을 2로 잘랐다"는 사실은 보이지만 **"오늘 증거금 상한이 얼마였나"**
  는 축소가 없는 날 전혀 남지 않는다. 26주 WFA **C2'** 가 *"⚠ `MarginCap` 은 조건부
  로그라 §5 규약 위반 — 무조건 상태 샘플을 먼저 만들 것"* 으로 **이미 등록한 항목**이며,
  오늘 5건→2건으로 줄어든 채 관측돼 그 필요성이 재확인됐다.
- **변경 파일**:
  - `main.py:16182~16202` (`get_order_available_qty()` → `if margin_qty < qty:` 블록) —
    축소 여부와 무관하게
    분당 1회 `[MarginCap] state= avail_qty=N sizer_qty=N capped=<True|False>` 형태의
    **무조건 상태 샘플**을 INFO로 남긴다(축소 시 기존 메시지는 그대로 유지).
  - `config/dailycheck_targets.json` — `stuck_indicators.patterns` 에
    `margin_cap_qty` 추가(C2' 등록분 3종 중 하나). ⚠ **패턴은 새 무조건 로그를 겨냥할 것** —
    기존 조건부 문구를 넣으면 100% 고착이 구조적으로 보장된다(468차 G-2 경고).
  > ⚠ **CLAUDE.md ⑧ 항목이 인용한 `main.py:15741 get_order_available_qty()` /
  > `:15757 margin_qty < qty` 는 행 번호가 낡았다** — 2026-08-19 실측 위치는
  > **`:16182` / `:16198~16202`** 다(470차 이후 `main.py` 가 늘어난 결과).
  > 함수·조건식 자체는 동일하므로 **결론은 유효**하다. 문서 행번호만 정정 대상.
- **회귀 위험**: 로그량 증가(하루 ~370줄). `main.py` 는 라이브 본체이므로 **장중 적용 절대 금지.**
- **절대원칙 저촉 여부**: 없음. **COM 콜백 내부가 아니다**(주문 가능수량 조회 경로) —
  절대원칙 ④의 `dynamicCall`·`emit` 금지 구간이 아님을 확인할 것.
- **런타임 주의**: **`py37_32`** (라이브 본체). 재학습 경로 아님.
- **검증 방법**: 다음 거래일 §12에 `margin_cap_qty` 가 **관측률 ≥0.9 로 값 분포를 내는지**.
  값이 한 개면 `고착`이 아니라 **정상 고착(benign)** 으로 등록할지 판단.
- **섀도 계측 필요 여부**: 해당 없음(계측 자체가 목적, 차단 신설 아님).
- **적용 시점**: **장후.** 단 ⑧ 사이징 재설계 논의 **전에** 들어가 있어야 의미가 있다
  (실전 자본 재설정 순간 증거금 상한이 풀리는 그 시점을 재려면 그 전 기준선이 필요하다).
- **결정 필요 사항**: 분당 1회 vs 진입 시도 시에만 — **권고: 분당 1회**(§12 관측률 축이
  분 단위 분모를 쓰므로).

### FI-3. `[FaultHandler]` 로그 문구 `행감지=30s` → `주기덤프=30s(행감지 아님)` — P2 · 대응 이상점 1-8

- **원인 가설**: 230차 도입 시의 문구가 그대로 남았고, 436차가 코드 주석으로만
  *"행 감지기가 아니다"* 를 바로잡았다. **로그를 읽는 쪽에는 그 정정이 전달되지 않는다.**
- **변경 파일**:
  - `main.py:16715~16719` — `"행감지=30s all_threads=True"` →
    `"주기덤프=30s(행 감지 아님 · repeat=True 무조건 기록) all_threads=True"`.
    ⚠ 로그 **문자열만** 바꾼다. `dump_traceback_later` 인자·로테이션 로직 무변경.
  - `.claude/skills/mireuk-daily-check/references/evidence_map.md` —
    `crash_fault.log` 항목에 *"`Timeout (0:00:30)!` 블록 수는 행 횟수가 아니다.
    30초 주기 무조건 덤프이며, 크래시 판정은 `0xC0000409` /
    `STATUS_STACK_BUFFER_OVERRUN` 문자열로 한다"* 를 명기.
- **회귀 위험**: 없음(문자열). 단 이 문구를 파싱하는 곳이 없는지 확인 —
  `grep -rn "행감지" --include=*.py .` 로 소비처 0건임을 배포 전 재확인할 것.
- **절대원칙 저촉 여부**: 없음.
- **런타임 주의**: **`py37_32`** (라이브 본체 기동부). **COM 콜백 아님.**
- **검증 방법**: 다음 기동 로그에 새 문구가 뜨는지 1줄 확인.
- **섀도 계측 필요 여부**: 불필요.
- **적용 시점**: **장후**(기동부 수정이라 다음 기동부터 반영).
- **결정 필요 사항**: 없음.

### 적용 순서와 커밋 계획 (장후)

1. **먼저 사용자 조치** — `.git/index.lock` 삭제 (장전 §9. 샌드박스가 unlink 불가).
   ⚠ **다른 git 프로세스 때문이 아니다. 라이브 프로세스를 죽이지 말 것.**
   현재 락 mtime 은 **09:07**(장전 세션이 만든 것) — 장중 세션은 git 을 쓰기(write)로
   호출한 적이 없다.

   > 🔴 **`NEXT_TODO` F-5(`git --no-optional-locks status` 로 교체) 구현 시 함정** —
   > `-c core.autocrlf=true` 를 **반드시 함께 유지**해야 한다. 장중 세션이 실측했다:
   > ```
   > git -c core.autocrlf=true status --porcelain   →  10건   (수집기 §2, 정답)
   > git --no-optional-locks   status --porcelain   →  100건+ (README.md·CORE.md·
   >                                                    ROADMAP.md·.gitignore 까지 전부 M)
   > ```
   > 리눅스 샌드박스에서는 CRLF 차이 때문에 거의 모든 텍스트 파일이 `M` 으로 뜬다.
   > 두 옵션은 **직교**하므로 `git -c core.autocrlf=true --no-optional-locks status
   > --porcelain` 형태로 **둘 다** 붙일 것. 한쪽만 바꾸면 §2 미커밋 건수가 100배로
   > 부풀어 다음 세션이 "누가 repo 전체를 고쳤다"고 오보한다.
2. `[MW0602] 476차: 0819 장중 점검 리포트 + evidence` — 본 절 · 라이브 무관
3. `[MW0602] 476차: phases.md B-2 STEP3 조건부 트리거 정정 + invariants §3-B 등재` — **FI-1**
4. `[MW0602] 476차: MarginCap 무조건 상태 샘플 + §12 margin_cap_qty (26주 WFA C2')` — **FI-2**
5. `[MW0602] 476차: FaultHandler 로그 문구 정정(행감지→주기덤프) + evidence_map 주석` — **FI-3**
   → **15:10 강제청산 확인 이후**, 가능하면 15:47 EOD 완료 후.
   장전 F-1(EOD 사이드카 `label_scheme`)과 **같은 배포 창**에 넣되 커밋은 분리한다.

> 장중 국면이므로 **본 계획은 수립만 하고 적용하지 않았다.** 라이브 프로세스가 돌고 있다.

---

## 3i. 고도화 방안 (장중 관측 출발)

> 장전 절의 G-1~G-3 과 번호 충돌을 피하려 **`GI-`** 접두를 쓴다.
> ⚠ 작성 전 §13 확정 결정 레지스트리(18건) 및 `VALIDATION_CAMPAIGN` 51키와 대조했다 —
> 같은 질문의 채널이 있는 항목(`grade_ev_inversion` · `weight_collapse_watch` ·
> `sizing_inversion_watch` · `zone_ban_breach_watch`)은 **제안하지 않고 인용만** 했다.

### GI-1. EOD 전략 배너의 `PSI` / `PSI/feat` 를 **다일 계열**로 뽑는다 — 실전전환기준 ⑦의 유일한 미충족 증거

- **관측 근거**: 오늘 PSI WARNING 0건이라 로그만 보면 "값을 모른다". 그런데
  `data/daily_reports/strategy_report_*.txt` 에는 **매 EOD 1점씩 이미 쌓여 있고**,
  최근 4거래일이 **0.110(WATCHLIST) → 0.063 → 0.036 → 0.034(CLEAR)** 로
  **실제로 오르내린다.** CLAUDE.md 전환기준 ⑦의 복원 조건이 정확히
  *"정상 구간에서 PSI가 오르내리는 것을 실측 확인"* 이다.
- **제안 내용**: `scripts/` 에 `psi_daily_series.py` 를 신설해
  `data/daily_reports/strategy_report_*.txt` 를 파싱, `(date, psi, level, per_feature)`
  시계열 CSV/JSON을 만들고 **금요일점검 EOD 체인**에 붙인다
  (`docs/정기점검/금요일점검/MW0602/psi_daily_series_YYYYMMDD.json`, 날짜본 규약 준수).
  주간 리포트에 **① 일자별 PSI 추이 ② 레벨 전이 횟수 ③ 피처별 PSI 순위**를 낸다.
- **기대 효과**: ⑦의 복원 판정을 "느낌"이 아니라 **N거래일 시계열**로 낼 수 있다.
  0818 O-7 이 "라이브 1일차 근거"라고만 적었던 것을 다일 근거로 승격시킨다.
  부수적으로 **1-6(`ofi_norm` 축퇴)의 독립 증거**가 자동 축적된다(`ofi` PSI ≈ 0 지속 여부).
- **비용·위험**: 낮음 — 기존 텍스트 파싱, 코드 경로 무변경, **라이브 무관**.
  위험은 파일명·배너 문구가 바뀌면 조용히 죽는 것 → 26주 WFA **C1'/C2'** 계열로
  "원천 문구 검증" 항목에 함께 등록할 것.
- **선행 조건**: 없음(과거 파일이 이미 있다). 다만 **⑦의 합격선(몇 거래일, 어떤 변동폭을
  "오르내린다"로 볼 것인가)은 사전등록이 필요**하다 — 313차 원칙 ④. **판정 기준을 먼저
  정하고 데이터를 보라.**
- **우선순위 제안**: **이번 주**(2026-08-22 금 주간회의 상정). 구현 자체는 장후 1시간.

### GI-2. 호라이즌별 **채점 밀도**를 §5/§12에 노출한다 — C-2를 매번 손으로 세지 않으려면

- **관측 근거**: C-2 의 `1m 221 / 3m 146 / 5m 87 / 10m 43 / 15m 27 / **30m 192**` 는
  이번 점검에서 **손으로 grep 해서야** 보였다. 수집기 어디에도 이 축이 없다.
  "퇴역한 30m 만 매분 채점된다"는 것이 사실이라면 **CB③(절대원칙 §2)의 입력 밀도가
  다른 호라이즌과 구조적으로 다르다**는 뜻이므로 상시 관측 대상이다.
- **제안 내용**: `collect_evidence.py` §5 에 **호라이즌별 채점 밀도** 소절 추가 —
  `LEARNING.log` 의 `✓/✗ <hz> 예측` 을 호라이즌별로 세고 **`건수 / 채점가능분`** 비율을
  함께 낸다(채점가능분 = 첫 채점 시각~마지막 루프분). 30m 행에는
  *"가중 0.0(296차 퇴역)이나 CB③의 유일한 입력원 — 밀도 저하는 CB③ 분모 변화"* 주석을 고정한다.
- **기대 효과**: ① CB③ 입력이 조용히 끊기는 것을 당일에 잡는다(292·303·371·468차와
  같은 실패 패턴의 예방) ② C-2 판정에 필요한 **일자단위 표본**이 자동으로 쌓인다.
- **비용·위험**: 낮음(수집기 전용, 라이브 무관). 위험은 로그 문구 변경 시 침묵 —
  §12 `무기록` 규약과 동일하게 **0건이면 `무기록`으로 표시**할 것.
- **선행 조건**: **없음.** ⚠ 단 **C-2의 원인 규명이 먼저**다 — 채점 스케줄이 호라이즌별로
  다른 것이 설계라면 "비율"의 기준선부터 달라진다. 규명 없이 임계를 걸지 말 것.
- **우선순위 제안**: **이번 주**(FI-2와 같은 장후 배포 창).

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| **⑦ FP-CRITICAL 재검토** | "371차 재설계 후 라이브 관찰 중" (기한 없음) | **GI-1 로 다일 PSI 계열을 만들고, 2026-08-22 주간회의에서 "몇 거래일·어떤 변동폭이면 복원인가"를 사전등록** | 근거는 이미 4거래일치 존재(0.110→0.034)인데 합격선이 없어 영구 관찰 상태다. ④(합격선 미정의)와 같은 형태의 정체 |
| **26주 WFA C2'** (§12 사이징 축 3종) | "470차 시점 미구현" | **FI-2 로 `margin_cap_qty` 부터 착수** | ⑧ 해제 순간 증거금 상한이 풀리는데, 그 전 기준선이 없으면 [28] 이 깨어나도 비교 대상이 없다 |
| **26주 WFA — 신규 후보** | — | **"호라이즌별 채점 밀도"(GI-2)를 C7' 로 신설 검토** | CB③ 입력원 감시. 468차 G-2·470차 C1'~C5' 와 같은 "계측이 조용히 죽는 것"을 겨냥 |
| **O-6 (`ofi_norm` 축퇴)** | `NEXT_TODO` 관측 항목 | **08-20 판정 시 근거 ③(EOD `PSI/feat ofi≈0`)을 함께 볼 것** | 로그 축과 DB/배너 축의 **독립 2원천**이 같은 방향을 가리키면 표본부족을 일부 상쇄한다 |

---

## 4i. 진입 승패 사후검증

**해당 없음(장후 전용).** — 단 장후 점검에서 쓸 재료를 남긴다:

| 진입 | 방향 | 계약 | 손절선 | 의도 손절폭 | 최악 실현폭 | **손절 준수** | 포지션 합(net) |
|---|---|---|---|---|---|---|---|
| 09:45:59 | LONG | 2 | 1014.97 | 4.15pt | −1.79pt (43%) | ✅ | −52,056원 |
| 09:56:00 | LONG | 2 | 1017.75 | 3.93pt | −3.91pt (**99.5%**) | ✅ (경계) | −308,066원 |
| 11:35:00 | SHORT | 2 | 1020.43 | 2.51pt | −2.05pt (82%) | ✅ | −83,052원 |

> **3/3 준수.** 417차 재분해가 지목한 유일한 유의 축(진입수량 vs 손절폭 초과비율
> rho=+0.318)에서 오늘은 초과 0건이다. ⚠ 전부 `qty=2` 이므로 **qty≥3 구간에 대한
> 정보는 0**이다(증거금 상한이 2에서 자른다 — "이미 반영" 2번).
> ⚠ 청산 사유 `하드스톱(틱)` 3건 중 **2건은 TP1 보호 트레일(이익 청산)** 이다.
> `exit_reason` 만으로 손절 3건이라고 세지 말 것.

## 5i. 수익률 향상방안

**해당 없음(장후 전용).**
⚠ 장후 세션 주의: C-1(3전3패 `A급(원시C)`)을 R-* 로 올리지 말 것 — `grade_ev_inversion`
**부결 확정(2026-08-01)** 사안이다.

---

## 6i. 다음 관측 예정 (장중 발)

> 장전 O-1~O-8 과 충돌하지 않도록 **`OI-`** 접두를 쓴다.

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| OI-1 | `[SchedForceExit]` · `[ForceExitPass]` · `[TimeExit]` (`{d}_TRADE.log`·`WARN.log`) | 15:10 시점 포지션이 **있으면** 1차 경로 정상 출현 / **없으면** 하트비트 `bar_pass=N회`만. ⚠ 15:11 에 `안전망 발동`(ERROR)이 뜨면 471차 F-1이 다시 죽은 것 | **오늘 장후** |
| OI-2 | `[ConfFloorGuard] state=` 하루 전체 관측률 | 장중 중간 **0.84**. 하루 전체 ≥0.9 → `bd3bbba` 완전 성공 / 0.5~0.9 → "복구 전 분기 무샘플" 가설 채택 (장전 **O-5 승계**) | 오늘 장후 |
| OI-3 | `[CORE준비도]` `ofi_norm` 포함률 | 0818 **69%** · 0819 **100%** → **2거래일 연속 ≥60%.** 08-20 도 ≥60%면 **O-6 승격 조건 충족** | **08-20** |
| OI-4 | 호라이즌별 채점 건수 (`✓/✗ <hz> 예측`) | 오늘 `1m 221 / 3m 146 / 5m 87 / 10m 43 / 15m 27 / 30m 192`(창 221분). **30m 밀도 ≈ 1.00 이 재현되는지**, 3m 이 0.67 근방인지 | 08-20 |
| OI-5 | `WeightCollapse` **고유 분** 비율 | 오늘 **44/221 = 19.9%** → `weight_collapse_watch` target 0.20±0.07 내. 장후 종가 기준으로도 밴드 내인지. ⚠ 라인 수(48)로 세지 말 것 | 오늘 장후 |
| OI-6 | `[MarginCap]` 건수·축소폭 | 오늘 **2건(3→2)**. 0814는 5/5. **⑧ 해제 전까지는 계속 3→2 로 잘릴 것** — 3이 통과하는 날이 나오면 그것이 이상 신호다 | 매일 |
| OI-7 | EOD 배너 `PSI` / `PSI/feat` | 오늘 값 미확정(장중 WARNING 0건 = ≤0.20). **≤0.20 이면 371차 재설계 유지 확인**, `ofi` 항이 여전히 ≤0.01 이면 1-6 근거 ③ 강화 | 오늘 장후 |

---

## 7i. 기한 도래 항목 (장중 재확인 — 장전과 동일, 변동 없음)

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| **CB② 복원** (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | **2026-08-29** | **10일** (거래일 8일) | 🔴 **2026-08-22(금) 주간회의 안건.** 오늘 `[CB] 연속 손절 1회`가 실제로 찍혔다 — 2~3 복원 시 오늘 같은 날 당일 정지가 걸릴 수 있음을 정량으로 제시할 재료 |
| TOX-SEVERE-SPREAD (전환기준 ⑨) | 실전 전환 전 | — | 주간회의에서 "선행조건 유지 vs 26주 WFA 이관" 결정. 오늘 `spread_extreme` 관련 로그 0건 — 표본 ETA 7.1개월 그대로 |
| ⑦ FP-CRITICAL 재검토 | 실전 전환 전 | — | **본 절 GI-1 — 합격선 사전등록을 주간회의 상정** |
| ⑧ 사이징 재설계 | 실전 전환 전 | — | 장전 P1-1(F-1→O-2)이 선행. **본 절 FI-2 를 그 앞에 넣을 것** |
| [25] `atr_lock_0.75` watch — MW0601 축적 | 2026-08-21(금) | 2일 | 변동 없음 |

---

## 8i. dev_memory 반영

**장중에는 커밋하지 않는다** — 스킬 규약대로 dev_memory 갱신은 **장후 점검에서 일괄** 수행한다.
장후 세션이 반영할 내용을 여기 예약해 둔다.

- `DECISION_LOG.md` 추가 예정: `## 2026-08-19 (MW0602 476차 — 장중 점검)` 아래
  **[1] `ofi_norm` 축퇴 100% + EOD PSI/feat 교차확인(O-6 근거 ③ 추가)**
  · **[2] `phases.md` B-2 STEP3 문서 드리프트** · **[3] 확인만 하고 판정하지 않은 것 (C-1~C-4)**
  · **[4] 이미 반영·확정돼 안건으로 올리지 않은 것 (8건)**
- `NEXT_TODO.md` 추가 예정 체크박스: **12건** (FI-1·FI-2·FI-3 / GI-1·GI-2 / OI-1~OI-7)
- 완료 처리 예정: 없음(장중 판정 없음)
