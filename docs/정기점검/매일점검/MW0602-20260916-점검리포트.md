# 미륵이 일일 점검 — MW0602 / 2026-09-16 / 장전

- 점검 시각: 08:59~09:02 KST · PC `MW0602`
- HEAD `34998ac` · 미커밋 0건 · 당일 커밋 0건
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260916_pre.md`

## 0. 한 줄 결론

**오늘 미륵이는 정상적으로 거래할 준비가 됐습니다.** 프로그램은 설계된 시간표대로 켜졌고, 어제 밤사이 모델 재학습도 성공했고, 시세 수신도 개장 전에 미리 시작됐습니다. 새로 생긴 위험 신호는 없고, 예전부터 알고 있던 몇 가지 숙제(담당자 결정을 기다리는 항목들)만 그대로 남아 있습니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 정상 | 기동·모델로드·Cybos연결·매크로/레짐확정·설정값 전부 정상. 새 이상점 없음 |
| 장중 | (장중 세션에서 기록) | |
| 장후 | (장후 세션에서 기록) | |

**절대원칙 준수 여부** (장전 시점 — 아직 매매가 시작되지 않아 진입 관련 원칙은 "해당 없음")

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | 해당 없음 | 장전 — 포지션 없음(`레짐=NEUTRAL 포지션=FLAT`) |
| ② | Circuit Breaker 5종 | 정상 | `CB_CONSEC_STOP_LIMIT=9999`(모의 한정 예외, 의도값과 일치) 외 CB 상수 전부 일치 |
| ③ | CORE 피처 호라이즌 그룹별 | 정상(섀도 관측만) | 워밍업 중 CORE 축퇴 발생했으나 08:58:59 기준 1/6로 회복 중 — §1 확인 필요 참조 |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 해당 없음 | 코드 미변경 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 해당 없음 | 코드 미변경 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 정상 | 자동 큐·자동 통합 관련 로그 없음 |

**설정 불변식** — 수집기 §3 표 24행 **전부 `일치`**. 이상 없음.

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

없음.

### P1 — 정확성·관측 훼손

없음 (새로 발견된 것 없음).

### P2 — 운영 부담·기술부채

없음 (새로 발견된 것 없음). 기존 P2/P1 항목은 아래 "이미 반영된 사안"·"기존 TODO" 참조.

### 확인 필요 (확정 아님)

#### 1-1. 개장 직전 워밍업 중 CORE 피처가 잠깐씩 "대체값"으로 계산됨

- **증상**: 08:45:21에 CORE 피처(방향 예측에 항상 써야 하는 핵심 피처) 6개 호라이즌 중 최대 4개가 "값이 거의 안 움직인다"는 이유로 정상값 대신 평평한 대체값(항등변환)으로 계산됐습니다. 08:47:59부터는 `ofi_norm`(호가 불균형 피처) 1개만 이 상태로 좁혀졌고, 08:58:59(마지막 관측)까지도 그 1개는 계속 대체값 상태였습니다.
- **근거**:
  ```
  2026-09-16 08:45:21 [WARNING] SIGNAL: [CORE준비도] trigger=A_WARMUP 축퇴 4/6 — above_vwap×6hz cvd_divergence×6hz ofi_norm×6hz opt_chain_pcr×6hz | 정상 진입 전 해소 여부를 확인하라
  2026-09-16 08:58:59 [WARNING] SIGNAL: [CORE준비도] trigger=A_WARMUP 축퇴 1/6 — ofi_norm×6hz | 정상 진입 전 해소 여부를 확인하라
  ```
  `logs/20260916_SIGNAL.log` · `ScalerRefresh`/`CORE준비도` 태그 53건
- **기준 위반**: CLAUDE.md 절대원칙 ③(CORE 피처는 호라이즌 그룹별로 항상 의미 있는 값이어야 함). 단, 이 계측 자체가 "섀도(관측만, 차단 없음)"이므로 즉시 원칙 위반으로 단정하지 않음
- **영향**: 장이 열리기 직전 몇 분 동안, 프로그램이 방향을 판단할 때 쓰는 핵심 재료 하나(호가 불균형)가 "변화 없음"으로 단순화된 값을 썼을 수 있습니다. 09:00 개장 직후 첫 판단 몇 번이 평소보다 덜 정교했을 가능성이 있지만, 실제로 진입 여부나 손익에 영향을 줬는지는 장중 로그로 확인이 필요합니다.
- **영향(기술)**: `ScalerRefresh` 가 `ofi_norm` raw_std≈0(0.0364, n=30, frac_zero=0.033)을 감지해 1m/3m/5m/10m/15m/30m 전 호라이즌에 identity(0,1) 스케일러를 강제 적용. `_CORE_MASK_EXEMPT_BY_HZ`(AutoMask 면제) 대상 여부와 무관하게 스케일러 단에서 이미 평탄화됨.
- **신규 여부**: 기존 관측 축 — 수집기 §12 `CORE축퇴_개장해소`(494차 G-1, 계측 08-14~)가 매일 이 패턴을 추적 중이며 대부분 "개장 후 해소"로 귀결(과거 8거래일: 무축퇴 4 · 해소 2 · 미해소 1 · 미관측 1). 오늘 몫은 **장중 절에서 09:00~09:05 로그로 해소 여부를 확인**할 것 — 확인 전까지는 판정 보류(313차 원칙).

### 이미 반영된 사안 (신규 아님)

아래는 수집기 §11·§12가 자동으로 띄운 항목이지만, 전부 이미 원인 규명·등록이 끝난 상태라 **신규 이상점으로 올리지 않습니다** (함정① — 같은 것을 새로 발견한 척 보고하지 않기 위한 절).

- **고착 지표 5종**(`CORE안전`✅100%·`GuardFair_유효`무효100%·`전략판정`INSUFFICIENT100%·`sgd_alpha`0.01000 100%·`drift_action`DRIFT_UP_SATURATED 100%) — `dev_memory/NEXT_TODO.md`·`DECISION_LOG.md`에 "기존 규명 완료" 상태로 반복 확인됨(예: NEXT_TODO.md:21441). `CORE안전`은 원래 정상 고착(안전장치가 계속 켜져 있다는 뜻)이고, 나머지 4종은 이미 등록된 결함(2026-08-24~26 세션에서 원인 확정)입니다.
- **`launcher_guard` 발화(고착 5/5일)** — 이미 `O-17`로 **P1 승격·기록 완료**된 사안(NEXT_TODO.md 다수 항목: "이미 P1 승격·기록됨, 재론하지 않음"). 오늘 값도 같은 상태의 연속이며 재승격하지 않습니다. `F-1`(런처 GUARD `!=` 오작동 수정 여부)이 여전히 사용자 결정 대기 중(15일째)입니다.
- **`CybosProbe` dispatch 실패 10건**(08:58:24·08:58:54) — `O-8`로 이미 **종결 확정**("매일 같은 패턴이면 정상", DECISION_LOG.md:31448). 기존 등록 `F-3`(P2, 새 안건 아님).
- **`ENTRY_HORIZON_B1/B2`, `HURST_WINDOW_N/MAX_LAG` 등 26주 WFA 드리프트 항목** — CLAUDE.md에 이미 다음 재검증 주기 등록됨. 오늘은 값 확인만(변경 없음).

---

## 2. Fix 작업 구현계획

오늘 장전 시점에 새로 필요한 Fix는 없습니다. (장전이므로 설령 있었어도 적용하지 않고 계획만 세우는 것이 원칙입니다 — 라이브 프로세스가 이미 돌고 있기 때문입니다.)

기존에 열려 있는 항목(오늘 점검에서 재확인만, 새 계획 아님):

- **F-1(기존)** 런처 GUARD가 `cmd == 의도한 실행경로`가 아니라 `"main.py" in cmdline` 방식으로 프로세스를 판별해, 이론상 같은 PC의 다른 파이썬 트레이딩 프로그램(`auto_trader_kiwoom` 등)을 잘못 종료시킬 수 있음. **15일째 사용자 결정 대기** — 이 점검에서 코드를 건드리지 않습니다.
- **F-2(기존)** 장전 예약 실행 시각(현재 08:58:40 기준)을 더 여유 있게 당길지 여부 — 사용자 승인 대기 지속.
- **F-2026-09-14 계열 test_457 실패(기존)** — `tests/test_457_fallback_visibility.py`의 한 테스트가 2026-09-14 MW0601 체리픽(`f6613c3`, 표시 전용) 이후로 계속 실패 중. 원인은 MW0601 쪽 대시보드 코드(`peter_paste` INSERT)이고 이 브랜치(`dev`) 관점에서는 근거 없이 예외 등록을 하는 것이 "규약 우회"라 보류 중(C등급, MW0601 결정 필요).

### 적용 순서와 커밋 계획

해당 없음 — 오늘 장전 코드 변경 없음.

---

## 3. 고도화 방안

당일 관측에서 새로 도출된 것은 없습니다. 기존에 등록된 고도화 제안(26주 WFA 재검증 항목, 주간회의 안건들)은 CLAUDE.md·NEXT_TODO.md에 이미 반영되어 있어 중복 제안하지 않습니다.

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| 해당 없음 | — | — | 오늘 장전 관측에서 새로 도출된 항목 없음 |

---

## 4. 진입 승패 사후검증 (장후 한정)

해당 없음(장후 전용) — 장전 시점이며 오늘 진입 0건(개장 전).

---

## 5. 수익률 향상방안 (장후 한정)

해당 없음(장후 전용).

---

## 6. 다음 거래일 관측 예정

다음 관측 ID는 **`O-82`**부터 발급합니다(수집기 §11 확인).

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| — | (오늘 장전은 신규 관측 항목 없음 — §1-1은 기존 축 `CORE축퇴_개장해소`로 이미 추적 중) | — | 장중 절에서 09:00~09:05 로그로 확인 |

## 7. 기한 도래 항목

`references/invariants.md` §2 재검토 기한 대조 — 오늘 날짜(2026-09-16) 기준 새로 도래한 항목 없음.

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| CB② 복원 (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | 사건 기한(⑧ 해제와 동일 커밋) — 날짜 아님 | — | 489차 D2, 변경 없음 |
| TOX-SEVERE-SPREAD 26주 재검증 | 다음 26주 WFA 주기 | — | 473차 F-8, 변경 없음 |

---

## 8. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-09-16 (MW0602 571차 — 장전 일일 점검)` — 아래 별도 기록
- `NEXT_TODO.md` 추가 체크박스: 0건 (오늘 장전 신규 이상점 없음)
- 완료 처리한 기존 항목: 없음

---

## 사용자 조치

| # | 할 일 | 명령·경로 | 기한 |
|---|---|---|---|
| 1 | (없음) 오늘 장전은 사용자가 지금 당장 해야 할 조치가 없습니다. | — | — |

> 참고 — 계속 쌓이고 있는 기존 숙제(오늘 새로 생긴 것 아님, 그대로 대기 중): `F-1`(런처 GUARD 오작동 수정 여부, 15일째) · `F-2`(장전 예약 시각 조정 승인, 누적) · `1-2`(3m 모델 교체 보류, 9거래일 연속) — 필요시 주간회의에서 한 번에 정리하는 것을 권합니다.

---

# 장중(intra)

- 점검 시각: 12:40~13:05 KST · PC `MW0602`
- HEAD `34998ac` · 미커밋 3건(`dev_memory/DECISION_LOG.md`·`NEXT_TODO.md` 수정분 + 오늘자 리포트 신규) · 당일 커밋 0건
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260916_intra.md`

## 0i. 한 줄 결론

**09:00부터 지금(12:40)까지 미륵이는 정상적으로 동작하고 있습니다.** 오늘 매매는 한 번 있었고, 이익으로 끝났습니다(짧게 들어갔다가 목표가 근처에서 안전하게 빠져나와 27,042원을 벌었습니다). 장전에 남아 있던 유일한 확인사항(개장 전 잠깐 계산이 단순화됐던 문제)은 개장과 동시에 정상으로 돌아온 것이 로그로 확인됐습니다. 새로 생긴 위험 신호는 없습니다.

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 정상 | (위 §0 참조) |
| 장중 | 정상 | 매분 루프 100% 생존, 거래 1건 이익 종료, CB 실발동 없음, 새 이상점 없음 |
| 장후 | (장후 세션에서 기록) | |

**절대원칙 준수 여부** (장중 12:40 시점 스냅샷 — 종가 기준 최종 판정은 장후에서)

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | 해당 없음(아직 미도래) | 현재 포지션 FLAT — 12:40 기준 청산 완료 상태 |
| ② | Circuit Breaker 5종 | 정상 | 실발동 0건. CB③-P4는 섀도(관측만) 단계 전환 1회(아래 §1i "이미 반영된 사안") |
| ③ | CORE 피처 호라이즌 그룹별 | 정상 | 09:00:58 개장과 동시에 축퇴 0/6으로 회복, 진입 건에 VWAP 미통과 없음 |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 정상 | `0xC0000409`·STACK_BUFFER 관련 로그 0건, 프로세스 무응답 0건 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 해당 없음 | 코드 미변경 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 정상 | 자동 큐·자동 통합 관련 로그 없음 |

**설정 불변식** — 수집기 §3 표 24행 **전부 `일치`**. 이상 없음.

---

## 0i-C. 이월 처리 — 앞 국면(장전) 항목 처분

| 원 번호 | 제목(줄임) | 원 국면 | 처분 | 근거 |
|---|---|---|---|---|
| 1-1 | 개장 직전 CORE 피처 워밍업 축퇴(최대 4/6, 이후 `ofi_norm` 1/6) | 장전 | ✅ **해소** | `logs/20260916_SIGNAL.log` 09:00:58 `[CORE준비도] trigger=B_OPEN 축퇴 0/6 — CORE 스케일 정상`. 08:58:59(장전 마지막 관측) `ofi_norm` 1/6 → 개장 즉시 0/6으로 완전 회복. 기존 계측 축 `CORE축퇴_개장해소`(494차 G-1) 과거 8거래일 실적(무축퇴4·해소3·미해소1)에 "해소" 1건 추가 |

- **미처분 0건** — 장전이 등록한 항목은 1-1 하나뿐이었고 위와 같이 처분했다. 장전이 신규 발급한 `O-*`/`P-*`는 없었다(다음 관측 ID는 여전히 `O-82`부터).

---

## 1i. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

없음. `[진입체크]` 원문을 직접 확인한 결과, 오늘 유일한 진입(10:09:59 SHORT 2계약)의 체크리스트 미통과 항목은 `fore`(외국인 수급)·`chas`(추격) 2개뿐이며 **`vwap` 미통과 진입은 0건**이다(절대원칙 ③ "강제 X" 규칙 준수 확인).

### P1 — 정확성·관측 훼손

없음(신규 없음).

### P2 — 운영 부담·기술부채

없음(신규 없음).

### 확인 필요 (확정 아님)

없음.

### 이미 반영된 사안 (신규 아님)

아래는 오늘 로그에 나타났지만 전부 기존에 원인이 규명됐거나 설계상 정상인 패턴이라 신규 이상점으로 올리지 않는다(함정①).

- **CB③-P4 섀도 단계 전환(10:44:58, NORMAL→RESTRICTED acc=16.7%)** — `CB3_P4_GRADE_BLOCK_ENABLED=False`(한시예외, CLAUDE.md 절대원칙 §2)라 실제 진입 차단으로 이어지지 않는 **관측 전용** 전환이다. 30m 호라이즌 자체는 앙상블에서 퇴역했지만 CB③·Contrarian의 유일한 입력원으로 계속 채점돼야 한다(CLAUDE.md 474차 각주) — 오늘도 그 채점이 정상 지속됨을 확인.
- **Contrarian 활성화(11:33:58, acc30m=23.3% streak=10, 역베팅방향=LONG)** — 위와 같은 T-30 채점 경로를 쓰는 설계된 동작이며 실제 역베팅 진입은 없었다(오늘 진입은 10:09:59 1건뿐).
- **사이저 산출 3계약 vs 실제 진입 2계약(`[SizerMatch] 3→2계약`, binding=hurst)** — 기존 `2-1`(0909) 지속 항목과 동일 패턴. 오늘도 Hurst 품질게이트가 구속(`binding_gate=hurst`)했다.
- **ConstOut 재적합 트리거(11:35~11:36, 12:14~12:16 두 차례, `5m` 상수 출력 감지 → 스케일러 재적합)** — STEP 3(GBM 배치 재학습)은 "30분마다"가 아니라 이벤트 구동이다(CLAUDE.md·`references/phases.md` B-2). 12:14:59 감지 → `retrain_intraday_20260916_121559.log`로 `py310_64` 서브프로세스가 13.1초 만에 정상 완료(`force=True intraday=True horizons=['5m']`) — 재학습 환경 분리 원칙(py37_32에서 돌리지 않는다) 그대로 준수.
- **ATR 변동성 부족 차단 12건(0.74~0.99pt, 임계 1.0pt 미만)** — 26주 WFA 재검증 항목으로 이미 등록된 `ATR_MIN_ENTRY=1.0` 고정 임계 이슈(CLAUDE.md 539차)와 같은 패턴. 오늘 새로 발견된 것이 아니라 그 항목의 실측치가 하루 더 쌓인 것.
- **메인 스레드 블로킹 2건(최대 2907ms, 08:41:23 기동 중 / 12:17:00 재학습 결과 반영 직후 추정)** — 둘 다 `CB_PIPE_PAUSE_MS=5,000ms` 임계 미만이라 CB⑤ 대상이 아니다. 5초 초과 0건.
- **`log_manager.signal()` TypeError 4회차 재발(F-2026-09-14-1)** — 이미 2026-09-14 저녁 MW0601 커밋(`13ac7ab`)으로 반영되고 2026-09-15(568차)에 재검증까지 끝난 항목. 오늘 로그에 `TypeError`/`log_manager` 관련 재발 0건 확인.

---

## 2i. Fix 작업 구현계획

오늘 장중 시점에 새로 필요한 Fix는 없습니다. (장중이므로 설령 있었어도 지금 적용하지 않고 계획만 세우는 것이 원칙입니다 — 라이브 프로세스가 돌고 있기 때문입니다.)

### 적용 순서와 커밋 계획

해당 없음 — 오늘 장중 코드 변경 없음.

---

## 3i. 고도화 방안

당일 관측에서 새로 도출된 것은 없습니다. 기존 26주 WFA 재검증 항목(ATR_MIN_ENTRY 등)과 주간회의 안건은 CLAUDE.md·NEXT_TODO.md에 이미 반영돼 있어 중복 제안하지 않습니다.

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| 해당 없음 | — | — | 오늘 장중 관측에서 새로 도출된 항목 없음 |

---

## 4i. 진입 승패 사후검증 (장후 한정)

해당 없음(장후 전용).

---

## 5i. 수익률 향상방안 (장후 한정)

해당 없음(장후 전용).

---

## 6i. 다음 거래일 관측 예정

오늘 장중은 새로 등록할 관측 항목이 없다. 다음 관측 ID는 여전히 **`O-82`**부터 발급한다.

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| — | (오늘 장중 신규 관측 항목 없음) | — | — |

> 참고(신규 등록 아님) — 기존 `O-81`(LONG/SHORT 방향별 승률 격차 표본 축적)에 오늘 데이터 포인트 1건 추가: 10:09:59 SHORT 진입·승. 새 채널이나 항목을 만들지 않고 기존 관측에 표본만 보탠다.

## 7i. 기한 도래 항목

`references/invariants.md` §2 재검토 기한 대조 — 오늘(2026-09-16) 기준 새로 도래한 항목 없음. 장전과 동일.

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| CB② 복원 (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | 사건 기한(⑧ 해제와 동일 커밋) — 날짜 아님 | — | 489차 D2, 변경 없음 |
| TOX-SEVERE-SPREAD 26주 재검증 | 다음 26주 WFA 주기 | — | 473차 F-8, 변경 없음 |

---

## 8i. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-09-16 (MW0602 572차 — 장중 일일 점검)` — 아래 별도 기록
- `NEXT_TODO.md` 추가 체크박스: 0건 (오늘 장중 신규 이상점 없음)
- 완료 처리한 기존 항목: 없음 (1-1은 이월 처리표에서 ✅해소로 처분, 별도 TODO 항목이 아니었으므로 체크박스 완료 처리 대상 아님)

---

## 사용자 조치 (장중분)

| # | 할 일 | 명령·경로 | 기한 |
|---|---|---|---|
| 1 | (없음) 오늘 장중은 사용자가 지금 당장 해야 할 조치가 없습니다. | — | — |

> 장전분 참고 숙제(변경 없음, 그대로 대기 중): `F-1`(런처 GUARD 오작동 수정 여부, 15일째) · `F-2`(장전 예약 시각 조정 승인, 누적) · `1-2`(3m 모델 교체 보류, 9거래일 연속, 오늘 EOD 확인 예정) · `2-1`(0909)(사이저 3계약 산출 vs 2계약 실체결, hurst 게이트 구속 — 오늘도 재확인, 원인 규명 완료 상태) — 필요시 주간회의에서 한 번에 정리하는 것을 권합니다.

---

# 장후(post)

- 점검 시각: 16:30~16:45 KST · PC `MW0602`
- HEAD `34998ac` · 미커밋 3건(`dev_memory/DECISION_LOG.md`·`NEXT_TODO.md` 수정분 + 오늘자 리포트) · 당일 커밋 0건
- 증거: `docs/정기점검/매일점검/evidence_MW0602-20260916_post.md`
- EOD+P8 완료 확인: `data/eod_retrain_done_20260916.txt`(15:52:28) · `data/session_state.json`의 `p8_last_success_date`/`eod_retrain_ok_date` — 둘 다 오늘 날짜로 기록됨(§C-3 통과)

## 0p. 한 줄 결론

**오늘 미륵이는 절대원칙을 전부 지키며 하루를 마쳤습니다.** 오늘 매매는 딱 한 번 있었고, 이익으로 끝났습니다(10시 10분경 짧게 들어갔다가 20초 만에 목표가 근처에서 안전하게 빠져나와 27,042원을 벌었습니다). 장이 끝날 때 포지션은 남아 있지 않았고, 밤새 자리를 지켜야 하는 위험(오버나이트)도 없었습니다. 저녁 모델 재학습과 그 뒤 마무리 작업도 모두 정상적으로 끝났습니다. 오늘 하루 전체에서 지금 당장 조치가 필요한 위험 신호는 없었습니다. 다만 지난 9거래일째 이어지던 한 가지 모델 교체 보류 문제가 오늘 자연히 해소됐고, 대신 다른 모델(1분봉용 보조 모델)에서 비슷한 보류가 사흘째 이어지고 있어 계속 지켜볼 필요가 있습니다.

**종합 판정표**

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 정상 | 기동·모델로드·Cybos연결·매크로/레짐확정·설정값 전부 정상 |
| 장중 | 정상 | 매분 루프 100% 생존, 거래 1건 이익 종료, CB 실발동(제한적) 외 이상 없음 |
| 장후 | 정상 | 15:10 강제청산 경로 정상 작동(청산 대상 없음), EOD·P8 성공, 새 P0/P1 없음 |
| **당일 최종** | **정상** | 종가 기준 net +27,042원, 절대원칙 6종 전부 준수, 포지션 0(FLAT) 마감 |

**절대원칙 준수 여부 — 종가 기준 최종 판정**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | 준수 | 오늘 유일한 포지션이 10:10:19에 이미 전량 청산돼 15:10 시점 포지션 0. `logs/20260916_SYSTEM.log` 15:11:21 `[SchedForceExit] 15:11 점검 — status=FLAT engine=0ct broker_cached=0ct bar_pass=1회 → 청산 대상 없음(정상)` |
| ② | Circuit Breaker 5종 | 정상 | CB①·②·④·⑤ 실발동 0건(②는 `CB_CONSEC_STOP_LIMIT=9999` 모의 한정 예외로 정상). CB③은 하루 중 여러 차례 단계 전환 + strict 모드 1회 진입(13:26:59) — 아래 §1p "이미 반영된 사안" 참조. 실거래 영향 없음 |
| ③ | CORE 피처 호라이즌 그룹별 | 준수 | 오늘 유일한 진입에서 `vwap` 통과 확인(미통과 진입 0건). EOD 시점 CORE 축퇴 0/6(`[CORE준비도] trigger=E_EOD 축퇴 0/6 — CORE 스케일 정상`, 15:52:29) |
| ④ | COM 콜백 내 dynamicCall·emit 금지 | 준수 | `crash_signature` 지표 오늘도 `0(무사고)`, `0xC0000409`·STACK_BUFFER 로그 0건 |
| ⑤ | GetRepeatCnt/GetCommData 파라미터 | 해당 없음 | 코드 미변경 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 준수 | 자동 큐·자동 통합 관련 로그 0건 |

**설정 불변식** — 수집기 §3 표 24행 전부 `일치`. 차단 게이트 32개 중 8개 꺼짐(전부 기록된 한시예외, 신규 없음).

**당일 최종 손익 — 종가 기준, 브로커 net 우선**

| 항목 | 값 |
|---|---|
| 포지션(승/전체) | 1건 (1승 / 1건, 100%) — **포지션 단위**. 청산 레그는 2건(TP1 부분청산 1 + 최종 1), 승패를 레그로 세지 않음 |
| gross | +31,000원 |
| 수수료 | 3,958원(요율 `commission_rate_used=0.0019%`, CREON 트레이딩 채널) |
| **net (전환기준①의 판정 원천)** | **+27,042원** |
| 자본 대비 | +0.054% (목표자본 5천만원 기준) |
| 검산 | §5 포지션 합계 +27,042원 ≒ 일일 마감 배너 +27,041원(차 1원, 반올림) — 일치 |
| MDD(참고, 롤링 20일 누적) | 2.13%(자본대비) / 77.7%(peak대비) — 오늘 단독 수치 아님, 일일 전략 리포트 인용 |
| 종가 포지션 | 0계약 (FLAT) |
| 진입/청산 STEP 중 걸린 것 | 없음 — STEP 1~9 전부 설계대로 |

---

## 0p-C. 이월 처리 — 앞 국면(장전+장중) 항목 처분

| 원 번호 | 제목(줄임) | 원 국면 | 처분 | 근거 |
|---|---|---|---|---|
| 1-1 | 개장 직전 CORE 피처 워밍업 축퇴(최대 4/6, 이후 `ofi_norm` 1/6) | 장전 | ✅ **해소** (장중에 이미 처분, 장후 재확인) | 09:00:58 `[CORE준비도] trigger=B_OPEN 축퇴 0/6`. 장후 EOD 시점(15:52:29)까지 축퇴 0/6 유지 재확인 — 하루 종일 재발 없음 |

- 장중이 신규 발급한 `O-*`/`P-*`는 없었다(기존 `O-81`에 데이터 1건만 추가). **미처분 0건.**
- 병행 세션 확인(§0 규약): `git --no-optional-locks log --since "2026-09-16 00:00"` — 당일 커밋 0건. `docs/정기점검/매일점검/` 최근 파일 목록에 오늘자 병행 산출물(딥다이브·특별조사 등) 없음 — 장전(09:00)·장중(12:40)·지금(16:30) 세 세션만 순차 실행됨.

---

## 1p. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

없음.

### P1 — 정확성·관측 훼손

없음.

### P2 — 운영 부담·기술부채 (신규 2건)

#### 1-2. RF 1분 호라이즌 모델 교체가 사흘째 보류 중 (신규 관찰 — 조치 불필요, 계속 지켜볼 것)

- **증상**: 저녁 모델 재학습(EOD)에서 RF(랜덤포레스트) 계열의 1분봉용 보조 모델이 사흘 연속으로 "새로 만든 모델이 기존 것보다 못하다"는 이유로 교체가 보류됐습니다. 안전장치가 원래 하려던 일을 정확히 하고 있는 것이라 지금 당장 손댈 곳은 없지만, 새 모델의 성능이 매일 조금씩 좋아지고 있어 언제 교체 기준을 넘길지 지켜볼 가치가 있습니다.
- **근거**:
  ```
  2026-09-14 15:52:18 [WARNING] LEARNING: [Retrain] RF 1m 교체 보류(EOD 모델가드) — acc 하락 0.0386 > 허용 0.0250 (new=0.4759 old=0.5145)
  2026-09-15 15:52:20 [WARNING] LEARNING: [Retrain] RF 1m 교체 보류(EOD 모델가드) — acc 하락 0.0363 > 허용 0.0250 (new=0.4782 old=0.5145)
  2026-09-16 15:52:28 [WARNING] LEARNING: [Retrain] RF 1m 교체 보류(EOD 모델가드) — acc 하락 0.0344 > 허용 0.0250 (new=0.4801 old=0.5145)
  ```
  `logs/retrain_eod_20260914.log`:74 · `logs/retrain_eod_20260915.log`:75 · `logs/retrain_eod_20260916.log`:74
- **기준 위반**: 없음 — EOD 모델가드(346차 도입)가 설계대로 동작 중인 것이며 결함이 아니다.
- **영향**: 없음. 기존 1분 모델(정확도 51.45%)이 계속 쓰이고 있어 오늘 실거래에 영향을 준 것은 없습니다.
- **영향(기술)**: 챔피언 모델 `old=0.5145` 고정, 신규 후보 3일 추이 `0.4759→0.4782→0.4801`. 단순 선형 추세로는 교체 기준(`old−0.025=0.4895`) 도달까지 약 4~5거래일 추정되나 참고치일 뿐 예측이 아니다. 보류 모델은 `model/horizons/rejected/`에 참고용으로만 저장됨.
- **신규 여부**: 신규 관찰. 09-10·09-11 EOD 로그에는 이 패턴이 없었다(주말 09-12·09-13은 로그 없음 — 휴장 추정). 09-14부터 시작된 것으로 잠정 판단.
- **처리**: `dev_memory/NEXT_TODO.md`에 관찰 항목으로 등록(조치 불필요, 매일 EOD 로그에서 자동 재확인됨). 별도 관측 ID(`O-*`) 미발급 — 이미 매일 로그에 남는 계측이라 채널 신설 불필요.

#### 1-3. 증거 수집기의 "다음 관측 ID" 자동 계산이 실제보다 1 큰 값을 낸다 (도구 결함, 낮은 우선순위)

- **증상**: 오늘 증거 다이제스트 §11이 "다음 관측 ID = O-83"이라고 자동 계산했습니다. 그런데 지금까지 실제로 발급된 관측 ID 중 가장 큰 것은 O-81이고, O-82는 아직 한 번도 발급된 적이 없습니다 — 장전·장중 리포트가 "다음은 O-82부터"라고 예고만 했을 뿐입니다.
- **근거**: `dev_memory/NEXT_TODO.md` — 실제 발급된 마지막 항목은 `**O-81**`(LONG/SHORT 방향별 승률 격차). "다음 관측 ID는 여전히 `O-82`부터"라는 문장(발급 아님, 예고)이 여러 세션에 반복 등장. 오늘 증거 다이제스트 §11: "다음 관측 ID = **O-83**(현행 최댓값 O-82, dev_memory/NEXT_TODO.md 기준)".
- **기준 위반**: 없음 — 수집기 로직의 사소한 결함이며 절대원칙과 무관.
- **영향**: 낮음. 다음에 새 관측을 등록할 때 실제로는 비어 있는 O-82를 건너뛰고 O-83부터 매길 위험이 있습니다 — 번호 하나가 비게 될 뿐 데이터가 잘못되는 것은 아닙니다.
- **영향(기술)**: `collect_evidence.py`의 관측 ID 스캐너가 정규식으로 `O-\d+` 패턴을 텍스트 전체에서 찾아 최댓값을 취하는 것으로 추정되며, `**O-NN**`(볼드, 실제 발급 형식)과 "O-NN부터"(예고 문구)를 구분하지 못해 예고 문구도 "이미 쓰인 번호"로 오인한 것으로 보인다. 스크립트 내부를 직접 읽어 확정한 것은 아니다(관찰 기반 추정).
- **신규 여부**: 신규 발견. 장전(571차) 리포트에서는 "O-82부터"로 정확히 계산됐다 — 오늘 사이(장중→장후)에 처음 이 오탐이 나타난 것으로 보인다.
- **처리**: 이번 리포트는 **O-82를 다음 발급 번호로 사용**한다(실제 최댓값 O-81 기준, §6p). 스캐너 정규식 개선은 P2로 NEXT_TODO에 등록 — 급하지 않음(매번 수동으로 실제 최댓값을 한 번 더 확인하면 되는 문제).

### 확인 필요 (확정 아님)

없음.

### 이미 반영된 사안 (신규 아님)

- **GBM 3m 호라이즌 EOD 모델 교체 보류(`1-2`(0907), 9거래일 연속) — 오늘 해소됨** — `logs/retrain_eod_20260916.log`: `3m 교체 (acc 0.4404→0.4531)`. 09-15까지 9거래일 연속 보류(전일 신규후보 acc 하락폭 0.0269 > 허용 0.0250)였으나, 오늘 신규 후보가 기준을 통과해 정상 교체됐다. `NEXT_TODO.md`에서 이 항목을 완료 처리한다(§8p).
- **CB③-P4 섀도 상태전환 다수(NORMAL↔WATCH↔RESTRICTED, 10:44:58·13:09:59·13:52:59·13:54:59·14:36:59·14:38:59) + CB③ strict 모드 1회 진입(13:26:59)** — `CB3_P4_GRADE_BLOCK_ENABLED=False`(한시예외)라 상태전환 자체는 등급 차단으로 이어지지 않는다. strict 모드는 "Mid-Conf Blind Spot"(중간신뢰도 60~85% 구간에서 7연속 오답) 트리거로 CB③ accuracy 임계를 35%→42%로 강화하는 **정식 설계 동작**(60차 도입, `CB_MID_CONF_WRONG_LIMIT=7`)이며, 오늘 유일한 진입(10:09:59)보다 3시간 이상 늦게(13:26:59) 발생해 실거래에는 영향이 없다. 이후 13:52:59(RESTRICTED→WATCH)·13:54:59(WATCH→NORMAL)로 자연 복귀했다.
- **ATR<1.0pt 변동성 부족 차단 12건** — 539차 기등록 26주 WFA 재검증 항목(`ATR_MIN_ENTRY=1.0` 고정 임계)의 실측치가 하루 더 쌓인 것. 신규 아님.
- **사이저 3계약 산출 vs 실제 2계약 체결(binding=hurst)** — 기존 `2-1`(0909) 패턴 재확인. `sizing_trace` 확인 결과 오늘도 hurst 품질게이트(quality_min=0.5)가 구속, 증거금 상한(2계약)과는 우연히 일치했을 뿐 binding은 hurst. 신규 아님.
- **메인 스레드 블로킹 3건(최대 2907ms)** — `CB_PIPE_PAUSE_MS=5,000ms` 미만이라 CB⑤ 대상 아님.
- **ScalerFloor(`macro_vix`) 1,590건** — 2026-06-25 CORE 강등 이후 계속되는 알려진 패턴(macro_vix가 일봉→분봉 상수화되면서 분산 바닥값이 상시 적용됨). 신규 아님.
- **`log_manager.signal()` TypeError** — 2026-09-14 MW0601 커밋(`13ac7ab`)으로 이미 반영, 09-15(568차) 재검증 완료. 오늘 재발 0건 재확인.

---

## 2p. Fix 작업 구현계획

오늘 새로 필요한 Fix는 없습니다(P0/P1 없음). 아래는 저우선순위 개선 제안이며 지금 적용하지 않습니다.

| # | 대상 | 내용 | 우선순위 |
|---|---|---|---|
| 1 | `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py` §11 관측 ID 스캐너 | `O-\d+` 정규식이 "다음은 O-NN부터" 같은 예고 문구까지 최댓값 후보로 잡는 것으로 추정(§1p-3). `**O-NN**`(볼드, 실제 발급 형식)만 매칭하도록 좁히거나 "다음"·"부터" 인접 문맥을 제외하는 방식 검토 | P2 |

### 적용 순서와 커밋 계획

해당 없음 — 오늘 코드 변경 없음(장후 예약 실행은 코드를 수정하지 않는다).

기존에 열려 있는 항목(재확인만, 새 계획 아님): `F-1`(런처 GUARD, 15일째 사용자 결정 대기) · `F-2`(장전 예약 시각, 누적 대기) · `tests/test_457_fallback_visibility.py` 실패(MW0601 유래, C등급 보류).

---

## 3p. 고도화 방안

당일 관측에서 새로 도출된 고도화 항목은 없습니다. RF 1m 모델 후보 정확도가 개선 추세(§1p-1-2)이므로 다음 EOD들에서 계속 추적하되, 지금은 표본 3거래일뿐이라 313차 원칙상 파라미터 변경이나 원인 조사를 제안하지 않습니다. 기존 26주 WFA 재검증 항목·주간회의 안건은 CLAUDE.md·NEXT_TODO.md에 이미 반영돼 있어 중복 제안하지 않습니다.

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| 해당 없음 | — | — | 오늘 관측에서 새로 도출된 항목 없음 |

---

## 4p. 진입 승패 사후검증 (EOD+P8)

### 4p-0. 선행조건 확인

- 증거 수집: `python3 .claude/skills/mireuk-daily-check/scripts/collect_evidence.py --phase post --pc MW0602 --skill-rev 2026-08-26e --out-auto` — 정상 실행, stderr 없음, `evidence_MW0602-20260916_post.md`(95.4KB) 생성 확인.
- 브랜치: `dev` — MW0602 운영 브랜치 일치.
- EOD·P8 완료: `data/eod_retrain_done_20260916.txt`(15:52:28, `horizons_replaced: 6/6`) · P8 `[P8] session_state p8_last_success_date + eod_retrain_ok_date 기록 완료`(15:52:29) — 둘 다 확인됨.

### 4p-1. 오늘 할일 대조

**당일 커밋**: 없음(예약 실행은 커밋하지 않는다 — 정상).

| # | NEXT_TODO 항목 | 당일 커밋 | 코드 실재 확인 | 상태 |
|---|---|---|---|---|
| 1 | `1-2`(0907) 3m EOD 모델 교체 보류 | 없음(EOD 자동 재학습, 코드 변경 아님) | `logs/retrain_eod_20260916.log`:37 "3m 교체 (acc 0.4404→0.4531)" | ✅완료-커밋확인(자연 해소, 코드 변경이 아니라 데이터 기반 자동 재평가) |

나머지 미완료 항목(`F-1`·`F-2`·`tests/test_457…` 등)은 오늘 진행 없음 — ⬜ **미착수**, 사유: 사용자 결정 대기 지속(변경 없음, 함정① 대상 아님 — 이미 등록된 대기 상태 그대로).

**함정① 게이트**: 위 표의 유일한 항목은 "완료"가 아니라 "자연 해소"이며 코드 변경이 아니므로 `VALIDATION_CAMPAIGN_DECISIONS`·DECISION_LOG 대조 대상이 아니다. 오늘 새로 "이미 반영된 것을 신규로 올린" 사례는 없다.

### 4p-2. 3원 대사

| 원천 | 값 |
|---|---|
| 로그 `[Position] 진입` | 1건 (`logs/20260916_TRADE.log`:11, 10:09:59) |
| DB `ensemble_decisions.entry_executed=1` (당일) | 1건 |
| DB `trades` `COUNT(DISTINCT entry_ts)` (당일) | 1건 (청산 레그 `COUNT(*)`=2건 — TP1 부분청산이 레그를 늘린 것, 불일치 아님) |

**1 = 1 = 1 — 일치. 딥다이브 진행.**

### 4p-3. 케이스 딥다이브 (포지션 1건 = 카드 1장)

```
[CASE-01] 진입 10:09:59 SHORT · 2계약 (등급 A/원시C · horizon=3m · hurst=mean-revert)
          → 10:10:13 TP1 부분청산 33% (+0.55pt/+25,521원) → 10:10:19 하드스톱(틱)[TP1보호] (+0.07pt/+1,521원)
          포지션 합 +0.62pt / net +27,042원(gross +31,000, 수수료 3,958) · 보유 20초

 진입근거   [진입체크] SHORT→SHORT 2계약 A급(원시C) | sign✅ conf✅ vwap✅ cvd✅ ofi✅ fore❌ prev✅
            time✅ risk✅ chas❌ coun✅ | conf=39.1% (logs/20260916_TRADE.log:11)
            [Sizer] 신뢰도배수=0.6 레짐배수=0.8 안전배수=1.00 → 3계약 산출
            → [SizerMatch] 3→2계약 (sizing_trace: binding_gate=hurst, quality_min=0.5,
              margin_cap_qty=2·margin_binding=0 — 증거금 상한과 수치는 같았지만 실제 구속은 hurst)

 예측 vs 실현  ensemble_decisions(10:09:00): direction=-1(SHORT) confidence=0.3905
            grade=raw C→A(승격) meta_action=reduce(size_mult=0.5) toxicity=pass(score=0.27)
            predictions(3m, 10:09:00): direction=-1 confidence=0.3387 → 3분 뒤 actual=0(횡보),
            단일모델 사후채점 correct=0
            ⚠ 앙상블 방향(SHORT)과 단일 3m 모델의 3분-뒤 사후채점(correct=0)이 엇갈린다.
              표본 1건이라 이것만으로 결론 내지 않는다(313차 ④). 실제 청산은 3분을 기다리지
              않고 20초 만에 TP1→보호스톱으로 종료됐다 — "3분 뒤 방향 채점"과 "실제 체결까지
              걸린 경로"는 서로 다른 시간축이라는 점에 주의.

 승패판정   ✅승 — exit_reason은 `하드스톱(틱)`이지만 `trades.tp1_reached=1`인
            TP1 보호트레일 이익 청산이다(465차 기준, §3-1). 손절로 세지 않았다.

 주 요인    B (타이밍) — 진입 직후 14초 만에 TP1 도달, 6초 뒤 보호스톱으로 조기 확정.
            방향 자체의 적중 여부는 단일모델 3분 채점상 불명확(correct=0)이라 A로 단정하지
            않는다.
 부 요인    D (사이징) — hurst 품질게이트가 3계약→2계약으로 구속(binding=hurst, quality_min=0.5,
            sizing_trace 확인).
            E (체결) — 해당 없음. 하드스톱(진짜 손절) 발동 없이 TP1 경로로만 종료돼 손절폭
            초과비율 계산 대상 자체가 없다.

 반사실     관찰만 기록한다 — 표본 1건으로 파라미터 변경 근거를 만들지 않는다(313차).
```

### 4p-3-1. 승패 판정 재확인

`exit_reason`만으로 세지 않았다 — 이 포지션의 두 청산 레그 모두 `trades.exit_outcome='이익'`이고 최종 레그(id=479)는 `tp1_reached=1`로 TP1 보호트레일 이익 청산임을 DB로 직접 확인했다. `하드스톱` 라벨에 손절이 섞여 있지 않은지 매번 확인하는 것이 규칙이며, 오늘은 애초에 진짜 손절(하드스톱 원래 의미) 자체가 0건이었다.

### 4p-4. 집계표

| 요인 | 승 | 패 | 순손익 | 평균 보유(분) | 관측 거래일 수 |
|---|---|---|---|---|---|
| 전체 | 1 | 0 | +27,042원 | 0.33 (20초) | 1 (오늘) |

- 단위는 **포지션**(레그 아님). 브라이어 스코어·캘리브레이션은 누적 기준으로만 보며 당일 단독으로 모델 열화를 단정하지 않는다.

### 4p-5. 313차 다섯 갈래 적용

**① 일자단위**: 관측 거래일 1일, 포지션 1건 — 어떤 통계적 결론도 낼 수 있는 표본이 아니다.
**② overlap 보정**: 해당 없음(단일 포지션, 중첩 표본 자체가 없다).
**③ 이상치 분해**: 해당 없음(표본 1건이라 "이상치"와 "전형값"을 나눌 수 없다).
**④ 사전등록**: 오늘 어떤 임계·기준도 결과를 보고 사후 조정하지 않았다.
**⑤ 부호 일관성**: 해당 없음(단일 관측치라 부호 교차검증 대상이 없다).

→ **오늘은 "관찰"만 기록한다. "…이 원인이다"·"…를 조인다" 류의 표현은 쓰지 않는다.**

### 4p-6. 함정② 재확인

이번 딥다이브에서 `2026-08-01 §9-3 이벤트(청산 레그) 단위 사이징 통계 4종`·`2026-06-25 SHAP=0 판정`을 근거로 인용하지 않았다.

---

## 5p. 수익률 향상방안

오늘은 포지션 1건뿐이라 §4의 요인 집계에서 통계적으로 도출할 방안이 없다(313차 원칙 + "일반론 금지" 규칙을 결합하면 "쓸 수 있는 방안 없음"이 올바른 결론이다). 아래는 제안이 아니라 **다음에 표본이 쌓이면 볼 항목**만 등록한다 — 6칸 형식은 표본이 차기 전까지는 채우지 않는다(사전등록 조건만 명시).

| 관찰 대상 | 근거(CASE-ID) | 표본 상태 | 재판정 조건 |
|---|---|---|---|
| 앙상블 방향과 단일 3m 모델 사후채점(correct)의 불일치 | CASE-01, 10:09:00 3m 예측 correct=0 vs 실제 승리 | n=1(오늘) | §12 `앙상블_단일호라이즌_방향대조`(0812~) 채널이 이미 이 질문을 추적 중 — 신규 채널 만들지 않는다(함정①). 그 채널의 사전등록 문턱(`불일치` 10건 누적 또는 10거래일) 도달 시 그 채널에서 판정 |

**함정① 재확인**: 위 관찰은 기존 채널 재사용이며 신규 계측 제안이 아니다. `config/settings.py:VALIDATION_CAMPAIGN` 채널 키·다이제스트 §13 확정 결정 레지스트리와 대조해 동일 질문의 신규 제안이 없음을 확인했다.

---

## 6p. 다음 거래일 관측 예정

다음 관측 ID는 **`O-82`**부터 발급한다(§1p-3 — 스캐너가 낸 `O-83`은 오탐, 실제 최댓값 `O-81` 기준).

| ID | 관측 대상 | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| — | RF 1m 모델 교체 보류 추이(§1p-1-2) — 신규 `O-*` 미발급, `logs/retrain_eod_*.log`에서 매일 EOD 자동 재확인 | 신규 후보 acc가 `old−0.025`(0.4895) 이상이면 정상 교체, 5거래일 이상 정체되면 원인(1m 피처셋·레이블) 재검토 착수 검토 | 다음 EOD(09-17) |

> 참고(신규 등록 아님) — `O-81`(LONG/SHORT 승률 격차)에는 이미 장중에 오늘 데이터가 반영됐다. `O-80`(장중·장후 프로세스 무응답 재발 감시)은 오늘 재발 0건.

## 7p. 기한 도래 항목

`references/invariants.md` §2 재검토 기한 대조 — 오늘(2026-09-16) 기준 새로 도래한 항목 없음. 장전·장중과 동일.

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| CB② 복원 (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | 사건 기한(⑧ 해제와 동일 커밋) — 날짜 아님 | — | 489차 D2, 변경 없음 |
| TOX-SEVERE-SPREAD 26주 재검증 | 다음 26주 WFA 주기 | — | 473차 F-8, 변경 없음 |

---

## 8p. dev_memory 반영

- `DECISION_LOG.md` 추가 항목: `## 2026-09-16 (MW0602 573차 — 장후 일일 점검)` — 아래 별도 기록
- `NEXT_TODO.md` 갱신:
  - 완료 처리: `1-2`(0907) 3m EOD 모델 교체 보류(9거래일 연속) → 오늘 해소, 체크박스 완료 처리
  - 신규 추가: RF 1m 모델 교체 보류 관찰(§1p-1-2, 조치 불필요) · §11 관측ID 스캐너 오탐(§1p-1-3, P2)
- 완료 처리한 기존 항목: `1-2`(0907) 3m EOD 모델 교체 보류(1건)

---

## 사용자 조치

| # | 할 일 | 명령·경로 | 기한 | 안 하면 생기는 일 |
|---|---|---|---|---|
| 1 | 오늘 리포트·증거·dev_memory 변경분 커밋 | 아래 "커밋 대기" 목록 참조 | 편할 때 | 다음 세션이 오늘 변경분을 안 반영된 상태로 다시 보게 됨(작업 손실은 아님, 그냥 미반영 상태 지속) |

> 참고 — 계속 쌓이고 있는 기존 숙제(오늘 새로 생긴 것 아님, 그대로 대기 중): `F-1`(런처 GUARD 오작동 수정 여부, 15일째) · `F-2`(장전 예약 시각 조정 승인, 누적) · `tests/test_457_fallback_visibility.py` 실패(MW0601 유래, C등급 보류) — 필요시 주간회의에서 한 번에 정리하는 것을 권합니다. **오늘 새로 생긴 사용자 결정 필요 사항은 없습니다.**

**커밋 대기**: `docs/정기점검/매일점검/MW0602-20260916-점검리포트.md`, `docs/정기점검/매일점검/evidence_MW0602-20260916_post.md`, `dev_memory/DECISION_LOG.md`, `dev_memory/NEXT_TODO.md`

세션 종료 시 `.git/index.lock` 확인: 시작·종료 2점 모두 **없음**(수집기 §2 자가점검).

---

## 장후 자동조치 구현 결과 (17:41 · 자동 실행)

> 예약 작업이 자동으로 덧붙인 절입니다. **위 본문은 고치지 않았습니다.**

### 한 줄

§2p 표에 남아 있던 **유일한 항목(관측 ID 자동 계산 오탐)을 고쳤습니다.** 고치려고 실물
코드를 열어 보니 리포트 추정보다 **한 단계 더 나빴습니다** — 이 결함은 **스스로 커지는
종류**였습니다.

### 구현한 것 (1건)

#### §2p-1 · §1p-3 「다음 관측 ID」 자동 계산 오탐 수정

- **왜 했나**: 관측 번호(`O-82` 같은 것)는 *"이 관측은 언제 판정하나"* 를 추적하는
  이름표입니다. 자동 계산이 틀리면 매번 사람이 손으로 다시 세야 하고, 실제로 오늘
  장후 점검이 그 확인에 한 절(§1p-3)을 썼습니다.
- **무엇이 문제였나**: 스캐너가 문서 전체에서 `O-숫자`를 전부 긁어 가장 큰 값을
  골랐습니다. 그런데 `NEXT_TODO.md`에는 *"다음 관측 ID는 `O-82`부터"* 같은 **예고
  문장**이 매일 실립니다. **예고를 "이미 쓴 번호"로 착각한 것**입니다.
- 🔴 **리포트 추정보다 나빴습니다**: 리포트는 스캐너가 `O-83`을 낸다고 적었지만, 실제로
  재어 보니 오늘은 **`O-84`**를 낼 참이었습니다. 어제의 오탐(`O-83`)을 적어 둔 문장이
  그대로 문서에 실려 **다음 날 최댓값을 한 칸 더 밀어 올리기** 때문입니다. 즉
  **오탐을 기록하는 행위 자체가 다음 오탐을 키웁니다.** 그대로 두면 관측 번호가
  **매일 하나씩** 비어 갑니다.
- **어떻게 고쳤나**: 체크박스 항목 머리에 붙은 이름표(= 실제 발급)만 세도록 바꿨습니다.
  ⚠ 그 용도의 정규식은 **494차부터 이 파일에 이미 있었고, 아무도 쓰지 않았습니다.**
  주석에 이 결함을 예고까지 해 둔 채로요 — *"이미 받고 있는데 안 보고 있던"* 493차 §⑤
  계열입니다. **새로 만든 게 아니라 있던 것을 꽂았습니다.**
- **곁들인 것 2가지**(계측 4원칙):
  - **③ 탈락 가시화** — 안 센 번호를 밝힙니다. 이제 다이제스트가 *"`O-82`·`O-83`은
    언급만 있고 발급되지 않아 세지 않았다"*라고 적습니다. 다음 세션이 같은 조사를
    반복하지 않습니다.
  - **④ 폴백 가시화** — 문서 형식이 바뀌어 이름표를 하나도 못 찾으면 **조용히 넘어가지
    않고** 그 사실을 적습니다.
- **매매에 영향**: **없습니다.** 점검 도구(수집기 스크립트)만 바뀌었고 거래 코드는 한 줄도
  건드리지 않았습니다.
- **걸린 시간**: 약 20분(검증 포함).
- **안 고쳤으면**: 관측 번호가 매일 하나씩 비고, 매번 사람이 실제 최댓값을 손으로 확인해야
  합니다.

**결과 확인**: 스캐너가 이제 `발급 최댓값 O-81 → 다음 O-82`를 냅니다(정답).

### 하지 않은 것과 그 이유

| 항목 | 왜 안 했나 |
|---|---|
| `F-1` 런처 GUARD · `F-2` 장전 예약 시각 | **사용자 결정 대기**(16일째/누적). 자동조치 권한 밖(C등급)입니다 |
| `tests/test_457_fallback_visibility.py` 실패 | MW0601 체리픽(`f6613c3`) 유래로 대시보드 코드를 고쳐야 합니다. 리포트가 C등급 보류로 명시 — 유지 |
| `§1p-1-2` RF 1m 모델 교체 보류 | 리포트가 *"조치 불필요, 지켜볼 것"*이라 적었고 표본 3거래일입니다(313차). 동의합니다 |

### 테스트

- 신규 `tests/test_573_obs_id_scanner.py` **8건 통과**.
  **음성 대조 실시** — 수정 전 코드로 바꿔 끼우면 **8건 전부 실패**함을 확인했습니다
  (헛도는 검사가 아닙니다).
- 전체 스위트 **1,429 통과 / 1 실패 / 3 건너뜀** (254초).
  유일한 실패는 위 `test_457`로 **기존 실패이며 이번 변경과 무관**합니다.

> ⚠ **작업 중 브랜치가 움직였습니다.** 17:26~17:29에 MW0601 체리픽 6건
> (`2cdf3e0`~`d9ef941`)이 `dev`에 들어왔습니다 — 세션 시작(17:25) 병행 세션 점검에서는
> 0건이었습니다. 첫 전체 스위트가 그 와중에 걸려 `test_511_exit_order_reject`가 한 번
> 실패했지만, HEAD가 안정된 뒤 재실행에서 통과했습니다. **소스가 실행 중에 바뀐 탓이지
> 결함이 아닙니다.**

### 커밋

커밋 해시는 이 절 아래에 이어서 기재합니다(기록 갱신과 커밋 순서 때문에 2단계로 남깁니다).

### 사용자가 직접 해야 할 일

1. **`F-1`·`F-2` 결정**(5분, 16일째) — 자동조치가 손댈 수 없는 항목입니다.
2. 그 외 **오늘 새로 생긴 사용자 결정 사항은 없습니다.**
