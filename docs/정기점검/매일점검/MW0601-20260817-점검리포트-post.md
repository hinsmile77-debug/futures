# 미륵이 일일 점검 — MW0601 / 2026-08-17 / 장후(post)

- 점검 시각: 16:55~17:05 KST · PC `MW0601` · 브랜치 `v9-dev` (규약 일치)
- HEAD `e995764` · 당일 커밋 **1건**(`e995764` 472차) · 실질 미커밋 **10파일**
  (수집기 §11이 표시한 "454건"은 샌드박스 개행 아티팩트 — §"이미 반영된 사안" 참조)
- 증거: `docs/정기점검/매일점검/evidence_MW0601-20260817_post.md` (생성 16:55:24)
- 코드 변경 **0건** · 커밋 **0건** (예약 지시 준수)

> **선행 고지 — 오늘은 KRX 휴장일이다(광복절 대체공휴일).**
> `config/krx_holidays.py:52` `"2026-08-17",  # 광복절 대체공휴일 (월)`.
> 진입·청산·예측·손익 전부 0이며, 그것이 **정상**이다.
> 따라서 이 리포트의 §4·§5(승패 사후검증·수익률 향상방안)는 표본 0으로 **판정 불가**이며,
> 대신 §1은 "휴장일이라 조용한 것"과 "고장 나서 조용한 것"을 분리하는 데 집중한다.

> **동시 세션 고지** — 오늘 같은 날짜의 장전(pre)·장중(intra) 예약 점검이 16:48~16:49에
> 함께 실행됐고, 장전 리포트
> `docs/정기점검/매일점검/MW0601-20260817-점검리포트-pre.md`(16:57 생성)가 이미 존재한다.
> **본 리포트는 그 리포트의 이상점 1-1~1-6을 재보고하지 않는다.** §"이미 반영된 사안"에
> 참조만 남기고, 장후에서만 확정 가능한 사실과 **장전 리포트에 없는 신규 2건**을 다룬다.

---

## 0. 한 줄 결론

**오늘은 휴장일이라 절대원칙을 위반할 기회 자체가 없었고 실제로 위반도 없었다. 다만 장후
뼈대(15:40 일일 마감 → 15:45 EOD 재학습 → P8 스케일러 재적합)가 통째로 미완주했고, 그
미완주가 로그·마커·DB 어디에도 "실패"로 기록되지 않았다 — 오늘은 무해하지만 거래일에
같은 일이 생기면 자동 탐지가 불가능하다.**

| 국면 | 판정 | 근거 요약 |
|---|---|---|
| 장전 | 정상 | 08:40:46 기동, 08:41:09 모델 6호라이즌 `ready=True`, 휴장일 정상 인지 |
| 장중 | 정상(휴장) | `[System] 대기 중 \| 공휴일·휴장일` 08:51:16~12:51:16 5분 간격. 진입 0 · 차단 0 |
| 장후 | **조건부 이상** | 12:55 본체 `user_close` → 15:40 `daily_close()` 미실행 → 15:45 EOD 재학습이 15:46:34 이후 **무기록 정지** → P8 미실행 |

**절대원칙 준수 여부**

| # | 원칙 | 판정 | 근거 |
|---|---|---|---|
| ① | 15:10 강제청산 (오버나이트 금지) | **해당 없음** | 휴장일. 포지션 0 · `trades` 당일 0행. `data/position_state.json` 최종 갱신 08-14 11:48 → 이월 포지션 없음 |
| ② | Circuit Breaker 5종 | **해당 없음** | 파이프라인 미가동. CB 발동 0건, 로그 `HALT`·`CircuitBreaker` 0건 |
| ③ | CORE 피처 호라이즌 그룹별 | 정상(설정) | 진입 0건이라 체크리스트 미발동. `config/settings.py` `HORIZON_CORE_GROUP` 무변경 |
| ④ | COM 콜백 내 `dynamicCall`·`emit` 금지 | 정상 | 0xC0000409 크래시 0건. `logs/crash_fault.log` 당일 신규 기록 없음(mtime 12:54 = 종료 시점 flush) |
| ⑤ | `GetRepeatCnt`/`GetCommData` 파라미터 | 정상 | TR 수신 오류 0건 |
| ⑥ | 알파 리서치 봇 자동통합 금지 | 정상 | 당일 백테스트 큐·자동통합 로그 0건 |

**설정 불변식** — 수집기 §3의 `일치` 21행은 이상 없음. `미발견` 5행
(`MODEL_LABEL_STATE_UNLOCK_ENABLED` · `PRE_RETRAIN_DONE_BY_EOD_ENABLED` ·
`ZONE_ENTRY_BAN_ENFORCE` · `ZONE_ENTRY_BAN_SHADOW_ENABLED` ·
`PIPE_LATENCY_EXCLUDE_MODEL_SWAP`)은 **본 리포트의 신규 안건이 아니다** —
장전 리포트 1-1이 원인(브랜치 분기)까지 확정했다. 실측 재확인만 옮긴다:
5종 모두 `config/settings.py` 출현 **0회**, repo 전체에서도 수집기 자신
(`collect_evidence.py:179~187`) 외 출현 0회. 해당 커밋은 전부 `origin/dev`에만 있다
(`git log --oneline --all | grep 468차` → 10건 전부 `[MW0602]`, `v9-dev` 내 0건).
`git rev-list --left-right --count v9-dev...origin/dev` = **320 / 315**.

---

## 1. 이상점 정리보고

### P0 — 오늘 거래에 직접 위험 · 절대원칙 위반

**없음.** 휴장일이며 진입·청산·포지션 전부 0이다.
15:10 강제청산 미이행은 성립하지 않는다 — 청산할 포지션이 존재하지 않았다.

---

### P1 — 정확성·관측 훼손

#### 1-1. EOD 재학습 당일 미완주가 **확정**됐다 — 데드라인 16:05:04이 지나도록 아무 종료 흔적이 없다

- **증상**: 장전 리포트 1-2가 16:49 시점에 "15:46:34 이후 로그 끊김"까지 관측했다.
  본 장후 점검은 그 **다음 국면의 사실**을 확정한다 — 설계상 타임아웃 시각
  `16:05:04`가 지난 **17:00 시점에도** 로그 파일은 그대로이고 완료 마커도 없다.
- **근거**:
  ```
  $ TZ=Asia/Seoul date
  Mon Aug 17 16:56:55 KST 2026
  $ stat -c '%y %s %n' logs/retrain_eod_20260817.log
  2026-08-17 15:46:34.175767500 +0900 1040 logs/retrain_eod_20260817.log
  ```
  마지막 기록 `15:46:34` · 1,040B에서 멈춘 채 **70분 경과**.
  설계상 30초마다 한 줄(`retrain_eod.py:236`), 16:05:04에
  `[WaitDC] daily_close() 20분 대기 타임아웃 — pkl 경합 위험 있으나 강제 진행`
  (`retrain_eod.py:242`, WARNING) → 이후 `데이터 로드 시작` 순서다. **셋 다 없다.**

  세 갈래 완료 스탬프가 전부 부재 — 파일·DB 양쪽에서 교차 확인했다:
  ```
  $ ls data/ | grep 20260817
  (없음)                                    # eod_retrain_done_20260817.txt · daily_close_done_20260817.txt 둘 다 미생성
  $ sqlite3 trades.db "SELECT * FROM daily_stats ORDER BY rowid DESC LIMIT 1"
  ('2026-08-14', 1, 1, 0.64, 28730.0, ...)  # 2026-08-17 행 자체가 없음 → daily_close() 미실행 확정
  ```
  전제 붕괴 시점도 확정된다 — 본체는 **12:55에 이미 종료**했다:
  ```
  Mireuk 종료. RestartCnt=0 Runtime=255min
  [AUTO-RESTART] 정상 종료 감지 -- 이유: user_close -- 재시작 안 함
  ```
  `logs/Mireuk_batch/launcher_20260817_084001_15180.log` 마지막 6행.
  본체가 없으므로 `daily_close_done_20260817.txt`는 **생성될 수 없었다** — 즉
  `_wait_for_daily_close()`는 처음부터 결코 만족될 수 없는 조건을 20분 기다린 것이다.
- **기준 위반**:
  - **계측 4원칙 ④(폴백 가시화)** — 성공·타임아웃·강제종료 어느 것도 로그를 남기지
    않아 세 상태가 **로그상 동일**하다. 사후에 구분할 방법이 없다.
  - CLAUDE.md 매분 파이프라인 `15:40 자가학습 일일 마감` 후속 단계.
  - CLAUDE.md 운영환경 절 *"실패 시 모델 미교체 → 익일 CB③ HALT 유발 이력"*.
  - `retrain_eod.py`에 **거래일 게이트가 없다** — 휴장일에도 무조건 기동한다
    (`main()` 진입부에 `is_trading_day` 계열 호출 0건, grep 확인).
- **영향**: **오늘은 실손해 0이다.** 휴장이라 신규 레이블이 0건이고, 재학습해도 모델이
  바뀔 이유가 없다. 현행 모델은 08-14 EOD 산출물이며 그 완주 기록이 남아 있다:
  ```
  $ cat data/eod_retrain_done_20260814.txt
  completed: 2026-08-14 15:48:45 / rows: 39799 / cols: 97 / horizons_replaced: 6/6
  ```
  **다음 거래일(08-18)의 PreRetrain 스킵 판단도 정상 동작한다** — 폴백 경로를 직접
  시뮬레이션해 확인했다(`main.py:5069~5085`, 마커 파일 1~5일 역탐색):
  | `_d` | 대상일 | 마커 존재 |
  |---|---|---|
  | 1 | 2026-08-17 | ✗ |
  | 2 | 2026-08-16 | ✗ |
  | 3 | 2026-08-15 | ✗ |
  | **4** | **2026-08-14** | **✓ → `_eod_retrain_ok=True`, PreRetrain 스킵** |

  08-14 이후 거래일이 없어 새 레이블도 없으므로 **스킵이 옳은 동작**이다.
  → 즉 **오늘의 실패는 내일로 전파되지 않는다.**
  위험한 것은 결함의 성격이다: 같은 상황(본체 조기 종료·크래시)이 **거래일**에 일어나면
  EOD 재학습이 소리 없이 사라지고 모델 미교체가 CB③ HALT 경로에 그대로 올라타는데,
  현재 그 실패는 **"로그가 없다"로만 표현**되므로 기계가 잡을 수 없다.
- **신규 여부**: 장전 리포트 1-2의 **후속 확정**(신규 아님). 다만 위 세 갈래 스탬프 교차
  확인(마커 2종 + `daily_stats` DB 행)과 08-18 폴백 시뮬레이션은 본 리포트가 처음이다.

---

#### 1-2. `session_state.json`에서 `p8_last_success_date`·`eod_retrain_ok_date` 두 키가 사라졌다 — 기록 로그는 남아 있는데 값이 없다 🆕

- **증상**: 스킬 문서가 P8(스케일러 재적합) 완료 도장으로 지정한 두 키가 **현재 상태
  파일에 아예 존재하지 않는다.** "오늘 P8이 안 돌아서 안 찍혔다"가 아니라, **08-14에
  찍혔다는 로그가 있는데도 없다**는 것이 문제다.
- **근거**:
  ```
  $ cat data/session_state.json            # mtime 2026-08-17 08:41
  {"date": "2026-08-17", "count": 1, "reverse_entry_enabled": false,
   "tp1_single_contract_mode": "atr_profit", "auto_shutdown_done_date": ""}
  ```
  키 5개뿐 — `p8_last_success_date` · `eod_retrain_ok_date` **둘 다 없다.**
  그런데 08-14 EOD 로그는 기록 성공을 명시한다:
  ```
  2026-08-14 15:48:45,526 [INFO] EOD_RETRAIN: [P8] 스케일러 재적합 완료 n=500봉 elapsed=0.04s horizons=['1m','3m','5m','10m','15m','30m']
  2026-08-14 15:48:45,537 [INFO] EOD_RETRAIN: [P8] session_state p8_last_success_date + eod_retrain_ok_date 기록 완료
  ```
  `logs/retrain_eod_20260814.log:139-140` (`retrain_eod.py:190~194`가 쓰는 지점).
  → **08-14 15:48:45에 기록됐고, 08-17 08:41에 다시 쓰인 파일에는 없다.**

  소실 시점 사이의 모든 쓰기 경로는 **merge 기반**이라 키를 지울 수 없다
  (`main.py:2909 _write_session_state()`는 `_read_session_state()` 결과에 덧쓴다).
  단 하나 키를 지우는 분기는 `state_persist_enabled=False`일 때
  `profit_guard_state`·`circuit_breaker_state`를 `pop`하는 것뿐이고
  (실측 `data/ui_prefs.json` → `"state_persist_enabled": false` — 그 두 키 부재는
  이것으로 설명된다), **P8 키 2종은 그 pop 대상이 아니다.**

  현재 파일의 키 구성은 `_read_session_state()`의 **예외 폴백 기본값**
  `{"date": …, "count": 0}`(`main.py:2896`)에 기동 시 복원 3종
  (`reverse_entry_enabled` · `tp1_single_contract_mode` · `auto_shutdown_done_date`)이
  덧붙은 모양과 **정확히 일치**한다. 그러나 그 폴백이 남기는
  `[SessionState] load failed:` 경고는 최근 6일 로그 전체에서 **0건**이다
  (`grep -h "SessionState" logs/2026081*_*.log` → 무출력).
  → **폴백이 탄 흔적도, 안 탔다는 증거도 없다.**
- **기준 위반**: **계측 4원칙 ④(폴백 가시화)** 정면.
  CLAUDE.md는 *"`getattr(self,"x",default)` 형태로 런타임 상태를 읽지 마라 … 미설정은
  `None`으로 둬 폴백 시점에 로그를 남겨라"* 를 규약으로 못박았는데,
  `_read_session_state()`는 **예외 시 조용히 기본 dict를 반환**한다 —
  `except` 안에 warning이 있으나 **파일이 정상 파싱되면서 키만 없는 경우**는
  예외가 아니므로 어떤 경로로도 로그가 남지 않는다.
  같은 계열 사고가 CLAUDE.md에 4건 등재돼 있다(`recent_accuracy()` 빈 버퍼 → 조용히
  0.5, `_verified_today` 리셋 후 읽기 → 항상 0 등).
- **영향**: **현재는 차단돼 있다** — 1-1에서 확인한 마커 파일 폴백
  (`main.py:5069~`)이 같은 판단을 대신하고, 08-18에는 정상 동작한다.
  문제는 **1차 경로(session_state)가 죽은 채 2차 경로(마커 파일)만으로 돌고 있고 그
  사실이 아무 데도 안 보인다**는 것이다. 마커 파일은 **5일 창**이 한계라
  (`for _d in range(1, 6)`), 연휴가 5일을 넘거나 EOD가 며칠 연속 실패하면 두 경로가
  동시에 비고 PreRetrain이 매일 재실행된다 — 그때 09:00 첫 파이프라인 지연이
  CB⑤(5초)에 닿을 수 있다. **오늘 그 일이 일어났다는 뜻은 아니다.**
  또한 P8 미실행 자체의 영향(다음날 시초 z-score 흔들림)은 오늘의 경우
  **거래일이 아니어서 무해**하다.
- **신규 여부**: **신규.** `DECISION_LOG.md`·`NEXT_TODO.md`에 `p8_last_success_date`
  소실을 다룬 항목이 없다(grep 확인 — 13950행의 `catch_up_eod.py` 관련 기술은
  *"스크립트가 의도적으로 기록하지 않는다"* 는 **다른 사안**이다).
  ⚠ **근본 원인은 미확정** — §"확인 필요" 참조.

---

### P2 — 운영 부담·기술부채

#### 1-3. 장후 뼈대 4단계가 전부 "무증거"로 지나갔다 — 휴장일 정상과 장애가 로그상 구분되지 않는다 🆕

- **증상**: CLAUDE.md 매분 파이프라인이 정의한 장후 뼈대
  (`15:10 강제청산` → `15:18 안전망` → `15:40 자가학습 마감 + SHAP 심사` →
  `15:45 EOD 재학습`) 중 **네 단계 모두 아무 로그도 남기지 않았다.**
  오늘은 본체가 12:55에 종료됐으니 당연한 결과지만, **그 "당연함"이 로그에
  기록되지 않는다**는 것이 문제다.
- **근거**: 수집기 §11 적신호가 이를 그대로 오탐으로 올린다.
  ```
  6. 장후인데 15:10 청산 경로가 아무 흔적도 남기지 않았다 — 실집행(`강제청산`)도
     하트비트(`[SchedForceExit]`)도 없다. 절대원칙 1 확인 필요
  7. 완료 마커 `daily_close_done` 없음
  8. 완료 마커 `eod_retrain_done` 없음 — 없으면 모델 미교체 → 다음날 CB③ HALT 위험
  10. 진입 0건 — 차단 0건. (진입0 딥다이브 절차를 따르라)
  ```
  `docs/정기점검/매일점검/evidence_MW0601-20260817_post.md` §11.
  이 중 6·7·8·10은 **휴장일이면 전부 정상**인데 적신호로 뜬다.
- **기준 위반**: 직접적 위반은 아니다. **계측 4원칙 ②(미측정 ≠ 0)** 의 확장 —
  *"휴장이라 안 돈 것"* 과 *"돌았어야 하는데 죽은 것"* 이 **같은 표현(로그 부재)** 을
  갖는다. CLAUDE.md가 `program_*` 3종에서 *"원천이 안 주는 키를 `.get(key,0)`으로 채워
  상수 0이 정상 수집으로 위장"* 했다고 적은 것과 같은 구조다.
- **영향**: ① 장후 점검이 매 휴장일마다 4건의 오탐을 처리해야 한다(운영 비용).
  ② 더 나쁜 쪽 — 오탐에 익숙해지면 **진짜 15:10 미이행이 같은 자리에 섞여 들어와도
  걸러진다.** 471차 F-1이 복구한 그 경로는 6개월간 라이브 0회 실행이었고,
  그 사실이 드러나지 않은 이유가 정확히 이것("조용히 성립하는 것처럼 보였다")이다.
  ③ 오늘 `F-1H`(하트비트 1일차 확인)는 **판정 불가로 이월**된다 — 본체가 15:11에
  살아 있지 않았으므로 `[SchedForceExit] 15:11 점검 … bar_pass=N회`가 나올 수 없다.
- **신규 여부**: 장전 리포트 G-1("휴장일 정상 프로파일을 수집기가 인지하게 한다")이
  같은 축을 고도화 방안으로 제안했다. 본 리포트는 이를 **장후 관측으로 확정**하고
  이상점(P2)으로 격상한다 — 장후는 뼈대 4단계가 걸리는 국면이라 오탐 밀도가 가장 높다.

#### 1-4. 같은 날 점검 세션 3개가 동시에 같은 파일 트리와 `DECISION_LOG.md`에 썼다 🆕

- **증상**: 오늘 pre·intra·post 세 국면 예약 점검이 **16:48 / 16:49 / 16:55에 연달아**
  서로 다른 샌드박스에서 기동했고, 그중 pre 세션은 본 세션 진행 중인 **16:57에**
  리포트와 `DECISION_LOG.md` 항목(24235행~)을 기록했다.
- **근거**: 세 다이제스트 헤더의 리포 경로가 전부 다르다(= 별개 세션).
  ```
  evidence_..._pre.md   : 생성 16:48:00 KST · PC UNKNOWN (claude) · 리포 /sessions/determined-affectionate-lamport/...
  evidence_..._intra.md : 생성 16:49:16 KST · PC MW0601           · 리포 /sessions/epic-compassionate-mendel/...
  evidence_..._post.md  : 생성 16:55:24 KST · PC MW0601           · 리포 /sessions/dazzling-charming-mendel/...
  $ ls -la docs/정기점검/매일점검/ | grep 20260817
  MW0601-20260817-점검리포트-pre.md   36276  Aug 17 16:57   ← 본 세션 진행 중 생성
  ```
- **기준 위반**: 함정 ③(멀티PC·세션 규약) 및 SKILL.md §6
  *"기존 내용을 덮어쓰지 않는다 — 두 파일 다 280~315KB 누적 기록이다"*.
  추가로 pre 다이제스트는 **PC가 `UNKNOWN`** 이다 — SKILL.md 1-B
  *"그 상태로 커밋하면 어느 PC의 관찰인지 영영 모르게 된다"* 에 정면으로 걸린다
  (파일명만 `MW0601`로 맞춰져 있어 **헤더와 파일명이 어긋난 상태**다).
- **영향**: ① `DECISION_LOG.md`(1.8MB)에 동시 append가 겹치면 항목 유실 위험 —
  MW0602가 470차 후속7에서 *"동시 세션 reset으로 유실 → 재커밋"* 을 실제로 겪었다
  (`483d41a`). ② 장전 점검이 마감 후 도착하면 **국면의 존재 이유가 사라진다**
  (장전의 목적은 개장 전에 P0를 사람에게 올리는 것이다).
- **신규 여부**: 시각 지연 자체는 장전 리포트 1-6이 등록했다. **동시 세션이 같은
  누적 파일에 쓰는 위험**과 **pre 다이제스트 PC=UNKNOWN**은 본 리포트가 처음이다.

---

### 확인 필요 (확정 아님)

| # | 무엇이 미확정인가 | 무엇을 더 보면 판정되는가 |
|---|---|---|
| C-1 | **EOD 재학습이 왜 15:46:34에 멈췄는가** — 프로세스 강제종료 / 콘솔 창 닫힘 / 예외 후 로거 미기록 셋 중 무엇인지 로그만으로 불가 | Windows 이벤트 뷰어 15:46~15:48 프로세스 종료 기록 · 작업 스케줄러 `Maitreya_EODretrain` 마지막 실행 결과 코드 · `EOD_RETRAIN.bat` 콘솔 잔존 여부. **원인과 무관하게 "흔적을 안 남긴다"는 결함은 독립적으로 성립**하므로 F-1은 유효하다 |
| C-2 | **`p8_last_success_date` 2키가 언제·어떻게 사라졌는가**(이상점 1-2) — merge 기반 쓰기 경로만 있는데 키가 없다 | 08-15~08-17 사이 `data/session_state.json` 백업/복원·수동 편집 여부. **재현 절차**: 08-18 EOD 완주 후 두 키 재생성 확인 → 다음 기동(08-19 08:41) 후 잔존 확인. 사라지면 main.py 기동 경로, 남으면 08-17 단발 사건 |
| C-3 | **예약 점검 3종이 왜 16:48~16:55에 몰렸는가** | 08-18에 pre가 08:57에 도는지 1회 더 관측하면 재현성 판정. **1일 관측이므로 313차 원칙에 따라 확정 결론 보류** |
| C-4 | **`v9-dev`가 `dev`의 315커밋을 받아야 하는가** | 브랜치 분리는 설계이므로 점검 세션 단독 결정 사안이 아니다 — **주간회의 안건**. 본 리포트는 "감시표와 코드가 어긋나 있다"는 사실만 확정한다 |

### 이미 반영된 사안 (신규 아님)

- **장전 리포트 1-1~1-6 전량** — 미발견 상수 5종(1-1) · EOD 무기록 정지 최초 관측(1-2) ·
  TimeRouter 휴장일 전환(1-3) · 473차 미커밋(1-4) · 정기점검 산출물 untracked(1-5) ·
  예약 시각 지연(1-6). **재보고하지 않는다.**
  → `docs/정기점검/매일점검/MW0601-20260817-점검리포트-pre.md`
- **수집기 §11 적신호 12번 "미커밋 변경 454건"은 사실이 아니다 — 샌드박스 개행 아티팩트다.**
  이 점검은 Linux 샌드박스에서 돌았고 작업트리는 CRLF다. 실측:
  ```
  $ git diff --stat | tail -1
  430 files changed, 241400 insertions(+), 241016 deletions(-)
  $ git diff --ignore-cr-at-eol --stat | tail -1
  10 files changed, 404 insertions(+), 20 deletions(-)
  ```
  **실질 변경은 10파일**이며 그 내역은 장전 리포트 1-4가 이미 다뤘다.
  ⚠ 수집기 §2·§11의 미커밋 건수는 **샌드박스 실행 시 신뢰하지 말 것**.
- **`[SHAP] 주간 심사 완료 | … CORE안전=⚠️`(08:41:14)는 신규 신호가 아니다.**
  08-13 7회 · 08-14 10회 심사 전부 `CORE안전=⚠️`로 동일하다(만성).
  장전 리포트가 이미 동일 판단을 내렸다(`DECISION_LOG.md:24347`).
- **`[Calibration]` WARNING 143건도 신규가 아니다.**
  기동 워밍업 시퀀스이며 일별 실측이 평탄하다 — 08-10 157 / 08-11 148 / 08-12 145 /
  08-13 141 / 08-14 144 / **08-17 143**. 최종 상태도 정상 복원이다:
  `08:41:09 [Calibration] 보정기 복원 완료 (n=888 method=platt fitted=True degenerate=False unreachable=False)`.
  오늘 전체 로그가 288행뿐이라 **비율만 높아 보이는 착시**다(08-14는 2,814행 중 144건).
- **`data/_exit_normally` 없음(적신호 13번)은 정상.** 기동 시 소비되는 플래그다.
- **`15:10 강제청산` 전기간 0건은 이미 CLAUDE.md에 등재된 사실이다.**
  실측 재확인 `SELECT COUNT(*) FROM trades WHERE exit_reason LIKE '%15:10%'` → **0**.
  실전 전환 기준 ②의 선행 확인사항(471차 추가)이며 `NEXT_TODO` **F-1R**이 실행 항목이다.
  **오늘도 미실시** — 휴장일이라 리허설 자체가 불가능했다.

---

## 2. Fix 작업 구현계획

> **본 계획은 수립만 한다. 코드를 고치거나 배포하지 않는다** (예약 지시).
> 적용 시점은 전부 **08-18 기동 전(오늘 밤)** 또는 **08-18 장 마감 후**다.
> ⚠ 장전 리포트가 이미 F-1~F-4를 제안했다. 아래는 **번호를 `PF-`로 분리**하고,
> 겹치는 항목은 "장전 F-2와 통합"으로 명시한다.

### PF-1. `retrain_eod.py`에 거래일 게이트 + 완주/중단 흔적을 붙인다 — P1 · 대응 이상점 1-1

- **원인 가설**: `main()`이 무조건 `_wait_for_daily_close()`를 부르고, 그 함수는
  "마커가 아직 안 생겼다"와 "마커가 영원히 안 생긴다"를 구분하지 않는다.
  본체 부재를 감지할 수단이 없다.
- **변경 파일**:
  - `retrain_eod.py` — `main()` 진입부(현행 `_check_env()` 직후, `_MARKER_PATH` 검사 앞):
    `utils/time_utils.py`의 거래일 판정을 호출해 **휴장일이면
    `[EODGate] 비거래일 — EOD 재학습 스킵` INFO 1줄을 남기고 `sys.exit(0)`**.
    ⚠ 반드시 **로그를 남기고** 종료할 것 — 조용한 종료는 이 이상점 자체의 재생산이다.
  - `retrain_eod.py` — `_wait_for_daily_close()`(현행 224~239행 while 루프):
    폴링 시작 시 **본체 생존 확인**을 추가한다.
    판정 근거는 `logs/Mireuk_batch/launcher_*.log`가 아니라
    `data/_exit_normally` 또는 `data/session_state.json:auto_shutdown_done_date` —
    본체가 이미 종료했으면 20분을 기다리지 말고
    `[WaitDC] 본체 부재 확인 — 대기 생략, 강제 진행` WARNING 후 즉시 반환한다.
  - `retrain_eod.py` — `main()` 최상위를 `try/finally`로 감싸고 `finally`에서
    **완주 여부와 무관하게** `[EOD] 종료 code=… elapsed=…s marker={생성|미생성}`
    1줄을 남긴다. 여기가 이 fix의 핵심이다 — 오늘 없었던 것이 정확히 이 줄이다.
- **회귀 위험**: 낮음. 재학습 로직 자체는 무변경이다. 단 **거래일 판정 함수가
  `py310_64`에서 import 가능한지 확인**할 것 — `utils/time_utils.py`가 32-bit 전용
  COM 모듈을 끌어오면 import 단계에서 죽는다. 끌어온다면 `config/krx_holidays.py`만
  직접 읽는 경량 판정으로 대체한다.
- **절대원칙 저촉 여부**: 없음. ①~⑥ 어디에도 닿지 않는다(장외 배치, 매매 정책 무변경).
- **런타임 주의**: **`py310_64` 전용**(191차 결정). COM 콜백 아님.
- **검증**: `tests/`에 `test_474_eod_gate.py` 신설 —
  ① 휴장일 날짜 주입 시 `SystemExit(0)` + 로그 1줄, ② 본체 부재 시 대기 20분이
  아니라 즉시 반환, ③ 예외 발생 시에도 `finally` 종료 줄이 남는지.
  **라이브 판정**: 08-18(거래일) EOD 로그에 `[EOD] 종료 code=0 … marker=생성`
  1줄이 반드시 나올 것. 다음 휴장일에는 `[EODGate] 비거래일` 1줄만 나올 것.
- **섀도 계측 필요 여부**: 불필요(게이트·차단 로직 신설 아님, 배치 스크립트 위생).
- **적용 시점**: **08-18 장 마감 후.** 오늘 밤 고치면 08-18 EOD가 미검증 코드로 돈다.
- **결정 필요 사항**: 없음.

### PF-2. `session_state` 완료 스탬프를 "미측정 구분 가능"하게 만든다 — P1 · 대응 이상점 1-2

- **원인 가설**: 미확정(C-2). 다만 **어느 원인이든 조치는 같다** — 1차 경로가
  비었을 때 그 사실이 보여야 한다.
- **변경 파일**:
  - `main.py` — `_read_session_state()`(2887~2896행): 현행은 예외 시에만 warning이고
    **정상 파싱 + 키 부재**는 무음이다. `p8_last_success_date`·`eod_retrain_ok_date`가
    없을 때 `[SessionState] EOD 스탬프 부재 — 마커 폴백 사용` INFO 1줄을 남긴다.
    ⚠ **매분 호출되는 함수다** — 반드시 **상태 변화 시 1회만** 찍는다
    (471차 후속4 `entry_mode` 폴백이 잡은 관례: *"매분 찍으면 370줄/일"*).
  - `main.py` — `PreRetrain` 폴백(5069~5085행): 마커 파일 경로로 스킵을 결정할 때
    현행 로그가 `(session_state 미기록 보완)`이라고만 적는다.
    **몇 일 전 마커인지와 5일 창의 잔여**를 함께 찍는다
    (`_days_ago=4 / 창 잔여 1일`). 창이 닫히기 전에 사람이 알아채는 유일한 신호다.
  - `retrain_eod.py` — `p8_scaler_refit()`(184~196행): 쓰기 **직후 되읽어** 두 키가
    실제로 파일에 있는지 확인하고, 없으면 `[P8] session_state 기록 검증 실패` WARNING.
    현행은 `json.dump` 성공만으로 "기록 완료"를 찍는다 — 오늘 문제의 로그가 그것이다.
- **회귀 위험**: 낮음(로그·검증 추가, 판단 로직 무변경). 다만 `_read_session_state()`는
  고빈도 호출 경로이므로 **로그 폭주 방지 플래그를 반드시 동반**할 것.
- **절대원칙 저촉 여부**: 없음.
- **런타임 주의**: `main.py`는 **`py37_32`**, `retrain_eod.py`는 **`py310_64`**.
  두 파일을 한 커밋에 담되 문법은 각각의 런타임에서 컴파일 확인할 것.
- **검증**: `tests/test_474_session_state_stamp.py` —
  ① 키 부재 시 로그 1회만, ② 반복 호출 시 중복 없음, ③ `p8` 쓰기 후 되읽기 검증 실패
  경로가 WARNING을 남기는지.
  **라이브 판정(C-2 재현 절차와 동일)**: 08-18 EOD 완주 후 `session_state.json`에
  두 키 생성 확인 → 08-19 08:41 기동 후 잔존 확인.
- **섀도 계측 필요 여부**: 불필요.
- **적용 시점**: **08-18 장 마감 후** (PF-1과 같은 커밋 묶음).
- **결정 필요 사항**: 없음. ⚠ 단 **근본 원인 조사(C-2)를 이 fix로 갈음하지 말 것** —
  본 fix는 "다음에 또 사라지면 보이게" 만들 뿐 사라지는 것을 막지 않는다.

### PF-3. 수집기 §11이 비거래일을 인지하게 한다 — P2 · 대응 이상점 1-3

- **원인 가설**: `collect_evidence.py`의 적신호 판정이 "장후인데 X가 없다"를
  **거래일 여부와 무관하게** 평가한다.
- **변경 파일**:
  - `.claude/skills/mireuk-daily-check/scripts/collect_evidence.py` — 적신호 생성부:
    `config/krx_holidays.py`를 읽어 대상 날짜가 비거래일이면
    다이제스트 **첫 줄에 `· 비거래일(휴장)` 배지**를 찍고,
    6·7·8·10번 적신호를 **삭제하지 말고 `(비거래일 — 정상)` 접미**로 강등한다.
    ⚠ **삭제는 금물** — 계측 4원칙 ③(탈락 가시화). 없어진 신호는 확인할 수 없다.
- **회귀 위험**: 거래일 판정이 틀리면 **진짜 15:10 미이행이 강등되어 숨는다.**
  그래서 삭제가 아니라 강등이고, 강등 사유를 문자열로 남긴다.
- **절대원칙 저촉 여부**: 없음(점검 도구, 라이브 무관).
- **런타임 주의**: 수집기는 표준 라이브러리 전용 — `krx_holidays.py`가
  외부 의존을 끌어오면 **날짜 리스트만 정규식으로 파싱**할 것.
- **검증**: 2026-08-17 날짜로 `--phase post` 재실행 → 적신호 6·7·8·10이
  `(비거래일 — 정상)`으로 표시되고 1·2·3·4·5(브랜치 격차)는 **그대로 남는지** 확인.
- **적용 시점**: 즉시 가능(라이브 무관). 다만 **PF-1·PF-2와 함께 커밋**해 이력을 묶는다.
- **결정 필요 사항**: 없음. 장전 리포트 **G-1과 동일 축** — 중복 구현 금지, 통합할 것.

### 적용 순서와 커밋 계획

1. **커밋 ①** — 473차 미커밋 10파일 + untracked 2스크립트
   (장전 리포트 F-3와 동일 항목, **중복 실행 금지**)
   → `[MW0601] 473차: F-8 Phase A/B — spread 섀도 계측 배선 + 테스트 격리`
2. **커밋 ②** — PF-1 + PF-2 (`retrain_eod.py` · `main.py` + 테스트 2종)
   → `[MW0601] 474차: EOD 완주 흔적 · 거래일 게이트 · session_state 스탬프 가시화`
3. **커밋 ③** — PF-3 (수집기 비거래일 인지, 장전 G-1 통합)
   → `[MW0601] 474차 후속: 수집기 §11 비거래일 배지 — 오탐 4건 강등`
4. **커밋 ④** — 정기점검 산출물 16종 추적 편입(장전 리포트 1-5 / F-3 후속)

---

## 3. 고도화 방안

당일 관측에서 출발한 것만 쓴다.

### G-1. 재학습 파이프라인에 **완주 마커**를 세운다 — 시작 흔적과 종료 흔적을 분리한다

- **관측 근거**: 오늘 EOD 재학습은 **시작 로그 7줄**을 남기고 사라졌다
  (`15:45:04` 헤더 6줄 + `[WaitDC]` 시작 1줄). 현행 체계에서 "완주"는
  `eod_retrain_done_{d}.txt` 생성 **하나뿐**이고, 그것이 없을 때 남는 정보는
  "없다"뿐이다. 08-14 대조군은 34,103B / 16:05:21 종료인데 오늘은 1,040B / 15:46:34다 —
  **크기와 마지막 시각만으로 사후 판별해야 했다.**
- **제안 내용**: 시작 시 `data/eod_retrain_running_{d}.txt`(PID·시작시각) 생성,
  정상·비정상 종료 모두 `finally`에서 삭제 + 결과 마커 기록.
  다음 세션이 `running` 마커만 남은 것을 보면 **"시작했고 끝내지 못했다"** 를
  즉시 안다. 수집기 §11에 `EOD 중단 흔적` 적신호로 배선한다.
- **기대 효과**: 오늘 같은 사건의 **탐지 지연 0**. 현재는 사람이 로그 크기를 비교해야
  알 수 있고, 실제로 오늘 세 개 점검 세션 중 pre 세션이 우연히 잡았다.
- **비용·위험**: 파일 2개 · 30줄 규모. 위험은 **마커 잔존**(강제종료 시 `running`이
  남는다) — 그것이 곧 의도한 신호이므로 문제가 아니지만, 기동 시 **전일 이전
  `running` 마커는 정리**해야 무한 누적을 막는다.
- **선행 조건**: PF-1(`try/finally` 도입)이 먼저다 — 같은 구조를 두 번 만들지 않는다.
- **우선순위 제안**: **이번 주.**

### G-2. "휴장일 정상 프로파일"을 기준선으로 저장한다 — 오탐을 지우는 대신 대조군을 만든다

- **관측 근거**: 오늘 로그 전체가 288~2,787행 스케일로 **평상시의 1/10**이다
  (LEARNING 288행 vs 08-14 2,814행). 진입·차단·사이저·CB 전부 0.
  이 프로파일은 **휴장일에는 정상, 거래일에는 중대 장애**다.
  현재 그 둘을 가르는 기준선이 어디에도 없다.
- **제안 내용**: 비거래일에도 수집기를 돌려
  `docs/정기점검/매일점검/baseline_nontrading_<PC>.json`에
  로그별 행수·채널 분포·마커 유무를 누적한다(휴장일마다 1행).
  거래일 점검에서 당일 프로파일이 **비거래일 기준선에 가까우면** 적신호로 올린다 —
  "장이 열렸는데 휴장일처럼 조용하다"가 자동 탐지된다.
- **기대 효과**: 471차가 6개월 만에 발견한 유형("조용해서 아무도 몰랐다")의
  탐지 시간을 **1거래일**로 단축.
- **비용·위험**: 기준선 표본이 적다(휴장일은 연 10여 일). **판정에 쓰지 말고
  경보에만 쓸 것** — 313차 원칙(표본 부족 시 확정 결론 금지).
- **선행 조건**: PF-3(비거래일 인지).
- **우선순위 제안**: **26주 WFA 주기** — 급하지 않고 표본이 시간으로만 쌓인다.

### G-3. 점검 세션 동시 실행을 파일 잠금으로 직렬화한다

- **관측 근거**: 오늘 pre·intra·post 세 세션이 16:48~16:57에 겹쳐 돌았고,
  pre 세션이 **본 세션 진행 중** `DECISION_LOG.md`(1.8MB)에 append했다.
  MW0602는 이미 같은 사고로 항목을 잃고 재커밋했다(`483d41a` 470차 후속7
  *"동시 세션 reset으로 유실"*).
- **제안 내용**: 수집기 `--out-auto` 경로에 이미 있는 `_<HHMM>` 충돌 회피(461차 F-6)를
  **`dev_memory` 쓰기까지 확장**한다. 구체적으로 `dev_memory/.dailycheck.lock`을
  두고, 잠금 획득 실패 시 append를 보류한 뒤
  `dev_memory/pending/<PC>-<날짜>-<국면>.md`에 조각으로 떨어뜨린다.
  다음 세션이 시작할 때 `pending/`을 먼저 흡수한다.
- **기대 효과**: 누적 기록 유실 0. 오늘처럼 3세션이 겹쳐도 순서만 늦어진다.
- **비용·위험**: 잠금 파일 스테일 위험(세션이 죽으면 잠금 잔존) — **mtime 10분 초과
  잠금은 무시**하는 규칙을 함께 넣는다. `.git/index.lock` 스테일 제거 전례가
  `DECISION_LOG.md`에 이미 있다(471차 [참고]).
- **선행 조건**: 없음.
- **우선순위 제안**: **이번 주** — 예약 3종이 계속 몰려 도는 한 매일 재발한다.

### 로드맵·실전전환 기준 반영 제안

| 항목 | 현 위치 | 제안 | 사유 |
|---|---|---|---|
| **F-1R 15:10 강제청산 리허설** | `NEXT_TODO` 최우선 · 실전 전환 기준 ② 선행 확인사항 | **08-18로 이월 확정** | 오늘 휴장으로 불가. 08-18은 거래일이며 지연 없이 실행 가능 |
| **F-1H 하트비트 1일차 확인** | `NEXT_TODO` 최우선 | **08-18로 이월** | 본체가 15:11에 부재해 `[SchedForceExit]` 관측 자체가 불가능했다 |
| **F-4M 스키마 마이그레이션 1일차** | `NEXT_TODO` 최우선 | **08-18로 이월** | 오늘 파이프라인 미가동 → `ensemble_decisions` 신규 행 0. 473차 컬럼 2종(`spread_ticks`·`spread_extreme_shadow`)도 같은 날 확인 대상에 합류 |
| **EOD 완주 관측** | 없음 | **신설 — 26주 WFA 재검증 항목 후보** | 15:10 리허설(471차 G-3)과 같은 성격이다: "코드에 있으니 지켜진다"를 실측으로 대체해야 하는 경로. 다만 **오늘 1건 관측이므로 지금 편입을 확정하지 않는다** — PF-1 배포 후 2주 관측 뒤 재론 |

---

## 4. 진입 승패 사후검증 (장후 한정)

**4-0. 선행 확인 — 두 관문 모두 미완료다.**

| 관문 | 상태 | 근거 |
|---|---|---|
| EOD `daily_close()` (15:40) | ❌ **미실행** | `data/daily_close_done_20260817.txt` 부재 · `daily_stats`에 `2026-08-17` 행 없음 |
| **P8** `p8_last_success_date` | ❌ **미실행 + 키 자체 부재** | `session_state.json`에 키 없음 (이상점 1-2) |

> 그러나 **이것이 사후검증을 막지는 않는다** — 오늘은 휴장일이라 검증할 진입이 0건이다.
> DB 접근·수집기 실행 모두 성공했다(stderr 없음). 장중 DB 분석 차단 구간도 아니다
> (현재 16:55 > 15:35).

**4-1. 오늘 할일 4분류**

| # | NEXT_TODO 항목 | 당일 커밋 | 코드 실재 확인 | 상태 |
|---|---|---|---|---|
| 1 | F-8 Phase A — `spread_extreme_watch` 소급 판정 | 없음(미커밋) | `scripts/spread_extreme_watch.py` (untracked, 실재) | 🔄 **완료-미커밋** |
| 2 | F-8 Phase B — `ensemble_decisions.spread_ticks`/`.spread_extreme_shadow` 배선 | 없음(미커밋) | `utils/db_utils.py`(+22) · `learning/prediction_buffer.py`(+16) | 🔄 **완료-미커밋** |
| 3 | 테스트 오염 격리 (`feature_exclusion_state.json`) | 없음(미커밋) | `tests/conftest.py`(+40) autouse 격리 실재 | 🔄 **완료-미커밋** |
| 4 | D8 피처 슈퍼셋 확장 인프라 | 없음(미커밋) | `scripts/feature_superset_expand.py` (untracked, 실재) | 🔄 **완료-미커밋** |
| 5 | **F-1R 15:10 강제청산 리허설** | — | — | ⬜ **미착수(휴장으로 불가)** |
| 6 | **F-1H 하트비트 1일차 확인** | — | — | ⬜ **미착수(관측 불가)** |
| 7 | **F-4M 스키마 마이그레이션 1일차 확인** | — | — | ⬜ **미착수(신규 행 0)** |
| 8 | UI Phase 5 전환 게이트 자동 판정 | **`e995764`** | `dashboard` 배지 로직 | ✅ **완료-커밋확인** |

> ⚠ **`⚠완료주장-커밋없음`은 0건이다.** 1~4는 "커밋만 안 된 것"이며 코드 실재를
> 파일 단위로 확인했다 — 상태를 🔄(범위변경/미커밋)로 분류한다.
> 커밋은 §2 커밋 ①로 등록돼 있다.
> 5~7은 **미착수가 아니라 물리적 불가**다(휴장일). 08-18로 이월한다.

**4-2. 3원 대사 (포지션 단위)**

| 원천 | 질의 | 값 |
|---|---|---|
| 로그 | `[Position] 진입` / `[체결진입]` | **0** / **0** |
| `ensemble_decisions` | `ts LIKE '2026-08-17%'` | **0** (최종 행 `2026-08-14 15:08:00`) |
| `predictions` | `ts LIKE '2026-08-17%'` | **0** (최종 행 `2026-08-14 15:08:00`) |
| `trades` | `COUNT(*) WHERE entry_ts LIKE '2026-08-17%'` | **0** |
| (참고) `trades` | `COUNT(*) WHERE exit_ts LIKE '2026-08-17%'` | **0** |

**3원 일치 — 불일치 0건.** 휴장일이므로 0이 정답이다.

**4-3. 케이스 카드** — **해당 없음(진입 0건).**

**4-4. 요인 집계** — **해당 없음(진입 0건).**

참고로 **직전 7거래일 포지션 단위** 집계를 남긴다. 계측 4원칙 ① 준수 —
포지션은 `entry_ts||direction||entry_price` 고유조합으로 세고, `COUNT(*)`(청산 레그)와
분리 표기한다. **레그 수가 더 많은 것은 불일치가 아니다.**

| 거래일 | 포지션 | 청산 레그 | 순손익(원) |
|---|---|---|---|
| 2026-08-05 | 19 | 22 | +26,663 |
| 2026-08-06 | 7 | 8 | −53,826 |
| 2026-08-07 | 11 | 18 | +690,825 |
| 2026-08-10 | 10 | 13 | −244,061 |
| 2026-08-11 | 3 | 6 | −3,760 |
| 2026-08-12 | 6 | 11 | +170,448 |
| 2026-08-14 | 1 | 2 | +28,730 |
| **2026-08-17** | **0** | **0** | **0 (휴장)** |

⚠ 이 표는 **참고용 맥락**이지 오늘의 판정 근거가 아니다. 7거래일 표본으로
방향성 결론을 내지 않는다(313차 원칙).

**4-5. 313차 가드 통과 여부**

| 갈래 | 적용 결과 |
|---|---|
| ① 일자단위 판정 | **관측일 0일 — 판정 불가** |
| ② overlap 보정 | 해당 없음(표본 0) |
| ③ 이상치 분해(drop-worst) | 해당 없음(표본 0) |
| ④ 사전등록 — 기준 불변 | **준수.** 본 리포트는 어떤 합격선·임계도 제안하지 않는다 |
| ⑤ 부호 일관성(기간·PC 교차) | 해당 없음. ⚠ MW0602와의 교차 대조는 **코드 세대가 달라**(v9-dev↔dev 320/315커밋) 현재 불가 |

---

## 5. 수익률 향상방안 (장후 한정)

**도출 불가 — 당일 진입 표본 0건.**

§4-4 요인 집계가 비어 있으므로 여기서 방안을 쓰면 **당일 관측이 아니라 일반론**이 된다.
템플릿이 요구하는 6칸 중 `근거`(CASE-ID + 요인 태그)와 `표본 상태`를 채울 수 없다.

> 이 절을 억지로 채우지 않는 것이 규약 준수다 — SKILL.md §5
> *"고도화 방안은 당일 관측에서 출발해야 한다. 일반론 금지."*
> 08-18 장후 점검에서 진입이 발생하면 그때 작성한다.

---

## 6. 다음 거래일(2026-08-18) 관측 예정

| ID | 관측 대상(로그 패턴/지표) | 판정 기준 | 판정 예정일 |
|---|---|---|---|
| O-1 | EOD 재학습 완주 — `logs/retrain_eod_20260818.log` | 파일 크기 10KB 이상 + `horizons_replaced: 6/6` + `data/eod_retrain_done_20260818.txt` 생성. **하나라도 빠지면 1-1이 거래일에 재현된 것 → 즉시 P0** | 2026-08-18 |
| O-2 | `session_state.json` P8 키 재생성 (C-2 1단계) | EOD 완주 후 `p8_last_success_date`·`eod_retrain_ok_date` **둘 다** 존재 | 2026-08-18 |
| O-3 | `session_state.json` P8 키 잔존 (C-2 2단계) | 08-19 08:41 기동 후에도 두 키 잔존. 사라지면 **main.py 기동 경로가 범인**으로 확정 | 2026-08-19 |
| O-4 | **F-1H** 하트비트 — `logs/20260818_SYSTEM.log` | `[SchedForceExit] 15:11 점검 … bar_pass=N회` **1건** 출현. `bar_pass=0`이면 F-1 경로 회귀 | 2026-08-18 |
| O-5 | **F-4M** 스키마 — `PRAGMA table_info(ensemble_decisions)` | `entry_block_axes`·`health_preblock`·`entry_mode_fallback` + **473차 신규 `spread_ticks`·`spread_extreme_shadow`** 존재 + 당일 행에 값 적재. ⚠ 이전 행 NULL은 **미측정**(계측 4원칙 ②) | 2026-08-18 |
| O-6 | **F-1R** 15:10 강제청산 리허설 — **사용자 실행 필요** | 15:05~15:09 모의 1계약 진입 → `[ForceExitPass]`→`[TimeExit]`→`[ExitAttempt]`가 15:10:00~05 출현 + `[SchedForceExit]` ERROR **미출현** | 2026-08-18 |
| O-7 | 예약 점검 시각 (C-3) | 장전 점검이 **08:57±5분**에 실행되는가. 다시 늦으면 2일 연속 → 재현성 확정 | 2026-08-18 |
| O-8 | 12:55 수동 종료 후 **08-18 08:40 자동 기동 여부** | `logs/Mireuk_batch/launcher_20260818_*.log` 존재. 없으면 사용자 수동 기동 필요 — **당일 거래 전면 불가로 이어지는 경로** | 2026-08-18 |

---

## 7. 기한 도래 항목

| 항목 | 기한 | 남은 일수 | 조치 |
|---|---|---|---|
| **CB② 복원** (`CB_CONSEC_STOP_LIMIT` 9999 → 2~3) | 2026-08-29 | **12일** | 08-29 주간회의 상정. 오늘 실측 `9999` 유지 확인(수집기 §3 `일치`). ⚠ 오늘은 휴장이라 표본 진전 0 |
| **실전 전환 기준 ⑨** (`TOX-SEVERE-SPREAD` 복원 여부) | 기한 없음 — **주간회의 안건화 완료** | — | 473차 F-8 Phase A가 `INSUFFICIENT` 판정(진입 6포지션/4거래일, ETA 7.1개월). ⚠ **문턱을 낮춰 판정하지 말 것**(458차 D6 위반). 선행조건이던 섀도 계측 배선은 Phase B로 완료 |
| **26주 WFA — Hurst 파라미터 재검증** (`HURST_WINDOW_N=90`/`MAX_LAG=9`) | 다음 WFA 주기 | — | 오늘 실측 `90`/`9` 일치. 조치 불요 |
| **26주 WFA — 15:10 강제청산 리허설 편입** (471차 G-3) | 다음 WFA 주기 | — | 문서 등재 완료(`f911e8d`). **1회차 리허설 자체는 미실시** — O-6으로 이월 |
| **브랜치 격차 처분** (`v9-dev` ↔ `dev` 320/315) | 미정 | — | **주간회의 안건**(C-4). 점검 세션 단독 결정 사안 아님 |

---

## 8. dev_memory 반영

- `DECISION_LOG.md` 추가 항목:
  `## 2026-08-17 (MW0601 473차 후속2 — 장후 점검 / KRX 휴장일)`
- `NEXT_TODO.md` 추가 체크박스: **7건**
  (PF-1 · PF-2 · PF-3 · G-1 · G-2 · G-3 · C-2 근본원인 조사)
- 완료 처리한 기존 항목: **없음**
  (F-1R·F-1H·F-4M은 휴장으로 **이월**이지 완료가 아니다)
- **커밋하지 않는다** — 사용자가 직접 확인 후 커밋한다.

---

*본 리포트는 코드를 변경하지 않았고, 라이브 DB는 읽기전용으로만 조회했다(현재 15:35 이후로
장중 분석 차단 구간이 아니다). 인용한 모든 수치는 2026-08-17 16:55~17:05 KST 실측이다.*
