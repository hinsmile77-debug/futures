# MW0601 이관 점검사항 — 멀티PC 정책 폐기(2026-08-23) 시점 스냅샷

> **작성**: MW0602 487차 (2026-08-23, 사용자 지시)
> **배경**: 2026-08-23부로 멀티PC 정책(CLAUDE.md 「멀티PC 작업 컨벤션」)이 폐기됐다.
> 이후 `dev`(MW0602)와 `v9-dev`(MW0601)는 컨벤션·안건·채널 번호를 상호 조율하지
> 않는다. 이 문서는 폐기 시점에 **MW0601 몫으로 남는 확인사항**을 한 곳에 모아둔
> 인수인계 기록이다 — MW0601 세션이 이 파일을 읽게 되면 아래를 자기 갈래에서
> **자체적으로** 판단하면 되고, `dev` 쪽 결정을 따를 의무는 없다.

---

## 1. 캠페인 채널 [50]·[51] — 소비부·생산부 정합 자체 확인 (0821 이상점 1-17 대응)

- **사실관계**: 생산부 커밋 `080c982`(457차 후속 G4~G9 — `scripts/direction_bias_watch.py`
  신설 + `scaler_daily.const_out_by_horizon` 컬럼)는 **`v9-dev`에만** 있다. 소비부
  커밋 `e9f1bdf`(471차 후속7 — 리포트 생성기의 채널 [50]/[51] 소비 경로)만 `dev`에
  들어와, `dev`의 주간 리포트에서 두 채널이 "실행 불가"로 죽어 있었다.
- **dev 쪽 처분(MW0602 한정)**: 생산부 체리픽 **안 함**. 두 채널은
  `NOT_AVAILABLE_ON_THIS_BRANCH` 명시 표기(안 B)로 처리 예정 — 상세는
  `dev_memory/DECISION_LOG.md` 2026-08-23(487차).
- **MW0601 확인사항**: `v9-dev`에는 생산부·소비부가 둘 다 있을 것이므로 두 채널이
  정상 판정을 내는지 **자기 갈래 주간 리포트에서 확인**할 것. 만약 `v9-dev`에도
  `INSUFFICIENT`가 뜨면 그건 브랜치 결손이 아니라 별개 결함이다.

## 2. 캠페인 채널 번호 `[51]` 중복 — 이후 두 갈래의 번호는 서로 비교 불가 (0821 이상점 1-18 / F-9)

- **사실관계**: `[51]`이 `dev`에서는 「저변동성 이익기하 붕괴」(462차, 2026-08-11 선착),
  `v9-dev` 유래 소비부에서는 「ConstOut 호라이즌 건강도」로 **같은 번호가 다른 채널**을
  가리킨다.
- **dev 쪽 처분**: 양 PC 동시 적용 요건이 정책 폐기로 소멸 → `dev` 단독으로 ConstOut을
  **`[54]`로 재배정** 예정([55]는 486차 신설 채널이 이미 선점). 채널 **키 문자열은
  불변**(번호는 표기 층일 뿐, 이력 식별자는 키다 — 461차 `mdd_pct` 교훈).
- **MW0601 확인사항**: 자기 갈래의 채널 번호 체계를 자체 관리할 것. **2026-08-23 이후
  두 PC 리포트의 채널 번호는 서로 비교 불가**다 — 대조가 필요하면 번호가 아니라
  **채널 키 문자열**(`const_out_horizon_watch` 등)로 할 것.

## 3. `phases.md` B-1·B-2 정정 — `dev`에는 반영 완료, `v9-dev`는 자체 확인

- `dev` 커밋 `54bff4a`(2026-08-23)가 점검 체크리스트 두 항목을 정정했다:
  - B-1: "T-30은 퇴역 대상" → **"T-30 채점은 계속돼야 한다(CB③·Contrarian 유일 입력원)"**
  - B-2: "GBM 재학습이 30분마다" → **조건부 트리거 3종(미발동 자체는 이상 아님)**
- 이 정정의 원 등록자는 MW0601(그쪽 F-8)이었다. `v9-dev`의
  `.claude/skills/mireuk-daily-check/references/phases.md`에 같은 정정이 반영됐는지
  **자체 확인**할 것 — 반영 전이면 그쪽 장중 점검이 거짓 이상점 2종을 계속 만든다.

## 4. `utils/analysis_db.py` — `v9-dev` 전용 모듈, `dev`에는 없음

- `scripts/monthly_cleanup.py:387`이 `utils.analysis_db.guard_intraday()`(479차, MW0601
  작성)를 임포트하는데 **`dev`에는 모듈이 없어** 경고 후 우회된다(2026-08-23 실측).
- **dev 쪽 처분**: 체리픽하지 않는다. 가드 필요성은 낮고(발화 지점이 장 마감 후 EOD
  체인뿐) 자체 구현 여부는 `dev` 백로그(P3)로 관리.
- **MW0601 확인사항**: 없음(그쪽엔 모듈이 있다). 단 앞으로 `v9-dev`에서 만든 모듈을
  전제하는 스크립트가 `dev`로 넘어오는 일은 더 이상 없어야 한다는 것이 이 사례의
  교훈이다 — 정책 폐기로 그런 이동 자체가 중단된다.

## 5. 주간 PC 대조(`cmp_summary.py` / `cmp_metrics.py`) — 의무 종료

- 채널 집합·번호가 갈라지므로 이후 대조 출력의 "차이"는 PC 차이가 아니라 **갈래
  차이**다(스크립트 자체가 경고하던 "코드 세대 차이"가 상시화됨). 두 PC 모두 주간
  대조를 **정기 절차에서 제외**할 것. 스크립트는 삭제하지 않는다(과거 주차 소급
  대조용).

## 6. dev_memory 세션 차수 — 충돌 방지 책임이 각 PC로

- 지금까지는 원격 기준으로 차수를 맞췄다(392차 관행). 조율 종료 후 두 PC가 같은
  번호를 독립적으로 쓸 수 있다(2026-07-02 "273차" 충돌 전례). **PC명 병기(`MW0601
  487차` 형식)는 양쪽 다 유지**하는 것을 권고 — 이것이 남아 있는 한 번호가 겹쳐도
  출처는 갈린다.

## 7. `dev` 브랜치 push — MW0601의 pull 의무 없음

- 2026-08-23 현재 `dev` 로컬 커밋(485~487차)은 push 대기 상태다. push 되더라도
  MW0601이 이를 당겨 반영할 의무는 없다. 반대로 `v9-dev`의 변경도 `dev`가 가져가지
  않는다.

---

*이 문서는 폐기 시점 스냅샷이며 이후 갱신하지 않는다. `dev` 쪽 후속 결정은
`dev_memory/DECISION_LOG.md`가 정본이다.*
