# 2026-07-19 주간회의 검토 보고 — MW0601 (라이브 운영 PC) 캠페인 7/5~7/16 데이터

- 작성: Claude (사용자 요청 — 7/5 수립 안건 6개에 대해 7/16까지 수집데이터 정밀 검토,
  **MW0602 357차(7/19) 동일 주제 개선 내용 반영**)
- 관계: `0719_주간회의_검토보고.md`(MW0602 데이터 기준)의 **자매편** — 이 문서는
  MW0601(라이브 운영, v9-dev, Cybos)의 자체 데이터로 동일 안건을 재판독한 것.
  두 PC는 trades/predictions DB가 서로 다르므로 판정도 PC별로 별도 근거가 필요.
- 데이터 소스: `data/validation_campaign_report.md`(2026-07-19 12:19 본 세션 수동 생성,
  --days 28 — MW0601 최초 생성), `data/db/trades.db`, `data/db/predictions.db`,
  `data/daily_reports/strategy_report_202607{14,15,16}*.txt`,
  `data/gate_ablation_report.txt`(2026-07-19 본 세션 최초 생성, 7일 창),
  `dev_memory/DECISION_LOG.md`(357차), git reflog(cherry-pick 시점 실측)
- 유령 레코드: 7/9 `GHOST_PENDING_MISS` 2건(-675만원 허수, 306차 Chejan echo —
  7/12 태깅 완료)은 **모든 집계에서 제외** (기존 컨벤션 준수)

---

## 요약 (TL;DR)

1. **MW0601은 341~355차 수정 15건이 전부 7/17 00:29에야 cherry-pick됨** (reflog 실측)
   — 7/16까지의 모든 거래는 구코드로 실행됐고, **7/20(월)이 이 PC의 첫 라이브 검증일**.
   340차 사이징 상향(5천만원/3%)도 7/20 첫 적용 — 아래 M-P0-2의 동시 라이브 리스크 참조.
2. **캠페인 실손익(유령 제외) -3,643,302원** (22건): W1 -433만(13건, 평균 3.5계약,
   최대 10계약) → W2 +69만(9건, 전부 1계약). **손실의 몸통은 W1 대형 사이즈 ×
   하드스톱(틱) 3건(-654만)** — 10계약 -6.35pt, 9계약 -4.04pt, 10계약 -2.01pt.
   341차(유령 하드스톱)·348차(틱스톱 60초 지연)가 겨냥한 바로 그 패턴이며 당시 미수정
   코드였음. W2에 손실이 멎은 것은 수정 효과가 아니라 **켈리 슬럼프로 사이즈가 1계약에
   눌린 효과**임에 주의.
3. **등급 역전 MW0602와 동일 재현**: A등급 13건 -469만(승률 54%) vs C등급 9건
   +105만(승률 67%) — 두 PC 독립 데이터에서 같은 방향. 단 원인(틱스톱 대형 손실)이
   수정된 상태라 357차 P1 결론(등급 수술 보류, 7/24 실측 재집계) 그대로 적용.
4. **[2] Meta-Gate·[3] 분위회귀는 MW0601에서도 사망** — 단 원인이 다름: MW0602는
   "버전 불일치 pkl", MW0601은 **모델 디렉토리 자체가 없음**(한 번도 학습된 적 없음).
   `ensemble_decisions` 16,976행 전 기간 non-null 0건 실측. 357차 NO-DATA 계측이
   MW0601 리포트에서 정확히 🔴 NO-DATA로 구분 표기됨(계측 개선 검증 성공).
   → **본 세션에서 재학습·소급 완료** (아래 체인 실행 결과).
5. **MW0601 고유 경보: [0] 표본 기아 🚨 STARVED** (주간 9건 < 하한 10, MW0602는 24건
   정상) — 사다리 2단계(MetaGate take_ceil 0.570→0.52) 검토 위치. 단 7/15~16 최대
   차단 사유가 "사이저 산출 수량 0"(켈리 슬럼프)이라 340차 상향이 직접 겨냥 —
   **2단계 적용은 보류하고 7/20~24 신코드 자연 회복 관찰 후 재판정 권고** (M-P1).
6. §4-1 주간 체인(ablation·캠페인 리포트·TB/분위/메타라벨 재학습)을 이 PC에서 **수동
   소급 실행 완료** — MW0601 사상 첫 ablation 리포트·캠페인 리포트 생성 포함.

---

## 안건 ① 순EV 추이 (유령 제외, MW0601 실측)

| 구간 | 거래일 | 건수 | 순PnL(원) | 비고 |
|---|---|---|---|---|
| W1 (7/5~7/12) | 7/9, 7/10 | 13 | **-4,332,002** | 7/9 -191만 / 7/10 -242만 |
| W2 (7/13~7/16) | 7/14~16 | 9 | **+688,700** | 7/14 +42만 / 7/15 +33만 / 7/16 -6만 |
| 캠페인 누적 | 5거래일 | 22 | **-3,643,302** | |

**등급별 (캠페인 기간, 유령 제외)**:

| 등급 | 건수 | 순PnL | 승률 | 해석 |
|---|---|---|---|---|
| A | 13 | **-4,689,034원** | 54% | 하드스톱(틱) 8건 -607만 — 그중 대형 3건(-654만)이 전부 |
| C | 9 | **+1,045,732원** | 67% | MW0602와 동일하게 C등급이 흑자 |

**대형 손실 상세 (전부 W1·구코드)**:

| 일자 | 등급 | 계약 | 순PnL | pnl_pts |
|---|---|---|---|---|
| 7/9 | A | 10 | -3,192,244 | **-6.35pt** |
| 7/10 | A | 9 | -1,835,349 | **-4.04pt** |
| 7/10 | A | 10 | -1,023,275 | -2.01pt |

-6.35pt·-4.04pt는 정상 스톱폭을 크게 초과 — 341차가 수정한 "트레일링 갱신 후 과거
봉고저가 소급" 유령 하드스톱 및 348차가 수정한 60초 지연 슬리피지와 정확히 부합하는
프로파일. **평균 계약수 W1 3.5 → W2 1.0**: W2 개선은 사이즈 축소 효과이므로
"손실 원인이 치유됐다"는 증거로 쓸 수 없음 — 7/20 이후 실측만이 증거.

**판독**: MW0602 판정(357차 P1: 등급-사이징 수술 보류, 수정 효과 실측 후 재평가)을
MW0601 데이터도 독립적으로 지지. 다만 MW0601은 사이즈가 클 때 같은 결함의 손실
배율이 커진다는 점이 추가 확인됨 → M-P0-2의 조건부 롤백 기준 제안으로 연결.

---

## 안건 ② 섀도우 KPI 진행률 (MW0601)

| 채널 | 진행률 | 상태 |
|---|---|---|
| [0] 표본 기아 | **🚨 STARVED** 주간 9건 < 10 | MW0601 고유 — M-P1 참조 (MW0602는 24건 정상) |
| [1] TB | OOS 1m 753/800, 3m 243, 5m 145, 10m 70, 15m 45, 30m 20 | MW0602와 유사 진도. 357차 해석(승격 대상 3m/5m 한정) 그대로 적용 |
| [2] Meta-Gate | **0/90 → 🔴 NO-DATA** | 모델 미학습(디렉토리 부재). 본 세션 재학습 완료 — 7/20부터 적재 시작 예상 |
| [3] 분위회귀 | **0/300 → 🔴 NO-DATA** | 동일 — 본 세션 재학습 완료 |
| [4] 신호소멸청산 | 1/10 (saved -0.44pt) | 339차 복구 후 적재 확인 — 정상 |
| [5] 레짐 ATR | trend 7/20, neutral 11/20, mean-revert 4/20 | trend -419만은 위 틱스톱 대형 손실이 지배 — 판정 오염 주의 |
| [6] Hurst | ❌ FAIL (n=165, hyp +42.3pt, 승률 69.7% vs 기준선 59.1%) | MW0602(+43.6pt)와 동일 방향 — 완화 유지·추가 완화 기각(357차 판정 적용) |
| [7] JointGate | 6/20 (hyp -8.38pt, 승률 50%) | 차단 정당 방향 — MW0602(-8.9pt)와 일치. 현행 유지 |
| [8] KellySkip | n=0 + `kelly_advised_skip` 컬럼 **미생성 실측** | 342차 이후 재기동 없음 — 7/20 기동 시 생성 확인 |
| [9] open_gap | n=0 + `open_gap_shadow` 테이블 **미생성 실측** | [8]과 동일 |

**[2]/[3] MW0601 사망 원인 (본 세션 실측)**: `model/horizons/`에 `meta_gate/`·
`quantile/` 디렉토리 자체가 없음 — MW0602(버전 불일치 pkl 로드 실패)와 달리 **학습이
한 번도 실행된 적 없는 상태**. `ensemble_decisions` 16,976행 non-null 0건.
357차가 추가한 NO-DATA 생존점검이 이 PC 리포트에서 [2]/[3]를 ⏳ INSUFFICIENT가 아닌
🔴 NO-DATA로 정확히 구분 — **357차 계측 개선의 첫 실전 검증 성공 사례**.

---

## 안건 ③ ablation 음의 기여 게이트 (MW0601, 7일 창 — 본 세션 최초 생성)

- **hurst_ok**: 단독 차단 n=7, avg **+0.0971pt**, 합계 +0.68pt — 약한 "차단 풀면 이득"
  방향. MW0602 7일 창(n=4, avg -0.53pt "차단이 이득")과 **방향이 엇갈림** — 표본이
  둘 다 한 자릿수라 어느 쪽도 결론 불가. [6] counterfactual FAIL(hyp +42.3pt)과 실측
  mean-revert 손실의 긴장 관계도 MW0602와 동일 → **357차 판정(×0.5 완화 유지, 추가
  완화 0.45→0.40 기각, half-size 실측 20건 대기) 그대로 적용**이 타당.
- **toxicity_gate**: 단독 차단 n=25, avg **-0.4064pt** (합계 -10.16pt) — 차단이 나쁜
  신호를 실제로 걸러냄. 현행 유지.
- **구형 meta_gate**: 단독 차단 사례 0건(7일 창) — 판정 보류 유지.
- 기준선(모든 게이트 통과) n=2 — 기아 상태가 ablation 기준선에도 드러남.

---

## 안건 ④ NEXT_TODO 소화 — MW0601 관점의 핵심 차이

**git reflog 실측**: 333차 후속~338차는 7/15 09:52~12:43에, 339·340차는 7/16
08:21·15:17에, **341~355차는 7/17 00:29에** cherry-pick됨. 즉:

- Hurst ×0.5 완화(7/15~)는 MW0601에 실반영됨 — 퍼널에서 Hurst 차단 7/14 47건 →
  7/15~16 **0건** 확인.
- **341~355차 수정 15건 + 340차 사이징 상향은 7/16 장중에 존재하지 않았음** —
  0719 보고(MW0602)의 "라이브 미검증" 목록이 MW0601에는 더 엄격하게 적용됨.
  7/20(월)이 첫 라이브: 341차 유령 하드스톱, 342차 kelly_advised_skip, 343차 추격필터,
  344차 FQAdj 게이트, 345차 14:50 컷, 346차 EOD 가드(7/20 EOD가 최초), 348차 틱스톱
  즉시화, 349차 급변장 가드, 352차 09:05~10 3m, 353차 ConfStuckBoost, 355차 open_gap,
  340차 사이징(5천만/3%), 356차 Slack 신규 워크스페이스, 그리고 본 세션의 [2]/[3]
  스코어러 적재.
- §4-1 주간 체인: 배선은 완료(333차 후속2, ①안 병합)·krx_holidays 7/17 소급 등록도
  pull됨 — **7/24(금) 15:45가 MW0601 최초 자동 실행**. 이번 주 치는 본 세션이 수동 소급.
- 유령 진입(GHOST_PENDING_MISS): 306차 근본 수정(7/9) 이후 재발 0건 실측 — 추가 조치
  불요, 관찰만.

---

## 안건 ⑤ 다음 주 변경 승인 — MW0601 제안 (이득/손실 + 구현계획)

### M-P0-1. [실행 완료] 357차 개선의 MW0601 소급 — 스코어러 재학습 + 주간 체인 수동 소급

- **이득**: [2]/[3] 채널이 이 PC에서도 부활 — 라이브 운영 PC의 실거래와 연동된 표본이
  쌓이기 시작(캠페인 판정의 본체는 라이브 PC 데이터). MW0601 사상 첫 ablation·캠페인
  리포트 생성으로 주간회의 데이터 기반 완성.
- **손실/리스크**: 없음 — 스코어러는 로깅 전용(실거래 의사결정 미관여), 재학습 산출물은
  로컬 pkl(커밋 제외 규칙 준수).
- **구현**: 본 세션에서 실행 — ①ablation(7일) ②캠페인 리포트(--days 28, 재학습 **전**
  실행으로 §3-1 OOS 순서 준수) ③섀도우 TB 재학습 ④분위회귀 ⑤메타라벨 (실행 결과는
  문서 말미 "체인 실행 결과" 참조). py37_32 크로스 로드 스모크 테스트 포함.
- **잔여 확인(7/20)**: `ensemble_decisions` 5개 컬럼 non-null 적재 시작 여부
  (`SELECT count(meta_entry_quality_prob) FROM ensemble_decisions WHERE ts>='2026-07-20'`).

### M-P0-2. 7/20 = "라이브 검증의 날" + 사이징 상향 동시 라이브에 대한 조건부 롤백 기준

- **배경**: MW0601 W1 손실 구조(대형 사이즈 × 틱스톱 결함 -654만)를 고친 수정(341·348차)과
  사이즈를 다시 키우는 수정(340차: 5천만/3%)이 **7/20에 동시에 첫 라이브**. 수정이
  실패하면 W1 패턴이 상향된 사이즈로 재발 — 이 PC 최대의 단일 리스크.
- **이득(기준 사전 정의의)**: 사고 당일 즉석 판단 대신 사전 등록된 롤백 기준으로 대응
  — §9 원칙과 일치. 349차 급변장 가드·CB④(ATR 3배)가 있으나 둘 다 미검증이므로
  이중 안전선이 필요.
- **손실**: 없음(관찰 + 기준 등록만). 기준 발동 시 사이징 롤백으로 표본 축적이 느려지는
  기회비용은 있으나, W1 실측 손실(-654만/3건) 대비 수용 가능.
- **구현계획**:
  1. 7/20 장중: 0720.txt 체크리스트(341~355차 검증 항목) 순회 — 0719 보고 안건 ④ 표 재사용.
  2. **조건부 롤백 기준(사전 등록 제안)**: 7/20~22 중 하드스톱(틱) 청산에서
     `|pnl_pts|`가 스톱폭+2pt를 초과하는 사례 1건이라도 재발하면 → 348차 수정 실패로
     간주하고 `SIZING_TARGET_CAPITAL_KRW` 5천만→2.5천만(또는 340차 이전 값) 임시 롤백
     + 원인 재수술. 재발 없이 20건 누적 시 340차 확정.
  3. 첫 틱스톱 발생 시 `[TickStop]` 예약~주문 로그 시간차 실측(348차 검증)을 같은 날 수행.

### M-P1. [0] 표본 기아 사다리 2단계(MetaGate take_ceil 0.570→0.52) — 보류 권고, 7/24 재판정

- **배경**: MW0601만 STARVED(주간 9건 < 10; MW0602는 24건). 리포트는 2단계 검토를 권고.
- **보류의 이득**: 7/15~16 최대 차단 사유가 "사이저 산출 수량 0"(7/15 9건→7/16 14건,
  켈리 슬럼프)이고 진입후보 분수도 급감(180→58→37분, conf 저하 — 353차 ConfStuckBoost가
  겨냥) — 즉 기아의 주범 둘 다 **7/20 첫 라이브인 수정(340·353차)이 직접 겨냥**하고
  있어, 게이트 완화 없이 자연 회복될 개연성이 큼. 지금 2단계까지 얹으면 회복 효과의
  귀속이 불가능(357차 P1 보류와 동일 논리).
- **보류의 손실**: 회복 실패 시 기아 1주 연장 — 단 캠페인 시계는 [2]/[3] 부활로 이미
  재시작이라 한 주 관찰의 기회비용은 낮음.
- **구현계획**: 7/24(금) 주간회의에서 7/20~24 주간 진입 수 재집계 — 10건 미만 지속이면
  그때 2단계 적용(설정 위치: `strategy/entry/meta_gate.py _GRADE_CFG['C']['take_ceil']`,
  DECISION_LOG 기록 필수). 회복되면 사다리 1단계 복귀.

### M-P2. Hurst 추가 완화(0.45→0.40) — 기각 유지 (357차 판정의 MW0601 확장 적용)

- MW0601 자체 데이터도 [6] FAIL(hyp +42.3pt) vs 7일 ablation(+0.10pt, n=7 약한 동조)로
  MW0602와 유사한 긴장 관계 — half-size 실측 20건 누적까지 관찰. 신규 조치 없음.

### M-P3. 등급-사이징 수술 — 보류(357차 P1과 동일), 7/24 A등급 exit_reason 재집계

- MW0601 데이터가 MW0602 결론을 독립 재현했으므로 같은 보류 논리 적용. 7/24 재집계
  쿼리는 본 보고 안건 ① 쿼리 재사용.

---

## 안건 ⑥ 이번 주 진입 퍼널 요약 (MW0601)

| 날짜 | 총분 | FLAT | conf미달 | Coherence | 게이트차단 | 후보 | 진입 | 2차차단 |
|---|---|---|---|---|---|---|---|---|
| 7/14 | 370 | 63 | 119 | 18 | 127 (Hurst 47·Regime 31·모드 30) | 43 | 2 | **JointGate 41** |
| 7/15 | 369 | **251** | 58 | 7 | 43 (Regime 12·미분류 12·**사이저0 9**) | 10 | 5 | JointGate 5 |
| 7/16 | 370 | **250** | 77 | 8 | 28 (**사이저0 14**·미분류 6) | 7 | 2 | 미분류 4·JointGate 1 |

- Hurst 완화(7/15~) 실반영 확인: 차단 47 → 0.
- **7/15부터 병목이 게이트에서 상류(FLAT 급증·conf 저하)와 사이저(수량 0)로 이동** —
  기아의 성격이 "차단"이 아니라 "신호 부재 + 켈리 슬럼프"임. M-P1 보류 근거.
- JointGate 2차 차단은 7/14 41건 이후 급감 — [7] 섀도우는 차단 정당 방향(hyp -8.38pt).
- 미분류는 7/16 총 10건(게이트 6 + 2차 4) — 357차 P4(저우선, 실측 기반 보강) 유지.

---

## 결정 요청 사항 (회의에서 승인/기각)

| # | 항목 | 권고 |
|---|---|---|
| 1 | M-P0-1 [2]/[3] MW0601 재학습·체인 소급 | **완료 보고** — 7/20 적재 확인만 잔여 |
| 2 | M-P0-2 조건부 롤백 기준 사전 등록 (틱스톱 슬리피지 >스톱폭+2pt 재발 시 340차 사이징 임시 롤백) | **승인 권고** |
| 3 | M-P1 기아 사다리 2단계 — 보류, 7/24 재판정 | **보류안 채택 권고** |
| 4 | M-P2 Hurst 추가 완화 기각 유지 | **기각 유지 권고** |
| 5 | M-P3 등급-사이징 수술 보류 (357차 P1 동일) | **보류안 채택 권고** |

> 승인/기각 결과는 `dev_memory/DECISION_LOG.md`에 기록(§4-3).

---

## 체인 실행 결과 (본 세션, 2026-07-19 12:19~12:43, MW0601)

`py310_64` 전체경로로 §4-1 순서(ablation → 캠페인 리포트 → 섀도우TB → 분위회귀 →
메타라벨) 그대로 수동 소급 실행, 전 스텝 rc=0.

| 스텝 | 결과 |
|---|---|
| 게이트 ablation(7일) | hurst_ok n=7 avg+0.10pt(약한 완화지지) / toxicity_gate n=25 avg-0.41pt(차단 정당) |
| 캠페인 판정 리포트 | [2]/[3] 🔴 NO-DATA 정확 표기 확인(357차 계측 개선 검증) — 재학습 **전** 실행이라 §3-1 OOS 순서 준수 |
| 섀도우 TB 재학습 | 6/6 호라이즌 전부 champion 대비 우위(1m 0.556 vs 0.371 … 30m 0.849 vs 0.273) |
| 분위회귀 재학습 | 6/6 완료, pinball 0.37~2.28, q10/q90 커버리지 대체로 0.12~0.20(이상 0.10 대비 다소 편향 — 후속 관찰) |
| 메타라벨 재학습 | 6/6 완료, AUC 0.50~0.57(약한 판별력 — MW0602 0.48~0.56과 유사 수준) |

**사고 및 처리**: 체인이 자체 서브프로세스로 분위회귀 재학습을 실행하는 도중,
동일 스크립트를 본 세션에서 별도로 한 번 더 수동 실행해 같은 출력 파일에 쓰기
경합이 발생함을 발견 — 즉시 중복 프로세스를 종료(TaskStop)하고 원본 체인만 완주시킴.
**사후 검증**: py37_32 런타임에서 두 스코어러 직접 로드+스코어링 — 6개 호라이즌
전부 정상 float/dict 반환(ALL_OK, 손상 없음 확인), 양쪽 환경 `py_compile` 통과.

---

## 부록: 지난 1주(7/12~7/19) 고도화 개선 · 실전 운영 종합 딥다이브

> 작성: Claude (사용자 요청 — 본 보고서 + `0719_주간회의_검토보고.md`(MW0602) +
> `dev_memory/DECISION_LOG.md` 357·358차를 종합해 개선 효과 딥다이브). 캠페인 데이터
> 구간 7/5~7/16(5거래일), 코드 변경 333~358차(커밋 25건+).

### A-1. 핵심 결론 (TL;DR)

| # | 발견 | 심각도 |
|---|---|---|
| 1 | 캠페인 채널 [2] Meta-Gate·[3] 분위회귀가 캠페인 전체 기간 "무음 사망" — 관련 컬럼 5개가 두 PC 합쳐 16,238~16,976행 중 **0건** 적재 | 🔴 중대 |
| 2 | 순EV 악화 + 등급 역전 — A등급(고신뢰)이 오히려 손실, C등급이 오히려 수익, 두 PC 독립 재현 | 🔴 중대 |
| 3 | 341~355차 수정 15건+이 아직 한 번도 라이브 검증 안 됨 — MW0601은 7/17 00:29에야 코드 반영, 7/20이 첫 검증일 | 🟡 대기중 |
| 4 | (당일 추가 발견, 358차) 프로덕션 스케일러 6개가 346차 자체 버그로 손상되어 월요일 개장 시 전 호라이즌 진입불가 상황이었을 뻔함 — 당일 발견·복구(MW0602 확인, MW0601은 손상 없음으로 후속 확인) | 🔴 아슬아슬 |

### A-2. 순손익 추이 — 등급 역전이 가장 눈에 띄는 신호

**MW0602 (일반 개발/검증 PC)**

| 구간 | 건수 | 순PnL |
|---|---|---|
| W1 (7/9~10) | 12 | +102,729원 |
| W2 (7/14~16) | 24 | **-205,010원** |
| 누적 | 36 | -102,281원 |

**MW0601 (실제 Cybos 라이브 운영 PC, 유령거래 2건 제외 — 본 보고서 안건①과 동일 수치)**

| 구간 | 건수 | 순PnL |
|---|---|---|
| W1 | 13 | **-4,332,002원** |
| W2 | 9 | +688,700원 |
| 누적 | 22 | **-3,643,302원** |

두 PC 모두 **A등급(고신뢰) 손실 / C등급(저신뢰) 수익**이라는 같은 역전 패턴을 독립적으로
재현(MW0602: A -198만원 vs C +114만원, MW0601: A -469만원 vs C +105만원). 원인 추적 결과
"등급 판정 자체가 틀렸다"기보다 **하드스톱(틱) 청산 인프라 결함**에 손실이 집중된 것으로
확인 — MW0601 대형 손실 3건(-654만원)이 전부 -6.35pt·-4.04pt·-2.01pt로 정상 스톱폭을 크게
초과한 슬리피지였고, 341차(유령 하드스톱 소급버그)·348차(틱스톱 60초 지연)가 정확히 겨냥해
수정한 패턴. 그래서 두 PC 다 "등급-사이징 재설계는 성급, 수정 효과 먼저 실측"으로 보류.

### A-3. 이번 주 가장 값진 발견 — "무음 사망 계측" 4번째 반복

메타라벨/분위회귀 스코어러가 로그상으로는 정상 작동하는 것처럼 보였지만 실제로는:
- MW0602: 모델이 sklearn≥1.1에서 학습돼 런타임(1.0.2)에서 로드 실패
- MW0601: 아예 학습이 한 번도 실행된 적 없음(모델 디렉토리 자체 부재)

두 원인 모두 실패 로그가 WARNING 1회/DEBUG 수준이라 사실상 방치됐고, 주간 재학습 체인에도
아예 편입돼 있지 않아 "정상 가동돼도 영구 사망" 구조였음. 이번 세션에서 재학습·체인 편입·
NO-DATA 표기 자동화(⏳ INSUFFICIENT와 🔴 NO-DATA 구분)까지 구현했고, MW0601에서 그 표기가
실제로 정확히 작동함을 확인(계측 개선 자체의 첫 실전 검증 성공 사례). v1.2 유령버전·
FP-PSI·신호소멸청산에 이은 4번째 "적재 0인데 아무도 몰랐던" 사례.

### A-4. 아직 검증 안 된 개선사항들 (7/20이 첫 시험대)

341~355차(틱스톱 즉시화, 추격필터, FQAdj 게이트, 14:50 컷, EOD 모델가드, ConfStuckBoost,
사이징 5천만/3% 상향 등 15건+)가 MW0601 기준 **7/17 00:29에야 반영**되어 7/16까지 실거래는
전부 구코드로 돌아감. 즉 지금까지의 손익 개선/악화는 이 수정들의 효과가 전혀 반영 안 된
수치이고, 7/20이 처음으로 검증되는 날. 손실을 줄이는 수정(341·348차)과 사이즈를 다시 키우는
수정(340차)이 동시에 첫 라이브라, M-P0-2 조건부 롤백 기준(하드스톱 슬리피지가 스톱폭+2pt
초과 재발 시 사이징 5천만→2.5천만 즉시 롤백)이 사전 등록됨.

### A-5. 오늘 발견된 부수적 사고 — 스케일러 손상 (358차)

346차("EOD 모델 교체 가드") 통합테스트가 격리를 제대로 못해 프로덕션 스케일러 6개(전
호라이즌)를 무작위 라벨 10피처 스케일러로 덮어씀. 7/17-19가 공휴일+주말로 거래일이 없어
당일까지 발견되지 않았고, 월요일 08:55 PreRetrain 스킵 로직이 "최근 EOD 성공"으로 오인해
자동 복구도 안 됐을 상황 — 발견 즉시 프로덕션 경로 그대로 재적합해 복구, 원인 코드(스케일러
저장 경로 격리 누락 + WarmupRetrain 자기스킵 버그) 2건 모두 수정(dev 브랜치 `14b7ca9`).
MW0601 쪽은 이 부록 작성 시점엔 손상 여부 미확인이었으나, 이후 세션(`0a91540`)에서
**손상 없음(97피처 일관)**이 확인됨.

### A-6. 종합 판단

이번 주는 "손익이 개선됐다"고 말하기엔 아직 이름 — 눈에 보이는 순EV 숫자는 오히려
나빠졌지만, 그 손실의 실체(하드스톱 슬리피지 집중)를 정확히 짚어낸 수정들이 방금 막
배포됐을 뿐 아직 하루도 실전에서 안 돌았기 때문. 오히려 이번 주의 실질적 성과는 **계측
사각지대 두 곳(메타게이트/분위회귀)을 되살리고, 프로덕션을 망가뜨릴 뻔한 스케일러 손상을
개장 전 잡아낸 것** — 즉 "판단 근거 데이터의 신뢰성"을 회복시킨 주. 진짜 개선 효과 판정은
7/20(첫 라이브)~7/24(주간회의 재검토)에 나옴.
