# [2] Meta-Gate 채널 · `entry_quality_prob` 스코어러 딥다이브 + 후속 숙제

> 작성: 2026-08-24 (MW0602 489차 후속)
> 계기: 0823 주간회의 **D1 — [2] Meta-Gate 최종 탈락 확정** 이후 사용자 질의로 진행한 코드·DB 추적.
> 근거: `dev_memory/DECISION_LOG.md` 489차 D1 · `docs/정기점검/금요일점검/MW0602/0823_주간회의_검토보고_MW0602.md`
>
> 🔴 **이 문서의 §2(숙제)에는 어제 결정에 없던 신규 실측 3건이 있다.** 그중 H1은 [2] 탈락의
> **상류 원인**으로 보이고, H2·H3은 스코어러 학습 파이프라인의 구조적 결함 후보다.
> 어느 것도 **매매 경로에 닿지 않는다**(스코어러는 섀도). 조치는 전부 주간회의 소관이다.

---

# 1. 설명 — [2] Meta-Gate와 스코어러는 무엇인가

## 1-1. 채널 [2]가 묻는 것

`entry_quality_prob` — 메타라벨링 스코어러가 매 신호에 매기는 **"이 진입이 좋은 진입일 확률"** —
이 실제로 좋은 진입과 나쁜 진입을 가르는가.

그 확률로 신호를 3분위로 나누고 각 분위의 **실현 순EV**(방향조건부 미래이동 − 왕복비용)를 잰다.

**합격선** (`config/settings.py:VALIDATION_CAMPAIGN["meta_gate"]`, 사전등록):

```python
"top_ev_min_pt":     0.0,   # 상위 33% 순EV > 0
"sep_cost_mult":     2.0,   # (상위 − 하위) > 왕복비용 × 2  = +0.1409pt
"min_per_tercile":   30,    # 분위별 최소 표본
```

**왜 중요했나** — 2026-07-04 감사 §5가 **"활성화 순서 2순위: Meta-gate 사이징"** 으로 올린
승격 후보였다. 이 확률이 좋은 진입을 가른다면 그걸로 계약수를 조절하자는 것이었다.

## 1-2. 스코어러의 정체

**한 줄 정의**

> **"이 신호로 진입하면 방향이 맞고 실이익이 나는가"를 직접 학습한 GBM 이진 분류기.**
> 그 확률이 `entry_quality_prob`이다.

앙상블이 *"가격이 어디로 갈까"* 를 예측한다면, 스코어러는 그 위에 한 겹 얹혀
*"그 예측을 믿고 진입해도 되는가"* 를 묻는다. 금융ML의 **메타라벨링(meta-labeling)** 구조다 —
1차 모델은 방향, 2차 모델은 실행 여부.

**구성** (`learning/meta_label_classifier.py`)

| 항목 | 값 |
|---|---|
| 학습 데이터 | `meta_labels` 테이블 (STEP 1 예측검증이 매분 축적, 6개 호라이즌 합산 **101,354행**) |
| 레이블 | `skip` = 0 / `take`·`reduce` = 1 |
| 알고리즘 | `HistGradientBoostingClassifier` (max_iter 200 · depth 4 · lr 0.05 · leaf 30) |
| 전처리 | `StandardScaler` |
| 불균형 처리 | `compute_sample_weight("balanced")` — pos_rate 0.18~0.27 |
| 검증 | `TimeSeriesSplit` 3폴드 → AUC |
| 최소 표본 | 호라이즌당 **2,000행** 미달 시 학습 스킵 |
| 산출물 | `model/horizons/meta_gate/{hz}_entry_quality{,_scaler,_features}.pkl` (gitignore, PC별) |
| 재학습 | **매주 금요일 EOD 캠페인 체인** (`scripts/campaign_steps.py` → `train_meta_label_classifier.py`) |

> 재학습이 스케줄에 붙은 것은 **357차**다. 그전에는 어떤 스케줄에도 연결돼 있지 않아
> [2] 채널이 갱신 불가였다 — 그 자체가 한 번 고쳐진 결함이다.

## 1-3. 레이블 정의와 임계의 근거

`learning/meta_labeling.py:derive_meta_label()`:

```
realized_move = (future_close − target_close) × predicted      # 방향 조건부 실현이동

predicted == FLAT                          → skip   (0.0)
방향 틀림  or  realized_move ≤ 0            → skip   (0.0)
realized_move ≥ max(threshold_move×2, 0.05)
   or  confidence ≥ 0.70                   → take   (1.0)
그 외 (맞았으나 follow-through 약함)        → reduce (0.5)
```

### 임계(`threshold_ratio`)는 어디서 오나

호출부는 [`learning/prediction_buffer.py:422`](../../learning/prediction_buffer.py) 한 곳뿐이다:

```python
if _sigma_saved > 0 and SIGMA_K > 0:
    threshold = _sigma_saved / 100.0 * SIGMA_K * math.sqrt(h_min)
else:
    threshold = HORIZON_THRESHOLDS.get(horizon, 0.0003)

actual = build_single_target(target_close, current_price, threshold)   # ← 같은 값
meta   = derive_meta_label(..., threshold_ratio=float(threshold))      # ← 같은 값
```

🔴 **핵심 — 메타라벨 전용 상수가 아니다.** 바로 위에서 `actual`(UP/DOWN/FLAT)을 만드는 데 쓴
**그 밴드를 그대로 재사용**한다.

| 항목 | 내용 |
|---|---|
| 정의 | `σ_at_t / 100 × SIGMA_K × √(호라이즌 분)` — 예측 시점 rolling σ(`predictions.sigma_at_t`) |
| `SIGMA_K` | **0.41** — 근거는 *"FLAT 34% 달성 계수"*. `scripts/optimize_sigma_k.py`가 FLAT 비중이 3클래스 균등(≈33%)에 오도록 역산 |
| `√h_min` | 확산 스케일링 (랜덤워크에서 σ ∝ √시간) |
| 폴백 | σ 미저장 시 정적 `HORIZON_THRESHOLDS` (1m 0.075% … 30m 0.476%) |

⇒ **`realized_move ≥ threshold_move × 2.0`의 뜻**:

> *"UP/DOWN으로 분류되기 위한 최소 이동폭의 **2배** 이상 실제로 갔는가."*

배수 2.0은 임의의 튜닝값이 아니라 **"간신히 방향이 맞은 것"과 "제대로 따라간 것"을 가르는 배수**다.
하한 `0.05`(pt)는 σ가 극단적으로 낮은 날 임계가 0에 수렴해 아무 움직임이나 take가 되는 것을 막는
절대 바닥이다.

> ℹ **학습과 채점이 일부러 다른 임계를 쓴다.** `USE_FIXED_LABEL_THRESHOLD = True`라
> `batch_retrainer`의 **학습 레이블은 고정 `HORIZON_THRESHOLDS`**를 쓰고, 위 채점 경로만
> rolling σ다. 코드 주석이 의도를 밝힌다 — *"실전 rolling sigma와 학습 임계값을 분리 →
> 레이블 드리프트 제거"*(개선 4). **결함이 아니라 설계다.**

### `confidence`는 앙상블 confidence가 **아니다**

호출부가 `predictions` 테이블에서 읽는다:

```python
row = pred_conn.execute("SELECT id, direction, confidence, ... FROM predictions "
                        "WHERE ts=? AND horizon=? AND actual IS NULL")
...
confidence = float(row["confidence"] or 0.0)
```

`predictions`는 **STEP 5 멀티 호라이즌 예측**이 호라이즌마다 한 행씩 쌓는 테이블이다.
즉 이 `confidence`는 **그 호라이즌 모델 자신의 confidence**이고,
앙상블 투표 결과인 `ensemble_decisions.confidence`와는 **다른 테이블·다른 스케일**이다.

실측 분포 (`meta_labels`, 방향성 예측만):

| hz | n | min | max | avg | ≥0.70 |
|---|---|---|---|---|---|
| 1m | 18,535 | 0.3333 | 1.0000 | 0.4465 | 1,284 |
| 3m | 11,372 | 0.3333 | 1.0000 | 0.4697 | 1,087 |
| 5m | 8,938 | 0.3333 | 1.0000 | 0.5093 | 1,161 |
| 10m | 7,341 | 0.3333 | 1.0000 | 0.5227 | 1,055 |
| 15m | 6,363 | 0.3333 | 1.0000 | 0.5561 | 1,104 |
| 30m | 12,743 | 0.3333 | 1.0000 | 0.5000 | 1,134 |

최솟값 0.3333은 3클래스 argmax의 구조적 바닥(1/3)이다.

🔴 **두 conf를 섞어 읽지 말 것.** CLAUDE.md 476차가 *"방향성 confidence는 2026-06-01 이후
0.60에 한 번도 닿지 않았다"* 고 못 박은 것은 **앙상블** 쪽이며, 이 임계와 무관하다.
이쪽은 0.70을 정기적으로 넘는다.

## 1-4. 피처의 "그 시점" — 예측 시점 T, 검증 시점 아님

```
T      STEP 4  피처 생성            → features (현행 141키)
       STEP 5  멀티 호라이즌 예측    → 그 features를 모델에 먹인다
       STEP 9  predictions INSERT   → ts=T, features=_feat_clean, sigma_at_t=σ_20
          ↓  (h분 경과)
T+h    STEP 1  verify_and_update(current_ts = T+h)
               target_ts = (T+h) − h = T                  ← 여기
               SELECT ... FROM predictions WHERE ts = target_ts
               meta_labels INSERT (ts=T, features=compact(그 행의 features))
```

검증 루프는 **현재 시각에서 h분을 되감아** 그때 남긴 `predictions` 행을 꺼낸다
(`horizon_targets = {h: current_dt − timedelta(minutes=m)}`).
`feat_dict`는 그 행의 `features` 컬럼 — **h분 전에 이미 저장돼 있던 값**이고 재계산하지 않는다.
`meta_labels.ts`도 `target_ts`(=T)이므로 테이블의 타임스탬프는 **예측 시점**을 가리킨다.

✅ **룩어헤드가 구조적으로 차단된다.** 피처는 T의 스냅샷, 레이블만 T+h 종가로 계산.
스코어러가 학습하는 관계는 정확히 *"T에 보이는 것으로 T→T+h 결과를 맞힐 수 있는가"* 이고,
이는 `MetaGate.evaluate()`가 매분 넘기는 것과 **같은 정보 집합**이다. **학습/서빙 정보 시점 일치.**

**어떤 피처인가** — `main.py:9767`이 넘기는 `features_clean`:

```python
_feat_clean = {k: round(float(v), 4) for k, v in features.items()
               if v is not None and v == v}      # v==v → NaN 제외
```

- STEP 4가 그 분에 만든 **피처 딕셔너리 전량** (호라이즌별로 고르지 않는다)
- **6개 호라이즌 행이 같은 features JSON을 공유**한다
- `compact_feature_json()`이 한 번 더 훑어 float 변환 실패 키를 버리고 6자리 반올림

⇒ 앙상블 모델의 배포 피처셋(`feature_names_hz`, 호라이즌당 10~17개)과는 **다른 집합**이다.
그쪽은 선별된 부분집합, 이쪽은 그날 만들어진 전량.

> 🔴 **463차 교락이 아팠던 이유가 여기 있다.** 6개 호라이즌의 입력이 동일하니
> 3m 진입에 1m 모델을 써도 **아무 오류 없이 그럴듯한 확률이 나온다.** 조용히 틀렸다.

## 1-5. 역사 4단계와 어제의 결정

**① 2026-08-01 — 첫 판정 FAIL, "탈락 확정"**
상위 −0.0499 vs 하위 +0.031 → 분리도 **−0.0809**(필요 +0.1457). **부호까지 반대.**
분위당 n=402라 표본 핑계도 안 됐다. 감사 §5 2순위 공식 삭제.

**② 2026-08-12 (463차) — 🔴 근거가 교락돼 있었다**
`self._entry_horizon_pre`가 179차 도입 이래 **한 번도 할당된 적이 없어**(read 2 / write 0)
MetaGate가 항상 폴백 `'1m'`으로 채점. 실측 `meta_gate_horizon` 2,587행 전부 `'1m'`인데
실제 진입은 3m 80 / 5m 56 / 1m 17건 → **88.9% 불일치**.
그러나 **결정은 유지** — 분리도가 부호까지 반대라 스코어러 교체가 그 폭을 메운다는 보장이 없고,
지금 재개하면 "관측 후 기준 수립"(313차 위반)이 된다.
대신 **재개 조건을 딱 하나** 남겼다:

> *"463차 배선(2026-08-12) **이후** 데이터로만 분위당 n≥300을 채운 뒤 재측정.
>  재측정에서도 분리도가 합격선 미달이면 그때 **최종** 탈락으로 확정한다."*

**③ 2026-08-21 — 조건 충족** (경계 이후 1,233건 → 분위당 411 ≥ 300)

**④ 2026-08-23 (489차 D1) — 재측정 후 최종 탈락 확정**

| 표본 | 분위당 | 상위EV | 중위 | 하위 | **분리도** |
|---|---|---|---|---|---|
| 전체(경계 혼용) | 1,052 | +0.0194 | −0.1525 | +0.0259 | **−0.0065** |
| **경계 이후만(≥08-12)** | **411** | **+0.0222** | −0.0792 | **+0.0358** | **−0.0136** |
| 경계 이전만 | 641 | +0.0215 | −0.1605 | −0.0233 | +0.0448 |

- 상위EV **+0.0222 > 0** → 첫 관문 통과 (0801엔 −0.0499로 실패). 스코어러 교체가 무의미하진 않았다.
- 그러나 분리도 **−0.0136** → **여전히 음수**. 필요값 +0.1409의 반대편.
- 스코어러가 "좋다"고 한 상위 분위가 "나쁘다"고 한 하위 분위**보다 못 번다.**

⇒ **교락을 제거해도 부호가 그대로.** 463차의 유보가 해소됐다.
`entry_quality_prob` 재설계 이관도 함께 종결. 재개하려면 **새 사전등록** 필요.

## 1-6. ⚠ 오독 방지 — 무엇이 탈락한 것인가

**① "채널 [2] 탈락"은 "MetaGate 폐기"가 아니다.**
MetaGate는 라이브에서 그대로 돈다 — `MetaConfidenceLearner` 기반 `blended_conf` →
take/reduce/skip, 등급별 임계, 431차 무정보 폴백 중립화. 전부 무변경.
탈락한 것은 **`entry_quality_prob`을 사이징 승격 근거로 쓰는 것** 하나다.

**② 스코어러를 끄자는 뜻도 아니다.**
`strategy/entry/meta_gate.py:105`가 못 박고 있다:

```python
# [260704 감사 P1] meta_labels 기반 진입품질(수익확률) 섀도우 스코어 — 로깅 전용,
# action/size_multiplier 결정에는 관여하지 않는다.
entry_quality_prob = self._eq_scorer.score(horizon, features)
```

**처음부터 로깅 전용**이라 이번 탈락의 **라이브 영향은 0**이고, 스코어링·기록·주간 재학습은 계속된다.
488차의 `opt_chain_pcr` 두 축 분리와 같은 구도다 — *"승격 자격 없음"* 과 *"수집을 끄자"* 는
다른 이야기이고, 끊으면 재설계 때 쓸 표본이 함께 끊긴다.

---

# 2. 숙제 — 후속 조사 항목

> **분류**: 🔴 = 실측으로 확인된 결함 후보 / 🟡 = 확인 필요 / ✅ = 확인 결과 무해
> **공통**: 스코어러는 섀도라 **어느 것도 매매 경로에 닿지 않는다.** 조치는 주간회의 소관.

## H1 🔴 스코어러 AUC가 0.49~0.55다 — [2] 탈락의 상류 원인

**실측** (`logs/retrain_eod_20260821.log`, 2026-08-21 재학습, TimeSeriesSplit 3폴드):

| hz | AUC | n | pos_rate |
|---|---|---|---|
| 1m | 0.5458 | 23,984 | 0.270 |
| 3m | 0.5092 | 16,770 | 0.250 |
| **5m** | **0.4971** | 14,055 | 0.230 |
| 10m | 0.5255 | 12,801 | 0.208 |
| 15m | 0.5477 | 11,818 | 0.184 |
| **30m** | **0.4890** | 21,926 | 0.204 |

🔴 **사실상 동전 던지기다.** 최고가 15m 0.5477이고, **5m·30m은 0.5 미만(역방향)**.

**왜 중요한가** — 이 AUC는 스코어러가 **자기 자신의 레이블**(take/reduce vs skip)을 맞히는
능력이다. 그것조차 무정보면 그 확률로 **손익을 분리하기를 기대할 근거가 없다.**
즉 [2] 채널의 FAIL은 분위 판정 단계의 문제가 아니라 **상류에서 이미 결정돼 있었다.**

⚠ **AUC와 [2] 분리도는 다른 축이다** — 전자는 라벨 예측, 후자는 손익 분리. AUC가 높다고
[2]가 통과한다는 보장은 없다. 다만 **AUC ≈ 0.5면 [2]가 통과할 리 없다**는 방향은 성립한다.

🔴 **그리고 이 수치는 매주 금요일 EOD 로그에 찍혀 왔는데 아무도 읽지 않았다.**
캠페인 리포트에도, 수집기 §12에도 없다. 292·303·371·468차가 반복한
*"안전장치가 한쪽 값에 붙박여 죽어 있는데 매번 사람이 뒤늦게 발견"* 과 같은 계열이다.

**할 일**
1. `[MetaLabelClf] {hz} AUC=` 를 **캠페인 리포트 [2] 절에 병기**하거나 수집기 §12에 등록.
   ⚠ 무조건 상태 샘플이므로 §5 규약(조건부 로그 금지)에 저촉되지 않는다.
2. 재설계 사전등록 시 **AUC를 1차 관문**으로 둘 것 — AUC가 기준 미달이면 분위 판정까지 갈 필요가 없다.

## H2 🔴 `feat_names` 스키마가 고착돼 폐기 피처 3종이 서빙 시 항상 0.0

**메커니즘** (`meta_label_classifier.py:74-89`):

```python
keys = list(fd.keys())
if feat_names is None or len(keys) >= feat_count:
    feat_count = len(keys); feat_names = keys      # 키가 가장 많은 행을 스키마로 채택
...
X = [[rec[0].get(f, 0.0) for f in feat_names] for rec in records]   # 없으면 0.0
```

**실측** (배포된 `1m_entry_quality_features.pkl`, 2026-08-21 16:02 학습본):

| 항목 | 값 |
|---|---|
| 모델 `feat_names` | **144개** |
| 최신 서빙 피처(2026-08-21) | **141개** |
| 모델에 있으나 서빙에 없음 → **항상 0.0** | `program_foreign_net_krw` · `program_individual_net_krw` · `program_institution_net_krw` |
| 서빙에 있으나 모델이 안 봄 | 없음 |

그 3종은 **451차(MW0601)가 의도적으로 폐기**한 오라벨 피처다
(`scripts/feature_health_report.py`: *"전체 프로그램 순매수 오라벨 — 451차 폐기 집행. 과거 행 잔재"*).
실측 소멸일은 **2026-08-10**(144키 → 141키, 깔끔한 단절).

🔴 **매주 재학습해도 자동으로 낫지 않는다.** 학습은 `meta_labels` **전체 이력**을 읽고
`feat_names`를 "최다 키 행"에서 뽑는데, 144키 행(2026-07-28~08-07)이 테이블에 남아 있는 한
**폐기된 피처가 영구히 스키마에 박힌다.**

**할 일**
- `feat_names` 채택 규칙을 *"최다 키 행"* 에서 **"최근 N일 행의 키 교집합"** 또는
  **명시적 피처 레지스트리 참조**로 바꾼다.
- ⚠ 영향도는 **미측정**이다. `HistGradientBoostingClassifier`는 `feature_importances_`가 없어
  기여도를 바로 못 본다 — 순열 중요도로 재야 한다. **"3개뿐이니 무해"라고 단정하지 말 것.**

## H3 🔴 학습 행의 86.6%가 0 패딩 — 시점 대리변수 위험

**실측** (1m, `meta_labels` 23,984행):

| 항목 | 값 |
|---|---|
| 채택 스키마 | 144키 (최초 등장 2026-07-28) |
| 스키마보다 키가 적은 행 | **20,762행 (86.6%)** → 나머지 열 0.0 패딩 |
| 거래일 기준 | 70일 중 **61일(87.1%)** |

**피처 키 개수 추이** (1m, 날짜별 첫 행):

```
2026-05-08  35키
2026-05-11  44키  (+9)      2026-06-02  116키 (+17)     2026-07-06  130키 (+9)
2026-05-13  52키  (+8)      2026-06-04  118키 (+2)      2026-07-14  136키 (+6)
2026-05-15  90키  (+38)     2026-06-10  114키 (−4)      2026-07-15  139키 (+3)
2026-05-18  91키  (+1)      2026-06-16  121키 (+7)      2026-07-20  140키 (+1)
2026-05-19  98키  (+7)      2026-06-18  120키 (−1)      2026-07-24  142키 (+2)
2026-05-21  91키  (−7)      2026-06-25  121키 (+1)      2026-07-28  144키 (+2)
2026-05-22  99키  (+8)                                  2026-08-10  141키 (−3)
```

첫날 35키 → 현재 141키. **111개 키가 나중에 추가**됐다
(`toxicity_*` · `opt_*` · `macro_*` · `hurst` · `spread_ticks` · `vpin` · `kyle_lambda` …).

🔴 **위험** — 어떤 피처가 특정 날짜부터 존재한다면, 그 열의 `0 / 비0`은 **시점 대리변수**가 된다.
모델이 *"`opt_chain_pcr == 0` → 2026-06-02 이전 → 그 시기의 기저 take율"* 을 학습할 수 있다.
371차가 PSI에서 겪은 것(**계측 결함이 만든 가짜 신호**)과 같은 계열이다.

그리고 `TimeSeriesSplit` 3폴드는 **초기 폴드가 희소 스키마 구간**이라
H1의 AUC 추정 자체도 이 영향을 받는다.

**할 일**
1. 학습 창을 **최근 N일로 자른다**(전체 이력 대신). 스키마가 안정된 구간만 쓰면
   패딩 비중이 급감한다 — 예: 2026-07-28 이후만 쓰면 144키 구간이 온전하다.
   ⚠ 다만 표본이 `MIN_ROWS_PER_HORIZON = 2000` 아래로 떨어지지 않는지 먼저 확인할 것.
2. 또는 **결측을 0이 아니라 `np.nan`으로 넣는다** — `HistGradientBoostingClassifier`는
   NaN을 네이티브로 처리하며 "없음"과 "0"을 구분한다. **한 줄 변경이고 가장 저렴하다.**
3. 어느 쪽이든 **H1의 AUC가 개선되는지**로 검증한다. 개선이 없으면 H3은 원인이 아니었다는 뜻이고,
   그때는 레이블 정의(H5·H6) 쪽이 남는다.

## H4 🟡 채점 경로가 `SIGMA_K` 스칼라를 전 호라이즌에 쓴다

`learning/prediction_buffer.py:17,422`가 **flat `SIGMA_K = 0.41`** 을 6개 호라이즌 전부에 적용한다.
그런데 settings에는 호라이즌별 최적값이 따로 있다:

```python
SIGMA_K_PER_HORIZON = {"1m": 0.41, "3m": 0.41, "5m": 0.41,
                       "10m": 0.41, "15m": 0.38, "30m": 0.33}
```

`batch_retrainer`·`model_meta`는 `SIGMA_K_PER_HORIZON`을 참조하는데 채점 경로만 스칼라다.
⇒ **15m·30m의 채점 밴드가 최적값보다 넓다**(k가 클수록 FLAT 구간이 넓어져 UP/DOWN 판정이 보수적).

🔴 **30m은 CB③의 유일한 입력원**이다(CLAUDE.md 절대원칙 §3 각주 — `main.py:5539`가
`if v["horizon"] == "30m"`으로 걸러 `circuit_breaker.record_accuracy()`에 먹인다).
따라서 "섀도 채널 이야기"로 끝나지 않을 수 있다.

**할 일**
- 의도적 설계인지 드리프트인지 **git 이력으로 먼저 확인**할 것
  (`SIGMA_K_PER_HORIZON` 도입 커밋이 채점 경로를 함께 바꿨는가).
- 드리프트라면 30m 정확도가 얼마나 달라지는지 **소급 재계산**으로 재고, CB③ 임계와 함께 읽는다.
- ⚠ **코드를 먼저 고치지 말 것.** CB③ 임계 0.28은 이 밴드 위에서 관측된 값이라,
  밴드를 바꾸면 임계도 함께 재설계 대상이 된다(전환기준 ⑥의 몫).

## H5 🟡 `confidence ≥ 0.70` OR 분기의 실효는 2.9%

`take` 16,316건을 두 분기로 분해:

| hz | take | realized_move로 | conf≥0.70 (교집합 포함) | **conf 때문에만 take** |
|---|---|---|---|---|
| 1m | 4,380 | 4,291 | 560 | **89** |
| 3m | 2,998 | 2,931 | 463 | **67** |
| 5m | 2,327 | 2,255 | 503 | **72** |
| 10m | 1,940 | 1,858 | 437 | **82** |
| 15m | 1,623 | 1,538 | 413 | **85** |
| 30m | 3,048 | 2,962 | 476 | **86** |
| **합** | **16,316** | **15,835** | 2,876 | **481 (2.9%)** |

⇒ **OR 분기가 실제로 라벨을 뒤집는 경우는 2.9%뿐이다.** 97.1%는 이동폭 조건만으로 이미 take.

⚠ **순환 주의**: `conf≥0.70`인 방향성 예측 6,825건 중 take가 2,876건(42%)인데,
그중 481건은 **이 규칙이 스스로 만든 것**이다. 자기가 take로 만들어 놓고
"conf가 높으면 take가 많다"고 읽으면 안 된다.
순환을 제거하면 35.1%(2,395/6,825) vs 기저 25.0% — 라벨 공간에서 약간의 lift가 보이나
**손익 축은 [39]가 일자단위로 이미 기각**했다(모델축 +0.16%p, n=25,240, 일자 p=0.1628).
**신호단위 lift를 근거로 삼지 말 것**(313차).

**할 일**
- 재설계 시 이 OR 분기를 **유지할지 결정**한다. 2.9%를 위해 레이블에 두 번째 축(confidence)을
  섞는 것이 값을 하는지, 아니면 **이동폭 단일 기준**으로 단순화할지.
- ⚠ 제거하면 레이블 정의가 바뀌므로 **§9-4 검증 시계 리셋 대상**이다. 과거 판정과 비교 불가.

## H6 — 재설계 시 사전등록 요건

[2]의 재개 조건은 **소진**됐다(489차 D1). 다시 후보로 올리려면 **새 사전등록**이 필요하며,
0816 D2([8] KellySkip)와 같은 처리다.

**이미 반증된 방향 — 재시도 금지**

> 🔴 **"호라이즌을 맞게 채점하면 나아진다"는 가설은 이미 반증됐다.**
> 463차 교락(1m 고정 채점)을 고친 뒤에도 경계 이후 분리도가 **−0.0136으로 음수**였다.
> 스코어링 호라이즌 수정만으로 이 채널을 되살릴 수 없다.

**남은 축 3개** (우선순위 순):

| 축 | 근거 | 선행 확인 |
|---|---|---|
| **① 학습 파이프라인** | H1(AUC≈0.5) · H2(스키마 고착) · H3(86.6% 패딩) | **가장 싸고 가장 유망하다.** H3의 NaN 처리는 한 줄이다. 고친 뒤 AUC가 움직이는지부터 본다 |
| **② 레이블 정의** | `skip` 판정이 `realized_move > 0` 하나에 걸려 있다. H5의 OR 분기 | 바꾸면 검증 시계 리셋 |
| **③ 피처 축** | 141키 전량 투입 — 선별이 없다. 앙상블은 호라이즌당 10~17개만 쓴다 | 26주 WFA 「호라이즌 방향예측 피처셋 재검증」과 묶어서 |

⚠ **사전등록 순서를 지킬 것** — ①을 고친 뒤 AUC를 보고, **그 결과를 보기 전에**
새 합격선을 settings에 박는다. 순서가 뒤집히면 313차 원칙 ④ 위반이다.

## ✅ 확인 결과 무해한 것 (재조사 불필요)

| 항목 | 확인 내용 |
|---|---|
| **confidence 누수 없음** | `features` 141키에 confidence·up_prob·down_prob 계열 **없음**. 이름만 비슷한 `cvd_direction`·`queue_directional_depletion`은 미시구조 피처다. 분류기가 `conf≥0.70` 규칙을 지름길로 외우는 누수는 **없다** |
| **학습/서빙 정보 시점 일치** | 피처는 T 스냅샷, 레이블만 T+h. `MetaGate.evaluate()`가 넘기는 것과 같은 정보 집합. **룩어헤드 없음** |
| **재학습 스케줄 살아 있음** | 357차가 캠페인 체인에 연결. pkl mtime 2026-08-21 16:02 = 직전 금요일 EOD. **죽은 코드 아님** |
| **학습/채점 임계 분리** | `USE_FIXED_LABEL_THRESHOLD=True`에 의한 **의도적 설계**(개선 4) |

> 🟢 **누수가 없다는 것이 D1의 결론을 오히려 단단하게 만든다** — 스코어러가 못 맞힌 이유가
> "누수로 망가져서"가 아니라 **그 대상 자체를 예측하기 어려워서**라는 쪽에 무게가 실린다.
> 다만 H1~H3이 그 난이도의 얼마를 설명하는지는 **아직 모른다.**

---

# 3. 요약

| # | 항목 | 상태 | 다음 행동 |
|---|---|---|---|
| **H1** | 스코어러 AUC 0.489~0.548 | 🔴 실측 | AUC를 리포트/§12에 노출 + 재설계 1차 관문으로 |
| **H2** | `feat_names` 스키마 고착(폐기 피처 3종 항상 0.0) | 🔴 실측 | 채택 규칙 교체. 영향도는 **미측정** |
| **H3** | 학습 행 86.6% 0 패딩 → 시점 대리변수 | 🔴 실측 | 학습 창 절단 **또는** NaN 처리(한 줄) → AUC 재측정 |
| **H4** | 채점 경로 flat `SIGMA_K` | 🟡 확인 필요 | git 이력 확인 우선. **30m은 CB③ 입력** |
| **H5** | `conf≥0.70` OR 분기 실효 2.9% | 🟡 판단 | 유지/제거 결정. 제거 시 시계 리셋 |
| **H6** | 재설계 사전등록 | 📋 절차 | ① 파이프라인 → ② 레이블 → ③ 피처 순. **결과 보기 전에** 합격선 확정 |

**라이브 영향 0** — 스코어러는 섀도이고, H1~H5 어느 것도 매매 경로에 닿지 않는다.
단 **H4만 예외 가능성**이 있다(30m 채점 → CB③ 입력).

**근거 파일**
- `learning/meta_label_classifier.py` · `learning/meta_labeling.py` · `learning/prediction_buffer.py`
- `strategy/entry/meta_gate.py` · `scripts/campaign_steps.py` · `scripts/train_meta_label_classifier.py`
- `config/settings.py:VALIDATION_CAMPAIGN["meta_gate"]` · `VALIDATION_CAMPAIGN_DECISIONS["meta_gate"]`
- `logs/retrain_eod_20260821.log` (AUC) · `data/db/predictions.db:meta_labels`
- `model/horizons/meta_gate/*_entry_quality_features.pkl`

---

# 부록 A. AUC 개선 탐색 실험 (2026-08-24, MW0602 490차)

> 🔴 **탐색적 실측이다.** 같은 표본에서의 스윕이므로 **순위 비교 전용**이며,
> 이 수치로 합격선을 만들면 313차 원칙 ④ 위반이다. 확증은 사전등록 후 신규 데이터로만(§9).
>
> **재현 검증**: 변형 A(현행 재현)가 `logs/retrain_eod_20260821.log`의 AUC와
> **소수 4자리까지 일치**한다(1m 0.5458 / 3m 0.5092 / 5m 0.4971).
> 하네스가 프로덕션 파이프라인을 정확히 복제했다는 뜻이므로 아래 델타는 신뢰할 수 있다.

## A-1. 방법

프로덕션과 동일 조건(HGB 동일 파라미터 · `TimeSeriesSplit` 3폴드)에서 **한 번에 하나씩**
바꿔 AUC 델타를 측정했다.

| 변형 | 1m | 3m | 5m |
|---|---|---|---|
| **A 현행 재현** (전행·0패딩·side없음·L0) | 0.5458 | 0.5092 | 0.4971 |
| **B** A + FLAT행 제거(방향성만) | **0.5147** | 0.5210 | 0.4982 |
| **C** B + NaN 패딩 | 0.5181 | 0.5251 | 0.5163 |
| **D** C + side(예측방향) 피처 추가 | 0.5197 | 0.5265 | 0.5169 |
| **E** D + 학습창 2026-06-02~ 절단 | 0.5101 | 0.4911 | 0.5123 |
| **F** D + 라벨=순이익(move ≥ 비용) | 0.4983 | 0.5116 | 0.5164 |
| **G** D + 라벨=강한추종(move ≥ 2×임계) | **0.5611** | **0.5468** | **0.5321** |

## A-2. 발견 ① 🔴 현행 AUC 자체가 부풀려진 값이었다

FLAT 예측 행이 **22.7%(1m)~46.2%(15m)** 인데, 이 행들은 피처와 무관하게 라벨이 무조건
`skip`이다(`predicted == FLAT → skip`). 분류기가 *"조용한 장 = skip"* 만 배워도 쉽게 맞힌다.

그런데 **[2] 채널은 방향성 행(`direction≠0`)만 평가한다.** 방향성만 남기면(B)
1m AUC가 0.5458 → **0.5147**로 떨어진다.

⇒ **채널과 관련 있는 진짜 AUC는 0.50~0.52**로, H1에서 본 것보다 더 나쁘다.
프로덕션 로그의 AUC는 **FLAT 인공물이 만든 착시**를 포함하고 있었다.

## A-3. 발견 ② side 부재·패딩은 원인이 아니었다 (가설 기각)

구조적으로는 유력해 보였다 — 분류기가 예측 방향(side)을 입력으로 못 받는데
(`features` 141키에 `predicted`/`direction` 계열 **없음**) 라벨은 방향조건부이고,
같은 `ts`에서 1m·3m이 **반대 방향**을 예측한 3,687쌍의 **라벨 불일치가 61%** 였다.

그런데 side를 피처로 넣어도(D) AUC는 **+0.002 미만**밖에 안 움직인다.
NaN 패딩(C)도 +0.003~+0.019로 소폭이다.
**학습창 절단(E)은 표본 손실이 커서 오히려 악화**된다 —
⇒ H3의 두 처방 중 **"창 절단"은 버리고 "NaN"만 채택**한다.

## A-4. 발견 ③ 배울 수 있는 것은 "이익"이 아니라 "추종 강도"다

- **F(순이익 라벨)**: 0.498~0.516 — **[2]가 묻는 바로 그 대상이 동전이다.**
- **G(강한 추종 라벨)**: 0.532~0.561 — 전 호라이즌 일관 개선.

이는 기존 판정들과 정확히 정합한다: [39] conf·[40] 방향이 기각됐고,
[3] 분위회귀는 **폭**을 잘 맞춘다(불확실성 상관 0.38 PASS, q50 방향 50.8%).
⇒ **이 피처셋이 예측하는 것은 방향·수익이 아니라 크기다.**

---

# 부록 B. P0~P2 구현 (2026-08-24 배포)

> **매매 경로 변경 0건.** 스코어러는 섀도이며(`strategy/entry/meta_gate.py:105`),
> 바뀐 것은 오프라인 학습·리포트 계층뿐이다.

## B-1. P0 — 학습 위생 (`config/settings.py:META_SCORER_TRAINING`)

| # | 변경 | 근거 |
|---|---|---|
| ① | 결측 채움 `0.0` → **`NaN`** | H3. HGB는 NaN 네이티브 처리 — "없음"과 "0"을 구분한다 |
| ② | 스키마 = **마지막 거래일 키 교집합** | H2. 종전 "최다 키 행"이 폐기 피처를 영구 고착시켰다 |
| ③ | **StandardScaler 제거** + **Purged CV**(엠바고 = 호라이즌 분 수) | 트리는 스케일 불변 · NaN과 만나면 유해 · 라벨이 T→T+h를 본다 |

**부수 조치** — 스케일 미사용 전환 시 **구 스케일러 파일을 삭제**한다.
사이드카가 유실되면 `_scaled_flag()`가 종전 동작(True)으로 폴백하는데, 그때 파일이 남아
있으면 **NaN 입력에 구 스케일러가 적용돼 조용히 쓰레기 점수**가 나온다.
파일을 없애면 그 경로에서 모델이 로드되지 않아 **틀린 값 대신 부재로 실패**한다.

### 배포 후 실측 (2026-08-24 재학습)

```
스키마 141개(2026-08-21) · nan=True · scaler=False · purged=True
```

🟢 **H2 해소 확인** — 배포 `feat_names`가 144 → **141개**로 정정됐고,
451차가 폐기한 `program_foreign_net_krw`·`program_individual_net_krw`·
`program_institution_net_krw` 3종이 **사라졌다**.
(`program_arb_net`·`program_non_arb_net`은 현행 라이브 피처라 정상 잔존.)
서빙 6개 호라이즌 전부 정상 스코어 반환 확인.

| hz | 혼합 AUC | **방향성 AUC** | **순이익 AUC** | 강한추종 AUC | 방향성 n |
|---|---|---|---|---|---|
| 1m | 0.5554 | 0.5153 | 0.5040 | 0.5331 | 14,013 |
| 3m | 0.5103 | 0.5121 | 0.5056 | 0.5292 | 7,959 |
| 5m | 0.5012 | 0.5334 | 0.4955 | 0.5530 | 6,218 |
| 10m | 0.5313 | 0.5427 | 0.5032 | 0.5663 | 4,979 |
| **15m** | 0.5639 | **0.5834** | 0.5152 | **0.6214** | 4,298 |
| 30m | 0.5048 | 0.5237 | 0.5131 | 0.5391 | 9,219 |

🔴 **순이익 AUC는 전 호라이즌 0.4955~0.5152** — 위생을 고쳐도 **[2]의 경제적 목표는
여전히 동전**이다. 이것이 P1 게이트가 닫혀 있어야 하는 이유이자, 게이트 설계가
의도대로 작동한다는 증거다.

🟡 **15m이 눈에 띈다**(방향성 0.5834 · 강한추종 0.6214). Purged CV로 누수를 제거한
뒤에도 남은 값이라 관찰 가치가 있다 — 다만 **판정하지 않는다**(사후탐색).

## B-2. P0-① — AUC 소비 경로 신설

종전에는 AUC가 **EOD 로그에만** 찍혀 캠페인 리포트도 수집기 §12도 볼 수 없었다.
매주 찍히는데 **아무도 읽지 않는 상태**가 몇 달 이어졌다(292·303·371·468차와 같은 계열).

- 사이드카 `model/horizons/meta_gate/training_metrics.json` 신설
  (`latest` + `history` 최근 26주 · `config` 스냅샷). ⚠ gitignore 대상 = **PC별 산출물**
- 리포트 **[2-A] 절** 신설 — 병기 AUC 4종 표 + 오독 방지 문구
- `load_training_metrics()` 공개 — 수집기 §12도 같은 경로를 쓸 수 있다

## B-3. P1 — 재개 게이트 (`VALIDATION_CAMPAIGN["meta_gate"]["reopen_gate"]`)

489차 D1이 [2]의 재개 조건을 **소진**시켰다. "소진"은 영원한 금지가 아니라
**"새 사전등록 없이는 재개 불가"** 이고, 이 블록이 그 새 사전등록이다.

```
지표     auc_net_directional   (배포 모델 점수 vs 순이익 라벨, 방향성 행만)
문턱     PredictionCalibrator.DEGENERATE_AUC_MIN = 0.53   ← 런타임 참조(하드코딩 금지)
조건     호라이즌 최솟값 ≥ 문턱  ·  4주 연속
판정창   2026-08-25~
```

- **문턱을 490차 실험 수치에서 역산하지 않았다**(313차 ④). 앵커는 이 채널과 무관하게
  이미 존재하던 시스템 공용 경계("순위 정보가 사실상 없다")다.
- **호라이즌 최솟값**으로 본다 — 하나라도 무정보면 통과로 치지 않는다.
- 현재 상태 **⏳ INSUFFICIENT**(판정창 시작 전). 순이익 AUC 실측상 다음 주에는
  **🔒 CLOSED**가 될 것이 예상된다 — 그것이 정상이고 의도한 결과다.

⚠ **403차의 *"0.53은 변별력이 없다"* 를 여기로 옮기지 말 것.** 그 비판은 **n=200**
(보정기 버퍼, SE≈0.047) 기준이다. 이 채널은 방향성 n≈4천~1.4만이라 명목 SE≈0.005로
0.53은 0.5에서 **약 6.7 SE**다. 앵커로 쓰는 것은 **값**이지 그 값에 대한 403차의 **판단**이
아니다. (CV 폴드 평균이라 실제 불확실성은 더 크다 — **정밀 검정용이 아니라 자원 배분
관문**으로만 쓴다.)

⚠ **게이트 통과 ≠ [2] 합격.** AUC는 라벨 순위, [2]는 손익 분리다.
성립하는 함의는 한 방향뿐 — **AUC≈0.5면 [2]가 통과할 리 없다.**

## B-4. P2 — 섀도 게이지 (판정 미반영)

라벨 G(강한추종)를 **별도 모델 학습 없이** 같은 홀드아웃에서 평가해
`auc_strong_directional`로 매주 기록한다. 비용이 거의 0이다.

묻는 것: *"이 피처셋이 방향·수익이 아니라 **크기**를 잡는가."*
그렇다면 소비자는 진입이 아니라 **청산 축**일 수 있다 — TP1 이후 러너를 얼마나 끌지는
정확히 "추종이 나올 상태인가"의 문제이고, 0816 D4가 확정한 우선순위
(**청산 기하 > 집행 > 진입**)와 방향이 맞다([22] MFE 캡처율 9%·[24] 반납 관찰의 축).

⚠ **재활용하려면 새 채널 사전등록이 필요하다**(주간회의 소관). 0.53~0.62는 여전히 약한
신호이고, **왕복비용을 이길지는 별개 문제**다. 지금은 관찰만 한다.

## B-5. 하지 않은 것 (명시적 기각)

| 항목 | 왜 |
|---|---|
| 학습창 절단 | 변형 E가 **반증** — 표본 손실이 이득을 넘는다 |
| side(예측방향) 피처 추가 | 변형 D 기여 **+0.002 미만**. 복잡도만 늘린다 |
| 호라이즌 채점 수정으로 [2] 부활 | **이미 반증**(463차 교락 제거 후에도 분리도 −0.0136) |
| AUC<0.5 반전 활용 | 방향 반전 계열 **457·458차 2회 기각**, `NEXT_TODO` 등재 |
| H4(`SIGMA_K` 스칼라) 수정 | 🔴 **30m은 CB③ 유일 입력원.** git 이력으로 의도/드리프트 판별이 선행이고, 밴드를 바꾸면 CB③ 임계 0.28도 재설계 대상이 된다(전환기준 ⑥) |
| H5(`conf≥0.70` OR 분기) 제거 | 레이블 정의 변경 = **§9-4 검증 시계 리셋**. 주간회의 소관 |

## B-6. 산출물·검증

| 파일 | 내용 |
|---|---|
| `config/settings.py` | `META_SCORER_TRAINING` 신설 · `meta_gate.reopen_gate` 신설 |
| `learning/meta_label_classifier.py` | P0 ①②③ + 사이드카 + 서빙 정합 + 스케일러 잔재 정리 |
| `scripts/generate_validation_campaign_report.py` | `_meta_reopen_gate()` + [2-A]·[2-B] 절 |
| `tests/test_490_meta_scorer_hygiene.py` | **신설 17종** |

**검증**: 전체 스위트 **480 passed / 0 failed** + 단독 5파일 OK + 신설 17종 통과
+ py37 문법 파싱 OK(라이브 런타임이 import하는 모듈) + 실제 재학습 1회 성공
+ 서빙 6/6 호라이즌 정상 + 리포트 [2-A]·[2-B] 렌더링 확인.

## B-7. 남은 숙제

| # | 항목 | 상태 |
|---|---|---|
| **H2 영향도** | 폐기 피처 3종의 기여도는 여전히 **미측정**(HGB는 `feature_importances_` 없음 — 순열 중요도 필요). 스키마는 고쳤으나 "얼마나 해로웠나"는 모른다 | 🟡 |
| **H4** | `SIGMA_K` 스칼라 vs `SIGMA_K_PER_HORIZON` — **git 이력 확인 선행** | 🟡 |
| **H5** | `conf≥0.70` OR 분기 유지/제거 — 주간회의 | 🟡 |
| **15m 이상치** | 방향성 0.5834 · 강한추종 0.6214가 왜 홀로 높은가 — **판정 금지**(사후탐색), 관찰만 | 👁 |
| **본질** | 위생을 고쳐도 순이익 AUC는 0.50이다. 병목은 파이프라인이 아니라 **피처에 담긴 정보 그 자체**이며, 그것은 26주 WFA 피처셋 재검증(L1~L3)의 몫이다 | 📋 |
