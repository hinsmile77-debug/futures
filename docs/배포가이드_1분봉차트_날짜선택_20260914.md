# 배포 가이드 — 1분봉 차트 날짜 선택 (567차)

대상 `dashboard/main_dashboard.py` · **매매 정책 무변경 · 표시 전용**
근거 `docs/1분봉차트_날짜선택_타당성검토_20260914.md` · 전체 diff `docs/1분봉차트_날짜선택_A단계.diff`

---

## 0. 한 줄

`MinuteChartDialog` 이 **오늘만** 그리던 것을 **262 거래일 아무 날이나** 그리도록 바꿨다.
과거 날짜를 보는 동안 실시간 유입은 **완전히 차단**되고, 레이어별로 「미측정」과 「0건」을
가르는 배지가 붙는다. 15:09~15:45 마감구간은 `session_bars` 에서 이어 붙인다.

**주문·게이트·사이징·청산 로직은 한 줄도 건드리지 않았다.**

---

## 1. 무엇이 바뀌었나

| 단계 | 내용 |
|---|---|
| **A** | 날짜 인자화 + `_live_mode` 플래그 + 달력 팝업 |
| **B** | 레이어별 수집 상태 배지 (봉·레짐·방향·거래·GP) |
| **C** | 마감구간(`session_bars` 15:09~15:34) 병합 + 경계 표시 |

### A — 날짜 선택

- `reload_today(session_date=None)` · `_start_reload_thread(session_date=None)` 로 인자화.
  하위 조회 4종(`fetch_today_trades` · `fetch_regime_today` · `fetch_direction_today` ·
  `_load_gp_trades`)은 **이미 날짜 인자를 받고 있었다** — 시그니처 변경 0건.
- 날짜칸 + **「달력 ▾」 버튼** + 「오늘」 버튼. 봉이 있는 날은 밝게·굵게, 없는 날은 흐리게.
  없는 날을 고르면 재적재하지 않고 되돌린 뒤 사유를 띄운다.
- 🔴 **`_live_mode` 가드 9곳** — 진입점 8개(`update_tick` · `on_candle_closed` ·
  `set_regime_at` · `set_direction_at` · `record_entry` · `record_exit` ·
  `sync_active_position` · `clear_active_position`) + `maybe_roll_session`.
  이게 없으면 과거 차트 뒤에 오늘 봉이 조용히 이어붙고, `maybe_roll_session` 이
  **다음 봉에 오늘로 강제 복귀**시킨다.
- 날짜 목록 조회는 **배경 스레드**(`_sig_dates_ready`). 실측 262일 DISTINCT 310ms —
  메인 스레드에서 돌리면 장중에 그만큼 화면이 멈춘다.

### B — 레이어 배지

「미측정」과 「0건」은 다르다. `_gp_status_text` 가 GP 한 층에 하던 구분을 5개 층으로 넓혔다.

| 문구 | 뜻 |
|---|---|
| `n건` / `384+27` | 측정값 (뒤 숫자는 마감구간) |
| `0건` | 원천이 돌았고 그날 실제로 0 (무포지션일) |
| `0건?` | 0 은 맞지만 가동 여부를 못 읽어 사유 미확정 |
| `미수집` | 그날 그 원천이 없었다 — **0건이 아니다** |
| `보관밖` | 레짐 EOD purge(30일) 이전 — 설계상 없다 |
| `기록없음` / `원천없음` / `미배선` / `미조회` | 테이블 빈값 / DB 못 읽음 / GP 미등록 / 조회 전 |

- 커버리지는 **날짜 집합**으로 읽는다(경계 min/max 로는 구간 안의 구멍을 못 잡는다).
- 거래·GP 가 0행일 때는 `direction`(ensemble_decisions)을 **증인**으로 써
  무포지션일과 미수집을 가른다. 증인을 못 읽으면 `0건?`.
- 배지에 마우스를 올리면 전체 판정표가 툴팁으로 뜬다.

### C — 마감구간

- `raw_candles` 가 15:08 에서 끝나는 건 **설계다**(`config/settings.py:441`, 533차).
  소비처 46파일의 "최근 N행" 창을 밀지 않으려고 15:09 이후를 `session_bars` 로 뺐다.
- `_merge_session_tail()` 이 **raw 의 마지막 ts 이후** 행만 붙인다.
  겹치는 구간은 손대지 않는다(실측: 겹치는 REGULAR 봉은 raw 와 완전히 동일).
- 경계에 세로 점선 + 옅은 배경 + 「수집 절단 → 마감구간 N봉 · 종가단일가 M」.
  15:45 `CLOSE_FILL` 은 **종가단일가**라 연속거래 봉과 성격이 다르다 — 갈라 그린다.
- 이건 계획서 §4 Phase 4 표의 「대시보드 차트 · 15:45까지 표시(표시 전용) · **게이트 없음**」 항목이다.

---

## 2. 배포 절차

원 커밋은 **v9-dev `ad33a64`** 다.

```
1) 미륵이 정지 (장 마감 후)
2) git push origin v9-dev        ← 윈도우에서. VM 에는 자격증명이 없다
3) 미륵이 재시작
```

⚠ **실행 중인 프로세스는 영향받지 않는다** — 파이썬은 임포트 시점에 읽으므로
재시작 전까지는 현재 동작 그대로다. 장중 배포도 무해하지만 권장하지 않는다.

의존성 추가 없음. DB 스키마 변경 없음. 설정값 변경 없음.

### 2-1. 🔴 dev 이식은 보류다 — 지금 체리픽하면 안 된다

시도했고 **충돌로 중단(abort)** 했다. 기록을 남긴다.

`dev` 의 `dashboard/main_dashboard.py` 는 v9-dev 보다 **1,252 삽입 / 606 삭제**만큼
뒤에 있다. 그 사이에 이 파일을 만진 v9-dev 커밋이 **10개** 있다:

```
da66651 555차 후속2: 출처축·차트 Y축·라벨 레지스트리 + GP 마커 재지정
c08128f 555차 후속: 손익 추이 「출처」축 화이트리스트
f6ac416 553차 후속4: GP 병행운용 Phase 4 — 수익 판넬 「GP(가상)」 구분
70ebd33 553차: GP 규칙 병행운용 Phase 0 — 차트 GP 마커
5a11474 545·546차: ProfitGuard L1~L4 배지 복구
a21e270 542차: 수동 맥점 산출 버튼
91d99da 534차 후속 · 4bb2dd3 534차: 당일 맥점 예측 UI
d03b629 519차 · 6dfe6d7 504차
```

567차는 그 위에 얹혀 있다. **자체 완결적이지 않다.**
강행하면 충돌 해소 과정에서 저 10개 기능의 조각이 섞여 들어간다.

**선행 이식이 먼저다.** 어느 것까지 dev 로 옮길지는 별도 판정 사안이고,
이 가이드의 범위가 아니다.

### 2-2. ⚠ 동시 세션 주의

배포 작업 중 **다른 Claude 세션이 같은 저장소에 동시에 커밋**하고 있었다.
`git add` 와 `git commit` 사이에 남의 파일이 인덱스에 들어와 **16개 파일이 섞인
커밋이 한 번 만들어졌다**(즉시 `reset --soft` 후 `commit --only -- <경로>` 로 복구).

교훈: 이 저장소에서는 **`git commit` 에 반드시 경로를 명시한다.**
인덱스 전체를 믿지 않는다.
또한 이 VM 의 git 은 `core.autocrlf=false` 라 **모든 명령에
`-c core.autocrlf=input` 을 붙여야 한다** — 안 붙이면 641 파일이 개행만으로
전부 변경으로 잡힌다.

---

## 3. 검증 체크리스트

`Ctrl+Shift+X` 로 차트를 열고:

| # | 확인 | 기대 |
|---|---|---|
| 1 | 날짜칸 | `2026-09-14 (월)` 형식 |
| 2 | 「달력 ▾」 클릭 | 팝업이 뜬다. 봉 있는 날 밝게, 주말·휴장 흐리게 |
| 3 | 과거 날짜 선택 | 제목이 「복기 · YYYY-MM-DD」, 모드 문구 **주황** |
| 4 | 복기 중 1분 대기 | **오늘로 튕기지 않는다** · 차트에 새 봉이 붙지 않는다 |
| 5 | 흐린 날(주말) 선택 | 재적재 없이 되돌아가고 「✕ … 봉 데이터 없음」 |
| 6 | 「오늘」 | 제목·모드 복귀, 실시간 반영 재개 |
| 7 | 9/11 또는 9/14 | 배지 `봉 384+27`, 오른쪽 끝에 마감구간 경계 |
| 8 | 9/11 | 배지 `거래 0건` (무포지션일) · GP 는 **숫자** |
| 9 | 2025년 날짜 | `레짐 보관밖` · `방향 미수집` · `거래 미수집` |
| 10 | 배지에 마우스 | 판정표 툴팁 |

**4번이 가장 중요하다.** 복기 중에 새 봉이 붙으면 `_live_mode` 가드가 안 걸린 것이다 —
즉시 롤백할 것.

---

## 4. 롤백

```
git revert <커밋 해시>
```

또는 배포 전 원본이 남아 있다면:

```
copy /Y dashboard\main_dashboard.py.bak_20260914_날짜선택A dashboard\main_dashboard.py
```

되돌려도 DB·설정은 그대로다 — 읽기 전용 변경이라 남는 상태가 없다.

---

## 5. 알려진 제약 (고치지 않은 것)

| 제약 | 내용 |
|---|---|
| 마감구간 29일치 | `session_bars` 는 2026-08-03 부터. 그 이전은 15:08 에서 끝난다 |
| 8월분 OI 없음 | `log_recovered` 는 OHLCV 만 복구 — ΔOI 는 9/7 이후만 |
| 8월분 15:33 까지 | 15:34~15:45 없음(만기 지난 월물은 차트 TR 조회 거부) |
| **계약월 미기록** | `raw_candles` 에 종목코드가 없다. **날짜 간 가격 수준을 비교하지 말 것** — 실측 2026-09-11 에 계약이 바뀌었고 화면에 표시가 없다 |
| 레짐 20일 | EOD purge(30일) — 그 이전은 영영 없다. 설계다 |

---

## 6. 검증 방식에 대한 기록

이 변경은 **오프스크린 PyQt5 렌더**(`QT_QPA_PLATFORM=offscreen`)로 실제 위젯을 띄워
검증했다. 그 과정에서 **속성 검사로는 안 잡히는 결함 3건**이 나왔다.

1. `QDateEdit::drop-down` 을 스타일시트로 덮자 화살표가 사라졌다 —
   `calendarPopup()` 은 `True`, `calendarWidget()` 도 정상. **API 는 멀쩡하고 픽셀만 비었다.**
2. `gp_rule_challenger_ids` 는 dict 인데 인덱스로 읽어 GP 층이 통째로 죽었다.
   같은 파일 안 `_load_gp_trades` 가 올바른 접근을 하고 있었다.
3. 무포지션일을 「미수집」으로 오판 — 날짜 집합만으로는 "왜 없는지"를 못 가른다.

**상호작용 요소는 열어서 찍어야 한다.** 속성만 보면 통과한다.

---

## 7. 후속 (이 커밋 범위 밖)

- `dev_memory/DECISION_LOG.md` **567차 항목 미기재** — 그 파일에 다른 작업의
  미커밋 변경(566차)이 있어 건드리지 않았다. 566차 커밋 후 추가할 것.
- 계약월 라벨(D단계)은 착수하지 않았다. `regular_candles.db` 가 코드를 갖고 있어
  **스키마 변경 없이 추정**이 가능하다 — 다만 261일 중 107일이 최근접 MAE>1.0 이라
  "기록"이 아니라 "추정"으로만 표시해야 한다.
- 미륵이 Phase 4 나머지(학습 라벨·분석 하네스)는 **게이트가 있다.** 손대지 않았다.
