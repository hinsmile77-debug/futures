# futures — 미륵이 (KOSPI 200 선물 자동매매)

## 프로젝트 개요

KOSPI 200 선물 1분봉 기반 방향 예측 + 자동매매 시스템 (별칭: **미륵이**).

상세 문서:
```
@CORE.md          (핵심 판단 규칙 — 코딩 전 반드시 확인)
@ROADMAP.md       (Phase별 구현 계획 + 마일스톤 체크리스트)
@_archive/plans/PROJECT_DESIGN.md (2026-04 v1.0 설계 브레인스토밍 — 30m 호라이즌 등 구식 내용 포함, 사료적 참고용만)
```

---

## 운영 환경

| 항목 | 값 |
|---|---|
| Python (런타임) | **3.7 32-bit** (`conda env: py37_32`) — Cybos Plus COM/OCX 필수 |
| Python (재학습) | **3.10 64-bit** (`conda env: py310_64`) — GBM/RF 배치 학습 전용 (226차, OOM 방지) |
| OS | Windows 전용 (Cybos Plus COM/OCX) |
| scipy | **1.5.4** (32-bit DLL 충돌 회피) |
| scikit-learn | 1.0.2, joblib 1.1.1 |
| 선물 분봉 TR | **OPT50029** (선물분차트요청) — OPT10080 사용 금지 |

> **재학습 환경 분리 이유**: py37_32에서 numpy float32 배열 OOM 반복으로 모델 미교체 → CB③ HALT 유발.
> `retrain_eod.py` / `retrain_intraday.py`는 `py310_64`에서 실행되며 `pickle protocol=4`로 저장해 py37_32 런타임에서 로드 호환.
> EOD 로그에 `Python 3.10.20 64-bit`가 찍혀도 정상 — 이상 환경이 아님.
> `EOD_RETRAIN.bat`도 **`py310_64`** 전용 (191차 결정). `scripts\eod_retrain.py`도 동일. 두 파일 모두 py37_32 언급은 구버전 잔재이며, py37_32로 실행하면 OOM 재발 — 다시 거론하지 말 것.

---

## 멀티PC 작업 컨벤션

> 🔴 **[2026-08-23 사용자 결정 / MW0602 487차] 이 절의 멀티PC 정책은 2026-08-23부로
> 폐기됐다.** MW0601·MW0602는 더 이상 컨벤션·안건·채널 번호를 상호 조율하지 않으며,
> 이 `dev` 브랜치는 **MW0602 단독 운영**이다.
>
> - **효력 종료**: 다른 PC 브랜치 커밋의 체리픽과 그 기록 의무 · 양 PC 합의를 전제한
>   안건 처리(예: F-9 채널 번호 동시 재배정, "주간회의에서 양 PC 합의") ·
>   `cmp_summary/cmp_metrics` 주간 PC 대조 의무 · "MW0601 반영"류 상호 반영 관행.
> - **유지(보수적 해석 — 조율 장치가 아니라 이력 연속성·출처 표기라 비용이 없다)**:
>   커밋 태그 `[MW0602]` · dev_memory 세션 헤더 PC명 병기 · 주간 산출물의 PC별 폴더
>   구조(`금요일점검/MW0602/`, 경로 호환). 이 유지 범위가 과하면 사용자 지시로 줄인다.
> - 아래 본문은 **사료로 보존**한다(과거 기록을 고쳐 쓰지 않는다). 폐기 시점에 MW0601
>   몫으로 남은 점검사항은 `docs/정기점검/MW0601_이관_점검사항_20260823.md` 참조.
> - 근거: `dev_memory/DECISION_LOG.md` 2026-08-23(487차).

MW0601·MW0602 두 PC가 같은 repo를 공유하며 각자 모의투자 세션을 돌린다
([[project_this_pc_is_mw0602]] 류 Claude 개인 메모리는 PC별로 분리 저장되어
다른 PC 세션에는 보이지 않으므로, PC간 공유가 필요한 컨벤션은 **반드시 이
CLAUDE.md처럼 git으로 커밋되는 문서에 적어야** 양쪽 PC 세션이 모두 따를 수
있다 — 402차가 이 원칙 자체를 어겨서(작업 PC의 Claude 개인 메모리에만 저장)
같은 날 다른 세션(401차)이 몰랐던 사례로 확인).

- **커밋 메시지 제목 앞에 PC명 태그**: `[MW0601]`/`[MW0602]` 형식 (예:
  `[MW0602] 402차: ...`).
- **`dev_memory/DECISION_LOG.md`·`NEXT_TODO.md` 세션 헤더에도 PC명 병기**:
  `## 2026-07-29 (MW0602 402차 — ...)` 형식.
- **다른 PC 브랜치의 커밋을 cherry-pick 하면 `DECISION_LOG.md`에 남긴다** —
  **원 커밋 sha · 원 PC · 가져온 이유** 세 가지. **코드 변경이 없는 문서 체리픽도 예외가
  아니다.** (2026-08-14 470차 등록)
  - **Why**: 그날 `dev` HEAD `a86b238`이 MW0601의 456차 커밋을 cherry-pick한 것이었는데
    (author `hinsmile77-debug`, 원 날짜 08-10 18:02, 변경 4파일 1,317줄 **전부 문서**),
    "왜 MW0601 커밋을 MW0602 운영 브랜치에 가져왔는가"가 어디에도 없었다. `git reflog`에
    `cherry-pick` 2회·`reset: moving to …` 4회가 남아 있고 `f6ade3e`는 커밋 메시지 첫 줄이
    `@` 였다(이후 amend). 커밋 태그 `[MW0601]`이 보존돼 **출처 추적은 되지만 의도는 안 남는다.**
  - 두 PC 브랜치가 다르다는 전제(`MW0601=v9-dev` / `MW0602=dev`, `config/settings.py`
    5,476줄 차이)에서 무기록 체리픽은 2026-08-12 MW0601 사고와 같은 계열의 씨앗이다.
- **Why**: 2026-07-02에 두 PC가 독립적으로 같은 세션 번호("273차")를 붙여
  `dev_memory/` 텍스트 충돌이 실제로 발생했다(차수를 원격 기준으로 맞추는
  392차 관행만으로는 "어느 PC의 관찰인지"까지는 구분 안 됨). PC명 태그를
  붙이면 커밋 로그/DECISION_LOG만 보고도 출처를 바로 구분할 수 있다.
- 관련: `dev_memory/DECISION_LOG.md` 2026-07-02(273차 충돌)·2026-07-29(402차)
  항목, [[feedback_multi_pc_commit_policy]].

---

## 절대 원칙 (변경 불가)

### 1. 오버나이트 금지 — 15:10 강제 청산
```
15:10 전 포지션 무조건 청산 (수익/손실 무관)
이유: 1분봉 시스템은 야간 데이터 없음 + 갭 리스크가 시스템 전체를 무력화
```

### 2. Circuit Breaker — Phase 2에서 반드시 구현 (건너뛰기 금지)
발동 조건 5종:
- ① 1분 내 신호 5번 반전 → 15분 정지
- ② 5분 내 손절 3연속 → 당일 정지
- ③ 30분 정확도 < 35% → 당일 정지  ※ **코드 실값은 0.28** — 아래 각주
- ④ 변동성 ATR 3배 초과 → 5분 정지
- ⑤ API 지연 5초 초과 → 즉시 청산

> **[2026-08-14 468차 문서-코드 병기] ③의 실제 임계는 35%가 아니라 0.28이다.**
> `config/settings.py:4735 CB_ACCURACY_MIN_30M = 0.28` (과신 연속 시 강화 임계
> `:4984 CB_ACCURACY_MIN_30M_STRICT = 0.42`, P4 사전제한 단계 경계
> `:4737 CB_ACC_WATCH_MIN = 0.35` / `:4738 CB_ACC_RESTRICTED_MIN = 0.30`).
> 0.35 → 0.28 완화 근거: 2026-06-02에 CB③ 집계에서 FLAT 예측을 제외하고 방향성 예측만
> 세도록 바뀌면서 랜덤 기준선이 50%(UP/DN 2클래스)가 됐고, 0.28은 그 56% 수준 —
> "명백히 노이즈일 때만 정지"로 잡은 값이다(같은 파일 정의부 주석).
> **이번 변경은 문서를 코드에 맞춘 것이며 코드는 바꾸지 않았다** — 30m 퇴역(296차) 이후
> CB③-P4 차단 자체가 비활성이라(아래 CB③-P4 항목) 실동작 영향이 없고, 임계 재설계는
> "실전 전환 기준" ⑥의 몫이다.
>
> ⚠ **같은 성격의 미해소 괴리가 ⑤에 하나 더 있다(별건).** `CB_API_LATENCY_LIMIT = 5.0`은
> Kiwoom 레거시로 Cybos에서 미사용이고, 실질 구현은 `CB_PIPE_PAUSE_MS = 5_000`
> (파이프라인 지연 5초 → **5분 진입 정지**)이다. 트리거 대상(API→파이프)도 동작
> (즉시 청산→진입 정지)도 달라 문구 병기만으로 정리되지 않으므로 468차 범위에서 제외했다.
> `dev_memory/NEXT_TODO.md` 468차 항목에 등록돼 있다.

> **[2026-07-05 모의투자 한정 예외] CB②** — `config/settings.py:CB_CONSEC_STOP_LIMIT = 9999` (사실상 비활성).
> 사유: 실투 전환 전에는 거래 기회 확보와 데이터 축적(레이블·SGD 온라인학습·SHAP 심사 표본)이
> 우선이며, 3연속 기준을 그대로 적용하면 모의투자 중 당일 정지가 잦아 표본이 희소해진다.
> **실투 전환 전 반드시 2~3으로 복원 필수** — 아래 "실전 전환 기준"에 체크리스트 항목으로 등록.
> (근거: `docs/260704_SYSTEM_AUDIT_UPGRADE_PROPOSAL.md` §7-1, P0)
>
> **[2026-08-01 W4 결산 결정] 복원 4주 연기 → 재검토 2026-08-29.**
> `docs/260705_...md` §7은 "캠페인 종료 시 CB② 복원 후 1주 재확인"을 권장했고 2026-08-02가
> 그 4주 시점이었으나, 같은 결산에서 검증 캠페인을 **상시 운영으로 전환**해 "종료 시점"이라는
> 트리거 자체가 사라졌다(아래 "검증 캠페인 운영 모드" 참조). 게다가 복원은 당일 정지를 늘려
> 진입 표본을 직접 줄이는 방향이라, min_samples 미달 채널이 다수인 지금은 검증 속도를 더
> 늦춘다. **무기한 유예가 아니다** — Phase 5 조건 ②·⑤가 여기 걸려 있고 CB②가 9999인 동안은
> 조건 ②를 CB③~⑤로만 충족해야 하므로, 2026-08-29에 반드시 재론할 것.
> (근거: `docs/정기점검/금요일점검/0801_주간회의_검토보고_MW0602.md`, `dev_memory/DECISION_LOG.md` 404차 후속11)
>
> 🔴 **[2026-08-23 주간회의 결정 / MW0602 489차 D2] 위 "재검토 2026-08-29"는
> 효력 종료. 기한을 날짜가 아니라 사건에 묶는다.**
>
> **왜 바꿨나.** 날짜 기한은 **이미 한 번 밀렸다**(2026-08-02 → 08-29). 두 번째 연기는
> 그때마다 새 날짜가 필요하고, 그 경로의 끝이 바로 이 절에 나란히 적힌 CB③-P4·
> FP-CRITICAL이다("재검토하기로 했는데 안 함"). 유예의 진짜 위험은 *잊히는 것*이다.
>
> **바뀐 것 — 복원 시점 = 실전 전환 기준 ⑧(실전 자본 재설정) 해제와 동일 커밋.**
> ⑧이 열리는 순간이 노출이 커지는 순간이고 CB②가 실제로 필요해지는 순간이다.
> "재검토 날짜"가 아니라 **⑧의 선행조건**이 되므로 ⑧을 건드리는 사람은 반드시 마주친다.
>
> **바뀌지 않은 것** — `CB_CONSEC_STOP_LIMIT = 9999` 유지. 복원 목표값 "2~3"도 그대로다.
> **실투 전환 전 복원 필수**라는 문장도 그대로다.
>
> **그리고 그 사이 매주 실측이 쌓인다** — 캠페인 채널 **[56] `cb2_restore_shadow`**
> 신설(`scripts/cb2_restore_shadow.py`). "CB②가 살아 있었다면 그날 몇 시에 멈췄고
> 그 뒤 진입분의 손익은 얼마였나"를 사전등록 기준으로 판정한다. 292·303·371차가
> 반복 확인한 *"계측 먼저, 그다음 배선"* 이다.
>
> ⚠ **집계 단위는 포지션이다(470차 C1).** 로그 `[CB] 연속 손절 N회`로 과거를 세면
> **안 된다** — 2026-08-14 이전 기록은 레그 단위라 과대계상된다(실측 대조 08-14:
> 로그 max=3 vs 포지션 재구성 max=2). 채널은 `trades.db`에서 현행 규칙으로 재구성한다.
>
> ⚠ **복원값 2와 3은 방향이 반대다** — 2026-08-23 사후탐색(캠페인 205포지션 재구성):
> `limit=2`는 50포지션 −363,145원을 제거(=CB②가 손실을 막음), `limit=3`은 14포지션
> **+741,670원**을 제거(=CB②가 수익을 막음)했고 후자는 **전부 08-18 하루**다
> (drop-worst 시 +83,979원 ≈ 0). 🔴 **이 수치로 값을 고르지 말 것**(313차 ④) —
> 사후탐색이며 채널의 판정 창은 2026-08-24부터다.
>
> ⚠ **CB②는 손익 장치가 아니라 실전 자본의 꼬리위험 장치다.** 채널이
> `RESTORE_COSTS`를 찍어도 그것은 "복원하지 말라"가 아니라 **대가의 계량**이다.
> (근거: `dev_memory/DECISION_LOG.md` 489차 D2, 0823 주간회의 검토보고)

> **[2026-07-06 한시 예외] CB③-P4** — `config/settings.py:CB3_P4_GRADE_BLOCK_ENABLED = False`
> (C등급 자동진입 차단만 비활성, accuracy_buf 누적·acc30m_stage 추적·대시보드 표시는 유지).
> 사유: 296차가 30m 호라이즌을 앙상블·CoherenceGate·CascadeCoherence에서 전면 퇴역 확정
> (EOD full_cv acc=0.3052 — 랜덤 이하). CB③-P4는 그 퇴역된 30m 정확도만으로 RESTRICTED를
> 판정하는데, `CB_ACC_RESTRICTED_MIN`(0.30)이 30m의 확정된 구조적 성능(0.3052)과 거의 같아
> 정상 샘플링 변동만으로 상시 RESTRICTED에 붙박여 무관한 정상 호라이즌의 C등급 진입까지
> 차단하는 부작용이 292차 진입0 딥다이브에서 실측됨(acc30m=0.0%로 C등급 상시 차단).
> **실투 전환 전 반드시 재검토** — 30m 재도입 또는 CB③ 기준 호라이즌 교체 시 True로 복원.
> (근거: 292차·296차·297차 커밋, 진입0 딥다이브)

> **[2026-07-08 한시 예외] FP-CRITICAL** — `config/settings.py:FP_CRITICAL_GRADE_BLOCK_ENABLED = False`
> (RegimeFingerprint PSI CRITICAL 시 진입 차단만 비활성, PSI 계산·file 로그·strategy_ops
> 패널 표시는 유지. 단 333차 후속5부터 이 WARNING 로그가 대시보드 "2 경보" 탭에
> 반복 표시되던 것은 제거 — file 로그만 남기는 셰도우 모니터링으로 전환).
> 사유: 이 게이트는 2026-05-07 배선됐으나 학습분포 저장 함수가 프로덕션에서 호출된 적이
> 없어 2026-07-07(299차)까지 약 2개월간 PSI=0.0 고정(사실상 죽은 코드, 미발동)이었음.
> 299차가 임시 부트스트랩 기준선을, 302차가 실제 WFA 26주 기준선을 배선해 "부활"시켰으나,
> 부활 후 이틀 연속(07-07·07-08) 서로 다른 기준선에서 공통적으로 PSI가 하루 종일
> CRITICAL(임계 0.30 대비 최대 4배)에 고착 — 신규 진입 이틀 다 0건. CORE 피처(예: ofi_norm)
> 학습분포가 균등폭 10-bin 중 한 구간에 98%+ 몰리는 첨봉 분포라, 라이브 값이 그 구간을
> 벗어나기만 해도 PSI가 수학적으로 크게 튀는 계측 결함으로 추정 — 진짜 시장 구조 변화
> 감지가 아닐 가능성이 높음. CB②/CB③-P4와 같은 취지로 차단만 비활성.
> **실투 전환 전 반드시 재검토** — 분위수 기반 bin 등 계측 재설계 후 정상 구간에서 PSI가
> 오르내리는 것을 확인하고 True로 복원할 것.
> (근거: 303차, `dev_memory/DECISION_LOG.md` 303차 항목)
>
> **[2026-07-23 371차 진행상황]** 균등폭 10-bin(cvd_divergence 98.31%·ofi_norm 97.47%가
> 10구간 중 1구간에 몰리는 메가빈 확정)을 분위수(quantile) 기반으로 재설계 완료
> (`strategy/regime_fingerprint.py`). 추가로 "라이브 버퍼가 20거래일 롤링 취지와 달리
> 매 거래일 재기동마다 빈 상태로 시작해 소표본 노이즈로 PSI가 더 부풀려지는" 별도 결함도
> 발견·수정(재기동 시 `raw_features`에서 워밍업). 과거 6개 날짜(2026-06-16~07-23) DB 재생
> 재현검증에서 PSI가 0.02(평온)~0.50(오늘 같은 극단 변동성)로 시장 상황과 합리적으로
> 연동됨을 확인 — 단 **DB 재생 기반 오프라인 검증이며 라이브 경로는 아직 미검증**.
> `FP_CRITICAL_GRADE_BLOCK_ENABLED`는 이번 수정으로 즉시 True 전환하지 않고 False 유지 —
> 며칠 라이브 관찰 후 판단할 것(`dev_memory/NEXT_TODO.md` 371차 항목).
> 또한 cvd_divergence·ofi_norm 학습표본의 91%가 정확히 0.0인 점질량은 분위수 비닝으로도
> 완전히 해소되지 않는 잔여 이슈로 별도 등록(CORE 피처 자체 조사 필요, 이번 수정 범위 밖).
> (근거: 371차, `dev_memory/DECISION_LOG.md` 371차 항목)

> **[2026-07-12 도입 / 2026-08-13 등재] TOX-SEVERE-SPREAD** —
> `config/settings.py:TOXICITY_SEVERE_SPREAD_BLOCK_ENABLED = False`
> (극단 스프레드 `spread_ticks ≥ 20.0` 진입 차단만 비활성. `spread_extreme_shadow`
> 계산과 ToxicityGate의 나머지 경로 — block 0.78 / reduce 0.58 / severe 8틱 폴백 —
> 은 모두 정상 동작).
>
> ⚠ **이것은 "비활성화된 게이트"가 아니라 "켜진 적이 없는 게이트"다.**
> 311차 후속4(`cf2d1b4`, 2026-07-12)가 **처음부터 `False`로 신설**했다 — 끈 커밋이
> 없으므로 `git log -S`로 "비활성 커밋"을 찾으면 안 나온다.
>
> 사유: 기존 `block(0.78)`은 **한 번도 발동한 적이 없어** 진짜 극단 상황
> (`spread_ticks` 실측 최댓값 **108**)에 대한 안전장치가 사실상 부재했다. 슬리피지
> 경제성 역산(ATR 중앙값 3.371pt) + 전체 분봉표본(n=7,275) 백분위 교차검증에서
> `spread_ticks ≥ 20`(p90)이면 1m 호라이즌 TP1 목표의 **40%가 스프레드 비용만으로
> 잠식**됨을 확인해 이 값을 후보 임계로 제안했다. 다만 그 컷을 검증할 실거래 표본이
> **n=8~19**뿐이라 노이즈가 커서, "섀도로 먼저 관찰한 뒤 활성화"로 남겼다.
> ToxicityGate가 `entry_horizon` 확정 **전에** 호출되는 구조적 제약 때문에 호라이즌별
> 차등은 보류하고 가장 취약한 1m 기준 단일 임계로 보수 설계했다.
>
> 🔴 **섀도 관찰 수단이 배선돼 있지 않았다.**
> `strategy/risk/toxicity_gate.py`가 `spread_extreme_shadow`를 매분 계산하지만
> **어디에서도 소비하지 않았다** — 로그·DB·대시보드 어디에도 안 남았다.
> 2026-07-12 도입 이래 **한 달 넘게 죽은 섀도**였다. FP-CRITICAL이 "학습분포 저장
> 함수가 프로덕션에서 호출된 적이 없어 2개월간 PSI=0.0 고정"이었던 것과 **같은 결함
> 패턴**이다. → **473차(F-8 Phase B)가 배선을 완료했다**
> (`ensemble_decisions.spread_ticks` / `.spread_extreme_shadow`).
>
> **[2026-08-17 473차 정정 ①] "`spread_ticks` 자체를 저장하는 컬럼도 없다"는 맞지만,
> 값이 없었다는 뜻으로 읽으면 틀린다 — 원값은 처음부터 남아 있었다.**
> `ensemble_decisions.features`(141키 JSON)에 매분 저장돼 왔다. 전수 파싱 실측
> (2026-08-17): **보유 24,113행 · max 104.9988 · ≥8틱 7,935행(32.9%) ·
> ≥20틱 911행(3.8%, 37 거래일 분산)**. 즉 결손은 데이터가 아니라 **질의 가능한
> 컬럼**이었다. 소급 판정 경로는 `scripts/spread_extreme_watch.py`.
>
> **[2026-08-17 473차 정정 ②] 도입 근거 한 축이 소멸했다 — "block은 한 번도 발동한
> 적이 없다"를 재인용하지 말 것.**
> 311차(2026-07-12) 시점에는 사실이었으나, **419차가 임계를 0.78 → 0.45로 재보정**하면서
> 합성 게이트가 살아났다. 주별 실측: `W18~W28 0건` → **`W29~W32 510건`**(W30은 22.5%).
> "진짜 극단 상황에 대한 안전장치가 사실상 부재"는 더 이상 성립하지 않으며, 남은 질문은
> **합성 게이트와 별도로 `spread_ticks ≥ 20` 전용 컷이 필요한가**로 좁아졌다.
>
> 🔴 **[2026-08-17 473차 / F-8 Phase A] 그 좁아진 질문은 실측으로 판정할 수 없다 —
> 표본이 오지 않는다.**
> 사전등록 기준(`VALIDATION_CAMPAIGN["spread_extreme_watch"]`)으로 소급 판정한 결과
> **INSUFFICIENT**다. 분봉은 911건이지만 **실제 진입은 6포지션 / 4 거래일**뿐이다
> (진입 스프레드 버킷: `<8` 149 · `8-12` 36 · `12-20` 10 · `≥20` **6**).
> 적립 속도 0.094건/거래일 → `min_samples=20` 도달 **ETA 약 149 거래일(7.1개월)**.
> ⚠ **문턱을 낮춰 판정하지 말 것** — 458차 D6과 같은 사전등록 위반이다.
> → **⑨는 "기다리면 닫히는 게이트"가 아니다.** 실전 전환의 선행조건으로 유지할지,
>   26주 WFA 재검증 항목으로 이관할지를 **주간회의에서 결정**해야 한다(D9와 별개 안건).
>
> **복원 절차(갱신)**
> 1. ~~섀도 계측 배선~~ → **[DONE 2026-08-17 473차]**
> 2. 표본은 7개월 스케일이므로, 손익 축 대신 **집행 비용 축**(슬리피지 실측)으로
>    판정할지 여부를 먼저 정한다. 그 전에는 `False` 유지.
>
> **실전 전환 기준 ⑨** 로 등록돼 있다 — 차단 게이트 27개 중 유일하게 복원 조건이
> 없어 체크리스트에서 구조적으로 누락되던 항목이었다(0812·0813 3일 연속 지적).
> (근거: 311차 후속4 `cf2d1b4` 및 `config/settings.py:4800~4814` 주석,
>  `dev_memory/DECISION_LOG.md` 2026-08-13 461차 문서(F-4))

### 3. CORE 피처 — 호라이즌 그룹별 분리 (절대 교체 불가)

피처 유효 구간이 호라이즌마다 달라 단일 CORE를 전 호라이즌에 강제하면
10m~30m에서 ofi_norm(틱 잡음)·cvd_divergence(희석) 등이 역효과.
호라이즌 그룹별로 의미 있는 CORE를 분리 정의한다.

| 그룹 | 호라이즌 | CORE 피처 | 파일 | 체크리스트 규칙 |
|---|---|---|---|---|
| **단기** | 1m·3m·5m | CVD 다이버전스 | `features/technical/cvd.py` | 미통과 → 등급 하락 |
| **단기** | 1m·3m·5m | VWAP 위치 | `features/technical/vwap.py` | 미통과 → **강제 X** |
| **단기** | 1m·3m·5m | OFI 불균형 | `features/technical/ofi.py` | 미통과 → 등급 하락 |
| **중기** | 10m·15m | VWAP 위치 | `features/technical/vwap.py` | 미통과 → **강제 X** |
| **장기** | 30m | opt_chain_pcr | `collection/options/option_chain_worker.py` | 미통과 → 등급 하락 ⚠ **미발동(도달 불가)** — 아래 488차 각주 |

> 🔴 **[2026-08-30 500차 / v9-dev 체리픽] 단기 3행의 "CORE 피처" 이름은 실제 소비
> 키가 아니다 — 이 표만 보고 인용하지 말 것.** 실소비 키는 `CORE_FEATURES_BY_GROUP["short"]`다:
>
> | 표의 이름 | 실제 체크리스트 소비 키 | 상태 |
> |---|---|---|
> | CVD 다이버전스 | **`cvd_delta_norm`** (2026-06-25 교체) | ✅ 건강 — 단 CVD가 아니다(아래) |
> | VWAP 위치 | `vwap_position` | ✅ 일치 |
> | OFI 불균형 | **`ofi_pressure`** | ⚠ = `sign(ofi_norm)` — 크기를 버린 부호 3값 |
>
> ⚠ **"CORE"의 정의가 세 곳에 있다 — 인용 전에 어느 것인지 확인할 것.**
> `config/settings.py:CORE_FEATURES_BY_GROUP`(체크리스트 게이트, **실집행**) ·
> `strategy/regime_fingerprint.py:_CORE_FEATURES`(PSI/FP-CRITICAL) ·
> `config/constants.py:CORE_FEATURES`(SHAP 심사 퇴출 면제).
> 500차 3단계에서 **앞의 둘은 실집행 정의로 통합**됐다(둘 다
> `['cvd_delta_norm','ofi_pressure','vwap_position']`). 셋째는 **의도적으로 레거시**
> (`['cvd_divergence','vwap_position','ofi_norm']`)로 남는다 — 468차 F-3의 SHAP
> 보호 합집합 `operational_core_names(hz) ∪ CORE_FEATURES`의 한 축이라, 운영 이름으로
> 바꾸면 배포 슬라이스에 남은 `cvd_divergence`·`ofi_norm`(3m)이 보호를 잃는다.
> ⚠ **이 지점이 v9-dev와 다르다** — MW0601은 셋째도 파생으로 바꿨다(그쪽엔 합집합이
> 없어 안전). 불변식은 `tests/test_500_stage3_decisions.py` S5a·S5a2가 고정한다.

> 🔴 **[2026-08-30 500차 / v9-dev 체리픽] `cvd_divergence`는 다이버전스가 아니다 — 재인용 금지.**
> **아래는 전부 MW0602 자체 실측이다**(`raw_features` n=5,382 · 2026-08-10~08-28).
> MW0601 수치(n=5,289)와 거의 같지만, 두 PC는 별개 계좌·별개 DB이므로 이 브랜치에서는
> 이 수치를 쓸 것.
>
> · **부호가 10분 가격방향 하나로 결정된다** — `sign(cvd_divergence) ==
>   sign(price_slope_10m)` **5,044/5,046 = 99.96%**(raw_candles 조인 실측).
>   `cvd.py:157`의 두 분기 중 `(가격↑ & CVD↓)`가 **구조적 도달 불가**여서
>   `(가격↓ & CVD↑)`만 남았기 때문이다 — CVD 기여분이 사실상 0이다.
>   ⚠ MW0601은 같은 사실을 `−sign(...)`로 적었다(부호 규약 차이). **구조가 요점이고
>     부호 방향은 인용할 때 코드로 확인할 것.**
> · **크기가 개장 후 경과시간의 함수다** — `|cvd_divergence|` 시각별 평균
>   08h **0.604** → 09h 0.290 → 10h 0.079 → 15h **0.024**로 **25.7배 단조감소**.
>   `cvd_slope_norm = (C_t − C_{t−9})/max|C|`인데 C가 단조증가라 분모가 하루 종일
>   커지기 때문이다. ⇒ **GBM이 이 피처로 시각을 읽을 수 있다.**
> · `|cvd_divergence| ≡ min(cvd_slope, 1)` — **5,382/5,382**(저장 자릿수 차이
>   3자리 vs 4자리에서 오는 최대 0.0005 오차 내). 두 컬럼은 크기 1개 + 부호 1비트를
>   나눠 가진 것이다.
>
> ⚠ **"SOP §2 D형(시계) 확정"이라 쓰지 말 것** — MW0601 초판의 임시 계산이었고
> 정본 분류기(`scripts/feature_health_report.py:temporal_profile_series`)로 재면
> `cvd`는 D형이 아니라 **상수형**이고 `cvd_slope`·`cvd_divergence`는 어느 형태로도
> 분류되지 않는다. **오염은 실재하는데 현행 형태 분류가 못 잡는다** — 그래서 500차
> 4단계가 `core_feature_discovery`에 CORE 우선 시계 스크린을 따로 붙였다.
> 인용할 때는 **정본 분류기 수치 또는 위 시각별 평균**을 쓸 것.
>
> **한 줄의 결과다.** `cvd.py:update_from_bar()`의 `delta = buy_vol − sell_vol`에서
> Cybos `buy_vol`이 편향돼(MW0602 실측 **buy>sell 99.2%** · 동률 6건 / n=5,382
> `raw_candles`) **누적 CVD가 단조증가**하고, 그러면 파생 정규화가 전부 붕괴한다:
>
> | 파생값 | 단조증가일 때 | MW0602 실측 |
> |---|---|---|
> | `cvd` (=`cvd_norm`) | C_t가 곧 max → 1.0 고정 | 최빈 1.0이 **98.74%** (고유값 22개) |
> | `cvd_direction` | 항상 +1 → 0.5 고정 | 최빈 0.5가 **99.44%**, 값이 {0.0, 0.5}뿐 |
> | `cvd_slope` | 항상 ≥ 0 | 범위 [0.0000, 0.9775], 음수 **0건** |
> | `cvd_delta_norm` (건강 대조군) | — | 음수 **49.8%**, [−1, +1] |
>
> ⚠ **2026-06-25에 이미 알고 있었다**(`settings.py`가 편향을 기록). 그때 조치는
> **체크리스트 소비 키만 `cvd_delta_norm`으로 바꾼 것**뿐이고, 피처 자체는 스케일러·
> PSI·TrendGate·앙상블 숏서킷에 그대로 남았다 — 500차가 그 넷을 고쳤다.
>
> **모델 노출 범위**(호라이즌별 배포 pkl 기준 — GBM은 97개가 아니라 8~13개로 학습한다.
> 97개는 `X_hz` 구성과 **스케일러 fit**에만 쓰인다, `batch_retrainer.py`):
> `cvd_divergence` → **3m·15m**, `cvd_slope` → **5m** ⇒ **앙상블 가중 0.71**.
> `cvd`·`cvd_direction`은 GBM 밖(스케일러·라이브 게이트에만).
> ⚠ 그래서 `CVD_DEBIAS_MODE`는 **`"shadow"` 기본**이다 — 계산만 바꾸면 EOD 재학습
> 전까지 학습/추론 분포가 어긋난다(332차·337차 계열).

> 🔴 **[2026-08-30 500차 / v9-dev 체리픽] OFI 3종의 독립 정보는 `ofi_norm` 하나뿐이다.**
> `ofi.py:compute()`가 같은 값을 세 번 내보낸다 — MW0602 실측(n=5,382):
> · `ofi_pressure ≡ sign(ofi_norm)` — **5,382/5,382 정확 일치**
> · `ofi_imbalance ≡ round(ofi_norm/3, 3)` — **5,382/5,382**(불일치로 세어지는 107건은
>   전부 `x.xxx5` 정확한 half-way tie의 반올림 방향 차이다. `ofi_norm`이 이미 ±3
>   클립이라 `clip(±1)`은 무동작 = 항등식)
> ⇒ 상관을 재기 전에 **소스에서 알 수 있는 항등식**이다. 재검증에서 이 셋을 독립
> 신호 3개로 세면 Bonferroni 분모가 틀리고 계열 검정의 유효 자유도가 무너진다
> (SOP §1 오측정 #9와 동형).
> ⚠ 그렇다고 `ofi_pressure`를 "중복이니 삭제"하면 안 된다 — 단기 CORE **실집행 키**다
> (`tests/test_500_stage3_decisions.py` S7이 고정).


> `macro_vix`는 2026-06-25 CORE 강등. 일봉 VIX → 분봉 상수, ~~SHAP 기여 ≈ 0~~, 임계 VIX 27.5 평상시 항상 통과 확인. 보조 피처로 GBM 피처셋에 유지.
> `macro_risk_off`는 2026-06-25 CORE 해제. 모든 호라이즌 feature_names_hz 미포함 확인 (GBM gain=0, ~~SHAP=0~~). 체크리스트·SGD 경로에도 없음. MacroFeatureTransformer 계산은 유지.
>
> **[2026-08-02 416차 근거 정정] 위 두 줄의 SHAP 수치는 무효다 — 재인용 금지.**
> 2026-06-25는 SHAP 이름↔값 정렬 오염 구간(2026-06-17~07-10, 18거래일) 한복판이다.
> 그날 비영 중요도는 이름 배열 앞 12칸(cvd_*/ofi_*/vwap 블록)에만 실렸고 **나머지 85개
> 피처는 조회하면 무조건 0.0**이었다. 즉 "SHAP=0"은 그 피처에 대해 아무 정보도 담고 있지
> 않다(진짜 중요도가 얼마였든 0으로 읽혔다).
> **단 두 결정은 유지된다 — 근거가 SHAP 하나가 아니었다.** `macro_vix`는 "일봉 → 분봉
> 상수"와 "임계 27.5 무발동"이, `macro_risk_off`는 "모든 호라이즌 feature_names_hz
> 미포함"(모델이 애초에 쓸 수 없음)이 SHAP과 논리적으로 독립이고 2026-08-02 실측
> 재확인에서도 그대로다(macro_risk_off 전 호라이즌 미포함 / macro_vix 10m·30m 보조 유지).
> 근거: `dev_memory/DECISION_LOG.md` 2026-08-02(414차·416차).

```
설정: config/settings.py  HORIZON_CORE_GROUP, CORE_FEATURES_BY_GROUP
모델: model/multi_horizon_model.py  _CORE_MASK_EXEMPT_BY_HZ (AutoMask 면제)
진입: strategy/entry/checklist.py  entry_horizon 인자로 그룹 결정
```

> 🔴 **[2026-08-17 474차] 위 표의 "중기"·"장기" 행은 발동한 적이 없고, 앞으로도 없다 —
> 표본 부재가 아니라 구조다.**
>
> 그룹을 고르는 인자 `entry_horizon`은 `model/ensemble_decision.py:select_entry_horizon()`이
> 만드는데, 이 함수는 경계가 셋(`ENTRY_HORIZON_LOW_BLOCK`/`_B1`/`_B2`)뿐이라
> **`1m`·`3m`·`5m`·`None`만 반환한다**(함수 docstring도 그렇게 적고 있다).
> ⇒ **중기(10m·15m)·장기(30m) 그룹은 도달 불가**다. 458차 항목이 "장기 그룹 발동
> 사상 0건"을 **표본 부재**로 설명했으나 원인은 코드 구조이며, **중기도 같은 상태**라는
> 사실은 그때 드러나지 않았다. 471차 F-1(15:10 강제청산 1차 경로가 구조적 도달 불가)과
> 같은 유형이고, 6개월간 어떤 계측에도 걸리지 않은 것까지 같다.
>
> **회귀가 아니라 태생이다** — git 실측:
> · 2026-06-08 **124차** `select_entry_horizon` 신설 — 처음부터 1m/3m/5m/None만 반환
> · 2026-06-16 **179차** CORE 그룹 분리(short/mid/long) 도입 — **8일 뒤**, 이미 도달
>   불가능한 라우터 위에 mid/long을 정의했다
> 방증: `ATR_HORIZON_TP1_MULT = {1m:0.3, 3m:0.5, 5m:0.7}` — **10m 이상의 청산 설계가
> 존재한 적이 없다.** 라우터 경계만 늘려서 열리는 문이 아니다.
>
> ⚠ **"10m·15m 예측이 불가능하다"는 뜻이 아니다** — 예측·채점·앙상블 투표는 정상이다
> (실측 채점 10m 12,281건 · 15m 11,379건, 가중 0.29·0.28로 **합 57%**). 막힌 것은
> ① mid/long CORE 체크리스트 발동 ② 그 호라이즌 기준 TP1 배수 적용
> ③ `trades.entry_horizon` 귀속 — 세 가지뿐이다.
>
> **처분은 미정이다**(주간회의 안건). 선택지: (A) mid/long 행 폐지 → §3을 단기 단일로
> 축소 / (B) "예약(도달 불가)"로 명기 유지 / (C) 라우터에 상위 경계 신설 —
> ⚠ (C)는 **매매 정책 변경**이며, 실측상 전제가 약하다: tf 밴드별 방향 적중률이
> 비단조라 "고변동 → 장호라이즌 우위"가 지지되지 않고(6.0~8.0에서 5m 46.8% ≈ 15m 47.6%,
> **≥8.0에선 1m 42.2%로 단기가 우위**), TP1·메타게이트 스코어러·라벨 설계가 모두 없다.
> 도달성 불변식은 `tests/test_473_core_group_reachability.py`가 고정한다 — 라우터나
> 그룹 정의가 바뀌면 그 테스트가 깨져 이 문단의 갱신을 강제한다.
>
> 🔴 **[2026-08-23 사용자 결정 / MW0602 488차] `opt_chain_pcr`은 이제 "이중 사문화"가
> 아니다 — 두 축을 분리해 읽어라.**
> 473·474차는 장기 CORE가 두 겹으로 죽었다고 정리했다: ① 체크리스트가 도달 불가 ·
> ② 그 피처가 모델 슈퍼셋(`active_features`)에도 없어 **모델이 쓸 수조차 없다.**
> **②는 더 이상 사실이 아니다** — `opt_chain_pcr`은 `data/db/shap_feature_registry.json`
> 의 `active_features`(105종)에 편입돼 있다(2026-08-23 실측).
>
> **사용자 결정**: *"30m CORE는 모델에 들지 않지만 `opt_chain_pcr`가 `active_features`에
> 편입하고 데이터 수집은 계속하자."* 즉 —
>
> | 축 | 상태 | 뜻 |
> |---|---|---|
> | **CORE 체크리스트 게이트** | 🚫 **여전히 도달 불가** | 위 표 「장기」 행은 발동하지 않는다. `opt_chain_pcr` 미통과가 등급을 떨어뜨리는 일은 **없다** |
> | **일반 피처 / 수집** | ✅ **유지** | GBM이 피처로 쓰고, `collection/options/option_chain_worker.py:102`가 매 사이클 수집을 계속한다 |
>
> ⚠ **둘은 모순이 아니다.** "CORE"는 체크리스트 게이트의 자격이고 `active_features`
> 편입은 GBM이 그 값을 쓴다는 뜻이다. 한쪽이 죽었다고 다른 쪽을 끄면 **IC 재검증
> (26주 WFA 「호라이즌 방향예측 피처셋 재검증」)에 쓸 표본이 함께 끊긴다** —
> `opt_gex_bn`이 설계 시점 IC 0.198 → 최신 0.013으로 감쇠한 것을 관측할 수 있었던 것도
> 수집이 계속됐기 때문이다.
> ⚠ **"편입됐으니 장기 그룹을 되살리자"로 읽지 말 것** — 라우터 경계는 그대로이며,
> (C)안(상위 경계 신설)은 여전히 매매 정책 변경이고 전제가 약하다(바로 위 문단).
> 불변식은 `tests/test_473_core_group_reachability.py::
> test_long_group_core_feature_stays_a_plain_feature`가 고정한다 — **편입이 빠지거나**
> **장기 그룹이 도달 가능해지면** 그 테스트가 깨져 이 문단의 갱신을 강제한다.
> 근거: `dev_memory/DECISION_LOG.md` 2026-08-23(488차 후속2).
>
> 🔴 **30m 학습을 중단하면 안 된다 — CB③의 유일한 입력원이다.**
> 30m은 앙상블 가중 0.0(296차 퇴역)이라 "가중 0인데 매 EOD 학습된다"가 낭비로 보이지만,
> `main.py:5539`가 **`if v["horizon"] == "30m"`** 으로 걸러 `circuit_breaker.record_accuracy()`
> 에 먹이고, `main.py:6203`이 `pred_buffer.recent_accuracy("30m", ...)`로 Contrarian
> 판단을 만든다. 30m 예측이 끊기면 **절대원칙 §2의 CB③이 입력을 잃는다.**
> 이것이 전환기준 ⑥이 "30m 재도입 **또는 CB③ 기준 호라이즌 교체**"라고 쓰인 이유다 —
> 30m 처분과 CB③ 기준 교체는 **한 묶음으로만** 가능하다.

### 4. COM 콜백 내 dynamicCall·emit 금지
```python
# 콜백(_on_receive_tr_data 등) 내부에서 허용:
#   상태 변수 저장 + QEventLoop.quit() 만
# 금지:
#   dynamicCall, pyqtSignal.emit()
# 이유: 0xC0000409 STATUS_STACK_BUFFER_OVERRUN 크래시
```

### 5. GetRepeatCnt / GetCommData 파라미터 구분
```python
GetRepeatCnt(sTrCode, sRecordName)   # 2번째: record_name (콜백 수신값)
GetCommData(sTrCode, sRQName, ...)   # 2번째: rq_name
# meta.get("record_name") or rq_name  으로 fallback
```

### 6. 알파 리서치 봇 — 자동 통합 절대 금지
```
백테스트 자동 큐: OFF
자동 통합: OFF (사용자 검토 필수)
이유: 검증 없는 알파 자동 통합은 시스템 전체를 망가뜨림
```

---

## 계측 4원칙 (코드 규약) [2026-08-11 457차 신설 / G3-A·G8]

> **이 시스템의 가장 비싼 버그는 크래시가 아니라 "조용히 그럴듯한 값"이었다.**
> 아래 4개는 실제로 발생한 사고에서 역산한 규약이다. 새 계측을 짤 때 반드시 확인할 것.

### ① 단위 명시 — `_per_leg` / `_per_position` 접미사

`trades.quantity`는 **청산 레그별 계약수**다(진입 수량이 아니다). 이익 포지션은
TP1/TP2/TP3로 쪼개져 작은 수량 여러 행이 되고, 손실 포지션은 하드스톱 전량청산이라
큰 수량 한 행이 된다. 레그 단위로 집계하면 *"계약수가 클수록 진다"* 는 인과 없는
상관이 만들어진다(417차 실측: 이벤트 단위 p=5.07e-07 → 포지션 단위 **p=0.93**).

→ 손익·승패 집계는 **포지션 단위가 기본**이다. 레그 단위를 쓸 거면 변수·컬럼·표
머리글에 단위를 박아라.

### ② 미측정 ≠ 0 — `*_measured` 동반

"측정하지 않았다"와 "측정했더니 0이다"를 같은 값으로 표현하지 마라.
`program_*` 3종은 원천이 안 주는 키를 `.get(key, 0)`으로 채워 **상수 0이 "정상
수집"으로 위장**했고 그대로 학습 피처가 됐다(451차, 폐기).

### ③ 탈락 가시화 — 필터는 제외 개수·사유를 남긴다

`missing[:5]`가 *"7개 미가용"* 이라 적어놓고 5개만 출력해, 운영자가 "7개 = 나열된
5개"로 오독했다(457차 C6). 하필 은닉된 쪽에 심사 중인 CORE 후보가 있을 수 있었다.

→ 절단할 거면 **잔여 개수를 명시**하라(`… 외 N개`).

### ④ 폴백 가시화 — 기본값이 쓰였으면 그 사실을 남긴다

**2026-08-11 하루에만 같은 패턴이 4건 나왔다.** 전부 "폴백이 정상값처럼 보인다"는
하나의 결함이다:

| 사례 | 증상 |
|---|---|
| `self._entry_horizon_pre` | 읽기 2곳 · **할당 0곳** → 영구 `"1m"`. `meta_gate_horizon` 370/370건 `'1m'` |
| `self._entry_horizon` | 읽기 1곳 · **할당 0곳** → SHAP 패널 CORE 그룹 영구 고정 |
| `recent_accuracy()` | 빈 버퍼 → **조용히 0.5** 반환. `daily_stats`에 8거래일 연속 기록 |
| `_verified_today` | 리셋 뒤 읽기 → 항상 0. 대시보드 추이가 8일치 **가짜 평선** |

규약:

- **`getattr(self, "x", default)` 형태로 런타임 상태를 읽지 마라.**
  `__init__`에서 명시 초기화하고, 미설정은 `None`으로 둬 폴백 시점에 로그를 남겨라.
- **"표본 없음"과 "값이 0.5"를 같은 반환 타입으로 표현하지 마라**
  (`None` 반환 또는 `*_measured` 동반 — ②의 확장).
- **DB 컬럼에 폴백값을 쓸 때는 폴백 여부 플래그를 같은 행에 써라.**
- 리셋과 저장이 같은 함수에 있으면 **리셋 전에 스냅샷을 잡아라**
  (`main.py:daily_close()`의 `_ccf_today` 관례).

**자동 검출**: `tests/test_457_fallback_visibility.py`가 "`getattr(self, "_x", ...)`로
읽히지만 `self._x = ` 할당이 없는 속성"을 전수 검사한다. 새로 만들지 말 것.
  ⚠ [MW0602] `tests/test_457_fallback_visibility.py`는 457차 미이관으로 이 PC에 없다 —
  규약 자체는 따르되, 검사 도구 이식은 457차 계열 이관 시 함께 한다.

### ⑤ 대사는 **모든 축**을 걸어라 — 한 축만 맞춰보면 나머지가 사각지대가 된다

> [2026-08-25 493차 신설] ④의 확장이다. ④가 "폴백을 드러내라"라면 ⑤는
> **"드러낼 대상을 빠뜨리지 마라"** 다.

477차 EOD 대사는 `broker gross vs engine gross`를 매일 비교하고 "대사 일치"를
찍었다. 그런데 `net = gross − 수수료`에서 **수수료는 엔진 가정**이라, 그 축은
어떤 대조도 받지 않았다. 브로커 전환 때 남은 키움 요율(실제의 1/6.54)이
**6개월간** DB·패널·전환기준 판정에 흘렀고, "일치" 문구가 오히려 신뢰를 보증했다.

🔴 **정답지는 처음부터 있었다.** `CpTd6197` 헤더의 예탁현금·익일가예탁현금이
매분 수신·기록되고 있었고 그 차이가 곧 브로커 실현 순손익이다. 없던 데이터가
아니라 **아무도 안 본 데이터**였다 — FP-CRITICAL 죽은 게이트, TOX 죽은 섀도와
같은 계열이다.

규약:

- **파생값(net)을 대사하려면 그 구성요소(gross·수수료)를 각각 걸어라.**
  한 축만 맞으면 "부분 일치"이지 일치가 아니다.
- **대사 성공 로그에 무엇을 비교했는지 쓴다** — `[BrokerPnl] … broker gross 대사
  일치`처럼 축을 이름에 박는다. "일치"만 찍으면 범위를 오해한다.
- **외부 원천(브로커·거래소)이 주는 값은 가정으로 대체하지 마라.** 이미 받고
  있는데 안 보고 있는 것은 아닌지 먼저 확인한다.

**자동 검출 불가** — 이건 코드 형태가 아니라 대사 설계의 문제다. 대신
`tests/test_493_commission_rate_and_net_recon.py`가 이 사건의 지문
(**gross는 일치하는데 net만 어긋난다**)을 회귀로 고정한다.

---

## 매분 실행 파이프라인 (9단계)

```
08:55  매크로 수집 → 시장 레짐 (RISK_ON / NEUTRAL / RISK_OFF) + 실시간 구독 사전 시작
09:00  장 시작

[매분]
STEP 1: 과거 예측 검증 (T-1·T-3·T-5·T-10·T-15·T-30분 채점)
STEP 2: SGD 온라인 자가학습 (즉시 업데이트)
STEP 3: GBM 배치 재학습 (**조건부 트리거** — 아래 각주. "30분마다"가 아니다)
STEP 4: 피처 생성 (수급·옵션·기술·매크로)
STEP 5: 멀티 호라이즌 예측 (1·3·5·10·15·30분)
STEP 6: 앙상블 진입 판단 + 등급 (A/B/C/X)
STEP 7: 진입 실행
STEP 8: 청산 트리거 감시 (하드스톱→손절계단화→TP1~TP3→15:10강제청산→15:18안전망, 5단계)
STEP 9: 예측 DB 저장

15:10  강제 청산
15:40  자가학습 일일 마감 + SHAP 피처 심사
```

> **[2026-08-14 470차 문서-코드 정합] STEP 3은 "30분마다"가 아니다 — 조건부 트리거다.**
> 구현은 `learning/batch_retrainer.py`, 장중 트리거 판정은 `main.py:5643-5709`이며
> 실제 발동 조건은 셋뿐이다:
> - **DriftRetrain 조건A**: `5m 정확도 < 25%` **and** n≥20 **and** 마지막 재학습 60분 경과
> - **DriftRetrain 조건B(조기)**: `5m 정확도 < 15%` **and** n≥15 **and** 30분 경과
>   (공통 가드: CB HALT 아님 · 중복 실행 아님 · 시도 후 5분 쿨다운)
> - 그 외: **주간 월요일 08:50** + **EOD `daily_close`(15:40 이후)**
>
> 기준 호라이즌은 303차 후속에서 30m → **5m**으로 교체됐다(30m은 296차에 앙상블·CB③에서
> 퇴역돼, 실거래에 반영되지 않는 호라이즌의 잡음으로 재학습을 반복 트리거하던 낭비를 제거).
> 재학습 자체는 `_start_gbm_retrain_subprocess()`가 **`py310_64` 서브프로세스**
> (`retrain_intraday.py`)로 돌린다 — 191차 OOM 결정 때문이며 `py37_32`로 되돌리지 말 것.
>
> **그래서 "당일 `[GBM] 배치 재학습` 로그 0건"은 결함이 아니라 조건 미충족(=정상)이다.**
> 2026-08-14 장중 점검이 이 문구를 근거로 STEP 3을 "판정 불가"로 올렸다 — 코드가 옳고
> 문서가 낡았던 경우다(468차 CB③ 병기와 같은 성격). **이번 변경은 문서를 코드에 맞춘
> 것이며 코드는 바꾸지 않았다.**

---

## 확률 판단 기준

| 범위 | 의미 | 행동 |
|---|---|---|
| 50~55% | 중립 | 관망 |
| 55~60% | 약한 방향성 | 관망 |
| 60~70% | 명확한 방향 | 진입 고려 |
| 70% 이상 | 강한 추세 | 적극 진입 |

> **[2026-08-19 476차 문서-코드 병기] 표의 상위 2행(60~70% · 70%+)은 현재 라이브에서
> 도달 불가다 — 표는 2026-05 이전 confidence 분포의 유산이다.**
> 앙상블 등급식(`model/ensemble_decision.py:1196-1224`)의 A/B 임계(conf≥0.70/0.60)에
> 대해, 방향성(`direction≠0`) confidence 는 **2026-06-01 이후 0.60에 한 번도 닿지
> 않았다**(최근 30거래일 p50 0.3478 · p99 0.5073 · p100 0.5970. `conf≥0.70`은 전 기간
> 473건 전부 2026-05-08~05-19). 귀결 세 가지(0819 리포트 1-9):
> ① 09:20~09:29 "앙상블 A등급만 허용" 게이트가 **실질 전면 진입 금지**로 동작
> (로그 54건 전부 B/C→X 강등, A 생존 0건 · 해당 구간 진입 0건)
> ② HCGuard(conf≥0.70 롤링 가드)·CRASH 레짐 A등급 숏 예외는 **발동 자체가 불가**
> ③ `trades.raw_grade` 기록 시작(2026-07-30) 이래 앙상블 `'A'` 0건.
> **이 병기는 문서를 실측에 맞춘 것이며 코드는 바꾸지 않았다** — 임계 재설계(인하 또는
> 09:20~09:29 명시적 금지 존 재정의)는 매매 정책 변경이라 **주간회의(2026-08-22) 안건**이다.
> 도달성은 `tests/test_476_grade_reachability.py`가 고정하고(등급식·임계가 바뀌면 이 각주의
> 갱신을 강제), 수집기 §12b 임계-분포 대조(476차 G-4)가 매일 실측 갱신한다.

---

## Phase 완료 현황

| Phase | 내용 | 코드 | 검증 |
|---|---|---|---|
| Phase 0 | 설계·인프라 | ✅ | ✅ |
| Phase 1 | 핵심 시스템 (데이터·피처·모델·전략) | ✅ | ⏳ 모의투자 필요 |
| Phase 2 | 안전장치 + Walk-Forward | ✅ | ⏳ CB 테스트·26주 데이터 필요 |
| Phase 3 | 알파 강화 (미시구조·레짐) | ✅ | ⏳ 실데이터 검증 필요 |
| Phase 4 | 차별화 (RL·베이지안·뉴스) | ✅ | ⏳ 실거래 데이터 필요 |
| Phase 5 | 실전 운영 | — | 미진입 |
| Phase 6 | 알파 리서치 봇 | ✅ (유전자 진화) | ⏳ 장외 스케줄 미연결 |

---

## 검증 캠페인 운영 모드 — 상시(standing) [2026-08-01 전환]

`docs/260705_OFFENSE_READINESS_AUDIT_AND_NEXT_PHASE.md`가 설계한 "4주 검증 캠페인"은
2026-07-05 시작 → 2026-08-01 W4 결산에서 **기한 없는 상시 운영 채널로 전환**했다
(`config/settings.py:VALIDATION_CAMPAIGN["mode"] = "standing"`).

- **왜**: §3-1(Triple-Barrier)은 표본 미달로 4주 안에 시험조차 못 했고(3m OOS 584/800),
  `min_samples=20`짜리 채널 다수가 실측 도달속도 기준 **9~16주 스케일**이다
  ([8] KellySkip 4건/4주 → 20건까지 약 16주). §8이 상정한 "W4에 활성화/폐기를 확정한다"는
  전제가 표본 현실과 맞지 않았다.
- **바뀌는 것**: 종료 기한 없음. 매주 금요일 EOD 리포트를 계속 생성하고, 채널별로
  `min_samples` 도달 시점에 개별 판정한다([1] TB가 384차부터 이미 그 방식).
- **바뀌지 않는 것**: **모든 합격선·판정문·`start_date` 무변경**. 상시 전환은 거버넌스
  결정이지 판정 기준 변경이 아니므로 §9-4의 검증 시계 리셋 대상이 아니다.

### 판정(verdict)과 결정(decision)은 별개다 — 오독 주의

리포트의 판정·권고는 **조치 여부와 무관하게 매주 재계산·재출력**된다. 따라서
"FAIL이 계속 뜬다 = 미조치"가 아니다. 실제 사고가 양방향으로 다 있었다:

- **이미 이행된 권고를 신규 안건으로 오인**: [6] Hurst 완화는 2026-07-15(333차 후속)에
  배포돼 라이브 중인데 리포트가 3주째 같은 권고를 찍었고, 0801 결산 초안이 그것을
  "승인 1순위"로 잘못 올렸다(코드 확인 후 철회).
- **일부러 적용하지 않기로 한 FAIL이 기록되지 않음**: 다음 세션이 같은 FAIL을 보고
  적용을 재시도할 위험.

→ 그래서 `config/settings.py:VALIDATION_CAMPAIGN_DECISIONS`(확정 결정 레지스트리)를
두고, 리포트가 요약표에 📌 인라인 마커 + 하단 "확정 결정 레지스트리" 섹션으로 렌더링한다.
**결정을 바꾸면 그 레지스트리를 갱신하고 `dev_memory/DECISION_LOG.md`에도 기록할 것.**
(근거: `dev_memory/DECISION_LOG.md` 404차 후속11, 0801 주간회의 검토보고)

### 주간 산출물 위치 규약 — PC별 폴더 + 날짜본 [2026-08-02 407차]

```
docs/정기점검/금요일점검/<PC명>/                  ← MW0601 / MW0602
    validation_campaign_report_YYYYMMDD.md      (코드 생성 — EOD 체인)
    validation_campaign_metrics_YYYYMMDD.json   (코드 생성 — EOD 체인)
    MMDD_주간회의_검토보고_<PC명>.md              (주간회의 세션에서 작성)
```

PC명은 `utils/db_utils.py:pc_id()`가 호스트명에서 뽑는다(`DeskTop-MW0601` → `MW0601`).
폴더가 PC를 나타내므로 **리포트 파일명에는 날짜만** 넣는다.

**두 가지 사고를 막기 위한 규약이다 — 둘 다 실제로 발생했다.**

1. **다른 PC의 리포트를 볼 수 없었다.** 예전 출력 위치 `data/`는 gitignore 대상이라
   2026-08-02 MW0601×MW0602 31채널 대조에서 MW0602 리포트를 사용자가 수동 복사해줄
   때까지 작업이 막혔다. → **커밋되는 위치로 옮겼다.** 이것은 "런타임 산출물 커밋 금지"
   (`feedback_git_commit_scope`)의 **의도적 예외**다 — 리포트는 두 PC가 서로를 보는
   유일한 통로이므로 되돌리지 말 것.
2. **지난주 리포트가 남지 않았다.** 고정 파일명이 매주 덮어써서 2026-07-31분이 08-01
   재생성에 덮였고, 405차 세션 스크래치패드에 우연히 백업이 남아 있지 않았으면 영영
   잃었을 것이다. → **날짜본으로 절대 덮어쓰지 않는다.**

대조 스크립트 `scripts/cmp_summary.py` / `cmp_metrics.py`가 PC 폴더의 **최신 날짜본**을
자동으로 짝지어 읽는다(`--date1/--date2`로 특정 주차 지정 가능).
⚠ 대조는 **같은 코드 세대로 생성된 리포트끼리** 할 것 — 채널 집합이 다르면(출력 상단
"only" 줄이 비지 않으면) 그 차이는 PC 차이가 아니라 코드 세대 차이다.

**새 PC**: 폴더가 없으면 리포트 생성 시 자동으로 만들어진다. 다만 호스트명에서 `MW####`를
뽑지 못하면 `금요일점검/<호스트명>/` 또는 `UNKNOWN/`이 생기므로 **경고를 stderr로 찍는다** —
EOD 로그에 그 경고가 보이면 호스트명을 확인할 것.

**보관(FIFO)**: `config/settings.py:VALIDATION_REPORT_KEEP_WEEKS = 4` — 최근 4주만 남기고
오래된 것부터 자동 삭제한다(0 이하면 비활성). **자동 생성물만 자동 삭제한다** —
접미사가 붙은 수동 스냅샷(`..._20260801_pre405.md`)과 주간회의 검토보고 md는 대상이 아니다.
자기 PC 폴더만 정리하며 다른 PC 폴더는 건드리지 않는다.

---

## 실전 전환 기준 (Phase 5 진입 조건)

```
① 모의투자 4주 통산 수익률 양수
   🔴 **[2026-08-25 493차 / F-1·F-4] 판정 원천이 바뀌었다 — 엔진 net으로 판정하지 말 것.**
   원천은 **브로커 실측 net**(`daily_broker_pnl.broker_net_krw`
   = 익일가예탁현금 − 예탁현금, CpTd6197)이며, 조회는
   `utils/db_utils.py:fetch_daily_net_for_verdict()`를 쓴다(브로커 우선 ·
   폴백 시 `source="engine"` 플래그 동반 — 계측 4원칙 ②·④).

   **왜 바꿨나 — 종전 원천으로는 판정이 뒤집힌다.** 2026-05-11 브로커 전환
   (키움→Cybos) 때 `FUTURES_COMMISSION_RATE`가 키움 값(편도 0.0015%)으로 남아
   실제(0.00981%)의 **1/6.54**만 차감됐다. ①이 정한 4주 창 그대로
   (2026-07-28~08-25, 20거래일 · 폴백 0일):
   **엔진 net +1,087,364원(양수 → 충족처럼 보임) vs 브로커 실측 −414,198원(음수)**.
   ⚠ 2026-08-25 이전에 "①은 충족"이라 판단한 기록이 있으면 **재판정할 것**.

   ⚠ **엔진 net 시계열은 2026-08-25에 불연속이다**(그날부터 실측 요율 적용).
     앞뒤를 직접 비교하지 말 것 — 461차 `mdd_pct`와 같은 유형이다.
     행별 세대는 `trades.commission_rate_used`가 명시한다.
   ⚠ 모의 요율이다. 실전 요율은 다를 수 있어 ⑧에서 함께 재설정한다.
   재검증: `python scripts/commission_rate_recon.py --verify` (26주 WFA 주기 항목).
   근거: `docs/정기점검/매일점검/MW0601-20260825-브로커손익불일치-딥다이브.md`

   > 🔴 **[2026-08-30 MW0602 501차 후속] 위 −414,198은 MW0601 DB 수치다 —
   > 이 브랜치(MW0602)의 판정에 그대로 쓰지 말 것.**
   > 두 PC는 별개 계좌·별개 `trades.db`다([[project_this_pc_is_mw0602]]).
   > MW0602 실측은 **정정 후에도 창에 따라 갈린다**:
   > 07-23~08-21 **−257,453**(미충족) / 07-28~08-25 **+362,029**(충족) /
   > 07-31~08-28 **−705,971**(미충족).
   > → **①의 「4주 창」 정의가 명시돼 있지 않다.** 결과를 보고 창을 고르면
   >   사후탐색이므로(§9-4 검증 시계 리셋 대상) **창을 먼저 정할 것** —
   >   `dev_memory/NEXT_TODO.md` T-6(주간회의 안건).
   >
   > ⚠ **`broker_net_krw` 시계열은 2026-08-30에 불연속이다.**
   > 장후 정산 판독을 채택해 net이 `−수수료`로 기록된 **4일**(2026-06-30 ·
   > 07-01 · 07-14 · 08-06)을 재산출했고, 누락 3일(08-26~28)을 신규 적재했다.
   > 예: 08-06 **−43,142 → −360,142**(8월 손실 6위로 진입).
   > `gross`(`pnl_krw`)는 전 일자 불변이며 **요율 0.0019%도 불변**이다
   > (`--verify` PASS 편차 0.00%). 전환 마커: `strategy_events` **id=86**
   > `METRIC_REDEFINITION`. 2026-08-30 이전 브로커 net 판정과 직접 비교 금지.
   > 근거: `docs/정기점검/매일점검/MW0602-20260830-손실일-딥다이브.md` §8,
   > `dev_memory/DECISION_LOG.md` 2026-08-30(501차 후속).
② Circuit Breaker 1회 이상 정상 작동 확인
   ※ **[2026-08-14 471차 추가] 별도 선행 확인사항 — "15:10 강제청산 경로 실집행 1회 확인".**
   CB①~⑤는 라이브 발동이 확인됐지만, 절대원칙 §1의 집행 경로는 **2026-06-10~08-14 전
   기간 실행 0회**다(`trades.exit_reason='15:10 강제청산'` 0건, `[TimeExit]`·
   `[ExitAttempt]`·`[SchedForceExit]` 로그도 0건). 진입 표본이 전부 15:10 이전에
   닫혔기 때문이며, 그 사이 1차 경로(STEP 8)가 **구조적으로 도달 불가**였다는 사실이
   6개월간 드러나지 않았다(471차 F-1에서 복구). "코드에 있으니 지켜진다"는 암묵 전제를
   실측으로 대체하기 전에는 ②를 충족으로 보지 말 것.
   검증 수단: 모의 1계약 리허설(`dev_memory/NEXT_TODO.md` F-1R에 절차·판정 기준).
   이후에는 26주 WFA 주기에 편입해 반복 확인한다(아래 "주기적 재검증 항목").

   > **[2026-08-13 461차] MDD는 반드시 "자본 대비"다 — 분모를 확인하고 인용할 것.**
   > 같은 이름 `mdd_pct`가 두 곳에서 다른 분모를 썼다:
   > `backtest/performance_metrics.py`는 **자본 대비**(이 기준선이 재는 값),
   > `config/strategy_registry.py`의 Live는 **누적 PnL 곡선의 peak 대비**였다.
   > peak 대비는 100%를 얼마든지 넘는다(2026-08-13 실측 20일 **215.4%**, 60일 **75,916%**).
   > 그 값을 자본 대비 15% 기준에서 빼서 `UNDERPERFORM` + 전략 교체 권고가 나왔다.
   > 같은 구간을 자본 대비로 재면 **3.33%**다.
   > → 461차부터 판정(`_compute_verdict`)·리포트 배너·대시보드는 **`mdd_pct_of_capital`**
   >   만 쓴다. `mdd_pct`는 `mdd_pct_of_peak`의 하위호환 별칭이므로 **판정에 쓰지 말 것**.
   >   분모 자본은 `SIZING_TARGET_CAPITAL_KRW`(현재 5천만원) — ⑧에서 실전 자본으로
   >   재설정되면 이 MDD도 함께 바뀐다.
   > ⚠ **2026-08-13 이전 verdict 시계열과 직접 비교 금지**(불연속).
   >   전환 마커: `strategy_events` id=76 `METRIC_REDEFINITION`.
   >   근거: `dev_memory/DECISION_LOG.md` 2026-08-13 461차 후속, 계측 4원칙 ①.
③ Walk-Forward 26주 통과 (Sharpe ≥ 1.5, MDD ≤ 15%, 승률 ≥ 53%)
④ 일일 수익률 변동성 안정적
⑤ CB② 복원 확인 — `CB_CONSEC_STOP_LIMIT` 9999 → 2~3 (모의투자 한정 예외 해제, 절대원칙 §2 참조)
   ※ **[2026-08-23 주간회의 D2] 기한이 날짜에서 사건으로 바뀌었다** —
     종전 "재검토 2026-08-29"는 **효력 종료**. 복원 시점은 **⑧ 해제와 동일 커밋**이다.
     그 사이 캠페인 채널 **[56] `cb2_restore_shadow`**가 매주 반사실을 재계산한다.
     상세·주의사항(포지션 단위 집계 · 복원값 2와 3의 방향 반대 · 사후탐색 수치 재인용 금지)은
     **절대원칙 §2 CB② 항목** 참조.

> 🔴 **[2026-08-23 주간회의 D6] ①·④는 현재 실측으로 미충족이다 — "인용 불가"가 아니라
> "미충족"이다.**
> 포지션 단위(417차) · `entry_source='SYSTEM_AUTO'` · `data/db/trades.db` 직독:
> - **① 미충족**: 최근 20거래일 통산 **−178,004원**(양수일 **8/20**).
>   07-25 이후 4주 **−175,321원**(157포지션). 캠페인 전체(07-14~08-21)는 205포지션
>   **+329,659원**이지만 **상위 2일을 빼면 −1,679,994원**(건당 −8,528원),
>   상하위 2일씩 빼면 −396,120원(건당 −2,213원)이다.
> - **④ 미충족**: 일손익 표준편차 **415,784원** = 일평균의 **47.9배**.
> - 3주 추이가 단조 악화다 — 상하위 제외 건당 0807 −1,368 → 0814 −2,062 → **0821 −2,213원**.
>
> ⚠ **새 결함이 아니다.** 488차 후속2 전까지 테스트가 ①을 `unmeasured`로 고정해
> 가려져 있었을 뿐이다 — **원래 그랬던 것을 도구가 이제 말한다.**
> ⚠ 따라서 **"모의투자 4주 통산 수익률 양수"를 실전 전환 근거로 인용하지 말 것.**
> (근거: `dev_memory/DECISION_LOG.md` 489차 D6, 0823 주간회의 검토보고 §1-1)
⑥ CB③-P4 재검토 — `CB3_P4_GRADE_BLOCK_ENABLED` False → 복원 여부 결정 (30m 재도입 또는
   CB③ 기준 호라이즌 교체 시 True, 그렇지 않으면 CB③ 자체를 다른 호라이즌 기준으로 재설계
   — 절대원칙 §2 참조)
⑦ FP-CRITICAL 재검토 — `FP_CRITICAL_GRADE_BLOCK_ENABLED` False → PSI 계측(균등폭 10-bin)을
   분위수 기반 등으로 재설계하고, 정상 구간에서 PSI가 오르내리는 것을 실측 확인한 뒤 True로
   복원 (절대원칙 §2 참조)
⑧ 사이징 재설계 — 실전 자본 재설정 **+ `MAX_CONTRACTS` 동반 재검토** (2026-08-01 재작성)

   > 🔴 **[2026-08-23 주간회의 D6] 선행조건 — ①이 충족되기 전에는 ⑧을 해제하지 않는다.**
   >
   > ⑧의 남은 관문은 *"실전 자본 재설정(사용자 결정)"* **하나**다. 즉 마음먹으면
   > 오늘이라도 열린다 — 그래서 명시적 잠금이 필요하다. 지금 열면 안 되는 이유 셋:
   > 1. **①·④가 미충족이다**(위 D6 블록). 최근 20거래일 −178,004원 · 양수일 8/20.
   > 2. **[28] `sizing_inversion_watch`가 아직 판정 능력이 없다** — qty1=165 / **qty3+=1**
   >    (각 20 필요). ⑧을 열면 증거금 상한이 올라 `qty≥3`이 발생하고, 그 채널이
   >    **깨어나는 바로 그 순간**에 입력이 비어 있다(470차).
   > 3. **원인이 미치료다.** 417차 재분해가 지목한 것은 방향이 아니라 **손절 준수율**
   >    (진입수량 vs 손절폭 초과비율 rho=+0.318, 1계약 29.4% vs 3계약+ **68.8%**)이고,
   >    2026-08-23 [33] Phase A 재실행이 **일자 내 순열검정**이라는 독립 방법으로 같은
   >    결론에 닿았다(`ctx.qty` FAST↑ +0.517SD, p=0.0002, BH FDR 10% 통과, 18/18일).
   >    노출을 키우는 것은 그 미치료 축을 키우는 것이다.
   >
   > ⚠ **⑧ 해제 커밋은 CB② 복원(⑤)을 함께 담는다** — 절대원칙 §2 CB② 항목이
   >   기한을 이 사건에 묶었다. 둘을 따로 처리하지 말 것.

   `config/settings.py:SIZING_TARGET_CAPITAL_ENABLED` True(모의투자 한정) →
   실전 자본 규모에 맞게 `SIZING_TARGET_CAPITAL_KRW`(현재 **5천만원**)를 재설정.
   모의 잔고(4.9억)가 실전 자본과 크게 다르면 PositionSizer의 base_risk가 왜곡돼
   계약수 산출이 부정확해진다 (근거: 311차 후속, 339차 후속에서 1억원 → 5천만원 조정).

   > ⚠️ **`False`로 전환하는 것을 단독 지시로 읽지 말 것 — 그것만 하면 손실 구간이 복원된다.**
   > `SIZING_TARGET_CAPITAL_ENABLED`를 끄면 `MAX_CONTRACTS = 10`(현행) 아래에서
   > 2026-07-10 이전의 3~10계약 체제가 그대로 되살아난다. 그 구간과 이후의 손익은
   > **집계 단위와 무관하게** 갈린다 — 포지션 단위로도 동일:
   > **~07-10 75포지션 -881만원(최대 10계약) / 07-14~ 58포지션 +60만원(최대 2계약)**.
   > 경계는 311차 후속4(`cf2d1b4`, 2026-07-12) `SIZING_TARGET_CAPITAL` 도입 시점과 일치하며,
   > 현행 흑자는 목표자본 5천만원이 **우발적으로** 사이즈를 1~2계약에 묶은 결과다.
   >
   > **따라서 ⑧은 다음 두 가지를 함께 만족해야 해제된다.**
   > 1. 실전 자본으로 `SIZING_TARGET_CAPITAL_KRW` 재설정 (또는 False 전환)
   > 2. ~~**`MAX_CONTRACTS` 재검토**~~ → **[2026-08-05 MW0601 431차] 조건 2 충족.**
   >    `MAX_CONTRACTS` **10 → 3** 배포 완료(사용자 지시로 주간회의 경로 대체).
   >    포지션 단위 카운터팩추얼: cap=1 +99만 / **cap=2 +169만(최대)** / cap=3 +53만 /
   >    cap=5 -233만 / 무제한 **-828만원**. 손실은 **qty≥6 9포지션 -1,609만원**에 집중.
   >    함께 배포: 품질군 게이트(Exec·Meta·Tox·Hurst) **min() 합성 + 단일 반올림**,
   >    MetaGate 무정보 폴백(reduce 밴드 47.6%가 하드코딩 0.50)의 **사이징 경로만** 중립화
   >    (JointGateBlock 차단 기준 `size_multiplier`는 무변경 — [7] PASS 게이트 보존).
   >    진입 91건 재시뮬: **총노출 137 → 134계약(-2.2%)**, 분포만 1계약 85%→63% /
   >    2~3계약 15%→37%로 재배분. **노출 확대가 아니다.**
   >    ⚠ 이것은 **증상 억제**다 — 원인은 손절 준수율(아래 재분해 참조).
   >    ⚠ 상한이 3이 된 뒤로는 [28] qty≥3 버킷이 "정확히 3"만 담는다.
   >    근거: `dev_memory/DECISION_LOG.md` 431차.
   >
   >    → 남은 해제 조건은 **1번(실전 자본 재설정)뿐이다.** 단, `MAX_CONTRACTS=3`은
   >      모의 자본 5천만원 기준으로 잡은 값이므로 실전 자본 확정 시 **함께 재산출**할 것.
   >
   > **[2026-08-14 468차 G-1 선행조건 충족]** 0813 리포트가 "⑧ 해제 전에 G-1을 선행
   > 조건으로 추가하라"고 제안했고, **G-1은 배포됐다**(사이즈 제한 해제를 이벤트→상태로:
   > `MODEL_LABEL_STATE_UNLOCK_ENABLED`, F-1 `PRE_RETRAIN_DONE_BY_EOD_ENABLED` 동반).
   > 이유: `_pre_retrain_done` 미해제가 base_risk와 **무관하게** 사이즈를 ×0.6으로 눌러왔고
   > (0813 실측 사이저 18회 전부), ⑧ 해제로 base_risk가 오르는 순간 이 배수의 구속력이
   > 커진다 — [28] `sizing_inversion_watch`가 "깨어나는" 바로 그 시점이다.
   > ⚠ **라이브 검증은 아직이다**(2026-08-14 O-1: `사이즈 축소 ×0.6` 0건 확인).
   > 그 확인 전에는 ⑧ 해제를 진행하지 말 것 — 두 변경의 노출 효과가 겹친다.
   > (근거: `dev_memory/DECISION_LOG.md` 2026-08-14 468차 커밋③·⑧)

   > **[2026-08-02 417차 근거 정정] 아래 4개 수치는 무효다 — 재인용 금지.**
   > 2026-08-01 점검 §9-3이 인용한 다음 값들은 **청산 이벤트(행) 단위 집계**이며,
   > 포지션 단위로 다시 세면 결론이 뒤집힌다.
   >
   > | 무효가 된 원 서술 | 포지션 단위 실측(2026-08-02) |
   > |---|---|
   > | 3계약 이상 **16건 6.2% (1승 15패)** -2,642만원 | **39포지션 22승 17패 (승률 56.4%)** -1,174만원 |
   > | 이항검정 **p=5.07e-07** — "우연이 아니다" | **p=0.93** — 유의성 소멸 |
   > | 최악 2일 제거 후에도 **10전 1승 -1,236만원** | **31전 19승 (61.3%) +45만원** (흑자) |
   > | 카운터팩추얼 **완전 단조** (1계약 +1,274 / 상한2 +865 / 상한3 +388만원) | **단조 아님** — 1계약 +103 / **상한2 +157(최대)** / 상한3 +60 / 상한5 -226만원 |
   >
   > **원인**: `trades.quantity`는 **청산 레그별 계약수**다(진입 수량이 아니다).
   > 이익 포지션은 TP1/TP2/TP3로 쪼개져 작은 수량 여러 행이 되고, 손실 포지션은 하드스톱
   > 전량청산이라 큰 수량 한 행이 된다 — 그래서 "계약수가 클수록 진다"가 인과 없이
   > 만들어진다(실측: TP2 평균 레그수량 1.00 vs 하드스톱 2.07). §9-3 표 머리글도
   > `n(이벤트)`로 단위를 자체 표기하고 있었다.
   >
   > **결정은 유지된다 — 근거가 이 통계 하나가 아니다.** ⑧의 두 조건은 그대로다:
   > ① 구간 분해(~07-10 -881만 / 07-14~ +60만)는 포지션 단위로도 같고,
   > ② 카운터팩추얼은 단조는 아니지만 **어떤 상한이든 흑자(+60~157만), 상한 없음만
   > 적자(-822만)** 라 "상한이 필요하다"는 결론은 살아 있다.
   > 죽은 것은 "**단조**"와 "**사이저가 역예측적**"이라는 해석이다.
   >
   > **재분해 결과 — 원인은 방향이 아니라 손절 준수율이다.**
   > 진입수량 vs 계약당손익 상관 **rho=+0.004 (p=0.96, 무관)**. 유일하게 유의한 관계는
   > 진입수량 vs (실현손실 ÷ 의도손절폭 ATR×1.5) **rho=+0.318 (p=0.015**, 이상치 제거 후
   > +0.269 p=0.045**)** — 손절폭 초과 비율이 1계약 29.4% vs 3계약+ **68.8%**다.
   > 큰 포지션은 방향을 더 못 맞히는 게 아니라 **손절선을 지키지 못한다**(미니선물 호가
   > 깊이를 여러 계약이 한 번에 훑는 슬리피지 가설 — **미검증**).
   > 손실도 "3계약 이상"에 고르게 있지 않다: 3계약(11승/17, +145만)·5계약(3승/3, +313만)은
   > 흑자이고 **6계약 이상 9포지션이 -1,609만원**으로 몰려 있다.
   > → 상한 인하는 **증상 억제**일 수 있다. 원인 치료는 집행(손절 체결) 축일 가능성이 높다.
   >
   > **[2026-08-05 431차 갱신] 아래 "눌려 잠든" 진단은 431차 조치로 해소됐다.**
   > 곱셈 게이트 체인이 min() 합성으로 바뀌어 qty≥3 체결이 다시 발생한다(시뮬 91건 중 9건).
   > ~~즉 [28]은 **⑧ 해제를 기다릴 필요 없이 지금부터 표본이 쌓인다.**~~ 아래 원 진단은
   > 배경 이해용으로 보존한다.
   >
   > **[2026-08-14 MW0602 470차 정정] 위 취소선 문장은 라이브가 반증했다 — 재인용 금지.**
   > 431차 배포(2026-08-05) 이후 **8거래일 74포지션이 전부 `qty≤2`이고 `qty≥3`은 0건**이다
   > (`data/db/trades.db`, `entry_source='SYSTEM_AUTO'`: qty1=60 / qty2=14 / qty≥3=**0**).
   > 마지막 `qty≥3` 포지션은 **431차 이전인 2026-07-31**(14:07:58, +0.33pt)이다.
   >
   > **원인은 431차가 고친 게이트 체인도, `MAX_CONTRACTS=3`도 아니다 — 브로커 증거금 상한이
   > 2에서 먼저 자른다.** 431차 시뮬(진입 91건 재시뮬)에 `get_order_available_qty()`가
   > 들어 있지 않았던 것이 괴리의 원인이다.
   > ```
   > 09:53:54 [Sizer] 실효잔고=50,000,000(실제잔고=29,721,639) … → 3계약 (최소=1)
   > 09:53:54 [MarginCap] SHORT 산출=3계약 → 증거금상한=2계약으로 축소
   > ```
   > 2026-08-14 실측: `[MarginCap]` 5건 — 사이저가 3을 냈고 진입까지 간 **5/5 전부 2로 축소**.
   > 구현은 `main.py:15741 get_order_available_qty()` → `:15757 margin_qty < qty`.
   >
   > **따라서 [28]은 "표본이 천천히 쌓이는 중"이 아니라 "⑧이 해제되기 전에는 켜지지 않는"
   > 상태다.** ⑧ 해제 순서를 이렇게 읽어라 —
   > **실전 자본 재설정 → 증거금 상한 상승 → `qty≥3` 발생 → [28] 표본 축적 → 판정.**
   > **[2026-08-20 477차 후속 선행조건] [28] 판정 전에 `entry_qty` 귀속 분리(F-1:
   > `ensemble_decisions.entry_qty_final` = 증거금 캡 이후 실체결 수량)와 `[MarginCap]`
   > 무조건 상태 샘플(G-2: `state=OK|CAP|BLOCK` 매 조회 성공 사이클 1줄)이 배선돼 있어야
   > 한다** — 기존 `entry_qty`는 캡 이전 산출 수량이라 MarginCap 발생일에 1계약 크게
   > 남고(0820 실측 140포지션 중 6건), ⑧ 해제로 증거금이 binding이 되는 빈도가 오르는
   > 바로 그 시점에 [28]의 입력이 부정확해진다. 두 배선은 2026-08-20 배포 완료 —
   > 라이브 검증은 다음 `[MarginCap]` 발생일의 `entry_qty_final == trades.entry_qty`
   > 대사(O-6)로 한다.
   > 부수 효과로 **`MAX_CONTRACTS=3`은 현재 한 번도 구속하지 않는 죽은 파라미터**다
   > (468차 G-2가 잡으려던 "고착" 계열과 같은 형태 — 값 자체는 타당하니 되돌리지 말 것).
   > ⚠ MarginCap이 **유일한** 원인이라는 단정은 하지 않는다(인과 관측은 5건·1거래일).
   >   다만 **"431차 이후 74포지션에서 qty≥3이 0건"** 이라는 부재 사실은 표본이 충분하다.
   > 근거: `dev_memory/DECISION_LOG.md` 2026-08-14 470차,
   >       `docs/정기점검/금요일점검/MW0602/validation_campaign_report_20260814.md` [28].
   >
   > **위 2번의 근거 채널은 지금 "눌려 잠든" 상태다 — 삭제하지 말 것.**
   > `VALIDATION_CAMPAIGN["sizing_inversion_watch"]`[28]은 417차 수정 후에도 qty≥3 버킷
   > 표본이 7건뿐이라 판정을 못 낸다(리포트가 `⚠구조적판정불가`로 표시). 다만 **원인은
   > 사이징 자본이 아니라 사이저 뒤의 곱셈 게이트 체인**이다 — 417차 로그 실측
   > (2026-07-14~07-31): `[Sizer]` 379건 중 **86건(22.7%)이 3계약 이상을 출력**(최대 6계약)
   > 했으나 진입까지 간 69건이 전부 meta·tox 배수에 눌려 1~2계약으로 체결
   > (**qty≥3 체결 0건**, 최빈 조합 `meta≈0.50 × tox=0.70 ≈ 0.35`).
   > 즉 **⑧을 해제해 base_risk가 오르는 순간 이 채널은 즉시 깨어난다** — 감시가 가장
   > 필요한 바로 그 시점이다. 기다린다고 채워지지는 않으므로 그전에 판정이 필요하면
   > 과거 3계약+ 구간(2026-06-10~07-10) 포지션 단위 재분석을 별도 근거로 쓸 것.
   >
   > ⚠ 표본 한계: 3계약+ 포지션 39건(시스템 진입만 37건), 손실 16건. **확정 결론 금지**(313차).
   > (근거: `dev_memory/DECISION_LOG.md` 2026-08-02 417차. 원 출처
   >  `docs/정기점검/매일점검/MW0601-20260731-점검리포트.md` §9-3은 **미정정 상태**이며
   >  같은 이벤트 단위 수치를 담고 있으므로 그쪽을 직접 인용하지 말 것)
   >
   > **[2026-08-14 471차 후속6 / G-1] 위 "379건 중 86건(22.7%)"은 이제 매주 실측으로
   > 갱신된다 — 그 수치를 더는 인용하지 말 것.**
   > 그 값은 417차에 TRADE 로그로 **한 번 손으로 센 것**(2026-07-14~07-31)이고, DB에
   > 사이저 원본 출력이 없어 캠페인 리포트가 매주 그대로 재인용해 왔다(스스로 "수치가
   > 낡았는지 의심되면 로그를 재집계할 것"이라 적어둔 채로). 471차 후속6이
   > `ensemble_decisions.sizing_trace`(JSON — 수량 계보 4단 + 품질군 배수 전량 +
   > `binding_gate` + 상한 2종)를 신설해, [28]이 `sizer_pressure`로 **매주 재계산**한다.
   > ⚠ **판정에는 관여하지 않는다** — 합격선은 사전등록 그대로다(§9).
   > ⚠ `sizing_trace`는 **2026-08-14 이후 행에만** 있다. 그 이전은 NULL이며 **미측정**
   >   이지 "압력 0"이 아니다(계측 4원칙 ②). 417차 수치와 직접 비교 금지 — 집계 원천이
   >   다르다(로그 vs DB).
   > 선행 순서였던 **F-3(오귀속 수정) → G-1(구조체 저장)은 둘 다 완료**됐다
   > (커밋 `8be4048` → `ca954b8`). 문자열을 고치기 전에 구조체를 만들면 틀린 값이
   > 굳는다는 것이 그 순서의 이유였다.
⑨ ~~TOX-SEVERE-SPREAD 재검토~~ → 🔴 **[2026-08-23 주간회의 D7] 26주 WFA 재검증 항목으로
   이관됨. 실전 전환 선행조건이 아니다.** (2026-08-13 461차 등재 → 2026-08-23 이관)

   > **왜 이관했나.** 선행조건이던 섀도 계측 배선은 473차 F-8 Phase B에서 **완료**됐다.
   > 남은 것은 손익 축 판정인데 **표본이 오지 않는다는 것이 실측으로 확정**됐다:
   > 66거래일 · 진입 스프레드 `≥20틱` **0건** · 관측 최댓값 **19.0002틱**
   > (473차 F-8 Phase A 기준 `min_samples=20` 도달 ETA **약 7.1개월**).
   > 7개월 스케일 표본을 실전 전환의 게이트로 두는 것은 비합리적이고, 도입 근거 한 축
   > (*"극단 상황에 안전장치 부재"*)은 419차 임계 재보정(0.78→0.45)으로 이미 소멸했다
   > (합성 게이트 W29~W32 **510건** 발동).
   >
   > **이관해도 라이브 동작은 변하지 않는다** — `TOXICITY_SEVERE_SPREAD_BLOCK_ENABLED`는
   > **False 유지**다. 바뀐 것은 "이것 때문에 실전 전환을 막지는 않는다"뿐이다.
   >
   > ⚠ **이관 조건 2개** — 461차가 이 항목을 등재한 이유가 *"복원 조건이 없어
   > 체크리스트에서 구조적으로 누락되던 항목"*이었다. 이관이 그 상태를 재현하면 안 된다.
   > 1. 아래 **"주기적 재검증 항목"에 명시 등재**(완료 — 그 절 참조).
   > 2. **판정 축을 손익 → 집행 비용(슬리피지 실측)으로 재설계**한다. 손익 축은
   >    7개월이지만 집행 비용 축은 분봉 표본(`≥20틱` **911건 / 37거래일**)으로 잴 수 있다.
   >
   > 🔴 **선행 규명 1건 (미해결)**: 473차는 `≥20틱` 진입 **6포지션/4거래일**이라 했고
   > 2026-08-23 실측은 **0건**이다. `12-20` 버킷도 10 → 9로 줄었는데 총 포지션은
   > 201 → 222로 늘었다. 스크립트는 474차 이후 무변경이다. **어느 쪽이 옳은지 단정하지
   > 말 것** — 다음 26주 WFA 재검증 때 함께 확인한다.
   > ⚠ **문턱을 낮춰 판정하지 말 것**(458차 D6과 같은 사전등록 위반).
   > 절대원칙 §2 TOX-SEVERE-SPREAD 항목에 실측·정정 전문.
   > (근거: `dev_memory/DECISION_LOG.md` 489차 D7, 0823 주간회의 검토보고)

→ 실전 첫 1개월: 최대 사이즈의 30%로 시작
```

---

## 주기적 재검증 항목 (26주 Walk-Forward 편입)

바로 위 "실전 전환 기준" ⑤~⑧과 다른 성격의 목록이다 — 그쪽은 "실전 전환 전 1회성
검토"이고, 여기는 파라미터가 시장 미시구조 변화에 따라 계속 드리프트할 수 있어
**매 26주 Walk-Forward 재검증 주기마다 반복 확인**해야 하는 항목이다. 별도 분기/월간
캘린더를 새로 만들지 않고 기존 26주 WFA 주기에 편입하는 이유: 관리 포인트가 늘어날수록
절대원칙 §2의 CB②·CB③-P4·FP-CRITICAL 예외처럼 "재검토하기로 했는데 안 함" 상태로
방치될 위험이 커지기 때문(317차 세션에서 사용자가 정한 원칙).

> **[2026-07-13 등록] Hurst 지수 추정 파라미터** — `config/settings.py:HURST_WINDOW_N=90`,
> `HURST_MAX_LAG=9` (구 60/20에서 317차 재보정, 라이브 배포 완료).
> 사유: 316차가 "grade=C 신호의 63%가 HurstGate 차단"을 발견해 조사하던 중, 운영값
> (N=60/max_lag=20)의 Hurst 추정기가 소표본 하향편향을 가져 진짜 랜덤워크(이론 H=0.5)도
> 평균 H≈0.33~0.36으로 잘못 읽히고, 실전 60거래일 검증에서 진짜 추세 분봉의 72.3%를
> 횡보로 오판해 차단(FalseBlock)하는 상시 구조적 문제임을 확인 — 07-13 폭락일 한정이
> 아니라 평상시(저변동일)에 오히려 더 심한 상시 문제. N=90/max_lag=9로 재보정해
> FalseBlock 72.3%→48.9%로 개선(그리드서치→실전 OOS검증→안정성체크→n_min 정밀스윕
> 4단계로 검증, 성긴 그리드의 우연이 아님을 확인).
> **왜 26주 주기 재검증이 필요한가**: 이 파라미터는 "10거래일 실측 노이즈·드리프트
> 프로파일"(1분 수익률 std=2.73pt, lag-1 자기상관=-0.024)로 캘리브레이션됐다 — 시장
> 변동성 체제가 근본적으로 바뀌면 이 프로파일도, 따라서 최적 N/max_lag도 함께 드리프트할
> 수 있다.
> **다음 26주 WFA 시** `scripts/hurst_oos_validation.py`/`hurst_stability_check.py`를
> 최신 60거래일 데이터로 재실행해 N=90/max_lag=9가 여전히 FalseBlock 최저 구간인지
> 확인 — 벗어나면 동일 절차(그리드서치→OOS검증→안정성체크)로 재보정할 것.
> (근거: 317~318차, `dev_memory/DECISION_LOG.md`·`NEXT_TODO.md` 317차 Phase 0~5 항목)

> **[2026-07-23 등록, 임계값 미변경] PSI 경보 임계값** — `strategy/regime_fingerprint.py:
> _PSI_WATCH=0.10 / _PSI_ALARM=0.20 / _PSI_CRIT=0.30` (교과서적 일반값, 이 시스템 데이터로
> 캘리브레이션된 적 없음 — 372차가 재보정을 시도했으나 근거 부족으로 **기존값 유지**).
> 사유: 371차가 PSI 계산결함(균등폭 bin·라이브버퍼 미영속)을 고친 뒤, 372차가 사용자 제안대로
> "손익검증 + 313차 z-test 방식"으로 58거래일(2026-04-28~07-23) walk-forward 재구성 데이터
> 기반 재보정을 시도. 신호단위(n=29,089) z-test는 `0.10≤PSI<0.15` 구간에서 유의한 적중률
> 저하(32.9% vs CLEAR 36.3%, p=0.0005, Bonferroni 생존)를 보였으나 그 외 구간은 비단조적.
> 실거래(n=197) 손익은 `0.10≤PSI<0.20` 구간이 극적으로 나빠 보였지만(-354,586원/건 평균)
> 손실의 91%가 단 2건(2026-07-09)에서 나온 이상치 착시로 확인. 신호단위 대신 일자단위
> (58개, 진짜 독립관측치)로 보면 PSI-적중률 상관계수 r=-0.099로 사실상 무관 — 표본 부족·
> 이상치 민감성 때문에 지금 임계값을 바꾸면 소수 이상치 거래로 정당화하는 꼴이 됨.
> **왜 26주 주기 재검증이 필요한가**: 이번 재구성은 raw_features 과거 기록의 walk-forward
> 재생일 뿐, 371차 워밍업이 실제로 적용된 진짜 라이브 PSI 데이터는 아직 없다. 라이브
> 데이터가 몇 달 쌓이거나 다음 26주 WFA 시점에 표본이 늘어나면 동일 방법론
> (`dev_memory/DECISION_LOG.md` 372차 항목의 스크립트)으로 재시도할 것 — 재시도 시 반드시
> 일자단위 상관과 손익 구간별 이상치 분해를 먼저 확인한 뒤 결론 낼 것.
> (근거: 371~372차, `dev_memory/DECISION_LOG.md`·`NEXT_TODO.md` 372차 항목)

> **[2026-08-09 455차 등록] 호라이즌 방향예측 피처셋 재검증** —
> `scripts/core_feature_discovery.py` · `validate_feature_set_purged_cv.py` ·
> `feature_ablation_purged_cv.py` · `horizon_signal_tradability.py`(L2, 455차 신설) ·
> `ic_decay_catalog.py`(455차 신설, 전 격자 감쇠 카탈로그).
> **왜 26주 주기 재검증이 필요한가**: 피처의 IC는 시장 미시구조 체제에 따라 드리프트한다 —
> `basis_pt`가 소표본 유의(IC +0.048~+0.099) → 표본 확대 후 전 호라이즌 비유의로 소멸한
> 사례, `va_bandwidth` 제거가 옳았음이 사후에야 확인된 사례, `opt_gex_bn` 설계 시점
> IC 0.198 → 최신 0.013 감쇠 사례가 모두 그 증거다.
> **다음 26주 WFA 시**: L1(`core_feature_discovery` 전 피처 스크리닝 — 2026-08-25부터
> **노이즈 하한선 병기 + 중복 군집 표기**, 둘 다 판정 무영향이며 `--no-noise`/`--no-dup`로
> 끌 수 있다) → L2(`horizon_signal_tradability` 비용차감 거래성) → L3(purged CV)를 순서대로
> 재실행해 현행 피처셋이 여전히 최적 근방인지 확인. 벗어나면 L3'(ablation)로 원인 피처를
> 분해한 뒤 단일 호라이즌씩 순차 교체. 감쇠 추세는 `ic_decay_catalog`의 월간 날짜본
> 대조로 확인한다.
> (근거: 07-30 실행계획 §2-4, `dev_memory/DECISION_LOG.md` 455차 항목)
>
> 🔴 **[2026-08-24 사용자 지시] 이 재검증은 반드시**
> **`docs/Spec for feature/피처_재검증_및_호라이즌배정_원칙.md`를 먼저 열고 시작한다.**
> L1→L2→L3를 바로 돌리지 말 것 — 그 앞에 **유형 분류 → 중복 축약 → 데이터 정제**가
> 선행해야 한다. 계기는 2026-08-24 재검증 중 발생한 **오측정 10건**이며, 전부 통계 지식이
> 아니라 **절차 부재** 때문이었다:
> · 분류 없이 전 피처에 수명 지표를 매겨 **상위 15개 중 11개가 부적격**(시계·누적·상수)
> · `phase_randomize`를 자기상관 지표의 널로 사용 — 그 널은 **ACF를 보존**해 검정이 무력화
> · `NaN` 전파로 순열검정 **p=0.0000**(실제 0.1426) — 에러 없이 **정반대 결론**
> · 중복 미인지로 수급 5피처를 독립 신호 5개로 계수(실제 `|r|`≥0.9999) → **계열 검정의
>   유효 자유도가 무너져** 판정 미달(p=0.1426)을 자초
> 그 문서는 위 10건에서 역산한 규약(§2~§8)과 **호라이즌 배정 4단계 절차**(§9),
> **금지 목록**(§10), **실행 체크리스트**(§12)를 담는다.
> ⚠ **호라이즌 배정은 착수 전 "배정의 시간 안정성" 확인을 통과해야 한다.**
> 2026-08-24 실측은 **보류 판정**이었다 — 전·후반 배정 재현 7/25(p=0.69), 홀짝 6/25(p=0.40),
> 게다가 **재현된 7개가 전부 h=30**이라 분화 자체가 없었다. 미통과 시 **정제만 수행하고
> 배정은 보류**한다.
> (근거: `docs/정기점검/26주WFA_피처셋재검증_L1L2L3_MW0601-20260824.md` §14·§15)

> **[2026-08-14 468차 G-2 등록] 고착 지표 점검** — 일일 점검 수집기 §12
> (`.claude/skills/mireuk-daily-check/scripts/collect_evidence.py:scan_stuck_indicators`).
> **왜 26주 주기 재검증인가**: 292차(CB③-P4 상시 RESTRICTED)·303차(FP-CRITICAL 상시
> CRITICAL)·371차(PSI 메가빈)·468차(`CORE안전` 6거래일 100% ⚠️)가 전부 **같은 실패**였다 —
> 안전장치가 한쪽 값에 붙박여 죽어 있는데 매번 사람이 뒤늦게 발견했다. 매일 §12가 돌지만,
> **감시 대상 목록 자체가 낡는 것**은 일일 점검이 못 잡는다(로그 문구가 바뀌면 `무기록`으로
> 뜨긴 하나, 새로 생긴 지표를 목록에 넣는 일은 아무도 안 한다).
> **다음 26주 WFA 시**: ① `무기록` 항목의 패턴을 현행 로그 문구로 갱신 ②
> 그 사이 새로 생긴 이진/범주형 상태 로그를 `stuck_indicators.patterns`에 추가
> ③ `benign` 목록이 여전히 타당한지 재확인(정상 고착이 비정상 고착으로 바뀐 것이 없는지).
> ⚠ 조건부 로그(임계 초과 시에만 출력)는 넣지 말 것 — 100% 고착이 구조적으로 보장된다.
> (근거: `dev_memory/DECISION_LOG.md` 2026-08-14 468차 G-2, 0813 리포트 §3 로드맵 제안)

> **[2026-08-14 470차 등록] 관측 계층 5종 — 468차 G-2(고착 지표)의 확장·자매 항목**
>
> **왜 26주 주기인가**: 468차 G-2가 "감시 대상 목록 자체가 낡는 것은 일일 점검이 못
> 잡는다"고 등록한 것과 같은 이유다. 아래 다섯은 **모두 470차에 새로 만든 계측**이라
> 지금은 정확하지만, 로그 문구가 바뀌거나 새 지표가 생기면 조용히 죽는다.
>
> **다음 26주 WFA 시 확인할 것**
>
> | # | 항목 | 무엇을 확인하나 |
> |---|---|---|
> | C1' | **`edge_only_indicators` 검사 신설** | §12가 "죽은 지표"를 잡는다면 이쪽은 **"열린 채 끝난 지표"**를 잡는다 — 발동 로그는 있는데 해제/상태 샘플이 없는 태그를 이름 규칙(`발동\|전환\|감지` vs `복구\|해제\|정상`)으로 훑는다. 0814 실측 대상 4종: `ConfFloorGuard`(470차 L2로 해소) · `[TimeRouter] 시간대 전환` · `[MetaGate] 5회 연속 과소` · `[PCR-Dampen] D_FORCE`. **오탐 가능** → §12와 같이 `benign` 화이트리스트를 둘 것 |
> | C2' | **§12 사이징 축 3종 추가** | `sizer_conf_mult` · `sizer_safe_mult` · `margin_cap_qty`. 0814에 `신뢰도배수=0.6` 22/22(10거래일 99.5%)·`안전배수=1.00` 22/22·`[MarginCap]→2` 5/5가 전부 한 값이었는데 감시 목록에 없어 조용히 지나갔다. ⚠ `신뢰도배수`는 431차가 이미 규명·계측 중이므로 **`benign`(정상고착·기록됨)으로 등록** — 목적은 발견이 아니라 *규명된 고착도 계속 보이게 두는 것*이다. ⚠ `MarginCap`은 **조건부 로그**(축소 시에만 출력)라 §5 규약 위반 — 무조건 상태 샘플을 먼저 만들 것 |
> | C3' | **`GuardFair 무효율` 추세** | 2026-08-14 EOD가 6/6 전 호라이즌 `무효` — 그날 교체한 모델의 우열을 한 건도 판정하지 못했다. 457차 `fair_valid` 설계상 `판정 무영향(섀도)`로 표기되고 채널 [23]은 PASS라 **죽은 것은 아니다.** 1일 표본이라 판정하지 않았다 — **무효율의 추세**를 볼 것 |
> | C4' | **집계 단위(레그/포지션)를 3번째 축으로** | 468차 G-3이 청산 라벨을 트리거/결과 2축으로 나눴듯, **집계 단위**를 명시한다. 470차 S3(수집기 §5)·C1(CB②)이 포지션 단위로 정렬됐으나 **Kelly는 여전히 레그를 센다**(470차 C2: 승률 차 -0.9%p). 새 집계가 생길 때마다 어느 축인지 확인할 것 |
> | C5' | **`TIME_ZONES` 11:50~13:00 정의 공백** | 462차 P1-a가 "판정 후 재검토"라 했으나 **기한도 등록부도 없었다** — CB②·CB③-P4·FP-CRITICAL이 방치됐던 경로와 동일. 매일 70분이 `OTHER`(진입 금지)다. 채널 [53] `zone_ban_breach_watch`(n=7, min_samples=20) 표본 진행을 확인하고, 도달했으면 판정할 것. ⚠ 위반 코호트가 흑자(+596,858원)라 **판정 전 집행 금지** |
>
> ⚠ **C1'·C2'는 470차 시점에 미구현이다.** 470차는 그 선행 조건(무조건 상태 샘플:
> `[ConfFloorGuard] state=` · `[CORE준비도] 축퇴 N/M`)만 배포하고 §12에 등록했다.
> (근거: `dev_memory/DECISION_LOG.md` 2026-08-14 470차, 0814 점검리포트 §3-intra·§3-post2)
> **[2026-08-14 471차 등록 / G-3] 15:10 강제청산 경로 리허설 (모의 1계약)** —
> 파라미터 재보정이 아니라 **집행 경로 생존 확인**이라는 점에서 위 항목들과 성격이
> 다르지만, 같은 이유로 이 목록에 넣는다: 별도 캘린더를 만들면 CB②·CB③-P4·
> FP-CRITICAL처럼 "재검토하기로 했는데 안 함" 상태가 된다.
> **왜 필요한가**: 절대원칙 §1의 집행 경로가 2026-06-10~08-14 **6개월간 라이브 실행
> 0회**였고, 그동안 1차 경로(STEP 8)가 구조적으로 도달 불가라는 사실이 아무 계측에도
> 걸리지 않았다 — 진입이 전부 15:10 이전에 닫혀 "조용히 성립하는 것처럼" 보였기 때문이다.
> 471차 F-1이 도달성을 복구하고 F-2가 하트비트(`[SchedForceExit] … bar_pass=N회`)를
> 붙였지만, **주문이 실제로 나가는지는 리허설로만 확인된다.**
> **다음 26주 WFA 시**: 15:05~15:09 모의 1계약 진입 후 15:10 관측.
> 정상 = `[ForceExitPass]` → `[TimeExit]` → `[ExitAttempt]`가 15:10:00~05에 출현하고
> `[SchedForceExit] … 안전망 발동`(ERROR)은 **미출현**. 15:11에 ERROR가 뜨면 1차 경로가
> 다시 죽은 것이다. 비용은 리허설 1건 왕복 수수료(1계약 약 1,600원).
> ⚠ 모의투자 계좌에서만 — 15:10까지 포지션을 들고 가는 시험이다.
> (근거: 471차 F-1·G-3, `dev_memory/NEXT_TODO.md` F-1R,
>  `docs/정기점검/매일점검/MW0601-20260814-점검리포트-post.md` P1-1)

> **[2026-08-23 등록 / 489차 D7] TOX-SEVERE-SPREAD — 실전 전환 기준 ⑨에서 이관**
> `config/settings.py:TOXICITY_SEVERE_SPREAD_BLOCK_ENABLED = False` (라이브 무변경).
> **왜 26주 주기인가**: 실전 전환의 게이트로 두기에는 손익 축 표본이 **7개월 스케일**이다
> (66거래일 · 진입 `≥20틱` **0건** · 관측 최댓값 19.0002틱). 그러나 폐기도 아니다 —
> 스프레드 체제는 시장 미시구조와 함께 드리프트하고, 419차가 합성 게이트 임계를
> 0.78 → 0.45로 재보정하자 W29~W32에 **510건**이 발동한 전례가 있다.
> **다음 26주 WFA 시**: ① `scripts/spread_extreme_watch.py` 재실행 —
> `≥20틱` 진입 표본이 `min_samples=20`에 닿았는지 확인(닿았으면 그때 손익 축으로 판정).
> ② 닿지 않았으면 **판정 축을 집행 비용(슬리피지 실측)으로 재설계**한다 — 분봉 표본은
> 이미 911건/37거래일로 충분하다. ③ 🔴 **473차 `6포지션/4거래일` vs 2026-08-23 `0건`
> 불일치를 규명**한다(스크립트는 474차 이후 무변경, 총 포지션은 201→222로 증가).
> ⚠ **문턱을 낮춰 판정하지 말 것**(458차 D6). ⚠ 이관은 폐기가 아니다 — 461차가 이 항목을
> 등재한 이유가 "복원 조건이 없어 체크리스트에서 구조적으로 누락되던 항목"이었다.
> (근거: `dev_memory/DECISION_LOG.md` 489차 D7, 절대원칙 §2 TOX-SEVERE-SPREAD 항목)

> **[2026-08-23 등록 / 489차] 사전등록 판정 창 3종 — 기한이 도래하면 판정할 것**
> 0823 주간회의가 **결과를 보기 전에** settings에 박은 합격선 셋이다. 판정 창이
> 열려 있는 동안은 매주 캠페인 리포트가 표본 진행만 보여주고, 도달하면 판정이 뜬다.
> **26주 WFA 시 확인할 것은 "아직도 표본이 안 찼는가, 그렇다면 그 채널은 살아 있는가"** —
> [10] TP2 홀드가 7주 연속 0건인 것처럼 **구조적 미발생**일 수 있고, 그때는 채널을
> 계속 돌리는 것이 아니라 **왜 발생하지 않는지**를 물어야 한다.
>
> | 채널 | 사전등록 키 | 판정 창 | 무엇을 묻나 |
> |---|---|---|---|
> | **[46]** Hurst 임계 승격 | `hurst_threshold_shadow.promotion_watch` | 2026-08-24~ | `abs_0.39`의 **신규 통과분**(현행은 차단·후보는 통과)이 왕복비용을 넘는가 |
> | **[55]** 09:20~09:29 (E)안 | `early_window_gate_shadow.vol_condition_prereg` | 2026-08-24~ | 등급 조건을 **ATR 조건**(앵커 `ATR_MAX_ENTRY`)으로 바꾸면 나은가 |
> | **[56]** CB② 복원 | `cb2_restore_shadow` | 2026-08-24~ | 복원값 2·3 각각에서 CB②가 **손실을 막나 수익을 막나** |
>
> ⚠ 셋 다 **문턱을 관측값에서 역산하지 않았다.** [46]은 손익분기(왕복비용) 그 자체,
> [55]는 무관한 기존 시스템 상수, [56]은 다른 채널과 같은 값을 썼다. **판정이 불리하게
> 나와도 사후에 완화하지 말 것**(§9-4 검증 시계 리셋 대상).

> **[2026-08-25 493차 등록 / F-1] 선물 수수료율** —
> `config/settings.py:FUTURES_COMMISSION_RATE` — 🔴 **[MW0602 495차 후속] 이제
> 고정 상수가 아니라 로그인 채널 감지로 파생된다**:
> `config/constants.py:BROKER_CHANNEL_SPECS[detect_broker_channel()]`.
> 대신증권은 로그인 매체별로 요율을 고시한다 — **CREON 트레이딩(MW0602) 0.0019% /
> CYBOS 사이버(MW0601) 0.0098104%** (양쪽 다 사용자 고시 화면 확인 + 각 PC 역산
> 일치, 2026-08-26). 493차가 CYBOS 값을 하드코딩해 들여오자 MW0602에서 5.16배
> 과대가 된 것이 파생 구조로 바꾼 계기다. 감지 근거는 스타터 로그
> (`C:\CREON\STARTER\ncstarter.log` 등) mtime — 레지스트리·프로세스 경로는 이중
> 설치/권한 문제로 오판한다(`constants.py` 주석). 검증:
> `tests/test_495_broker_channel_rate.py`.
> **성격이 다르다 — 시장 프로파일이 아니라 "브로커 계약 조건"이다.** 그래도 여기
> 넣는 이유는 CB②·CB③-P4·FP-CRITICAL과 정확히 같다: 별도 캘린더를 만들면
> "재검토하기로 했는데 안 함"이 된다.
> **왜 주기 재검증이 필요한가**: 이 값은 브로커·계좌·상품이 바뀌면 함께 바뀌는데,
> 실제로 2026-05-11 브로커 전환(키움→Cybos) 때 **갱신되지 않아 6개월간 실제의
> 1/6.54만 차감**됐다. 요율은 조용히 바뀌고(수수료 정책 변경·계좌 등급 조정),
> 틀려도 크래시가 나지 않으며, 틀린 방향이 항상 **낙관** 쪽이다.
> **다음 26주 WFA 시**: `python scripts/commission_rate_recon.py --verify` —
> 브로커 실측(gross − 예탁현금 Δ)을 약정대금에 회귀해 실효 요율을 재산출한다.
> 편차 ±2% 초과면 FAIL이며, 재보정 시 **불연속 마커**(`strategy_events`
> `METRIC_REDEFINITION`)와 `trades.commission_rate_used` 세대 표기를 함께 남길 것.
> ⓐ 고정비 성분(레그당)이 유의해지면 요율 상수 하나로는 재현 불가 — 스크립트가
>    2변수 회귀로 함께 검정한다(2026-08-25 실측 −0.14원 ≈ 0).
> ⓑ 실전 계좌 요율은 모의와 다를 수 있다 — 전환기준 ⑧과 묶어 확인.
> ⓒ 상시 감시는 F-2 net 대사가 맡는다(매 EOD `[NetRecon]`) — 이 26주 항목은
>    그 경보가 없더라도 요율 자체를 다시 재는 이중 안전장치다.
> ⓓ [MW0602 495차 후속] **채널 감지도 함께 재확인**: `BROKER_CHANNEL_SOURCE`가
>    `fallback`이면 스타터 로그 경로(`constants._STARTER_LOG_CANDIDATES`)가
>    낡은 것이다 — 대신 설치 경로 변경 시 후보 목록을 갱신할 것.
> (근거: `docs/정기점검/매일점검/MW0601-20260825-브로커손익불일치-딥다이브.md`,
>  `dev_memory/DECISION_LOG.md` 2026-08-25 493차)

향후 "시장 프로파일 실측치로 캘리브레이션한 파라미터" 성격의 항목이 새로 생기면
같은 형식으로 이 목록에 추가할 것 — 예: 359차 레짐 불안정도 섀도 게이트
(`INSTABILITY_TRANSITION_THRESHOLD` 등)도 섀도 검증을 거쳐 실적용으로 전환되면
편입 검토 대상.
