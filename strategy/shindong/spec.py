# -*- coding: utf-8 -*-
"""신동 규칙 규격 — **사전등록 고정값.** 결과를 보고 바꾸지 않는다.

2026-09-24 사전등록(`docs/신동거래/신동_사전등록_20260924.md`). 9/21–9/23 세 날에서
만든 공통 규칙 1–5 를 기계 판정 가능한 형태로 고정한 것이다.

🔴 값을 바꾸면 **검증 시계가 초기화된다.** 바꿔야 한다면
  ① `SPEC_VERSION` 을 올리고 ② 사전등록 문서에 개정 사유를 적고
  ③ 채점은 새 버전 첫날부터 다시 센다. 조용히 바꾸면 이전 표본과 섞인다
  (461차 `mdd_pct` 교훈 — 같은 이름에 다른 뜻).
"""

SPEC_VERSION = "SD-2026-09-24-v1"

# ── R1 장전 방향 ─────────────────────────────────────────────────────────
PREMARKET_LAST = "08:59"      # 장전 판단에 쓰는 마지막 분
BIAS_MIN = 50                 # |콜−풋 금액(백만원)| < 이것 → 방향 보류(중립)

# ── R2 개장 확정 ─────────────────────────────────────────────────────────
CONF_BASE = "09:00"           # 확정 기준 분
CONF_START = "09:01"
CONF_END = "09:10"
CONF_MIN = 50                 # 09:00 대비 콜−풋 변화(백만원) 문턱
OR_STOP_BUF = 1.0             # 개장 진입 손절 = 09:00 이후 고(저)점 ± 이것

# ── R3 구조맥점 반전 ─────────────────────────────────────────────────────
R3_START = "09:30"
NEW_ENTRY_END = "14:30"       # 이 시각 이후 신규 진입 없음
TOUCH_NEAR = 1.0              # 맥점 이쪽 편 허용 폭(pt)
TOUCH_FAR = 1.5               # 맥점 건너편 허용 폭(pt)
REV_MIN = 50                  # 터치 뒤 콜−풋 반전 문턱(백만원)
REV_WIN_MIN = 10              # 반전 대기 창(분)
LV_STOP_BUF = 1.5             # 맥점 반전 손절 = 맥점 ± 이것 (터치 극값 +0.5 가 더 멀면 그쪽)
EXT_STOP_BUF = 0.5

# ── 청산 ────────────────────────────────────────────────────────────────
T1_MIN_DIST = 2.0             # 1차 목표 후보는 진입가에서 이만큼 이상 떨어져야 한다
TP_BUF = 0.5                  # 목표가를 맥점 이만큼 앞에 둔다
TIME_EXIT = "15:05"           # 시간 청산
LEGS = 2                      # 2계약 — 1차·최종 나눠 청산

# ── 손익 환산(가상) ──────────────────────────────────────────────────────
PT_VALUE_KRW = 50000          # 미니선물 1pt
COMMISSION_RATE = 0.000098104 # CYBOS 편도 (constants.BROKER_CHANNEL_SPECS["CYBOS"])
SLIP_TICK = 0.02              # 시장가 1틱 — 진입 + (손절·시간 청산)

# ── 변형 ────────────────────────────────────────────────────────────────
# MAIN   = 현행(사전등록 본안) — 차트 「거래신동」에 그린다
# SHADOW = E2+F2 섀도 — 기록만 한다(차트에 그리지 않는다)
#   E2: 1차 청산 뒤 1차–최종 중간 지점에 닿을 때까지 원 손절 유지(그 뒤 본전)
#   F2: 맥점 반전은 당일 흐름 방향(09:00 이후 콜−풋 누적)과 같은 쪽만
VARIANTS = ("MAIN", "SHADOW_E2F2")

# ── R3 중단 판정(사전등록) ───────────────────────────────────────────────
# [2026-09-24 사전 정정] 9/29 → **9/28**. 취지는 「추석 연휴 뒤 첫 거래일」이었는데 9/28 을
#   대체공휴일로 잘못 알고 있었다(krx_holidays 오등록 정정). 채점 전 정정이라 표본 영향 없음 —
#   규칙 값은 그대로이므로 SPEC_VERSION 은 올리지 않는다(사전등록 문서 §6-1에 기록).
SCORING_START = "2026-09-28"
R3_KILL_AFTER_DAYS = 10       # 채점 시작 후 이 거래일 수가 차면 판정
R3_KILL_NET_MAX = 0           # R3 누적 순손익이 이것 미만이고
R3_KILL_WINRATE_MAX = 0.35    # R3 승률(거래 단위)이 이것 미만이면 R3 중단
