# 절대원칙 · 한시예외 · 재인용 금지 목록

이 문서는 **바뀌면 안 되는 것**과 **일부러 꺼둔 것**과 **인용하면 안 되는 것**을 한곳에 모은다.
점검 중 이 셋을 헷갈리면 보고서가 정반대 방향으로 간다.

> 출처는 전부 `CLAUDE.md`다. 값이 CLAUDE.md와 어긋나 보이면 **CLAUDE.md가 옳다** — 이 문서가
> 낡은 것이다. 갱신하고 넘어가라.

---

## 0. 브랜치 — MW0601과 MW0602는 다른 브랜치로 돈다 ⚠

| PC | 운영 브랜치 |
|---|---|
| MW0601 | **`v9-dev`** |
| MW0602 | **`dev`** |

같은 리포를 공유하지만 작업 라인이 갈려 있다. **커밋·체리픽 전에 반드시 확인하라.**
`config/settings.py` 가 브랜치 간 5,476줄 차이 난다 — 아래 §2 한시예외 값도 `v9-dev` 기준이다.

## 1. 절대원칙 6종 (변경 불가)

제안이 이 중 하나에 저촉되면 **그 제안 자체를 내지 않는다.**

### ① 오버나이트 금지 — 15:10 강제 청산
15:10 전 포지션 무조건 청산(수익/손실 무관). 1분봉 시스템은 야간 데이터가 없고 갭 리스크가
시스템 전체를 무력화한다.
**점검**: 15:10 강제청산 로그 존재, 15:18 안전망에서 잔여 0 확인.

### ② Circuit Breaker 5종
- ① 1분 내 신호 5번 반전 → 15분 정지
- ② 5분 내 손절 3연속 → 당일 정지
- ③ 30분 정확도 < 35% → 당일 정지 (**코드 실값 `CB_ACCURACY_MIN_30M = 0.28`** — 468차에
  CLAUDE.md에 병기됨. 아래 §3-B 참조)
- ④ 변동성 ATR 3배 초과 → 5분 정지
- ⑤ API 지연 5초 초과 → 즉시 청산 (**실질 구현은 `CB_PIPE_PAUSE_MS = 5_000`** — 아래 §3-B,
  아직 미해소 드리프트)

**점검**: 발동이 있었으면 사유·지속·복구까지 추적. 발동이 없었으면 "정말 조건 미달이었나"를 확인.

### ③ CORE 피처 — 호라이즌 그룹별 분리
| 그룹 | 호라이즌 | CORE 피처 | 미통과 시 |
|---|---|---|---|
| 단기 | 1m·3m·5m | CVD 다이버전스 | 등급 하락 |
| 단기 | 1m·3m·5m | VWAP 위치 | **강제 X** |
| 단기 | 1m·3m·5m | OFI 불균형 | 등급 하락 |
| 중기 | 10m·15m | VWAP 위치 | **강제 X** |
| 장기 | 30m | opt_chain_pcr | 등급 하락 |

관련 코드: `config/settings.py:HORIZON_CORE_GROUP`, `CORE_FEATURES_BY_GROUP` /
`model/multi_horizon_model.py:_CORE_MASK_EXEMPT_BY_HZ` / `strategy/entry/checklist.py`

- `macro_vix`: 2026-06-25 CORE 강등 (일봉→분봉 상수, 임계 27.5 무발동). 보조 피처로 유지.
- `macro_risk_off`: 2026-06-25 CORE 해제 (전 호라이즌 feature_names_hz 미포함).
- **30m 호라이즌은 296차에 앙상블·CoherenceGate·CascadeCoherence에서 전면 퇴역**했다
  (EOD full_cv acc=0.3052 — 랜덤 이하). 30m 관련 제안을 낼 때 이 사실을 빠뜨리지 마라.

### ④ COM 콜백 내 dynamicCall·emit 금지
콜백(`_on_receive_tr_data` 등) 내부에서 허용되는 것은 **상태 변수 저장 + `QEventLoop.quit()`** 뿐.
`dynamicCall`·`pyqtSignal.emit()` 금지 — `0xC0000409 STATUS_STACK_BUFFER_OVERRUN` 크래시.
**점검**: 로그에 `0xC0000409` / `STACK_BUFFER` / 프로세스 무응답 흔적.

### ⑤ GetRepeatCnt / GetCommData 파라미터 구분
```python
GetRepeatCnt(sTrCode, sRecordName)   # 2번째: record_name (콜백 수신값)
GetCommData(sTrCode, sRQName, ...)   # 2번째: rq_name
# meta.get("record_name") or rq_name  으로 fallback
```

### ⑥ 알파 리서치 봇 — 자동 통합 절대 금지
백테스트 자동 큐 OFF, 자동 통합 OFF. 사용자 검토 필수.

---

## 2. 한시 예외 — 일부러 꺼둔 것 (끈 상태가 정상이다)

수집기 §3 "설정 불변식" 표가 이 값들을 자동 확인한다.
**`일치`가 정상이고 `불일치`가 사건이다.** 반대로 읽지 마라.

| 상수 | 정상값 | 왜 껐나 | 언제 되돌리나 |
|---|---|---|---|
| `CB_CONSEC_STOP_LIMIT` | `9999` | CB② 3연속 손절 기준이 모의투자에서 당일 정지를 너무 자주 만들어 표본이 희소해진다 | 실투 전환 전 2~3 복원 필수. **재검토 기한 2026-08-29** (2026-08-01 결산에서 4주 연기) |
| `CB3_P4_GRADE_BLOCK_ENABLED` | `False` | 30m 퇴역 후 CB③-P4가 퇴역된 30m 정확도만으로 판정 → 상시 RESTRICTED 고착, 무관한 호라이즌의 C등급 진입까지 차단(292차 실측) | 30m 재도입 또는 CB③ 기준 호라이즌 교체 시 `True` |
| `FP_CRITICAL_GRADE_BLOCK_ENABLED` | `False` | PSI 계측 결함(균등폭 10-bin 메가빈)으로 이틀 연속 CRITICAL 고착, 신규 진입 0건 | 371차 분위수 재설계 후 **라이브 관찰 중**. 정상 구간에서 PSI가 오르내리는 것을 실측 확인한 뒤 `True` |
| `MAX_CONTRACTS` | `3` | 431차 10→3 인하. 손실이 qty≥6 9포지션 -1,609만원에 집중 | 실전 자본 확정 시 **함께 재산출** |
| `SIZING_TARGET_CAPITAL_ENABLED` | `True` | 모의 잔고(4.9억)와 실전 자본 괴리로 base_risk 왜곡 방지 | 실전 자본으로 `SIZING_TARGET_CAPITAL_KRW` 재설정 |

> ⚠ **`SIZING_TARGET_CAPITAL_ENABLED`를 `False`로 바꾸라는 말을 단독 지시로 읽지 마라.**
> 그것만 하면 2026-07-10 이전 3~10계약 체제가 되살아난다. 포지션 단위 실측으로
> **~07-10 75포지션 -881만원 / 07-14~ 58포지션 +60만원**이다.

**이 예외들은 "잊고 방치"가 가장 큰 위험이다.** 점검할 때마다 재검토 기한이 지났는지 확인하라.

---

## 3. 재인용 금지 수치 — 근거로 쓰면 안 되는 것

CLAUDE.md가 **"무효다 — 재인용 금지"** 로 명시한 것들이다. 원 출처 문서가 아직 정정되지 않은
경우가 있으니, 그쪽에서 봤다고 해서 인용하지 마라.

### 3-1. 2026-06-25 SHAP 수치 (416차 정정)
`macro_vix` / `macro_risk_off` CORE 강등 근거로 쓰인 "SHAP 기여 ≈ 0", "SHAP=0"은 무효다.
2026-06-25는 **SHAP 이름↔값 정렬 오염 구간(2026-06-17~07-10, 18거래일) 한복판**이다.
비영 중요도가 이름 배열 앞 12칸에만 실렸고 나머지 85개 피처는 조회하면 무조건 0.0이었다.

**단, 두 결정 자체는 유지된다** — 근거가 SHAP 하나가 아니었다.
`macro_vix`는 "일봉→분봉 상수"와 "임계 27.5 무발동", `macro_risk_off`는
"전 호라이즌 feature_names_hz 미포함"이 SHAP과 독립적으로 성립한다.

### 3-2. 2026-08-01 §9-3 사이징 통계 4종 (417차 정정)
`trades.quantity`는 **청산 레그별 계약수**다(진입 수량이 아니다). 이익 포지션은 TP1/TP2/TP3로
쪼개져 작은 수량 여러 행이 되고, 손실 포지션은 하드스톱 전량청산이라 큰 수량 한 행이 된다.
그래서 "계약수가 클수록 진다"가 인과 없이 만들어졌다.

| 무효가 된 원 서술 | 포지션 단위 실측 |
|---|---|
| 3계약 이상 16건 6.2% (1승 15패) -2,642만원 | 39포지션 22승 17패 (56.4%) -1,174만원 |
| 이항검정 p=5.07e-07 "우연이 아니다" | **p=0.93 — 유의성 소멸** |
| 최악 2일 제거 후에도 10전 1승 -1,236만원 | 31전 19승 (61.3%) **+45만원** |
| 카운터팩추얼 완전 단조 | **단조 아님** — 상한2 +157만원이 최대 |

**죽은 것**: "단조", "사이저가 역예측적"이라는 해석.
**살아 있는 것**: "어떤 상한이든 흑자, 상한 없음만 적자" → 상한이 필요하다.
**진짜 원인**: 방향이 아니라 **손절 준수율**. 진입수량 vs 손절폭 초과비율 rho=+0.318 (p=0.015).
1계약 29.4% vs 3계약+ 68.8%. 슬리피지 가설은 **미검증**.

원 출처 `docs/정기점검/매일점검/MW0601-20260731-점검리포트.md` §9-3은 **미정정 상태**다.
그쪽을 직접 인용하지 마라.

---

## 3-B. 문서-코드 드리프트 (2026-08-12 실측 발견)

CLAUDE.md 절대원칙 §2의 서술과 `config/settings.py` 실제 값이 어긋나 있다.
**코드가 옳고 문서가 낡았다** — 그렇더라도 "절대원칙"이 실제와 다른 상태는 위험하다.
보고서에 올려 문서 갱신을 제안하라.

| CLAUDE.md 서술 | 실제 코드 | 해석 | 상태 |
|---|---|---|---|
| CB③ "30분 정확도 **< 35%** → 당일 정지" | `CB_ACCURACY_MIN_30M = 0.28` (주석: 0.35→0.28) | 임계가 완화됐는데 절대원칙 문구는 그대로였다 | **해소 (468차)** — CLAUDE.md §2에 실값·근거 병기. 코드는 무변경 |
| CB⑤ "API 지연 5초 초과 → 즉시 청산" | `CB_API_LATENCY_LIMIT = 5.0` 은 **"레거시 — Kiwoom용, Cybos에서는 사용 안 함"**. 실질 구현은 `CB_PIPE_PAUSE_MS = 5_000` (5분 진입 정지) | 트리거 대상도(API→파이프) 동작도(즉시 청산→진입 정지) 다르다 | **미해소** — 468차 F-5 범위 밖(별건). 발견하면 계속 보고할 것 |

### v9-dev 차단 게이트 전수 (2026-08-12 실측, 27개 중 7개 False)

| 플래그 | 값 | 성격 |
|---|---|---|
| `CB3_P4_GRADE_BLOCK_ENABLED` | False | 차단 게이트 — 기록됨 |
| `FP_CRITICAL_GRADE_BLOCK_ENABLED` | False | 차단 게이트 — 기록됨 |
| `TOXICITY_SEVERE_SPREAD_BLOCK_ENABLED` | False | 차단 게이트 — 기록됨 (311차, `settings.py:4770-4781`) |
| `HEALTH_DEGRADED_BLOCK_MANUAL_ENTRY` | False | 차단 게이트 — 설계 선택(수동진입은 막지 않음) |
| `LIMIT_PIN_ENTRY_BLOCK_ENABLED` | True | 차단 게이트 — 켜짐 |
| `HURST_SOFT_BLOCK_ENABLED` | True | 소프트 차단(사이즈 0.5배) |
| `HEALTH_DEGRADED_BLOCK_AUTO_ENTRY` | True | 차단 게이트 — 켜짐 |
| `LOSS_TIER1_QTY1_ENABLED` · `LIMIT_ENTRY_FIRST_ENABLED` · `TICKUI_TRACE_ENABLED` | False | 기능 토글 (차단 아님) |

수집기는 전수 표에 전부 싣되 **이름에 `BLOCK` 이 든 것만** 적신호로 올린다
(`gate_flag_alert_pattern`). 기능 토글이 꺼진 것과 차단 장치가 꺼진 것은 무게가 다르다.

### 네 번째 비활성 차단 게이트 — 오탐이었다 (468차 종결)

CLAUDE.md 한시예외는 3건(CB②·CB③-P4·FP-CRITICAL)인데 코드에는 하나 더 있다.

```python
TOXICITY_SEVERE_SPREAD_BLOCK_ENABLED = False
TOXICITY_SEVERE_SPREAD_BLOCK_TICKS = 20.0
```

2026-08-12 초판은 "근거가 CLAUDE.md에도 DECISION_LOG에도 없다"고 적었으나 **틀렸다.**
근거는 `config/settings.py:4770-4781` 에 311차 결정으로 상세히 있다 — 합성 toxicity_score가
실측 최댓값 0.393으로 block 임계(0.78)에 도달한 적이 없어 안전장치가 사실상 부재였고,
슬리피지 역산으로 `spread_ticks>=20`(p90)을 후보 임계로 제안하되 **"실거래 표본(n=8~19)만으로는
노이즈가 커 이 컷을 검증 못함 — 섀도우 로그로 먼저 관찰 후 활성화"** 가 활성화 조건이다.

→ 468차에 `documented_disabled_flags` 에 등록해 적신호에서 뺐다(사본 2벌 + 수집기 기본값).
**교훈: "CLAUDE.md·DECISION_LOG에 없다"는 "근거가 없다"와 다르다** — 게이트 근거는
`settings.py` 정의부 주석에 있는 경우가 많다. 적신호로 올리기 전에 **정의부 앞 20줄을 먼저
읽어라.**

> 수집기 §3 하단 **차단 게이트 전수 인벤토리**가 `*_BLOCK*`/`*_ENABLED*` 불리언을 매번
> 훑는다. 손으로 적어둔 목록은 새로 생긴 것을 놓치지만, 이름 규칙으로 훑으면
> "꺼져 있는데 아무도 기록해두지 않은 게이트"가 저절로 드러난다.

### 26주 재검증 후보 (드리프트 항목)

| 상수 | 현재값 | 이력 |
|---|---|---|
| `ENTRY_HORIZON_B1` | 3.2 | 374차 1.5→3.5, 387차 3.5→3.2 |
| `ENTRY_HORIZON_B2` | 4.4 | 374차 2.5→4.0, 387차 4.0→4.4 |
| `CB_ACCURACY_MIN_30M_STRICT` | 0.42 | 0.50→0.42 완화 |
| `CB_ACCURACY_MIN_30M` | 0.28 | 0.35→0.28 완화 |

두 차례(374·387차) 연속 재보정된 값은 그 자체가 "아직 안정되지 않았다"는 신호다.
§5의 26주 WFA 재검증 목록에 편입을 검토하라.

---

## 4. 표본 부족 시 확정 결론 금지 (313차 원칙)

`min_samples=20`짜리 채널 다수가 실측 도달속도 기준 **9~16주 스케일**이다.
리포트가 `⚠구조적판정불가`로 표시하는 채널에 확정 결론을 내리지 마라.

- [28] `sizing_inversion_watch` — 431차 조치로 qty≥3 체결이 다시 발생하므로 지금부터 표본이 쌓인다
- 3계약+ 포지션 39건(시스템 진입 37건), 손실 16건 — **확정 결론 금지**

---

## 5. 주기적 재검증 항목 (26주 Walk-Forward 편입)

"실전 전환 전 1회성 검토"(실전 전환 기준 ⑤~⑧)와는 성격이 다르다. 여기는 시장 미시구조
변화에 따라 계속 드리프트하므로 **매 26주 WFA 주기마다 반복 확인**한다.

| 항목 | 현행값 | 재검증 방법 |
|---|---|---|
| Hurst 추정 파라미터 | `HURST_WINDOW_N=90`, `HURST_MAX_LAG=9` | `scripts/hurst_oos_validation.py` · `hurst_stability_check.py`를 최신 60거래일로 재실행. FalseBlock 최저 구간 확인 |
| PSI 경보 임계값 | `_PSI_WATCH=0.10` / `_PSI_ALARM=0.20` / `_PSI_CRIT=0.30` | 372차 방법론 재시도. **일자단위 상관과 손익 구간별 이상치 분해를 먼저** 확인한 뒤 결론 |
| 호라이즌 방향예측 피처셋 | — | L1(`core_feature_discovery`) → L2(`horizon_signal_tradability`) → L3(purged CV) 순차 재실행 |

---

## 6. 실전 전환 기준 (Phase 5 진입 조건) — 진행 상황

| # | 조건 | 상태 |
|---|---|---|
| ① | 모의투자 4주 통산 수익률 양수 | — |
| ② | Circuit Breaker 1회 이상 정상 작동 확인 | CB②가 9999인 동안 CB③~⑤로만 충족해야 함 |
| ③ | Walk-Forward 26주 통과 (Sharpe ≥ 1.5, MDD ≤ 15%, 승률 ≥ 53%) | — |
| ④ | 일일 수익률 변동성 안정적 | — |
| ⑤ | `CB_CONSEC_STOP_LIMIT` 9999 → 2~3 복원 | **재검토 2026-08-29** |
| ⑥ | `CB3_P4_GRADE_BLOCK_ENABLED` 재검토 | 미해제 |
| ⑦ | `FP_CRITICAL_GRADE_BLOCK_ENABLED` 재검토 | 371차 수정 후 라이브 관찰 중 |
| ⑧ | 사이징 재설계 | 조건 2(`MAX_CONTRACTS` 재검토) **431차에 충족**. 남은 것은 **조건 1 — 실전 자본 재설정뿐** |

→ 실전 첫 1개월은 최대 사이즈의 30%로 시작.
