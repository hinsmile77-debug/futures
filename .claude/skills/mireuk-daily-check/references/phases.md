# 국면별 점검 체크리스트 — 미륵이

각 항목을 **확인됨 / 확인 안 됨 / 위반** 셋 중 하나로 판정한다.
"로그에 없다"는 "안 일어났다"가 아니라 **"관측되지 않았다"** 이다 — 그 자체가 이상점 후보다.

기준 시각(KST): 08:55 매크로 → 09:00 개장 → [매분 STEP 1~9] → 15:10 강제청산 →
15:18 안전망 → 15:40 자가학습 마감 + SHAP 심사 → EOD 재학습(py310_64)

---

## A. 장전 (pre) — "오늘 거래할 자격이 되는가"

### A-0. 브랜치 확인 — 가장 먼저 ⚠

두 PC가 **서로 다른 브랜치**로 돈다. MW0601 = `v9-dev`, MW0602 = `dev`.

- [ ] `git branch --show-current` 가 이 PC의 운영 브랜치와 같은가
- [ ] 다르면 **그 자체로 P0** — 그 상태로 기동하면 다른 세대 코드가 돈다
      (2026-08-12 MW0601이 실제로 289커밋 낡은 `dev` 로 전환돼 `config/settings.py` 가
       5,476줄 짧아진 사고가 있었다)
- [ ] `origin` 대비 behind 가 몇 커밋인가

### A-1. 기동
- [ ] 프로세스 기동 시각과 PID — 중복 기동 없는가
- [ ] Python 런타임이 **3.7 32-bit (`py37_32`)** 인가 (장중 본체). Cybos Plus COM/OCX 필수 조건
- [ ] Cybos Plus 접속·로그인 성공, COM 객체 생성 정상
- [ ] `scipy 1.5.4` / `scikit-learn 1.0.2` / `joblib 1.1.1` — 32-bit DLL 충돌 회피 조합 유지
- [ ] 모델 번들 로드 성공 (`pickle protocol=4` — py310_64에서 저장한 것을 py37_32가 읽는다)
- [ ] 전일 EOD 재학습이 성공했는가 → **실패 시 모델 미교체 → CB③ HALT 유발 이력 있음**

### A-2. 08:55 매크로 수집
- [ ] 매크로 수집 완료 → 시장 레짐 판정 (`RISK_ON` / `NEUTRAL` / `RISK_OFF`)
- [ ] 실시간 구독 **사전** 시작됨 (09:00에 시작하면 첫 봉을 놓친다)
- [ ] 옵션 체인 로드 — `opt_chain_pcr`(30m CORE)의 입력이다
- [ ] 선물 분봉 TR이 **`OPT50029`** 인가 (`OPT10080` 사용 금지)

### A-3. 설정 불변식
- [ ] 수집기 §3 표의 모든 행이 `일치` 또는 의도된 `값 확인` 인가
- [ ] `불일치`가 있으면 → **누가 언제 왜 바꿨는지 `dev_memory/DECISION_LOG.md`에 근거가 있는가**
- [ ] 한시 예외의 **재검토 기한이 지났는가** (CB② 2026-08-29 등) — `references/invariants.md` §2

### A-4. 코드 상태
- [ ] 미커밋 변경이 있는가 — 실전 PC에 커밋 안 된 수정을 남기지 않는다
- [ ] 최근 커밋에 **PC명 태그** `[MW0601]`/`[MW0602]` 가 붙었는가
- [ ] 다른 PC가 올린 커밋을 pull 했는가 (멀티PC 공유 repo)

---

## B. 장중 (intra) — "매분 9단계가 설계대로 도는가"

### B-0. 매분 루프 생존
- [ ] 수집기 §6 **매분 루프 커버리지** — 09:00~15:10 중 몇 분이 기록됐는가
- [ ] 연속 3분 이상 기록 없는 구간 — 루프가 멈춘 것인가, 로깅이 없는 것인가
- [ ] 프로세스 무응답·재기동 흔적

### B-1. STEP 1 과거 예측 검증
- [ ] T-1·T-3·T-5·T-10·T-15 채점이 매분 돌았는가
- [ ] **T-30은 퇴역 대상**이다 — 30m이 앙상블에서 빠졌으므로 여기 남아 있다면 그 자체가 점검거리
- [ ] 채점 결과가 accuracy_buf에 누적되는가 (CB③ 입력)

### B-2. STEP 2~3 자가학습
- [ ] SGD 온라인 업데이트가 매분 도는가
- [ ] GBM 배치 재학습이 30분마다 도는가
- [ ] 재학습 중 메모리 이슈(`MemoryError`, OOM) 없는가 — py37_32의 고질 문제

### B-3. STEP 4~5 피처·예측
- [ ] 피처 생성(수급·옵션·기술·매크로) 결측/NaN 비율
- [ ] 멀티 호라이즌 예측 1·3·5·10·15분 발행 (30m 제외 상태 확인)
- [ ] CORE 피처가 호라이즌 그룹별로 올바르게 적용되는가 (절대원칙 3)
- [ ] AutoMask 면제(`_CORE_MASK_EXEMPT_BY_HZ`)가 의도대로 동작하는가

### B-4. STEP 6~7 진입 판단·실행

**수집기 §5 거래일 요약이 이 절의 대부분을 자동으로 낸다.** 표를 읽고 판단만 하면 된다.

- [ ] 등급 분포. `A급(원시C)` 표기는 **원시 C가 A로 상향된 것** — 상향 경로가 정당한지 본다
- [ ] `[진입체크]` 의 체크리스트에서 **진입했는데 미통과인 항목**
      (2026-08-12: `chas`×4 `cvd`×3 `prev`×2 `ofi`×1)
      → **`vwap` 미통과 상태로 진입한 건이 있으면 P0** (절대원칙 3, 강제 X)
- [ ] **진입 0건이거나 적으면 `[차단]` 누적으로 분해하라.** 2026-08-12 실측 115건:
      `2_confidence`×54 · `3_vwap`×41 · `6_foreign`×22 · `5_ofi`×20 · `4_cvd`×19 ·
      `7_prev_bar`×18 · `10_chase`×10 · `11_countertrend`×2
      → 하나가 압도적이면 그 게이트의 임계를 의심한다 (316차 HurstGate 63%가 그렇게 발견됨)
- [ ] 체크리스트 외 차단 사유도 본다
      — `자동진입 Degraded 최소신뢰도 62.0% 미달`(HEALTH_DEGRADED 경로)
      / `14:50 이후 — 신규 진입 금지 구간 (345차)`
      / `OPEN_VOLATILE 시가이격 과다`
      / `청산 후 쿨다운`
- [ ] **`[Sizer]` 출력 vs 실제 진입 계약수** — 2026-08-12: 사이저 3계약×6·2계약×3 →
      실제 1계약×1·2계약×4·3계약×1. 배수 조합이 `conf=0.6 regime=0.8 safe=1.00` 로 고정이면
      신뢰도·레짐 배수가 실질적으로 상수처럼 작동하는 것은 아닌지 본다
- [ ] `MAX_CONTRACTS=3` 상한에 실제로 걸린 건이 있는가 (431차 이후 qty≥3 표본 축적 중)
- [ ] 확률 판단 기준 준수: 50~55 관망 / 55~60 관망 / 60~70 진입 고려 / 70+ 적극
      — 단 `[진입체크]` 의 `conf=41.4%` 같은 값은 **모델 확률이 아니라 신뢰도 지표**다. 혼동 말 것

### B-4B. 성능·모델 열화 (미륵이 특유)

레벨이 WARNING이라 눈에 안 띄지만 실질 위험이다.

- [ ] **`메인 스레드 블로킹`** — 2026-08-12 42건, 최대 8172ms, **5초 초과 7건**.
      `CB_PIPE_PAUSE_MS = 5_000` 이 CB⑤의 실질 구현이므로 초과분은 발동 대상이다.
      계측만 되고 지나갔는지 확인하라
- [ ] **`[Brier] 과신 경고 | 이동평균=0.352 > 0.35`** — `CB_BRIER_WARN` 임계 접촉
- [ ] **`[SHAP] 슬로우 감지 984ms (임계 900ms) — 다음 5분 건너뜀`** — SHAP 심사 표본 손실
- [ ] **`축퇴 감지`·`WeightCollapse`·`ConstOut`·`ConfFloorGuard`** — 모델이 상수 출력에
      가까워지는 신호. `[Calibration] 축퇴 감지 — span=0.00162 auc=0.518` 형태
- [ ] **`degraded=ON`** — HealthPolicy Degraded 진입 (2026-08-12는 0건)
- [ ] `[Health] level=… latency=…ms quality=… cache_age=…s exceptions_10m=…` 추이

### B-5. STEP 8 청산 트리거 (5단계)
- [ ] 하드스톱 → 손절계단화 → TP1~TP3 → 15:10 강제청산 → 15:18 안전망 순서 유지
- [ ] **손절 준수율** — 실현손실 ÷ 의도손절폭(ATR×1.5). 417차 재분해에서 유일하게 유의했던 축이다
- [ ] 손절폭 초과 건의 진입수량 분포

### B-6. Circuit Breaker
- [ ] CB①~⑤ 발동 여부. 발동했으면 사유·지속·복구
- [ ] CB② 는 `9999`로 사실상 비활성 — **발동 안 한 것이 정상**이다
- [ ] CB③-P4 는 차단 비활성이나 `accuracy_buf` 누적·`acc30m_stage` 추적은 유지돼야 한다
- [ ] FP-CRITICAL PSI 값 추이 — 차단은 비활성이나 계산·로그는 유지 (셰도 모니터링)

### B-7. 장중 금지
- [ ] 장중 코드 배포·재기동 흔적 없는가
- [ ] 알파 리서치 봇의 자동 통합 흔적 없는가 (절대원칙 6)

---

## C. 장후 (post) — "오늘 하루가 절대원칙을 지켰는가"

### C-1. 15:10 강제 청산 (절대원칙 1 — 최우선)
- [ ] 15:10 강제청산 실행 로그가 있는가
- [ ] 청산 후 **잔여 포지션 0** 확인 (15:18 안전망에서 재확인)
- [ ] 오버나이트 포지션이 남았다면 **무조건 P0**
- [ ] 포지션이 애초에 없었다면 그 사실을 명시하라 — "청산 로그 없음"과 "청산할 것이 없었음"은 다르다

### C-2. 15:40 자가학습 마감 + SHAP
- [ ] 일일 마감 처리 완료
- [ ] SHAP 피처 심사 실행 및 결과
- [ ] **SHAP 이름↔값 정렬이 올바른가** — 2026-06-17~07-10에 18거래일간 오염된 이력이 있다
- [ ] 심사 결과로 피처셋을 바꿀 때는 `references/invariants.md` §3-1을 먼저 읽어라

### C-3. EOD 재학습 + P8
- [ ] `EOD_RETRAIN.bat` / `scripts/eod_retrain.py` 가 **`py310_64`** 로 돌았는가
- [ ] 로그에 `Python 3.10.20 64-bit` 가 찍혀도 **정상**이다 — 이상 환경이 아니다
- [ ] `pickle protocol=4` 저장 확인 (py37_32 로드 호환)
- [ ] OOM 없이 완주했는가 — 실패 시 모델 미교체 → 다음날 CB③ HALT 위험
- [ ] 226차 이력: py37_32에서 numpy float32 배열 OOM 반복 → 환경 분리한 이유
- [ ] **P8 스케일러 재적합**(`retrain_eod.py:p8_scaler_refit()`)이 끝났는가 —
      `data/eod_retrain_done_{d}.txt` + `data/session_state.json` 의
      `p8_last_success_date`·`eod_retrain_ok_date` 둘 다 오늘 날짜인가
- [ ] P8이 빠졌으면 **다음날 시초 z-score 불안정**을 예고하라. 수동 복구는
      `scripts/catch_up_eod.py`(STEP 2). 197차에 `daily_close()` 밖으로 옮긴 이유도 함께 기억할 것

### C-4. STEP 9 예측 DB
- [ ] 당일 예측이 DB에 저장됐는가 (`data/db/predictions.db`)
- [ ] 레이블링 완료 건수 — SGD 온라인학습·SHAP 심사 표본의 원천이다
- [ ] `ensemble_decisions` 당일 행수와 `entry_executed=1` 건수 — **차단 사유의 1차 자료**
- [ ] ⚠ `predictions` 에 `30m` 행이 계속 쌓이는 것은 **정상**이다(퇴역은 앙상블 편입에서
      뺀 것). 다만 그 행을 근거로 삼지 마라
- [ ] 스키마·조회 명령은 `evidence_map.md` §8

### C-5. 검증 캠페인 (금요일이면 특히)
- [ ] 주간 리포트가 `docs/정기점검/금요일점검/<PC명>/` 에 **날짜본**으로 생성됐는가
- [ ] 고정 파일명으로 덮어쓰지 않았는가 (2026-07-31분이 08-01 재생성에 덮인 사고 있음)
- [ ] FIFO 보관(`VALIDATION_REPORT_KEEP_WEEKS=4`)이 자동 생성물만 지웠는가 —
      수동 스냅샷(`..._20260801_pre405.md`)과 주간회의 검토보고는 대상이 아니다
- [ ] 호스트명에서 `MW####`를 못 뽑아 `UNKNOWN/` 폴더가 생기지 않았는가 (stderr 경고 확인)
- [ ] **리포트의 FAIL/권고 중 이미 코드에 반영된 것이 있는가** — 함정 ①

### C-6. 진입 승패 사후검증 (제4·5부) — 절차는 `references/postmortem.md`

이 절은 **관문만** 둔다. 실제 절차는 중복해 적지 않는다.

- [ ] EOD·P8 완료(C-3)를 확인한 뒤 시작했는가
- [ ] 3원 대사가 **포지션 단위**로 일치하는가 — 로그 `[Position] 진입` /
      `ensemble_decisions.entry_executed=1` / `COUNT(DISTINCT trades.entry_ts)`
      (`trades` 행수는 청산 레그라 더 많다. 그건 불일치가 아니다)
- [ ] 승패를 `exit_reason` 만으로 세지 않았는가 — `하드스톱`에 **TP1 보호 이익 청산이 섞여 있다**
      (`tp1_reached` / `[TP1보호]` 태그로 가른다. `evidence_map.md` §8-4)
- [ ] 요인 태그 A~F에 로그 인용과 시각이 붙었는가. **E(손절 준수율)를 빠뜨리지 않았는가**
- [ ] 313차 다섯 갈래를 다 적용했는가 (`postmortem.md` §4-2)

### C-7. 기록 의무
- [ ] `dev_memory/DECISION_LOG.md` · `NEXT_TODO.md` 갱신
- [ ] 세션 헤더에 PC명 병기 (`## 2026-08-12 (MW0601 46X차 — …)`)
- [ ] 세션 차수를 원격(git) 기준으로 맞췄는가
- [ ] 당일 커밋 전부에 `[MW####]` 접두어

---

## D. 국면 공통 — 놓치기 쉬운 것

- **건수 0은 두 가지다** — 진짜 없었거나, 계측이 없거나. 로그로 구분한다.
- **"눌려 잠든" 채널을 삭제하지 마라.** 표본이 안 쌓이는 것과 쓸모없는 것은 다르다.
- **판정과 결정은 별개다.** 리포트 FAIL이 반복되는 것은 미조치의 증거가 아니다.
- **재인용 금지 수치**를 근거로 쓰지 마라 (`invariants.md` §3).
- **30m 호라이즌은 퇴역했다.** 30m 기반 지표를 근거로 삼지 마라.
- **표본 부족 채널에 확정 결론 금지** (313차). 요약 한 줄이 아니라 다섯 갈래다 —
  일자단위 판정 / overlap 보정 / 이상치 분해 / 사전등록(기준 불변) / 부호 일관성.
  `invariants.md` §4, 적용은 `postmortem.md` §4-2.
- **다른 PC의 관찰과 섞이지 않게** 하라. PC명 태그가 유일한 구분자다.
- 전일 다이제스트가 있으면 **델타**를 본다. 절대값보다 변화가 정보량이 크다.
